# Stress-test comparators (E-T1 FedProx, E-T2 personalization, E-T3
# B-FedStatsBenign) on Regime A, outside the causal ladder
# (docs/protocol/identity_lock.md #8). FedProx and personalization require
# their own training runs.
name: stress_tests
status: contract_only
regimes: [A]
experiment_ids: [E-T1, E-T2, E-T3]
training_enabled: true
requires_score_reuse: false
allow_training_override: false
