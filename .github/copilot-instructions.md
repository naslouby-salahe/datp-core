---
applyTo: "**"
---

# Copilot instructions — DATP Core

Read `AGENTS.md` first; it is the single source of governance and is loaded automatically.
For a task, select the matching command in `ai/commands/` and use `ai/skills/` as checks.
Do not duplicate policy here.

Every response ends with the final-report format defined in `AGENTS.md`.
