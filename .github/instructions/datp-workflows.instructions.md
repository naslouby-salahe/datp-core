Read `AGENTS.md` first.
Read `ai/README.md`.
Use `ai/` as the source of truth.
Read `ai/workflows/`.

Select exactly one workflow before execution.
Follow workflow gates in order.
Do not mix workflows silently.
Report skipped gates with reasons.
Do not duplicate full workflow content here.
No backward compatibility by default.
Do not perform release, tag, or versioning work.
Final response must follow `AGENTS.md`.
