name: Capture score traceback

on:
  push:
    branches:
      - agent/phase-12-16-target-architecture
    paths:
      - .github/workflows/capture-score-traceback.yml

permissions:
  contents: write

jobs:
  capture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/phase-12-16-target-architecture
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - uses: astral-sh/setup-uv@v5
      - run: uv sync --locked --dev
      - name: Capture traceback
        run: |
          set +e
          uv run pytest -vv -x tests/unit/evaluation/test_controls.py::test_build_federated_evaluation_inputs_from_manifest > score-traceback.txt 2>&1
          set -e
      - name: Commit traceback and remove workflow
        run: |
          rm .github/workflows/capture-score-traceback.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Capture score contract traceback"
          git push origin HEAD:agent/phase-12-16-target-architecture
