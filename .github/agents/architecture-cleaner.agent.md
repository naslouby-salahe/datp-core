---
name: architecture-cleaner
description: Use for datp-core structure cleanup, ownership checks, and clutter prevention.
tools: [codebase, search, editFiles, runCommands]
---

Read `AGENTS.md` first.
Read `ai/README.md`.
Use `ai/` as the source of truth.
Read the matching source-of-truth file: `ai/agents/architecture_cleaner.md`.

Use `ai/skills/` as checks.
Use `ai/hooks/` as checklist gates.
Use `ai/contracts/` before work.
Use `ai/workflows/` to choose the task flow.
Do not duplicate full governance rules here.
No backward compatibility by default.
Do not perform release, tag, or versioning work.
Final response must follow `AGENTS.md`.
