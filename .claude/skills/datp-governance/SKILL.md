---
name: datp-governance
description: Use for datp-core tasks requiring DATP AI governance, strict scope, no backward compatibility, clean structure, typed protocol state, hook discipline, contracts, workflows, and uniform final reports.
---

Read `AGENTS.md` first.
Read `ai/README.md`.
Use `ai/` as the source of truth.

Select the relevant workflow from `ai/workflows/`.
Use the relevant contract from `ai/contracts/`.
Use agents from `ai/agents/`.
Use skills from `ai/skills/`.
Use hooks from `ai/hooks/` as checklist gates.

Do not create compatibility layers.
Do not create temp or audit clutter.
Do not perform release, tag, or versioning work.
Final response must follow `AGENTS.md`.
