Read `AGENTS.md`, then follow `ai/commands/audit.md`. This workflow is read-only and never edits or
creates files. `ai/` is the source of truth; do not duplicate policy here. End with the `AGENTS.md`
final-report format.
