---
name: datp-governance
description: Use for all datp-core repository tasks requiring DATP AI governance, strict scope, no backward compatibility, clean structure, typed protocol state, hook discipline, contracts, workflows, and uniform final reports.
---

Read `AGENTS.md` first.
Read `ai/README.md`.
Use `ai/` as the source of truth.

Select the workflow from `ai/workflows/`.
Use contracts from `ai/contracts/`.
Use agents from `ai/agents/`.
Use skills from `ai/skills/`.
Use hooks from `ai/hooks/` as checklist gates.

Never create shims, redirects, aliases, compatibility layers, temp files, audit reports, release work, tag work, or versioning work.
Final response must follow `AGENTS.md`.
