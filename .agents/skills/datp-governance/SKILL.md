---
name: datp-governance
description: DATP Core AI governance — scope, no backward compatibility, validated config, typed protocol state, quality gates, and uniform final reports. Use for any datp-core repository task.
---

Read `AGENTS.md` first; it holds the non-negotiable rules.
Select the matching task workflow in `ai/commands/` and apply `ai/skills/` as checks.
`ai/` is the source of truth — do not restate its policy here.
End every response with the `AGENTS.md` final-report format.
