---
name: datp-governance
description: Use for datp-core governance, contracts, workflows, checks, and final reports.
---

Read `AGENTS.md` first.
Read `ai/README.md`.
Use `ai/` as the source of truth.

Select the workflow from `ai/workflows/`.
Use the contract from `ai/contracts/`.
Use `ai/agents/` for role behavior.
Use `ai/skills/` for checks.
Use `ai/hooks/` as checklist gates.

No backward compatibility by default.
No release, tag, or versioning work.
Final response must follow `AGENTS.md`.
