# P1-T029 — Implement experiment identity/profile/cell aggregates and closed profiles

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T029` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T029
2. **Title.** Implement experiment identity/profile/cell aggregates and closed profiles
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** domain
7. **Owner.** experiment-engineer
8. **Supporting agents.** datp-protocol-guardian
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** E-C1
12. **Architecture contracts/types owned.** `ExperimentIdentity`, `ExperimentSpec`, `ExperimentProfileSpec`, `CentralizedModelComparatorProfileSpec`, `ExperimentCell`, `RegimeCompatibilitySpec`, `SweepSpec`, `ConfirmatoryExperimentProfileSpec` (E-C1 locked).
13. **Objective.** Implement `ExperimentIdentity`, `ExperimentSpec`, `ExperimentProfileSpec`, `CentralizedModelComparatorProfileSpec`, `ExperimentCell`, `RegimeCompatibilitySpec`, `SweepSpec`, and `ConfirmatoryExperimentProfileSpec` (E-C1 locked), together with the profile catalogue holding the roadmap's fixed grids (E-S2 q ∈ {0.90, 0.95, 0.975, 0.99}; E-S3 α ∈ {0.1, 0.3, 0.5, 1.0, 10.0, IID}; E-V1 n ∈ {50, 100, 250, 500, 1000, 5000}; E-V2 λ ∈ {0.00, 0.25, 0.50, 0.75, 1.00}; E-V3 α = 0.05; E-Q5 k ∈ {2.0, 2.5, 3.0}) and every locked absorption/temporal-recovery/Regime-D-viability/suppression/sign-requirement gate.
14. **Why this ticket exists.** Named experiments must be executable only through closed profiles, never through a caller-supplied scientific protocol — this is the structural guarantee that prevents an implementation shortcut from silently redefining what "E-C1" means. `ConfirmatoryExperimentProfileSpec` is a further-closed specialization admitting only Regime A, N-BaIoT, nine physical-device clients, FedAvg, E=1, full participation, B1/B2 only, `CV_FPR` only, exactly ten paired seeds, Δ = B1 − B2, and the positive-direction 95% BCa procedure — it has no B0/B3/B4/variant/comparator arm and no alternate metric, so the single most safety-critical claim in the entire project (the Tier 1 confirmatory endpoint) cannot be constructed in any shape other than its locked one.
15. **Authority references.** Architecture §8.3 (`ExperimentProfileSpec`, `ConfirmatoryExperimentProfileSpec`, `RegimeCompatibilitySpec`), §8.4 (aggregate class diagram), §29.1 (invalid-state guardrails). Roadmap §9 (experiment matrix), §9.3 (fixed grids and pre-specified bands/gates).
16. **Dependencies.** P1-T006, P1-T007, P1-T028.
17. **Blocks.** P1-T031, P4-T001.
18. **Downstream consumers.** P1-T031 (Pydantic boundary schemas) and P4-T001 (outside this extracted phase) consume these aggregates directly; the planner (P1-T038) expands only cells authorized by a closed profile constructed here.
19. **Preconditions.** P1-T006, P1-T007, P1-T028 are `DONE`.
20. **Inputs.** Architecture §8.3, §8.4, §29.1; Roadmap §9, §9.3.
21. **Allowed scope.** `domain/experiments/identities.py`, `specifications.py`, `claims.py`, `feasibility.py`.
22. **Forbidden actions.** No caller-supplied scientific protocol accepted under a named experiment id; no non-confirmatory arm representable on `ConfirmatoryExperimentProfileSpec`; no path for B0 through the FedAvg-profile route (`CentralizedModelComparatorProfileSpec` is the only route for B0); no invented seed value anywhere — `authorized_seed_plan` must reference the resolved authoritative seed specification, never a hardcoded literal.
23. **Required configuration or NONE.** Profile-catalogue values are taken from the roadmap only; a value the roadmap does not state remains an explicit, recorded readiness blocker rather than an invented default.
24. **Implementation responsibilities.** Implement every named profile as a frozen dataclass; enforce the role/tier invariant (built in P1-T006) at `ExperimentIdentity` construction; implement `RegimeCompatibilitySpec` as a closed mapping (Regime A → N-BaIoT/nine `NATURAL_DEVICE` clients; B-a → CICIoT2023 file-level pseudo-clients, boundary-only; C → N-BaIoT/20 `DIRICHLET_SYNTHETIC` clients; D → Edge-IIoTset with the feasibility-approved partition; D-temporal → the same D partition plus genuine timestamps); represent Regime B-a as `ArchitectureCatalogueId("B_A_APPLICABILITY_BOUNDARY")`, never as a numbered `ExperimentId`.
25. **Required unit tests.** `ConfirmatoryExperimentProfileSpec` rejects any attempt to add a B0/B3/B4/variant/comparator arm or an alternate primary metric; an unauthorized dataset/regime/policy/grid combination is rejected at construction; B0 is constructible only through `CentralizedModelComparatorProfileSpec`, never through the FedAvg `ExperimentProfileSpec` route.
26. **Required property tests.** Sweep expansion (via `SweepSpec`) produces only cells present in the profile's `authorized_protocols` — no expansion can synthesize an unauthorized combination.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE beyond the baseline framework-confinement check for `domain/experiments/`.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; property lane; Pyright strict.
33. **Acceptance criteria.** [ ] Named cells are constructible only from closed profiles. [ ] `ConfirmatoryExperimentProfileSpec` admits E-C1's confirmatory shape only. [ ] Every roadmap-fixed grid is present verbatim; any absent required value is a recorded blocker, never a guessed default. [ ] No seed value is invented anywhere in this ticket.
34. **Completion evidence.** Changed files: `domain/experiments/identities.py`, `specifications.py`, `claims.py`, `feasibility.py`, and their tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** `DomainValidationError` or a recorded readiness blocker on an unauthorized cell. Stop and raise a blocker if a required grid or band value the roadmap should state is genuinely absent — never invent a value to fill the gap.
36. **Stop conditions.** Stop if a required grid/band value is absent from the roadmap; carry as a readiness blocker.
37. **Deliverables.** Experiment identity/profile/cell aggregates and the closed profile catalogue.
38. **Final review checklist.** [ ] No unauthorized experiment cell is representable by any code path. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

### B3. Architecture boundary audit

Check the complete project for any path constructing E-C1 (or any named experiment) from a caller-supplied protocol rather than its closed profile, dependency-direction violations, framework leakage, and incorrect type placement. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw grid value (a bare `[0.90, 0.95, 0.975, 0.99]` list) duplicated somewhere outside the profile catalogue instead of referencing it, `Any`/unjustified `cast`, and a hardcoded seed literal anywhere in this ticket's scope.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25) and property lane (line 26), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Contract/architecture-specific/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; these four modules must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

Check specifically that `authorized_seed_plan` never hardcodes a seed value and always references the resolved authoritative seed specification; confirm no seed-list position is ever used as an implicit identity anywhere in this ticket's scope, per the master log's Section A warning against exactly that pattern.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm every fixed grid (E-S2, E-S3, E-V1, E-V2, E-V3, E-Q5) matches Roadmap §9.3 exactly, member-for-member; confirm `ConfirmatoryExperimentProfileSpec` admits E-C1's exact locked shape (Regime A, B1 vs B2, CV(FPR), ten seeds, positive-direction BCa) per Roadmap §3/§5.1, with no additional arm.

**Audit 2 — Architecture fidelity.** Confirm `RegimeCompatibilitySpec` matches Architecture §8.3's closed mapping exactly, including B-a's `ArchitectureCatalogueId` representation and its boundary-only reporting role.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a future contributor add a "diagnostic" B4 arm to `ConfirmatoryExperimentProfileSpec` for convenience during debugging, and would a test catch it? Does the unauthorized-combination rejection test cover every dataset/regime/policy axis independently, or only a single combined example? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.
