# P1-T015 — Implement immutable typed collections and the object-dict prohibition

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T015` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T015
2. **Title.** Implement immutable typed collections and the object-dict prohibition
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** domain
7. **Owner.** implementation-engineer
8. **Supporting agents.** naming-auditor
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `ClientRoster`, `ClientCalibrationScoreMap`, `ClientTestScoreMap`, `ClientTemporalScoreMap`, `ClientEvaluationMap`, `ThresholdAssignmentSet`, `SeedTuple`/`SeedRoleTuple`/`SeedMap`/`ConfirmatorySeedCohort`, `EnumMap`, `MetricMap`, `ClientMap`, `ArtifactReferenceCollection`, `StageDependencyCollection`.
13. **Objective.** Implement the immutable typed collections of Architecture §10 listed in field 12, each validating its own invariants at construction, and establish the repository-wide prohibition on object-shaped dictionaries in domain and application contracts.
14. **Why this ticket exists.** A raw `dict[ClientId, float]` masquerading as a client score map has no name, no invariant enforcement (nothing stops a duplicate or missing client), and no type-level guarantee that two collections share the same roster. `ClientRoster`-keyed collections replace every such raw mapping with a named, validated type that enforces roster-equality and ordering-stability invariants at construction — this is the collection-level counterpart to P1-T011's value-object work and directly enables P1-T023's atomic score-artifact aggregates.
15. **Authority references.** Architecture §10 (immutable typed collections), §7.4 (immutable mapping fields), §23 (rejection of object-shaped dictionaries).
16. **Dependencies.** P1-T011, P1-T012.
17. **Blocks.** P1-T023.
18. **Downstream consumers.** P1-T023 (scoring and split-scoped score-artifact specifications) consumes these collections directly as carriers (never themselves identity-bearing).
19. **Preconditions.** P1-T011, P1-T012 are `DONE`.
20. **Inputs.** Architecture §10, §7.4, §23.
21. **Allowed scope.** Collection modules co-located with their capability subpackage per Architecture §10 (no centralized `collections.py`).
22. **Forbidden actions.** No `dict[str, Any]`/`Mapping[str, object]` anywhere in a domain or application contract; no junk-drawer `collections.py` module holding unrelated collection types; no foreign-key-shaped field (an ID referencing a collection member by position rather than by validated key).
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Implement each collection as a frozen type storing an immutable snapshot (a `types.MappingProxyType` over a copied mapping, or a frozen tuple of items) per Architecture §7.4; validate roster-equality and lineage-role invariants at construction; keep `SeedTuple` and `SeedRoleTuple` distinct types (never interchangeable, since one is positional and the other is role-keyed).
25. **Required unit tests.** Object-shaped-dictionary absence in every domain/application contract signature (a repository-wide scan test, not merely a check on this ticket's own new code); duplicate-client rejection at construction for every `Client*Map`/`ClientRoster`; `SeedTuple` and `SeedRoleTuple` are not interchangeable.
26. **Required property tests.** Roster-key equality across paired collections (e.g. a `ClientCalibrationScoreMap` and a `ClientEvaluationMap` built from the same roster compare rosters equal); ordering stability under repeated construction; duplicate-key rejection under Hypothesis-generated inputs.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** No `dict[str, Any]` appears anywhere in a domain or application contract signature (a static/architecture-level scan, run repository-wide).
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Property lane; unit lane; architecture lane; Pyright strict.
33. **Acceptance criteria.** [ ] Every Architecture §10 collection is implemented with its invariants enforced at construction. [ ] No object-shaped dictionary exists in any domain/application contract. [ ] `SeedTuple`/`SeedRoleTuple` remain distinct types.
34. **Completion evidence.** Changed files: the collection modules and their tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** `DomainValidationError` on an invariant breach (duplicate client, mismatched roster, etc.); no other failure is expected in this ticket's own scope.
36. **Stop conditions.** None.
37. **Deliverables.** Typed collection modules.
38. **Final review checklist.** [ ] No junk-drawer collections module. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files — this ticket's own repository-wide object-dict scan (line 25/28) is itself a strong candidate for B2's "not only new/modified files" requirement. No defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for object-shaped dictionaries in domain/application contracts (this ticket's own primary concern, verified repository-wide, not diff-only), cycles/side effects, incorrect type placement, and a centralized junk-drawer collections module. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

This ticket **is** the primary raw-dictionary audit mechanism for the domain/application layers: scan the complete project for `dict[str, Any]`, `Dict[str, Any]`, untyped `dict`, arbitrary nested dictionaries, `Any`, unexplained `cast`, tuple-based pseudo-objects, and primitive obsession in place of the collections defined here. Every hit outside a narrow, immediately-converted configuration boundary must be replaced with one of this ticket's typed collections or rejected as a design defect.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the property lane (line 26), unit lane (line 25), and architecture lane (line 28), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Contract/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; every collection module must pass strict typing with no unexplained `Any` — this is directly load-bearing for this ticket's own object-dict prohibition.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

Check specifically for ordering instability in any collection (a roster or map whose iteration order is not reproducible across runs would silently affect reporting/fingerprint order). Confirm no collection here participates in a fingerprint directly (that remains P1-T012/P1-T013's scope) without a documented, stable ordering.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — this ticket introduces structural collection types with no direct scientific claim`. Positive finding required: confirm no collection here allows a benign calibration roster and a test roster to be silently merged or compared as if interchangeable, which would risk conflating calibration and evaluation populations (Roadmap §2's benign-only calibration rule).

**Audit 2 — Architecture fidelity.** Confirm every Architecture §10 collection exists with its stated invariant enforced, and confirm the repository-wide object-dict scan (this ticket's own B4) actually ran against the complete `src/` tree, not a sample.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a future contributor add a new `dict[str, float]` parameter to a domain function without this ticket's scan catching it, because the scan only ran once at ticket-completion time rather than being wired into a repeatable lint rule? Is the roster-equality property test comparing actual key sets, or just lengths? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is a specialized contributor to the raw-dictionary/object-dict prohibition that `P0-T023` canonically owns as a repository-wide hook; this ticket supplies the typed-collection vocabulary that hook checks callers against. Otherwise not a cross-cutting owner; must remain compliant with `P0-T008`, `P0-T022`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.
