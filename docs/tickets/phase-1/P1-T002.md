# P1-T002 — Implement model/training/checkpoint/score domain vocabulary

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.

Standalone implementation specification. This file is understandable without reading `docs/MASTER_TICKET_LOG.md`; where this file and the master log ever disagree in wording, this file governs execution and the master log's Section G/H rows for `P1-T002` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T002
2. **Title.** Implement model/training/checkpoint/score domain vocabulary
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** domain
7. **Owner.** federated-learning-engineer
8. **Supporting agents.** —
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `ActivationFunction`, `AggregationStrategy` (`FEDAVG`, `FEDPROX`), `ModelPersonalizationStrategy` (`NONE`, `DITTO`, `FEDREP_AE`, `FEDPER_AE`), `ParticipationStrategy` (`FULL` only), `OptimizerType`, `LrSchedulerType`, `PrecisionMode`, `DeterminismLevel`, `CheckpointKind`, `CheckpointSelectionStrategy` (`REGIME_A_GLOBAL_PRIMARY` only), `QuantileEstimatorType`.
13. **Objective.** Implement the `domain/learning/` enums listed in field 12 as `StrEnum`s with UPPER_SNAKE members and stable snake_case serialized values, co-located across `models.py`, `training.py`, `checkpoints.py`, `scores.py` per the Architecture §5.1 module map.
14. **Why this ticket exists.** This vocabulary encodes several locked scientific rules at the type level before any specification can be built on top of it: FedAvg is the core training baseline and FedProx exists only as a stress-test aggregation strategy (never a ladder member); `ParticipationStrategy` has exactly one member (`FULL`) because partial participation is future work with no executable path; `CheckpointSelectionStrategy` has exactly one member (`REGIME_A_GLOBAL_PRIMARY`) because per-regime or test-driven checkpoint selection is forbidden; `ModelPersonalizationStrategy` encodes the roadmap's naming lock that `FedRep-AE`/`FedPer-AE` must never be mislabeled `Ditto` unless the true Ditto algorithm is implemented; `QuantileEstimatorType` has only exact estimators, never an approximate one.
15. **Authority references.** Architecture §6.1 (scientific vocabulary table), §6.2 (model/preprocessing/estimation vocabulary), §6.3 (`SeedRole` is explicitly excluded from this ticket — it belongs to P1-T009), §29.4 (invalid-state guardrails for participation/selection/personalization). Roadmap §2 (FedAvg core baseline, threshold-scope-only causal variable), §4 (threshold-policy and comparator nomenclature — `Ditto`/`FedRep-AE`/`FedPer-AE` naming lock), SB-24 (never call the personalization fallback "Ditto" unless the true algorithm is implemented).
16. **Dependencies.** P0-T026.
17. **Blocks.** P1-T021, P1-T023.
18. **Downstream consumers.** P1-T021 (model/federation/training/batch specifications) and P1-T023 (scoring and split-scoped score-artifact specifications) build directly on this vocabulary; every training/scoring-aware specification and configuration schema in later phases depends on it transitively.
19. **Preconditions.** P0-T026 is `DONE`.
20. **Inputs.** Architecture §6.1, §6.2, §6.3, §29.4; Roadmap §2, §4, SB-24.
21. **Allowed scope.** `domain/learning/models.py`, `training.py`, `checkpoints.py`, `scores.py` — enum definitions only; no specification dataclass (owned by P1-T021/P1-T022/P1-T023).
22. **Forbidden actions.** No partial-participation enum member; no per-regime or test-driven checkpoint-selection member; no approximate-quantile member; no member implying BatchNorm or any other architecture change to the fixed encoder; naming-lock violation (labeling `FedRep-AE`/`FedPer-AE` as `Ditto`) is forbidden; no specification dataclass added in this ticket's scope.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Define each enum as a `StrEnum`; ensure `CheckpointSelectionStrategy` has exactly one member and `ParticipationStrategy` has exactly `FULL`; ensure `ModelPersonalizationStrategy` includes `NONE`, `DITTO`, `FEDREP_AE`, `FEDPER_AE` with no alias collapsing the latter two into the first.
25. **Required unit tests.** Membership and serialized-value stability for each enum; an explicit test that `CheckpointSelectionStrategy` has exactly one member and `ParticipationStrategy` has exactly one member (`FULL`); an explicit test that `ModelPersonalizationStrategy.FEDREP_AE`/`FEDPER_AE` are distinct members from `DITTO` (naming-lock regression test).
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** Framework confinement: `domain/learning/` imports no framework (PyTorch, Flower, scikit-learn, SciPy, Pydantic) and no `infrastructure`/`config`/`application` module.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Pyright strict over `domain/learning/`; the architecture lane; the unit-test lane for `domain/learning/`.
33. **Acceptance criteria.** [ ] One checkpoint-selection member. [ ] Participation is `FULL` only. [ ] All quantile estimators are exact. [ ] The personalization naming lock is enforced by a passing regression test. [ ] No framework import in `domain/learning/`.
34. **Completion evidence.** Changed files: `domain/learning/models.py`, `training.py`, `checkpoints.py`, `scores.py`, and their unit tests. Commands run and actual results. Cleanup confirmation.
35. **Failure and blocker behavior.** A genuinely ambiguous enum member (one whose exact scope is not settled by Architecture §6.1–§6.3 or Roadmap §2/§4) is raised as a design-decision blocker rather than guessed.
36. **Stop conditions.** Stop if the roadmap's model-personalization naming lock (SB-24) cannot be unambiguously encoded as distinct enum members.
37. **Deliverables.** `domain/learning/` enum modules and their unit tests.
38. **Final review checklist.** [ ] No partial participation. [ ] No approximate quantile. [ ] Naming lock enforced. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap before starting this ticket.
- [ ] Read the complete architecture before starting this ticket.
- [ ] Read this ticket file completely.
- [ ] Read `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify every dependency listed above is `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS` before implementation begins; update `docs/tickets/TICKET_STATUS.md` in the same change.
- [ ] Record the start timestamp and current implementation step in `docs/tickets/TICKET_STATUS.md`.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before beginning the mandatory audits in B2–B10.
- [ ] Record the result of each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately if a blocking condition is found; record the exact blocker and unblock condition in both status locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B10 passes.
- [ ] Record all completion evidence in both this file and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project — `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/pytest/Sonar configuration, provider adapters, root metadata — not only files this ticket touches. A defect cannot be dismissed as pre-existing, another ticket's/agent's fault, outside the diff, legacy, or inconvenient to fix. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision when authority is silent.

### B3. Architecture boundary audit

Check the complete project for dependency-direction violations, framework leakage, config-boundary leaks, CLI wiring dependencies, import cycles/side effects, service locators, universal context objects, deep inheritance, incorrect type placement, duplicate abstractions, forbidden generic module names, backward-compatibility scaffolding, hidden defaults, silent fallbacks, mutable scientific specs, unstable identity construction, improperly typed process-boundary objects, and out-of-phase behavior. Run import-linter (`P0-T011`) and pytest-archon (`P0-T012`); record results. For this ticket specifically: confirm `domain/learning/` imports nothing beyond the standard library.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan the complete project for raw strings standing in for enums, hardcoded scientific/status/policy identifiers, object-shaped dictionaries, `dict[str, Any]`/`Any`/unjustified `cast`/`type: ignore`, boolean flags replacing discriminated variants, incomplete enum dispatch, missing `assert_never`, and primitive obsession. Confirm no caller represents `AggregationStrategy`/`ModelPersonalizationStrategy`/`CheckpointSelectionStrategy` as a raw string once this ticket's enums exist.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches. Runtime/test code expresses the rule, never its planning-document origin.

### B6. Documentation and comment audit

Check for stale documentation, stale paths, retired IDs, contradictory documents, inaccurate docstrings, stale comments, banner/separator/narrative comments, misleading status claims, and unverified "checks run" claims. Confirm the retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit-test lane (line 25) and the architecture lane (line 28), plus Pyright strict, Ruff lint/format, Pylance compatibility, import-linter, pytest-archon, the complete test suite, randomized ordering, all Nox sessions, Sonar, complete git-diff inspection, and untracked-file/cleanup inspection. Property, contract, integration, CUDA, and system categories are `NONE` and recorded as such. Never delete tests, weaken assertions, add unjustified skips, relax typing, or suppress Sonar solely to pass.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity repository-wide; this ticket's four modules must pass strict typing with no unexplained `Any` or suppression.

### B9. Sonar requirements

`P0-T027` owns the Sonar quality gate repository-wide; this ticket's contribution must pass with no unresolved blocker/critical/major issue and no artificial suppression.

### B10. Determinism and reproducibility audit

Check for scattered global seeds, nondeterministic algorithms, silent CPU fallback/batch reduction, unsafe CUDA multiprocessing, unstable serialization, and `NaN`/infinity in identity-bearing fields. For this ticket: `NOT_APPLICABLE — this ticket introduces closed string enums only; no seed, CUDA, or numeric identity field exists in its scope.` Confirm no such path was introduced regardless.

### B11. Phase 1 boundary confirmation

Confirm this ticket introduced no real N-BaIoT/Edge-IIoTset/CICIoT2023 access, real training/scoring, or any reachable scientific-execution path. Enum-only vocabulary is inherently compliant; validation uses no data. This ticket applies the master log's explicit Phase 1 "Permitted work"/"Forbidden work" statement in place of the ticket-conversion task's Phase-0-specific Section 19, per the conversion task's own authority order (master log governs phase-membership/boundary content); this substitution is recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm FedAvg is encoded as the core baseline and FedProx only as a stress-aggregation member (Roadmap §2); confirm the personalization naming lock (SB-24) is enforced; confirm no executable path for partial participation, per-regime checkpoint selection, or approximate quantiles exists.

**Audit 2 — Architecture fidelity.** Confirm the four modules match Architecture §5.1 exactly and `domain/learning/` imports nothing beyond the standard library per §3.1.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: does every member trace to an explicit citation rather than convenience? Does the naming-lock test actually distinguish `FEDREP_AE`/`FEDPER_AE` from `DITTO`, or merely assert the enum exists? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.
