# P1-T032 — Implement YAML loading, override composition, and schema-to-domain mapping

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T032` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T032
2. **Title.** Implement YAML loading, override composition, and schema-to-domain mapping
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** configuration
7. **Owner.** configuration-engineer
8. **Supporting agents.** —
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `config/loader.py`, `config/compose.py`, `config/mapping/scientific.py`, `execution.py`, `artifacts.py`, `reporting.py` pure `map_*(schema) -> DomainSpec` functions, `resolve_experiment_profile`.
13. **Objective.** Implement `config/loader.py` (YAML→schema), `config/compose.py` (explicit base+override, single precedence), and `config/mapping/{scientific,execution,artifacts,reporting}.py` pure `map_*(schema) -> DomainSpec` functions plus `resolve_experiment_profile`.
14. **Why this ticket exists.** After mapping completes, no configuration type exists in the running system (Architecture §3.4): `config/loader.py` turns YAML text into a validated Pydantic schema (P1-T031's schemas), `config/compose.py` composes an explicit base configuration with zero or more overrides under a single declared precedence (Architecture §11.4's closing paragraph — no ambiguous override precedence, no untyped configuration merging), and `config/mapping/{scientific,execution,artifacts,reporting}.py` expose pure `map_*(schema) -> DomainSpec` functions, the only place a configuration schema becomes a domain specification (Architecture §12.1, §3.4). `resolve_experiment_profile` must run before any of a profile's closed cells are mapped (Architecture §11.4's first bullet): a caller cannot submit a generic sweep, an arbitrary policy set, or an unapproved fallback behavior under a named experiment identifier. This ticket is also where the roadmap's most safety-critical mapping rules are enforced at the boundary: B0 maps only through its dedicated centralized-comparator mapper, never through a FedAvg threshold-construction arm; FedProx mapping requires a strictly positive µ from the frozen pre-registered grid, never zero, never copied from FedAvg, and never chosen post-hoc from confirmatory outcomes; and canonical D-temporal mapping accepts only a 70/30 boundary and genuine capture-time evidence, rejecting any pseudo-time substitute.
15. **Authority references.** Architecture §3.4 (boundary mapping — pure `map_*` functions, one module per configuration concept, called once by `composition/root.py`), §11.4 (mapping and validation rules — profile-first resolution, B0/FedProx/canonical-D-temporal mapping rules, single override precedence), §12.1 (`map_experiment_schema`/`resolve_experiment_profile` signatures).
16. **Dependencies.** P1-T031.
17. **Blocks.** P1-T036, P2-T003.
18. **Downstream consumers.** P1-T036 (persistence/runtime application ports) and P2-T003 consume mapped domain specs directly.
19. **Preconditions.** P1-T031 is `DONE`.
20. **Inputs.** Architecture §3.4, §11.4, §12.1.
21. **Allowed scope.** `config/loader.py`, `config/compose.py`, `config/mapping/scientific.py`, `execution.py`, `artifacts.py`, `reporting.py`.
22. **Forbidden actions.** No YAML parsing outside `config/loader.py` — never in `application`, `analysis`, `composition`, or `tests`; no ambiguous override precedence in `config/compose.py`; no path that maps B0 through a FedAvg threshold-construction arm; no FedProx µ of zero, a value copied from FedAvg, or a value chosen post-hoc from confirmatory test outcomes; no non-70/30 canonical D-temporal split; no seed value invented anywhere in a mapping function.
23. **Required configuration or NONE.** Mapping functions consume `configs/` external YAML data (Architecture §11) but define no configuration of their own beyond that consumption.
24. **Implementation responsibilities.** Implement `map_*(schema) -> DomainSpec` as pure functions, one module per configuration concept, per Architecture §11/§12.1; implement `resolve_experiment_profile` to run before any closed cell of that profile is mapped; implement the dedicated B0 centralized-comparator mapper as the only route to `CentralizedModelComparatorSpec`; implement the FedProx µ grid rule and the canonical D-temporal 70/30 rejection-of-pseudo-time rule; accept an explicitly unresolved field only for `DEVELOPMENT`/`SMOKE` `ExecutionMode` and reject it for `SCIENTIFIC`/`PRINT_GRADE`.
25. **Required unit tests.** Profile-first resolution is enforced (a cell cannot be mapped before its profile resolves); the B0 mapper is structurally distinct from the FedAvg mapper and is the only path to `CentralizedModelComparatorSpec`; a FedProx µ of zero, a FedAvg-copied value, or a confirmatory-outcome-selected value is rejected; canonical D-temporal mapping rejects a non-70/30 split and rejects pseudo-time evidence; override composition precedence is deterministic across repeated compositions of the same base and overrides.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE.
29. **Required integration tests.** A synthetic base-plus-override composition produces the exact expected resolved-configuration shape (a regression test standing in for the retired golden-test category for this ticket's scope).
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; integration lane (synthetic); Pyright strict.
33. **Acceptance criteria.** [ ] No configuration type (Pydantic model or raw mapping) survives past this ticket's mapping functions. [ ] Override composition resolves under a single declared precedence. [ ] B0/FedProx/canonical-D-temporal mapping rules are enforced exactly as authored. [ ] No YAML parsing occurs anywhere outside `config/loader.py`.
34. **Completion evidence.** Changed files: `config/loader.py`, `config/compose.py`, `config/mapping/scientific.py`, `execution.py`, `artifacts.py`, `reporting.py`, and their tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** `ConfigurationError` on an invalid mapping; a field marked unresolved is rejected for `SCIENTIFIC`/`PRINT_GRADE` and carried only for `DEVELOPMENT`/`SMOKE`, per Architecture §11.4's closing bullet.
36. **Stop conditions.** None.
37. **Deliverables.** The loader, compose, and mapping modules, plus `resolve_experiment_profile`.
38. **Final review checklist.** [ ] Mapping is pure and deterministic; no configuration type survives mapping. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for YAML parsing occurring anywhere outside `config/loader.py` (this ticket's own primary forbidden pattern), an ambiguous or implicit override-precedence path, dependency-direction violations, framework leakage, and a B0 cell reachable through the FedAvg mapper. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw `dict`-shaped merge substituting for `config/compose.py`'s explicit precedence, `Any`/unjustified `cast`, a hardcoded seed literal inside a mapping function, and a FedProx µ value hardcoded to zero or duplicated from a FedAvg profile.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25) and the integration lane (line 29), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/contract/architecture-specific/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `config/loader.py`, `config/compose.py`, and `config/mapping/scientific.py`, `execution.py`, `artifacts.py`, `reporting.py` must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

Confirm `config/compose.py`'s override composition is deterministic: composing the same base and the same ordered overrides twice yields byte-identical resolved schemas, and no composition step depends on dictionary iteration order or wall-clock time. Confirm no mapping function invents a seed value and that the FedProx µ grid and canonical D-temporal 70/30 rule are enforced exactly as authored, not merely accepted as passed-through configuration.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm the B0 dedicated mapper, the FedProx µ grid rule, and the canonical D-temporal 70/30 rule match Architecture §11.4's roadmap-derived mapping rules exactly. Positive finding required: confirm profile-first resolution cannot be bypassed to synthesize an unauthorized cell that the roadmap never approved.

**Audit 2 — Architecture fidelity.** Confirm every `config/mapping/*.py` function is pure — no side effect, no I/O — per Architecture §3.4, and that `resolve_experiment_profile`'s signature matches §12.1 exactly.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a caller construct an override sequence whose effective precedence silently depends on argument order rather than the declared rule? Could a B0 cell be smuggled through the FedAvg mapper by supplying an empty or degenerate federation policy? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.
