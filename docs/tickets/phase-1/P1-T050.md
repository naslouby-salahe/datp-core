# P1-T050 — Implement the architecture-boundary and framework-confinement test suite

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T050` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T050
2. **Title.** Implement the architecture-boundary and framework-confinement test suite
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** architecture
7. **Owner.** architecture-cleaner
8. **Supporting agents.** —
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `tests/architecture/test_dependency_rules.py`, `test_framework_confinement.py`, `test_no_forbidden_module_names.py`, `test_no_module_side_effects.py`, `test_no_cycles.py`.
13. **Objective.** Implement `tests/architecture/` completely: the five test modules of field 12, plus assertions that every closed union added across Phase 1 is `assert_never`-exhausted and that no root `experiments/` package exists anywhere in `src/datp_core`.
14. **Why this ticket exists.** Every individual Phase 1 ticket has already claimed, in its own B3 audit, that its own modules respect the Architecture §3 dependency graph and framework confinement — this ticket is what makes those claims collectively verifiable in one place, with the clear pytest-archon failure diffs Architecture §21.2 requires, rather than each ticket's claim resting on manual review alone. Because this suite runs after every other Phase 1 implementation ticket has produced real modules to check (it depends on P1-T068 and P1-T069, the composition root and CLI), it is deliberately positioned as one of the last Phase 1 tickets, immediately upstream of the socle-readiness gate (P1-T070) that depends on it.
15. **Authority references.** Architecture §3.3 (the enforced layer dependency diagram), §21.2 (architecture-test category requirements), §29.2 (invalid-state guardrails this suite must catch).
16. **Dependencies.** P0-T011, P0-T012, P1-T068, P1-T069.
17. **Blocks.** P1-T070.
18. **Downstream consumers.** P1-T070 (the Phase 1 socle-readiness gate) depends on this suite passing before the gate can close.
19. **Preconditions.** P0-T011, P0-T012, P1-T068, P1-T069 are `DONE`.
20. **Inputs.** Architecture §3.3, §21.2, §29.2.
21. **Allowed scope.** `tests/architecture/`.
22. **Forbidden actions.** No production code under `src/datp_core` may import anything from `tests/`; no test in this suite may be relaxed or excluded to make it pass rather than fixing the underlying boundary violation it finds.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Implement pytest-archon assertions matching the Architecture §3.3 diagram edge-for-edge; implement a forbidden-generic-module-name scan (`utils`, `helpers`, `common`, `base`, `misc`, `shared`, and their variants); implement an import-time-side-effect-free-import assertion; implement cycle detection across the complete `src/datp_core` package graph; implement an exhaustiveness-verification pass confirming every `match`/`case` over a closed union ends in `assert_never`.
25. **Required unit tests.** NONE — this ticket's five modules are themselves the test suite; there is no separate "unit test of the test suite" beyond confirming each of the five modules actually executes and produces a pass/fail result.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** All five architecture tests are present and passing against the complete Phase 1 socle: dependency rules, framework confinement, no forbidden module names, no import-time side effects, no cycles.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** The architecture lane; import-linter.
33. **Acceptance criteria.** [ ] All five architecture tests are present and pass on the full Phase 1 socle. [ ] No framework object crosses any port boundary. [ ] No forbidden module name, import cycle, or import-time side effect exists anywhere in `src/datp_core`. [ ] No root `experiments/` package exists.
34. **Completion evidence.** Changed files: `tests/architecture/` and its five modules. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** A boundary breach fails the architecture lane outright; no relaxation, exclusion, or suppression is ever permitted to force a pass — the underlying violation must be fixed in the offending module instead.
36. **Stop conditions.** None.
37. **Deliverables.** The complete architecture test suite.
38. **Final review checklist.** [ ] Matches Architecture §3.3 exactly, edge for edge. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files — this is precisely the ticket whose own suite performs that repository-wide inspection for architecture boundaries, so its own audit must run the suite against the complete `src/datp_core` tree, not a sample. No defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

This ticket IS the architecture boundary audit mechanism for Phase 1. Its own self-check: confirm the five test modules genuinely fail when a deliberately introduced violation (a temporary test fixture with a bad import) is present, and genuinely pass once removed — a suite that always passes regardless of input is not a real test. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw string module-name list duplicated outside the single canonical forbidden-name list this suite should own, `Any`/unjustified `cast`, and an assert_never-exhaustiveness check that only samples a subset of closed unions rather than scanning all of them.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the architecture lane (line 28) itself as the primary validation, plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Unit/property/contract/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `tests/architecture/` must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

Check specifically that the architecture suite's own execution is deterministic (the same codebase always produces the same pass/fail result, with no test-order dependency that could mask a violation depending on collection order), and confirm the suite itself detects unsafe process start methods and non-picklable process-boundary objects wherever those checks are architecturally in scope.

### B11. Phase 1 boundary confirmation

Confirm this suite introduces no real N-BaIoT/Edge-IIoTset/CICIoT2023 data access itself, and — as its own positive contribution — confirm it is the mechanism that would catch a future accidental Phase-2-onward scientific-execution path if one were introduced prematurely into the Phase 1 socle. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — an architecture test suite carries no direct scientific claim.` Positive finding required: confirm this suite is the mechanism that would catch an accidental executable path for an out-of-scope roadmap concept (poisoning, Dynamic DATP, etc.) if one were ever introduced, since such a path would necessarily violate a layer or framework-confinement rule to exist.

**Audit 2 — Architecture fidelity.** Confirm the suite's five modules collectively enforce every edge and non-edge in Architecture §3.3's diagram, with no gap left unchecked.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could this suite itself contain a bug that makes it trivially pass (for example, a test that collects zero test cases due to a glob-pattern typo and therefore "passes" vacuously)? Was each of the five tests verified to actually fail against a deliberately broken fixture before this ticket claims `DONE`? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is the canonical mechanism owner for repository-wide architecture-boundary enforcement across Phase 1 and all later phases — every other Phase 1 ticket's B3 audit ("run import-linter/pytest-archon; record results") ultimately executes against the suite this ticket builds. It must otherwise remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.
