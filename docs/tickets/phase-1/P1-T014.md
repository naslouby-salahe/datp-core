# P1-T014 — Implement resource, traffic-rate, and byte value objects

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T014` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T014
2. **Title.** Implement resource, traffic-rate, and byte value objects
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** DONE
5. **Priority.** P0 — Blocking
6. **Ticket type.** domain
7. **Owner.** implementation-engineer
8. **Supporting agents.** experiment-engineer
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `BatchSize`, `GradientAccumulationSteps`, `WorkerCount`, `ChunkRowCount`, `RamBudgetBytes`, `VramFraction`, `GpuIndex`, `NumericTolerance`, `ByteCount`, `DiskCapacity`, `BootstrapResampleCount`, `TrafficRate` (+`TrafficRateUnit`), `SampleCount`, `ConfusionCount`, `CalibrationSampleCount` (+`CalibrationSampleCountRef`), `RelativeArtifactPath`.
13. **Objective.** Implement each value object of field 12 as a frozen, slotted, keyword-only dataclass with `__post_init__` validation; `BatchSize`/`GradientAccumulationSteps` are always identity-bearing (fingerprinted); `WorkerCount` is identity-bearing only when it is not proven deterministically equivalent regardless of value.
14. **Why this ticket exists.** `BootstrapResampleCount` has no default anywhere in the codebase — an implicit default resample count would be a hidden statistical parameter silently affecting every BCa confidence interval, including the confirmatory endpoint's. `RelativeArtifactPath` must reject `..`, a leading `/`, a drive letter, and whitespace at construction, because a path-traversal-capable value object would let a resolved artifact location escape its bound storage root (Architecture §15). `TrafficRate` must be strictly positive and reject zero/negative/`NaN`/infinity so a fabricated zero-cost alert-burden estimate can never masquerade as measured evidence (Roadmap SB-20).
15. **Authority references.** Architecture §7 (value-object table), §7.4 (immutable mapping fields), §16.6 (batch/worker semantics and identity-bearing rules), §10/§27 (bootstrap authority — explicit resample count, no default). Roadmap SB-20 (no hypothetical alert/day numbers presented as measurements).
16. **Dependencies.** P1-T011.
17. **Blocks.** P1-T026.
18. **Downstream consumers.** P1-T026 (evaluation, operating-point, and alert-burden result types) consumes these value objects directly.
19. **Preconditions.** P1-T011 is `DONE`.
20. **Inputs.** Architecture §7, §7.4, §16.6, §10, §27; Roadmap SB-20.
21. **Allowed scope.** `domain/runtime/admissibility.py`, `domain/artifacts/keys.py`/`references.py`, `domain/evaluation/alert_burden.py`.
22. **Forbidden actions.** No default value for `BootstrapResampleCount` anywhere; no `..`, absolute path, or whitespace accepted by `RelativeArtifactPath`; no zero, negative, or non-finite `TrafficRate`; no live `dict` field on any of these value objects (Architecture §7.4's immutable-mapping rule).
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Implement every value object with range and, where float-backed, finiteness checks; implement `RelativeArtifactPath`'s traversal-rejection logic explicitly (not merely delegating to `pathlib` without a dedicated check); implement `TrafficRate` as a decimal rate paired with a `TrafficRateUnit` member, strictly positive.
25. **Required unit tests.** `BootstrapResampleCount` construction without an explicit value is a type error (no keyword default exists to omit); `RelativeArtifactPath` rejects `../x`, `/abs/x`, `C:\x`, and `"x y"` (whitespace) explicitly, each as its own test case.
26. **Required property tests.** Range/finiteness for every numeric value object; `RelativeArtifactPath` traversal rejection under Hypothesis-generated adversarial strings; `TrafficRate` positivity under generated inputs including zero, negative, and non-finite values.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE beyond the baseline framework-confinement check.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Property lane; unit lane; Pyright strict.
33. **Acceptance criteria.** [x] `RelativeArtifactPath` rejects every tested traversal/absolute-path/whitespace case. [x] `BootstrapResampleCount` has no default value anywhere. [x] `TrafficRate` is strictly positive with a supported unit. [x] `BatchSize`/`GradientAccumulationSteps` are fingerprintable value objects.
34. **Completion evidence.** Changed files: `domain/runtime/admissibility.py`, `domain/artifacts/keys.py`/`references.py`, `domain/evaluation/alert_burden.py`, and their tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** `DomainValidationError` on any invalid value; no other failure is expected in this ticket's own scope.
36. **Stop conditions.** None.
37. **Deliverables.** Resource/traffic/byte value-object modules.
38. **Final review checklist.** [ ] No hidden bootstrap default. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [x] Read the complete roadmap and architecture before starting.
- [x] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [x] Verify every dependency is `DONE`.
- [x] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [x] Keep both status locations synchronized while active.
- [x] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [x] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for a hidden `BootstrapResampleCount` default anywhere, a path-traversal-capable path construction bypassing `RelativeArtifactPath`, framework leakage, cycles/side effects, and hidden defaults generally. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw `int` used where `BootstrapResampleCount`/`BatchSize` is required, a raw `str`/`Path` used where `RelativeArtifactPath` is required, `Any`/unjustified `cast`, and a live mutable `dict` field on any value object here.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the property lane (line 26) and unit lane (line 25), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Contract/architecture-specific/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; these modules must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

Check specifically that `BatchSize`/`GradientAccumulationSteps` are always treated as identity-bearing (fingerprinted) and that `WorkerCount`'s identity-bearing status is not silently assumed to be execution-only without a proven deterministic-equivalence justification. Confirm no automatic batch-size reduction concept exists anywhere in this ticket's scope.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm `TrafficRate` can never be constructed as zero, negative, or a hypothetical/fabricated estimate presented as measured (Roadmap SB-20); confirm `BootstrapResampleCount` is always explicit, protecting the confirmatory BCa procedure from a hidden resample-count default.

**Audit 2 — Architecture fidelity.** Confirm every value object matches Architecture §7's table exactly, and that `RelativeArtifactPath`'s traversal rejection matches §15's path-safety requirement.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a URL-encoded or Windows-style traversal string (`%2e%2e/`, `..\\`) bypass the `RelativeArtifactPath` rejection? Is the "no default" test for `BootstrapResampleCount` actually attempting construction without the argument and confirming a `TypeError`/`DomainValidationError`, not just checking documentation? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.
