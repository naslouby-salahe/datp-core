# P1-T023 — Implement scoring and split-scoped score-artifact specifications

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T023` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T023
2. **Title.** Implement scoring and split-scoped score-artifact specifications
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** DONE
5. **Priority.** P0 — Blocking
6. **Ticket type.** domain
7. **Owner.** data-pipeline-engineer
8. **Supporting agents.** artifact-lineage-auditor
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `ScoreGenerationSpec` (owns `ScoringBatchSpec`), `ScoringBatchSpec`, `ClientCalibrationScoreArtifact`, `ClientTestScoreArtifact`, `ClientTemporalScoreArtifact`, `CalibrationScoreArtifactSet`/`TestScoreArtifactSet`/`TemporalScoreArtifactSet`, `SplitScopedScoreBundle`.
13. **Objective.** Implement `ScoreGenerationSpec` (owning `ScoringBatchSpec`), `ClientCalibrationScoreArtifact` (no attack field), `ClientTestScoreArtifact` (benign and attack refs committed atomically as one aggregate), `ClientTemporalScoreArtifact`, the three role-scoped artifact-set collections, and `SplitScopedScoreBundle`.
14. **Why this ticket exists.** This is the type-level enforcement point for the roadmap's benign-only calibration rule: `ClientCalibrationScoreArtifact` has no field capable of carrying an attack score, so calibration/evaluation leakage becomes a compile-time impossibility rather than a runtime discipline. `ClientTestScoreArtifact` must hold its benign and attack score references as one committed aggregate — never two independently persisted references — because a partially committed test-score pair (benign written, attack missing) would silently corrupt every downstream evaluation that assumes both are present (Architecture §9.2's atomic pair protocol).
15. **Authority references.** Architecture §9.2 (score specifications and the atomic benign/attack pair protocol), §13.3 (reuse lineage for scoring identities), §29.1 (invalid-state guardrails).
16. **Dependencies.** P1-T002, P1-T015.
17. **Blocks.** P1-T024, P2-T011.
18. **Downstream consumers.** P1-T024 (threshold-construction union) and P2-T011 (outside this extracted phase) consume these artifacts directly; every threshold construction in Phase 1 onward reads calibration score sets built from this ticket's types.
19. **Preconditions.** P1-T002, P1-T015 are `DONE`.
20. **Inputs.** Architecture §9.2, §13.3, §29.1.
21. **Allowed scope.** `domain/learning/scores.py`.
22. **Forbidden actions.** No attack field on `ClientCalibrationScoreArtifact`; no loose (independently referenceable) benign/attack pair on `ClientTestScoreArtifact`; no threshold-construction configuration field on any scoring spec; the scoring batch is owned exclusively by `ScoreGenerationSpec`, never duplicated in a training or preprocessing spec.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Implement each artifact as a frozen dataclass carrying full lineage references, content hashes, and row-order checksums; implement `ClientTestScoreArtifact` holding two immutable `ArtifactRef` members (benign, attack) plus one aggregate manifest hash that is the sole citable identity of the pair.
25. **Required unit tests.** `ClientCalibrationScoreArtifact` has no attack-score field at the type level (a structural/attribute-inventory test, not just a construction test); a `ClientTestScoreArtifact` exposes exactly one aggregate identity, never two independently addressable child identities to a caller.
26. **Required property tests.** NONE.
27. **Required contract tests.** The threshold constructor port (declared in P1-T034) rejects a `TestScoreArtifactSet` passed where a `CalibrationScoreArtifactSet` is required, and vice versa for the evaluator — a construction-time or type-level rejection, not a runtime data-shape check.
28. **Required architecture tests.** NONE beyond the baseline framework-confinement check for `domain/learning/`.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; contract lane; Pyright strict.
33. **Acceptance criteria.** [x] `ClientCalibrationScoreArtifact` has no attack field. [x] The test benign/attack pair is committed and referenced as exactly one aggregate. [x] `ScoringBatchSpec` is owned exclusively by `ScoreGenerationSpec`. [x] Calibration/test/temporal role types are disjoint and non-substitutable.
34. **Completion evidence.** Changed files: `domain/learning/scores.py` and its tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** `ScoringError`/`PartialArtifactError` code paths are defined for adapter-level use (the adapter that persists these artifacts is P1-T042/P1-T045's scope). Stop and raise a blocker if score-batch semantics (for example, the exact chunking granularity for streamed scoring) are genuinely unresolved by either authority — carry as a readiness blocker rather than guessing.
36. **Stop conditions.** Stop if score-batch semantics are unresolved; carry as a readiness blocker.
37. **Deliverables.** Scoring and split-scoped score-artifact specification modules.
38. **Final review checklist.** [ ] Calibration/test leakage is a type error, not a runtime check. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [x] Read the complete roadmap and architecture before starting.
- [x] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [x] Verify every dependency is `DONE`.
- [x] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; recorded 2026-07-14T21:50:00Z; implement role-scoped score artifacts and atomic test pairs.
- [x] Keep both status locations synchronized while active.
- [x] Change status to `IN_REVIEW` before the B2–B10 audits; recorded 2026-07-14T22:02:00Z; B12 audits recorded separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for dependency-direction violations, framework leakage, cycles/side effects, incorrect type placement, and hidden defaults. Run import-linter/pytest-archon; record results. Confirm `domain/learning/scores.py` imports nothing beyond the standard library plus `domain` value objects.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw tuple/dict standing in for `ClientTestScoreArtifact`'s aggregate pair, `Any`/unjustified `cast`, an optional attack-score field bolted onto the calibration artifact "for convenience," and a boolean flag substituting for the role-type distinction.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25) and contract lane (line 27), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/architecture-specific/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `domain/learning/scores.py` must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

Check specifically that every score artifact carries a row-order checksum so scoring order is verifiable and reproducible, and that no artifact here permits a partial (benign-only or attack-only) test-score commit to be treated as complete. This is a lineage-integrity-relevant ticket, not `NOT_APPLICABLE`.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm calibration artifacts structurally cannot carry an attack score, matching Roadmap §2's benign-only calibration lock, and that attack scores exist only on `ClientTestScoreArtifact` (Architecture §9.2's evaluation-only scope for attack data).

Record, 2026-07-14T22:02:00Z: PASS. The calibration field inventory contains no attack surface; attack references exist only in the typed test aggregate and calibration/test/temporal sets are nominally distinct.

**Audit 2 — Architecture fidelity.** Confirm the module matches Architecture §5.1/§9.2 exactly, including the atomic benign/attack pair protocol referenced from §13.3's reuse-lineage rules.

Record, 2026-07-14T22:02:00Z: PASS. Every score artifact carries its required split/checkpoint/preprocessor/schema lineage, canonical content hashes, and row-order checksums. The test artifact validates both immutable children against its sole aggregate manifest identity.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a future refactor add an optional `attack_score: float | None` field to `ClientCalibrationScoreArtifact` "just for a diagnostic," silently reopening the leakage path this ticket exists to close? Does the contract test actually attempt the wrong-role substitution and confirm rejection, or only construct valid instances? Fix and rerun until clean.

Record, 2026-07-14T22:02:00Z: PASS for the owned type boundary. The field-inventory test fails on any future calibration attack field; no optional role toggle or raw pair exists. The named threshold/evaluator port has not yet been introduced (P1-T034 depends on P1-T028), so its conformance case is explicitly deferred to P1-T034 rather than fabricated early.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.

### Batch completion evidence — 2026-07-15T00:45:00Z

- The P1-T020–P1-T029 whole-project quality checkpoint passed: `uv run nox -s full_suite` (Ruff, format, strict Pyright/Pylance parity, import-linter, architecture pytest, randomized xdist 224 tests, Sonar, and CodeScene).
- The repository-wide raw-dictionary, `Any`, typing-suppression, planning-reference, documentation, no-backward-compatibility, and cleanup scans passed; no scientific execution occurred.
