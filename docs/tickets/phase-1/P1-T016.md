# P1-T016 — Implement locked dispersion, quantile, and pooled-variance mathematics

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T016` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T016
2. **Title.** Implement locked dispersion, quantile, and pooled-variance mathematics
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** DONE
5. **Priority.** P0 — Blocking
6. **Ticket type.** domain
7. **Owner.** statistics-auditor
8. **Supporting agents.** threshold-policy-engineer
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `cv_fpr`, `pooled_variance` (with the between-client mean-shift term), `fpr_target`, exact quantile helpers, `is_canonical_k`, eligibility helpers.
13. **Objective.** Implement `domain/mathematics/dispersion.py` (`cv_fpr`, `pooled_variance` including the between-client mean-shift term), `quantiles.py` (`fpr_target`, exact quantile helpers), and `pooled_statistics.py` (`is_canonical_k`, eligibility helpers), all as pure, framework-free functions.
14. **Why this ticket exists.** `pooled_variance` must always include the between-client mean-shift term (`σ²_global = Σ n_k·[σ_k² + (µ_k − µ_global)²] / Σ n_k`) — this is a structural rule, not a configurable option, because the roadmap's `B-FedStatsBenign` locked contract (§17) explicitly forbids the simplified pooled-variance formula (SB-26); a version of this function that silently drops the between-term would understate dispersion whenever clients have heterogeneous means, exactly the condition the journal extension studies. `cv_fpr` must match the roadmap's CV(FPR) definition exactly and never be silently redefined (Roadmap SB-15).
15. **Authority references.** Architecture §5.1 (module map), §7.1 (`is_canonical_k`), §9.2 (`B-FedStatsBenign` variance requirement), §29.4 (invalid-state guardrails). Roadmap §10 (CV(FPR) definition, locked), §17 (`B-FedStatsBenign` full pooled-variance contract), SB-15 (never silently change the CV(FPR) definition), SB-26 (never use the simplified pooled-variance formula).
16. **Dependencies.** P1-T011.
17. **Blocks.** P1-T026, P2-T016.
18. **Downstream consumers.** P1-T026 (evaluation/operating-point/alert-burden result types) and P2-T016 (outside this extracted phase) consume these pure functions directly.
19. **Preconditions.** P1-T011 is `DONE`.
20. **Inputs.** Architecture §5.1, §7.1, §9.2, §29.4; Roadmap §10, §17, SB-15, SB-26.
21. **Allowed scope.** `domain/mathematics/dispersion.py`, `quantiles.py`, `pooled_statistics.py`.
22. **Forbidden actions.** No simplified pooled-variance formula (one omitting the between-client term) anywhere, not even as an alternate/configurable path; no configurable variance-formula or tie-break toggle; no framework import in `domain/mathematics/`.
23. **Required configuration or NONE.** NONE — these are locked formulas with no configuration surface.
24. **Implementation responsibilities.** Implement `cv_fpr = σ_FPR / µ_FPR` over eligible clients exactly; implement the full pooled-variance formula with the between-client term always present; implement exact (non-approximate) quantile helpers; implement `is_canonical_k` returning true only for K = 3.
25. **Required unit tests.** Known-value checks for `cv_fpr`, `pooled_variance` (including a hand-computed example with heterogeneous client means to exercise the between-term), `fpr_target`, and `is_canonical_k`.
26. **Required property tests.** `cv_fpr` correctness against an independent reference computation; `pooled_variance` always includes the between-term (a property test comparing against the naive within-only formula confirms they differ whenever client means differ); `fpr_target = 1 − q` exactly; `is_canonical_k` is true only for K = 3 across generated K values.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** No framework import anywhere in `domain/mathematics/`.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Property lane; unit lane; Pyright strict.
33. **Acceptance criteria.** [x] Pooled variance always includes the between-client term, with no toggle to disable it. [x] `cv_fpr` matches the roadmap's locked definition exactly. [x] `is_canonical_k` is true only for K = 3. [x] No framework import anywhere in `domain/mathematics/`.
34. **Completion evidence.** Changed files: `domain/mathematics/dispersion.py`, `quantiles.py`, `pooled_statistics.py`, and their tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** A zero-mean CV computation returns a typed degeneracy signal consumed by the result types built in P1-T026, never a divide-by-zero exception and never a silently substituted value.
36. **Stop conditions.** None — these formulas are locked by the roadmap, not open design decisions.
37. **Deliverables.** Locked mathematics modules.
38. **Final review checklist.** [ ] Between-term present with no toggle. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [x] Read the complete roadmap and architecture before starting.
- [x] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [x] Verify every dependency is `DONE`.
- [x] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [x] Keep both status locations synchronized while active.
- [x] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [x] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for any simplified pooled-variance implementation competing with this ticket's locked function, framework leakage into `domain/mathematics/`, duplicate mathematical-formula abstractions, and hidden defaults. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw `float`/`tuple` return standing in for a typed result where P1-T026's result types should be used instead, `Any`/unjustified `cast`, and a configurable boolean toggle for the pooled-variance formula (which this ticket explicitly forbids).

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the property lane (line 26) and unit lane (line 25), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Contract/architecture-specific/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; these three modules must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

Check specifically for raw-float hashing or non-deterministic floating-point summation order in `pooled_variance`/`cv_fpr` that could make repeated computation of the same inputs yield a different bit pattern across runs, which would threaten fingerprint stability wherever these values are later hashed. Confirm no `NaN`/infinity can propagate out of these functions un-flagged.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm `pooled_variance` always includes the between-client term (Roadmap §17, SB-26) with no path to omit it; confirm `cv_fpr` matches Roadmap §10's definition exactly with no silent redefinition (SB-15); confirm `is_canonical_k` is true only for K = 3, matching Roadmap §4's canonical-K lock.

**Audit 2 — Architecture fidelity.** Confirm the three modules match Architecture §5.1 exactly and import nothing beyond the standard library plus (if needed) `domain` value objects.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: does the between-term unit test actually use client means that differ (a same-mean test case would pass even with a broken between-term formula)? Could a future refactor accidentally reintroduce the simplified pooled-variance formula as a "performance optimization" without a regression test catching it? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.
