# P1-T036 — Implement persistence/runtime application ports

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T036` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T036
2. **Title.** Implement persistence/runtime application ports
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** application
7. **Owner.** artifact-lineage-auditor
8. **Supporting agents.** implementation-engineer
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `ArtifactStore`, `CheckpointStore` (scientific vs recovery methods), `ManifestStore`, `RunStateStore`, `ArtifactLockProvider`, `HardwareInspector`, `CudaGuard`, `ResourcePressureMonitor`.
13. **Objective.** Implement `application/ports/{persistence,runtime}.py`: the eight ports of field 12, each narrowed by storage semantics, with no god repository and no `ArtifactPathResolver` port.
14. **Why this ticket exists.** A single `ArtifactRepository` interface covering datasets, checkpoints, scores, thresholds, manifests, and run state would become an unbounded surface that every unrelated change could touch — Architecture principle 11 ("selective abstraction") and the explicit rejection of a god repository in §12.7 exist to prevent exactly this. `CheckpointStore` in particular must keep its scientific and recovery methods deliberately distinct rather than a generic save/load pair, because conflating them is precisely how a resume-in-progress recovery checkpoint could be mistaken for a citable scientific model state (Architecture §17). `ArtifactPathResolver` is deliberately excluded from this port list: path resolution is an internal `infrastructure` collaborator (P1-T054), never something `application` code calls directly, since `application` reasons about semantic storage roots and artifact keys, never filesystem paths.
15. **Authority references.** Architecture §12.2 (persistence/runtime port declarations), §12.7 (no god repository), §15 (semantic storage separation), §17 (scientific vs recovery checkpoint separation).
16. **Dependencies.** P1-T008, P1-T009, P1-T033.
17. **Blocks.** P1-T054, P1-T055, P1-T056, P1-T057, P1-T058, P1-T059, P1-T060, P1-T061, P1-T062, P1-T063, P1-T064, P1-T068.
18. **Downstream consumers.** Every Phase 1 persistence/runtime infrastructure adapter (P1-T054–P1-T064) and the composition root (P1-T068) implement or consume these ports directly — this ticket is on the critical path for eleven later tickets.
19. **Preconditions.** P1-T008, P1-T009, P1-T033 are `DONE`.
20. **Inputs.** Architecture §12.2, §12.7, §15, §17.
21. **Allowed scope.** `application/ports/persistence.py`, `runtime.py`.
22. **Forbidden actions.** No `ArtifactRepository`-style god interface covering more than one storage semantic; no `ArtifactPathResolver` port (path resolution stays internal to `infrastructure`); no `pathlib.Path` type crossing the `application` boundary anywhere in these signatures; no generic save/load method conflating scientific and recovery checkpoint semantics.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Define each port as a `Protocol` with typed requests/results; keep `CheckpointStore`'s scientific (`find_compatible`, `save`) and recovery (`save_recovery`, `load_recovery`) methods textually and semantically distinct on the same Protocol; specify `ArtifactLockProvider.acquire` as yielding a context-manager lease type.
25. **Required unit tests.** No signature across either module contains a raw `pathlib.Path` parameter or return type; `CheckpointStore`'s scientific and recovery methods are distinct callables with distinct request/result types (not a shared generic pair).
26. **Required property tests.** NONE.
27. **Required contract tests.** Contract scaffolding is established here for later adapter conformance; the full conformance suites are exercised by the concrete adapters (P1-T054–P1-T064).
28. **Required architecture tests.** No god persistence interface exists (a single port covering more than one Architecture §15 storage semantic is a violation).
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; architecture lane; Pyright strict.
33. **Acceptance criteria.** [ ] The eight ports are non-overlapping by storage/runtime semantic. [ ] No path-resolution port exists at the application boundary. [ ] Scientific and recovery checkpoint methods are structurally distinct. [ ] No `Path` type appears in any signature.
34. **Completion evidence.** Changed files: `application/ports/persistence.py`, `runtime.py`, and their tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** Ports themselves raise the typed persistence/lock error families defined in P1-T030; no other failure is expected in this ticket's own scope.
36. **Stop conditions.** None.
37. **Deliverables.** Persistence and runtime application ports.
38. **Final review checklist.** [ ] No god repository anywhere. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for a god-repository interface anywhere, a `pathlib.Path` crossing the `application` boundary, dependency-direction violations, and framework leakage. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw `dict` request/result standing in for one of these eight ports' typed contracts, `Any`/unjustified `cast`, and a generic save/load method silently merging the scientific and recovery checkpoint semantics.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25) and architecture lane (line 28), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; these two modules must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE` for direct randomness — these are Protocol declarations, not implementations. Confirm regardless that `ArtifactLockProvider`'s declared lease semantics do not imply an unbounded or silently-retried lock hold that could mask a nondeterministic race condition; the concrete determinism-relevant checks belong to the adapters (P1-T054–P1-T064).

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — port declarations carry no scientific claim.` Positive finding required: confirm no port here could be used to bypass the scientific/recovery checkpoint separation the roadmap's reproducibility requirements depend on.

**Audit 2 — Architecture fidelity.** Confirm all eight ports match Architecture §12.2 exactly and that none of them is a god repository, per §12.7.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a future adapter implement `CheckpointStore`'s recovery methods by silently delegating to its scientific methods (or vice versa), defeating the separation this ticket declares at the type level but cannot itself enforce behaviorally? Is that risk flagged for the adapter ticket (P1-T063) to close with its own contract test? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.
