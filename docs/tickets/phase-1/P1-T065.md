# P1-T065 — Implement the structured telemetry adapter

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T065` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T065
2. **Title.** Implement the structured telemetry adapter
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P1 — Mandatory
6. **Ticket type.** telemetry
7. **Owner.** implementation-engineer
8. **Supporting agents.** report-output-auditor
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `EventSink` (implementation).
13. **Objective.** Implement `infrastructure/telemetry/structured_events.py`: an `EventSink` implementation backed by `structlog`'s context binding, rendering to console (`LogSink.CONSOLE`) and JSONL (`LogSink.JSONL_FILE`) per the selected `LogFormat`, non-blocking and best-effort.
14. **Why this ticket exists.** This ticket implements the concrete adapter for the `EventSink` port and `StructuredEvent`/detail-union vocabulary that P1-T051 declares — telemetry rendering is a distinct, format-specific responsibility from CUDA/hardware/pressure (P1-T060–P1-T062), checkpoint/run-state persistence (P1-T063–P1-T064), and the environment/provenance adapter (P1-T066), so it earns its own ticket. `structlog`'s context binding is used specifically because it removes the need for a hand-rolled JSON formatter (Architecture §4.1) while still confining rendering entirely to `infrastructure/telemetry/`; a rendering failure here must never propagate into scientific computation, because an event is diagnostic-only and never the sole source of truth for any scientific value.
15. **Authority references.** Architecture §4.1 (structlog role), §12.2 (`EventSink` port), §12.4 (implementation), §19.1–19.3 (event model, rendering rules, relationship to provenance).
16. **Dependencies.** P1-T051, P1-T035.
17. **Blocks.** P1-T068, P1-T070.
18. **Downstream consumers.** Every stage or service that publishes a `StructuredEvent` depends on this adapter being wired at composition (P1-T068).
19. **Preconditions.** P1-T051, P1-T035 are `DONE`.
20. **Inputs.** Architecture §4.1, §12.2, §12.4, §19.1–19.3.
21. **Allowed scope.** `infrastructure/telemetry/structured_events.py`.
22. **Forbidden actions.** No blocking of scientific computation on a rendering failure; no scientific value logged as its sole source of truth (every scientific value must also exist in a persisted artifact); no array, per-sample score, secret, dataset row, or full-configuration dump ever rendered, even indirectly.
23. **Required configuration or NONE.** `LogSink`/`LogFormat` selection comes from `ExecutionPolicy`/composition.
24. **Implementation responsibilities.** Bind `structlog` context per event; render human-readable console output and JSONL per the selected sink/format; ensure a rendering failure never propagates into scientific computation; log an exception exactly once at its translation boundary, never re-logged repeatedly as it propagates upward.
25. **Required unit tests.** A rendering failure (for example, a broken sink) does not raise into the caller; an exception is logged exactly once at its translation boundary, never duplicated at each level it passes through.
26. **Required property tests.** NONE.
27. **Required contract tests.** `EventSink.publish` satisfies its Section 12.2 signature exactly.
28. **Required architecture tests.** No scientific value is sourced only from this adapter — every scientific value logged here must also exist in a persisted artifact elsewhere (verified by cross-checking event detail fields against artifact schemas).
29. **Required integration tests.** A synthetic sequence of `StructuredEvent`s renders correctly to both the console and JSONL sinks; parallel workers' events preserve `worker_role`/`process_id`/`gpu_index` attribution correctly.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; contract lane; integration lane (reporting).
33. **Acceptance criteria.** [ ] Rendering is non-blocking and best-effort. [ ] No scientific value exists only in a log. [ ] An exception is logged exactly once at its translation boundary. [ ] Worker attribution is preserved under parallel execution.
34. **Completion evidence.** Changed files: `structured_events.py` and its tests. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** A sink failure never propagates into scientific computation; it is itself logged once, best-effort, and the calling stage continues.
36. **Stop conditions.** None.
37. **Deliverables.** The `EventSink` adapter module.
38. **Final review checklist.** [ ] Diagnostic only, never a scientific source of truth. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

### B3. Architecture boundary audit

Check the complete project for a scientific value sourced only from this adapter, a rendering failure that could propagate into scientific computation, and dependency-direction violations. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw dict payload substituting for the typed detail-union vocabulary owned by P1-T051, `Any`/unjustified `cast`, and a rendered event carrying an array/per-sample score/secret/dataset-row/full-config-dump.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25), contract lane (line 27), and integration lane (line 29), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/architecture-specific/CUDA/system categories are `NONE` beyond the baseline check at line 28, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `structured_events.py` must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE` for scientific determinism — telemetry rendering has no scientific-identity role by design. Confirm regardless that rendering itself never mutates or delays the scientific computation it describes (a slow or blocked sink must never stall a stage).

### B11. Phase 1 boundary confirmation

Confirm no real N-BaIoT/Edge-IIoTset/CICIoT2023 data appears in any rendered event anywhere in this ticket's tests — all synthetic events use fabricated identifiers and values. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — telemetry rendering carries no scientific claim.` Positive finding required: confirm no manuscript-relevant number (a CV(FPR) value, a BCa interval bound) is ever sourced only from a rendered log line rather than a persisted, traceable artifact.

**Audit 2 — Architecture fidelity.** Confirm the adapter matches Architecture §12.4/§19.1–19.3 exactly, and that `structlog` usage is confined entirely to this module.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a detail-type field added later (outside this ticket's scope) accidentally carry a raw array through this renderer without the forbidden-payload check catching it, since this adapter trusts the detail-union shape defined elsewhere? Does the "logged exactly once" test actually simulate an exception propagating through multiple call layers, or only a single-layer case? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.
