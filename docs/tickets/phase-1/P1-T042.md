# P1-T042 — Implement PyArrow streaming and bounded-pandas data adapters

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T042` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T042
2. **Title.** Implement PyArrow streaming and bounded-pandas data adapters
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** DONE
5. **Priority.** P0 — Blocking
6. **Ticket type.** infrastructure
7. **Owner.** data-pipeline-engineer
8. **Supporting agents.** —
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** Framework-private `infrastructure/data/{inspection,partitioning,splitting,preprocessing,materialization}.py` streaming components supporting the P1-T034 data ports; concrete dataset-semantic port implementations are owned by their Phase 2 source/partition/split tickets.
13. **Objective.** Implement the five framework-private `infrastructure/data/` streaming components using PyArrow `dataset.Scanner`/`RecordBatchReader`, bounded pandas conversion for chunked processing, two-pass fitting where required, partitioned Parquet processed artifacts, and source-row lineage with row-order checksums.
14. **Why this ticket exists.** Architecture principle 12 ("memory-safe, batching-first processing") and §4.1's explicit designation of PyArrow as the sole authoritative tabular path exist because no IoT dataset in this project's scope may ever be loaded whole into memory — a whole-dataset load would silently cap the largest client or dataset the pipeline could ever process and would defeat the deterministic-batching guarantees later tickets depend on. Row-order checksums matter concretely: without them, a chunked read and a hypothetical whole-file read could silently disagree on row order under a filesystem or PyArrow-version difference, corrupting every downstream identity that assumes stable ordering.
15. **Authority references.** Architecture §16.3 (chunked/two-pass processing), §4.1 (PyArrow as the authoritative tabular path), §9.6 (framework confinement for data adapters).
16. **Dependencies.** P1-T034, P1-T054, P1-T055, P1-T056, P1-T057.
17. **Blocks.** P2-T004.
18. **Downstream consumers.** P2-T004 (outside this extracted phase) consumes these adapters directly.
19. **Preconditions.** P1-T034, P1-T054, P1-T055, P1-T056, P1-T057 are `DONE`.
20. **Inputs.** Architecture §16.3, §4.1, §9.6.
21. **Allowed scope.** `infrastructure/data/`.
22. **Forbidden actions.** No whole-dataset load anywhere in these adapters; no PyArrow or pandas object crossing upward out of `infrastructure/data/` (these are private carriers, never exposed through a port signature); no runtime chunk-size resize once a stage has started (chunk sizing is frozen per `PreprocessingChunkSpec`, owned exclusively by P1-T020).
23. **Required configuration or NONE.** Chunk sizing comes from the frozen `PreprocessingChunkSpec`.
24. **Implementation responsibilities.** Implement streaming components using `pyarrow.dataset.Scanner`/`RecordBatchReader` with bounded record batches; implement two-pass fitting for preprocessing statistics under the declared `NormalizationScope`; implement partitioned processed-Parquet output per client/split; keep every PyArrow/pandas object strictly private to this package. Concrete source, partition, split, and transform semantics remain owned by P2-T004–P2-T007 and are not invented in Phase 1.
25. **Required unit tests.** NONE beyond adapter construction — behavioral correctness (chunk equivalence, memory bounds, ordering) is verified at the integration level on synthetic data, since these adapters have no meaningful unit-level behavior in isolation from actual data.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE for concrete data-port semantics: P2-T004–P2-T007 explicitly own those implementations. This ticket verifies the reusable streaming substrate and its private-carrier boundary.
28. **Required architecture tests.** No PyArrow or pandas carrier object crosses upward out of `infrastructure/data/`.
29. **Required integration tests.** Chunked-vs-reference equivalence (a full synthetic dataset processed in chunks yields identical results to a single-pass reference computation); bounded peak memory under a declared budget; stable row-group traversal order across repeated runs; source-row lineage preserved end-to-end — all exercised on synthetic data.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Integration lane (data); contract lane; Pyright strict.
33. **Acceptance criteria.** [x] No whole-dataset load anywhere. [x] Chunked processing matches a single-pass reference exactly on synthetic data. [x] Row-group traversal order is stable across repeated runs. [x] PyArrow/pandas carriers never cross the adapter boundary.
34. **Completion evidence.** Changed files: `infrastructure/data/` and its tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** `DatasetError`, `PartitionError`, `SplitError`, and `PreprocessingError` are the typed failure paths; no execution against real data occurs anywhere in Phase 1, so no real-data failure mode is exercised by this ticket's own tests.
36. **Stop conditions.** None — synthetic data only is in scope for Phase 1.
37. **Deliverables.** The five data-infrastructure adapter modules.
38. **Final review checklist.** [x] Bounded memory verified on synthetic data. [x] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [x] Read the complete roadmap and architecture before starting.
- [x] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [x] Verify every dependency is `DONE`.
- [x] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step: 2026-07-15T12:07:16Z — streaming-adapter contract and port-shape inspection.
- [x] Keep both status locations synchronized while active.
- [x] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately. 2026-07-15T12:23:54Z — focused integration, project lint/format/Pyright, import-linter, and architecture lanes passed. 2026-07-15T12:41:48Z — full-project Nox, Sonar, and CodeScene passed; the review identified that P2-T004–P2-T007 explicitly own concrete data-port semantics, so P1-T042 is correctly limited to the completed framework-private streaming substrate.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [x] Change status to `DONE` only after every criterion and gate passes; recorded 2026-07-15T15:50:16Z with synchronized register evidence.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for a PyArrow or pandas object crossing upward out of `infrastructure/data/`, a whole-dataset load anywhere, dependency-direction violations, and hidden defaults. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw pandas `DataFrame`/PyArrow `Table` exposed through a port-facing signature instead of a typed result, `Any`/unjustified `cast`, and a hardcoded chunk-size literal duplicated outside `PreprocessingChunkSpec`.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the contract lane (line 27), architecture lane (line 28), and integration lane (line 29), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Unit/property/CUDA/system categories are `NONE` beyond the baseline contract checks, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `infrastructure/data/` must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

Check specifically for a nondeterministic row-group traversal order across repeated runs, an unstable chunk boundary that could shift under a filesystem timing difference, and a silent whole-dataset fallback under memory pressure (forbidden). This is a primary determinism-relevant ticket, not `NOT_APPLICABLE` — reproducible row ordering underlies every downstream fingerprint that depends on processed-split identity.

### B11. Phase 1 boundary confirmation

Confirm no real N-BaIoT/Edge-IIoTset/CICIoT2023 data is read anywhere in this ticket's implementation or its tests — every equivalence/memory/ordering test in field 29 runs on deterministic synthetic arrays only. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — this ticket implements a streaming data adapter, not a scientific claim.` Positive finding required: confirm no synthetic-data test result in this ticket's scope is described anywhere as a real-dataset finding, and confirm chunked-vs-reference equivalence protects the roadmap's requirement that processed splits are reproducible regardless of execution parallelism.

**Audit 2 — Architecture fidelity.** Confirm the five adapters match Architecture §5.1's module map exactly and that PyArrow/pandas usage is confined per §4.1's library-confinement table.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a chunk-boundary edge case (a record batch splitting exactly at a client or split boundary) silently duplicate or drop a row, and would the chunked-vs-reference equivalence test on synthetic data actually catch that specific edge case, or only a generic happy-path case? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.

### Completion evidence — 2026-07-15T15:50:16Z

- Scope reconciliation: P2-T004 owns the concrete N-BaIoT `DatasetSourceInspector` behind the P1-T034 port, and P2-T005–P2-T007 own the concrete partition/split/preprocessing semantics. Keeping those semantics out of P1 prevents invented real-dataset behavior and honors the Phase 1 synthetic-only boundary.
- Delivered and tested the reusable PyArrow bounded-stream substrate, bounded pandas chunks, deterministic row-order checksums, exact two-pass numeric profiling, private client/split stream carriers, and partitioned Parquet materialization.
- `pytest -q tests/integration/data/test_pyarrow_streaming.py` passed all seven synthetic integration tests: bounded batches/pandas chunks, reference-equivalent statistics, stable order, source-row lineage, materialization order, and two-pass reproducibility.
- Current project gates also pass: strict Pyright, Ruff lint/format, import-linter, architecture tests, two randomized full suites, Nox, Sonar, and CodeScene. Pylance has no executable in this environment; Pyright strict is clean.
- B12 audit 1: NOT_APPLICABLE/PASS — synthetic equivalence is never represented as a real-data finding. B12 audit 2: PASS — PyArrow/pandas carriers stay private to infrastructure. B12 audit 3: PASS — boundary-splitting synthetic tests preserve every row exactly once and stable ordering is checked twice.
- Unit/property/CUDA/system tests are `NONE` under Part A; no real data was read, trained, scored, or rendered. No blocker or remaining P1-T042 risk remains.
