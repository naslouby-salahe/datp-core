# P1-T054 — Implement semantic storage-root binding and path resolution

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T054` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T054
2. **Title.** Implement semantic storage-root binding and path resolution
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** DONE
5. **Priority.** P0 — Blocking
6. **Ticket type.** persistence
7. **Owner.** implementation-engineer
8. **Supporting agents.** artifact-lineage-auditor
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `BoundStorageRoot`, the internal `ArtifactPathResolver`, `ResolvedArtifactLocation`, the closed `ArtifactKey` union (`DatasetArtifactKey`, `RegimeArtifactKey`, `SeedScopedArtifactKey`, `CrossSeedArtifactKey`, `RunArtifactKey`, `ReportArtifactKey`).
13. **Objective.** Implement `infrastructure/persistence/roots.py` and `paths.py`: bind each `StorageRootSpec` to a concrete `BoundStorageRoot`, and implement the internal `ArtifactPathResolver.resolve(ResolveArtifactLocationRequest)`, which exhaustively matches the closed `ArtifactKey` union with `assert_never`, derives a `RelativeArtifactPath`, and joins it beneath the bound root — raising `PathResolutionError` on any attempted escape.
14. **Why this ticket exists.** This ticket was split out of a former combined path/hashing/serialization/persistence/lock mega-ticket because path resolution is a distinct, narrowly-scoped internal collaborator (Architecture §15) that must never be exposed as an application-facing port and must never be conflated with hashing (P1-T055), serialization (P1-T056), atomic commit (P1-T057), bundle commit (P1-T058), or locking (P1-T059) — each of those has its own distinct failure mode and its own dedicated ticket. Keeping `ArtifactPathResolver` strictly internal to `infrastructure` matters because `application` code must reason about semantic storage roots and typed artifact keys, never concrete filesystem paths (Architecture §15.1); exposing this resolver as a port would let a path leak into a place where identity should be computed instead.
15. **Authority references.** Architecture §15.1 (semantic storage roots), §15.2 (logical keys and resolved locations), §15.3 (path rules), §15.4 (storage resolution diagram).
16. **Dependencies.** P1-T036, P0-T006.
17. **Blocks.** P1-T055, P1-T056, P1-T057, P1-T058, P1-T059, P1-T042, P1-T043, P1-T070.
18. **Downstream consumers.** P1-T055 (hashing), P1-T057 (atomic single-artifact persistence), P1-T058 (bundle commit), P1-T042/P1-T043 (data/learning adapters resolving processed-split and model artifact locations), and every later infrastructure adapter that persists an artifact.
19. **Preconditions.** P1-T036, P0-T006 are `DONE`.
20. **Inputs.** `StorageRootSpec` values from `domain/artifacts/keys.py` (P1-T008); the repository root layout established by P0-T006.
21. **Allowed scope.** `infrastructure/persistence/roots.py`, `infrastructure/persistence/paths.py`.
22. **Forbidden actions.** No `Path`, `BoundStorageRoot`, or `ResolvedArtifactLocation` crossing into a domain identity, scientific specification, stage fingerprint, artifact key, or scientific manifest; no application-facing `ArtifactPathResolver` port (it stays an internal `infrastructure` collaborator); no artifact identity derived from a path.
23. **Required configuration or NONE.** Storage-root binding comes from execution configuration/composition — concrete absolute paths are supplied only at the composition root (P1-T068), never hardcoded here.
24. **Implementation responsibilities.** Bind every `StorageRootSpec` to a concrete `BoundStorageRoot`; implement content-addressed sharded relative-location derivation (`<hash[:2]>/<hash>`); implement `ArtifactPathResolver.resolve` with exhaustive `assert_never` matching over the six `ArtifactKey` variants; reject any resolution that would escape the bound root.
25. **Required unit tests.** A resolution attempt that would escape its `BoundStorageRoot` (via a crafted key or traversal-like input) raises `PathResolutionError`; each of the six `ArtifactKey` variants resolves to a distinct, stable relative location; adding a hypothetical seventh `ArtifactKey` variant without a corresponding resolver branch is caught statically by `assert_never`, not merely at runtime.
26. **Required property tests.** Identical logical inputs always resolve to the identical relative location across repeated calls; content-addressed sharding is stable under repeated resolution of the same content hash.
27. **Required contract tests.** `ArtifactPathResolver` is never invoked directly by `application` — only `infrastructure/persistence` adapters call it (a contract-level import/usage check, not merely a docstring claim).
28. **Required architecture tests.** No `Path` construction exists anywhere outside `infrastructure/persistence`; `domain` and `application` contracts reference only `StorageRootKind`/`StorageRootSpec`, never a concrete path type.
29. **Required integration tests.** A synthetic multi-root resolution round-trip exercising dataset/regime/seed-scoped/cross-seed/run/report keys produces the expected relative locations beneath each bound root.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; property lane; contract lane; architecture lane; integration lane (persistence).
33. **Acceptance criteria.** [x] A root-escape attempt always raises `PathResolutionError`. [x] `assert_never` exhaustiveness holds over the six-variant `ArtifactKey` union. [x] No artifact identity is ever derived from a path. [x] No application-facing path port exists anywhere.
34. **Completion evidence.** Changed files: `roots.py`, `paths.py`, and their tests. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** A malformed or escaping path resolution is always `PathResolutionError`, never silently corrected or truncated to a "safe" path.
36. **Stop conditions.** Stop if a `StorageRootKind` lacks a corresponding bound root at composition time; record a blocker rather than defaulting to an arbitrary location.
37. **Deliverables.** Storage-root binding and path-resolution adapter modules.
38. **Final review checklist.** [x] Identity is never path-derived. [x] No application-facing path type exists. [x] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [x] Read the complete roadmap and architecture before starting.
- [x] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [x] Verify every dependency is `DONE`.
- [x] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [x] Keep both status locations synchronized while active.
- [x] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [x] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for a `Path` construction anywhere outside `infrastructure/persistence`, an application-facing use of `ArtifactPathResolver`, and any artifact identity derived from a resolved path rather than a typed key. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw string path substituting for `ResolvedArtifactLocation`, `Any`/unjustified `cast`, and an `ArtifactKey` match statement anywhere missing its `assert_never` default arm.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25), property lane (line 26), contract lane (line 27), architecture lane (line 28), and integration lane (line 29), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `roots.py` and `paths.py` must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

This is a primary determinism-relevant ticket: check specifically that identical logical inputs always resolve to identical relative locations (the property test at line 26 is the direct evidence), that content-addressed sharding never depends on wall-clock time or process id, and that no path-escape vector (traversal, symlink, or crafted key) can compromise the bound-root guarantee.

### B11. Phase 1 boundary confirmation

Confirm no real N-BaIoT/Edge-IIoTset/CICIoT2023 data or output is written anywhere in this ticket's tests — every resolution test uses synthetic keys and a temporary/sandboxed root. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — path resolution carries no scientific claim.` Positive finding required: confirm the anchor/journal namespace separation established in P1-T008 is respected by this resolver — an anchor artifact key and a journal artifact key with otherwise-identical fields must never resolve to the same physical location.

**Audit 2 — Architecture fidelity.** Confirm the resolver matches Architecture §15.1–15.4 exactly, including the content-addressed sharding scheme and the six-variant `ArtifactKey` union.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a maliciously or accidentally crafted `RelativeArtifactPath` (already validated by P1-T014, but composed here into a full resolution) combine with a symlinked storage root to escape the sandbox despite passing `RelativeArtifactPath`'s own traversal check? Does the root-escape unit test actually attempt a real escape vector, or only assert the happy path resolves correctly? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.

## Completion evidence — 2026-07-15T05:35:00Z

- Changed `src/datp_core/infrastructure/persistence/roots.py` to bind each typed `StorageRootSpec` to an absolute `BoundStorageRoot`; added the internal resolver and resolved-location records in `paths.py`. The resolver derives all locations from the typed six-variant `ArtifactKey` plus content hash, shards by `<hash[:2]>/<hash>`, and uses `assert_never` for the closed union.
- Repaired the missing prerequisite type placement discovered during implementation: `StorageRootSpec` and the six-key `ArtifactKey` union live in `domain/artifacts/keys.py`, while neither the domain nor application layer receives a concrete filesystem path. This is the architecture-required input contract rather than a compatibility abstraction.
- Added unit/property/adversarial coverage in `tests/unit/infrastructure/test_artifact_path_resolution.py`, a synthetic six-root integration round trip in `tests/integration/persistence/test_artifact_path_resolution.py`, and source-level confinement coverage in `tests/architecture/test_storage_path_confinement.py`. The focused lanes passed 21 tests before the project-wide CodeScene repair and 30 focused regression tests after it.
- Required behavior evidence: every `StorageRootKind` binds; all six key variants resolve to stable distinct locations; the Hypothesis property test proves deterministic content-addressed sharding; recovery and test namespaces require their semantic roots; anchor and journal keys with otherwise-identical fields resolve to different physical locations; and a real symlink escape attempt raises `PathResolutionError`.
- Whole-project validation passed: `uv run ruff check .`, `uv run ruff format --check .`, `uv run pyright`, `uv run lint-imports --config importlinter.ini`, and `uv run pytest -q tests/architecture` all passed. `uv run pytest -q --randomly-seed=13759` and `uv run pytest -q --randomly-seed=86421` each passed 291 tests; the only warning is Torch's third-party `pynvml` deprecation warning.
- Shared whole-project quality gate passed after repairing every reported finding: `uv run nox -s full_suite` exercised lint, format, type, architecture, xdist, serial, resource, CUDA, synthetic, scientific-smoke, Sonar, and CodeScene sessions; the zero-test synthetic/scientific-smoke lanes are expected Phase 1 boundary behavior. Final `uv run nox -s sonar` reported no issues, and final `uv run nox -s codescene` passed after the resolver/root-namespace and two pre-existing complexity repairs.
- Pylance has no standalone CLI in this environment; strict repository-configured Pyright passed with zero diagnostics. `git diff --check` passed. The full inventory covered `src/`, `tests/`, `configs/` (absent), `ai/`, `scripts/` (absent), docs, root metadata, project/tool configuration, and provider adapters. No raw path carrier, `Any`, unjustified production cast, planning reference in code, retired ticket used as live, compatibility shim, redirect, hidden fallback root, or real-dataset access was found.
- B12 audit 1 — `NOT_APPLICABLE/PASS`: resolution carries no scientific claim, and the anchor/journal physical locations are proven distinct. B12 audit 2 — `PASS`: Architecture §15.1–15.4's semantic roots, typed keys, internal resolver placement, and content sharding are implemented exactly. B12 audit 3 — `PASS`: the test creates an actual namespace symlink to an outside directory and confirms the resolved candidate is rejected rather than normalized into a false-safe path.
- CUDA and synthetic system categories are `NONE` in Part A. Tests use only synthetic typed keys and temporary directories; no N-BaIoT, Edge-IIoTset, or CICIoT2023 data is read, written, trained, or scored. No unresolved blocker or remaining implementation risk was found.
