# P1-T004 — Implement metric-family enums and the MetricId union

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T004` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T004
2. **Title.** Implement metric-family enums and the MetricId union
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** domain
7. **Owner.** statistics-auditor
8. **Supporting agents.** —
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `MetricFamily`, `OperatingPointMetric` (includes `CV_FPR`), `DetectionQualityMetric` (includes `AUROC`, control-only), `EquityMetric`, `EstimationMetric`, `ClusterMetric`, `DistributionMetric`, `DiagnosticRatio`, `ResourceMetric`, the disjoint `MetricId` union, `MetricSpec`.
13. **Objective.** Implement `domain/evaluation/metrics.py`: the eight metric-family `StrEnum`s of field 12, the `type MetricId = ...` disjoint union over them, and `MetricSpec` (a frozen dataclass carrying `family`, `is_control`, `needs_eligible_only`, `higher_is_better` per `MetricId`).
14. **Why this ticket exists.** This vocabulary makes the roadmap's CV(FPR)-primary/AUROC-control identity a structural, type-checked fact rather than a convention: `MetricSpec` for `CV_FPR` must carry the primary/eligible-only flags, and `MetricSpec` for `AUROC` must carry `is_control = True`, so no future code path can silently treat AUROC as the thresholding verdict. Disjoint families make the `MetricId` union unambiguous — a metric identifier belongs to exactly one family, never two.
15. **Authority references.** Architecture §6.1 (metric-family enum table), §6.8 (`MetricId` union), §9.3 (`MetricSpec` fields), §11.4 (primary metric must be `CV_FPR`). Roadmap §10 (CV(FPR) primary, AUROC control metric).
16. **Dependencies.** P0-T026.
17. **Blocks.** P1-T026.
18. **Downstream consumers.** P1-T026 (evaluation, operating-point, and alert-burden result types) is the sole immediate consumer; every metric-reporting specification in later phases depends on this vocabulary transitively.
19. **Preconditions.** P0-T026 is `DONE`.
20. **Inputs.** Architecture §6.1, §6.8, §9.3, §11.4; Roadmap §10.
21. **Allowed scope.** `domain/evaluation/metrics.py`; the alert-burden-specific enums live in `domain/evaluation/alert_burden.py` and are P1-T014's scope, not this ticket's.
22. **Forbidden actions.** No metric identifier appearing in more than one family (breaks union disjointness); no `AUROC` member lacking `is_control = True` in its `MetricSpec`; no generic metric bag replacing the typed union.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Define the eight family `StrEnum`s exactly as named in Architecture §6.1; define `type MetricId = (OperatingPointMetric | DetectionQualityMetric | EquityMetric | EstimationMetric | ClusterMetric | DistributionMetric | DiagnosticRatio | ResourceMetric)`; define `MetricSpec` as a frozen, slotted, keyword-only dataclass; populate a `MetricMap`-compatible template (the collection type itself is P1-T015's scope) assigning each `MetricId` its correct flags.
25. **Required unit tests.** Family disjointness (no metric id appears in two families); `MetricSpec` for `CV_FPR` carries `is_control = False`, and `needs_eligible_only = True`; `MetricSpec` for `AUROC` carries `is_control = True`.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** Framework confinement for `domain/evaluation/metrics.py`; no framework import.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Pyright strict; architecture lane; unit lane for `domain/evaluation/metrics.py`.
33. **Acceptance criteria.** [ ] Metric ids disjoint across families. [ ] `AUROC` is control-only by its `MetricSpec` flag, not by convention. [ ] `MetricSpec` carries all four flags for every `MetricId`. [ ] The union is unambiguous.
34. **Completion evidence.** Changed files: `domain/evaluation/metrics.py` and its unit tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** A non-metric key passed where a `MetricId` is expected is rejected by the typed collection built in P1-T015, not here; no other failure is expected in this ticket's own scope.
36. **Stop conditions.** None.
37. **Deliverables.** Metric vocabulary and `MetricSpec` in `domain/evaluation/metrics.py`.
38. **Final review checklist.** [ ] `CV_FPR` primary; `AUROC` control. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition, recording the exact blocker and unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files. No defect is dismissible as pre-existing, someone else's, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

### B3. Architecture boundary audit

Check the complete project for dependency-direction violations, framework leakage, config-boundary leaks, cycles/side effects, service locators, incorrect type placement, duplicate abstractions, forbidden module names, backward-compatibility scaffolding, hidden defaults, and out-of-phase behavior. Run import-linter/pytest-archon; record results. Confirm `domain/evaluation/metrics.py` imports nothing beyond the standard library.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for raw strings standing in for `MetricId`, object-shaped dictionaries, `dict[str, Any]`/`Any`/unjustified `cast`, boolean flags replacing discriminated variants, and incomplete enum dispatch. Confirm no caller hardcodes `"cv_fpr"`/`"auroc"` as a string where `MetricId` is available.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, stale comments, and unverified "checks run" claims. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25) and architecture lane (line 28), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/contract/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; this ticket's module must pass strict typing with no unexplained `Any` or suppression.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — this ticket introduces closed string enums and a frozen flag-carrying dataclass only; no seed, CUDA, or fingerprinted numeric field exists in its scope.` Confirm no such path was introduced regardless.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm `CV_FPR` is marked primary/eligible-only and `AUROC` is marked control-only in `MetricSpec`, matching Roadmap §10 exactly; confirm no metric elevates AUROC to a thresholding verdict anywhere in this ticket's scope.

**Audit 2 — Architecture fidelity.** Confirm `domain/evaluation/metrics.py` matches Architecture §5.1/§6.8 exactly and imports nothing beyond the standard library.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: does the `AUROC`-is-control test actually assert the `MetricSpec.is_control` flag, or merely that the enum member exists? Does any family overlap silently with another? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.
