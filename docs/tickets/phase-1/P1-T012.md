# P1-T012 — Implement identity, seed-plan, and stage-fingerprint value objects

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T012` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T012
2. **Title.** Implement identity, seed-plan, and stage-fingerprint value objects
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** domain
7. **Owner.** artifact-lineage-auditor
8. **Supporting agents.** reproducibility-auditor
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `ExperimentId`, `ArchitectureCatalogueId`, `CellId`, `ArtifactId`, `RunIdentity`, `ExecutionAttemptId`, `StageRunIdentity`, `CheckpointId`, `StageFingerprint`, `Seed`, `RoundNumber`, `SeedTuple`, `SeedRoleTuple`, `SeedMap`, `ConfirmatorySeedCohort`, `derive_seed`.
13. **Objective.** Implement `ExperimentId`/`ArchitectureCatalogueId`/`CellId`/`ArtifactId`/`RunIdentity`/`ExecutionAttemptId`/`StageRunIdentity`/`CheckpointId`/`StageFingerprint`/`Seed`/`RoundNumber` with their validation regexes and cardinalities, the seed collections `SeedTuple`/`SeedRoleTuple`/`SeedMap`/`ConfirmatorySeedCohort`, and the `derive_seed(experiment_seed, role, stage_fingerprint)` function.
14. **Why this ticket exists.** `RunIdentity` and `ExecutionAttemptId` must be distinct, non-substitutable types: the master log's Section B states that a new execution attempt continues the same `RunIdentity` when scientific configuration is unchanged, while any scientific-configuration change produces a new `RunIdentity` — conflating the two types would make an operational retry indistinguishable from a genuine new scientific run. `ArtifactId` must be deterministically derived (type, key, stage identity, namespace), never randomly generated, so identical scientific content always yields the same identity for reuse comparison. `ConfirmatorySeedCohort` enforces exactly ten paired seeds for the journal confirmatory endpoint without ever inventing or guessing seed values — the ten seeds themselves come from the resolved authoritative seed specification, never from this ticket.
15. **Authority references.** Architecture §7 (identity value-object table), §7.2 (per-stage identity dataclasses, forward reference to P1-T013), §10 (seed collections), §16.7 (`derive_seed`), §13.5 (fingerprint stability). Roadmap §3, §10 (ten paired seeds for the confirmatory endpoint; distinct anchor/journal seed cohorts, master log Section A).
16. **Dependencies.** P1-T009.
17. **Blocks.** P1-T013.
18. **Downstream consumers.** P1-T013 (per-stage nominal identity dataclasses) wraps `StageFingerprint` from this ticket for every stage identity; every artifact/run/checkpoint identity in later phases depends on this ticket transitively.
19. **Preconditions.** P1-T009 is `DONE`.
20. **Inputs.** Architecture §7, §7.2, §10, §16.7, §13.5; Roadmap §3, §10.
21. **Allowed scope.** `domain/experiments/identities.py`, `domain/artifacts/references.py`, `domain/runtime/seeds.py`.
22. **Forbidden actions.** No random scientific `ArtifactId` (randomness is permitted only for the operational `ExecutionAttemptId`); no seed value invented or hardcoded anywhere in this ticket; no interchange of `RunIdentity` and `ExecutionAttemptId`; no `type X = StageFingerprint` PEP 695 alias used as an identity guard (that would not prevent substitution — nominal dataclasses are required, built in P1-T013).
23. **Required configuration or NONE.** NONE — seed values themselves come from resolved configuration (later phases), not from this ticket.
24. **Implementation responsibilities.** Implement regex/format validation for each string-backed identity; implement `CellId = <ExperimentId>#<hash16>` derived from the canonical resolved cell specification (never raw configuration text or unordered fields); implement `ConfirmatorySeedCohort` with a cardinality invariant of exactly ten paired seeds; implement `derive_seed(experiment_seed, role, stage_fingerprint)` as a pure, deterministic function.
25. **Required unit tests.** `ConfirmatorySeedCohort` construction with fewer or more than ten paired seeds is rejected; random-identifier prohibition for every scientific `ArtifactId` construction path (an explicit test asserting determinism, not randomness).
26. **Required property tests.** Identity-format validation across generated valid/invalid strings; `derive_seed` is deterministic (same inputs always yield the same seed) and produces distinct seeds for distinct `(role, stage_fingerprint)` pairs.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE beyond the baseline framework-confinement check.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Property lane; unit lane; Pyright strict.
33. **Acceptance criteria.** [ ] `RunIdentity` and `ExecutionAttemptId` are distinct, non-substitutable types. [ ] Every scientific identifier is deterministic, never random. [ ] `ConfirmatorySeedCohort` requires exactly ten paired seeds. [ ] `derive_seed` is deterministic. [ ] No seed value is hardcoded anywhere in this ticket.
34. **Completion evidence.** Changed files: `domain/experiments/identities.py`, `domain/artifacts/references.py`, `domain/runtime/seeds.py`, and their tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** `DomainValidationError` on a malformed identifier or an incorrectly sized cohort. Stop and raise a blocker if the authoritative seed-specification format itself is unresolved by either authority — never invent seed values or a seed-count to fill the gap.
36. **Stop conditions.** Stop if the authoritative seed-specification format is unresolved; raise a blocker rather than inventing values.
37. **Deliverables.** Identity and seed value objects, plus `derive_seed`.
38. **Final review checklist.** [ ] No invented seeds anywhere. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

### B3. Architecture boundary audit

Check the complete project for dependency-direction violations, framework leakage, unstable identity construction (specifically: any `ArtifactId` derived from randomness or from a mutable path), cycles/side effects, and hidden defaults. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw string standing in for `RunIdentity`/`ExecutionAttemptId`, unstable identity construction, `Any`/unjustified `cast`, and a `type X = StageFingerprint` alias used anywhere as an identity guard.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the property lane (line 26) and unit lane (line 25), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Contract/architecture-specific/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; these modules must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

This ticket is a primary determinism-surface owner: check specifically for scattered global seeds, seed-list position used as identity, and a seed derived non-deterministically. Confirm `derive_seed` is pure (no hidden state, no environment dependency) and that `RunIdentity` never incorporates an execution-attempt-specific (operational) value.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced, and specifically that no seed value used anywhere in this ticket's tests is presented as an authoritative campaign seed. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm `ConfirmatorySeedCohort` requires exactly ten paired seeds (Roadmap §3, §10) and that no seed value is invented anywhere in this ticket; confirm the anchor and journal seed cohorts (master log Section A) are never conflated by any shared type here.

**Audit 2 — Architecture fidelity.** Confirm every identity type matches Architecture §7's table exactly, including its "prevents" and "distinct from" columns, and that `derive_seed`'s signature matches §16.7.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could `RunIdentity` be reconstructed from an `ExecutionAttemptId` (or vice versa) by any caller, defeating the distinction the master log's Section B relies on? Does the ten-seed cohort test actually reject nine or eleven, not just assert "ten works"? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.
