# P1-T068 — Implement the composition root and strategy registries

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T068` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T068
2. **Title.** Implement the composition root and strategy registries
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** composition
7. **Owner.** implementation-engineer
8. **Supporting agents.** architecture-cleaner
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `composition/root.py` use-case entrypoints (for example `run_experiment`), `composition/registries.py` (`EnumMap` strategy registries, `StageRunnerRegistry`).
13. **Objective.** Implement `composition/root.py` (construct every concrete adapter, bind each to its `application/ports/*` Protocol, assemble the concrete application services, select explicit strategy implementations, map resolved configuration into the object graph, and expose complete use-case entrypoints) and `composition/registries.py` (`EnumMap` strategy registries populated exactly once, plus the exhaustive `StageRunnerRegistry`).
14. **Why this ticket exists.** This ticket was split out of a former combined composition/CLI mega-ticket because `composition` and `cli` are distinct architectural layers with distinct responsibilities and distinct forbidden actions (Architecture §3.1, §12.5): `composition` is the sole wiring place and the only layer with edges to every other layer, while `cli` (P1-T069) has exactly one outbound edge and must never construct anything itself — a single ticket type cannot honestly describe both without conflating them. This is also the ticket where every one of the twenty infrastructure and application tickets it depends on finally converges into one working object graph, making it the natural place a missing binding first becomes visible as a construction-time failure rather than a runtime surprise deep inside a stage.
15. **Authority references.** Architecture §3.1 (composition depends on every layer), §3.2 (forbidden: a direct framework import inside composition), §12.5 (composition-root/CLI separation), §12.6 (stage dispatch and rendering), §29.2 (composition is never the CLI's responsibility).
16. **Dependencies.** P1-T039, P1-T042, P1-T043, P1-T044, P1-T045, P1-T046, P1-T054, P1-T055, P1-T056, P1-T057, P1-T058, P1-T059, P1-T060, P1-T061, P1-T062, P1-T063, P1-T064, P1-T065, P1-T066, P1-T067.
17. **Blocks.** P1-T069, P1-T050, P1-T070.
18. **Downstream consumers.** P1-T069 (the CLI boundary invokes composition use cases), P1-T050 (the architecture-boundary test suite verifies composition's exclusive wiring role), P1-T070 (the socle-readiness gate exercises a use case end-to-end).
19. **Preconditions.** All twenty listed infrastructure and application tickets are `DONE`.
20. **Inputs.** Every application port declared in P1-T034–P1-T036, and every infrastructure adapter listed in the Dependencies field.
21. **Allowed scope.** `composition/root.py`, `composition/registries.py`.
22. **Forbidden actions.** No direct framework import anywhere in `composition` itself (frameworks are reached only through the `infrastructure` adapters composition constructs, never imported by `composition` code directly); no service-locator pattern anywhere; no scientific computation performed by composition itself — composition only wires, it never computes.
23. **Required configuration or NONE.** The resolved configuration (scientific, execution, artifact, and reporting specs) is mapped into the object graph here.
24. **Implementation responsibilities.** Construct every concrete adapter; bind each to its `application/ports/*` Protocol; assemble the concrete application services (`ExperimentPlanner`, `ExecutionPreflight`, `ScoreReuseGate`, `CheckpointSelector`, `PolicyEvaluator`, `AnchorReproductionGate`, `FeasibilityGateEvaluator`, `PlanExecutor`, `TableFigureTracer`); populate `composition/registries.py`'s `EnumMap` strategy registries (for example, `ThresholdConstructionKind` → `ThresholdStrategy` implementation) and the exhaustive `StageRunnerRegistry`; expose `run_experiment(cell: ExperimentCell) -> ExecutionSummary` and any other complete use-case entrypoints.
25. **Required unit tests.** Every use-case entrypoint resolves its complete object graph with no missing binding; `StageRunnerRegistry` is exhaustive over `PipelineStage` — a missing stage handler is a construction-time failure, never a silent runtime gap.
26. **Required property tests.** NONE.
27. **Required contract tests.** Every registered strategy implementation satisfies its declared port's contract.
28. **Required architecture tests.** No framework import appears directly in `composition/root.py` or `registries.py` outside of constructing a named `infrastructure` adapter; `composition` is the only node in the layer graph with edges to every other layer.
29. **Required integration tests.** A synthetic use case (with fake or test-double infrastructure where needed) runs end-to-end through the fully composed object graph.
30. **Required CUDA tests.** NONE — CUDA-specific behavior is exercised by the adapters themselves; composition only wires them together.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; contract lane; architecture lane; integration lane (synthetic).
33. **Acceptance criteria.** [ ] Composition is the sole wiring place in the entire codebase. [ ] The registries are exhaustive over `PipelineStage`. [ ] No framework import exists directly inside `composition` code. [ ] Every exposed use case resolves fully with no missing binding.
34. **Completion evidence.** Changed files: `composition/root.py`, `registries.py`, and their tests. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** A missing binding or an unregistered stage handler is always a construction-time failure, never a silent runtime gap discovered mid-pipeline.
36. **Stop conditions.** Stop if a required infrastructure adapter dependency is genuinely incomplete; never wire a partial adapter into the object graph.
37. **Deliverables.** The composition root and strategy registries.
38. **Final review checklist.** [ ] No service-locator pattern exists anywhere. [ ] No framework leakage into composition itself. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for a direct framework import anywhere inside `composition/root.py`/`registries.py` outside constructing a named adapter, a service-locator pattern anywhere, and scientific computation performed by composition itself rather than delegated to a service it wires. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw dict standing in for an `EnumMap` strategy registry, `Any`/unjustified `cast`, and an incomplete `StageRunnerRegistry` missing a handler for any `PipelineStage` member.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25), contract lane (line 27), architecture lane (line 28), and integration lane (line 29), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `composition/` must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

Check specifically that object-graph construction itself is deterministic (the same resolved configuration always produces an equivalent object graph) and that no registry population depends on module-import order in a way that could silently bind the wrong strategy implementation to an enum key.

### B11. Phase 1 boundary confirmation

Confirm no real N-BaIoT/Edge-IIoTset/CICIoT2023 execution occurs anywhere in this ticket — the synthetic integration test at line 29 exercises the composed object graph with test-double or fake infrastructure only. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — composition wiring carries no direct scientific claim.` Positive finding required: confirm the strategy registry correctly separates the causal-ladder threshold strategies (B1–B4) from the stress-test/comparator strategies (FedProx, `B-FedStatsBenign`, model personalization), so no wiring error could accidentally substitute one for the other.

**Audit 2 — Architecture fidelity.** Confirm composition matches Architecture §3.1/§12.5/§12.6 exactly: it is the sole node with edges to every layer, and it never performs scientific computation itself.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could two strategy registrations for the same enum key silently overwrite each other during registry population, with the second one winning without any warning? Does the "exhaustive over `PipelineStage`" test actually enumerate every stage member and confirm a handler exists, or does it only spot-check a few? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is the canonical owner of object-graph composition and strategy-registry population for the entire Phase 1 socle and every phase built on top of it — every later ticket that adds a new strategy implementation registers it here. It must otherwise remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.
