# P1-T026 — Implement evaluation, operating-point, and alert-burden result types

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T026` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T026
2. **Title.** Implement evaluation, operating-point, and alert-burden result types
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** DONE
5. **Priority.** P0 — Blocking
6. **Ticket type.** domain
7. **Owner.** statistics-auditor
8. **Supporting agents.** experiment-engineer
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `ClientEvaluationResult`, `ValidCvResult`/`UndefinedCvResult`/`CvOutcome`, `EligibleClientSet`, `EligibilityCoverageResult`, `ConformalCoverageResult`, `FleetDispersionResult`, `FleetDetectionResult`, `FleetEquityResult`, `ClusterDispersionResult`, `PolicyEvaluationResult`, `TemporalPolicyEvaluationResult`, `TrafficRateEvidence` union (`MeasuredTrafficRateEvidence`/`CitedTrafficRateEvidence`), `AlertBurdenResult`, `MetricSpec`.
13. **Objective.** Implement `ClientEvaluationResult`, `ValidCvResult`/`UndefinedCvResult`/`CvOutcome`, `EligibleClientSet`, `EligibilityCoverageResult`, `ConformalCoverageResult`, `FleetDispersionResult`, `FleetDetectionResult`, `FleetEquityResult`, `ClusterDispersionResult`, `PolicyEvaluationResult`, `TemporalPolicyEvaluationResult`, the closed `TrafficRateEvidence` union, `AlertBurdenResult`, and `MetricSpec` wiring, all as frozen result types.
14. **Why this ticket exists.** Architecture §9.3 requires that `EligibleClientSet` be constructed once per paired policy comparison from shared calibration lineage and supplied unchanged to every compared policy — a policy-specific eligible population would let a favorable exclusion silently shift a comparison's denominator, which is exactly the loophole Architecture §29.4 (invalid state 19) rules out. The same section requires zero-mean CV degeneracy to be a typed `UndefinedCvResult`, never an exception, because an expected statistical degeneracy that raises is indistinguishable from a genuine bug and risks being "fixed" by silently substituting a different value. `AlertBurdenResult` must be evidence-gated because Roadmap SB-20 forbids presenting a hypothetical alert/day number as a measurement — evidence absence must fail construction, not silently default to zero or an assumed rate.
15. **Authority references.** Architecture §9.3 (evaluation and statistics result types: `ClientEvaluationResult`, `CvOutcome`, `EligibleClientSet`, `EligibilityCoverageResult`, `ConformalCoverageResult`, `FleetDispersionResult`, `FleetDetectionResult`, `FleetEquityResult`, `ClusterDispersionResult`, `PolicyEvaluationResult`, `TemporalPolicyEvaluationResult`, `TrafficRateEvidence`, `AlertBurdenResult`, `MetricSpec`), §29.1 (protocol-wide eligibility rule owned once; eligibility/conformal coverage disjoint identities), §29.4 (invalid states 19–20: policy-specific eligible population or eligibility/conformal substitution; alert burden without valid evidence). Roadmap §10 (Metrics and Statistical Plan: CV(FPR) eligibility over `n_k ≥ n_min = 100`; alert burden only with a real or cited traffic rate; SB-20).
16. **Dependencies.** P1-T004, P1-T014, P1-T016, P1-T018.
17. **Blocks.** P1-T028, P2-T016.
18. **Downstream consumers.** P1-T028 (scientific-protocol and policy aggregates) and P2-T016 (outside this extracted phase) consume these result types directly.
19. **Preconditions.** P1-T004, P1-T014, P1-T016, P1-T018 are `DONE`.
20. **Inputs.** Architecture §9.3, §29.1, §29.4; Roadmap §10.
21. **Allowed scope.** `domain/evaluation/operating_points.py`, `domain/evaluation/alert_burden.py`.
22. **Forbidden actions.** No policy-specific eligible population (every paired comparison reuses one persisted `EligibleClientSet`); no substitution of eligibility coverage for conformal coverage or vice versa; no `AlertBurdenResult` constructible without valid `TrafficRateEvidence`; no exception raised for an expected zero-mean CV (it is always a typed `UndefinedCvResult`).
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Implement all listed result types as frozen dataclasses; construct `EligibleClientSet` exactly once per paired policy comparison from shared calibration lineage, and pass it unchanged to every compared policy; implement `UndefinedCvResult` carrying absolute-dispersion companions (IQR, range) and a `ClaimOutcome` (`wording_outcome`) alongside its `reason` and `mean_value`, never as an exception; keep `EligibilityCoverageResult` and `ConformalCoverageResult` as structurally distinct types with no shared substitutable field; require `TrafficRateEvidence` (measured or cited) before any `AlertBurdenResult` can be constructed.
25. **Required unit tests.** Two paired policies evaluated over the same roster share exactly one `EligibleClientSet` instance/identity, not two independently constructed ones; `EligibilityCoverageResult` and `ConformalCoverageResult` are not interchangeable and cannot be substituted for one another at any call site; `AlertBurdenResult` construction fails without valid `TrafficRateEvidence` and succeeds with either the measured or cited variant.
26. **Required property tests.** Confusion-count derivations are recomputable: for every generated `ClientEvaluationResult`, `benign_test_count == TN + FP` and `attack_test_count == TP + FN`, and every derived rate/precision/recall/F1/balanced-accuracy value is reproducible purely from the stored counts and the declared zero-denominator policy.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE beyond the baseline framework-confinement check.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; property lane; Pyright strict.
33. **Acceptance criteria.** [x] Every paired policy comparison shares one persisted `EligibleClientSet`. [x] Zero-mean CV degeneracy is always a typed `UndefinedCvResult`, never an exception. [x] `EligibilityCoverageResult` and `ConformalCoverageResult` remain disjoint, non-substitutable identities. [x] `AlertBurdenResult` cannot be constructed without evidence.
34. **Completion evidence.** Changed files: `domain/evaluation/operating_points.py`, `domain/evaluation/alert_burden.py`, and their tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** A zero-mean CV computation always yields `UndefinedCvResult`, never `StatisticsError`; missing or invalid traffic-rate evidence fails `AlertBurdenResult` construction with a typed validation error and is never treated as a client-eligibility reason. Stop and raise a readiness blocker if the operational policy for zero-mean CV wording (`ClaimOutcome`) is unresolved by either authority document, rather than inventing a default wording.
36. **Stop conditions.** Stop if the zero-mean CV policy is unresolved; carry as a readiness blocker.
37. **Deliverables.** Evaluation, operating-point, and alert-burden result types.
38. **Final review checklist.** [x] No CV exception path exists anywhere. [x] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [x] Read the complete roadmap and architecture before starting.
- [x] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [x] Verify the P1-T004/P1-T014/P1-T016/P1-T018 completed dependencies.
- [x] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; recorded 2026-07-14T23:00:00Z; implement frozen operating-point and alert-burden results.
- [x] Keep both status locations synchronized while active.
- [x] Change status to `IN_REVIEW` before the B2–B10 audits; recorded 2026-07-14T23:18:00Z after focused validation and local audits; B12 audits recorded separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for dependency-direction violations, framework leakage, cycles/side effects, and hidden defaults. Run import-linter/pytest-archon; record results. This ticket's architecture-specific concern is confirming no code path anywhere constructs a policy-specific `EligibleClientSet` or substitutes eligibility coverage for conformal coverage.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw `float`/`tuple`/`dict` standing in for `CvOutcome`, `EligibleClientSet`, or `TrafficRateEvidence`, `Any`/unjustified `cast`, and a bare exception raised where a typed `UndefinedCvResult` is required. This ticket's own specific concern is confirming no zero-mean CV path anywhere raises rather than returning a typed result.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

```
grep -rn "P[0-9]\+-T[0-9]\+\|MASTER_TICKET_LOG\|TICKET_STATUS\|DATP Core Architecture\|Journal_Extension_Master_Roadmap\|docs/tickets\|implemented for ticket\|required by the roadmap\|required by the architecture" src/ tests/ --include="*.py"
```

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25) and property lane (line 26), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Contract/architecture-specific/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `domain/evaluation/operating_points.py` and `domain/evaluation/alert_burden.py` must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE` — these are pure frozen result types with no seed, identity-derivation, or configuration-resolution surface of their own; determinism of the values they carry is owned by the mathematics and scoring tickets that populate them (P1-T016 and upstream).

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm CV(FPR) eligibility follows Roadmap §10's `n_k ≥ n_min = 100` rule via the shared `EligibleClientSet` and that `AlertBurdenResult` can never be constructed from a hypothetical or assumed rate (SB-20).

Record, 2026-07-14T23:18:00Z: PASS. The paired eligible population is a typed, identity-bearing `EligibleClientSet`; alert burden accepts only measured or cited typed traffic evidence, never a bare rate.

**Audit 2 — Architecture fidelity.** Confirm every Architecture §9.3 type in this ticket's scope exists with its stated fields and invariants, and that `EligibleClientSet` is built exactly once per paired comparison per §29.1's "owned once" rule.

Record, 2026-07-14T23:18:00Z: PASS. Client counts, threshold, metrics, eligibility/fallback metadata, calibration count reference, test-split identity, and the paired-set identity are stored. CV, coverage, fleet, temporal, equity, cluster, and alert-burden result types are frozen and structurally distinct.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a future evaluator call construct two different `EligibleClientSet` instances for the same paired comparison, silently letting each policy see a different population? Does the confusion-count property test actually verify `benign_test_count == TN + FP` and `attack_test_count == TP + FN` from generated data, or does it just assert a tautology on hard-coded numbers? Fix and rerun until clean.

Record, 2026-07-14T23:18:00Z: PASS. `PolicyEvaluationResult` requires every roster-complete client result to carry its same persisted eligible-set identity. The generated-count property test verifies both count equalities and recomputed FPR/TPR/precision/recall; constructor validation additionally rejects inconsistent derived metrics.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.

### Batch completion evidence — 2026-07-15T00:45:00Z

- The P1-T020–P1-T029 whole-project quality checkpoint passed: `uv run nox -s full_suite` (Ruff, format, strict Pyright/Pylance parity, import-linter, architecture pytest, randomized xdist 224 tests, Sonar, and CodeScene).
- The repository-wide raw-dictionary, `Any`, typing-suppression, planning-reference, documentation, no-backward-compatibility, and cleanup scans passed; no scientific execution occurred.

### Local-review evidence — 2026-07-14T23:18:00Z

- Changed `domain/evaluation/operating_points.py` and `alert_burden.py`; added focused unit/property tests.
- Focused Ruff, Pyright strict, pytest, import-linter, pytest architecture, planning-reference, and diff checks passed. Pylance has no independent CLI; configured Pyright strict is the parity gate.
- Contract, integration, CUDA, and system test categories are `NONE` for this pure-domain ticket. Full-suite, randomized-order, Nox, Sonar, and CodeScene remain intentionally batched through P1-T029; this ticket is not `DONE` until that grouped gate passes.
