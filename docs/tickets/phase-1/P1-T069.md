# P1-T069 — Implement the CLI boundary and command invocation

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T069` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T069
2. **Title.** Implement the CLI boundary and command invocation
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** cli
7. **Owner.** implementation-engineer
8. **Supporting agents.** architecture-cleaner
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE (the CLI owns no domain or application type; it only renders and exits).
13. **Objective.** Implement `cli/main.py` and `cli/commands/`: parse command-line input into a typed request, invoke exactly one `composition` use-case entrypoint, render the returned boundary result (or a typed failure, via `rich`) with all rendering confined to `infrastructure` reporting reached only through composition, and convert the outcome into a process exit code.
14. **Why this ticket exists.** This ticket was split out of the former combined composition/CLI mega-ticket because the CLI has a single outbound edge (to `composition`) and a narrow, purely boundary-facing responsibility (Architecture §3.2, §12.5) that must never be conflated with wiring, adapter construction, or path resolution — all of which belong exclusively to P1-T068. Keeping the CLI this thin means it can never accidentally become a second composition root: it has no way to construct an adapter, bind a port, or resolve a path, so any future feature that seems to need those things is a signal it belongs in `composition`, not `cli`.
15. **Authority references.** Architecture §3.2 (`cli →` only `composition` and shared boundary result/error types), §12.5 (the CLI never instantiates an adapter, never wires a dependency, never resolves a path itself), §12.6 (the CLI renders typed errors and preflight findings with `rich`, translated through composition), §29.2 (the CLI is never the composition root).
16. **Dependencies.** P1-T068.
17. **Blocks.** P1-T050, P1-T070.
18. **Downstream consumers.** P1-T050 (the architecture-boundary test suite verifies the CLI's single outbound edge) and P1-T070 (the socle-readiness gate exercises the CLI use-case path) depend on this ticket directly.
19. **Preconditions.** P1-T068 is `DONE`.
20. **Inputs.** The use-case entrypoints exposed by `composition/root.py`.
21. **Allowed scope.** `cli/main.py`, `cli/commands/`.
22. **Forbidden actions.** No adapter instantiation anywhere in `cli/`; no dependency wiring; no filesystem path resolution; no scientific or stage-orchestration logic; no direct import of `infrastructure`, `config`, or any framework anywhere in `cli`.
23. **Required configuration or NONE.** NONE — the CLI parses arguments only; configuration loading happens entirely inside the use case it invokes.
24. **Implementation responsibilities.** Parse CLI arguments into a typed request; call exactly one appropriate `composition` use-case entrypoint; render the returned boundary result or typed failure via `rich`; map a `DatpCoreError` to the correct process exit code.
25. **Required unit tests.** The CLI module never imports `infrastructure`, `config`, or a framework directly (an explicit import-scan test); a typed failure returned by the use case maps to a distinct, correct exit code for each error family, never a generic catch-all exit code.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** `cli` has exactly one outbound edge, to `composition`; no adapter construction or path resolution occurs anywhere inside `cli/`.
29. **Required integration tests.** A synthetic CLI invocation of a use case (with test-double infrastructure supplied through composition) renders the expected boundary result and produces the expected exit code.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; architecture lane; integration lane (synthetic).
33. **Acceptance criteria.** [ ] The CLI wires nothing itself. [ ] The CLI resolves no filesystem path itself. [ ] The CLI has exactly one outbound edge, to `composition`. [ ] Every typed failure maps to a correct, distinct exit code.
34. **Completion evidence.** Changed files: `cli/main.py`, `cli/commands/`, and their tests. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** A `DatpCoreError` translates to a legible boundary message and a distinct exit code; the CLI never swallows or reinterprets the error's meaning.
36. **Stop conditions.** None.
37. **Deliverables.** The CLI boundary and command-invocation modules.
38. **Final review checklist.** [ ] The CLI is genuinely thin — no wiring, no path resolution, no adapter construction anywhere. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for a direct `infrastructure`/`config`/framework import anywhere inside `cli/`, an adapter constructed inside `cli/`, and a path resolved inside `cli/`. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw dict standing in for the typed CLI request, `Any`/unjustified `cast`, and a generic catch-all exit code masking distinct error families that should each map to their own code.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25), architecture lane (line 28), and integration lane (line 29), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/contract/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `cli/` must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE` for direct seed/CUDA surface — the CLI is a pure boundary-parsing and rendering layer with no scientific computation of its own. Confirm regardless that argument parsing itself is deterministic (the same command-line input always produces the same typed request), since any nondeterminism here would be invisible to every downstream determinism guarantee the use case itself provides.

### B11. Phase 1 boundary confirmation

Confirm no real N-BaIoT/Edge-IIoTset/CICIoT2023 execution is reachable through any CLI command implemented in this ticket — the synthetic integration test at line 29 exercises the CLI against test-double infrastructure only. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — CLI boundary code carries no direct scientific claim.` Positive finding required: confirm no CLI flag or argument exists that could bypass a scientific-configuration validation step that would otherwise run inside the invoked use case.

**Audit 2 — Architecture fidelity.** Confirm the CLI has exactly one outbound edge (to `composition`) and performs no wiring, adapter construction, or path resolution anywhere, matching Architecture §3.2/§12.5 exactly.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a future CLI command "temporarily" import an infrastructure adapter directly for a quick debugging convenience, silently creating a second composition root? Does the exit-code test actually cover every distinct error family with a distinct expected code, or does it collapse several into "any nonzero code"? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.
