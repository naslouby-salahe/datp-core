# P1-T061 — Implement hardware inventory and GPU assignment

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T061` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T061
2. **Title.** Implement hardware inventory and GPU assignment
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** infrastructure
7. **Owner.** implementation-engineer
8. **Supporting agents.** —
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `HardwareInspector` (implementation), `HardwareInventory`, `DeviceSpec` (inventory-facing use).
13. **Objective.** Implement `infrastructure/runtime/hardware.py`: `HardwareInspector.inspect() -> HardwareInventory`, read-only, reporting `cuda_available = False` rather than raising, with graceful degrade when psutil/pynvml are absent.
14. **Why this ticket exists.** Split out because hardware inventory is a read-only, independently testable responsibility distinct from the fail-loud `CudaGuard` (P1-T060) and from resource-pressure monitoring (P1-T062), which consumes this inventory but has its own cooperative-response logic.
15. **Authority references.** Architecture §12.2 (`HardwareInspector` port), §12.4 (implementation, read-only), §4.1 (psutil/pynvml optional, graceful degrade to stdlib/`torch.cuda` queries), §9.4 (`HardwareInventory` fields).
16. **Dependencies.** P1-T035, P1-T036.
17. **Blocks.** P1-T062, P1-T066, P1-T068, P1-T070.
18. **Downstream consumers.** P1-T062 (resource-pressure monitoring), P1-T066 (environment/provenance inventory adapter), P1-T039 (preflight).
19. **Preconditions.** P1-T035, P1-T036 are `DONE`.
20. **Inputs.** Architecture §12.2, §12.4, §4.1, §9.4.
21. **Allowed scope.** `infrastructure/runtime/hardware.py`.
22. **Forbidden actions.** No raising on missing optional dependency (psutil/pynvml absence degrades gracefully); no write or mutation of hardware state; no CUDA ownership decision made here (that is `CudaGuard`'s responsibility, P1-T060).
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Query CUDA availability, GPU name/count, VRAM, PyTorch/CUDA-runtime/driver versions, CPU count, and RAM via `torch.cuda`/stdlib with optional psutil/pynvml enrichment; return a complete `HardwareInventory` even when CUDA is absent.
25. **Required unit tests.** `inspect()` returns `cuda_available = False` rather than raising when no GPU is present; missing psutil/pynvml degrades to the stdlib/`torch.cuda` path without error.
26. **Required property tests.** NONE.
27. **Required contract tests.** `HardwareInspector.inspect` satisfies its Section 12.2 signature exactly.
28. **Required architecture tests.** NONE.
29. **Required integration tests.** NONE beyond the CUDA-lane test at field 30.
30. **Required CUDA tests.** On a CUDA-available host, `inspect()` reports a consistent GPU name/count/VRAM matching the actual device.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; contract lane; CUDA lane.
33. **Acceptance criteria.** [ ] Read-only; inspection never raises on hardware absence. [ ] Graceful degrade without optional libraries (psutil/pynvml). [ ] A complete `HardwareInventory` is returned in every case, including no-CUDA hosts.
34. **Completion evidence.** Changed files: `hardware.py` and its tests. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** NONE expected; inspection never raises.
36. **Stop conditions.** None.
37. **Deliverables.** `HardwareInspector` adapter.
38. **Final review checklist.** [ ] Read-only; graceful optional-dependency degrade. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

### B3. Architecture boundary audit

Check the complete project for a `HardwareInspector` call site that raises on missing hardware or an absent optional dependency instead of degrading gracefully, and for any CUDA device-ownership decision made inside `hardware.py` rather than deferred to `CudaGuard`. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw dict/tuple substituting for `HardwareInventory`, `Any`/unjustified `cast`, and a bare `try`/`except Exception` swallowing a genuine unexpected error rather than the specific optional-import failure it is meant to tolerate.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25), contract lane (line 27), and CUDA lane (line 30), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/architecture/integration/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `hardware.py` must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

This is a determinism-relevant ticket. Check specifically that `inspect()` is read-only and never mutates process, driver, or device state as a side effect of inspection; that repeated calls on an unchanged host return a consistent `HardwareInventory` (no wall-clock- or process-id-dependent field); and that the pinned PyTorch/CUDA-runtime/driver version fields are read directly from the environment rather than hardcoded, since the accompanying `EnvironmentInventory` (Architecture §9.5) depends on this inventory being an honest snapshot for reproducibility reporting.

### B11. Phase 1 boundary confirmation

Confirm no real N-BaIoT/Edge-IIoTset/CICIoT2023 data is read anywhere in this ticket — every unit and CUDA-lane test inspects the host/device environment only, never dataset content. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — hardware inspection carries no scientific claim.` Positive finding required: confirm this ticket protects reproducibility reporting (Architecture §9.5's `EnvironmentInventory`) by guaranteeing that every downstream provenance record is built from an honest, non-raising hardware snapshot rather than a partially populated or exception-truncated one.

**Audit 2 — Architecture fidelity.** Confirm the adapter matches Architecture §12.2/§12.4/§4.1/§9.4 exactly: the zero-argument `inspect() -> HardwareInventory` signature, `cuda_available = False` on absence rather than a raised error, and graceful degrade to stdlib/`torch.cuda` queries when psutil/pynvml are not installed.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could an unhandled exception inside the optional-library enrichment path (psutil/pynvml) propagate past `inspect()` instead of degrading, given that "missing" and "present-but-erroring" are different failure modes? Does the missing-library unit test actually simulate the absence of the import, or only assert the happy path returns a populated inventory? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.
