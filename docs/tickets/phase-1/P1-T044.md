# P1-T044 — Implement Flower FedAvg/FedProx and centralized trainers

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T044` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T044
2. **Title.** Implement Flower FedAvg/FedProx and centralized trainers
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** infrastructure
7. **Owner.** federated-learning-engineer
8. **Supporting agents.** —
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `infrastructure/learning/federation/{trainer,strategies/fedavg,strategies/fedprox}.py`, `infrastructure/learning/centralized/trainer.py`.
13. **Objective.** Implement the shared federated trainer lifecycle plus the FedAvg and FedProx strategies, and a distinct centralized trainer for B0, converting every framework handle to a typed checkpoint descriptor before it crosses the training port; a full-participation round aborts on any missing, timed-out, malformed, non-finite, or shape-incompatible client update; FedProx inherits every non-strategy setting from FedAvg and adds only a frozen-grid µ value.
14. **Why this ticket exists.** Flower must stay entirely behind the `FederatedTrainer`/`CentralizedModelTrainer` ports declared in P1-T034 — no Flower `Strategy` or client object may ever be visible to `application` code, per Architecture §12.4's adapter-confinement rule. Full-participation abort-on-any-failure is not a convenience default: `ParticipationStrategy.FULL` has exactly one member (locked in P1-T002) because the roadmap's core ladder assumes every client contributes every round, and a round that silently continued with a missing client would produce a subtly different (and non-reproducible) trained model. FedProx must never derive its µ from a FedAvg or confirmatory outcome — µ comes only from a pre-registered frozen grid, closing the door on a post-hoc "µ that made the result look better."
15. **Authority references.** Architecture §12.4 (trainer adapter implementation), §16.5 (full-participation semantics), §9.6 (framework confinement), §25 (adding a new training strategy). Roadmap §9.2 (E-T1 FedProx stress test, µ-grid frozen).
16. **Dependencies.** P1-T043.
17. **Blocks.** P2-T009, P4-T016.
18. **Downstream consumers.** P2-T009 and P4-T016 (outside this extracted phase) consume these trainers directly.
19. **Preconditions.** P1-T043 is `DONE`.
20. **Inputs.** Architecture §12.4, §16.5, §9.6, §25; Roadmap §9.2.
21. **Allowed scope.** `infrastructure/learning/federation/`, `centralized/`.
22. **Forbidden actions.** No Flower `Strategy`/client/server object crossing the training port; no partial-participation code path anywhere; FedProx's µ must never be derived from a FedAvg result or any confirmatory outcome; the centralized (B0) trainer must share no identity type with the federated trainer.
23. **Required configuration or NONE.** From the frozen `FederationSpec`/`TrainingSpec`; FedProx's µ comes only from its pre-registered frozen grid.
24. **Implementation responsibilities.** Implement the shared trainer lifecycle and the FedAvg/FedProx strategy implementations; implement round-abort logic on any missing, timed-out, malformed, non-finite, or shape-incompatible client update; implement the centralized trainer with no federation-specific field on its request/result types.
25. **Required unit tests.** NONE beyond basic construction — the meaningful behavioral guarantees (full-participation abort, FedProx equivalence to FedAvg outside µ) are exercised at the integration/CUDA level, since they depend on genuine multi-round training dynamics that a unit test in isolation cannot meaningfully simulate.
26. **Required property tests.** NONE.
27. **Required contract tests.** Both the FedAvg/FedProx strategy adapters and the centralized trainer satisfy their respective P1-T034 training ports exactly.
28. **Required architecture tests.** No Flower object crosses the training port boundary in either direction.
29. **Required integration tests.** A round aborts correctly when a client update is missing, malformed, non-finite, or shape-incompatible; FedProx matches FedAvg on every non-strategy setting (same encoder, same optimizer, same schedule) while differing only in its µ contribution; the centralized trainer rejects a FedAvg identity offered to it (all exercised on synthetic data, cuda lane).
30. **Required CUDA tests.** Full-participation abort behavior and FedProx-vs-FedAvg equivalence, both verified on a synthetic tiny model.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Serialized CUDA lane; contract lane; Pyright strict.
33. **Acceptance criteria.** [ ] A round aborts on any single invalid client update under full participation. [ ] FedProx's µ comes only from its frozen grid. [ ] The centralized (B0) trainer shares no identity with the federated trainer. [ ] No Flower object is ever visible outside `infrastructure/learning/`.
34. **Completion evidence.** Changed files: `infrastructure/learning/federation/`, `centralized/`, and their tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** `FullParticipationViolationError`, `RoundAbortedError`, the client-failure error family, `TrainingError`, and `CudaOutOfMemoryError` are the typed failure paths. Stop and implement only a smoke-level FedProx path if the µ-grid is not yet pre-registered by either authority — carry the real grid as a readiness blocker.
36. **Stop conditions.** Stop if the FedProx µ-grid is not pre-registered; implement smoke-level only and carry as a readiness blocker.
37. **Deliverables.** FedAvg/FedProx and centralized trainer adapters.
38. **Final review checklist.** [ ] Stress-test trainers (FedProx) remain structurally outside the causal ladder. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for a Flower object crossing the training port in either direction, a partial-participation code path anywhere, and a shared identity type between the federated and centralized trainers. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw `float` µ value hardcoded outside the frozen grid, `Any`/unjustified `cast`, and a generic trainer request type shared between FedAvg/FedProx/centralized instead of their distinct typed requests.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the contract lane (line 27), architecture lane (line 28), integration lane (line 29), and CUDA lane (line 30), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Unit/property/system categories are `NONE` beyond baseline contract checks, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; these modules must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

Check specifically that full-participation abort behavior is deterministic (the same missing/malformed client always produces the same abort, never a race-dependent partial continuation), that no client failure ever triggers a silent CPU fallback, and that FedProx's frozen µ-grid is never mutated at runtime based on observed intermediate training behavior.

### B11. Phase 1 boundary confirmation

Confirm no real N-BaIoT/Edge-IIoTset/CICIoT2023 training occurs anywhere in this ticket — every test in fields 29–30 uses a synthetic tiny model and synthetic client data. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm FedAvg remains the core-ladder trainer and FedProx is exercised only as the stress-test comparator outside the causal ladder, matching Roadmap §2/SB-25; confirm FedProx's µ is never selected post-hoc based on a favorable outcome.

**Audit 2 — Architecture fidelity.** Confirm the trainer lifecycle matches Architecture §12.4/§16.5 exactly, and that full-participation abort covers every failure category §16.5 names (missing, timed-out, malformed, non-finite, shape-incompatible).

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a client update with a subtly non-finite value (e.g. a `NaN` buried inside one tensor element rather than the whole tensor) slip past the abort check due to an incomplete finiteness scan? Does the FedProx-vs-FedAvg equivalence test actually hold every non-strategy setting fixed, or does it silently vary something else alongside µ? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.
