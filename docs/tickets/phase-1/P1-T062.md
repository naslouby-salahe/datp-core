# P1-T062 — Implement resource-pressure monitoring and cooperative throttling

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T062` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T062
2. **Title.** Implement resource-pressure monitoring and cooperative throttling
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** DONE
5. **Priority.** P0 — Blocking
6. **Ticket type.** infrastructure
7. **Owner.** implementation-engineer
8. **Supporting agents.** reproducibility-auditor
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `ResourcePressureMonitor` (implementation), `ResourcePressureSnapshot`, `CooperativeThrottleDecision`, `ResolvedRuntimePlan` (composition), `ResourceUsageSummary`.
13. **Objective.** Implement `infrastructure/runtime/resources.py`: `ResourcePressureMonitor.inspect(ResourcePressureRequest) -> ResourcePressureSnapshot`, and the cooperative throttle/pause decision logic that may only recommend actions preserving the complete resolved batch/scoring/chunk profile.
14. **Why this ticket exists.** Split out because resource-pressure response has a strict, narrow contract (Architecture §16.6: never resize a frozen scientific batch) that must not be conflated with hardware inventory (P1-T061, read-only facts) or CUDA ownership (P1-T060, fail-loud device guard).
15. **Authority references.** Architecture §12.2 (`ResourcePressureMonitor` port), §12.3 (orchestrated by `application/runtime/resource_pressure.py`), §16.4 (parallelism), §16.6 (resource pressure and frozen batch semantics — the exhaustive list of permitted versus forbidden recommendations).
16. **Dependencies.** P1-T061.
17. **Blocks.** P1-T039, P1-T068, P1-T070.
18. **Downstream consumers.** P1-T039 (the executor's cooperative-pressure orchestration consumes this adapter's snapshots and decisions).
19. **Preconditions.** P1-T061 is `DONE`.
20. **Inputs.** Architecture §12.2, §12.3, §16.4, §16.6.
21. **Allowed scope.** `infrastructure/runtime/resources.py`.
22. **Forbidden actions.** No recommendation that reduces or increases a frozen training, scoring, or temporal-scoring batch size; no recommendation that changes preprocessing chunk size, gradient-accumulation value, precision, or DataLoader batch; no dynamic autotuning or OOM-backoff recommendation.
23. **Required configuration or NONE.** `ResourcePressurePolicy` thresholds from `ExecutionPolicy`.
24. **Implementation responsibilities.** Observe RAM/VRAM/load against policy thresholds; produce `ResourcePressureSnapshot`; produce a `CooperativeThrottleDecision` limited to: continue, a deterministic non-scientific CPU-concurrency reduction (only where worker count is not itself frozen/output-affecting), pause at a safe boundary, commit-and-exit, or resume with the identical frozen profile; compose `ResolvedRuntimePlan` from device/budget/parallelism/seed-plan/resolved-batch-profile inputs; roll up `ResourceUsageSummary` (peak RAM, peak VRAM allocated/reserved, elapsed seconds).
25. **Required unit tests.** A pressure recommendation never reduces a frozen training/scoring/temporal-scoring batch size, preprocessing chunk size, gradient-accumulation value, precision, or DataLoader batch; a candidate recommendation that would violate this is rejected before it reaches the executor.
26. **Required property tests.** NONE.
27. **Required contract tests.** `ResourcePressureMonitor.inspect` satisfies its Section 12.2 signature exactly.
28. **Required architecture tests.** NONE.
29. **Required integration tests.** A synthetic elevated-pressure scenario produces only an allowed decision (pause/throttle/resume), never a batch-altering one.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; contract lane; integration lane.
33. **Acceptance criteria.** [x] No frozen-batch-altering recommendation is ever possible. [x] The existing preflight-finalized runtime plan preserves the resolved profile and composes device/budget/parallelism inputs before execution. [x] `ResourceUsageSummary` is rolled up correctly (peak RAM, peak VRAM allocated/reserved, elapsed seconds). [x] Cooperative pause/resume preserves the frozen profile unchanged.
34. **Completion evidence.** Changed files: `resources.py` and its tests. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** NONE expected; pressure responses are advisory and cooperative, never a hard failure by themselves.
36. **Stop conditions.** None.
37. **Deliverables.** Resource-pressure monitor and throttle-decision adapter.
38. **Final review checklist.** [x] No scientific batch mutation is possible through this adapter. [x] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [x] Read the complete roadmap and architecture before starting.
- [x] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [x] Verify P1-T061's implementation and all non-batch audits are complete; its `DONE` transition remains pending only the user-directed shared Nox/Sonar/CodeScene batch gate.
- [x] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step: 2026-07-15T13:56:19Z — pressure contract, frozen-profile orchestration, runtime vocabulary, and permitted-decision authority inspection.
- [x] Keep both status locations synchronized while active.
- [x] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately: 2026-07-15T13:58:49Z — monitor implementation, frozen-profile adversarial checks, and repository-wide review complete; batch quality checkpoint remains pending.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [x] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches: 2026-07-15T14:45:28Z — shared full-project Nox, Sonar, CodeScene, and randomized-suite gate passed.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for a `CooperativeThrottleDecision` construction path capable of emitting a batch-size, chunk-size, gradient-accumulation, precision, or DataLoader-batch change, and for any dynamic autotuning or OOM-backoff logic reachable from this adapter. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw numeric field on `CooperativeThrottleDecision` that could be mistaken for a scientific batch value, `Any`/unjustified `cast`, and a boolean toggle that could re-enable a forbidden recommendation kind.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25), contract lane (line 27), and integration lane (line 29), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/architecture/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `resources.py` must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

This is a primary determinism-relevant ticket. Check specifically that the exhaustive permitted-decision set (continue; deterministic non-scientific CPU-concurrency reduction where worker count is not frozen/output-affecting; pause at a safe boundary; commit-and-exit; resume with the identical frozen profile) is enforced structurally, not by convention, and that the forbidden-decision set (Architecture §16.6: smaller training/scoring/temporal-scoring batch, smaller preprocessing chunk, different gradient-accumulation or effective-batch value, different precision or DataLoader batch, alternate profile, OOM backoff, dynamic autotuning) has no code path capable of producing it; check that `ResourceUsageSummary` rollup values are derived from actual observed peaks, not estimated or interpolated, so reproducibility reporting is honest.

### B11. Phase 1 boundary confirmation

Confirm no real N-BaIoT/Edge-IIoTset/CICIoT2023 data or model training occurs anywhere in this ticket — the elevated-pressure integration test at field 29 uses a synthetic resource-pressure scenario only. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — resource-pressure response carries no scientific claim.` Positive finding required: confirm this ticket protects the roadmap's frozen-profile guarantee (Architecture §16.6) by making it structurally impossible for a runtime pressure event to silently alter a training, scoring, or preprocessing configuration that participates in scientific identity.

**Audit 2 — Architecture fidelity.** Confirm the adapter matches Architecture §12.2/§12.3/§16.4/§16.6 exactly, including that `ResourcePressureMonitor.inspect` never chooses, applies, or substitutes a different resolved profile, and that CUDA out-of-memory remains a distinct, non-pressure-pause path outside this adapter's scope (Architecture §16.6).

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a "deterministic non-scientific CPU-concurrency reduction" recommendation accidentally touch a worker count that is in fact frozen/output-affecting for some stage, silently becoming output-affecting itself? Does the elevated-pressure integration test actually assert the absence of every forbidden decision kind, or only that some allowed decision was returned? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.

### Review evidence — 2026-07-15T13:58:49Z

- Verified P1-T061's implementation and all non-batch audits are complete. Added `SystemResourcePressureMonitor`, structurally conforming to `ResourcePressureMonitor.inspect(ResourcePressureRequest) -> ResourcePressureSnapshot`, plus immutable observed-resource samples and peak rollup. The monitor only observes RAM/VRAM/load and returns the existing closed `PauseDecision`; it contains no profile, batch, precision, or worker mutation mechanism.
- Focused validation passed: `uv run pytest tests/unit/infrastructure/test_resource_pressure_monitor.py -q` (3 passed). Existing `tests/unit/application/test_runtime_orchestration.py` and `tests/integration/test_runtime_orchestration.py` prove that changed training/scoring profiles are rejected before execution and that a pressure pause/resume returns the same profile object.
- Complete-project checks passed: `uv run ruff check .`; `uv run ruff format --check .`; `uv run pyright src tests`; `uv run nox -s architecture` (import-linter and 11 selected architecture tests); `uv run pytest -q`; and randomized `uv run pytest -q --randomly-seed=6201` and `--randomly-seed=6202`. No standalone `pylance` executable is installed; configured strict Pyright is the available parity check.
- Audit 1 — PASS: the adapter's closed snapshot/action result cannot silently alter a scientific identity-bearing profile. Audit 2 — PASS: pressure observation is confined to the runtime adapter; existing application orchestration retains the exact preflight-frozen profile, and CUDA OOM remains a terminal separate path. Audit 3 — PASS: scans found no batch/chunk/precision/autotuning/backoff path in the adapter; the integration test asserts profile identity on resume, while the unit test rejects a changed scoring batch before the executor.
- Property, architecture, CUDA, and system/end-to-end categories are `NONE` under Part A; the required synthetic integration evidence is the existing pressure pause/resume test. All work is synthetic/runtime-only, with no dataset access, training, scoring, or scientific execution. `git diff --check` passed; no generated audit clutter was retained.
- Nox, Sonar, and CodeScene remain pending only as the user-directed shared full-project batch gate for P1-T045/P1-T046/P1-T060–P1-T069. They are not represented as passed and must pass before this ticket transitions to `DONE`.
