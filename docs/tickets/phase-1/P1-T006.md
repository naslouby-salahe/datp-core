# P1-T006 — Implement experiment-role/claim-tier/status vocabulary and the role/tier invariant

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T006` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T006
2. **Title.** Implement experiment-role/claim-tier/status vocabulary and the role/tier invariant
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** DONE
5. **Priority.** P0 — Blocking
6. **Ticket type.** domain
7. **Owner.** experiment-engineer
8. **Supporting agents.** datp-protocol-guardian
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `ExperimentRole` (nine roles), `ClaimTier` (`IntEnum` `TIER_1`..`TIER_9`), `ExecutionStatus`, the constructor-enforced role/tier invariant.
13. **Objective.** Implement `domain/experiments/claims.py`: `ExperimentRole`, `ClaimTier`, `ExecutionStatus`, and a validation function/invariant enforcing `evidence_role == CONFIRMATORY ⇔ tier == TIER_1`.
14. **Why this ticket exists.** This is the type-level guarantee that a supportive, mechanism, stress-test, boundary, exploratory, or future-work experiment can never be silently promoted into the confirmatory claim — the single most safety-critical invariant in the roadmap's claim hierarchy (Roadmap §5), because the entire nine-tier claim discipline depends on Tier 1 remaining singular and isolated.
15. **Authority references.** Architecture §6.1 (role/tier/status enums), §8.3 (role/tier invariant, enforced at `ExperimentIdentity` construction in P1-T029), §29.1. Roadmap §5 (nine claim tiers, Tier 1 isolated), §3 (locked confirmatory claim).
16. **Dependencies.** P0-T026.
17. **Blocks.** P1-T029.
18. **Downstream consumers.** P1-T029 (experiment identity/profile/cell aggregates) enforces this invariant at every `ExperimentIdentity` construction site.
19. **Preconditions.** P0-T026 is `DONE`.
20. **Inputs.** Architecture §6.1, §8.3, §29.1; Roadmap §5, §3.
21. **Allowed scope.** `domain/experiments/claims.py` — the enums and the invariant-checking function only; the full `ExperimentIdentity` dataclass that calls this invariant is P1-T029's scope.
22. **Forbidden actions.** No confirmatory role permitted outside Tier 1; no code path that constructs an `ExperimentIdentity`-shaped value bypassing this invariant; no free-text role string substituting for `ExperimentRole`.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Define `ExperimentRole` with exactly nine members (`CONFIRMATORY`, `SUPPORTIVE`, `EXTERNAL_VALIDATION`, `STRESS_TEST`, `MECHANISM`, `BOUNDARY`, `EXPLORATORY`, `FUTURE_WORK`, `FORBIDDEN`); define `ClaimTier` as an `IntEnum` with exactly nine ordered members; define `ExecutionStatus`; implement the invariant-checking function raising `DomainValidationError` on a role/tier mismatch.
25. **Required unit tests.** `CONFIRMATORY` paired with `TIER_1` passes; `CONFIRMATORY` paired with any other tier is rejected; any non-`CONFIRMATORY` role paired with `TIER_1` is rejected.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE beyond the standard framework-confinement check for `domain/experiments/`.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Pyright strict; unit lane for `domain/experiments/claims.py`.
33. **Acceptance criteria.** [ ] Nine roles, nine tiers, exactly. [ ] The role/tier invariant is enforced by a callable, testable function. [ ] Only `CONFIRMATORY` may carry `TIER_1`.
34. **Completion evidence.** Changed files: `domain/experiments/claims.py` and its unit tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** `DomainValidationError` on a role/tier mismatch; implementation is blocked until that authoritative exception exists without violating the ticket's allowed scope.
36. **Stop conditions.** None. The required `DomainValidationError` is a domain dependency mandated by this ticket; B3's standard-library restriction excludes framework dependencies and does not override the required typed domain error.
37. **Deliverables.** Experiment-role/tier/status vocabulary and the role/tier invariant function.
38. **Final review checklist.** [ ] No promotion path exists. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for dependency-direction violations, framework leakage, cycles/side effects, service locators, incorrect type placement, forbidden module names, backward-compatibility scaffolding, hidden defaults, and out-of-phase behavior. Run import-linter/pytest-archon; record results. Confirm `domain/experiments/claims.py` imports nothing beyond the standard library.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for raw strings standing in for `ExperimentRole`/`ClaimTier`, object-shaped dictionaries, `Any`/unjustified `cast`, boolean flags substituting for the role/tier invariant (e.g. an `is_confirmatory: bool` field instead of the enum check), and incomplete enum dispatch.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/contract/architecture/integration/CUDA/system categories are `NONE` beyond the baseline framework-confinement check, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; this module must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — closed enums and one pure validation function; no seed, CUDA, or numeric identity field exists in this ticket's scope.` Confirm no such path was introduced regardless.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm the invariant makes it structurally impossible to promote a supportive/mechanism/stress-test/boundary/exploratory/future-work experiment to Tier 1, matching Roadmap §5's isolation of the confirmatory claim.

**Audit 2 — Architecture fidelity.** Confirm the module matches Architecture §5.1/§8.3 exactly and that the invariant is enforced by a callable function, not a comment or convention.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a caller construct a role/tier pairing that bypasses this invariant by not calling the validation function? Is the rejection test asserting the raised exception type, not just "no crash"? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.

## Completion evidence

- Implemented the closed claim-role, claim-tier, and execution-status vocabularies plus the typed `validate_role_tier` invariant in `domain/experiments/claims.py`.
- Focused unit tests, full pytest under randomized seeds 101 and 202, Ruff lint/format, Pyright, configured import-linter, and architecture tests passed.
- The full Nox suite passed on 2026-07-14, including Sonar and CodeScene after the oversized error-hierarchy test was repaired without reducing coverage.
- Audit 1 passed: only `CONFIRMATORY` can be paired with `TIER_1`; no scientific execution path was introduced.
- Audit 2 passed: the module is framework-free, uses the canonical typed domain error, and boundary checks passed.
- Audit 3 passed: every non-confirmatory role is tested against `TIER_1` and rejection asserts `DomainValidationError`; P1-T029 will consume this callable at `ExperimentIdentity` construction.
- Property, contract, integration, CUDA, and system test categories are `NONE` for this ticket. No ticket-specific repository scratch was created.
