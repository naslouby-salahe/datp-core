# P1-T060 — Implement CUDA guard and deterministic device initialization

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T060` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T060
2. **Title.** Implement CUDA guard and deterministic device initialization
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** infrastructure
7. **Owner.** implementation-engineer
8. **Supporting agents.** reproducibility-auditor
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `CudaGuard` (implementation), `GpuAssignment`, `DevicePolicy` enforcement.
13. **Objective.** Implement `infrastructure/runtime/cuda.py`: `CudaGuard.require_cuda(device: DeviceSpec) -> GpuAssignment`, validating CUDA availability before any training/scoring stage and raising `CudaUnavailableError`/`CudaDeviceMismatchError`/`InvalidCpuFallbackError` with no silent CPU fallback.
14. **Why this ticket exists.** Split out because CUDA device ownership and its fail-loud contract (Architecture §16.1) is a distinct, independently testable responsibility from hardware inventory (P1-T061), resource pressure (P1-T062), and every persistence/telemetry/reporting concern (P1-T063–P1-T067).
15. **Authority references.** Architecture §12.2 (`CudaGuard` port), §12.4 (implementation), §16.1 (CUDA execution: one host, one selected GPU, at most one active scientific job), §20.1 (`CudaUnavailableError`/`CudaDeviceMismatchError`/`InvalidCpuFallbackError`).
16. **Dependencies.** P1-T035, P1-T036.
17. **Blocks.** P1-T039, P1-T068, P1-T070.
18. **Downstream consumers.** P1-T039 (preflight/executor), P1-T043/P1-T044/P1-T045 (CUDA-required adapters), P1-T068 (composition wires this adapter to the port).
19. **Preconditions.** P1-T035, P1-T036 are `DONE` (ports declared).
20. **Inputs.** Architecture §12.2, §12.4, §16.1, §20.1.
21. **Allowed scope.** `infrastructure/runtime/cuda.py`.
22. **Forbidden actions.** No silent CPU fallback for a CUDA-required stage; no multi-GPU/multi-node scheduling (Phase 0/1 scope is one host, one selected GPU); no unsafe concurrent model ownership on one GPU.
23. **Required configuration or NONE.** `DeviceSpec` (policy, `GpuIndex`) from `ExecutionPolicy`.
24. **Implementation responsibilities.** Validate CUDA availability and the requested `GpuIndex`; raise the correct typed error on absence/mismatch/invalid-fallback-request; return `GpuAssignment` on success; enforce deterministic CUDA algorithm settings for `DeterminismLevel.STRICT`.
25. **Required unit tests.** A CUDA-required stage with no CUDA available raises `CudaUnavailableError` and never falls back to CPU silently; a device-index mismatch raises `CudaDeviceMismatchError`.
26. **Required property tests.** NONE.
27. **Required contract tests.** `CudaGuard.require_cuda` satisfies its Section 12.2 signature exactly.
28. **Required architecture tests.** No stage bypasses `CudaGuard` to touch CUDA directly.
29. **Required integration tests.** NONE beyond the CUDA-lane tests at field 30.
30. **Required CUDA tests.** On a CUDA-available host, `require_cuda` returns a valid `GpuAssignment`; on a CUDA-unavailable host (or a forced-absent test double), it raises without ever falling back to CPU.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; contract lane; CUDA lane (serialized).
33. **Acceptance criteria.** [ ] No silent CPU fallback for a CUDA-required stage. [ ] The correct typed error is raised per failure mode (unavailable, mismatch, invalid fallback). [ ] One host/one selected GPU scope is enforced. [ ] Deterministic CUDA settings are applied under `DeterminismLevel.STRICT`.
34. **Completion evidence.** Changed files: `cuda.py` and its tests. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** `CudaUnavailableError` is `RUN_BLOCKING`; never retried, never silently downgraded to CPU.
36. **Stop conditions.** None.
37. **Deliverables.** `CudaGuard` adapter.
38. **Final review checklist.** [ ] Fail-loud, no fallback — no CUDA-required stage ever proceeds on CPU. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for a CUDA call reaching a device directly without going through `CudaGuard`, a silent CPU fallback anywhere a CUDA-required stage is invoked, and any multi-GPU/multi-node scheduling code path. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw `torch.device`/`torch.cuda` handle crossing outside `infrastructure/runtime/cuda.py`, `Any`/unjustified `cast`, and a `DeviceSpec`/`GpuAssignment` field represented as a raw string or boolean instead of its typed value object.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25), contract lane (line 27), architecture lane (line 28), and CUDA lane (line 30), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/integration/system categories are `NONE` beyond the CUDA-lane evidence, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `cuda.py` must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

This is a primary determinism-relevant ticket. Check specifically that a CUDA-unavailable or device-mismatched host always produces the same typed error and never a partial or silently degraded scientific result; that no code path anywhere converts a `CudaUnavailableError`/`CudaDeviceMismatchError` into a CPU execution path; and that deterministic CUDA algorithm settings (disabled nondeterministic operations and cuDNN algorithm variation) are actually applied, not merely documented, whenever `DeterminismLevel.STRICT` is requested.

### B11. Phase 1 boundary confirmation

Confirm no real N-BaIoT/Edge-IIoTset/CICIoT2023 data or model training occurs anywhere in this ticket's tests — every unit and CUDA-lane test exercises `CudaGuard` in isolation against a synthetic or forced-absent device, never a real training or scoring workload. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — CUDA device initialization carries no scientific claim.` Positive finding required: confirm this ticket protects the roadmap's confirmatory ladder by guaranteeing that a CUDA-required stage never silently substitutes CPU execution, which would otherwise produce an unfingerprinted, non-reproducible variation in every downstream trained model and score.

**Audit 2 — Architecture fidelity.** Confirm the adapter matches Architecture §12.2/§12.4/§16.1/§20.1 exactly: the `require_cuda(device: DeviceSpec) -> GpuAssignment` signature, the three named error types, and the one-host/one-selected-GPU/at-most-one-active-scientific-job scope.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a future change silently catch `CudaUnavailableError` at a call site and substitute a CPU device object instead of propagating the error? Does the device-mismatch unit test actually construct a genuine index mismatch (e.g. a `GpuIndex` beyond the visible device count), or does it only assert the happy path succeeds? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.
