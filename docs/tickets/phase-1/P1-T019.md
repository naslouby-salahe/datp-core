# P1-T019 — Implement dataset, partition, and split specifications

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T019` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T019
2. **Title.** Implement dataset, partition, and split specifications
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** domain
7. **Owner.** data-pipeline-engineer
8. **Supporting agents.** —
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `DatasetSpec`, `DatasetSourceInspectionResult`, `ClientPartitionSpec` (plus its closed strategy variants including `DirichletPartitionSpec`), `ClientPartitionResult`, the closed `SplitSpec` union (`TrainingSplitSpec` / `BenignCalibrationSplitSpec` / `TestSplitSpec`), `SplitCollectionSpec`, `SplitManifestResult`, `TemporalWindowSpec` / `TemporalBoundary`, `TemporalProtocolSpec`, `OneShotRecalibrationSpec`, `ConformalSplitSpec`.
13. **Objective.** Implement `DatasetSpec`, `DatasetSourceInspectionResult`, `ClientPartitionSpec` (with its closed strategy variants, including `DirichletPartitionSpec`), `ClientPartitionResult`, the closed `SplitSpec` union (`TrainingSplitSpec` / `BenignCalibrationSplitSpec` / `TestSplitSpec`), `SplitCollectionSpec`, `SplitManifestResult`, `TemporalWindowSpec` / `TemporalBoundary`, `TemporalProtocolSpec`, `OneShotRecalibrationSpec`, and `ConformalSplitSpec` in `domain/data/`.
14. **Why this ticket exists.** Roadmap §2 locks calibration as benign-only ("attack data are reserved for evaluation and never used to fit or tune thresholds"), and Architecture §9.1 encodes this at the type level: the calibration variant has no label-policy field capable of admitting attack rows, and the TEST variant has no field capable of fitting thresholds. Architecture §7.5 similarly locks the D-temporal boundary at 70/30 (Roadmap §11), and Dirichlet-specific fields (`α`, IID sentinel handling) exist only on `DirichletPartitionSpec`, never as generic optional fields on the base `ClientPartitionSpec`, so a caller cannot construct a partition that is ambiguously Dirichlet-shaped and IID-shaped at once. These specs make benign-only calibration and TEST-cannot-fit-thresholds structural guarantees rather than runtime conventions that a careless caller could violate.
15. **Authority references.** Architecture §9.1 (dataset/split/model specification table), §8.1 (`SplitCollectionSpec` holds exactly one training, one calibration, one test split variant; calibration has no label-policy field), §7.5 (temporal locks: canonical D-temporal 70/30 boundary; inherited train/calibration allocation blocker).
16. **Dependencies.** P1-T001, P1-T011.
17. **Blocks.** P1-T028, P2-T005.
18. **Downstream consumers.** P1-T028 (scientific-protocol and policy aggregates) composes these specs directly into `ScientificProtocolSpec`; P2-T005 (outside this extracted phase) consumes them for stage implementation.
19. **Preconditions.** P1-T001, P1-T011 are `DONE`.
20. **Inputs.** Architecture §9.1, §8.1, §7.5.
21. **Allowed scope.** `domain/data/` spec modules (`datasets.py`, `partitioning.py`, `splitting.py`).
22. **Forbidden actions.** No attack-capable field on the benign calibration split variant; no generic boolean that weakens or bypasses the calibration benign-only rule; no non-70/30 temporal boundary representable as a valid `TemporalWindowSpec`/`TemporalBoundary`; TEST excluded from every conformal fit path (`ConformalSplitSpec` cannot reference the TEST split as its proper-fit or calibration substrate).
23. **Required configuration or NONE.** NONE — the mapping from external configuration to these specs is P1-T032's scope, not this ticket's.
24. **Implementation responsibilities.** Implement every listed type as a frozen, slotted, keyword-only dataclass; implement the `SplitSpec` union as a closed, explicitly tagged union (`TrainingSplitSpec` / `BenignCalibrationSplitSpec` / `TestSplitSpec`) per §6.10's discriminated-union rule; implement `ClientPartitionSpec` as a base with closed strategy-specific variants, keeping Dirichlet-only fields (`alpha`, IID sentinel) isolated to `DirichletPartitionSpec` and never present on a non-Dirichlet variant; implement `SplitCollectionSpec` to hold exactly one of each split role with exhaustive role coverage; implement `TemporalWindowSpec`/`TemporalBoundary` and `TemporalProtocolSpec` accepting only the locked 70/30 fraction and genuine capture-time evidence; implement `ConformalSplitSpec` with an explicit finite-sample quantile-index rule and no TEST-referencing field.
25. **Required unit tests.** A `BenignCalibrationSplitSpec` construction attempt carrying an attack-labeled row (or an attack-admitting field) is rejected; a `TestSplitSpec` cannot be supplied where threshold fitting is required; `ConformalSplitSpec` construction referencing the TEST split for either its proper-fit or calibration substrate is rejected; the canonical D-temporal fraction is locked at exactly 0.70 and any other fraction is rejected.
26. **Required property tests.** Split-role exhaustiveness: for every generated `SplitCollectionSpec`, exactly one `TrainingSplitSpec`, one `BenignCalibrationSplitSpec`, and one `TestSplitSpec` are present with no overlap and no missing role.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE beyond the baseline framework-confinement check.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; property lane; strict type lanes (Pyright strict).
33. **Acceptance criteria.** [ ] Calibration is benign-only by type, not by convention. [ ] TEST cannot fit thresholds. [ ] Dirichlet-only fields are isolated to `DirichletPartitionSpec`. [ ] The canonical D-temporal fraction is locked at 0.70.
34. **Completion evidence.** Changed files: `domain/data/` spec modules and their tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** `DomainValidationError` on an invalid split composition (missing role, duplicated role, attack-admitting calibration field, non-70/30 temporal boundary, TEST-referencing conformal spec). Stop and raise a blocker if the first-70%-of-data train/calibration allocation is needed at this ticket's scope: Architecture §7.5 records that allocation as an inherited-semantics blocker still unresolved by either authority document, so this ticket must not invent a split ratio to fill that gap.
36. **Stop conditions.** Stop if the first-70% train/calibration allocation is needed here; carry it forward to readiness as an inherited-semantics blocker rather than guessing a value.
37. **Deliverables.** Dataset, partition, and split specification modules.
38. **Final review checklist.** [ ] No attack-in-calibration path exists anywhere in the constructed types. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for dependency-direction violations, framework leakage into `domain/data/`, cycles/side effects, hidden defaults, and any competing split/partition specification defined outside `domain/data/`. Run import-linter/pytest-archon; record results. Confirm `domain/data/datasets.py`, `partitioning.py`, and `splitting.py` import nothing beyond the standard library plus `domain` value objects.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw boolean or optional field standing in for the closed `SplitSpec`/`ClientPartitionSpec` discriminated unions, a `dict[str, Any]` used in place of a typed split/partition manifest, `Any`/unjustified `cast`, and any Dirichlet-specific field (`alpha`, IID sentinel) leaking onto a non-Dirichlet partition variant.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25) and property lane (line 26), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Contract/architecture-specific/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; every module in `domain/data/` touched by this ticket must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE` — these are frozen specification types with no seed, batching, or recovery-identity surface of their own; partition/split seed derivation itself is P1-T012's `derive_seed` scope, not this ticket's.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced, and specifically that no concrete Edge-IIoTset/N-BaIoT/CICIoT2023 sample data or file path is referenced by any test fixture here. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm the benign-only calibration rule (Roadmap §2) is unrepresentable as violated by any constructible `BenignCalibrationSplitSpec`, and confirm the D-temporal 70/30 chronological boundary (Roadmap §11) is the only constructible `TemporalBoundary` fraction, with pseudo-time (file, row, merge, directory, or synthetic ordering) never accepted as genuine capture-time evidence.

**Audit 2 — Architecture fidelity.** Confirm every type in Architecture §9.1's dataset/split/model table for this ticket's scope exists with its stated key fields and invariants, and confirm `SplitCollectionSpec` matches §8.1's exact-one-of-each-role rule.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a caller construct a `ClientPartitionSpec` that presents Dirichlet-shaped fields while claiming a non-Dirichlet strategy tag, defeating the closed-variant discrimination this ticket relies on? Does the split-role-exhaustiveness property test actually verify no duplicate role can appear (not merely that at least one of each exists)? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.
