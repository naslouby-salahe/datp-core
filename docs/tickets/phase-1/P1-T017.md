# P1-T017 — Implement Cliff's delta and effect-size pure functions

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T017` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T017
2. **Title.** Implement Cliff's delta and effect-size pure functions
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P1 — Mandatory
6. **Ticket type.** domain
7. **Owner.** statistics-auditor
8. **Supporting agents.** —
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `cliffs_delta`.
13. **Objective.** Implement `domain/mathematics/effect_sizes.py::cliffs_delta` as a vetted, property-tested pure function — not sourced from SciPy — with antisymmetry, bounds in `[−1, 1]`, and correct handling of tied observations.
14. **Why this ticket exists.** Architecture §4.1 states explicitly that Cliff's delta is not available in SciPy and is instead implemented as a vetted, property-tested pure function in `domain/mathematics/effect_sizes.py`; Roadmap §10 lists Wilcoxon signed-rank and Cliff's δ as descriptive secondary evidence only, never a confirmatory decision branch. Because no external library supplies this function, its correctness (including the tie-handling rule that distinguishes it from a naive rank-sum computation) is this repository's own responsibility, and it must be verified by dedicated properties rather than assumed correct by analogy to a vetted external implementation.
15. **Authority references.** Architecture §4.1 (Cliff's delta implemented in-repo, not in SciPy), §5.1 (module map: `domain/mathematics/effect_sizes.py`), §21.2 (dedicated property-test requirement: antisymmetry, bounds in `[−1, 1]`, correct ties).
16. **Dependencies.** P1-T011.
17. **Blocks.** P1-T027.
18. **Downstream consumers.** P1-T027 (statistical/confirmatory/anchor-gate result types) is the sole immediate consumer: `CliffsDeltaResult` in `domain/evaluation/statistical_results.py` wraps this function's output as descriptive secondary evidence alongside the Wilcoxon result.
19. **Preconditions.** P1-T011 is `DONE`.
20. **Inputs.** Architecture §4.1, §5.1, §21.2.
21. **Allowed scope.** `domain/mathematics/effect_sizes.py`.
22. **Forbidden actions.** No framework import (no SciPy, NumPy, or any third-party numerical library) in `domain/mathematics/effect_sizes.py`; no use of Cliff's delta as a confirmatory decision branch or gating condition anywhere it is consumed.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Implement `cliffs_delta(sample_a: tuple[float, ...], sample_b: tuple[float, ...]) -> float` as a pure, deterministic function operating on plain finite-float sequences; reject non-finite inputs with `DomainValidationError`; compute the statistic as the difference between the proportion of pairs favoring `sample_a` and the proportion favoring `sample_b`, with tied pairs contributing to neither proportion (the correct-ties rule).
25. **Required unit tests.** Known-value tests: two disjoint samples yield delta = ±1; two identical samples yield delta = 0; a hand-computed small mixed sample with known ties yields the exact expected value.
26. **Required property tests.** Antisymmetry (`cliffs_delta(a, b) == -cliffs_delta(b, a)` for generated sample pairs); bounds (`-1 <= cliffs_delta(a, b) <= 1` for all generated finite samples); correct ties (a property comparing the tie-aware result against an independently computed pairwise-comparison reference, confirming tied pairs are excluded from both the favoring-`a` and favoring-`b` counts rather than silently counted toward one side).
27. **Required contract tests.** NONE.
28. **Required architecture tests.** No framework import anywhere in `domain/mathematics/effect_sizes.py` (baseline framework-confinement check).
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Property lane; unit lane; Pyright strict.
33. **Acceptance criteria.** [ ] `cliffs_delta` is antisymmetric. [ ] `cliffs_delta` is bounded in `[-1, 1]`. [ ] Tied observations are handled correctly (excluded from both favoring counts). [ ] `cliffs_delta` is descriptive-only and never a confirmatory decision branch anywhere it is consumed.
34. **Completion evidence.** Changed files: `domain/mathematics/effect_sizes.py` and its tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** `DomainValidationError` on a non-finite input value; no other failure is expected in this ticket's own scope.
36. **Stop conditions.** None.
37. **Deliverables.** The `cliffs_delta` pure function and its test suite.
38. **Final review checklist.** [ ] Property-tested and framework-free. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

### B3. Architecture boundary audit

Check the complete project for dependency-direction violations, framework leakage, cycles/side effects, and hidden defaults. Run import-linter/pytest-archon; record results. Confirm `domain/mathematics/effect_sizes.py` imports nothing beyond the standard library.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw `dict`/`tuple` return standing in for a typed result where P1-T027's result types should be used instead, `Any`/unjustified `cast`, and any consumer using `cliffs_delta`'s return value in a `match`/`case` or `if` branch that gates a confirmatory pass/fail decision.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the property lane (line 26) and unit lane (line 25), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Contract/architecture-specific/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `domain/mathematics/effect_sizes.py` must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE` — `cliffs_delta` is a pure arithmetic function over immutable finite-float sequences with no seed, batching, or recovery-identity surface; determinism follows directly from the absence of any hidden state or floating-point summation-order dependency, which B4's typing/raw-value audit already covers.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm Cliff's δ is treated only as descriptive secondary evidence per Roadmap §10, alongside Wilcoxon signed-rank, and never used to gate the Tier-1 confirmatory verdict, which remains BCa-CI-on-Δ only.

**Audit 2 — Architecture fidelity.** Confirm `cliffs_delta` lives only in `domain/mathematics/effect_sizes.py` per Architecture §5.1, imports nothing beyond the standard library per §4.1's explicit "not in SciPy" statement, and that no duplicate or competing effect-size implementation exists elsewhere in the repository.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: does the correct-ties property test actually construct samples with genuine ties and verify they are excluded from both favoring counts, or does it merely pass on tie-free generated data by coincidence? Could a future caller silently wire `cliffs_delta`'s sign into a confirmatory branch without this ticket's own tests catching it? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.
