# P1-T008 — Implement storage/artifact/manifest vocabulary

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T008` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T008
2. **Title.** Implement storage/artifact/manifest vocabulary
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** domain
7. **Owner.** artifact-lineage-auditor
8. **Supporting agents.** —
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `StorageRootKind`, `StorageVisibility`, `ArtifactNamespace` (`DATP_ANCHOR`, `JOURNAL_EXTENSION`, `RECOVERY`, `CACHE`, `STAGING`, `TEST_SANDBOX`), `SerializationFormat`, `WriteDisposition`, `ManifestType`, `ArtifactType`, `LockScope`, `ValidationStatus`, `IntegrityStatus`, `SchemaCompatibility`.
13. **Objective.** Implement the twelve `domain/artifacts/` enums of field 12 as `StrEnum`s exactly per Architecture §6.4, co-located across `keys.py`, `manifests.py`, `references.py`, `lineage.py`.
14. **Why this ticket exists.** `ArtifactNamespace` keeps `DATP_ANCHOR` and `JOURNAL_EXTENSION` structurally separate so an anchor artifact can never be silently overwritten by, or confused with, a journal artifact (Architecture §6.4, §15.2) — this is the type-level backbone of the two-track protocol separation (`ProtocolTrack.DATP_ANCHOR`/`ProtocolTrack.JOURNAL_EXTENSION`) stated in the master log's Section A. `ArtifactType` gives every independently persisted entity exactly one precise member, so no two unrelated artifact kinds share a generic catch-all type.
15. **Authority references.** Architecture §6.4 (storage/persistence vocabulary table), §15.1 (semantic roots), §15.2 (logical keys/namespace separation), §9.5 (artifact/manifest/provenance types).
16. **Dependencies.** P0-T026.
17. **Blocks.** P1-T054, P1-T055, P1-T056, P1-T057, P1-T058, P1-T059.
18. **Downstream consumers.** All six Phase 1 persistence tickets (P1-T054–P1-T059) depend on this vocabulary for their storage/artifact/manifest types; every later persisted scientific artifact depends on it transitively.
19. **Preconditions.** P0-T026 is `DONE`.
20. **Inputs.** Architecture §6.4, §15.1, §15.2, §9.5.
21. **Allowed scope.** `domain/artifacts/keys.py`, `manifests.py`, `references.py`, `lineage.py` — enum definitions only in this ticket; identity dataclasses and value objects that use these enums belong to P1-T012/P1-T013/P1-T014.
22. **Forbidden actions.** No concrete path represented as an enum member; no generic `ArtifactType` catch-all covering more than one independently persisted entity; no merged anchor/journal namespace; no namespace allowing cross-track resolution.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Define each of the twelve enums as a `StrEnum` exactly per Architecture §6.4; keep `DATP_ANCHOR`/`JOURNAL_EXTENSION`/`RECOVERY`/`CACHE`/`STAGING`/`TEST_SANDBOX` as six distinct `ArtifactNamespace` members with no shared or overlapping resolution path.
25. **Required unit tests.** Membership completeness for all twelve enums; an explicit test that every `ArtifactType` in Architecture §9.5's persisted-entity list maps to exactly one member (cross-checked against the architecture text, not merely counted); an explicit test that `ArtifactNamespace.DATP_ANCHOR` and `.JOURNAL_EXTENSION` are distinct and non-substitutable.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** Framework confinement for `domain/artifacts/`.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Pyright strict; architecture lane; unit lane.
33. **Acceptance criteria.** [ ] Namespaces separated with no shared resolution path. [ ] Every Architecture §9.5 persisted entity type maps to exactly one `ArtifactType`. [ ] No enum member is a concrete path.
34. **Completion evidence.** Changed files: `domain/artifacts/keys.py`, `manifests.py`, `references.py`, `lineage.py`, and their unit tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** NONE expected in this ticket's own scope.
36. **Stop conditions.** None.
37. **Deliverables.** Storage/artifact/manifest vocabulary.
38. **Final review checklist.** [ ] One `ArtifactType` per entity, no catch-all. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for dependency-direction violations, framework leakage, cycles/side effects, service locators, incorrect type placement, duplicate abstractions, forbidden module names, backward-compatibility scaffolding, and hidden defaults. Run import-linter/pytest-archon; record results. Confirm `domain/artifacts/` imports nothing beyond the standard library.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for raw strings standing in for `ArtifactNamespace`/`ArtifactType`, hardcoded path fragments that belong in a typed path builder (owned by P1-T054), object-shaped dictionaries, `Any`/unjustified `cast`, and a generic `ArtifactType` catch-all.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25) and architecture lane (line 28), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/contract/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; these four modules must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — closed string enums only; no seed, CUDA, path, or numeric identity field exists in this ticket's scope (path resolution itself belongs to P1-T054).` Confirm no such path was introduced regardless.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm the anchor/journal namespace separation matches the master log's Section A statement that the two protocol tracks and their namespaces are never merged or substituted for each other.

**Audit 2 — Architecture fidelity.** Confirm the four modules match Architecture §5.1/§6.4/§9.5 exactly, that every Architecture §9.5 persisted entity has exactly one `ArtifactType`, and that the modules import nothing beyond the standard library.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could any two unrelated persisted entities be forced to share one `ArtifactType`? Could `DATP_ANCHOR` and `JOURNAL_EXTENSION` namespaces ever resolve to the same physical location under any future path-resolution design? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.
