# P1-T052 — Implement analysis reporting vocabulary

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T052` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T052
2. **Title.** Implement analysis reporting vocabulary
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P1 — Mandatory
6. **Ticket type.** reporting
7. **Owner.** report-output-auditor
8. **Supporting agents.** implementation-engineer
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `ReportArtifactType`, `TableType`, `FigureType` (no Sankey member), `RenderingStatus`.
13. **Objective.** Implement the reporting vocabulary in `analysis/report_models.py` (`ReportArtifactType`, `TableType`, `FigureType`) and `RenderingStatus` in `application/reporting/tracing.py`, with `FigureType` explicitly carrying no Sankey member.
14. **Why this ticket exists.** Split out of the former combined observability/reporting/test-vocabulary ticket because this vocabulary spans the `analysis` and `application` layers, distinct from the `application`-only telemetry vocabulary (P1-T051) and the test-only vocabulary (P1-T053); each layer/concern owns exactly one ticket type per Section D. The explicit no-Sankey rule matters scientifically: Roadmap SB-19 forbids a Sankey diagram for B4 interpretability at K=3/9, requiring a contingency table or small heatmap instead — encoding "no Sankey member" at the vocabulary level makes that reviewer-facing rule structurally unrepresentable to violate, rather than a style guideline a future contributor could ignore.
15. **Authority references.** Architecture §6.6 (reporting vocabulary), §22.1 (no Sankey; B4 interpretability uses a contingency table or heatmap). Roadmap SB-19 (no Sankey diagram for B4 interpretability).
16. **Dependencies.** P0-T026.
17. **Blocks.** P1-T049.
18. **Downstream consumers.** P1-T049 (analysis table/figure/wording/report-model specification layer) and P1-T067 (report renderers) both consume this vocabulary.
19. **Preconditions.** P0-T026 is `DONE`.
20. **Inputs.** Architecture §6.6, §22.1; Roadmap SB-19.
21. **Allowed scope.** `analysis/report_models.py`, `application/reporting/tracing.py` (vocabulary only; the tracing service itself belongs to P1-T040).
22. **Forbidden actions.** No Sankey `FigureType` member; no generic coverage field shared by eligibility and conformal coverage (Architecture §22.1's disjointness rule); no persistence or rendering-library import in `analysis`.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Define `ReportArtifactType`, `TableType`, `FigureType` as `StrEnum`s with the exact roadmap-required members and no Sankey member; define `RenderingStatus` (`PENDING`, `RENDERED`, `TRACE_REFUSED`).
25. **Required unit tests.** An explicit test that no `FigureType` member is a Sankey variant; `TableType` and `FigureType` cover every roadmap table/figure family named in Roadmap §5/§9 (cross-checked by name, not merely counted).
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** `analysis/report_models.py` imports no framework, no persistence adapter, and no `infrastructure` module.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Pyright strict; architecture lane; unit lane.
33. **Acceptance criteria.** [ ] No Sankey figure type. [ ] `ReportArtifactType`/`TableType`/`FigureType` complete against the roadmap's tables/figures. [ ] `RenderingStatus` defined. [ ] `analysis` stays framework-free.
34. **Completion evidence.** Changed files: `analysis/report_models.py`, `application/reporting/tracing.py` (vocabulary additions), and their unit tests. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** NONE expected in this ticket's own scope.
36. **Stop conditions.** None.
37. **Deliverables.** Analysis reporting vocabulary.
38. **Final review checklist.** [ ] No Sankey. [ ] Framework-free `analysis`. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

### B3. Architecture boundary audit

Check the complete project for dependency-direction violations, framework/persistence leakage into `analysis`, cycles/side effects, service locators, incorrect type placement, forbidden module names, and hidden defaults. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for raw strings standing in for `TableType`/`FigureType`, a generic `coverage: float` field shared between eligibility and conformal coverage instead of the disjoint columns Architecture §22.1 requires, `Any`/unjustified `cast`, and incomplete enum dispatch.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25) and architecture lane (line 28), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/contract/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; these modules must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — closed string enums only; no seed, CUDA, or numeric identity field exists in this ticket's scope.` Confirm no such path was introduced regardless.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm no `FigureType` member is a Sankey variant (SB-19); confirm `TableType`/`FigureType` cover the roadmap's table/figure families by name (Roadmap §5, §9).

**Audit 2 — Architecture fidelity.** Confirm the modules match Architecture §5.1/§6.6/§22.1 exactly, including the disjoint eligibility/conformal coverage columns, and that `analysis/report_models.py` imports no framework/persistence.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a future `FigureType` addition sneak in a Sankey-equivalent member under a different name (e.g. `FLOW_DIAGRAM`)? Is the no-Sankey test checking member names/semantics, not just a literal string "sankey"? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.
