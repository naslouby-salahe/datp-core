# P1-T005 — Implement statistical-method/claim-outcome/absorption vocabulary

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T005` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T005
2. **Title.** Implement statistical-method/claim-outcome/absorption vocabulary
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** DONE
5. **Priority.** P0 — Blocking
6. **Ticket type.** domain
7. **Owner.** statistics-auditor
8. **Supporting agents.** —
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `StatisticalMethod` (`BCA_BOOTSTRAP` primary, `PERCENTILE_BOOTSTRAP`, `WILCOXON_SIGNED_RANK`, `CLIFFS_DELTA`, `SPEARMAN`, `LINEAR_REGRESSION_R2`), `ClaimOutcome` (seven outcomes), `AbsorptionBand` (four bands).
13. **Objective.** Implement `domain/evaluation/statistical_results.py`'s three enums as `StrEnum`s exactly per Architecture §6.1.
14. **Why this ticket exists.** `StatisticalMethod` encodes BCa as the primary confirmatory procedure and keeps Wilcoxon/Cliff's delta strictly descriptive/secondary, never a confirmatory decision branch (Roadmap §10). `ClaimOutcome` is the closed fallback-wording selector (Roadmap §12): strong-positive, weak-positive, mixed, null, opposite, feasibility-rejection, suppressed — exactly seven, no more, no fewer, so a future wording module cannot invent an eighth "convenient" outcome. `AbsorptionBand` encodes the four pre-specified model-personalization absorption bands (Roadmap §9.3) that must be applied without adjustment once computed.
15. **Authority references.** Architecture §6.1 (enum table). Roadmap §9.3 (four absorption bands, applied without adjustment), §12 (seven fallback-wording outcomes per major claim).
16. **Dependencies.** P0-T026.
17. **Blocks.** P1-T027.
18. **Downstream consumers.** P1-T027 (statistical, confirmatory, and anchor-gate result types) is the sole immediate consumer.
19. **Preconditions.** P0-T026 is `DONE`.
20. **Inputs.** Architecture §6.1; Roadmap §9.3, §12.
21. **Allowed scope.** `domain/evaluation/statistical_results.py` — enums only in this ticket; the result dataclasses that use them are P1-T027's scope.
22. **Forbidden actions.** No silent BCa-to-percentile substitution path represented anywhere in this vocabulary; no extra absorption band beyond the four locked bands; no free-text claim outcome (a `ClaimOutcome` string constructed outside the enum).
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Define the three `StrEnum`s exactly per Architecture §6.1's member lists.
25. **Required unit tests.** Membership and serialized-value stability for each of the three enums; an explicit test that `ClaimOutcome` has exactly seven members and `AbsorptionBand` has exactly four.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** Framework confinement for `domain/evaluation/statistical_results.py`.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Pyright strict; architecture lane; unit lane.
33. **Acceptance criteria.** [x] `BCa` present and distinguished as primary in downstream usage (enforced at P1-T027). [x] Exactly four absorption bands. [x] Exactly seven claim outcomes.
34. **Completion evidence.** Changed files: `domain/evaluation/statistical_results.py` and its unit tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** NONE expected in this ticket's own scope; exhaustiveness is enforced at consumers via `assert_never`.
36. **Stop conditions.** None.
37. **Deliverables.** Statistical-method/claim-outcome/absorption vocabulary enums.
38. **Final review checklist.** [x] Bands/outcomes match the roadmap exactly. [x] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [x] Read the complete roadmap and architecture before starting.
- [x] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [x] Verify every dependency is `DONE`.
- [x] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [x] Keep both status locations synchronized while active.
- [x] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [x] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for dependency-direction violations, framework leakage, cycles/side effects, service locators, incorrect type placement, forbidden module names, backward-compatibility scaffolding, hidden defaults, and out-of-phase behavior. Run import-linter/pytest-archon; record results. Confirm `domain/evaluation/statistical_results.py` imports nothing beyond the standard library.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for raw strings standing in for `ClaimOutcome`/`AbsorptionBand`/`StatisticalMethod`, object-shaped dictionaries, `Any`/unjustified `cast`, and incomplete enum dispatch.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25) and architecture lane (line 28), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/contract/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; this module must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — closed string enums only; no seed, CUDA, or numeric identity field exists in this ticket's scope.` Confirm no such path was introduced regardless.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm exactly four absorption bands matching Roadmap §9.3 and exactly seven claim outcomes matching Roadmap §12; confirm no eighth outcome or fifth band was added for convenience.

**Audit 2 — Architecture fidelity.** Confirm the module matches Architecture §5.1/§6.1 exactly and imports nothing beyond the standard library.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: is the member count test asserting the exact count (not `>=`)? Does any downstream design note (even outside this ticket) imply a BCa-to-percentile silent fallback this vocabulary should prevent? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.

## Completion record

**2026-07-14 — completed.** Added closed `StatisticalMethod`, `ClaimOutcome`, and `AbsorptionBand` vocabularies with stable snake-case serialization. Tests assert every member and serialized value, exactly seven claim outcomes, and exactly four absorption bands. `BCA_BOOTSTRAP` is present as the explicit primary method; this enum-only ticket intentionally does not provide a percentile substitution path.

**Evidence ledger.** P0-T026 was verified `DONE`. Focused pytest passed (3 tests); Ruff lint and format, Pyright strict, import-linter, architecture pytest, and `git diff --check` passed. Two randomized complete-pytest orders passed (25 tests each), and all Nox sessions, including Sonar and CodeScene, passed. Pylance has no independent repository command; configured Pyright strict is the compatible static-analysis check. Property, contract, integration, CUDA, and system tests are `NONE` by Part A. Source/test scans found serialized names only in the enum definitions and explicit stability tests, no raw-string callers, no `Any`/cast/suppression, no planning references in code/tests, and no compatibility scaffolding. The module imports only `StrEnum`; no data, training, scoring, or scientific execution path was introduced. B12 passed: exact locked counts and names align with the roadmap; architecture co-location and standard-library confinement hold; hostile review found no silent BCa-to-percentile fallback. The known vacuous Nox synthetic/scientific-smoke lanes remain assigned to P1-T070; no premature workaround was introduced. Final diff and untracked inspection contain only current Phase 1 work, the user task file, and the authorized execution workspace.
