# P1-T021 — Implement model, federation, training, and batch specifications

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T021` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T021
2. **Title.** Implement model, federation, training, and batch specifications
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** DONE
5. **Priority.** P0 — Blocking
6. **Ticket type.** domain
7. **Owner.** federated-learning-engineer
8. **Supporting agents.** —
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `AutoencoderSpec` (no BatchNorm), `FederationSpec`, `TrainingSpec` (owns `TrainingBatchSpec` exclusively), `TrainingBatchSpec`.
13. **Objective.** Implement `AutoencoderSpec` (no BatchNorm), `FederationSpec` (aggregation strategy, `local_epochs`, participation strategy, `rounds_max`, `fedprox_mu` absent for `FEDAVG` and a strictly positive frozen-grid member for `FEDPROX`), `TrainingSpec` (owns `TrainingBatchSpec` exclusively; personalization `NONE` for the core ladder), and `TrainingBatchSpec` (`effective_batch_size == micro_batch_size × gradient_accumulation_steps`).
14. **Why this ticket exists.** Roadmap §2 locks the trained autoencoder/encoder as fixed for the core DATP ladder with FedAvg as the main training baseline (`E = 1`, full participation); Roadmap SB-02 forbids adding FedBN because the encoder has no BatchNorm and adding it breaks the fixed-encoder identity; Roadmap SB-13 forbids changing the mainline AE architecture, FedAvg aggregator, or round budget within a dataset ladder. Architecture §9.1 states `fedprox_mu` is absent for `FEDAVG` and a strictly positive member of the pre-registered frozen grid for `FEDPROX`, and that personalization is `NONE` for the core ladder and matched FedProx stress cells (personalization is a separate Tier-4 stress test per Roadmap §5.4/§9.2, never a core-ladder member). These rules must be structural: a `FederationSpec` that could carry a µ value under `FEDAVG`, or a `TrainingSpec` that silently permitted personalization on the core ladder, would let a stress-test comparator masquerade as a core B1–B4 result.
15. **Authority references.** Architecture §9.1 (model/federation/training specification table), §6.1/§6.2 (scientific and model vocabulary enums: `AggregationStrategy`, `ParticipationStrategy`, `ModelPersonalizationStrategy`, `ActivationFunction`), §16.6 (frozen batch semantics: no runtime component may reduce, adapt, or renegotiate a configured batch size), §29.1 (scientific consistency: B1–B4 share one fixed checkpoint/score lineage). Roadmap §2 (fixed encoder, FedAvg baseline, E=1, full participation), SB-02 (no FedBN / no BatchNorm), SB-13 (no mainline AE/aggregator/round-budget change within a ladder).
16. **Dependencies.** P1-T002.
17. **Blocks.** P1-T028, P2-T008.
18. **Downstream consumers.** P1-T028 (scientific-protocol and policy aggregates) composes these specs into `ScientificProtocolSpec`; P2-T008 (outside this extracted phase) consumes them for stage implementation.
19. **Preconditions.** P1-T002 is `DONE`.
20. **Inputs.** Architecture §9.1, §6.1, §6.2, §16.6, §29.1; Roadmap §2, SB-02, SB-13.
21. **Allowed scope.** `domain/learning/models.py`, `domain/learning/training.py`.
22. **Forbidden actions.** No BatchNorm field or layer representable in `AutoencoderSpec`; no `fedprox_mu` value admissible under `AggregationStrategy.FEDAVG`; no partial-participation member representable where full participation is required by the core ladder; no batch-size field duplicated outside `TrainingBatchSpec`; no personalization strategy other than `NONE` representable on the core-ladder `TrainingSpec`.
23. **Required configuration or NONE.** NONE — configuration mapping into these specs is P1-T032's scope, not this ticket's.
24. **Implementation responsibilities.** Implement `AutoencoderSpec` with `input_dim`, `hidden_dims`, `bottleneck_dim`, `activation: ActivationFunction`, and no BatchNorm-related field of any kind; implement `FederationSpec` with `aggregation: AggregationStrategy`, `local_epochs: int`, `participation: ParticipationStrategy`, `rounds_max: int`, `fedprox_mu: float | None`, validating in `__post_init__` that `fedprox_mu` is `None` under `FEDAVG` and a strictly positive frozen-grid member under `FEDPROX`; implement `TrainingSpec` with `seed`, `autoencoder`, `federation`, `optimizer`, `lr`, `scheduler`, `training_batch: TrainingBatchSpec`, `precision`, `determinism`, `personalization`, owning `TrainingBatchSpec` exclusively; implement `TrainingBatchSpec` with `micro_batch_size: BatchSize`, `gradient_accumulation_steps: GradientAccumulationSteps`, `effective_batch_size: BatchSize`, `dataloader_batch_size: BatchSize`, `client_batch_partitioning`, `optimizer_step_semantics`, validating `effective_batch_size == micro_batch_size × gradient_accumulation_steps` in `__post_init__`.
25. **Required unit tests.** `AutoencoderSpec` construction is inspected to confirm no BatchNorm-related field exists; `FederationSpec` construction with a non-`None` `fedprox_mu` under `FEDAVG` is rejected, construction with `fedprox_mu = None` or a non-frozen-grid value under `FEDPROX` is rejected, and construction with a valid frozen-grid positive value under `FEDPROX` succeeds; `TrainingBatchSpec` construction where `effective_batch_size != micro_batch_size × gradient_accumulation_steps` is rejected.
26. **Required property tests.** Batch arithmetic: for generated `micro_batch_size`/`gradient_accumulation_steps` pairs, the only accepted `effective_batch_size` is their exact product, with no rounding or approximation tolerance.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE beyond the baseline framework-confinement check.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; property lane; strict type lanes (Pyright strict).
33. **Acceptance criteria.** [x] No BatchNorm is representable in `AutoencoderSpec`. [x] `fedprox_mu` rules are enforced exactly (absent for FEDAVG, strictly positive frozen-grid for FEDPROX). [x] Full participation is the only representable core-ladder participation strategy. [x] The effective-batch invariant holds exactly. [x] Core-ladder personalization is `NONE` and no other value is representable there.
34. **Completion evidence.** Changed files: `domain/learning/models.py`, `domain/learning/training.py`, and their tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** `DomainValidationError` on `fedprox_mu` misuse (present under FEDAVG, absent or non-frozen-grid under FEDPROX) or on a batch-arithmetic inconsistency.
36. **Stop conditions.** None. Architecture §27.1 fixes the inherited core-training profile and FedProx grid; this ticket waits only for its canonical value-object inputs.
37. **Deliverables.** Model, federation, training, and batch specification modules.
38. **Final review checklist.** [ ] The fixed-encoder identity is representable only correctly — no path exists to a BatchNorm-bearing or personalization-bearing core-ladder model. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [x] Read the complete roadmap and architecture before starting.
- [x] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [x] Verify every dependency is `DONE`.
- [x] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [x] Keep both status locations synchronized while active.
- [x] Change status to `IN_REVIEW` before the B2–B10 audits; recorded 2026-07-14T21:24:00Z; B12 audits will be recorded separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for dependency-direction violations, framework leakage into `domain/learning/`, cycles/side effects, hidden defaults, and any batch-size field duplicated outside `TrainingBatchSpec`. Run import-linter/pytest-archon; record results. Confirm `domain/learning/models.py` and `domain/learning/training.py` import nothing beyond the standard library plus `domain` value objects.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw `float | None` standing in for `fedprox_mu` outside `FederationSpec`'s own validated field, a raw `int` batch-size parameter competing with `TrainingBatchSpec`, `Any`/unjustified `cast`, and a `bool` toggle that could enable BatchNorm or non-`NONE` personalization on the core ladder.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25) and property lane (line 26), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Contract/architecture-specific/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `domain/learning/models.py` and `domain/learning/training.py` must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

This ticket's own scope genuinely touches batch/seed semantics: check specifically that `TrainingBatchSpec`'s effective-batch invariant cannot be bypassed by a later mutation (all fields frozen), that `TrainingSpec.seed` is a validated `Seed` value object rather than a raw `int`, and that no field here independently re-derives a seed outside P1-T012's `derive_seed`. Confirm no default batch size or accumulation-step value is silently substituted when a field is omitted — every field is required, with no hidden default.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm `AutoencoderSpec` cannot represent BatchNorm (Roadmap SB-02), confirm `FederationSpec` matches Roadmap §2's fixed FedAvg/E=1/full-participation core-ladder identity exactly, and confirm `TrainingSpec.personalization` can only be `NONE` for the core ladder, keeping Tier-4 personalization stress tests (Roadmap §9.2, E-T2) structurally outside this spec's core-ladder path.

Record, 2026-07-14T21:28:00Z: PASS. The frozen field set excludes every BatchNorm surface; `FederationSpec` admits only `FULL`, `local_epochs == 1`, and `rounds_max == 200`; `TrainingSpec` rejects every personalization member other than `NONE`. Tier-4 FedProx remains distinguished by its aggregation tag and frozen coefficient grid, not a core-ladder alternative.

**Audit 2 — Architecture fidelity.** Confirm `FederationSpec`/`TrainingSpec`/`TrainingBatchSpec` match Architecture §9.1's table exactly, including the `fedprox_mu` absent-for-FEDAVG/positive-frozen-grid-for-FEDPROX rule and the `effective_batch_size` invariant.

Record, 2026-07-14T21:28:00Z: PASS. The Architecture §9.1 field sets are represented directly; `fedprox_mu` is `float | None`, absent for FedAvg, and limited to the Architecture §27.1 grid `(0.001, 0.01, 0.1)` for FedProx. Property tests prove exact integer batch arithmetic without tolerance.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a future contributor add an optional `batch_norm_enabled: bool = False` field to `AutoencoderSpec` that defaults safely today but creates a representable-but-forbidden state tomorrow? Does the `fedprox_mu` unit test actually attempt a non-frozen-grid positive value under FEDPROX (not just `None` and zero), to confirm the frozen-grid membership check itself is enforced, not merely a sign check? Fix and rerun until clean.

Record, 2026-07-14T21:28:00Z: PASS after repair. The exact-field unit test fails if any BatchNorm-related field is added; the FedProx test rejects the positive non-grid value `0.2`; and the audit repaired raw-enum bypasses by requiring `AggregationStrategy`, `ParticipationStrategy`, and `ModelPersonalizationStrategy` members at runtime.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.

### Batch completion evidence — 2026-07-15T00:45:00Z

- The P1-T020–P1-T029 whole-project quality checkpoint passed: `uv run nox -s full_suite` (Ruff, format, strict Pyright/Pylance parity, import-linter, architecture pytest, randomized xdist 224 tests, Sonar, and CodeScene).
- The repository-wide raw-dictionary, `Any`, typing-suppression, planning-reference, documentation, no-backward-compatibility, and cleanup scans passed; no scientific execution occurred.
