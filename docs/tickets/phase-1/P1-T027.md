# P1-T027 — Implement statistical, confirmatory, and anchor-gate result types

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T027` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T027
2. **Title.** Implement statistical, confirmatory, and anchor-gate result types
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** DONE
5. **Priority.** P0 — Blocking
6. **Ticket type.** domain
7. **Owner.** statistics-auditor
8. **Supporting agents.** —
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** E-C1
12. **Architecture contracts/types owned.** `PairedDeltaResult`, `ValidBootstrapIntervalResult`/`DegenerateBootstrapIntervalResult`/`BootstrapIntervalOutcome`, `WilcoxonSignedRankResult`/`CliffsDeltaResult`, `AbsorptionResult`, `TemporalRecoveryResult`, `ConfirmatoryAnalysisResult`, `StatisticalAnalysisResult` union, `AnchorReferenceInterval`/`AnchorReproductionGateSpec`/`AnchorReproductionResult`.
13. **Objective.** Implement `PairedDeltaResult` (`Δ = B1 − B2`, locked), `ValidBootstrapIntervalResult`/`DegenerateBootstrapIntervalResult`/`BootstrapIntervalOutcome`, `WilcoxonSignedRankResult`/`CliffsDeltaResult`, `AbsorptionResult`, `TemporalRecoveryResult`, `ConfirmatoryAnalysisResult` (the Tier-1 pass rule), the closed `StatisticalAnalysisResult` union, and `AnchorReferenceInterval`/`AnchorReproductionGateSpec`/`AnchorReproductionResult`.
14. **Why this ticket exists.** Roadmap §3 and §5.1 fix the sole confirmatory endpoint's pass rule as a positive-direction 95% BCa interval on the per-seed delta that excludes zero — `ConfirmatoryAnalysisResult.passes` must encode exactly this rule so no downstream reporting code can reinterpret a crossing or negative-direction interval as a pass. Architecture §16.5 and §20.1 require BCa degeneracy (small sample sizes, ties, or acceleration-constant failure) to be a typed `DegenerateBootstrapIntervalResult`, never a silent fallback to a percentile interval, because a silently substituted interval method would misrepresent the confirmatory claim's actual statistical support. Roadmap §10's seed-extension honesty rule requires the ten-seed result to be reported even when less favorable than the five-seed anchor, so `AnchorReproductionResult`'s failed variant must be a first-class, non-suppressible outcome rather than an exception path a caller could catch and discard.
15. **Authority references.** Architecture §9.3 (`PairedDeltaResult`, `BootstrapIntervalOutcome`, `WilcoxonSignedRankResult`/`CliffsDeltaResult`, `AbsorptionResult`, `TemporalRecoveryResult`, `ConfirmatoryAnalysisResult`, `StatisticalAnalysisResult`, `AnchorReferenceInterval`/`AnchorReproductionGateSpec`/`AnchorReproductionResult`), §16.5 (Deterministic device settings context governing when reproducibility failures could otherwise masquerade as statistical degeneracy — degeneracy must remain typed, never silently substituted), §29.1 (confirmatory endpoint pass rule enforced by `StatisticalAnalysisSpec` lock and `ConfirmatoryAnalysisResult` pass rule; a less-favorable ten-seed result is never suppressed). Roadmap §3 (locked main journal claim: positive-direction BCa CI must exclude zero), §5.1 (Tier 1 confirmatory claim specification and minimum pass condition), §10 (seed-extension honesty rule: the ten-seed result becomes the main result if it widens the CI or brings it near zero; a less-favorable ten-seed result is never suppressed; a reproduced five-seed CI shifting toward zero or more than ≈20% wider than the reference width blocks expansion until resolved).
16. **Dependencies.** P1-T005, P1-T017.
17. **Blocks.** P1-T028, P2-T018.
18. **Downstream consumers.** P1-T028 (scientific-protocol and policy aggregates) and P2-T018 (outside this extracted phase) consume these result types directly.
19. **Preconditions.** P1-T005, P1-T017 are `DONE`.
20. **Inputs.** Architecture §9.3, §16.5, §29.1; Roadmap §3, §5.1, §10.
21. **Allowed scope.** `domain/evaluation/statistical_results.py`.
22. **Forbidden actions.** No silent substitution of a percentile interval for a degenerate BCa interval; no confirmatory branch that decides its pass/fail from a Wilcoxon or Cliff's delta result (those remain descriptive secondary evidence only); no suppression, hiding, or discarding of a less-favorable ten-seed result.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Implement all listed result types as frozen dataclasses; implement `ConfirmatoryAnalysisResult.passes` as true if and only if the positive-direction BCa confidence interval excludes zero; implement `BootstrapIntervalOutcome` as the closed `ValidBootstrapIntervalResult | DegenerateBootstrapIntervalResult` union with an exhaustive `assert_never` at every consumer, so degeneracy is a typed result, never an exception; implement `PairedDeltaResult` with the metric locked to `Δ = B1 − B2` (never the reverse sign); implement `AnchorReferenceInterval` with the exact reference CI `[0.647, 0.769]` (width 0.122) and `AnchorReproductionGateSpec`/`AnchorReproductionResult` encoding the toward-zero and approximately-20%-wider blocking rules.
25. **Required unit tests.** `ConfirmatoryAnalysisResult.passes` is true only for a positive-direction BCa interval excluding zero, and false for a crossing, negative-direction, or degenerate interval; `BootstrapIntervalOutcome` degeneracy is always the typed `DegenerateBootstrapIntervalResult` variant, never an exception, across small-sample and tied-value inputs; `PairedDeltaResult`'s metric is always `Δ = B1 − B2`, never `B2 − B1`.
26. **Required property tests.** Interval direction logic: for generated bootstrap interval bounds, `excludes_zero` and `direction_positive` are derived consistently from `lower`/`upper` across the full range of generated positive, negative, and zero-crossing intervals.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE beyond the baseline framework-confinement check.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; property lane; Pyright strict.
33. **Acceptance criteria.** [x] `ConfirmatoryAnalysisResult.passes` iff the positive-direction BCa CI excludes zero. [x] BCa degeneracy is always typed, never silently downgraded to a percentile interval. [x] `PairedDeltaResult`'s `Δ = B1 − B2` is locked. [x] `AnchorReproductionGateSpec` encodes the toward-zero and ≈20%-wider blocking rules exactly.
34. **Completion evidence.** Changed files: `domain/evaluation/statistical_results.py` and its tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** `StatisticsError` is reserved for a genuinely unexpected failure (adapter crash, corrupted input, unsupported method, or invalid procedure configuration) only, never for an expected BCa degeneracy, which is always a typed `DegenerateBootstrapIntervalResult`. Stop and raise a readiness blocker if the operational meaning of "material movement toward zero" for the anchor gate is unresolved by either authority document, per Architecture §27's genuine-blockers discipline, rather than inventing a numeric threshold.
36. **Stop conditions.** Stop if the operational meaning of "material movement toward zero" is unresolved; carry as a readiness blocker.
37. **Deliverables.** Statistical, confirmatory, and anchor-gate result types.
38. **Final review checklist.** [x] No hidden percentile substitution anywhere. [x] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [x] Read the complete roadmap and architecture before starting.
- [x] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [x] Verify P1-T005 and P1-T017 are `DONE`.
- [x] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; recorded 2026-07-14T23:25:00Z; implement closed statistical, confirmatory, and anchor-gate results.
- [x] Keep both status locations synchronized while active.
- [x] Change status to `IN_REVIEW` before the B2–B10 audits; recorded 2026-07-14T23:38:00Z after focused validation and local audits; B12 audits recorded separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for dependency-direction violations, framework leakage, cycles/side effects, and hidden defaults. Run import-linter/pytest-archon; record results. This ticket's architecture-specific concern is confirming no code path anywhere substitutes a percentile interval for a degenerate BCa interval or lets a Wilcoxon/Cliff's result influence `ConfirmatoryAnalysisResult.passes`.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw `float`/`tuple` standing in for `BootstrapIntervalOutcome` or `PairedDeltaResult`, `Any`/unjustified `cast`, and a boolean flag that could route a degenerate interval through the valid-interval code path.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

```
grep -rn "P[0-9]\+-T[0-9]\+\|MASTER_TICKET_LOG\|TICKET_STATUS\|DATP Core Architecture\|Journal_Extension_Master_Roadmap\|docs/tickets\|implemented for ticket\|required by the roadmap\|required by the architecture" src/ tests/ --include="*.py"
```

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25) and property lane (line 26), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Contract/architecture-specific/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `domain/evaluation/statistical_results.py` must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE` — these are pure frozen result types carrying values computed elsewhere; they hold no seed, identity-derivation, or configuration-resolution surface of their own. Reproducibility of the underlying bootstrap computation is owned by the statistics-execution tickets that populate these types.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm `ConfirmatoryAnalysisResult.passes`'s rule matches Roadmap §5.1's minimum pass condition exactly (positive-direction BCa CI excludes zero) and that `AnchorReferenceInterval` carries the exact reference values `[0.647, 0.769]`, width 0.122, per Roadmap §3.

Record, 2026-07-14T23:38:00Z: PASS. The pass property accepts only an ordered BCa interval with lower bound above zero. The immutable anchor interval is exactly `[0.647, 0.769]` and width 0.122.

**Audit 2 — Architecture fidelity.** Confirm every Architecture §9.3 type in this ticket's scope exists with its stated fields, that `BootstrapIntervalOutcome` remains a closed valid/degenerate union with no silent percentile fallback, and that `PairedDeltaResult`'s metric direction matches §9.3's locked `Δ = B1 − B2`.

Record, 2026-07-14T23:38:00Z: PASS. The bootstrap outcome is a closed valid-or-degenerate union, and the confirmatory consumer accepts no secondary Wilcoxon/Cliff input. Paired deltas reject every direction except `B1_MINUS_B2`.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a future statistics module silently catch a `DegenerateBootstrapIntervalResult` and substitute a percentile-based `ValidBootstrapIntervalResult` to "recover" a clean-looking CI? Does the anchor-gate test actually construct a five-seed reproduction wider than ≈0.147 and confirm it blocks expansion, or does it only assert the reference values exist? Fix and rerun until clean.

Record, 2026-07-14T23:38:00Z: PASS. Both interval dataclasses require `BCA_BOOTSTRAP`; no percentile construction is accepted. The unit test uses a five-seed gate and a 0.150-wide interval, exceeding 1.2 × 0.122 = 0.1464, and receives the typed failed result. Material toward-zero assessment is explicit typed evidence because neither authority supplies an invented numeric cutoff.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.

### Batch completion evidence — 2026-07-15T00:45:00Z

- The P1-T020–P1-T029 whole-project quality checkpoint passed: `uv run nox -s full_suite` (Ruff, format, strict Pyright/Pylance parity, import-linter, architecture pytest, randomized xdist 224 tests, Sonar, and CodeScene).
- The repository-wide raw-dictionary, `Any`, typing-suppression, planning-reference, documentation, no-backward-compatibility, and cleanup scans passed; no scientific execution occurred.

### Local-review evidence — 2026-07-14T23:38:00Z

- Changed `domain/evaluation/statistical_results.py`; added focused unit/property tests.
- Focused Ruff, Pyright strict, pytest, import-linter, pytest architecture, and diff checks passed. Pylance has no independent CLI; configured Pyright strict is the parity gate.
- Contract, integration, CUDA, and system test categories are `NONE`. Full-suite, randomized-order, Nox, Sonar, and CodeScene remain intentionally batched through P1-T029; this ticket is not `DONE` until that group gate passes.
