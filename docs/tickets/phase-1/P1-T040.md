# P1-T040 — Implement anchor/feasibility gates, readiness evaluator, freeze, and tracing

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T040` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T040
2. **Title.** Implement anchor/feasibility gates, readiness evaluator, freeze, and tracing
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** application
7. **Owner.** experiment-engineer
8. **Supporting agents.** statistics-auditor, artifact-lineage-auditor
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `AnchorReproductionGate`, `FeasibilityGateEvaluator`, the `ScientificReadinessResult` evaluator, `TableFigureTracer` (provenance-closure), result-freeze eligibility.
13. **Objective.** Implement `application/planning/gates.py` (`AnchorReproductionGate`, `FeasibilityGateEvaluator`), the `ScientificReadinessResult` evaluator, and `application/reporting/{freeze,tracing}.py` (`TableFigureTracer` provenance-closure check and result-freeze eligibility).
14. **Why this ticket exists.** These are the pure decision services that make the master log's phase-gating rules executable: the journal track cannot begin real expansion without a passed `AnchorReproductionGate` (Roadmap §20's Go/No-Go framing, realized structurally), and Regime D materialization is blocked by `FeasibilityGateEvaluator` unless the source/partition lineage matches exactly — this directly protects against a Regime D claim being built on a silently different dataset partition than the one the feasibility audit actually approved. `TableFigureTracer.trace` refusing to let an untraced specification render is the mechanism that makes "every table and figure traces to a manifest" (Roadmap §17) a structural guarantee rather than a manuscript-writing discipline.
15. **Authority references.** Architecture §12.3 (gate/evaluator orchestration), §22.2 (provenance-closure tracing), §27 (unresolved blockers feed `ScientificReadinessResult`). Roadmap §20 (Go/No-Go/conditional-go gates).
16. **Dependencies.** P1-T038, P1-T027.
17. **Blocks.** P2-T020, P4-T013.
18. **Downstream consumers.** P2-T020 and P4-T013 (outside this extracted phase) consume these gates and the readiness evaluator directly.
19. **Preconditions.** P1-T038, P1-T027 are `DONE`.
20. **Inputs.** Architecture §12.3, §22.2, §27; Roadmap §20.
21. **Allowed scope.** `application/planning/gates.py`, `application/reporting/freeze.py`, `tracing.py`.
22. **Forbidden actions.** No shortcut that renders a table or figure by bypassing the anchor gate; no render before the result-freeze/provenance-closure check succeeds; no relabeling of a failed feasibility result as passed (a failed `FeasibilityGateEvaluator` result must remain visibly failed, never silently downgraded to a warning).
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Implement each gate and the readiness evaluator as pure decision services operating only on already-typed evidence (no framework, no port, no I/O); implement `FeasibilityGateEvaluator` blocking Regime D materialization unless source and partition lineage match exactly; implement `TableFigureTracer.trace` raising `ProvenanceError` whenever the provenance chain does not close.
25. **Required unit tests.** The anchor gate blocks journal-track expansion when the anchor reproduction result has failed; the feasibility gate blocks Regime D whenever eligibility coverage is below the roadmap's 90% threshold or lineage does not match exactly; the tracer refuses to render any specification whose provenance chain has a gap.
26. **Required property tests.** `ScientificReadinessResult` correctly aggregates every blocker passed to it (a property test constructing varied blocker combinations confirms none is silently dropped from the aggregated result).
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE beyond the baseline framework-confinement check.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; property lane; Pyright strict.
33. **Acceptance criteria.** [ ] The anchor gate blocks journal expansion on any failure. [ ] The feasibility gate enforces exact lineage plus the 90% coverage threshold. [ ] The tracer refuses every untraced specification. [ ] `ScientificReadinessResult` carries every blocker passed to it, with none dropped.
34. **Completion evidence.** Changed files: `application/planning/gates.py`, `application/reporting/freeze.py`, `tracing.py`, and their tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** `AnchorReproductionFailure` blocks the journal track; `ProvenanceError` blocks rendering; blocking reasons are recorded in `ScientificReadinessResult` rather than silently discarded. No other failure is expected in this ticket's own scope.
36. **Stop conditions.** None.
37. **Deliverables.** Anchor/feasibility gates, the readiness evaluator, and result-freeze/tracing services.
38. **Final review checklist.** [ ] No untraced output is ever rendered. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for any render path that bypasses `TableFigureTracer.trace`, any journal-track expansion path that bypasses `AnchorReproductionGate`, dependency-direction violations, and framework leakage. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw boolean substituting for the typed `ScientificReadinessResult` blocker collection, `Any`/unjustified `cast`, and a "warning" severity silently downgrading what should be a hard `ProvenanceError`/feasibility failure.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25) and property lane (line 26), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Contract/architecture-specific/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; these three modules must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE` for direct seed/CUDA surface — these are pure decision services over already-typed evidence. Confirm regardless that a gate decision is a pure function of its typed evidence input, with no hidden clock or environment dependency that could make the same evidence yield a different gate outcome on a rerun.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm the anchor gate structurally enforces the requirement that journal-track work depends on anchor acceptance where the roadmap requires it (Roadmap §20's Go/No-Go framing), and that the feasibility gate's 90% coverage threshold matches Architecture §7.5's `REGIME_D_MINIMUM_COVERAGE` exactly, with no independently drifted threshold value.

**Audit 2 — Architecture fidelity.** Confirm `TableFigureTracer.trace`'s provenance-closure check matches Architecture §22.2 exactly, and that no gate or evaluator here performs I/O or imports a framework.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a caller construct a `ScientificReadinessResult` by hand that omits a real blocker, and would any test catch the omission? Does the feasibility gate's "exact lineage" check actually compare full typed identities, or could two differently-sourced partitions with coincidentally matching summary statistics slip through? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.
