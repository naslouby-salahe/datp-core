# P1-T037 — Implement reusable pipeline stage functions and concrete services

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T037` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T037
2. **Title.** Implement reusable pipeline stage functions and concrete services
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** DONE
5. **Priority.** P0 — Blocking
6. **Ticket type.** application
7. **Owner.** implementation-engineer
8. **Supporting agents.** experiment-engineer
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `application/stages/*` functions (`inspect_dataset` through `analyze_statistics`), `CheckpointSelector`, `PolicyEvaluator` concrete services.
13. **Objective.** Implement the `application/stages/*` pipeline-stage functions and the concrete services `CheckpointSelector` and `PolicyEvaluator`, all orchestrating ports (P1-T034–P1-T036) using domain types only; `train_model` dispatches to either the federated or centralized training port from one shared stage function, and B0 runs through the identical stage shape as the FedAvg ladder.
14. **Why this ticket exists.** These stage functions are the reusable pipeline the planner (P1-T038) later sequences into experiment cells — each stage is a pure orchestration of ports with no framework import, so the same `train_model` stage function that runs FedAvg for B1–B4 also runs B0's centralized training, without a parallel, duplicated code path (Architecture §9.2's "B0 via same stages" rule). `PolicyEvaluator` deliberately consumes a single committed test-score aggregate rather than two independently passed benign/attack references, because accepting two loose references would reopen exactly the partial-commit risk P1-T023's atomic score-artifact design closes.
15. **Authority references.** Architecture §5.1 (stage module list and order), §9.2 (B0 uses the same stage shape), §12.3 (stage orchestration rules).
16. **Dependencies.** P1-T034, P1-T035, P1-T036.
17. **Blocks.** P1-T038, P2-T004.
18. **Downstream consumers.** P1-T038 (ExperimentPlanner and ScoreReuseGate) and P2-T004 (outside this extracted phase) consume these stage functions and services directly.
19. **Preconditions.** P1-T034, P1-T035, P1-T036 are `DONE`.
20. **Inputs.** Architecture §5.1, §9.2, §12.3.
21. **Allowed scope.** `application/stages/`, including the concrete services `application/stages/select_checkpoint.py` and `evaluate_policy.py`.
22. **Forbidden actions.** No framework import anywhere in `application/stages/`; no combined partition-and-preprocess stage (each remains its own named stage per Architecture §5.1's module list); `PolicyEvaluator` must never accept two loose (independently referenced) benign/attack score aggregates in place of one committed `ClientTestScoreArtifact`.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Implement each named stage function calling only ports and domain types; implement dispatch by resolved specification (`train_model` reads the resolved `FederationSpec`/centralized identity to select its training port); implement `CheckpointSelector` as a concrete, framework-free service that reads candidate descriptors and decides which round to select without persisting anything itself; implement `PolicyEvaluator` as pure confusion-count arithmetic over a committed test-score aggregate and the fixed `EligibleClientSet` built in P1-T026.
25. **Required unit tests.** Stage dispatch is exercised with test doubles for every port (a stage function under test never touches a real adapter); `CheckpointSelector` only decides and never calls a persistence port itself (verified by asserting no store-port method is invoked during selection); `PolicyEvaluator` rejects a calibration-role score set passed where a test-role set is required.
26. **Required property tests.** NONE.
27. **Required contract tests.** Every stage function is verified to use ports exclusively — no direct framework or adapter-internal call from within a stage function body.
28. **Required architecture tests.** No framework import anywhere in `application/stages/`.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; contract lane; architecture lane; Pyright strict.
33. **Acceptance criteria.** [x] Every named stage is implemented purely over ports. [x] B0 executes through the identical stage shape as the FedAvg ladder, with no duplicated parallel path. [x] `CheckpointSelector` decides; a separate store persists. [x] `PolicyEvaluator` consumes only a committed test-score aggregate.
34. **Completion evidence.** See the B14 completion-evidence ledger below.
35. **Failure and blocker behavior.** Stage-level errors surface as the typed families defined in P1-T030; no other failure is expected in this ticket's own scope.
36. **Stop conditions.** None.
37. **Deliverables.** Reusable pipeline stage functions and the two concrete services.
38. **Final review checklist.** [x] No framework import anywhere in `application/stages/`. [x] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [x] Read the complete roadmap and architecture before starting.
- [x] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [x] Verify every dependency is `DONE`.
- [x] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [x] Keep both status locations synchronized while active.
- [x] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [x] No blocking condition arose; no `BLOCKED` transition was required.
- [x] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for a framework import anywhere in `application/stages/`, a stage function bypassing a port to call an adapter directly, dependency-direction violations, and a combined partition-and-preprocess stage. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw tuple standing in for a typed stage request/result, `Any`/unjustified `cast`, and `PolicyEvaluator` accepting two loose score references instead of one committed aggregate.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25), contract lane (line 27), and architecture lane (line 28), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `application/stages/` must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

Check specifically that `CheckpointSelector`'s decision is a pure function of its candidate descriptors (no hidden clock or environment dependency) and that no stage function here introduces an unowned/scattered seed call — seed ownership remains with the adapters constructed in later tickets (P1-T043).

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced; all stage-function tests in this ticket use test doubles over synthetic data. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm B0 runs through the identical stage shape as B1–B4 rather than a parallel path, matching Roadmap §4's statement that B0 is a separate centralized reference but still a comparable pipeline output; confirm `PolicyEvaluator` never fits a threshold from evaluation data (it only evaluates, per the roadmap's benign-only calibration split).

**Audit 2 — Architecture fidelity.** Confirm every stage listed in Architecture §5.1 exists with the correct name and order, and that `application/stages/` imports no framework, per §12.3.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could `train_model`'s dispatch logic accidentally route a FedAvg-ladder cell through the centralized training port (or vice versa) under a malformed resolved spec, and would a test catch that misroute before it reached a real adapter? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.

## B14 completion-evidence ledger

- **Dependencies.** P1-T034, P1-T035, and P1-T036 were verified `DONE` in the synchronized ticket register before implementation.
- **Implementation.** Implemented the eleven exact Architecture §5.1 stage modules, shared discriminated training dispatch, pure `CheckpointSelector`, and pure committed-test-only `PolicyEvaluator` in `src/datp_core/application/stages/`; added port-double unit coverage and stage-boundary architecture coverage.
- **Ticket-specific validation.** `uv run ruff check tests/unit/application/test_stage_services.py tests/architecture/test_stage_layer_confinement.py`, `uv run ruff format --check tests/unit/application/test_stage_services.py tests/architecture/test_stage_layer_confinement.py`, `uv run pyright tests/unit/application/test_stage_services.py tests/architecture/test_stage_layer_confinement.py`, `uv run pytest tests/unit/application/test_stage_services.py -q`, and `uv run pytest tests/architecture/test_stage_layer_confinement.py -m architecture -q` passed (`8 passed` unit tests; `2 passed` architecture tests).
- **Contract and architecture lanes.** `uv run nox -s architecture` passed: import-linter and all seven architecture tests, including the stage confinement test. The stage functions use only typed application ports and domain values; no adapter or framework import is reachable from `application/stages/`.
- **Project-wide checkpoint.** The complete Nox suite passed: Ruff lint/format, Pyright, import-linter, architecture, xdist-safe, serial, resource-intensive, CUDA, synthetic, scientific-smoke, Sonar, and CodeScene. The ticket’s direct property, integration, CUDA, and system/end-to-end categories are `NONE`; no real data, training, or scoring was performed. The configured scientific-smoke lane collected no tests.
- **Randomized full-suite evidence.** `uv run pytest -q --randomly-seed=13759` and `uv run pytest -q --randomly-seed=86421` each passed (`301 passed`); the only warning was Torch's third-party `pynvml` deprecation warning.
- **Type and quality gates.** Pyright strict passed with no errors; this is the configured Pylance-parity gate, and no standalone Pylance CLI is configured. Sonar reported no unresolved issue; CodeScene completed successfully. `git diff --check` passed (line-ending conversion warnings only).
- **Repository and documentation audit.** Complete project inventory reviewed across `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, root metadata, `pyproject.toml`, `noxfile.py`, and configured lint/type/test/Sonar/CodeScene tooling. Static scans found no `Any`, `cast`, or type-ignore in the stage scope; no framework/adapter dependency or seed call in `application/stages/`; and no prohibited planning references in production or test code. Retired ticket IDs appear only in explicit historical-retirement documentation, never as live tickets.
- **Scientific-drift audit 1 — roadmap.** One `train_model` stage selects its typed federated or centralized port from the resolved training request; B0 therefore shares the stage shape used by B1–B4. `PolicyEvaluator` only evaluates count evidence from a committed test aggregate and cannot fit a threshold or receive calibration scores.
- **Scientific-drift audit 2 — architecture.** All eleven §5.1 stage names are present in the mandated order; `application/stages/` has only application-port and domain imports and has no framework import.
- **Scientific-drift audit 3 — adversarial dispatch.** Concrete typed federated and centralized port doubles prove each request reaches only its matching port. A malformed/mismatched request is rejected by the typed request shape before any real adapter is invoked.
- **No-backward-compatibility and cleanup.** No aliases, shims, redirects, deprecated APIs, or legacy stage names were introduced. The full worktree is intentionally shared with the ordered Phase 1 implementation; its existing tracked and untracked files were inspected and preserved. No temporary file was introduced by this ticket.
- **Blockers, risks, and skipped checks.** No blocker arose. No mandatory check was skipped. Direct property, integration, CUDA, and system checks were skipped only because Part A explicitly specifies `NONE`; their configured project lanes nevertheless ran at the shared checkpoint. The third-party Torch deprecation warning remains external to this ticket.
