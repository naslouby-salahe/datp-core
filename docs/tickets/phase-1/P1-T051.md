# P1-T051 — Implement application telemetry vocabulary and contracts

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T051` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T051
2. **Title.** Implement application telemetry vocabulary and contracts
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P1 — Mandatory
6. **Ticket type.** telemetry
7. **Owner.** implementation-engineer
8. **Supporting agents.** —
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `LogEventKind`, `LogSink`, `LogFormat`, `StructuredEvent`, `EventContext`, the discriminated per-kind detail-type union (`RunPlannedDetail`, `StageLifecycleDetail`, `ArtifactEventDetail`, `ResourceEventDetail`, `DeterminismEventDetail`, `LineageEventDetail`, `FederatedRoundEventDetail`, `HeartbeatEventDetail`, `RecoveryEventDetail`, `TestProfileDetail`), the `EventSink` Protocol.
13. **Objective.** Implement `application/ports/telemetry.py`: the observability vocabulary and typed event envelope of field 12, with the `EventSink.publish(event: StructuredEvent) -> None` Protocol as the sole port; the concrete adapter implementing this port is P1-T065's scope, not this ticket's.
14. **Why this ticket exists.** This ticket exists as a genuine split, not an arbitrary one: it is one of four responsibilities (telemetry, reporting vocabulary, test-support vocabulary, and the retired mega-ticket's original scope) that a single prior ticket tried to own together, in violation of Section D's rule that a ticket has exactly one type and `domain/application` are distinct layers owned by distinct tickets. Observability vocabulary and the event envelope are an `application` concern, not a `domain` one (Architecture §6.5): log-rendering and event-envelope shape are operational, not scientific meaning, so this ticket must not live in `domain/` alongside P1-T001–P1-T009's scientific vocabulary. It is also distinct from the `analysis`/`application` reporting vocabulary (P1-T052, a different layer combination) and the `tests/support/`-only test vocabulary (P1-T053, never imported by production code).
15. **Authority references.** Architecture §6.5 (observability vocabulary — an `application` concern), §19.1 (event model), §19.2 (rendering/rules), §9.2 layer-ownership rule via §3.5 (ownership matrix: `application` owns ports).
16. **Dependencies.** P0-T026.
17. **Blocks.** P1-T065.
18. **Downstream consumers.** P1-T065 (structured telemetry adapter) implements the `EventSink` Protocol defined here; every stage function that publishes a `StructuredEvent` in later phases depends on this vocabulary transitively through the port.
19. **Preconditions.** P0-T026 is `DONE`.
20. **Inputs.** Architecture §6.5, §19.1, §19.2.
21. **Allowed scope.** `application/ports/telemetry.py` only.
22. **Forbidden actions.** No placement of observability enums in `domain/`; no event kind bound to more than one detail type; no array, per-sample score, secret, dataset-row, or full-configuration-dump field in any detail type (Architecture §19.2's payload restriction).
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Define `LogEventKind`, `LogSink`, `LogFormat` as `StrEnum`s; define `EventContext` and `StructuredEvent` as frozen, slotted, keyword-only dataclasses; define the closed detail-type union with exactly one detail type bound per `LogEventKind` member; define the `EventSink` Protocol.
25. **Required unit tests.** Each `LogEventKind` member binds to exactly one detail type (an exhaustive one-to-one mapping test, not a sample check); no detail type carries an array, per-sample score, secret, dataset row, or full configuration dump (a field-inventory test against the forbidden-payload list).
26. **Required property tests.** NONE.
27. **Required contract tests.** `EventSink.publish` accepts every constructed `StructuredEvent` variant without a type error (a construction-and-dispatch test over every detail type).
28. **Required architecture tests.** `application/ports/telemetry.py` imports no framework and no `infrastructure` module.
29. **Required integration tests.** NONE — adapter-level integration belongs to P1-T065.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Pyright strict; architecture lane; unit lane; contract lane.
33. **Acceptance criteria.** [ ] `LogEventKind`/`LogSink`/`LogFormat` defined. [ ] `StructuredEvent`/`EventContext` frozen and complete. [ ] The detail union is exactly one-to-one with `LogEventKind`. [ ] `EventSink` Protocol defined with no framework import.
34. **Completion evidence.** Changed files: `application/ports/telemetry.py` and its tests. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** A genuinely ambiguous event-kind-to-detail mapping is recorded as a design-decision blocker rather than guessed; no other failure is expected.
36. **Stop conditions.** None.
37. **Deliverables.** Application telemetry vocabulary and `EventSink` port.
38. **Final review checklist.** [ ] No framework leakage. [ ] One detail type per event kind. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

### B3. Architecture boundary audit

Check the complete project for dependency-direction violations (specifically: this vocabulary living in `domain/` instead of `application/` would itself be a violation), framework leakage, cycles/side effects, service locators, incorrect type placement, forbidden module names, and hidden defaults. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for raw strings standing in for `LogEventKind`, a generic `dict[str, Any]` event-detail payload replacing the discriminated union, `Any`/unjustified `cast`, and a detail type carrying a raw array/row/secret field.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live, and that no comment or docstring describes this vocabulary as living in `domain/`.

### B7. Complete validation requirements

Run the unit lane (line 25), contract lane (line 27), and architecture lane (line 28), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; this module must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — this ticket defines a diagnostic event envelope and port only; it carries no scientific value, seed, or CUDA path.` Confirm regardless that no scientific value is ever sourced only from an event (every scientific value must also exist in a persisted artifact per Architecture §12.4's later requirement, verified again at P1-T065).

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — this ticket carries no scientific claim or metric`. Positive finding required: confirm no scientific value is representable only through an event detail (it must also be persistable as an artifact elsewhere).

**Audit 2 — Architecture fidelity.** Confirm this vocabulary lives in `application/`, not `domain/`, per Architecture §6.5's explicit statement, and that the module imports no framework/`infrastructure`.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a future detail type smuggle a raw NumPy array or per-sample score past the forbidden-payload test by nesting it inside another field? Is the one-to-one event-kind-to-detail test exhaustive over every `LogEventKind` member, or only a sample? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.
