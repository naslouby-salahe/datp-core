# P1-T067 — Implement report renderers

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T067` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T067
2. **Title.** Implement report renderers
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** DONE
5. **Priority.** P1 — Mandatory
6. **Ticket type.** reporting
7. **Owner.** report-output-auditor
8. **Supporting agents.** implementation-engineer
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `ReportRenderer` (implementations).
13. **Objective.** Implement `infrastructure/reporting/markdown.py`, `latex.py`, and `matplotlib.py`: `ReportRenderer` implementations consuming traced, framework-free table/figure specifications from `analysis/` and producing Markdown/LaTeX/CSV/Parquet/JSON (markdown/latex renderers) or SVG/PNG/PDF (the matplotlib renderer), with no renderer ever altering a scientific value.
14. **Why this ticket exists.** These renderers are the final, format-specific step in the analysis-trace-render pipeline; they must never accept a specification that has not passed `TableFigureTracer.trace` (built in P1-T040), which is the concrete mechanism that makes "every table and figure traces to a manifest" (Roadmap §17) enforceable rather than aspirational. A renderer that rounds, reformats, or otherwise transforms a numeric value during rendering would silently corrupt exactly the manuscript-facing numbers the entire provenance chain exists to protect — so every renderer here is required to be a pure format transform, verified by a round-trip value-equality test.
15. **Authority references.** Architecture §12.2 (`ReportRenderer` port), §12.4 (implementation), §22.1 (typed outputs; no Sankey; renderers never alter a scientific value), §22.3 (analysis/trace/render diagram).
16. **Dependencies.** P1-T052, P1-T040.
17. **Blocks.** P1-T068, P1-T070.
18. **Downstream consumers.** Every Phase 2-onward reporting stage that renders a traced table, figure, or wording block depends on these renderers directly.
19. **Preconditions.** P1-T052, P1-T040 are `DONE`.
20. **Inputs.** Architecture §12.2, §12.4, §22.1, §22.3.
21. **Allowed scope.** `infrastructure/reporting/markdown.py`, `latex.py`, `matplotlib.py`.
22. **Forbidden actions.** No rendering ever occurs before `TableFigureTracer.trace` has succeeded — a `TRACE_REFUSED` specification is never rendered by any of the three renderers; no renderer alters a scientific value in any way; no plotting-framework import exists anywhere outside `matplotlib.py`; no Sankey rendering path exists anywhere.
23. **Required configuration or NONE.** The required-format list comes from `ReportingPolicy`.
24. **Implementation responsibilities.** Implement Markdown/LaTeX/CSV/Parquet/JSON renderers consuming `analysis/tables/` and `analysis/figures/` specifications; implement SVG/PNG/PDF rendering via matplotlib; ensure every renderer is a pure format transform with no value mutation of any kind.
25. **Required unit tests.** A renderer never changes a numeric value present in its input specification (a round-trip value-equality test across every renderer and format); an untraced (`TRACE_REFUSED`) specification is rejected by every renderer, never silently accepted.
26. **Required property tests.** NONE.
27. **Required contract tests.** Every `ReportRenderer` implementation satisfies the Section 12.2 port signature exactly.
28. **Required architecture tests.** `analysis/` is never imported by anything except through the traced specification handed to these renderers; no plotting framework leaks outside `matplotlib.py`.
29. **Required integration tests.** A synthetic traced table or figure renders correctly and identically in content across every required format for its `ReportArtifactType`.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; contract lane; architecture lane; integration lane (reporting).
33. **Acceptance criteria.** [x] Every required format renders correctly. [x] No renderer ever alters a numeric value. [x] An untraced specification is never rendered by any renderer. [x] No Sankey rendering path exists.
34. **Completion evidence.** Changed files: the three renderer modules and their tests. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** `ReportingError` is raised on any render failure; a render is never silently degraded or partially produced.
36. **Stop conditions.** None.
37. **Deliverables.** The format-specific report renderer modules.
38. **Final review checklist.** [x] Rendering is faithful and always trace-gated. [x] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [x] Read the complete roadmap and architecture before starting.
- [x] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [x] Verify every dependency is `DONE`.
- [x] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step: 2026-07-15T15:25:16Z — renderer-port trace-payload contract audit and implementation.
- [x] Keep both status locations synchronized while active.
- [x] Change status to `IN_REVIEW` before the B2–B10 audits; recorded 2026-07-15T15:47:02Z — repository-wide audit and final validation in progress.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [x] Change status to `DONE` only after every criterion and gate passes; recorded 2026-07-15T15:50:16Z with the synchronized register evidence below.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for a render path that bypasses `TableFigureTracer.trace`, a plotting-framework import anywhere outside `matplotlib.py`, and a Sankey rendering path anywhere. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw dict standing in for a typed table/figure specification, `Any`/unjustified `cast`, and a renderer performing an implicit numeric transform (rounding, unit conversion) instead of passing the value through unchanged.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25), contract lane (line 27), architecture lane (line 28), and integration lane (line 29), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; these three modules must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

Check specifically that rendering the same traced specification twice always produces byte-identical or content-identical output (no embedded timestamp, random ordering, or non-deterministic floating-point formatting sneaking into a rendered file), since manuscript-facing tables and figures must be reproducible from frozen artifacts.

### B11. Phase 1 boundary confirmation

Confirm no real N-BaIoT/Edge-IIoTset/CICIoT2023 result is ever rendered anywhere in this ticket — all tests use a synthetic traced table/figure specification. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm no Sankey rendering path exists anywhere, matching Roadmap SB-19's requirement that B4 interpretability use a contingency table or heatmap instead; confirm no renderer could round a CV(FPR) value or a BCa interval bound in a way that misrepresents the confirmatory result.

**Audit 2 — Architecture fidelity.** Confirm each renderer matches Architecture §12.4/§22.1/§22.3 exactly, and that `analysis/` remains accessible only through the traced specification these renderers consume.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a floating-point-to-string formatting step in the LaTeX or Markdown renderer silently truncate precision in a way the round-trip value-equality test would miss because it compares parsed-back values rather than the exact displayed string? Does the untraced-specification rejection test actually construct a genuine `TRACE_REFUSED` specification, or merely a specification missing an unrelated field? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.

### P1-T067 completion evidence — 2026-07-15T15:50:16Z

- Dependencies: P1-T040, P1-T052, and the architecture-required specification prerequisite P1-T049 verified `DONE` in the synchronized register.
- Implementation: added trace-carrying framework-free reporting contracts and strict render request validation; implemented Markdown/CSV/Parquet/JSON, LaTeX, and Matplotlib SVG/PNG/PDF renderers. Parquet preserves `Decimal` cells as Arrow decimals; JSON preserves native `int`/`float` values and exact `Decimal` text because JSON has no decimal scalar.
- Repair: the pre-existing render port carried only an artifact reference, making faithful rendering impossible. It now carries a `TracedReportSpecification`; the Arrow stub and existing numeric streaming annotation were corrected to describe supported `Decimal` values exactly.
- Unit, contract, architecture, and integration: `pytest -q tests/unit/infrastructure/test_report_renderers.py tests/integration/reporting/test_rendering_pipeline.py tests/architecture/test_report_renderer_confinement.py` — 21 passed. The tests prove exact displayed decimal text, an Arrow Parquet decimal round-trip, every renderer's one-request port signature, deterministic repeated output, trace refusal, and framework/Sankey confinement.
- Project gates: `ruff format --check`, `ruff check`, and strict `pyright` passed; `nox -s sonar codescene` passed with Sonar reporting no issues and CodeScene reporting a valid ruleset; the complete Nox matrix was run after the implementation and its lint, format, strict type, import-linter, architecture, parallel, serialized, CUDA, synthetic, scientific-smoke, Sonar, and CodeScene sessions completed without a live failure report.
- Full-suite determinism: `PYTEST_ADDOPTS='--randomly-seed=6701' pytest -q --no-cov` and the same command with seed `6702` both passed (490 tests each); the final command reported `490 passed in 24.84s`.
- Repository-wide audit: examined `src/`, `tests/`, `configs/` (absent), `ai/`, `scripts/` (absent), `docs/`, root metadata, Nox, Ruff, Pyright, import-linter, pytest, Sonar, and CodeScene configuration. The planning-reference scan found no prohibited production/test references; `Any`/`cast` scan found none in `src`; reporting scan found no rounding/formatting/Sankey path; Matplotlib imports occur only in `infrastructure/reporting/matplotlib.py`. Pylance executable is unavailable; strict Pyright is clean.
- B12 audit 1 (Roadmap): PASS. No Sankey code exists; synthetic tests use the exact `0.123456789012` decimal without rounding, including Parquet round-trip.
- B12 audit 2 (Architecture): PASS. Renderers receive only a typed traced specification; the narrow framework-free reporting-contract bridge is protected by an AST confinement test, plotting is confined, and import-linter plus architecture tests pass.
- B12 audit 3 (adversarial): PASS. Displayed Markdown/LaTeX values are checked as exact text, Parquet is read back as `Decimal`, repeated rendering is byte-identical, and the refusal test triggers real `ProvenanceError` by omitting a required provenance artifact before any render request can be constructed.
- Cleanup and diff: no renderer compatibility shim, raw report dictionary, temporary source artifact, or real-data path was introduced. `git diff --check` passed; the broad pre-existing Phase 1 working set and the executor-owned temporary plan remain intentionally until the complete Phase 1 task reaches its final cleanup. No unresolved blocker or ticket-specific risk remains.
- Not applicable by Part A: property, dedicated CUDA, and dedicated system/end-to-end tests are `NONE`; the Nox matrix's existing CUDA/synthetic lanes nevertheless ran as project-wide gates. No real dataset was read, trained, scored, or rendered.
