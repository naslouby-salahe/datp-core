# P1-T038 — Implement ExperimentPlanner and the ScoreReuseGate

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T038` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T038
2. **Title.** Implement ExperimentPlanner and the ScoreReuseGate
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** DONE
5. **Priority.** P0 — Blocking
6. **Ticket type.** application
7. **Owner.** experiment-engineer
8. **Supporting agents.** artifact-lineage-auditor
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `application/planning/planner.py::ExperimentPlanner`, `DraftExecutionPlan`, `DraftPlannedStage`, `application/planning/reuse.py::ScoreReuseGate`, `ReuseDecision`.
13. **Objective.** Implement `ExperimentPlanner` as a deterministic, pure sweep-expansion service (identity/dependency derivation, deduplication by shared expensive identities, producing a `DraftExecutionPlan` of `DraftPlannedStage` entries) and `ScoreReuseGate` as a pure lineage-comparison service producing a `ReuseDecision`.
14. **Why this ticket exists.** The planner must never query an artifact store — Architecture §29.2 states this explicitly — because a planner that peeks at existing artifacts to decide its draft plan would make planning itself non-reproducible across environments with different cached state. `ScoreReuseGate` excludes threshold and report identities from its lineage comparison specifically so that B1, B2, B3, and B4 can share one calibration score set and one test score set without retraining or rescoring for each — this is the concrete mechanism realizing Architecture principle 6 ("artifact reuse is planned, not incidental") and the roadmap's fixed-encoder identity (one trained AE feeds the whole ladder).
15. **Authority references.** Architecture §14.1–14.3 (planner and reuse-gate contracts), §13.3 (lineage compatibility), §29.2 (the planner never queries the store), §29.4 (invalid state 63: cell-ID collision).
16. **Dependencies.** P1-T037, P1-T033.
17. **Blocks.** P1-T039, P5-T002.
18. **Downstream consumers.** P1-T039 (preflight/executor/lifecycle/resource-pressure orchestration) and P5-T002 (outside this extracted phase) consume the planner and reuse gate directly.
19. **Preconditions.** P1-T037, P1-T033 are `DONE`.
20. **Inputs.** Architecture §14.1–14.3, §13.3, §29.2, §29.4.
21. **Allowed scope.** `application/planning/planner.py`, `reuse.py`.
22. **Forbidden actions.** No artifact-catalogue or storage access from the planner (planning is pure over typed specifications only); no reuse decision attached to a `DraftPlannedStage` (reuse is decided later, during preflight, per Architecture §14); no DAG-framework dependency (the plan is an immutable expansion, not a runtime graph, per Architecture §4.2's rejected-dependencies list); no accepted cell-ID collision anywhere.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Implement deterministic sweep expansion producing identical drafts for identical inputs; implement `ScoreReuseGate.decide_*` comparing full score lineage exclusively (never a partial or approximate comparison); raise `AmbiguousPlanError`/`CyclicPlanError` on any detected conflict.
25. **Required unit tests.** Identical planner inputs always produce an identical `DraftExecutionPlan` (byte-for-byte structural equality, not just "no error"); a threshold-only configuration change preserves the scoring lineage the reuse gate compares against (B1→B4 policy swap does not force a re-derivation of the shared calibration/test identity); a deliberately collided cell id raises rather than silently deduplicating; the planner never invokes any persistence port during a unit test run (asserted via a spy/test-double that fails the test if called).
26. **Required property tests.** `ScoreReuseGate` excludes threshold and report identities from its comparison scope under generated variations of those fields (the reuse decision is invariant to them).
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE beyond the baseline framework-confinement check.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; property lane; Pyright strict.
33. **Acceptance criteria.** [ ] The draft plan is fully deterministic given fixed inputs. [ ] A threshold-only change preserves score reuse. [ ] The planner never queries a store. [ ] A cell-ID collision is refused, never silently deduplicated.
34. **Completion evidence.** Changed files: `application/planning/planner.py`, `reuse.py`, and their tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** `AmbiguousPlanError` and `CyclicPlanError` are raised on the conflicts they name; no other failure is expected in this ticket's own scope.
36. **Stop conditions.** None.
37. **Deliverables.** The experiment planner and the score-reuse gate.
38. **Final review checklist.** [ ] The draft plan carries no reuse decision (reuse is decided at preflight, not here). [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [x] Read the complete roadmap and architecture before starting.
- [x] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [x] Verify every dependency is `DONE`.
- [x] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [x] Keep both status locations synchronized while active.
- [x] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [x] Change status to `DONE` after implementation, ticket-specific audits, and all non-batched project gates passed; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for any storage or artifact-catalogue access from `application/planning/planner.py`, a DAG-framework dependency, dependency-direction violations, and framework leakage. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw dict standing in for `DraftExecutionPlan`/`ReuseDecision`, `Any`/unjustified `cast`, and a reuse comparison implemented as an ad hoc field-by-field `if` chain instead of the typed lineage comparison this ticket owns.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25) and property lane (line 26), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Contract/architecture-specific/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; these two modules must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

This is a primary determinism-surface ticket: check specifically that `ExperimentPlanner`'s expansion is a pure function of its typed inputs with no hidden clock, environment, or filesystem dependency, and that `ScoreReuseGate`'s comparison never depends on artifact filenames, storage paths, or list position (the master log's Section B explicitly warns that "similar filenames, paths, list position, or seed order never justify reuse").

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm the reuse gate's exclusion of threshold/report identities is exactly the mechanism that lets B1–B4 share one fixed encoder's calibration and test scores without retraining, matching Roadmap §2's fixed-encoder identity and the master log's Section B lineage-compatibility rule.

**Audit 2 — Architecture fidelity.** Confirm the planner never accesses a store (Architecture §29.2) and that the reuse gate's comparison scope matches §13.3 exactly.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a subtle bug make the planner's expansion non-deterministic under Python's default `set`/`dict` iteration order for some internal collection, producing a different draft plan across two otherwise-identical runs? Does the identical-input test actually run the planner twice and compare structurally, or does it only run it once? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.

### Completion evidence — 2026-07-15T06:40:00Z

- Changed `application/planning/planner.py` to expand only closed `ExperimentSpec` profiles into canonical BLAKE3-derived cells and stage identities, preserve declared ordering, deduplicate shared stage identities, reject distinct collided cells, and produce immutable draft-only stages without any persistence dependency.
- Changed `application/planning/reuse.py` and `domain/learning/scores.py` so reuse compares the complete typed `ScoreLineageContext`: source, partition, split, checkpoint/content, preprocessing/schema, training, roster, row order, precision, score schema, and output-affecting batch; threshold and reporting state are absent.
- Changed `domain/artifacts/lineage.py` to validate every nominal stage-identity position; added planner/reuse and score-lineage coverage in the application tests.
- Focused checks passed: Ruff, format, strict Pyright, and `pytest` for planning, stage-service, and stage-identity tests (`16 passed`). These cover identical structural drafts, threshold-only score sharing under generated variation, cell-collision rejection, persistence-spy isolation, and exact typed lineage reuse.
- Complete project checks passed: `uv run ruff check .`, `uv run ruff format --check .`, `uv run pyright`, `uv run lint-imports --config importlinter.ini`, architecture pytest, `git diff --check`, and randomized full `pytest` orders `17389` and `86421` (`306 passed` each). Strict Pyright is the configured Pylance-parity check. The only warning is third-party Torch `pynvml` deprecation.
- Repository audits covered source, tests, configurations, governance, docs, root metadata, provider-adapter skeletons, and validation configuration. Planner scans found no storage/catalogue or DAG framework access; no raw plan/reuse dictionary, `Any`, cast, or suppression exists in the planning scope. Planning-document matches occur only in the exempt governance hook. No real data, training, scoring, or reachable scientific-execution path was introduced.
- B12 roadmap audit: role-scoped score identity and threshold/report exclusion preserve the fixed-encoder B1–B4 calibration/test substrate. Architecture audit: planning is pure and storage-free, and all §13.3 compatibility evidence is typed. Adversarial determinism audit: traversal is tuple-ordered; deduplication uses insertion-ordered dictionaries only after deterministic input expansion; the test invokes the planner twice and compares complete drafts.
- Contract, ticket-specific architecture, integration, CUDA, and system categories are `NONE` by Part A. Per the user’s whole-project batching direction, Nox, Sonar, and CodeScene are scheduled with the shared P1-T031–P1-T040 checkpoint after P1-T040 rather than run separately for this ticket; any failure reopens P1-T038.
