# P1-T007 — Implement feasibility/rejection/reuse/blocking vocabulary

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T007` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T007
2. **Title.** Implement feasibility/rejection/reuse/blocking vocabulary
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** domain
7. **Owner.** experiment-engineer
8. **Supporting agents.** artifact-lineage-auditor
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `FeasibilityStatus`, `RejectionReason` (covering E-R1..E-R8), `ReuseIncompatibilityReason` (13 members), `BlockingReason`, `ClientEligibilityStatus`, `ClientEligibilityReason`.
13. **Objective.** Implement `domain/experiments/feasibility.py`'s four enums and the eligibility enums in `domain/evaluation/operating_points.py`, each a `StrEnum` exactly per Architecture §6.1.
14. **Why this ticket exists.** Rejected experiments (E-R1 CICIoT2023 B-b, E-R2 CICIoT2023 temporal probing, E-R3 FedBN, E-R4 `B-LaridiFaithful`, E-R5 MIA leakage probe, E-R6 streaming drift, E-R7 Byzantine-robust conformal, E-R8 broad personalized-FL benchmark) must be represented as non-executable typed records, never as a free-text note that a future contributor could reinterpret as executable. `ReuseIncompatibilityReason`'s 13 members give every artifact-reuse rejection a typed cause rather than a generic "incompatible" flag. `ClientEligibilityReason` is deliberately narrow: missing or invalid traffic-rate evidence is explicitly *not* a client-eligibility reason — it fails alert-burden configuration/evaluation-suite construction before per-client eligibility is ever evaluated (Architecture §6.1).
15. **Authority references.** Architecture §6.1 (enum table, including the explicit note on traffic-rate evidence). Roadmap §9.4 (suppressed/rejected experiments E-R1..E-R8 and their reasons), §7 (Regime B-b rejection), §11 (temporal rejection, `TEMPORAL_REJECTED_NO_TIMESTAMPS`).
16. **Dependencies.** P0-T026.
17. **Blocks.** P1-T029, P4-T012.
18. **Downstream consumers.** P1-T029 (experiment identity/profile/cell aggregates) and P4-T012 (outside this extracted phase) consume this vocabulary directly.
19. **Preconditions.** P0-T026 is `DONE`.
20. **Inputs.** Architecture §6.1; Roadmap §9.4, §7, §11.
21. **Allowed scope.** `domain/experiments/feasibility.py`; the eligibility-status enums in `domain/evaluation/operating_points.py`.
22. **Forbidden actions.** No executable member (planner arm, port, or strategy) for a rejected experiment; no free-text rejection/blocking reason string; missing/invalid traffic-rate evidence must not be represented as a `ClientEligibilityReason`.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Define `FeasibilityStatus`, `RejectionReason` (with stable rendered labels `B_B_REJECTED_NO_METADATA` and `TEMPORAL_REJECTED_NO_TIMESTAMPS`), `ReuseIncompatibilityReason` (exactly 13 members), `BlockingReason`, `ClientEligibilityStatus`, `ClientEligibilityReason` as `StrEnum`s.
25. **Required unit tests.** Membership completeness for each enum; stable rendered labels for `B_B_REJECTED_NO_METADATA`/`TEMPORAL_REJECTED_NO_TIMESTAMPS`; an explicit test that no `ClientEligibilityReason` member represents a traffic-rate-evidence failure.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** Rejected-experiment members carry no executable path anywhere in the codebase (asserted jointly with the catalogue tests owned by P1-T050/Phase 4); framework confinement for both modules.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Pyright strict; architecture lane; unit lane.
33. **Acceptance criteria.** [ ] E-R1..E-R8 reasons present, exactly. [ ] 13 reuse-incompatibility reasons, exactly. [ ] Stable rendered rejection labels match the roadmap's exact strings. [ ] No free-text reason anywhere.
34. **Completion evidence.** Changed files: `domain/experiments/feasibility.py`, `domain/evaluation/operating_points.py` (eligibility enums), and their unit tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** NONE expected; rejections are typed, never a free-text fallback.
36. **Stop conditions.** None.
37. **Deliverables.** Feasibility/rejection/reuse/blocking vocabulary.
38. **Final review checklist.** [ ] Rejected is never executable. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

### B3. Architecture boundary audit

Check the complete project for dependency-direction violations, framework leakage, cycles/side effects, service locators, incorrect type placement, forbidden module names, backward-compatibility scaffolding, hidden defaults, and — specifically for this ticket — any executable code path (planner branch, port method, strategy registration) reachable from a `RejectionReason` member. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for raw strings standing in for `RejectionReason`/`BlockingReason`/`ReuseIncompatibilityReason`, object-shaped dictionaries, `Any`/unjustified `cast`, and a boolean flag standing in for a typed eligibility reason.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live; confirm `docs/decisions/` (if present) is consistent with these rejection reasons rather than contradicting them.

### B7. Complete validation requirements

Run the unit lane (line 25) and architecture lane (line 28), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/contract/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; these two modules must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — closed string enums only; no seed, CUDA, or numeric identity field exists in this ticket's scope.` Confirm no such path was introduced regardless.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced — including no executable path for any rejected experiment (E-R1..E-R8). This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm all eight rejected experiments (Roadmap §9.4) are represented with the correct reason and no executable path; confirm the stable rendered labels match the roadmap's exact strings; confirm no rejected item was silently promoted to executable scope.

**Audit 2 — Architecture fidelity.** Confirm both modules match Architecture §5.1/§6.1 exactly and import nothing beyond the standard library.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: is there any planner, port, or strategy code path anywhere in the repository that a `RejectionReason` member could accidentally trigger? Does the traffic-rate-evidence exclusion test actually exercise the alert-burden configuration failure path, or merely check enum membership? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.
