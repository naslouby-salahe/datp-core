# P1-T053 — Implement test-support vocabulary and typed test profiles

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T053` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T053
2. **Title.** Implement test-support vocabulary and typed test profiles
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** DONE
5. **Priority.** P1 — Mandatory
6. **Ticket type.** test-support
7. **Owner.** implementation-engineer
8. **Supporting agents.** —
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `TestSuiteKind`, `TestDataScale`, `TestIsolationMode`, `TestDeviceRequirement`, `TestParallelismMode`, `ExternalDependencyPolicy`, `ArtifactRetentionPolicy`, `TestOutcome`, `TestResourceBudget`, `TestArtifactPolicy`, `TestLoggingSpec`, `TestProfileSpec`.
13. **Objective.** Implement the test vocabulary and `TestProfileSpec` under `tests/support/`: the eight `StrEnum`s of field 12, plus `TestResourceBudget`, `TestArtifactPolicy`, `TestLoggingSpec`, and the composed `TestProfileSpec`, explicitly never imported by any `src/datp_core` production package.
14. **Why this ticket exists.** Split out of the former combined observability/reporting/test-vocabulary ticket because test infrastructure is a `tests/support/` concern, never a production application contract (Architecture §21.1, §12.3), and must never share a ticket type with the `application`/`analysis` vocabularies of P1-T051/P1-T052 — Section D's controlled vocabulary lists `test-support` as its own distinct ticket type precisely to prevent this concern from being folded into `application` or `analysis`.
15. **Authority references.** Architecture §6.7 (test vocabulary), §21.1 (`TestProfileSpec`), §21.3 (configuration ownership: `configs/tests/` holds profile data, `pyproject.toml` holds markers only).
16. **Dependencies.** P0-T026.
17. **Blocks.** P1-T070.
18. **Downstream consumers.** Every `tests/` fixture and profile resolution from Phase 1 onward, and the Phase 1 socle-readiness gate (P1-T070), which exercises test profiles end-to-end.
19. **Preconditions.** P0-T026 is `DONE`.
20. **Inputs.** Architecture §6.7, §21.1, §21.3.
21. **Allowed scope.** `tests/support/profiles.py` and related test-vocabulary modules under `tests/support/`.
22. **Forbidden actions.** No import of this vocabulary by any `src/datp_core` production package; no test-profile field placed in `pyproject.toml`; no raw mapping standing in for `TestProfileSpec`.
23. **Required configuration or NONE.** Profile data lives in `configs/tests/` as plain YAML with no `__init__.py` and no executable Python; this ticket implements the code-side types that map to it.
24. **Implementation responsibilities.** Define the eight test-vocabulary `StrEnum`s; define `TestResourceBudget`, `TestArtifactPolicy`, `TestLoggingSpec`, and the composed `TestProfileSpec` as frozen dataclasses.
25. **Required unit tests.** `tests/support/` vocabulary and `TestProfileSpec` are not importable from any `src/datp_core` module — verified by an architecture-level import check, not a trivial existence test.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** No production module under `src/datp_core` imports `tests/support/`; the dependency runs one way only (Architecture §21).
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Pyright strict; architecture lane; unit lane.
33. **Acceptance criteria.** [ ] All eight test-vocabulary enums defined. [ ] `TestProfileSpec` composed correctly. [ ] Not importable from `src/datp_core`. [ ] No profile data placed in `pyproject.toml`.
34. **Completion evidence.** Changed files: `tests/support/` vocabulary modules and their tests. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** BLOCKED — `TestLoggingSpec` requires the canonical application logging enums; no duplicate test-only lookalikes are permitted.
36. **Stop conditions.** `TestLoggingSpec` is defined with `LogSink` and `LogFormat` in Architecture §21.1, but their canonical P1-T051 vocabulary is blocked pending typed identities. Unblock after P1-T051 is complete; do not replace the fields with raw strings or define parallel enums under `tests/support/`.
37. **Deliverables.** Test-support vocabulary and `TestProfileSpec`.
38. **Final review checklist.** [ ] Test-only; never imported by production code. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [x] Read the complete roadmap and architecture before starting.
- [x] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [x] Verify every dependency is `DONE`.
- [x] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [x] Keep both status locations synchronized while active.
- [x] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [x] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [x] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for any `src/datp_core` production import of `tests/support/` (the one-way-dependency violation specific to this ticket), framework leakage, cycles/side effects, service locators, and hidden defaults. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for raw strings standing in for `TestSuiteKind`/`TestDataScale`, a raw mapping standing in for `TestProfileSpec`, `Any`/unjustified `cast`, and a test-profile field hardcoded into `pyproject.toml` rather than `configs/tests/`.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25) and architecture lane (line 28), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/contract/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `tests/support/` must pass strict typing with no unexplained `Any`, consistent with the repository-wide strict-typing requirement even for test code.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — this ticket defines test-configuration vocabulary only; no seed, CUDA, or scientific-identity field exists in its scope.` Confirm no such path was introduced regardless.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — test-execution configuration carries no scientific claim.` Positive finding required: confirm `TestDataScale.REAL_FULL`/`REAL_SUBSAMPLE` members exist only as a declarative label a profile *could* select, with no Phase 1 profile actually selecting real data (Phase 1 validation is synthetic-only per the master log's Phase 1 overview).

**Audit 2 — Architecture fidelity.** Confirm the vocabulary lives under `tests/support/`, never `src/datp_core`, per Architecture §21.1, and that the one-way dependency (tests may reference production code; production code never references `tests/support/`) holds.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a future `src/datp_core` module import `tests/support/` for a "convenient" helper, silently violating the one-way dependency? Does the import-check test actually scan the full `src/datp_core` tree, or only a sample module? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.

## Completion record

**2026-07-14 — completed after P1-T051 unblocked canonical logging types.** `tests/support/profiles.py` now owns the seven test-only closed enums and frozen composed profile dataclasses. It deliberately imports the existing domain `ArtifactRetentionPolicy`, which Architecture §6.7 declares shared with `ArtifactPolicy`; no duplicate enum was introduced. `TestLoggingSpec` uses the canonical application `LogSink` and `LogFormat`.

**Evidence ledger.** Focused unit and whole-tree production-isolation architecture tests passed (5 tests). The architecture test parses every `src/datp_core/**/*.py` module and rejects every `tests.support` import. Ruff lint/format, strict Pyright, configured import-linter (`7 kept, 0 broken`), the complete architecture lane, `git diff --check`, and two randomized whole-project pytest runs passed (`261 passed` each; only the third-party `pynvml` deprecation warning). Pylance has no separate repository command; strict Pyright is the compatible check. Property, contract, integration, CUDA, and system tests are `NONE` by Part A. Nox, Sonar, and CodeScene remain queued for the shared P1-T031–P1-T040 project-quality checkpoint under the user-directed batching policy.

**Audits.** Audit 1: real-data enum labels are declarative only; no `configs/tests/` profile selects real data and no execution path was added. Audit 2: the vocabulary is under `tests/support/` and production imports remain one-way. Audit 3: the AST scan is exhaustive over the full production tree, not a sample. No raw profile mapping, `Any`/cast, planning reference, compatibility layer, profile data in `pyproject.toml`, real data, training, scoring, or scientific execution was introduced. Repository inventory covered source, tests, configs, governance, docs, root tooling, Nox, and provider-adapter skeletons; `scripts/` is absent.
