# P1-T001 — Implement dataset/regime/partition/split domain vocabulary

Standalone implementation specification. This file is understandable without reading `docs/MASTER_TICKET_LOG.md`; where this file and the master log ever disagree in wording, this file governs execution and the master log's Section G/H rows for `P1-T001` govern identity (ID, title, dependencies, blocks, type, priority, scientific-execution classification, campaign scope).

## Part A — Ticket template

1. **Ticket ID.** P1-T001
2. **Title.** Implement dataset/regime/partition/split domain vocabulary
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED (must match `docs/tickets/TICKET_STATUS.md` at all times)
5. **Priority.** P0 — Blocking
6. **Ticket type.** domain
7. **Owner.** implementation-engineer
8. **Supporting agents.** datp-protocol-guardian
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** — (none; this ticket implements vocabulary only, not an experiment)
12. **Architecture contracts/types owned.** `Dataset` (`N_BAIOT`, `CICIOT2023`, `EDGE_IIOTSET`), `Regime` (`A`, `B_A`, `C`, `D`, `D_TEMPORAL` — no `B-b` member), `ClientDefinitionStrategy`, `SplitRole`, `RecalibrationMode`, `TemporalOutcome`.
13. **Objective.** Implement the finite dataset/regime/partition/split enums in `domain/data/`: `Dataset`, `Regime`, `ClientDefinitionStrategy`, `SplitRole`, `RecalibrationMode`, `TemporalOutcome`, each as a `StrEnum` with stable UPPER_SNAKE members and snake_case serialized values, co-located with the capability subpackage rather than centralized.
14. **Why this ticket exists.** These vocabularies gate every downstream dataset/regime/partition/split-aware specification and identity built in later Phase 1 tickets (most directly P1-T019). Regime B-b (the rejected CICIoT2023 device/MAC repartition, `B_B_REJECTED_NO_METADATA`) must have no executable member anywhere in this vocabulary, because the architecture provides no executable path for out-of-scope work (Architecture §1.3). This is a discrete, single-capability vocabulary ticket, kept separate from every other domain vocabulary ticket (P1-T002–P1-T009) so that each finite vocabulary has exactly one owner and one review surface.
15. **Authority references.** `docs/DATP Core Architecture.md` §6.1 (enum table: `Dataset`, `Regime`, `ClientDefinitionStrategy`, `SplitRole`, `RecalibrationMode`, `TemporalOutcome`), §5.1 (`domain/data/` module map), §6.10 (exhaustiveness and explicit discrimination); `docs/Journal_Extension_Master_Roadmap.md` §7 (experimental regime table — Regime A/B-a/C/D/D-temporal, no Regime B-b), §9.4 (E-R1: CICIoT2023 B-b rejected, `B_B_REJECTED_NO_METADATA`).
16. **Dependencies.** P0-T026.
17. **Blocks.** P1-T019.
18. **Downstream consumers.** P1-T019 (dataset, partition, and split specifications) builds directly on these enums; every later dataset/regime-aware specification, identity, and configuration schema in Phase 1 onward depends on them transitively.
19. **Preconditions.** P0-T026 is `DONE` (repository baseline quality gate passed: layered skeleton, Ruff, Pyright strict, import-linter, pytest-archon, Nox, and the governance catalogue all exist and pass).
20. **Inputs.** Architecture §6.1, §5.1, §6.10; Roadmap §7, §9.4.
21. **Allowed scope.** `domain/data/datasets.py`, `domain/data/partitioning.py`, `domain/data/splitting.py` — enum definitions only; no specification, value object, or identity dataclass in this ticket's scope (those belong to P1-T019, P1-T020, and later tickets).
22. **Forbidden actions.** No `Regime.B_B` or any other out-of-scope member; no framework import inside `domain/data/`; no concrete path, client identifier, or dynamic label represented as an enum member; no specification dataclass added here (scope creep into P1-T019's ownership); no centralized `vocabulary.py` module.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Define each vocabulary as a `StrEnum` with UPPER_SNAKE member names and stable snake_case serialized values; co-locate `Dataset`/`ClientDefinitionStrategy` in `datasets.py`/`partitioning.py`, `SplitRole`/`RecalibrationMode`/`TemporalOutcome` in `splitting.py`, exactly per the Architecture §5.1 module map; ensure `Regime` has exactly the five members `A`, `B_A`, `C`, `D`, `D_TEMPORAL` and no sixth member.
25. **Required unit tests.** Membership completeness and serialized-value stability for each of the six enums (`Dataset`, `Regime`, `ClientDefinitionStrategy`, `SplitRole`, `RecalibrationMode`, `TemporalOutcome`); an explicit test asserting `Regime` has no `B_B`/out-of-scope member.
26. **Required property tests.** NONE — these are small closed enumerations; membership is exhaustively covered by the unit tests above, not a property.
27. **Required contract tests.** NONE — no port or adapter consumes these enums directly in this ticket's scope.
28. **Required architecture tests.** `domain/data/` imports no framework (no NumPy/pandas/PyArrow/Torch/Pydantic) and no `infrastructure`/`config`/`application` module; no forbidden generic module name (`utils`, `helpers`, `common`, `base`, `misc`, `shared`) is introduced.
29. **Required integration tests.** NONE — pure vocabulary has no adapter to integrate.
30. **Required CUDA tests.** NONE — no CUDA execution path exists in this ticket's scope.
31. **Required system/end-to-end tests.** NONE — end-to-end validation is owned by the Phase 1 socle-readiness gate (P1-T070).
32. **Validation commands.** Pyright strict over `domain/data/`; the architecture lane (import-linter + pytest-archon framework-confinement and no-forbidden-name checks); the unit-test lane for `domain/data/`.
33. **Acceptance criteria.** [ ] All six enums exist exactly as named in Architecture §6.1. [ ] `Regime` has no `B_B` or other out-of-scope member. [ ] No framework import exists anywhere under `domain/data/`. [ ] Every enum's serialized values are stable snake_case strings verified by a unit test.
34. **Completion evidence.** Changed files: `domain/data/datasets.py`, `domain/data/partitioning.py`, `domain/data/splitting.py`, and their unit tests. Commands run and their actual results (Pyright, architecture lane, unit lane). Cleanup confirmation: no scratch file left behind; `git status` before and after matches except for the listed files.
35. **Failure and blocker behavior.** If a member's exact meaning is ambiguous against the roadmap's regime table (for example, whether a new regime variant is implied anywhere), stop and record an explicit design-decision blocker in `docs/tickets/TICKET_STATUS.md` rather than inventing a member.
36. **Stop conditions.** Stop immediately if `docs/Journal_Extension_Master_Roadmap.md` §7 or `docs/DATP Core Architecture.md` §6.1 cannot be read, or if a member's meaning is ambiguous against the roadmap regime table.
37. **Deliverables.** `domain/data/datasets.py`, `partitioning.py`, `splitting.py` enum modules and their unit tests.
38. **Final review checklist.** [ ] `Regime.B_B` absent. [ ] Vocabulary co-located per capability, not centralized. [ ] No framework import. [ ] `Status` in this file and in `docs/tickets/TICKET_STATUS.md` match at all times per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap (`docs/Journal_Extension_Master_Roadmap.md`) before starting this ticket.
- [ ] Read the complete architecture (`docs/DATP Core Architecture.md`) before starting this ticket.
- [ ] Read this ticket file completely.
- [ ] Read `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify that every dependency listed in this ticket's "Dependencies" field is `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change this ticket's status to `IN_PROGRESS` before implementation begins.
- [ ] Update `docs/tickets/TICKET_STATUS.md` in the same change.
- [ ] Record the start timestamp and current implementation step in `docs/tickets/TICKET_STATUS.md`.
- [ ] Keep both status locations (this file's `Status` field and `docs/tickets/TICKET_STATUS.md`) synchronized while work remains active.
- [ ] Change the status to `IN_REVIEW` before beginning the mandatory audits in B2–B10 below.
- [ ] Record the result of each of the three scientific-drift audits (B12) separately.
- [ ] Change the status to `BLOCKED` immediately if a blocking condition is found; record it in both status locations.
- [ ] Record the exact blocker and unblock condition.
- [ ] Change the status to `DONE` only after every acceptance criterion above and every gate in B2–B10 passes.
- [ ] Record all completion evidence in both this file and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify that this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only new/modified files, the current git diff, this ticket's own module, or files directly owned by this ticket. At minimum inspect: `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, the import-linter configuration, the Ruff configuration, the Pyright configuration, the pytest configuration, the Sonar configuration, provider-specific adapters (`.github/`, `.claude/`, `.agents/`, `.codex/`), and root project metadata.

A defect found during this audit cannot be dismissed because it already existed, another ticket introduced it, another agent introduced it, this ticket did not touch the file, it is outside the current diff, it is described as legacy code, or fixing it is inconvenient. When the roadmap and architecture provide an unambiguous correction, fix the issue before marking this ticket `DONE`. When the issue requires a scientific or architectural decision that is not defined by either authority, mark this ticket `BLOCKED` and record the exact unresolved decision — never invent a solution.

### B3. Architecture boundary audit

Check the complete project for: dependency-direction violations; framework imports outside infrastructure; framework leakage into domain or application; Pydantic or external configuration schemas leaking past the configuration boundary; CLI wiring dependencies directly; import cycles; import-time side effects; service locators; universal context objects; deep inheritance where composition is required; incorrect type placement; duplicate abstractions; forbidden generic modules (`utils`, `helpers`, `common`, `base`, `misc`, `shared`); backward-compatibility shims, redirects, aliases, or compatibility facades; hidden defaults; silent fallbacks; mutable scientific specifications; unstable identity construction; improperly typed process-boundary objects; accidental future-phase implementation; source behavior forbidden in the current phase (Section B11 below). Run import-linter (`P0-T011`) and pytest-archon (`P0-T012`) and record their result. For this ticket specifically: confirm `domain/data/` imports nothing beyond the standard library, per Architecture §3.1's `domain` row.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan the complete project for: raw strings where controlled enums are required; hardcoded scientific identifiers; hardcoded status values; hardcoded policy names; hardcoded path fragments that belong in typed path builders; mutable scientific objects; object-shaped dictionaries; `dict[str, Any]`; `Dict[str, Any]`; untyped `dict`; arbitrary nested dictionaries; `Any`; broad `object` payloads; unexplained `cast`; unjustified `type: ignore`; tuple-based pseudo-objects; boolean flags replacing discriminated variants; optional-field combinations used to infer variants; generic request/result/payload/context/manager/handler objects; incomplete enum dispatch; missing `assert_never`; duplicated constants; primitive obsession; raw defaults spread across the codebase. Internal domain and application contracts must use enums, frozen dataclasses, value objects, typed request/result objects, discriminated variants, or dedicated collection types; third-party mappings are allowed only at narrow boundaries, precisely typed, validated, and immediately converted into approved typed internal models before propagating further. For this ticket: confirm no downstream module represents `Dataset`/`Regime`/`ClientDefinitionStrategy`/`SplitRole` as a raw string anywhere it is available as an enum.

### B5. Prohibition on ticket and document references in code

Search the complete repository (implementation code, test code, runtime configuration, exception messages, logs, names, comments, and docstrings — planning Markdown under `docs/` is exempt) for each of the following patterns and confirm zero matches outside planning documents:

```
P[0-9]+-T[0-9]+
MASTER_TICKET_LOG
TICKET_STATUS
DATP Core Architecture
Journal_Extension_Master_Roadmap
docs/tickets
implemented for ticket
required by the roadmap
required by the architecture
```

Runtime and test code must express the actual scientific or engineering rule the code enforces, never its planning-document origin.

### B6. Documentation and comment audit

Check the complete project for: stale documentation; stale paths; stale package names; retired IDs; references to nonexistent files; contradictory documents; inaccurate docstrings; comments that no longer match behavior; copied AI explanations; banner comments; separator comments; verbose narrative comments; misleading status claims; duplicated documentation; undocumented behavior changes; claimed checks that were not actually run. Do not delete useful existing documentation merely to reduce comments; correct stale documentation wherever the correction is unambiguously supported by the roadmap and architecture. Confirm the retired Phase 1 ticket IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live tickets anywhere this ticket touches.

### B7. Complete validation requirements

Run: the unit-test lane for `domain/data/` (line 25); the architecture lane (line 28); Pyright strict; Ruff lint and format; Pylance configuration compatibility; import-linter; pytest-archon; the complete test suite; randomized test ordering; all mandatory Nox sessions; Sonar analysis and quality gate; complete git-diff inspection; untracked-file and cleanup inspection. Property, contract, integration, CUDA, and system/end-to-end categories are `NONE` for this ticket (lines 26–31) and are recorded as such explicitly, not omitted.

Impacted tests never replace the complete test suite. Do not delete tests to make the suite pass, weaken assertions, add unjustified skips or `xfail`, exclude difficult files, relax typing, add broad ignores, suppress Sonar issues solely to obtain a passing gate, or add artificial coverage exclusions. A pre-existing failure must be investigated and corrected when the required correction is authority-grounded.

### B8. Pyright and Pylance requirements

`P0-T008` is the canonical owner of Pyright strict configuration and Pylance configuration parity for the whole repository. This ticket's three modules must pass Pyright strict with no unexplained `Any`, no untyped definition, no broad file exclusion, and no unjustified suppression, and must remain compatible with Pylance's workspace configuration.

### B9. Sonar requirements

`P0-T027` is the canonical owner of the Sonar project configuration and quality gate for the whole repository. This ticket's contribution must pass that gate with no unresolved blocker/critical issue, no unexplained major issue, no artificial source exclusion, and no suppression added solely to obtain a passing result.

### B10. Determinism and reproducibility audit

Check the complete project for: scattered global seeds; seed-list position used as identity; nondeterministic algorithms; silent CPU fallback; silent batch-size reduction; automatic scientific-configuration mutation; automatic retry after CUDA OOM; unsafe CUDA multiprocessing; incorrect process start methods; non-picklable process-boundary objects; unstable serialization; raw-float hashing; `NaN`/infinity in identity-bearing fields; hidden environment influence on scientific identity; unrecorded configuration changes; unsafe parallel CUDA execution. For this ticket: `NOT_APPLICABLE — this ticket introduces closed string enums only; no seed, CUDA path, serialization, or numeric field exists in its scope.` Confirm no such path was introduced regardless.

### B11. Phase 1 boundary confirmation

Confirm, and record explicitly, that this ticket's implementation introduced none of the following, which remain forbidden throughout Phase 1 regardless of any other instruction: real N-BaIoT/Edge-IIoTset/CICIoT2023 data access; real training or scoring; reduced real-data debugging runs; scientific execution of any kind; a reachable path to any of the above from any Phase 1 module. This ticket is inherently compliant: it defines closed vocabulary only, with no data-reading or execution behavior. Validation uses no data at all (enums have no data dependency), consistent with `docs/MASTER_TICKET_LOG.md` Section F's Phase 1 "Permitted work"/"Forbidden work" statement — the ticket-conversion task's own Section 19 is written for `PHASE_NUMBER = 0`; for Phase 1 this ticket instead applies the master log's explicit Phase 1 boundary statement, since the master log is the authority for phase-membership and phase-boundary content per the conversion task's own authority order. This substitution is recorded once here and in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

All three audits below are required before this ticket may reach `DONE`; each must be recorded separately with its own evidence, and each must be rerun after any fix until it passes.

**Audit 1 — Roadmap fidelity.** Confirm `Regime` has exactly the five roadmap-defined regimes (A, B-a, C, D, D-temporal) and no `B-b` member, matching Roadmap §7's regime table and §9.4's `E-R1` rejection (`B_B_REJECTED_NO_METADATA`); confirm no executable path for out-of-scope work was created; confirm no scientific execution or scientific implementation occurred during this ticket.

**Audit 2 — Architecture fidelity.** Confirm the three modules match Architecture §5.1's module map exactly (no enum misplaced into another capability's module) and that `domain/data/` imports nothing beyond the standard library per §3.1.

**Audit 3 — Adversarial cross-check.** Review this ticket's implementation as a hostile reviewer: does every enum member trace to an explicit Architecture §6.1 or Roadmap §7 citation rather than an invented convenience value? Does any test merely assert construction succeeds without checking the specific exhaustiveness/no-B-b invariant named in "Why this ticket exists"? Fix any issue found and rerun this audit until it passes clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is not the canonical owner of any cross-cutting governance responsibility. It must remain compliant with the gates owned by `P0-T008` (Pyright/Pylance parity), `P0-T022` (repository-wide post-implementation audit mechanism), `P0-T023` (raw-dictionary and ticket/document-reference prohibitions), `P0-T024` (scientific-drift audit mechanism), `P0-T025` (stale-documentation enforcement), `P0-T026` (ticket-status lifecycle governance), `P0-T027` (Sonar quality gate), and the Phase 1 socle-readiness gate `P1-T070`, which depends on this ticket transitively through P1-T019.
