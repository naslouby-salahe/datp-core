# P1-T063 — Implement the CheckpointStore adapter (scientific and recovery persistence)

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T063` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T063
2. **Title.** Implement the CheckpointStore adapter (scientific and recovery persistence)
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** persistence
7. **Owner.** implementation-engineer
8. **Supporting agents.** artifact-lineage-auditor, reproducibility-auditor
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `CheckpointStore` (implementation: `find_compatible`, `save`, `save_recovery`, `load_recovery`), `CheckpointLookupResult`, `CheckpointWriteResult`, `RecoveryWriteResult`, `RecoveryLookupResult`, `RecoveryCheckpointPolicy` (enforcement), `RecoveryState` (persistence).
13. **Objective.** Implement `infrastructure/persistence/checkpoints.py` and `recovery.py`: one `CheckpointStore` adapter with methodically distinct methods, roots (`SCIENTIFIC_CHECKPOINTS` versus `RECOVERY_STATE`), and namespaces for scientific checkpoints versus recovery state, so recovery state can never be resolved as a scientific checkpoint.
14. **Why this ticket exists.** Split out, but kept as a single ticket (rather than splitting scientific-versus-recovery further) because the architecture requires `CheckpointStore` to be ONE port with deliberately distinct methods and roots — never two separate ports and never a generic save/load pair (Architecture §12.2). Splitting the implementation across two tickets would risk an inconsistent single interface.
15. **Authority references.** Architecture §12.2 (`CheckpointStore` port), §12.4 (implementation), §17.1 (scientific checkpoint), §17.2 (recovery state), §17.3 (persistence/resume semantics), §29.4 invalid state 35 (recovery never selected/cited as scientific).
16. **Dependencies.** P1-T054, P1-T055, P1-T056, P1-T057, P1-T059.
17. **Blocks.** P1-T068, P1-T070, P2-T010.
18. **Downstream consumers.** P1-T037 (`CheckpointSelector` reads candidate descriptors via `find_compatible`), P2-T010 (anchor checkpoint persistence, outside this extracted phase).
19. **Preconditions.** P1-T054, P1-T055, P1-T056, P1-T057, P1-T059 are `DONE`.
20. **Inputs.** Architecture §12.2, §12.4, §17.1–17.3, §29.4.
21. **Allowed scope.** `infrastructure/persistence/checkpoints.py`, `infrastructure/persistence/recovery.py`.
22. **Forbidden actions.** No generic save/load pair conflating scientific and recovery state; no resolution of `RECOVERY_STATE`-rooted content as a `SCIENTIFIC_CHECKPOINTS`-rooted checkpoint even when round numbers coincide; no raw Torch state dict crossing into the application request (only opaque staged artifact references and typed descriptors).
23. **Required configuration or NONE.** `RecoveryCheckpointPolicy` (cadence, retention, atomic commit, compatibility identity) from `ExecutionPolicy`.
24. **Implementation responsibilities.** Implement `find_compatible`/`save` for scientific checkpoints (kind = SCIENTIFIC, saved only after completed roadmap-scheduled rounds); implement `save_recovery`/`load_recovery` for `RecoveryState` (safe completed-round boundaries only, never mid-round/mid-optimizer-step); validate schema/hash/training-identity/`ResolvedBatchExecutionProfile`/seeds/last-completed-round on recovery load, raising `RecoveryStateMismatchError`/`ResumeIncompatibilityError` on mismatch.
25. **Required unit tests.** Recovery state can never be loaded through the scientific-checkpoint method or vice versa; a recovery load with a mismatched training identity/batch profile/seed raises the correct typed error rather than resuming.
26. **Required property tests.** NONE.
27. **Required contract tests.** `CheckpointStore` satisfies its full Section 12.2 signature (all four methods) with no generic fallback.
28. **Required architecture tests.** No raw Torch state dict appears in `SaveScientificCheckpointRequest`/`SaveRecoveryStateRequest`.
29. **Required integration tests.** A synthetic scientific checkpoint and a synthetic recovery checkpoint persist to distinct roots/namespaces and cannot be cross-loaded; a recovery load with a changed batch profile is rejected and starts fresh.
30. **Required CUDA tests.** A synthetic tiny-model checkpoint save/load round-trips correctly under the CUDA lane.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; contract lane; architecture lane; integration lane (persistence); CUDA lane.
33. **Acceptance criteria.** [ ] Scientific and recovery methods/roots/namespaces are structurally distinct. [ ] Recovery is never resolvable as scientific, and vice versa. [ ] Mismatch on resume raises the correct typed error (`RecoveryStateMismatchError`/`ResumeIncompatibilityError`) rather than silently coercing compatibility. [ ] No raw Torch state appears at the application boundary.
34. **Completion evidence.** Changed files: `checkpoints.py`, `recovery.py`, and their tests. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** `CheckpointError`/`RecoveryStateMismatchError`/`ResumeIncompatibilityError` block reuse/resume rather than silently coercing compatibility.
36. **Stop conditions.** None.
37. **Deliverables.** `CheckpointStore` adapter (scientific + recovery).
38. **Final review checklist.** [ ] Recovery is not scientific, structurally, not merely by convention. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

### B3. Architecture boundary audit

Check the complete project for a generic `save`/`load` pair competing with `find_compatible`/`save`/`save_recovery`/`load_recovery`, a code path that resolves `RECOVERY_STATE`-rooted content through the scientific-checkpoint method (or the reverse), and a raw Torch state dict crossing into `application`. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw dict/tuple substituting for `CheckpointLookupResult`/`CheckpointWriteResult`/`RecoveryWriteResult`/`RecoveryLookupResult`, `Any`/unjustified `cast`, and a shared identity type between `CheckpointDescriptor` (kind SCIENTIFIC) and `RecoveryState` (kind RECOVERY) that would let one be substituted for the other.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25), contract lane (line 27), architecture lane (line 28), integration lane (line 29), and CUDA lane (line 30), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `checkpoints.py` and `recovery.py` must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

This is a primary determinism-relevant ticket. Check specifically that scientific checkpoints are saved only after completed roadmap-scheduled rounds ({25, 50, 75, 100, 125, 150, 200}, Architecture §17.1) and never mid-round; that `RecoveryCheckpointPolicy` commits recovery state only at safe completed-round boundaries and never mid-round or mid-optimizer-step (Architecture §17.2); that a recovery load validates schema, hash, training identity, the frozen `ResolvedBatchExecutionProfile`, seeds, and last-completed-round exactly, with any mismatch raising rather than silently resuming (Architecture §17.3); and that hash-verified immutable content is never overwritten by either method.

### B11. Phase 1 boundary confirmation

Confirm no real N-BaIoT/Edge-IIoTset/CICIoT2023 checkpoint or model state is persisted anywhere in this ticket — every unit, contract, architecture, integration, and CUDA-lane test uses a synthetic tiny model and a temporary/sandboxed root. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — checkpoint/recovery persistence carries no scientific claim.` Positive finding required: confirm this ticket protects the roadmap's checkpoint-selection integrity (Architecture §17.1, §29.4 invalid state 35) by making it structurally impossible for a recovery-only artifact to ever be selected, cited, or reported as the scientific checkpoint that `CheckpointSelector` (P1-T037) chose.

**Audit 2 — Architecture fidelity.** Confirm the adapter matches Architecture §12.2/§12.4/§17.1–17.3 exactly: the four-method `CheckpointStore` signature, distinct `SCIENTIFIC_CHECKPOINTS`/`RECOVERY_STATE` roots and namespaces, and the exact validation set (schema, hash, training identity, `ResolvedBatchExecutionProfile`, seeds, last-completed-round) enforced on recovery load.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a scientific checkpoint and a recovery checkpoint saved at the same round number, with the same content hash by coincidence, be cross-loaded through a path that only compares hash and round rather than root/namespace/kind? Does the cross-load unit test actually attempt a genuine cross-load (constructing a recovery descriptor and passing it to the scientific lookup path, or the reverse), or does it only assert each method works correctly in isolation? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.
