# P1-T049 — Implement the analysis table/figure/wording/report-model specification layer

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T049` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T049
2. **Title.** Implement the analysis table/figure/wording/report-model specification layer
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P1 — Mandatory
6. **Ticket type.** reporting
7. **Owner.** report-output-auditor
8. **Supporting agents.** statistics-auditor
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `analysis/{tables,figures,wording,report_models}.py` specification modules (report identity, specification side).
13. **Objective.** Implement `analysis/{tables,figures,wording,report_models}.py`: framework-free table and figure specifications (one module per `TableType`/`FigureType` family that needs a distinct one), `ClaimOutcome`-driven wording selection, and shared report-model construction derived only from domain result types.
14. **Why this ticket exists.** `analysis` is architecturally distinct from `infrastructure/reporting/` (P1-T067): this layer produces framework-free *specifications* of what a table or figure should contain, never rendered output, and it imports `domain` only — no framework, no persistence, no path (Architecture §3.5's ownership matrix). Keeping this separation means the same specification can be rendered to Markdown, LaTeX, or an image without the specification itself ever depending on a rendering library, and `ClaimOutcome`-driven wording selection is what turns the roadmap's pre-committed fallback wording (Roadmap §12) into a deterministic, testable selection rather than manuscript-writing-time discretion.
15. **Authority references.** Architecture §5.1 (module map), §22.1 (typed outputs, disjoint coverage columns, no Sankey), §3.5 (analysis ownership matrix), §6.6 (reporting vocabulary).
16. **Dependencies.** P1-T052, P1-T026, P1-T027.
17. **Blocks.** P2-T019, P4-T021.
18. **Downstream consumers.** P2-T019 and P4-T021 (outside this extracted phase) consume this specification layer directly.
19. **Preconditions.** P1-T052, P1-T026, P1-T027 are `DONE`.
20. **Inputs.** Architecture §5.1, §22.1, §3.5, §6.6.
21. **Allowed scope.** `analysis/`.
22. **Forbidden actions.** No framework, persistence, path, or rendering-library import anywhere in `analysis`; no Sankey specification of any kind; no generic coverage field shared between eligibility and conformal coverage; no repository read from this layer (specifications are constructed purely from already-computed domain result types passed in).
23. **Required configuration or NONE.** The reporting policy is consumed only via already-mapped specs, never read directly by this layer.
24. **Implementation responsibilities.** Implement framework-free specification dataclasses for every required `TableType`/`FigureType`; implement wording selection keyed on `ClaimOutcome`; implement shared report-model construction reading only domain result types.
25. **Required unit tests.** Every specification module constructs successfully with no framework dependency present (verified by importing these modules in an environment where framework imports would fail if attempted); eligibility and conformal coverage occupy disjoint specification columns, never a shared generic field; wording selection produces the correct wording variant for each `ClaimOutcome` member.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** `analysis` imports no framework and no persistence adapter anywhere.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; architecture lane; Pyright strict.
33. **Acceptance criteria.** [ ] `analysis` remains completely framework-free. [ ] No Sankey specification exists anywhere. [ ] Eligibility and conformal coverage columns are disjoint. [ ] Wording selection is keyed correctly on `ClaimOutcome`.
34. **Completion evidence.** Changed files: `analysis/tables.py`, `figures.py`, `wording.py`, `report_models.py`, and their tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** NONE expected in this ticket's own scope; rendering errors belong entirely to `infrastructure/reporting/` (P1-T067), not this specification layer.
36. **Stop conditions.** None.
37. **Deliverables.** The analysis specification layer modules.
38. **Final review checklist.** [ ] Domain-only imports throughout. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

### B3. Architecture boundary audit

Check the complete project for a framework or persistence import anywhere in `analysis/`, a Sankey specification of any kind, and a generic coverage field merging eligibility and conformal coverage. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw dict standing in for a table/figure specification, `Any`/unjustified `cast`, and a wording-selection `if`/`match` chain missing `assert_never` over `ClaimOutcome`.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25) and architecture lane (line 28), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/contract/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; every module in this layer must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE` for seed/CUDA surface — these are framework-free specification dataclasses with no random or device dependency. Confirm regardless that wording selection is a pure function of `ClaimOutcome` (the same outcome always selects the same wording variant, never influenced by hidden state).

### B11. Phase 1 boundary confirmation

Confirm no real N-BaIoT/Edge-IIoTset/CICIoT2023 result feeds any specification anywhere in this ticket — all tests construct specifications from synthetic domain result instances. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm every `ClaimOutcome` maps to the correct pre-committed fallback wording family from Roadmap §12 (strong-positive, weak-positive, mixed, null, opposite, feasibility-rejection, suppressed), with no outcome silently defaulting to a more favorable wording than its evidence supports.

**Audit 2 — Architecture fidelity.** Confirm every specification module matches Architecture §5.1/§22.1 exactly, and that `analysis` imports nothing beyond `domain`, per §3.5.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a future contributor import a plotting library "just to compute a figure dimension" inside `analysis/figures.py`, silently breaking the framework-free guarantee this ticket exists to protect? Does the wording-selection test cover every `ClaimOutcome` member independently, or only the strong-positive happy path? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.
