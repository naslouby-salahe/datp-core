# P1-T034 — Implement data/learning/scoring/thresholding application ports

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T034` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T034
2. **Title.** Implement data/learning/scoring/thresholding application ports
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** application
7. **Owner.** implementation-engineer
8. **Supporting agents.** data-pipeline-engineer
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `DatasetSourceInspector`, `ClientPartitioner`, `SplitManifestBuilder`, `PreprocessorFitter`, `ProcessedSplitMaterializer`, `FederatedTrainer`, `CentralizedModelTrainer`, `ScoreGenerator`, `ThresholdConstructor`, `ThresholdStrategy`, `ClusteringStrategy`, `QuantileEstimator` (Protocols; no framework carrier).
13. **Objective.** Implement `application/ports/{data,learning,scoring,thresholding}.py` Protocols with dedicated typed requests/results, listed above.
14. **Why this ticket exists.** A port exists only at a genuine external variation point — a framework boundary, a hardware/runtime inspection, a persistence backend, or a telemetry/reporting sink (Architecture §12.2, §3.6) — and this ticket owns exactly the data/learning/scoring/thresholding tier of that boundary. Architecture §9.6 states the confinement rule directly: NumPy arrays, pandas objects, PyArrow batches, `nn.Module`, Torch tensors/state dictionaries, sklearn estimators, and Flower clients/strategies are private implementation carriers inside `infrastructure` adapters and never appear in a domain or application port signature; application contracts exchange bounded framework-neutral descriptors instead (`ArtifactRef`, `ProcessedSplitDescriptor`, `ScientificCheckpointDescriptor`, `CalibrationScoreArtifactSet`, `TestScoreArtifactSet`). Each Protocol below takes exactly one named request and returns exactly one named result (Architecture §9.7's guarantee) — no weakly-typed `put`/`get`/`compute` dispatch exists anywhere. `ScoreGenerator` is split into three distinctly named methods (`generate_calibration_scores`, `generate_test_scores`, `generate_temporal_scores`) precisely so that calibration/test/temporal score generation cannot be confused at the type level, and `ThresholdConstructor`'s request type structurally cannot carry a test or attack score field, making calibration/test leakage into threshold construction a type error rather than a runtime discipline someone has to remember.
15. **Authority references.** Architecture §12.2 (port contracts — `data.py`/`learning.py`/`scoring.py`/`thresholding.py` Protocol signatures), §9.6 (framework confinement and application carriers), §9.7 (application contracts class diagram — one named request/result per port method).
16. **Dependencies.** P1-T028, P1-T030.
17. **Blocks.** P1-T037, P1-T043.
18. **Downstream consumers.** P1-T037 (pipeline stage functions) and P1-T043 (PyTorch AE model/device adapters) implement/consume these ports directly.
19. **Preconditions.** P1-T028, P1-T030 are `DONE`.
20. **Inputs.** Architecture §12.2, §9.6, §9.7.
21. **Allowed scope.** `application/ports/data.py`, `learning.py`, `scoring.py`, `thresholding.py`.
22. **Forbidden actions.** No framework carrier (a NumPy array, a pandas object, a PyArrow batch, an `nn.Module`, a Torch tensor/state dict, a sklearn estimator, a Flower client/strategy) in any port signature; no generic `put`/`get`/`compute` method anywhere; no test or attack score field on `ConstructThresholdsRequest` or any other threshold-construction request type.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Implement each Protocol of Architecture §12.2's `data.py`/`learning.py`/`scoring.py`/`thresholding.py` grouping with one dedicated named request/result type per method; keep `FederatedTrainer` and `CentralizedModelTrainer` as structurally distinct ports rather than one trainer port with a mode flag; keep `ScoreGenerator`'s calibration/test/temporal methods distinct; keep `ThresholdConstructor`'s request free of any field a test or attack score could occupy.
25. **Required unit tests.** `ConstructThresholdsRequest` (and any sibling threshold-construction request) has no field capable of carrying a test or attack score — a field-inventory test against the forbidden-field list.
26. **Required property tests.** NONE.
27. **Required contract tests.** Contract scaffolding for later adapter conformance to each Protocol (the full conformance suites are exercised against concrete adapters in P1-T037 and the infrastructure tickets that implement these ports).
28. **Required architecture tests.** No framework object (NumPy, pandas, PyArrow, Torch, sklearn, Flower) appears in any port signature in `application/ports/data.py`, `learning.py`, `scoring.py`, or `thresholding.py`.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Architecture lane; unit lane; Pyright strict.
33. **Acceptance criteria.** [ ] Every port method has exactly one named request and one named result type. [ ] No framework carrier appears in any port signature. [ ] No threshold-construction request can carry a test or attack score. [ ] `FederatedTrainer` and `CentralizedModelTrainer` remain structurally distinct ports.
34. **Completion evidence.** Changed files: `application/ports/data.py`, `learning.py`, `scoring.py`, `thresholding.py`, and their tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** These ports raise the typed error families of P1-T030 where a port implementation fails; no other failure is expected in this ticket's own scope.
36. **Stop conditions.** None.
37. **Deliverables.** The data/learning/scoring/thresholding application ports.
38. **Final review checklist.** [ ] Every port is framework-neutral. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

### B3. Architecture boundary audit

Check the complete project for a framework object (NumPy, pandas, PyArrow, Torch, sklearn, Flower) appearing in any `application/ports/` signature (this ticket's own primary forbidden pattern), dependency-direction violations (`application → infrastructure` beyond a Protocol declaration), cycles/side effects, and incorrect type placement. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a generic `dict`-shaped request/result replacing a named dataclass, `Any`/unjustified `cast`, a single trainer port covering both federated and centralized training behind a mode flag, and a threshold-construction request carrying a test or attack score field under a disguised name.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25), the contract lane (line 27), and the architecture lane (line 28), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `application/ports/data.py`, `learning.py`, `scoring.py`, and `thresholding.py` must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE` — this ticket defines Protocol signatures only; it introduces no seed, no CUDA path, and no randomness of its own. Confirm regardless that no port signature implies an implicit retry or fallback capable of silently altering a scientific value (Architecture §16's determinism concerns belong to the concrete adapters implementing these ports, not to the Protocol declarations themselves).

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — a port signature carries no scientific claim of its own.` Positive finding required: confirm `ThresholdConstructor`'s request structurally cannot carry a test/attack score, preserving the calibration-only threshold-construction rule the roadmap depends on.

**Audit 2 — Architecture fidelity.** Confirm every Protocol and method signature matches Architecture §12.2's `data.py`/`learning.py`/`scoring.py`/`thresholding.py` listing exactly, and that Architecture §9.6's framework-confinement rule is testable against every port via the architecture lane.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a future contributor add an optional `test_scores` field to a threshold-construction request "just for debugging," or route a NumPy array through a generically typed field on a request? Does the unit test enumerate every field on every threshold-construction request, or only check one? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.
