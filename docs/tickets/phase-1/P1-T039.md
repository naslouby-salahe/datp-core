# P1-T039 — Implement preflight, executor, lifecycle, and resource-pressure orchestration

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T039` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T039
2. **Title.** Implement preflight, executor, lifecycle, and resource-pressure orchestration
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** application
7. **Owner.** implementation-engineer
8. **Supporting agents.** reproducibility-auditor
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `application/runtime/{preflight,executor,lifecycle,resource_pressure}.py`: `ExecutionPreflight`, `FinalExecutionPlan`, `PlanExecutor`, the run/stage state machine, cooperative pressure orchestration.
13. **Objective.** Implement `ExecutionPreflight` (validates the exact resolved batch/execution profile, classifies every draft stage as `REUSE`/`RECOMPUTE`/`BLOCKED`, and freezes a `FinalExecutionPlan`), `PlanExecutor` (executes stages idempotently), the run/stage lifecycle state machine, and cooperative resource-pressure orchestration.
14. **Why this ticket exists.** Preflight is deliberately the *only* consumer of the artifact-catalogue snapshot in the entire pipeline — the planner (P1-T038) never touches it, and this separation means storage state can only ever influence which stages are skipped, never what the plan's shape is. The executor must decide reuse before compute (never `RUNNING → REUSED`, which would imply a stage started computing and was retroactively marked as reused), and a CUDA out-of-memory failure must be terminal for its execution attempt rather than auto-transitioning to `PAUSED`/`RECOVERED` — this is the master log's Section B execution-attempt vocabulary made concrete: only a later, explicit new attempt may resume from a previously committed compatible recovery checkpoint.
15. **Authority references.** Architecture §12.3 (orchestration responsibilities), §14 (preflight/execution contracts), §16.5–16.7 (execution semantics, OOM terminality, resource pressure), §18 (lifecycle state machine).
16. **Dependencies.** P1-T038.
17. **Blocks.** P1-T068, P3-T005.
18. **Downstream consumers.** P1-T068 (composition root) and P3-T005 (outside this extracted phase) consume this orchestration directly.
19. **Preconditions.** P1-T038 is `DONE`.
20. **Inputs.** Architecture §12.3, §14, §16.5–16.7, §18.
21. **Allowed scope.** `application/runtime/`.
22. **Forbidden actions.** No substitution of the resolved batch/execution profile at any point after preflight freezes it; no `RUNNING → REUSED` transition; no automatic `FAILED_OOM → PAUSED`/`RECOVERED` transition; resource pressure must never resize a scientific batch, chunk size, or precision setting.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Implement preflight's classification and freeze logic; implement the executor's stage lifecycle with reuse decided strictly before compute begins; implement CUDA-OOM as terminal for the current `ExecutionAttemptId`; implement resource-pressure orchestration that only ever recommends equivalence-preserving actions (continue, safe-boundary pause, commit-and-exit, or resume with the identical frozen profile).
25. **Required unit tests.** A stage classified `REUSE` never enters `RUNNING` (reuse-before-compute is structurally enforced, not merely convention); a CUDA-OOM failure leaves the stage in a terminal state for that attempt, never auto-transitioning; a resource-pressure recommendation is rejected before reaching the executor if it would alter the frozen profile.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** The executor's stage-dispatch table is exhaustive over `PipelineStage` (a missing stage handler is a static/construction-time failure, not a silent runtime gap).
29. **Required integration tests.** Reuse-before-compute, OOM-terminal, and pressure pause/resume equivalence are each verified end-to-end on synthetic data with test-double adapters.
30. **Required CUDA tests.** NONE (real CUDA-OOM behavior is exercised by the concrete CUDA guard adapter, P1-T060; this ticket verifies the orchestration logic with a simulated OOM signal).
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; synthetic integration lane; Pyright strict.
33. **Acceptance criteria.** [ ] The exact resolved profile is validated once and never substituted afterward. [ ] Reuse is always decided before compute begins. [ ] A CUDA-OOM failure is terminal for its attempt. [ ] Resource pressure never alters the frozen scientific profile.
34. **Completion evidence.** Changed files: `application/runtime/` and its tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** `RamPreflightError`, `DiskSpaceError`, and `UnsafeParallelismError` are raised at plan time (before any stage runs); `CudaOutOfMemoryError` is terminal for the attempt and is never silently retried.
36. **Stop conditions.** None.
37. **Deliverables.** Preflight, executor, lifecycle, and resource-pressure orchestration modules.
38. **Final review checklist.** [ ] No silent scientific batch change under pressure. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for any artifact-catalogue access outside preflight, a `RUNNING → REUSED` transition anywhere, an automatic OOM-to-resume transition, and dependency-direction violations. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw string standing in for `RunStatus`, a boolean flag substituting for the reuse-before-compute state machine, `Any`/unjustified `cast`, and an incomplete stage-dispatch table lacking `assert_never`.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25), architecture lane (line 28), and integration lane (line 29), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/contract/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `application/runtime/` must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

This is a primary determinism-surface ticket. Check specifically for: automatic retry after CUDA OOM (forbidden — verify the terminal-disposition test actually asserts no auto-transition); silent batch-size reduction under pressure (forbidden — verify the rejection test at line 25); scattered global seeds introduced by this orchestration layer (none should exist — seed ownership stays with P1-T012/P1-T043); unsafe CUDA multiprocessing or an incorrect process start method chosen by this layer (process-start-method selection belongs to P1-T043, not here — confirm no duplication or conflicting decision).

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced; all integration tests here run against synthetic data with test-double adapters. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm no automatic retry or batch-size reduction after CUDA OOM exists anywhere in this orchestration, matching the master log's Section A/§16.6 "automatic batch-size reduction remains forbidden" rule, and that this remains true even under simulated resource pressure.

**Audit 2 — Architecture fidelity.** Confirm the lifecycle state machine matches Architecture §18 exactly (no undocumented transition) and that reuse-before-compute is enforced structurally, not just by convention, per §14.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a race condition between the executor marking a stage `RUNNING` and a concurrent reuse-decision update let a stage be computed and reused simultaneously? Does the OOM-terminal test simulate an actual OOM signal path, or merely assert the disposition value on a hand-constructed error object? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.
