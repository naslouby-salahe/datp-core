# P1-T043 — Implement the PyTorch AE model and deterministic device/seed/DataLoader adapters

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T043` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T043
2. **Title.** Implement the PyTorch AE model and deterministic device/seed/DataLoader adapters
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** DONE
5. **Priority.** P0 — Blocking
6. **Ticket type.** infrastructure
7. **Owner.** federated-learning-engineer
8. **Supporting agents.** reproducibility-auditor
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `infrastructure/learning/models/autoencoder.py` (fixed AE, no BatchNorm), deterministic device/seed/DataLoader adapters.
13. **Objective.** Implement `infrastructure/learning/models/autoencoder.py` (the fixed AE architecture, with no BatchNorm layer) and deterministic device/seed/DataLoader adapters: explicit CUDA device ownership, `DeterminismLevel.STRICT` algorithm settings, an explicit `torch.Generator`/`worker_init_fn` pair, per-worker seeded RNG, and a spawn process-start context for every CUDA-touching stage.
14. **Why this ticket exists.** No BatchNorm exists in this AE because adding it would change the fixed-encoder identity the entire causal ladder depends on — BatchNorm's running statistics are themselves a form of hidden, mutable model state that would make "the same trained model feeds B1–B4" no longer strictly true (Roadmap SB-02, Roadmap §16's FedBN rejection for the identical reason). Determinism is structural, not incidental: every seed is derived through `derive_seed` (P1-T012) into an explicit `torch.Generator` and `worker_init_fn`, so there is no scattered `torch.manual_seed()` call anywhere whose ordering relative to other calls could silently change results.
15. **Authority references.** Architecture §16.1–16.2 (device ownership, exact k-means note is elsewhere but §16.1 covers CUDA execution model), §16.4 (parallelism), §16.7 (seed derivation and process start method), §9.6 (framework confinement). Roadmap SB-02 (no FedBN — the encoder has no BatchNorm).
16. **Dependencies.** P1-T034, P1-T054, P1-T055, P1-T056.
17. **Blocks.** P1-T044, P2-T008.
18. **Downstream consumers.** P1-T044 (FedAvg/FedProx/centralized trainers) and P2-T008 (outside this extracted phase) consume this model and these adapters directly.
19. **Preconditions.** P1-T034, P1-T054, P1-T055, P1-T056 are `DONE`.
20. **Inputs.** Architecture §16.1–16.2, §16.4, §16.7, §9.6; Roadmap SB-02.
21. **Allowed scope.** `infrastructure/learning/models/`, plus the device/seed/DataLoader adapters in `infrastructure/runtime/{determinism,processes}.py`.
22. **Forbidden actions.** No global `multiprocessing.set_start_method` call anywhere; no CUDA stage using `fork` (CUDA stages must spawn); no scattered global seed call (`torch.manual_seed()` outside the explicit, derived-seed generator path); no BatchNorm layer anywhere in the AE; no `nn.Module` handle crossing upward out of `infrastructure/learning/models/`.
23. **Required configuration or NONE.** From the frozen training/execution specifications built in earlier tickets.
24. **Implementation responsibilities.** Configure deterministic CUDA algorithm settings under `DeterminismLevel.STRICT`; construct DataLoader generators from the `DataLoaderSeedPlan`; use a spawn process context per CUDA-touching stage; keep `torch.compile` off (Architecture §4.2's rejected-dependencies note: considered only after determinism/equivalence tests, not adopted by default).
25. **Required unit tests.** No global seed call exists anywhere in this ticket's modules (a static scan/test asserting `torch.manual_seed` is never called outside the one explicit, derived-seed construction path).
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** No `nn.Module` instance crosses upward out of `infrastructure/learning/models/` through any port-facing signature.
29. **Required integration tests.** Zero-worker-vs-multi-worker, sequential-vs-parallel, repeated-run, and resumed-run equivalence, each verified via row-order checksums on a synthetic tiny model (exercised in the serialized CUDA lane).
30. **Required CUDA tests.** Determinism equivalence checks on a synthetic tiny model under `DeterminismLevel.STRICT` — two runs with identical seeds and identical inputs must produce bit-identical (or declared-tolerance-identical) output.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Serialized CUDA lane; unit lane; Pyright strict.
33. **Acceptance criteria.** [x] No BatchNorm layer exists anywhere in the AE. [x] Every seed is derived explicitly, never set via a scattered global call. [x] Every CUDA-touching stage spawns; none forks. [x] Worker-count equivalence holds on a synthetic tiny model under the CUDA lane.
34. **Completion evidence.** Changed files: `infrastructure/learning/models/`, `infrastructure/runtime/determinism.py`, `processes.py`, and their tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** `DeterminismViolationError` is raised on a detected equivalence failure. Stop and implement only an explicit, non-citable smoke AE if the final AE architecture is genuinely unresolved by either authority — carry the real architecture decision as a readiness blocker rather than guessing at layer widths/depths.
36. **Stop conditions.** Stop if the AE architecture itself is unresolved; implement only an explicit non-citable smoke AE and carry the real decision as a readiness blocker.
37. **Deliverables.** The fixed AE model and the deterministic device/seed/DataLoader adapters.
38. **Final review checklist.** [x] Determinism is structural, not a convention. [x] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [x] Read the complete roadmap and architecture before starting.
- [x] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [x] Verify every dependency is `DONE`.
- [x] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step: 2026-07-15T12:23:54Z — architecture, roadmap, dependency, model, and seed-contract inspection.
- [x] Keep both status locations synchronized while active.
- [x] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately. 2026-07-15T12:39:28Z — focused unit, architecture, integration, and CUDA checks passed; full project quality checkpoint is next.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [x] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches. 2026-07-15T12:41:48Z.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for a BatchNorm layer anywhere, an `nn.Module` crossing upward out of `infrastructure/learning/models/`, a `fork`-based CUDA stage, and a global `set_start_method` call. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw `torch.Generator`/seed integer passed loosely instead of through the typed `DataLoaderSeedPlan`, `Any`/unjustified `cast`, and a scattered `torch.manual_seed()` call anywhere outside the one sanctioned path.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25), architecture lane (line 28), integration lane (line 29), and CUDA lane (line 30), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/contract/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; these modules must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

This ticket is a primary determinism-surface owner. Check specifically for: scattered global seeds (forbidden — verified by line 25's test); an unsafe CUDA multiprocessing pattern (fork under CUDA is forbidden); an incorrect process start method for any stage; a non-picklable object crossing a spawn process boundary; unstable serialization of model state affecting cross-run equivalence. Confirm the CUDA-lane equivalence test in line 30 actually compares two independent runs, not a single run against itself.

### B11. Phase 1 boundary confirmation

Confirm no real N-BaIoT/Edge-IIoTset/CICIoT2023 data is used anywhere in this ticket — every equivalence test runs on a synthetic tiny model and synthetic tensors. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm no BatchNorm layer exists anywhere in the AE, matching Roadmap SB-02's explicit rejection of FedBN for exactly this reason; confirm the AE architecture, once locked, remains identical across every core-ladder cell (B1–B4 never each get a differently-shaped model).

**Audit 2 — Architecture fidelity.** Confirm deterministic settings match Architecture §16.1–16.2/§16.7 exactly, and that `infrastructure/learning/models/` never leaks an `nn.Module` upward, per §9.6.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a PyTorch version upgrade silently change a default algorithm's determinism behavior without this ticket's `DeterminismLevel.STRICT` settings catching it? Does the zero-vs-multi-worker equivalence test actually vary the worker count between runs, or does it construct the DataLoader once and merely assert its shape? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.

## Completion evidence — 2026-07-15T12:41:48Z

- Dependencies: P1-T034, P1-T054, P1-T055, and P1-T056 were `DONE` before implementation.
- Implementation: fixed canonical ReLU AE topology is `input → 80 → 40 → 20 → 40 → 80 → input`; deterministic local-generator initialization, strict CUDA settings, typed `DataLoaderSeedPlan`, per-worker Python/NumPy/PyTorch initialization, and a per-stage spawn context are implemented. `DataLoaderSeedPlan`/`SeedPlan` were added to P1-T012 after the audit identified their omission from that ticket's delivered seed contract.
- Required tests: `uv run pytest -q tests/unit/domain/test_identity_value_objects.py tests/integration/learning/test_deterministic_autoencoder.py tests/architecture/test_learning_framework_confinement.py` passed 19 tests; `uv run pytest -q tests/integration/cuda/test_deterministic_autoencoder.py -m cuda` passed 1 CUDA test. The integration test compares zero-worker and spawn-worker synthetic row-order checksums and a recreated run; the CUDA test compares two independently constructed strict-mode models bit-for-bit.
- Project quality: `uv run nox -s full_suite` passed; it included clean Ruff lint/format, strict Pyright, import-linter, architecture tests, randomized parallel-safe suite (391 passed), serial/resource/CUDA/synthetic/scientific lanes, and the configured full-project test coverage. `uv run nox -s sonar codescene` passed: Sonar found no issues and CodeScene found no delta findings.
- Audits: planning-reference scan outside planning documents was empty; no PyArrow/pandas carrier appears in application/domain signatures; the only `torch.manual_seed` call is the explicitly typed worker initializer; no global process start method, `fork` CUDA path, `torch.compile`, BatchNorm model module, or application-layer PyTorch import exists. Existing probability-test casts are narrow intentional invalid-runtime-value tests, not production typing escapes. `git diff --check` passed; untracked files were inspected and are the Phase 1 source/tests/stubs and executor-owned task artifacts.
- B12 audit 1: Roadmap SB-02 is met because the fixed AE contains no BatchNorm and has one canonical topology, independent of B1–B4 policy scope.
- B12 audit 2: Architecture §§9.6 and 16.1–16.2/16.7 are met by infrastructure-only PyTorch handles, strict deterministic settings, typed seed plans, and spawn process contexts.
- B12 audit 3: strict mode is enabled before equivalence checks; zero and two worker counts are genuinely distinct DataLoader constructions under spawn; repeated recreated runs and independently initialized CUDA models were compared. No PyTorch version-specific default was relied on for the asserted deterministic settings.
- Not applicable by Part A: property, contract, and system/end-to-end tests. Pylance executable is unavailable in this environment; strict Pyright passed. No real dataset access, training, scoring, or scientific execution occurred.
