# P1-T009 — Implement runtime/lifecycle/seed-role/pipeline-stage vocabulary

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T009` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T009
2. **Title.** Implement runtime/lifecycle/seed-role/pipeline-stage vocabulary
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** domain
7. **Owner.** implementation-engineer
8. **Supporting agents.** reproducibility-auditor
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `ExecutionMode`, `DevicePolicy`, `PipelineStage` (authoritative ordered stages), `RunStatus`, `SeedRole`, `ReuseDecisionKind`, `StageConcurrency`, `ProcessStartMethod`, `WorkerRole`, `FailureDisposition`, `RoundDisposition`, `ResourcePressureLevel`, `PauseDecision`, `ReuseImpact`.
13. **Objective.** Implement `domain/runtime/policies.py`, `domain/runtime/seeds.py`, and `domain/artifacts/lineage.py`'s execution/lifecycle enums of field 12, each a `StrEnum` exactly per Architecture §6.3.
14. **Why this ticket exists.** `PipelineStage` is the single authoritative, ordered stage identity that every later specification, stage function, and lineage comparison in Phase 1 onward relies on (Architecture §13.1) — an incorrect or reordered stage list here would silently corrupt every downstream reuse decision. `SeedRole` must cover every random-state owner exhaustively so no seed is ever a scattered, unowned global call. `ReuseImpact` is the typed vocabulary the resolved-spec differ (P1-T033) classifies changes into; it must be closed and precise so a threshold-only change can never be misclassified as invalidating training.
15. **Authority references.** Architecture §6.3 (execution/lifecycle vocabulary table), §13.1 (stage order authority), §16.6–§16.7 (seed ownership, process start method), §18 (lifecycle/execution-attempt vocabulary).
16. **Dependencies.** P0-T026.
17. **Blocks.** P1-T036, P1-T039.
18. **Downstream consumers.** P1-T036 (persistence/runtime application ports) and P1-T039 (preflight/executor/lifecycle/resource-pressure orchestration) consume this vocabulary directly; every stage-identity and lineage type in Phase 1 onward depends on `PipelineStage` transitively.
19. **Preconditions.** P0-T026 is `DONE`.
20. **Inputs.** Architecture §6.3, §13.1, §16.6–16.7, §18.
21. **Allowed scope.** `domain/runtime/policies.py`, `seeds.py`, and the enum members of `domain/artifacts/lineage.py` — enums only in this ticket; the identity dataclasses that use `PipelineStage` are P1-T012/P1-T013's scope.
22. **Forbidden actions.** No scattered global-seed concept represented anywhere in this vocabulary (every seed role is named and owned); no execution enum member capable of mutating scientific identity; no multi-GPU/multi-node member (Phase 0/1 scope is one host, one selected GPU, per Architecture §16.1).
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Define each enum as a `StrEnum`; order `PipelineStage`'s members exactly per Architecture §6.3 (`SOURCE_INSPECTION` through `REPORT`); ensure `SeedRole` covers every random-state owner named in Architecture §6.3 (`TRAINING_INIT`, `DATA_PARTITION`, `DATALOADER_SHUFFLE`, `DATALOADER_WORKER`, `SAMPLER`, `CLIENT_ORDERING`, `CLUSTERING`, `BOOTSTRAP`, `PERSONALIZATION`, `COMPARATOR`).
25. **Required unit tests.** `PipelineStage` member order matches Architecture §6.3 exactly (an explicit ordered-tuple comparison, not just a membership set check); every `SeedRole` owner named in the architecture is present; no multi-GPU/multi-node enum member exists.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** Framework confinement for `domain/runtime/` and `domain/artifacts/lineage.py`.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Pyright strict; architecture lane; unit lane.
33. **Acceptance criteria.** [ ] `PipelineStage` matches Architecture §6.3's order exactly. [ ] All `SeedRole` owners present. [ ] No multi-GPU/multi-node member. [ ] `ReuseImpact` is closed and precise.
34. **Completion evidence.** Changed files: `domain/runtime/policies.py`, `seeds.py`, and the lifecycle-enum portion of `domain/artifacts/lineage.py`, plus their unit tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** Exhaustiveness over these enums is enforced at every dispatch site via `assert_never`; no other failure is expected in this ticket's own scope.
36. **Stop conditions.** None.
37. **Deliverables.** Runtime/lifecycle/seed-role/pipeline-stage vocabulary.
38. **Final review checklist.** [ ] Stage order authoritative and exact. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

### B3. Architecture boundary audit

Check the complete project for dependency-direction violations, framework leakage, cycles/side effects, service locators, incorrect type placement, forbidden module names, backward-compatibility scaffolding, hidden defaults, and — specifically — any multi-GPU/multi-node scheduling concept. Run import-linter/pytest-archon; record results. Confirm `domain/runtime/` and the lineage enums import nothing beyond the standard library.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for raw strings standing in for `PipelineStage`/`SeedRole`, object-shaped dictionaries, `Any`/unjustified `cast`, and incomplete enum dispatch (a `match` over `PipelineStage` missing `assert_never`).

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25) and architecture lane (line 28), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/contract/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; these modules must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

This ticket is the vocabulary that later determinism enforcement (P1-T012, P1-T043, P1-T060) depends on; check specifically that `SeedRole` has no gap that would force a future scattered/unowned seed, and that `ProcessStartMethod` offers no unsafe default. `NOT_APPLICABLE` for actual seed derivation or CUDA execution — those are owned by P1-T012 and P1-T043/P1-T060 respectively; this ticket is enum-only.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm this vocabulary introduces no executable path for multi-node/fleet-scale (K > 100) execution, consistent with the roadmap's out-of-scope boundary; confirm no scientific execution occurred during this ticket.

**Audit 2 — Architecture fidelity.** Confirm `PipelineStage`'s member order is byte-for-byte the same sequence as Architecture §6.3 and that all three modules import nothing beyond the standard library.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: is the `PipelineStage` order test comparing an ordered tuple/sequence, or merely a set (which would miss a reordering defect)? Does `SeedRole` omit any owner implied elsewhere in the architecture? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.
