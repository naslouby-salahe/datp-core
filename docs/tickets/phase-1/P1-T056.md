# P1-T056 — Implement serialization and schema-version handling

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T056` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T056
2. **Title.** Implement serialization and schema-version handling
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** persistence
7. **Owner.** implementation-engineer
8. **Supporting agents.** artifact-lineage-auditor
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `ArtifactSchemaVersion` (serialization enforcement; the value object itself is domain-owned by P1-T012).
13. **Objective.** Implement `infrastructure/persistence/serialization.py`: msgspec-based (de)serialization for manifests and provenance records, with `ArtifactSchemaVersion` recorded and checked on every read.
14. **Why this ticket exists.** This ticket was split out of a former combined persistence mega-ticket because serialization and schema-version enforcement is a distinct, independently testable responsibility (Architecture §9.5) from hashing (P1-T055), path resolution (P1-T054), atomic commit (P1-T057), bundle commit (P1-T058), and locking (P1-T059). msgspec is used here specifically because it is stricter and faster than `json` plus `dataclasses.asdict` and keeps Pydantic — which Architecture §4.1 confines strictly to the `config` boundary — out of `infrastructure` entirely; every persisted manifest or provenance record carries an `ArtifactSchemaVersion` checked on read so a schema drift between two code versions is caught explicitly rather than silently misinterpreted.
15. **Authority references.** Architecture §4.1 (msgspec role), §9.5 (`ArtifactSchemaVersion` on every persisted artifact).
16. **Dependencies.** P1-T054.
17. **Blocks.** P1-T057, P1-T058, P1-T042, P1-T043, P1-T046, P1-T070.
18. **Downstream consumers.** P1-T057, P1-T058, and every adapter that serializes a manifest, provenance record, or other structured artifact.
19. **Preconditions.** P1-T054 is `DONE`.
20. **Inputs.** Architecture §4.1, §9.5.
21. **Allowed scope.** `infrastructure/persistence/serialization.py`.
22. **Forbidden actions.** No Pydantic model used at this boundary (Pydantic remains confined to `config`); no `json.dumps` of raw floats for identity purposes; no silent acceptance of a schema-version mismatch on read.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Implement msgspec encode/decode for every manifest and provenance-record type; attach and verify `ArtifactSchemaVersion` on every read; raise a typed incompatibility result on a version mismatch rather than attempting an implicit upgrade.
25. **Required unit tests.** A schema-version mismatch on read is rejected, never silently coerced to the current schema; round-trip encode/decode preserves every field exactly, including edge-case values (empty collections, boundary numeric values).
26. **Required property tests.** Round-trip encode/decode is idempotent for arbitrary valid manifest instances within the declared schema (encode then decode then encode again yields byte-identical output).
27. **Required contract tests.** NONE.
28. **Required architecture tests.** No Pydantic import exists anywhere in `infrastructure/persistence/serialization.py`.
29. **Required integration tests.** A synthetic manifest serializes, persists, and deserializes with an unchanged value and an unchanged schema version.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; property lane; architecture lane; integration lane (persistence).
33. **Acceptance criteria.** [ ] msgspec is used for every manifest/provenance (de)serialization. [ ] A schema-version mismatch is always rejected, never silently coerced. [ ] Round-trip encode/decode is exact. [ ] No Pydantic model exists at this boundary.
34. **Completion evidence.** Changed files: `serialization.py` and its tests. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** A schema-version mismatch surfaces as a typed `SchemaCompatibility.INCOMPATIBLE` result to the caller, never silently upgraded or downgraded.
36. **Stop conditions.** None.
37. **Deliverables.** The serialization and schema-version-handling adapter module.
38. **Final review checklist.** [ ] No Pydantic leakage into `infrastructure`. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for a Pydantic import anywhere inside `infrastructure/`, a silently-coerced schema-version mismatch anywhere, and dependency-direction violations. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw `json.dumps`/`dataclasses.asdict` call substituting for the msgspec path this ticket owns, `Any`/unjustified `cast`, and a manifest field lacking `ArtifactSchemaVersion`.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25), property lane (line 26), architecture lane (line 28), and integration lane (line 29), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Contract/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `serialization.py` must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

This is a primary determinism-relevant ticket: check specifically that round-trip encode/decode is exactly idempotent (the property test at line 26 is the direct evidence) and that no unstable serialization path (e.g., dict-key ordering dependent on hash randomization) could make two logically identical manifests serialize to different byte sequences across two runs.

### B11. Phase 1 boundary confirmation

Confirm no real N-BaIoT/Edge-IIoTset/CICIoT2023 manifest is serialized anywhere in this ticket — all tests use synthetic manifest instances. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — serialization carries no direct scientific claim.` Positive finding required: confirm the schema-version-mismatch rejection protects the roadmap's reproducibility requirement that a manifest read under a changed schema is never silently misinterpreted as if it were the current schema.

**Audit 2 — Architecture fidelity.** Confirm msgspec is used exclusively (no Pydantic, no bare `json`) and `ArtifactSchemaVersion` is checked on every read, matching Architecture §4.1/§9.5 exactly.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a manifest field silently round-trip to a subtly different value (for example, a `Decimal` losing precision through an intermediate float representation) without the round-trip exactness test catching it? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.
