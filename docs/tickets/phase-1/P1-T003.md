# P1-T003 — Implement threshold-policy/variant/comparator domain vocabulary

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T003` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T003
2. **Title.** Implement threshold-policy/variant/comparator domain vocabulary
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** domain
7. **Owner.** threshold-policy-engineer
8. **Supporting agents.** —
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `CoreThresholdPolicy` (`B1`–`B4`), `ThresholdConstructionKind` (nine variants), `SharedThresholdConstruction` (`MEAN`/`POOLED`/`WEIGHTED`), `ThresholdVariant`, `ThresholdComparatorRole` (`CENTRALIZED_MODEL_B0`, `FED_STATS_BENIGN`), `ConformalMode`.
13. **Objective.** Implement the `domain/thresholding/` enums listed in field 12 as `StrEnum`s, co-located across `policies.py`, `variants.py`, `federated_statistics.py` per Architecture §5.1.
14. **Why this ticket exists.** `ThresholdConstructionKind` is the explicit discriminator tag every threshold-construction variant in the closed union (built in P1-T024) carries; the variant must never be inferred from which optional field is set (Architecture §6.10). `CoreThresholdPolicy` restricts the causal ladder to exactly B1–B4 — B0 and `B-FedStatsBenign` are comparator roles, never ladder members (Roadmap §4). `B-LaridiFaithful` has no executable member anywhere in this vocabulary because the anomaly-labeled Laridi reproduction is out of scope (Roadmap §4, SB-29).
15. **Authority references.** Architecture §6.1 (threshold vocabulary rows), §6.2 (`ConformalMode`), §6.10 (explicit discriminator tag rule). Roadmap §4 (threshold-policy and comparator nomenclature, naming locks: `B-FedStatsBenign`/`B-LaridiFaithful` replace any prior `B5` label; `τ-shrink`/LGS replace any prior `B3-LGS` label), SB-29 (never call a benign-only Laridi adaptation "faithful").
16. **Dependencies.** P0-T026.
17. **Blocks.** P1-T024.
18. **Downstream consumers.** P1-T024 (threshold-construction union and suite specifications) is the sole immediate consumer; every threshold-aware specification, adapter, and configuration schema in later phases depends on this vocabulary transitively.
19. **Preconditions.** P0-T026 is `DONE`.
20. **Inputs.** Architecture §6.1, §6.2, §6.10; Roadmap §4, SB-29.
21. **Allowed scope.** `domain/thresholding/policies.py`, `variants.py`, `federated_statistics.py` — enum definitions only; the closed `ThresholdConstructionSpec` union itself is owned by P1-T024, not this ticket.
22. **Forbidden actions.** No `B-LaridiFaithful` executable member; no `B5` label anywhere; B0 must never appear as a `CoreThresholdPolicy` member; no q-mutated fingerprint concept represented as an enum member (fingerprint composition belongs to P1-T025).
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Define each enum as a `StrEnum`; ensure `ThresholdConstructionKind` has exactly the nine variants named in Architecture §6.1 with `SHARED`, `LOCAL`, `FAMILY`, `CLUSTER`, `ROBUST_CLUSTER_MEDIAN`, `SHRINKAGE`, `CALIB_SIZE_FALLBACK`, `CONFORMAL`, `FED_STATS_BENIGN`; ensure `ThresholdComparatorRole` keeps B0 and FedStats disjoint from `CoreThresholdPolicy`.
25. **Required unit tests.** Membership and serialized-value stability for each enum; an explicit test that `CoreThresholdPolicy` has exactly four members (B1–B4) and that neither B0 nor `FED_STATS_BENIGN` is among them; an explicit test that no member renders as `B5` or `Laridi[Ff]aithful`.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** Framework confinement for `domain/thresholding/`; no framework import.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Pyright strict; architecture lane; unit lane for `domain/thresholding/`.
33. **Acceptance criteria.** [ ] Nine construction kinds present, exactly. [ ] B0/`FED_STATS_BENIGN` are comparator roles, never ladder members. [ ] No Laridi-faithful executable member. [ ] Naming locks (no `B5`, no `B3-LGS`) honored.
34. **Completion evidence.** Changed files: `domain/thresholding/policies.py`, `variants.py`, `federated_statistics.py`, and their unit tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** NONE expected; exhaustiveness over the nine-variant union is enforced at its consumer (P1-T024) via `assert_never`.
36. **Stop conditions.** Stop if a construction kind's exact meaning conflicts between Roadmap §4 and any other roadmap section; raise a design-decision blocker.
37. **Deliverables.** `domain/thresholding/` enum modules and their unit tests.
38. **Final review checklist.** [ ] Ladder is B1–B4 only. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each of the three B12 audits separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition, recording the exact blocker and unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project (`src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, tooling configuration, provider adapters, root metadata), not only this ticket's files. No defect is dismissible as pre-existing, someone else's, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

### B3. Architecture boundary audit

Check the complete project for dependency-direction violations, framework leakage, config-boundary leaks, CLI wiring dependencies, cycles/side effects, service locators, universal context objects, incorrect type placement, duplicate abstractions, forbidden module names, backward-compatibility scaffolding, hidden defaults, silent fallbacks, mutable scientific specs, and out-of-phase behavior. Run import-linter/pytest-archon; record results. Confirm `domain/thresholding/` imports nothing beyond the standard library.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for raw strings standing in for enums, object-shaped dictionaries, `dict[str, Any]`/`Any`/unjustified `cast`/`type: ignore`, boolean flags replacing discriminated variants, incomplete enum dispatch, and missing `assert_never`. Confirm no caller represents `ThresholdConstructionKind`/`CoreThresholdPolicy` as a raw string.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, stale comments, and unverified "checks run" claims. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit-test lane (line 25) and architecture lane (line 28), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/contract/integration/CUDA/system categories are `NONE`, recorded explicitly. Never weaken tests or suppress Sonar to pass.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; this ticket's three modules must pass strict typing with no unexplained `Any` or suppression.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue and no artificial suppression.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — this ticket introduces closed string enums only; no seed, CUDA, or numeric identity field exists in its scope.` Confirm no such path was introduced regardless.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. Enum-only vocabulary is inherently compliant. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm the causal ladder is exactly B1–B4 (Roadmap §4); confirm B0 and `B-FedStatsBenign` never appear as ladder members; confirm no Laridi-faithful executable member exists (SB-29); confirm no `B5`/`B3-LGS` label survives.

**Audit 2 — Architecture fidelity.** Confirm the three modules match Architecture §5.1 exactly and that `ThresholdConstructionKind` is the sole discriminator tag with no optional-field-inferred variant anywhere in this vocabulary's design intent.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: does every member trace to an explicit citation? Does the naming-lock test actually assert the label text, not merely enum existence? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.
