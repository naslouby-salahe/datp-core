# P1-T018 — Implement locked domain constants and the protocol eligibility rule

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T018` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T018
2. **Title.** Implement locked domain constants and the protocol eligibility rule
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** DONE
5. **Priority.** P0 — Blocking
6. **Ticket type.** domain
7. **Owner.** datp-protocol-guardian
8. **Supporting agents.** statistics-auditor
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `PROTOCOL_MINIMUM_ELIGIBLE_CALIBRATION_SAMPLES`, `REGIME_D_TEMPORAL_HISTORICAL_FRACTION`, `ProtocolEligibilitySpec`. Eligibility classification feeds evaluation identity. Regime D coverage is owned by the mapped `RegimeDViabilityGateSpec`.
13. **Objective.** Implement the locked domain constants of Architecture §7.5 — `PROTOCOL_MINIMUM_ELIGIBLE_CALIBRATION_SAMPLES = CalibrationSampleCount(100)` and `REGIME_D_TEMPORAL_HISTORICAL_FRACTION = Probability(Decimal("0.70"))` — and `ProtocolEligibilitySpec` as the sole authority for the minimum client-calibration count. The Regime D coverage threshold is configuration-backed, mapped once, and injected into the feasibility gate.
14. **Why this ticket exists.** Roadmap §10 fixes `n_min = 100` as the eligibility threshold for the confirmatory CV(FPR) population, Roadmap §7 requires a Regime D partition to reach at least 90% client coverage against that threshold before science may proceed, and Roadmap §11 locks the D-temporal chronological boundary at 70/30. The eligibility minimum and temporal boundary are domain definitions; the Regime D gate consumes the validated, mapped catalog specification rather than a duplicate constant.
15. **Authority references.** Architecture §7.5 (locked domain definitions), §9.3 (protocol eligibility classification and its role in evaluation identity), §11.4 (mapping and validation rules: no configurable override for these constants). Roadmap §10 (`n_min = 100` eligibility threshold), §7 (Regime D 90% coverage gate), §11 (70/30 chronological boundary, sole temporal substrate Edge-IIoTset).
16. **Dependencies.** P1-T011, P1-T016.
17. **Blocks.** P1-T026.
18. **Downstream consumers.** P1-T026 (evaluation/operating-point/alert-burden result types) consumes these constants and `ProtocolEligibilitySpec` directly to classify clients before building `EligibleClientSet` and computing `EligibilityCoverageResult`.
19. **Preconditions.** P1-T011, P1-T016 are `DONE`.
20. **Inputs.** Architecture §7.5, §9.3, §11.4; Roadmap §10, §7, §11.
21. **Allowed scope.** `domain/mathematics/pooled_statistics.py`; `ProtocolEligibilitySpec` defined in the owning domain module (`domain/mathematics/pooled_statistics.py`, alongside the constants it wraps).
22. **Forbidden actions.** No configuration override of `PROTOCOL_MINIMUM_ELIGIBLE_CALIBRATION_SAMPLES` or `REGIME_D_TEMPORAL_HISTORICAL_FRACTION`; no confusion of the protocol eligibility minimum with a per-dataset ingestion minimum; no alternate coverage source or temporal-boundary value introduced as a caller-controlled parameter.
23. **Required configuration or NONE.** Regime D coverage is a validated catalog field mapped to `RegimeDViabilityGateSpec`; the remaining domain definitions carry no configuration surface.
24. **Implementation responsibilities.** Declare each domain constant as a module-level `Final` binding using the already-implemented value objects (`CalibrationSampleCount`, `Probability`); implement `ProtocolEligibilitySpec` as a frozen dataclass whose required minimum is injected explicitly; implement an eligibility classification helper that compares a client's recorded calibration sample count against the spec's minimum and returns a typed eligible/ineligible classification with reason.
25. **Required unit tests.** Single-ownership test confirming `PROTOCOL_MINIMUM_ELIGIBLE_CALIBRATION_SAMPLES` is referenced from exactly one definition site and that no second module defines a competing minimum; eligibility classification test below the threshold (99 samples) and at/above the threshold (100 and 101 samples) both classify correctly; a test confirming the constants are `Final` and cannot be reassigned or supplied as a constructor override.
26. **Required property tests.** Eligibility classification is monotonic in sample count: for generated sample counts, crossing the threshold from below to at/above never flips a client from eligible back to ineligible for a higher count.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE beyond the baseline framework-confinement check.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; property lane; Pyright strict.
33. **Acceptance criteria.** [x] `n_min = 100` is owned once in `domain/mathematics/pooled_statistics.py` with no configuration override anywhere. [x] Regime D coverage is represented by the mapped and composition-injected viability gate; `REGIME_D_TEMPORAL_HISTORICAL_FRACTION = 0.70` remains a locked constant. [x] Eligibility classification is correct at and around the threshold boundary.
34. **Completion evidence.** Changed files: `domain/mathematics/pooled_statistics.py` and its tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** NONE expected; these are locked definitions, not blockers, and require no exceptional failure path of their own.
36. **Stop conditions.** None.
37. **Deliverables.** Locked constants and the `ProtocolEligibilitySpec` eligibility authority.
38. **Final review checklist.** [ ] No duplicate coverage authority or configuration override of the domain constants exists. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [x] Read the complete roadmap and architecture before starting.
- [x] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [x] Verify every dependency is `DONE`.
- [x] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [x] Keep both status locations synchronized while active.
- [x] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [x] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for a second definition site of the eligibility minimum, coverage gate, or temporal fraction competing with this ticket's locked constants, any configuration schema field capable of overriding them, framework leakage into `domain/mathematics/`, and hidden defaults. Run import-linter/pytest-archon; record results. Confirm `domain/mathematics/pooled_statistics.py` imports nothing beyond the standard library plus `domain` value objects.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw `int`/`float`/`Decimal` literal standing in for one of these three constants anywhere else in the repository, `Any`/unjustified `cast`, and a configurable boolean or numeric field that would let a caller override the eligibility minimum, coverage gate, or temporal fraction.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25) and property lane (line 26), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Contract/architecture-specific/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `domain/mathematics/pooled_statistics.py` must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE` — these are static `Final` constants and a pure classification helper with no seed, batching, or recovery-identity surface; determinism follows directly from their immutability, which B3's boundary audit already covers by confirming no second definition site exists.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm `n_min = 100` matches Roadmap §10's confirmatory eligibility threshold exactly, the mapped Regime D viability gate is `0.90` as required by Roadmap §7, and `REGIME_D_TEMPORAL_HISTORICAL_FRACTION = 0.70` matches Roadmap §11's chronological 70/30 boundary exactly, with no silent rounding or unit mismatch (percentage versus fraction).

**Audit 2 — Architecture fidelity.** Confirm the two domain constants and `ProtocolEligibilitySpec` are defined exactly once in `domain/mathematics/pooled_statistics.py`, and that the composition root injects the mapped Regime D viability gate without a parallel coverage constant.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a future configuration schema field or experiment profile silently pass an alternate `minimum_calibration_samples` value into `ProtocolEligibilitySpec` without this ticket's own tests catching it? Does the eligibility-monotonicity property test actually cross the exact threshold boundary (99 to 100), not merely compare two arbitrary counts on the same side of it? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.
