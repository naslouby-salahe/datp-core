# P1-T013 — Implement per-stage nominal identity dataclasses

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T013` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T013
2. **Title.** Implement per-stage nominal identity dataclasses
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** domain
7. **Owner.** artifact-lineage-auditor
8. **Supporting agents.** —
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `DatasetSourceIdentity`, `FeatureSchemaIdentity`, `PartitionIdentity`, `SplitIdentity`, `FittedPreprocessorIdentity`, `ProcessedSplitIdentity`, `TrainingIdentity`, `CheckpointIdentity`, `CheckpointSelectionIdentity`, `CalibrationScoringIdentity`, `TestScoringIdentity`, `TemporalScoringIdentity`, `ThresholdIdentity`, `EvaluationIdentity`, `StatisticalIdentity`, `ResultFreezeIdentity`, `ReportIdentity`, the centralized-branch identities, `TemporalWindowIdentity`, `ResolvedConfigurationIdentity`, `RecoveryCompatibilityIdentity`, `StageIdentity`.
13. **Objective.** Implement each per-stage identity listed in field 12 as its own frozen, slotted, keyword-only dataclass wrapping a `StageFingerprint` (never a PEP 695 structural alias), and implement `StageIdentity` composing the distinct `CalibrationScoringIdentity`, `TestScoringIdentity`, and `TemporalScoringIdentity` fields.
14. **Why this ticket exists.** Nominal distinctness makes cross-stage and cross-role substitution a compile-time type error rather than a runtime bug: a `type CalibrationScoringIdentity = StageFingerprint` alias would let a calibration identity be passed anywhere a test-scoring identity is expected, because both are structurally just `StageFingerprint`. A dedicated `@dataclass` per identity prevents exactly this — Architecture §7.2 gives the worked example of `TrainingIdentity` vs `CalibrationScoringIdentity` as two distinct dataclasses, not aliases, and this ticket implements every remaining identity the same way, including centralized (B0) identities that must never be substitutable for their FedAvg counterparts.
15. **Authority references.** Architecture §7.2 (per-stage identity dataclasses, worked example), §13.1–§13.2 (stage-scoped lineage and identity composition), §29.2 (invalid-state guardrails against identity substitution).
16. **Dependencies.** P1-T012.
17. **Blocks.** P1-T028.
18. **Downstream consumers.** P1-T028 (scientific-protocol and policy aggregates) composes these identities transitively through every downstream specification; the reuse gate (P1-T038) compares these identities directly.
19. **Preconditions.** P1-T012 is `DONE`.
20. **Inputs.** Architecture §7.2, §13.1–13.2, §29.2.
21. **Allowed scope.** `domain/artifacts/lineage.py` and the identity dataclasses it declares.
22. **Forbidden actions.** No `type X = StageFingerprint` alias used as an identity anywhere in this ticket's scope; no identity shared between the FedAvg ladder and the centralized (B0) branch; no substitutable calibration/test/temporal scoring identity (each must be its own dataclass).
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Implement each identity as a frozen dataclass wrapping a `StageFingerprint`; implement `StageIdentity` as the composing dataclass that holds distinct `CalibrationScoringIdentity`/`TestScoringIdentity`/`TemporalScoringIdentity` fields so the reuse gate compares like roles and stages only.
25. **Required unit tests.** A calibration identity cannot be passed where a test identity is required — asserted via a nominal-type check (an attempted substitution is a static/type-checker failure, and a runtime test confirms the two dataclasses are not `==`-comparable across type even with identical wrapped fingerprints); a centralized identity is never accepted where a FedAvg identity is required.
26. **Required property tests.** Fingerprint-wrapping stability: constructing the same identity dataclass from the same `StageFingerprint` value always yields an equal instance.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE beyond the baseline framework-confinement check.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Pyright strict; unit lane; property lane.
33. **Acceptance criteria.** [ ] Every stage identity is a distinct nominal dataclass, never a structural alias. [ ] No alias is used as an identity anywhere. [ ] Centralized identities are separate from FedAvg identities. [ ] `StageIdentity` composes distinct roles correctly.
34. **Completion evidence.** Changed files: `domain/artifacts/lineage.py` (identity dataclasses) and their tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** NONE expected; cross-identity substitution is caught by strict typing and the nominal-type unit test, not by a runtime exception path.
36. **Stop conditions.** None.
37. **Deliverables.** Per-stage identity dataclasses.
38. **Final review checklist.** [ ] Nominal, not structural. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

### B3. Architecture boundary audit

Check the complete project for any `type X = StageFingerprint` structural alias used as an identity anywhere, incorrect type placement, unstable identity construction, cycles/side effects, and hidden defaults. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw `StageFingerprint` passed where a specific identity dataclass is required, `Any`/unjustified `cast`, and duplicated identity abstractions (two dataclasses meaning the same identity).

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25) and property lane (line 26), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Contract/architecture-specific/integration/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `domain/artifacts/lineage.py` must pass strict typing with no unexplained `Any` and no structural alias substituting for a nominal type.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

Check specifically that no identity dataclass here permits raw-float hashing or an unstable serialization path; confirm fingerprint-wrapping is stable and reproducible across repeated construction. This is a determinism-relevant ticket, not `NOT_APPLICABLE`.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset access, real training/scoring, or reachable scientific-execution path was introduced. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm the centralized (B0) identities remain structurally separate from the FedAvg-ladder identities, matching Roadmap §4's statement that B0 is a separate centralized reference, never a ladder member.

**Audit 2 — Architecture fidelity.** Confirm every identity listed in field 12 exists as its own dataclass matching Architecture §7.2/§13.1–13.2 exactly, with no identity missing and no two identities merged.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: does the nominal-type test actually attempt a cross-identity substitution and confirm it fails, or does it merely construct each identity independently? Could a future refactor collapse two of these dataclasses into one without a test catching it? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.
