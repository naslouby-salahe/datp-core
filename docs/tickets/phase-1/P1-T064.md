# P1-T064 — Implement run-state persistence and lifecycle storage

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T064` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T064
2. **Title.** Implement run-state persistence and lifecycle storage
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** persistence
7. **Owner.** implementation-engineer
8. **Supporting agents.** artifact-lineage-auditor
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `RunStateStore` (implementation: `append`, `status_of`), `StageStartRecord`, `StageCompletionRecord`, `StageReuseRecord`, `StageBlockRecord`, `StageFailureRecord`, `StageRecoveryRecord`.
13. **Objective.** Implement `infrastructure/persistence/run_state.py`: `RunStateStore.append(record) -> None` and `status_of(stage_fingerprint) -> RunStatus`, persisting the six lifecycle-record kinds beneath the `RUN_STATE` root.
14. **Why this ticket exists.** Split out because a lifecycle record is not an artifact and does not share `ArtifactStore`'s content-addressed commit protocol (Architecture §12.2); it is deliberately a narrower, distinct port.
15. **Authority references.** Architecture §12.2 (`RunStateStore` port, deliberately narrower than `ArtifactStore`), §12.4 (implementation), §18.3 (lifecycle records).
16. **Dependencies.** P1-T054, P1-T056.
17. **Blocks.** P1-T068, P1-T070.
18. **Downstream consumers.** P1-T039 (the executor records every stage transition here); the Phase 1 socle-readiness gate (P1-T070) verifies lifecycle records during its synthetic end-to-end run.
19. **Preconditions.** P1-T054, P1-T056 are `DONE`.
20. **Inputs.** Architecture §12.2, §12.4, §18.3.
21. **Allowed scope.** `infrastructure/persistence/run_state.py`.
22. **Forbidden actions.** No use of this store as a generic `ArtifactStore` substitute; no lifecycle record missing its stage/cell/stage-fingerprint/timestamp fields.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Implement `append` for each of the six record kinds; implement `status_of` returning the current `RunStatus` for a given stage fingerprint from the persisted record history.
25. **Required unit tests.** Each of the six record kinds persists with its required fields (stage, cell id, stage fingerprint, timestamps, and — for failure — the error family); `status_of` reflects the most recent recorded transition.
26. **Required property tests.** NONE.
27. **Required contract tests.** `RunStateStore` satisfies its Section 12.2 signature exactly (narrower than `ArtifactStore`, no content-addressed commit protocol).
28. **Required architecture tests.** NONE.
29. **Required integration tests.** A synthetic sequence of stage transitions (planned → running → completed/reused/blocked/failed/recovered) is recorded and queryable via `status_of`.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; contract lane; integration lane (persistence).
33. **Acceptance criteria.** [ ] All six record kinds are implemented and persist their required fields. [ ] `status_of` reflects the latest recorded transition. [ ] The store is not conflated with `ArtifactStore` (no content-addressed commit protocol, no generic object method).
34. **Completion evidence.** Changed files: `run_state.py` and its tests. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** NONE expected; a write failure here is `STAGE_BLOCKING` for the stage attempting to record its own transition.
36. **Stop conditions.** None.
37. **Deliverables.** Run-state/lifecycle persistence adapter.
38. **Final review checklist.** [ ] Narrower than `ArtifactStore`, as required by Architecture §12.2. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

### B3. Architecture boundary audit

Check the complete project for a call site using `RunStateStore` as a substitute for `ArtifactStore` (content-addressed lookup, atomic write, or bundle commit), and for a lifecycle record missing its stage/cell/stage-fingerprint/timestamp fields. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw dict/tuple substituting for one of the six lifecycle-record types, `Any`/unjustified `cast`, and a generic `record` parameter typed as `object` anywhere outside the `append(record: object) -> None` port signature itself (Architecture §12.2).

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25), contract lane (line 27), and integration lane (line 29), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/architecture/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; `run_state.py` must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

This is a determinism-relevant ticket. Check specifically that each of the six lifecycle-record kinds always carries its stage, cell id, stage fingerprint, and timestamp fields (and, for `StageFailureRecord`, the error family) with no optional/nullable gap that could make a later `status_of` query ambiguous; that `status_of` deterministically reflects the most recently appended record for a given stage fingerprint rather than an unordered or racy read; and that lifecycle-record integrity is independent of wall-clock ordering artifacts (e.g. two records appended within the same clock tick remain distinguishable and correctly ordered).

### B11. Phase 1 boundary confirmation

Confirm no real N-BaIoT/Edge-IIoTset/CICIoT2023 data or model output is written anywhere in this ticket — the synthetic stage-transition sequence at field 29 uses fabricated stage/cell identities and a temporary/sandboxed `RUN_STATE` root. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — lifecycle/run-state persistence carries no scientific claim.` Positive finding required: confirm this ticket protects the roadmap's resumability and audit-trail guarantees (Architecture §18) by giving every stage transition — start, completion, reuse, block, failure, recovery — an honest, queryable, typed record independent of any scientific artifact's content.

**Audit 2 — Architecture fidelity.** Confirm the adapter matches Architecture §12.2/§12.4/§18.3 exactly: the two-method `RunStateStore` signature (`append`, `status_of`), the six named record types, and their required fields, with no content-addressed commit protocol borrowed from `ArtifactStore`.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could two lifecycle records for the same stage fingerprint, appended in rapid succession, be misordered by `status_of` due to a coarse or colliding timestamp, silently reporting a stale status? Does the synthetic stage-transition integration test actually exercise all six record kinds in a realistic sequence (including a block and a recovery), or only the common completed/reused path? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.
