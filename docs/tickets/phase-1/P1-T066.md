# P1-T066 — Implement the environment and provenance inventory adapter

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T066` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T066
2. **Title.** Implement the environment and provenance inventory adapter
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** persistence
7. **Owner.** reproducibility-auditor
8. **Supporting agents.** implementation-engineer
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `CodeStateProvider` (implementation), `DependencyLockStateProvider` (implementation), `Clock` (implementation; frozen in tests).
13. **Objective.** Implement the infrastructure providers that produce `EnvironmentInventory`, `CodeState`, and `DependencyLockState` domain records: `CodeStateProvider` (VCS commit identity, clean/dirty working-tree state, a dirty-diff hash where permitted), `DependencyLockStateProvider` (the exact dependency-lock identity and pinned versions), and `Clock.now() -> datetime`.
14. **Why this ticket exists.** This ticket exists to make provenance honest by construction: a dirty working tree must always be reported as dirty, never silently rounded to "clean" because that would make a manuscript table traceable to code that did not actually produce it. `Clock` is injected and frozen in test contexts specifically because any direct `datetime.now()` call scattered through the codebase would make timestamp-dependent tests flaky and would make provenance timestamps themselves impossible to reproduce in a test double — routing every timestamp through one `Clock` collaborator closes both problems at once.
15. **Authority references.** Architecture §9.5 (`EnvironmentInventory`, `CodeState`, `DependencyLockState`, provenance types), §12.4 (`CodeStateProvider`/`DependencyLockStateProvider`/`Clock` implementation), §22.4 (pre-specification provenance).
16. **Dependencies.** P1-T061.
17. **Blocks.** P1-T068, P1-T070.
18. **Downstream consumers.** Every `ProvenanceRecord` construction from Phase 2 onward, and every campaign-freeze ticket (P3-T002, P5-T008, outside this extracted phase), depend on this adapter's output.
19. **Preconditions.** P1-T061 is `DONE`.
20. **Inputs.** Architecture §9.5, §12.4, §22.4.
21. **Allowed scope.** `infrastructure/runtime/` (the provider modules) and the domain-record construction boundary they feed.
22. **Forbidden actions.** No environment fact (a dirty-diff hash, a pinned-version string) ever entering scientific identity or a stage fingerprint; no `Clock` call anywhere outside this provider (tests always freeze it explicitly, never rely on wall-clock time); no dependency-lock or code-state fact fabricated when unavailable — an unverifiable fact is recorded as `UNVERIFIED`/absent explicitly, never guessed.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Implement `CodeStateProvider` reading VCS commit identity and clean/dirty state; implement `DependencyLockStateProvider` reading the committed lock established by P0-T004; implement `Clock.now()`; assemble the pinned exact versions of scikit-learn, PyArrow, NumPy, SciPy, blake3, and msgspec into `EnvironmentInventory`, alongside the `HardwareInventory` produced by P1-T061.
25. **Required unit tests.** A dirty working tree is always reported as dirty with a diff hash where permitted, never silently reported clean; `Clock` is injectable and frozen in test contexts, never falling back to real wall-clock time inside a test.
26. **Required property tests.** NONE.
27. **Required contract tests.** `CodeStateProvider`/`DependencyLockStateProvider` produce exactly the fields required by `CodeState`/`DependencyLockState` (Architecture §9.5), with no extra or missing field.
28. **Required architecture tests.** No environment or provenance fact ever enters a stage fingerprint or scientific specification.
29. **Required integration tests.** A synthetic clean checkout and a synthetic dirty checkout produce correctly distinguished `CodeState` records; the dependency-lock provider's output matches the P0-T004 committed lock exactly.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; contract lane; architecture lane; integration lane.
33. **Acceptance criteria.** [ ] A dirty working tree is never reported as clean. [ ] Pinned dependency versions are recorded exactly, matching the committed lock. [ ] No environment or provenance fact ever enters a scientific identity. [ ] `Clock` is always frozen in test contexts.
34. **Completion evidence.** Changed files: the provider modules and their tests. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** `EnvironmentIncompatibilityError` is raised when a required pinned version is genuinely absent; the absence is recorded, never fabricated.
36. **Stop conditions.** None.
37. **Deliverables.** The environment and provenance inventory provider modules.
38. **Final review checklist.** [ ] No fabricated provenance fact exists anywhere. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every dependency is `DONE`.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for a direct `datetime.now()`/`time.time()` call anywhere outside this provider's `Clock` implementation, an environment fact entering a stage fingerprint, and dependency-direction violations. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw string standing in for `CodeState`/`DependencyLockState`, `Any`/unjustified `cast`, and a fabricated default value substituting for an `UNVERIFIED`/absent marking when a fact is genuinely unavailable.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25), contract lane (line 27), architecture lane (line 28), and integration lane (line 29), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Property/CUDA/system categories are `NONE`, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; these provider modules must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

This is a primary reproducibility-relevant ticket: check specifically that `Clock` is the sole timestamp source anywhere provenance is recorded, that a dirty-diff hash is computed deterministically from the actual working-tree state (never a random or session-specific value), and that no environment fact leaks into a fingerprinted scientific identity.

### B11. Phase 1 boundary confirmation

Confirm no real N-BaIoT/Edge-IIoTset/CICIoT2023 provenance record is ever produced anywhere in this ticket — all tests use a synthetic clean/dirty checkout and a synthetic dependency lock. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — environment inventory carries no direct scientific claim.` Positive finding required: confirm this provider is exactly what makes the roadmap's implementation-clarity checklist item "reproducible run manifests" achievable — a manuscript claim can be traced back to the exact code commit and dependency lock that produced it.

**Audit 2 — Architecture fidelity.** Confirm `EnvironmentInventory`/`CodeState`/`DependencyLockState` match Architecture §9.5 exactly, and that this provider never enters a stage fingerprint, per §22.4.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a working tree with only whitespace or comment changes be reported as "clean" by a naive diff check that ignores non-functional changes, when the architecture requires it be reported dirty regardless? Does the dirty-checkout integration test actually modify a tracked file, or only add an untracked one (which VCS tools sometimes treat differently)? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.
