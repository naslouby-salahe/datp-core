# P1-T046 — Implement the SciPy statistics adapter and per-family metric calculators

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T046` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T046
2. **Title.** Implement the SciPy statistics adapter and per-family metric calculators
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** DONE
5. **Priority.** P0 — Blocking
6. **Ticket type.** infrastructure
7. **Owner.** statistics-auditor
8. **Supporting agents.** —
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `infrastructure/statistics/scipy_adapter.py`, `infrastructure/evaluation/metrics.py::MetricCalculator`.
13. **Objective.** Implement `infrastructure/statistics/scipy_adapter.py` (BCa bootstrap, Wilcoxon signed-rank, Spearman, Jensen–Shannon divergence, plus an in-repo `cliffs_delta` caller) and `infrastructure/evaluation/metrics.py`'s `MetricCalculator`, which exposes one typed method per metric family rather than a single generic `compute(family, inputs)` dispatcher.
14. **Why this ticket exists.** BCa is the primary confirmatory procedure and must never silently substitute a percentile bootstrap when the BCa computation is degenerate at small sample sizes — that substitution would quietly weaken the exact statistical guarantee the confirmatory claim depends on, so degeneracy is instead surfaced as `DegenerateBootstrapIntervalResult`, a typed outcome the confirmatory analysis (P1-T027) must handle explicitly. `MetricCalculator` exposing one named method per family (rather than a generic dispatcher) is the concrete enforcement of Architecture principle 4 ("typed contracts... no generic Handler"): a generic `compute(family, inputs)` signature would let a caller pass a mismatched family/input pair that only fails at runtime, whereas one method per family makes that mismatch a type-checker error instead.
15. **Authority references.** Architecture §12.4 (adapter implementation), §4.1 (SciPy's confined role; Cliff's delta explicitly *not* from SciPy), §9.3 (metric specification), §16.5 (degeneracy handling).
16. **Dependencies.** P1-T035, P1-T056, P1-T057.
17. **Blocks.** P2-T016, P2-T018.
18. **Downstream consumers.** P2-T016 and P2-T018 (outside this extracted phase) consume this adapter and calculator directly.
19. **Preconditions.** P1-T035, P1-T056, P1-T057 are `DONE`.
20. **Inputs.** Architecture §12.4, §4.1, §9.3, §16.5.
21. **Allowed scope.** `infrastructure/statistics/`, `infrastructure/evaluation/`.
22. **Forbidden actions.** No generic `compute(family, inputs)` metric method anywhere; no silent BCa-to-percentile substitution on degeneracy; no SciPy object (a `scipy.stats` result object, for example) crossing upward out of `infrastructure/statistics/`.
23. **Required configuration or NONE.** From the frozen statistical/evaluation specifications; the bootstrap resample count is always explicit (`BootstrapResampleCount`, P1-T014), never defaulted.
24. **Implementation responsibilities.** Implement BCa with an explicit, caller-supplied resample count; implement the typed degeneracy result path for small-sample BCa failure; implement one named `MetricCalculator` method per metric family, matching the families declared in P1-T004.
25. **Required unit tests.** BCa on a synthetic set of paired deltas produces a correctly-directioned interval; a deliberately tiny sample size (below BCa's minimum viable resample support) produces a typed `DegenerateBootstrapIntervalResult`, never a silent percentile substitution; each `MetricCalculator` family method is exercised on a known-value input.
26. **Required property tests.** BCa sanity: the computed interval always contains the point estimate, and its bounds are ordered correctly, across generated paired-delta inputs.
27. **Required contract tests.** The adapter satisfies the `StatisticalProcedureRunner` port from P1-T035 exactly.
28. **Required architecture tests.** No SciPy object crosses upward out of `infrastructure/statistics/`.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Unit lane; property lane; contract lane; Pyright strict.
33. **Acceptance criteria.** [x] BCa is the primary procedure and always takes an explicit resample count. [x] Degeneracy is a typed result, never a silent downgrade. [x] Every metric family has its own named calculator method. [x] No SciPy object crosses upward out of the adapter.
34. **Completion evidence.** Changed files: `infrastructure/statistics/scipy_adapter.py`, `infrastructure/evaluation/metrics.py`, and their tests. Commands run and results. Cleanup confirmation.
35. **Failure and blocker behavior.** `StatisticsError` is reserved for a genuinely unexpected failure only (not for a degenerate-but-valid statistical outcome, which is `DegenerateBootstrapIntervalResult`); no other failure is expected in this ticket's own scope.
36. **Stop conditions.** None.
37. **Deliverables.** The SciPy statistics adapter and per-family metric calculators.
38. **Final review checklist.** [x] No percentile substitution ever occurs silently. [x] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [x] Read the complete roadmap and architecture before starting.
- [x] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [x] Verify every dependency is `DONE`.
- [x] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step: 2026-07-15T13:33:29Z — frozen statistical result/port and metric-family contract inspection.
- [x] Keep both status locations synchronized while active.
- [x] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately: 2026-07-15T13:41:38Z — adapter implementation and focused validation complete; repository-wide review is active.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition.
- [x] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches: 2026-07-15T14:45:28Z — shared full-project Nox, Sonar, CodeScene, and randomized-suite gate passed.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files; no defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

The audit inventory must explicitly cover `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No “pre-existing,” “outside the diff,” “another agent,” “legacy,” or “untouched file” exemption is valid.

### B3. Architecture boundary audit

Check the complete project for a SciPy object crossing upward out of `infrastructure/statistics/`, a generic metric-computation dispatcher anywhere, and a silent BCa-to-percentile fallback path. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for a raw tuple/dict standing in for `DegenerateBootstrapIntervalResult`/`ValidBootstrapIntervalResult`, `Any`/unjustified `cast`, and a hardcoded default resample count anywhere (there must be none — the count is always explicit, per P1-T014).

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, contradictory documents, and stale comments. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the unit lane (line 25), property lane (line 26), and contract lane (line 27), plus Pyright strict, Ruff, Pylance compatibility, import-linter, pytest-archon, full suite, randomized ordering, Nox, Sonar, git-diff and untracked-file inspection. Architecture-specific/integration/CUDA/system categories are `NONE` beyond the baseline confinement check, recorded explicitly.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; these modules must pass strict typing with no unexplained `Any`.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this ticket must pass with no unresolved blocker/critical/major issue.

### B10. Determinism and reproducibility audit

Check specifically that BCa's resample count is always explicit (no hidden default that could silently change confidence-interval width between two otherwise-identical runs), and that the bootstrap resampling itself is driven only by an explicitly derived seed (via `SeedRole.BOOTSTRAP`, locked in P1-T009), never an unowned global random state.

### B11. Phase 1 boundary confirmation

Confirm no real-dataset statistical result is computed anywhere in this ticket — all unit/property tests use synthetic paired-delta arrays. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; recorded once in `docs/tickets/phase-1/README.md`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm BCa is treated as the primary confirmatory procedure with Wilcoxon/Cliff's δ as descriptive secondary evidence only, matching Roadmap §10, and that degeneracy is never silently downgraded to a percentile bootstrap in a way that could mask a weak confirmatory result.

**Audit 2 — Architecture fidelity.** Confirm the adapter matches Architecture §12.4/§4.1 exactly (SciPy confined to this adapter; Cliff's delta called in from `domain/mathematics/effect_sizes.py`, never reimplemented here) and that `MetricCalculator` has no generic dispatch method, per Architecture principle 4.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a caller pass a resample count of zero or one and receive a silently meaningless "valid" interval instead of a typed degeneracy result? Does the property test for BCa sanity actually generate small-sample edge cases likely to trigger degeneracy, or only comfortable large-sample cases? Fix and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

Not a cross-cutting owner. Must remain compliant with `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, `P0-T027`, and the Phase 1 gate `P1-T070`.

### B14. Completion-evidence ledger

Before `DONE`, record a check-by-check ledger: changed files and reasons; verified dependencies; implementation steps; exact commands and results; each required unit, property, contract, architecture, integration, CUDA, and synthetic system/end-to-end test (or its specific Part A `NONE` reason); complete-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; the three B12 audit records; blockers; remaining risks; and every skipped check with its exact reason. A mandatory check cannot be skipped merely to obtain a green status, and no pre-existing-defect exemption is valid.

This is an evidence requirement, not an authorization to access real N-BaIoT, Edge-IIoTset, or CICIoT2023 data, train, score, or conduct any scientific execution. `P1-T070` remains the sole Phase 1 socle-readiness gate; this ticket must not duplicate the gate's responsibility.

### Review evidence — 2026-07-15T13:45:24Z

- Verified P1-T035, P1-T056, and P1-T057 are `DONE`. Implemented the seeded SciPy BCa/Wilcoxon/Spearman/Jensen–Shannon adapter, delegated Cliff's delta to the existing pure domain function, implemented the exact `StatisticalProcedureRunner` BCA port adapter, and added eight distinct typed `MetricCalculator` family methods. The narrow local SciPy stubs preserve strict static typing without propagating SciPy types upward.
- Focused unit/property/contract/architecture checks passed: `uv run pytest tests/architecture/test_scipy_statistics_confinement.py tests/unit/infrastructure/test_scipy_statistics_adapter.py tests/unit/infrastructure/test_metric_calculator.py tests/property/test_scipy_statistics_adapter.py -q` (7 passed). The synthetic BCa tests cover valid interval directionality, below-minimum samples, a resample count of one, deterministic seed reuse, port conformance, and all eight metric families.
- Complete-project checks passed after the final architecture-test addition: `uv run ruff check .`; `uv run ruff format --check .`; `uv run pyright src tests`; `uv run nox -s architecture` (import-linter and 10 selected architecture tests); `uv run pytest -q`; and randomized `uv run pytest -q --randomly-seed=4601` and `--randomly-seed=9773`. No standalone `pylance` executable is installed; the configured strict Pyright check is the available parity check.
- Repository-wide audit covered `src/`, `tests/`, `configs/`, `ai/`, `docs/`, `pyproject.toml`, `noxfile.py`, root metadata, provider adapters, and local typing stubs. Source scans found SciPy imports only in `infrastructure/statistics/scipy_adapter.py`; no generic `compute(family, inputs)` dispatcher, silent BCa-to-percentile path, ticket/document reference in runtime code, `Any`/`cast` in the touched surfaces, raw interval carrier, hidden resample default, or compatibility shim. `git diff --check` passed; the broad uncommitted Phase 1 worktree contains no generated audit clutter.
- Audit 1 — PASS: BCa remains the primary confirmatory method and the Wilcoxon/Cliff's-delta primitives do not alter the typed confirmatory outcome; degeneracy returns `DegenerateBootstrapIntervalResult`, never a percentile result. Audit 2 — PASS: SciPy is confined to the adapter, Cliff's delta is imported from `domain/mathematics/effect_sizes.py`, and metric calculation is separated into named family methods. Audit 3 — PASS: one resample and undersized samples return typed degeneracy; the generated-property test admits small valid samples and validates every valid interval's ordering and point containment.
- Integration, CUDA, and system/end-to-end categories are `NONE` under Part A; all tests use synthetic values, with no real-dataset access, training, scoring, or scientific execution. No blocker is present.
- Nox, Sonar, and CodeScene remain pending only as the user-directed shared full-project batch gate for P1-T045/P1-T046/P1-T060–P1-T069. They are not represented as passed and must pass before this ticket transitions from `IN_REVIEW` to `DONE`.
