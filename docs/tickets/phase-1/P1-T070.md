# P1-T070 — Implement the lineage/reuse/atomicity/determinism validation and synthetic end-to-end socle test

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P1-T070` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P1-T070
2. **Title.** Implement the lineage/reuse/atomicity/determinism validation and synthetic end-to-end socle test
3. **Phase.** 1 — Complete Technical Socle
4. **Status.** NOT_STARTED
5. **Priority.** P0 — Blocking
6. **Ticket type.** application
7. **Owner.** reproducibility-auditor
8. **Supporting agents.** artifact-lineage-auditor, experiment-engineer
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `tests/system/synthetic/` end-to-end suite; the reuse/invalidation, atomicity, checkpoint/recovery-separation, deterministic-seed, and batching-equivalence test suites; the campaign-execution-prohibition guard. **This is the Phase 1 socle-readiness gate.**
13. **Objective.** Implement `tests/system/synthetic/` (a reduced synthetic end-to-end run of the complete stage sequence), the reuse/invalidation, atomicity, checkpoint/recovery-separation, deterministic-seed, and batching-equivalence suites, and a campaign-execution-prohibition guard asserting that no Phase 1 code path can perform real scientific execution.
14. **Why this ticket exists.** This ticket is renumbered from the prior revision's `P1-T051` as part of the Phase 1 mega-ticket decomposition recorded in `docs/MASTER_TICKET_LOG.md`; the ID `P1-T051` is retired from this role and now denotes the unrelated "application telemetry vocabulary and contracts" ticket, and every reference to the old gate ID has been updated to `P1-T070` throughout this extraction. This is the socle-readiness gate because it is the one place that proves, on synthetic data, that every capability built across the other 65 Phase 1 tickets actually composes into a working whole: that B1–B4 genuinely share one calibration score set and one test score set (the reuse mechanism P1-T038 declares), that atomicity and determinism hold under the concrete adapters (not just at the type level), and that no code path anywhere in the socle can reach real scientific execution before Phase 2 begins.
15. **Authority references.** Architecture §21.2 (system/synthetic and reuse test categories), §13.3 (reuse/lineage compatibility), §16.7 (determinism/seed derivation), §29 (invalid-state guardrails, broadly); `docs/MASTER_TICKET_LOG.md` Section F (Phase 1 gate definition).
16. **Dependencies.** P1-T001–P1-T009, P1-T011–P1-T040, P1-T042–P1-T046, P1-T049–P1-T050, P1-T051–P1-T069 (every other Phase 1 ticket).
17. **Blocks.** P2-T001.
18. **Downstream consumers.** P2-T001 and all of Phase 2 (outside this extracted phase) depend on this gate passing before any anchor-implementation work begins.
19. **Preconditions.** Every Phase 1 implementation ticket P1-T001 through P1-T069 (excluding this gate itself) is `DONE` and its own completion evidence is recorded in `docs/tickets/TICKET_STATUS.md`.
20. **Inputs.** Architecture §21.2, §13.3, §16.7, §29; `docs/MASTER_TICKET_LOG.md` Section F.
21. **Allowed scope.** `tests/system/synthetic/`, `tests/integration/{learning,persistence}/`, `tests/property/`.
22. **Forbidden actions.** No real-data path is exercised anywhere in this ticket's own tests; no scientific output root (`checkpoints/`, `outputs/anchor/`, `outputs/journal/`) is ever written to by this ticket; no campaign execution of any kind occurs here — this gate proves the socle works, it does not run a campaign.
23. **Required configuration or NONE.** Synthetic test profiles come from `configs/tests/` (established by P1-T053).
24. **Implementation responsibilities.** Implement a synthetic end-to-end run exercising the full stage sequence (source inspection through reporting) on fabricated data; implement reuse assertions confirming B1–B4 share exactly one calibration identity and one test identity, and confirming a role/lineage substitution attempt fails; implement atomicity, recovery-separation, determinism, and batching-equivalence tests against the concrete adapters built across P1-T042–P1-T067; implement a guard test proving real-data execution is unreachable anywhere in the Phase 1 socle.
25. **Required unit tests.** NONE — this ticket's own scope is exercised through property, integration, and system-level tests; it introduces no new unit-testable pure function of its own.
26. **Required property tests.** Reuse/invalidation properties (a threshold-only change never invalidates the shared calibration/test identity; any scientifically distinct change always does) and batching-equivalence properties (sequential and parallel execution of the same synthetic plan are equivalent within the declared numeric tolerance).
27. **Required contract tests.** NONE beyond what each individual adapter ticket already established; this ticket exercises those contracts together, not in isolation.
28. **Required architecture tests.** The campaign-execution-prohibition guard: no real-data path is reachable from any Phase 1 module, verified by a repository-wide check that no code path under `src/datp_core` as it stands at the end of Phase 1 can resolve a real dataset source, real checkpoint, or real score artifact.
29. **Required integration tests.** Atomicity, determinism, and checkpoint/recovery-separation, each exercised in the `learning` and `persistence` integration lanes against the concrete Phase 1 adapters (not test doubles), on synthetic data.
30. **Required CUDA tests.** NONE beyond what the individual CUDA-lane tickets (P1-T043, P1-T044, P1-T045, P1-T060, P1-T063) already require; this ticket's own system test may run in the CUDA lane if it exercises a CUDA-required stage, but it introduces no additional CUDA-specific assertion beyond composing what those tickets already verify.
31. **Required system/end-to-end tests.** A synthetic end-to-end run of the complete stage sequence, exercising telemetry, reporting specifications, artifact schema/version handling, persistence, recovery, composition, and the CLI contract together in one coherent pass.
32. **Validation commands.** The complete Nox validation run across all lanes, executed against synthetic data only.
33. **Acceptance criteria.** [ ] Every ticket P1-T001–P1-T069 is `DONE`; no socle capability remains unowned. [ ] The synthetic end-to-end run passes completely. [ ] B1–B4 share exactly one calibration identity and one test identity; a role/lineage substitution attempt fails. [ ] Atomicity, determinism, and batching-equivalence all pass against the concrete adapters. [ ] Telemetry, reporting specifications, artifact schema/version handling, persistence, recovery, composition, and CLI contracts are all exercised together. [ ] No real-data path is reachable anywhere in the Phase 1 socle. [ ] The socle-readiness gate is green.
34. **Completion evidence.** Changed files: `tests/system/synthetic/`, `tests/integration/{learning,persistence}/`, `tests/property/`, and their results. Commands run and results, including the complete Nox validation output. Cleanup confirmation.
35. **Failure and blocker behavior.** Any suite failure fails the gate outright; the gate blocks all of Phase 2 from beginning until every failure is resolved and the complete suite is green.
36. **Stop conditions.** Stop and block Phase 2 entry if any socle suite fails for any reason.
37. **Deliverables.** The synthetic system test, the socle-validation suites (reuse, atomicity, recovery-separation, determinism, batching-equivalence), and the campaign-execution-prohibition guard.
38. **Final review checklist.** [ ] The gate is green on synthetic data only — no real data was ever touched to reach this state. [ ] Status synchronized per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and architecture before starting.
- [ ] Read this ticket file and `docs/tickets/TICKET_STATUS.md` completely.
- [ ] Verify every one of the 65 dependency tickets is `DONE` in `docs/tickets/TICKET_STATUS.md` — this gate's own dependency list is the complete Phase 1 ticket set minus itself, and every entry must show `DONE`, not merely `IN_REVIEW` or `IN_PROGRESS`, before this ticket may even begin.
- [ ] Change status to `IN_PROGRESS`, updating `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and step.
- [ ] Keep both status locations synchronized while active.
- [ ] Change status to `IN_REVIEW` before the B2–B10 audits; record each B12 audit separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker/unblock condition — a `BLOCKED` state here blocks the entire Phase 2 entry, so the blocker record must be unambiguous.
- [ ] Change status to `DONE` only after every criterion and gate passes; record completion evidence in both locations; verify final status matches.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only this ticket's files. Because this is the phase gate, this audit is deliberately the broadest of any Phase 1 ticket: it re-confirms every prior ticket's own B2 audit collectively holds across the finished socle, not merely that this ticket's own new test files are clean. No defect is dismissible as pre-existing, another's fault, out-of-diff, legacy, or inconvenient. Fix authority-grounded issues before `DONE`; mark `BLOCKED` with the exact unresolved decision otherwise.

### B3. Architecture boundary audit

Run the complete architecture suite built by P1-T050 against the finished Phase 1 socle and confirm it passes cleanly; additionally confirm no test in this ticket's own scope introduces a new dependency-direction violation, framework leak, or forbidden module name. Run import-linter/pytest-archon; record results.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Re-run the repository-wide object-shaped-dictionary scan established by P1-T015 against the complete finished socle, confirming no regression was introduced by any of the 65 dependency tickets collectively; scan this ticket's own new test code for `Any`/unjustified `cast` and raw dicts standing in for typed test fixtures.

### B5. Prohibition on ticket and document references in code

Search the complete repository for `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture` outside planning documents; confirm zero matches across the complete finished Phase 1 socle, not only this ticket's own new files.

### B6. Documentation and comment audit

Check the complete project for stale documentation, retired IDs, contradictory documents, and stale comments — with particular attention to confirming every reference to the former gate ID `P1-T051` has actually been updated to `P1-T070` everywhere in the repository, since this ticket is the renumbered gate itself. Confirm retired IDs `P1-T010`, `P1-T041`, `P1-T047`, `P1-T048` are never referenced as live.

### B7. Complete validation requirements

Run the complete Nox validation session across every lane — unit, property, contract, architecture, integration, the serialized CUDA lane, and the synthetic system lane — plus repository-wide Ruff lint and format, Pyright strict, Pylance configuration compatibility, import-linter, pytest-archon, the complete test suite under randomized ordering, Sonar analysis and quality gate, complete git-diff inspection, and untracked-file/cleanup inspection. This is the one Phase 1 ticket where "impacted tests" is explicitly insufficient by definition — the complete suite is the requirement, not a subset.

### B8. Pyright and Pylance requirements

`P0-T008` owns Pyright strict/Pylance parity; this gate additionally re-confirms that Pyright strict passes across the complete `src/datp_core` and `tests/` tree as it stands at the end of Phase 1, with no unexplained `Any` anywhere in the finished socle.

### B9. Sonar requirements

`P0-T027` owns the Sonar gate; this gate additionally re-confirms the complete Phase 1 socle passes Sonar's quality gate with no unresolved blocker/critical/major issue and no artificial suppression anywhere.

### B10. Determinism and reproducibility audit

This ticket is the primary determinism-verification gate for the entire Phase 1 socle. Check specifically: reuse/invalidation lineage is exact (never inferred from filename, path, or list position, per the master log's Section B); the deterministic-seed suite confirms every seed traces back to `derive_seed` with no scattered global call surviving anywhere in the finished socle; batching-equivalence holds under both sequential and parallel execution within the declared numeric tolerance; atomicity holds under a simulated interruption at the adapter level (not merely at the type level, since this is the first ticket to exercise the concrete adapters together); checkpoint and recovery state remain structurally and behaviorally separate end-to-end.

### B11. Phase 1 boundary confirmation

This ticket's own campaign-execution-prohibition guard (line 28) IS the enforcement mechanism for this section: confirm the guard actually proves no real N-BaIoT/Edge-IIoTset/CICIoT2023 data path is reachable from any module in the finished Phase 1 socle, rather than merely asserting the absence of a specific known path. This ticket applies the master log's Phase 1 "Permitted work"/"Forbidden work" statement in place of the conversion task's Phase-0-specific Section 19, per the conversion task's authority order; this substitution is recorded here and in `docs/tickets/phase-1/README.md`, which this ticket's own completion should cross-check for consistency.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** Confirm the finished Phase 1 socle introduces no executable path for any out-of-scope roadmap concept (poisoning, Dynamic DATP, privacy guarantees, deployment profiling, backdoor, evasion, full drift detection, generic FL-IDS expansion — Roadmap §2, §5.9, §16) and that the fixed-encoder/benign-only-calibration/CV(FPR)-primary/AUROC-control identity is structurally represented throughout, not merely asserted in comments.

**Audit 2 — Architecture fidelity.** Confirm the complete socle passes the architecture-boundary suite (P1-T050) cleanly, that every layer respects Architecture §3's dependency graph, and that the synthetic end-to-end run genuinely exercises every stage in Architecture §6.3's `PipelineStage` order, with none skipped.

**Audit 3 — Adversarial cross-check.** Review the finished Phase 1 socle as a hostile reviewer, cross-checking: does the reuse-sharing assertion (B1–B4 share one calibration/test identity) actually construct four distinct threshold policies and confirm their upstream identities are `==`-equal, or does it merely assert "no error occurred"? Could the campaign-execution-prohibition guard be satisfied vacuously because no adapter yet implements real-data reading, rather than because a genuine guard actively blocks it? Is any ticket's own `NOT_APPLICABLE` marking (across all 65 dependency tickets) actually a disguised skip of real work rather than a genuine non-applicability? Fix every issue found and rerun all three audits until each passes clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is the Phase 1 socle-readiness gate itself — the terminal cross-cutting checkpoint for every responsibility every other Phase 1 ticket established: Pyright/Pylance parity (`P0-T008`), repository-wide post-implementation auditing (`P0-T022`), raw-dictionary and ticket/document-reference prohibitions (`P0-T023`), the scientific-drift audit mechanism (`P0-T024`), stale-documentation enforcement (`P0-T025`), ticket-status lifecycle governance (`P0-T026`), and the Sonar quality gate (`P0-T027`) must all demonstrably hold across the complete finished socle before this gate — and therefore Phase 2 — may open.
