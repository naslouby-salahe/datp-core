# P6-T001 — Final readiness confirmation and journal execution-attempt creation

Standalone Phase 6 work contract. It governs only its stated classification and allowed scope. Authority conflicts are recorded as `BLOCKED`; they never authorize execution, status advance, or an implicit choice.

## Part A — Ticket template


- **Ticket ID.** P6-T001
- **Title.** Final readiness confirmation and journal execution-attempt creation
- **Phase.** 6
- **Status.** NOT_STARTED
- **Priority.** P0 — Blocking
- **Ticket type.** campaign
- **Owner.** roadmap-orchestrator
- **Supporting agents.** reproducibility-auditor
- **Scientific-execution classification.** PLANNING_ONLY
- **Campaign scope.** JOURNAL
- **Roadmap experiment IDs.** —
- **Architecture contracts/types owned.** `ExecutionAttemptId`
- **Objective.** Confirm the go decision, clean worktree, and frozen manifest, and create the initial journal `ExecutionAttemptId` under the frozen journal `RunIdentity`.
- **Why this ticket exists.** Execution begins under a coherent, recorded attempt identity bound to the frozen campaign identity.
- **Authority references.** Arch §7, §18; this backlog §B.
- **Dependencies.** P5-T009.
- **Blocks.** P6-T002.
- **Downstream consumers.** P6-T002 consumes the initial journal `ExecutionAttemptId`.
- **Preconditions.** P5-T009 (go).
- **Inputs.** Go decision, clean worktree state, frozen manifest, frozen journal `RunIdentity`.
- **Allowed scope.** Readiness confirmation; attempt-identity creation.
- **Forbidden actions.** No scientific config change; no execution before attempt creation.
- **Required configuration or NONE.** NONE.
- **Implementation responsibilities.** Verify go/clean/frozen; mint the initial attempt id.
- **Required unit tests.** NONE.
- **Required property tests.** NONE.
- **Required contract tests.** NONE.
- **Required architecture tests.** NONE.
- **Required integration tests.** NONE.
- **Required CUDA tests.** NONE.
- **Required system/end-to-end tests.** Campaign checks: attempt id bound to frozen `RunIdentity`.
- **Validation commands.** Readiness confirmation; manifest inspection.
- **Acceptance criteria.** [ ] Go/clean/frozen confirmed. [ ] Initial attempt id minted. [ ] `RunIdentity` unchanged. [ ] No config drift.
- **Completion evidence.** Confirmed go/clean/frozen state; minted attempt id; validation command results; no config drift confirmation.
- **Failure and blocker behavior.** Reject any post-freeze config drift.
- **Stop conditions.** Stop if not go or worktree dirty.
- **Deliverables.** Journal execution-attempt identity.
- **Final review checklist.** [ ] Bound to frozen identity.

## Part B — Mandatory Phase 6 campaign lifecycle and audits

### B1. Lifecycle, campaign identity, and status discipline

- [ ] Before work, read the complete roadmap, complete architecture, this ticket, direct dependencies, downstream consumers, the Phase 6 index, the frozen Phase 5 manifest, and `docs/tickets/TICKET_STATUS.md`.
- [ ] Synchronize this ticket and its status-register row through `NOT_STARTED → READY → IN_PROGRESS → IN_REVIEW → DONE`; record timestamp, current step, campaign `RunIdentity`, `ExecutionAttemptId` where applicable, evidence, blockers, and all audit outcomes.
- [ ] Do not mark `DONE` until every Part A acceptance criterion, validation command, applicable audit, cleanup check, and completion-evidence item is complete. A partial artifact, narrow test, or report without provenance never suffices.
- [ ] Set both records to `BLOCKED` immediately for an authority, dependency, feasibility, identity, lineage, execution, or validation blocker, preserving evidence and the exact unblock condition.

### B2. Journal-campaign scientific and recovery locks

Only tickets classified `JOURNAL_CAMPAIGN_ALLOWED` may perform real journal scientific work, and only under the frozen Phase 5 campaign manifest. The phase is a coordinated campaign, not a collection of mini-campaigns or exactly-once physical writes. Preserve fixed model state and compatible score artifacts within each B1–B4 ladder cell; threshold scope alone varies, calibration is benign-only, CV(FPR) is primary, and AUROC is control. B0 has a separate centralized lineage; stress tests remain outside the core B1–B4 causal ladder, and the matched comparator has its stated separate role. External and temporal B1–B4 branches retain their roadmap-defined external/boundary roles, passed gates, and identities, but never alter the locked Regime-A confirmatory endpoint.

Keep resume, compatible recovery, bounded transient infrastructure retry, technical-invalidity correction, affected-stage recomputation, a new attempt, and a new campaign distinct. A valid weak, null, mixed, unfavorable, or opposite-direction outcome is frozen and reported with pre-committed wording; it never authorizes retry, configuration changes, selection, suppression, or replacement. Only diagnosed technical invalidity permits corrective work with preserved failed evidence, root cause, affected-stage scope, tests, approval, and unchanged-science confirmation (or a new campaign identity).

### B3. Mandatory implementation-quality audit for future ticket execution

After execution or implementation, audit the full repository rather than only the diff: source, tests, configurations, documentation, governance, scripts, root metadata, quality-tool configuration, artifact namespaces, campaign/attempt records, and the status register. Check roadmap fidelity; architecture layers and framework confinement; typed configuration, identities, lineage/reuse, persistence, lifecycle, determinism, CUDA/process boundaries, and namespace separation; no raw protocol strings/dictionaries, hidden defaults, stale documentation, unsupported claims, aliases, redirects, shims, compatibility wrappers, or planning-document references in source/test code.

Run every applicable Part A validation lane and the repository quality gates required by the governing tickets; record unavailable tools and `NONE` categories honestly. Completion evidence must list changed files and reasons, exact commands and results, dependency state, identities/attempts/failures, audit results, cleanup, final diff/untracked-file inspection, blockers, risks, and skipped checks.

### B4. Independent completion audits

- **Roadmap fidelity.** Confirm frozen-plan execution, fixed-encoder/FedAvg B1–B4 causal ladder, benign-only calibration, ten paired E-C1 seeds, complete-cell outcomes, fallback wording, stress/comparator separation, and no result-driven protocol change.
- **Architecture fidelity.** Confirm typed identities and configuration, full-lineage reuse, immutable/atomic persistence, deterministic CUDA/process behavior, typed failure/recovery handling, and anchor/journal namespace protection.
- **Adversarial review.** Check dependencies, status, manifests, configuration, outputs, attempt/failure evidence, and freezes for invalid reuse, untracked recomputation, result shopping, suppression, partial outputs, premature closure, scientific drift, or claimed-but-unrun validation.

Record the three audits separately. These requirements authorize only the ticket's explicit Phase 6 classification and never broaden the frozen plan.
