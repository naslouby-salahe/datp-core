# P2-T008 — Implement the fixed autoencoder, optimizer, and scheduler

Standalone Phase 2 implementation specification. It is independently actionable; the master log remains authoritative for canonical identity, phase membership, ordering, and dependency graph. This conversion creates documentation only and does not implement the ticket.

## Part A — Ticket template

1. **Ticket ID.** P2-T008
2. **Title.** Implement the fixed autoencoder, optimizer, and scheduler
3. **Phase.** 2
4. **Status.** DONE
5. **Priority.** P0 — Blocking
6. **Ticket type.** training
7. **Owner.** federated-learning-engineer
8. **Supporting agents.** datp-protocol-guardian
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** ANCHOR
11. **Roadmap experiment IDs.** E-C1
12. **Architecture contracts/types owned.** `AutoencoderSpec`, `TrainingSpec`
13. **Objective.** Implement the fixed autoencoder (recovered architecture, no BatchNorm), optimizer, and scheduler as the anchor `AutoencoderSpec`/`TrainingSpec`, validated on a synthetic tiny model without real training.
14. **Why this ticket exists.** The fixed encoder is the locked ladder substrate; its architecture/optimizer/scheduler must match recovered semantics, not be guessed.
15. **Authority references.** Arch §9.1, §16.2, §27 (AE/optimizer blockers); Road §2.
16. **Dependencies.** P2-T007, P1-T043, P2-T001.
17. **Blocks.** P2-T009.
18. **Downstream consumers.** P2-T009 consumes the anchor training spec and fixed autoencoder for FedAvg training.
19. **Preconditions.** P2-T007, P1-T043, P2-T001 complete.
20. **Inputs.** Arch §9.1, §16.2, §27; Road §2.
21. **Allowed scope.** `infrastructure/learning/models/autoencoder.py` anchor path; anchor training spec.
22. **Forbidden actions.** No BatchNorm; no guessed architecture presented as recovered; no real training run.
23. **Required configuration or NONE.** Anchor training config (AE dims/activation/optimizer/lr/scheduler).
24. **Implementation responsibilities.** AE per recovered spec; optimizer/scheduler; synthetic forward/backward smoke.
25. **Required unit tests.** No BatchNorm.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE.
29. **Required integration tests.** Forward/backward runs deterministically (synthetic tiny model).
30. **Required CUDA tests.** Synthetic forward/backward deterministic (cuda lane).
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** CUDA lane (synthetic); unit lanes.
33. **Acceptance criteria.** [ ] AE matches recovered spec. [ ] No BatchNorm. [ ] Synthetic forward/backward deterministic. [ ] No real training.
34. **Completion evidence.** Changed files (autoencoder.py, anchor training spec); commands and tests run with results; training identity inputs; cleanup confirmation; skipped checks/reasons.
35. **Failure and blocker behavior.** `TrainingError` on invalid config; readiness blocker if unresolved.
36. **Stop conditions.** Stop if AE/optimizer/scheduler is unresolved; carry as a readiness blocker (smoke only).
37. **Deliverables.** Fixed AE + optimizer/scheduler.
38. **Final review checklist.** [ ] Recovered, not guessed.

## Part B — Mandatory Phase 2 lifecycle, boundaries, and audits

### B1. Lifecycle checklist

- [ ] Immediately before implementation, read the complete roadmap, complete architecture, this ticket, the Phase 2 index/gate, `docs/tickets/TICKET_STATUS.md`, each direct dependency, and cited authority sections.
- [ ] Verify each dependency, including `P1-T070` where required, is `DONE`.
- [ ] Set this ticket and the corresponding status-register row to `IN_PROGRESS` in the same change; record start time and current step.
- [ ] Confirm no real anchor scientific execution is permitted; keep both status locations synchronized.
- [ ] Set `IN_REVIEW` before audits; record Audit 1, Audit 2, and Audit 3 independently.
- [ ] On any blocker, set both locations to `BLOCKED` immediately, recording exact cause, evidence, affected identity, and unblock condition.
- [ ] Set `DONE` only after all acceptance criteria, validation, audits, cleanup, and completion evidence pass; record synchronized final status and evidence.
- [ ] Verify final status consistency between this ticket and `docs/tickets/TICKET_STATUS.md`; a mismatch is a blocker.

### B2. Scientific and recovered-semantics locks

These are constraints, not authorization to run a campaign: this ticket may perform only the Part A work under the synthetic-only boundary. Work only in `ProtocolTrack.DATP_ANCHOR` and the separate anchor namespace. One authorized FedAvg AE is trained per seed, selected then frozen, and its compatible calibration/test score artifacts feed B1–B4; threshold-only changes never retrain or rescore. Calibration is benign-only; attacks are evaluation-only; no test data, attacks, thresholds, or downstream outcomes select a checkpoint. B0 is a separate centralized model and lineage, not a ladder policy or FedAvg substitute. B1 is shared, B2 local, B3 is an authorized-taxonomy family mean, and B4 is exact k-means++ on `[mean,std,skew,p95]` at canonical `K=3`.

For every recovered historical fact, record subject, read-only evidence location, roadmap rule, architecture representation, recovered behavior, justified difference, tests, uncertainty, and blocker state. Do not copy/import the reference repository, reuse its layout, claim “same as old DATP,” or guess. The roadmap wins scientific conflicts; the architecture wins technical conflicts. Preserve `ANCHOR_CHECKPOINT_PROTOCOL_UNRESOLVED` when applicable; never substitute the journal schedule for historical anchor evidence.

### B3. Synthetic-only boundary

Validate only with deterministic synthetic arrays/clients/labels, fakes, test doubles, property/contract/architecture tests, lineage simulations, dry-run planning, and isolated test storage. Read-only source/reference inspection is allowed only when this ticket explicitly permits it. Real N-BaIoT training/scoring/thresholds/metrics, reduced runs, real-data smoke/debug work, scientific outputs/checkpoints, invented seeds, and result-driven tuning are forbidden. Synthetic outputs never enter an anchor scientific namespace or masquerade as evidence.

### B4. Required repository-wide audits

After implementation, audit the complete repository—not merely the diff—for DATP scope; dependency direction; framework confinement; domain/application/config/composition/CLI boundaries; import cycles and side effects; type placement; named typed requests/results and ports; no service locator/universal context/generic utility; no Pydantic beyond config; typed lineage/artifact identities; atomic persistence; deterministic process isolation; and no aliases, redirects, shims, wrappers, or compatibility paths. An authority-grounded defect is not excused as pre-existing: fix it or record a decision blocker. Run configured import-linter and pytest-archon.

Scan for raw protocol strings/dictionaries, uncontrolled tracks/namespaces/policies/roles/stages, `dict[str, Any]`, broad `Any`/object, mutable scientific specifications, tuple pseudo-objects, Boolean variant flags, optional-field inference, missing `assert_never`, generic Request/Result/Payload/Context/Manager/Handler names, and scattered defaults. Search code/tests (planning Markdown exempt) for ticket IDs, ticket files, authority-document names/sections, and planning-language claims. Audit stale docs/paths/names/status claims, unsupported completion claims, AI/banner/decorative comments, copied architecture prose, and broken links.

Audit the entire repository explicitly: `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter configuration, Ruff configuration, Pyright/Pylance configuration, pytest configuration, Sonar configuration, provider adapters, and root metadata. Do not accept “pre-existing,” “outside the diff,” “another agent,” “legacy code,” or “untouched file” exemptions.

Run full Ruff lint/format, Pyright strict, Pylance parity when available, configured Sonar gate, applicable tests, complete suite, randomized order, mandatory Nox sessions, serialized CUDA where applicable, diff, untracked-file, and cleanup checks. Record unavailable tools honestly; do not weaken tests/types/exclusions to pass.

### B5. Determinism, CUDA, and artifacts

Verify stage-specific seed derivation, no seed-position identity, no CPU fallback, no automatic batch/chunk/precision/model/data reduction, batching-first processing, spawn-safe picklable boundary objects, serialized CUDA tests, terminal CUDA-OOM behavior, no configuration-changing retry, stage lineage, typed artifact identity, and atomic commits. Test benign/attack child artifacts are readable only as a committed aggregate; incompatible reuse is rejected or `RECOMPUTE`, never silently reused.

### B6. Three independent scientific-drift audits

- **Audit 1 — Roadmap fidelity.** Check fixed encoder, FedAvg/E=1/full participation, threshold-only B1–B4, benign calibration, attack isolation, CV(FPR)-primary/AUROC-control, B0 separation, B3 taxonomy, B4 fingerprint/K, anchor/journal and seed separation, no claim promotion, no real execution, and no guesses.
- **Audit 2 — Architecture fidelity.** Check layers, typed boundaries/configuration, lineage/reuse, persistence, determinism/CUDA/batching, namespace isolation, no raw dictionaries/hidden defaults, and no compatibility scaffolding.
- **Audit 3 — Adversarial anchor review.** Cross-check authorities, ticket/dependencies, implementation/tests/evidence/configuration/identities/status for leakage, source copying, fake equivalence, B1–B4 retraining, B0/FedAvg mixing, wrong B4, namespace mixing, real-data paths, vacuous tests, incomplete audits, or unsupported tool claims.
- [ ] Correct each authority-grounded finding and rerun all three audits; preserve separate evidence for each.

### B7. Completion evidence

Record a check-by-check completion ledger: changed files and why; verified dependencies; recovered behavior/evidence where applicable; implementation steps; exact commands/results; every required unit, property, contract, architecture, integration, CUDA, and synthetic end-to-end/system test (or the Part A reason it is not applicable); full-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; all three audit records; blockers; remaining risks; and skipped checks with exact reasons. A mandatory check cannot be skipped for green status, and no pre-existing-defect exemption is valid.

This ticket consumes Phase 0 governance and `P1-T070`; it must not recreate them. `P2-T020` is the Phase 2 gate and cannot pass until every mandatory Phase 2 responsibility reaches a valid terminal state through the authoritative graph.

### Completion evidence — 2026-07-15

- Dependencies verified: `P2-T007` `DONE`, `P1-T043` `DONE` (`docs/tickets/TICKET_STATUS.md:211`, fixed ReLU 80/40/20 AE + strict CUDA/DataLoader determinism), `P2-T001`/`P2-T002` `DONE` (recovered-semantics register, `docs/MASTER_TICKET_LOG.md` §N.1 rows 1/2/3/7).
- Key finding before writing any code: `P1-T043` already implements the fixed AE architecture, and it already matches the recovered anchor semantics **exactly**. `src/datp_core/infrastructure/learning/models/autoencoder.py:77-90` (`_validate_fixed_architecture`) hard-locks `hidden_dims=(80,40)`, `bottleneck_dim=20`, `activation=RELU` — structurally identical to register row 1 (`encoder_dims=[80,40,20]`, ReLU, no BatchNorm — `_network` never constructs a `BatchNorm*` module). `domain/learning/training.py` (`OptimizerType.ADAM`, `LrSchedulerType.NONE`, and the complete absence of any `weight_decay` field anywhere in `TrainingSpec`/`TrainingBatchSpec`) already structurally supports register rows 2/3 exactly (Adam, lr=0.001, no scheduler, no weight decay anywhere). `FederationSpec._validate_local_epochs`/`_validate_rounds_max` already hard-lock `local_epochs=1`/`rounds_max=200` — the domain-locked new-ladder policy, independent of and not a claim about the historical repository values (register row 7: repo-default/proven-executed `local_epochs=5`, published paper `E=1`). Even `tests/unit/domain/test_training_specifications.py:92-122` (pre-existing, from P1) already exercises `input_dim=115`, `hidden_dims=(80,40)`, `bottleneck_dim=20`, `OptimizerType.ADAM`, `lr=0.001`, `LrSchedulerType.NONE`, batch size 256 as its worked example — independently confirming these are the correct recovered anchor values, not a new guess made in this ticket.
- What was genuinely missing and implemented in this ticket: no infrastructure code anywhere constructed a real `torch.optim` optimizer from `OptimizerType`, and no single named, locked object represented "the anchor training spec" as a concrete, citable artifact (the correct values existed only implicitly, scattered across a domain unit test). Added `infrastructure/learning/models/nbaiot_anchor_training.py`:
  - `ANCHOR_AUTOENCODER_SPECIFICATION` — the locked `AutoencoderSpec(input_dim=115, hidden_dims=(80,40), bottleneck_dim=20, activation=RELU)`, citing register rows 1/2/3/7 in a code comment (not copied prose, a citation).
  - `anchor_training_spec(*, seed) -> TrainingSpec` — builds the complete locked anchor `TrainingSpec` (Adam/lr=0.001/no-scheduler/FedAvg/local_epochs=1/rounds_max=200/full participation/batch=256/`WHOLE_CLIENT`/`AFTER_GRADIENT_ACCUMULATION`/FP32/STRICT determinism/no personalization), reusing every existing Phase 1 domain type unchanged.
  - `is_authorized_anchor_training_policy`/`build_anchor_optimizer` — an explicit typed authorization gate (mirroring the `is_authorized_nbaiot_policy` pattern established in P2-T007) that raises `TrainingError` if anything ever tries to build an anchor optimizer from a non-recovered optimizer/scheduler/lr/architecture combination, then returns a real `torch.optim.Adam(model.parameters(), lr=training.lr)`.
  - `anchor_scheduler(*, training) -> None` — validates `scheduler is LrSchedulerType.NONE` (raising `TrainingError` otherwise) and returns `None`, making "no scheduler" an explicit, enforced decision rather than an implicit absence.
- No BatchNorm, no real training: `_network` (P1-T043, unchanged) only ever appends `nn.Linear`/activation modules; nothing in this ticket's new code constructs, imports, or references any `nn.BatchNorm*`/`nn.SyncBatchNorm` type. No N-BaIoT data, real client, or FedAvg round was read or executed; all coverage below uses synthetic tensors only.
- Required unit test (Part A item 25, "No BatchNorm"): `tests/unit/infrastructure/test_nbaiot_anchor_training.py`, 5 tests — anchor specification equals the recovered architecture and its built model contains no `BatchNorm1d/2d/3d`/`SyncBatchNorm` module; the anchor training spec carries `OptimizerType.ADAM`/`lr=0.001`/`LrSchedulerType.NONE`/`local_epochs=1`; the authorization policy accepts only the exact recovered combination (rejects `SGD`, rejects `COSINE` scheduler); `build_anchor_optimizer` raises `TrainingError` on an unauthorized optimizer and returns a real `torch.optim.Adam` with `lr=0.001` on the authorized one. All pass.
- Required integration test (Part A item 29, "Forward/backward runs deterministically (synthetic tiny model)"): `tests/integration/learning/test_nbaiot_anchor_training.py` — builds the anchor model + optimizer from two independent constructions with the same seed, runs one forward pass, one MSE-reconstruction backward pass, and one `optimizer.step()` on a synthetic `torch.randn(4, 115)` batch (`torch.Generator` seeded, not real data), and asserts the resulting post-step forward output is bit-identical across the two same-seed runs and differs for a different seed. Passes.
- Required CUDA test (Part A item 30, "Synthetic forward/backward deterministic (cuda lane)"): `tests/integration/cuda/test_nbaiot_anchor_training.py`, `@pytest.mark.cuda`, mirrors the CPU integration test on `device="cuda"` with `skip_if_cuda_unavailable()` guard (established pattern from `tests/support/cuda_lane.py`). CUDA hardware was actually available in this environment, so the test genuinely executed on GPU rather than being skipped: `pytest tests/integration/cuda/test_nbaiot_anchor_training.py -m cuda -q` → 1 passed.
- Validation commands run: `pytest tests/unit/infrastructure/test_nbaiot_anchor_training.py tests/integration/learning/test_nbaiot_anchor_training.py -q --no-cov` (6 passed); `pytest tests/integration/cuda/test_nbaiot_anchor_training.py -m cuda -q --no-cov` (1 passed, real CUDA); `pytest tests/architecture -q --no-cov` (45 passed, pytest-archon included); `python -m lint-imports --config importlinter.ini` (7 contracts kept, 0 broken); `ruff check`/`ruff format --check` (clean); `python -m pyright` on all four changed/added files (0 errors, 0 warnings). Full-suite/randomized-order run and Nox deferred to the next batched checkpoint (`P2-T010`), per the standing cadence instruction.
- Sonar/CodeScene: live dashboard sweep deferred to the `P2-T010` checkpoint. Local `cs check src/datp_core/infrastructure/learning/models/nbaiot_anchor_training.py` on this ticket's own new file: **10.00** on first pass (no iteration needed — the authorization-gate/factory-function pattern from P2-T007 was applied from the outset).
- Audit 1 (Roadmap fidelity): PASS — AE architecture/optimizer/scheduler exactly match the recovered evidence (register rows 1/2/3), not a guess; `local_epochs=1`/`rounds_max=200` are the domain-locked new-ladder policy, explicitly documented in code and evidence as *not* a claim of historical E/round reproduction; `ANCHOR_LOCAL_EPOCH_PROTOCOL_UNRESOLVED` remains open, unaffected, and is referenced directly in this ticket's evidence and in the new module's code comment (not silently dropped); no real training occurred.
- Audit 2 (Architecture fidelity): PASS — reused `FixedAutoencoder`/`build_fixed_autoencoder`/`AutoencoderSpec`/`TrainingSpec`/`FederationSpec`/`TrainingBatchSpec` unchanged from Phase 1 rather than duplicating; new code is infrastructure-layer only (no port added, since P2-T009 owns the `FederatedRoundExecutor` that will consume these factories); typed `TrainingError` used for the authorization gate, matching Part A item 35 exactly; import-linter confirms no boundary violation.
- Audit 3 (Adversarial anchor review): PASS — cross-checked the new module's literal values against both the recovered-semantics register (`docs/MASTER_TICKET_LOG.md` rows 1/2/3/7) and the independent, pre-existing P1 domain test (`test_training_specifications.py:92-122`) before writing any code, rather than asserting equivalence without checking; the `is_authorized_anchor_training_policy` gate makes a future accidental optimizer/scheduler substitution fail loudly (`TrainingError`) instead of silently drifting; no reference-repository content was copied; no scientific execution occurred.
- Blockers: NONE new. `ANCHOR_LOCAL_EPOCH_PROTOCOL_UNRESOLVED` (print-grade, from P2-T001/P2-T002) remains open and is explicitly referenced in this ticket's evidence and code comment; it does not block this ticket's completion (this ticket fits/locks the new-ladder `local_epochs=1` policy, which is independent of and does not resolve the historical E conflict). Carried forward to `P2-T009`/`P2-T010`/`P2-T020` per the standing instruction.
- Remaining risks: `build_anchor_optimizer`/`anchor_scheduler` are pure factory functions consumed directly by infrastructure code (no application port), since P2-T009 owns the concrete `FederatedRoundExecutor` that will call them per local training step; revisit if P2-T009 reveals a different intended wiring point.
- Cleanup: no scratch files; no real N-BaIoT data touched; only the four listed changed/added files plus this ticket's own evidence.

