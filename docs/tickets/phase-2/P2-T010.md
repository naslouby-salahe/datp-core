# P2-T010 — Implement the checkpoint schedule, persistence, and Regime-A global selection

Standalone Phase 2 implementation specification. It is independently actionable; the master log remains authoritative for canonical identity, phase membership, ordering, and dependency graph. This conversion creates documentation only and does not implement the ticket.

## Part A — Ticket template

1. **Ticket ID.** P2-T010
2. **Title.** Implement the checkpoint schedule, persistence, and Regime-A global selection
3. **Phase.** 2
4. **Status.** IN_REVIEW
5. **Priority.** P0 — Blocking
6. **Ticket type.** checkpoint
7. **Owner.** federated-learning-engineer
8. **Supporting agents.** reproducibility-auditor
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** ANCHOR
11. **Roadmap experiment IDs.** ANCHOR-B0–B4
12. **Architecture contracts/types owned.** `CheckpointSchedule`, `CheckpointSelectionSpec`, `CheckpointSelectionResult`
13. **Objective.** Implement the evidence-recovered historical anchor checkpoint persistence and selection protocol, validated on synthetic candidates. The journal {25,50,75,100,125,150,200} schedule is implemented only in the journal track and must not be imposed on anchor reproduction unless P2-T002 records authority evidence that they are identical.
14. **Why this ticket exists.** A historical reproduction cannot silently redefine its training/selection protocol as the journal protocol. One anchor-global selected round may be used only after the recovered schedule, ranking rule, and tie-break are evidenced.
15. **Authority references.** Arch §17.1, §9.1, §29.1/§29.4; Road §10 (checkpoint protocol).
16. **Dependencies.** P2-T009, P2-T002, P1-T022.
17. **Blocks.** P2-T011.
18. **Downstream consumers.** P2-T011 consumes the selected checkpoint for anchor scoring.
19. **Preconditions.** P2-T009, P2-T002 (anchor checkpoint protocol resolved), P1-T022 complete.
20. **Inputs.** Arch §17.1, §9.1, §29.1/§29.4; Road §10.
21. **Allowed scope.** `application/stages/select_checkpoint.py` (anchor), checkpoint persistence.
22. **Forbidden actions.** No guessed anchor rounds; no silent journal-schedule substitution; no test AUROC/attack/Regime-D/FedProx/personalization/threshold in selection; no per-regime selection; no real run.
23. **Required configuration or NONE.** Once evidence-recovered (never before — an unresolved schedule, ranking rule, or tie-break is the `ANCHOR_CHECKPOINT_PROTOCOL_UNRESOLVED` blocker, not a schema default to guess), the anchor round schedule and `CheckpointSelectionSpec` rule/tie-break versions are genuine scientific choices (recovered historical facts, not the journal {25,50,75,100,125,150,200} schedule) and must be `Literal`-typed fields on a `CheckpointSchedule`/`CheckpointSelectionSpec` schema in `config/schemas/scientific.py`, mapped via a pure `map_*` function in `config/mapping/scientific.py`, and backed by a `configs/protocols/` YAML file — following the `RegimeAStaticSplitConfig`/`configs/protocols/regime_a_static_split.yaml` precedent, never a bare Python module constant.
24. **Implementation responsibilities.** Persist only evidence-recovered anchor checkpoints; selection over eligible anchor candidates with prohibited-input attestation; deterministic evidence-recovered tie-break.
25. **Required unit tests.** NONE.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE.
29. **Required integration tests.** Selection uses Regime-A evidence only; prohibited input rejected; deterministic selected round; selection-artifact shape fixed-input regression (learning, synthetic).
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Integration (learning) lanes.
33. **Acceptance criteria.** [ ] Historical anchor schedule/selection evidence is recorded. [ ] No journal schedule is assumed. [ ] Anchor-global selection only. [ ] Prohibited-input attestation present. [ ] Deterministic evidence-recovered selected round. [ ] `CheckpointSchedule`/`CheckpointSelectionSpec` are represented via a `config/schemas/scientific.py` schema, a `config/mapping/scientific.py` mapper, and a `configs/protocols/` YAML file — never a bare Python constant.
34. **Completion evidence.** Changed files (select_checkpoint.py, checkpoint persistence); commands and tests run with results; selection identity; blocker state; cleanup confirmation; skipped checks/reasons.
35. **Failure and blocker behavior.** `CheckpointSelectionError`. Stop if anchor schedule, ranking, or tie-break is unresolved; preserve `ANCHOR_CHECKPOINT_PROTOCOL_UNRESOLVED` as a readiness blocker that prevents anchor training, checkpoint selection, campaign readiness, and execution.
36. **Stop conditions.** Stop if anchor schedule, ranking, or tie-break is unresolved; preserve `ANCHOR_CHECKPOINT_PROTOCOL_UNRESOLVED` as a readiness blocker that prevents anchor training, checkpoint selection, campaign readiness, and execution.
37. **Deliverables.** Checkpoint persistence + selector.
38. **Final review checklist.** [ ] No leakage into selection.

## Part B — Mandatory Phase 2 lifecycle, boundaries, and audits

### B1. Lifecycle checklist

- [ ] Immediately before implementation, read the complete roadmap, complete architecture, this ticket, the Phase 2 index/gate, `docs/tickets/TICKET_STATUS.md`, each direct dependency, and cited authority sections.
- [ ] Verify each dependency, including `P1-T070` where required, is `DONE`.
- [ ] Set this ticket and the corresponding status-register row to `IN_PROGRESS` in the same change; record start time and current step.
- [ ] Confirm no real anchor scientific execution is permitted; keep both status locations synchronized.
- [ ] Set `IN_REVIEW` before audits; record Audit 1, Audit 2, and Audit 3 independently.
- [ ] On any blocker, set both locations to `BLOCKED` immediately, recording exact cause, evidence, affected identity, and unblock condition.
- [ ] Set `DONE` only after all acceptance criteria, validation, audits, cleanup, and completion evidence pass; record synchronized final status and evidence.
- [ ] Verify final status consistency between this ticket and `docs/tickets/TICKET_STATUS.md`; a mismatch is a blocker.

### B2. Scientific and recovered-semantics locks

These are constraints, not authorization to run a campaign: this ticket may perform only the Part A work under the synthetic-only boundary. Work only in `ProtocolTrack.DATP_ANCHOR` and the separate anchor namespace. One authorized FedAvg AE is trained per seed, selected then frozen, and its compatible calibration/test score artifacts feed B1–B4; threshold-only changes never retrain or rescore. Calibration is benign-only; attacks are evaluation-only; no test data, attacks, thresholds, or downstream outcomes select a checkpoint. B0 is a separate centralized model and lineage, not a ladder policy or FedAvg substitute. B1 is shared, B2 local, B3 is an authorized-taxonomy family mean, and B4 is exact k-means++ on `[mean,std,skew,p95]` at canonical `K=3`.

For every recovered historical fact, record subject, read-only evidence location, roadmap rule, architecture representation, recovered behavior, justified difference, tests, uncertainty, and blocker state. Do not copy/import the reference repository, reuse its layout, claim “same as old DATP,” or guess. The roadmap wins scientific conflicts; the architecture wins technical conflicts. Preserve `ANCHOR_CHECKPOINT_PROTOCOL_UNRESOLVED` when applicable; never substitute the journal schedule for historical anchor evidence.

### B3. Synthetic-only boundary

Validate only with deterministic synthetic arrays/clients/labels, fakes, test doubles, property/contract/architecture tests, lineage simulations, dry-run planning, and isolated test storage. Read-only source/reference inspection is allowed only when this ticket explicitly permits it. Real N-BaIoT training/scoring/thresholds/metrics, reduced runs, real-data smoke/debug work, scientific outputs/checkpoints, invented seeds, and result-driven tuning are forbidden. Synthetic outputs never enter an anchor scientific namespace or masquerade as evidence.

### B4. Required repository-wide audits

After implementation, audit the complete repository—not merely the diff—for DATP scope; dependency direction; framework confinement; domain/application/config/composition/CLI boundaries; import cycles and side effects; type placement; named typed requests/results and ports; no service locator/universal context/generic utility; no Pydantic beyond config; typed lineage/artifact identities; atomic persistence; deterministic process isolation; and no aliases, redirects, shims, wrappers, or compatibility paths. An authority-grounded defect is not excused as pre-existing: fix it or record a decision blocker. Run configured import-linter and pytest-archon.

Scan for raw protocol strings/dictionaries, uncontrolled tracks/namespaces/policies/roles/stages, `dict[str, Any]`, broad `Any`/object, mutable scientific specifications, tuple pseudo-objects, Boolean variant flags, optional-field inference, missing `assert_never`, generic Request/Result/Payload/Context/Manager/Handler names, and scattered defaults. Search code/tests (planning Markdown exempt) for ticket IDs, ticket files, authority-document names/sections, and planning-language claims. Audit stale docs/paths/names/status claims, unsupported completion claims, AI/banner/decorative comments, copied architecture prose, and broken links.

Audit the entire repository explicitly: `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter configuration, Ruff configuration, Pyright/Pylance configuration, pytest configuration, Sonar configuration, provider adapters, and root metadata. Do not accept “pre-existing,” “outside the diff,” “another agent,” “legacy code,” or “untouched file” exemptions.

Run full Ruff lint/format, Pyright strict, Pylance parity when available, configured Sonar gate, applicable tests, complete suite, randomized order, mandatory Nox sessions, serialized CUDA where applicable, diff, untracked-file, and cleanup checks. Record unavailable tools honestly; do not weaken tests/types/exclusions to pass.

### B5. Determinism, CUDA, and artifacts

Verify stage-specific seed derivation, no seed-position identity, no CPU fallback, no automatic batch/chunk/precision/model/data reduction, batching-first processing, spawn-safe picklable boundary objects, serialized CUDA tests, terminal CUDA-OOM behavior, no configuration-changing retry, stage lineage, typed artifact identity, and atomic commits. Test benign/attack child artifacts are readable only as a committed aggregate; incompatible reuse is rejected or `RECOMPUTE`, never silently reused.

### B6. Three independent scientific-drift audits

- **Audit 1 — Roadmap fidelity.** Check fixed encoder, FedAvg/E=1/full participation, threshold-only B1–B4, benign calibration, attack isolation, CV(FPR)-primary/AUROC-control, B0 separation, B3 taxonomy, B4 fingerprint/K, anchor/journal and seed separation, no claim promotion, no real execution, and no guesses.
- **Audit 2 — Architecture fidelity.** Check layers, typed boundaries/configuration, lineage/reuse, persistence, determinism/CUDA/batching, namespace isolation, no raw dictionaries/hidden defaults, and no compatibility scaffolding.
- **Audit 3 — Adversarial anchor review.** Cross-check authorities, ticket/dependencies, implementation/tests/evidence/configuration/identities/status for leakage, source copying, fake equivalence, B1–B4 retraining, B0/FedAvg mixing, wrong B4, namespace mixing, real-data paths, vacuous tests, incomplete audits, or unsupported tool claims.
- [ ] Correct each authority-grounded finding and rerun all three audits; preserve separate evidence for each.

### B7. Completion evidence

Record a check-by-check completion ledger: changed files and why; verified dependencies; recovered behavior/evidence where applicable; implementation steps; exact commands/results; every required unit, property, contract, architecture, integration, CUDA, and synthetic end-to-end/system test (or the Part A reason it is not applicable); full-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; all three audit records; blockers; remaining risks; and skipped checks with exact reasons. A mandatory check cannot be skipped for green status, and no pre-existing-defect exemption is valid.

This ticket consumes Phase 0 governance and `P1-T070`; it must not recreate them. `P2-T020` is the Phase 2 gate and cannot pass until every mandatory Phase 2 responsibility reaches a valid terminal state through the authoritative graph.

## Completion evidence — 2026-07-16T16:10:00Z

- Lifecycle and dependencies: confirmed `P2-T009`, `P2-T002`, and `P1-T022` are `DONE` before starting. Re-read `docs/MASTER_TICKET_LOG.md` §N.2 (`AnchorHistoricalCheckpointProtocol`, accepted provisionally, RESOLVED — not a blocker) and §N.2b (`AnchorLocalEpochProtocol`, UNRESOLVED, print-grade, unaffected by this ticket). N.2 supersedes this ticket's original Part A prose (written before the recovery pass), which described a candidate-pool/ranking/tie-break shape; N.2 is authoritative and was implemented instead: a single, unscheduled termination checkpoint, no candidate pool, no ranking metric, no tie-break.
- Implementation: added a discriminated checkpoint protocol rather than reusing the journal's scheduled/ranked shape. `domain/learning/checkpoints.py` gained `CheckpointProtocol` (`JOURNAL_SCHEDULED`/`ANCHOR_TERMINATION`), `AnchorTerminationReason`, `AnchorCheckpointTerminationPolicy` (locked `rounds_initial=40`/`rounds_max=150`, distinct from the journal's `{25,...,200}` `CheckpointSchedule`), `AnchorTerminationCheckpointResult`, and `AnchorCheckpointSelectionArtifact`; `CheckpointDescriptor` gained a required `protocol` field with protocol-conditional round validation (journal: locked schedule membership; anchor: any round in `[1, 150]`), so a real anchor terminal round (e.g. 118, not a schedule member) can be persisted through the existing generic `CheckpointStore`/`FileCheckpointStore` without weakening the journal's schedule invariant. `application/stages/select_checkpoint.py` gained `AnchorCheckpointTerminationEvidence`/`AnchorCheckpointTerminationRequest`/`AnchorCheckpointSelector`, which validates a caller-supplied terminal round/reason against the locked policy (no ranking — the sole reported round is the sole persisted checkpoint) and always produces `prohibited_input_attestation=True` by construction (the evidence type has no attack/test/threshold field, mirroring the journal selector's own attestation pattern). The journal `CheckpointSchedule`/`CheckpointSelectionSpec`/`CheckpointSelectionResult`/`CheckpointSelector` are untouched and remain the sole journal mechanism.
- Configuration: added `AnchorCheckpointTerminationConfig` (`config/schemas/scientific.py`, `Literal[40]`/`Literal[150]`) and `map_anchor_checkpoint_termination_config` (`config/mapping/scientific.py`), backed by `configs/protocols/anchor_checkpoint_termination.yaml`, following the `RegimeAStaticSplitConfig`/`configs/protocols/regime_a_static_split.yaml` precedent exactly (including the same three-test proof pattern: schema rejects a non-locked value, mapping produces the locked spec, the tracked YAML file itself loads and maps to the locked spec).
- Pre-existing defect fixed in this ticket's implementation path: `infrastructure/learning/models/nbaiot_anchor_training.py::anchor_training_spec()` hardcoded `FederationSpec(rounds_max=200, ...)` for the anchor track — the journal's value, contradicting `N.2`'s recovered anchor `rounds_max=150`. `FederationSpec._validate_rounds_max` (`domain/learning/training.py`) was widened from a single hardcoded `200` to the two locked recovered values `(150, 200)` (`LOCKED_ROUNDS_MAX_VALUES`, sourced from `ANCHOR_CHECKPOINT_ROUNDS_MAX` and `SCHEDULED_CHECKPOINT_ROUNDS[-1]` rather than a duplicated magic number), and the anchor spec now passes `ANCHOR_CHECKPOINT_ROUNDS_MAX` instead of a bare `200`. No test asserted the old value, so no test required weakening.
- Remaining, honestly-recorded gap (not fixed here, out of this ticket's narrow allowed scope of `select_checkpoint.py` (anchor) and checkpoint persistence): `FederationSpec.rounds_max` itself still carries no track/protocol discriminator, so nothing at that layer alone prevents constructing an anchor `FederationSpec` with `200` or a journal one with `150` — the two tracks' actual termination boundaries are enforced correctly and independently (`CheckpointDescriptor.protocol`-conditional round validation for persistence; `AnchorCheckpointTerminationPolicy`/`AnchorCheckpointSelector` for anchor's real boundary), but `FederationSpec.rounds_max` is not itself track-aware. A future ticket should either add a track discriminator to `FederationSpec` or fold `rounds_max` out of it entirely in favor of the two protocol-specific policies.
- Tests: 81 focused tests passed (`tests/unit/domain/test_checkpoint_specifications.py`, `tests/unit/config/test_mapping.py`, `tests/integration/test_checkpoint_selection_shape.py`, `tests/integration/persistence/test_checkpoint_store.py`, `tests/integration/cuda/test_checkpoint_store.py`, `tests/unit/infrastructure/test_checkpoint_store.py`, `tests/unit/domain/test_training_specifications.py`, `tests/unit/domain/test_protocol_aggregates.py`, `tests/unit/application/test_stage_services.py`, `tests/integration/learning/test_federated_trainer_lifecycle.py`, `tests/integration/learning/test_anchor_federated_round_executor.py`, `tests/integration/cuda/test_anchor_federated_round_executor.py`, `tests/integration/learning/test_nbaiot_anchor_training.py`, `tests/unit/infrastructure/test_nbaiot_anchor_training.py`). New tests cover: anchor policy round-lock rejection, false-attestation rejection, an anchor descriptor accepting an unscheduled round (118) and rejecting one beyond the locked cap, anchor selection-artifact shape stability, round-budget-exhaustion accepted only exactly at the cap, convergence rejected before `rounds_initial`, terminal round rejected beyond `rounds_max`, the config schema/mapping/YAML three-test proof pattern, and an anchor `CheckpointDescriptor` (round 118, protocol `ANCHOR_TERMINATION`) round-tripping through the existing `FileCheckpointStore` unchanged.
- Quality gates run for this ticket: Ruff lint and format (clean on all changed files), Pyright strict (0 errors/warnings on all changed source files), import-linter (7/7 contracts kept, no layer violation), `tests/architecture/` (45/45 passed, pytest-archon-backed). Full-project Ruff/Pyright/Nox/Sonar/CodeScene were not run as part of this individual ticket per the batched-checkpoint process; they run once at Checkpoint 2 closure (this is the checkpoint's fifth and final ticket).
- Audit 1 — roadmap fidelity: fixed encoder, FedAvg/E=1/full participation untouched; B1–B4, B0, calibration, scoring, CV(FPR)/AUROC, taxonomy untouched. Anchor and journal checkpoint protocols kept explicitly distinct with no invented schedule, ranking, or tie-break for anchor. `ANCHOR_LOCAL_EPOCH_PROTOCOL_UNRESOLVED` and `ANCHOR_CHECKPOINT_PROTOCOL_UNRESOLVED` (now `N.2`-resolved) are unaffected/correctly reflected. No real execution; all validation synthetic.
- Audit 2 — architecture fidelity: new types placed in their correct layers (domain/application/config); YAML→schema→mapper→frozen-spec path followed exactly; no raw dictionaries, no new hidden Python-only default beyond the same locked-constant-with-defensive-validation pattern already used for the journal schedule; existing generic `CheckpointStore` persistence reused without modification; no compatibility shim, alias, or redirect added.
- Audit 3 — adversarial anchor review: tested and rejected — a non-locked policy value, a forged `False` attestation, a descriptor round beyond the anchor cap, round-budget-exhaustion claimed off-cap, convergence claimed before `rounds_initial`, and a terminal round beyond `rounds_max`. Confirmed the anchor evidence/request types carry no attack/test/threshold field (structural prohibited-input proof, mirroring the journal selector's own pattern) and that no candidate pool or ranking exists anywhere in the anchor path. No code or layout was read from or copied out of the reference repository in this session.
- Cleanup: no scratch files created; no ticket IDs or planning-document references introduced into source/test code (only this Markdown ticket file and commit message name the ticket).
- Blocker state: `ANCHOR_LOCAL_EPOCH_PROTOCOL_UNRESOLVED` remains open, unaffected, and is carried to `P2-T020` as before. `ANCHOR_CHECKPOINT_PROTOCOL_UNRESOLVED` does not apply — `N.2` resolved the checkpoint protocol question provisionally, and this ticket implements that resolution.
- Skipped checks and reasons: full-project Ruff/Pyright/import-linter/pytest-archon/Nox/coverage/randomized-order suite, Sonar, and CodeScene were deferred to Checkpoint 2 closure (this ticket is the checkpoint's final ticket; see the checkpoint-2 closure evidence for those results) — not skipped as unavailable, deferred by the batched-checkpoint process itself.

