# P2-T023 — Implement B0 pooled threshold, evaluation, statistics, and reporting route

Standalone Phase 2 implementation specification. It is independently actionable; the master log remains authoritative for canonical identity, phase membership, ordering, and dependency graph. This conversion creates documentation only and does not implement the ticket.

## Part A — Ticket template

1. **Ticket ID.** P2-T023
2. **Title.** Implement B0 pooled threshold, evaluation, statistics, and reporting route
3. **Phase.** 2
4. **Status.** IN_PROGRESS
5. **Priority.** P1-Mandatory
6. **Ticket type.** evaluation
7. **Owner.** threshold-policy-engineer
8. **Supporting agents.** statistics-auditor, report-output-auditor
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** ANCHOR
11. **Roadmap experiment IDs.** — (ANCHOR-B0; not a formal journal experiment ID)
12. **Architecture contracts/types owned.** `ThresholdConstructionSpec`, `PolicyEvaluationResult`, `StatisticalAnalysisResult`, `ReportIdentity`
13. **Objective.** Apply B0 pooled calibration threshold and route centralized results through evaluation, statistics, and reporting.
14. **Why this ticket exists.** B0 is a non-ladder centralized reference with its own complete evidence path.
15. **Authority references.** Road §4, §9; Arch §9.2–§9.4, §22.
16. **Dependencies.** P2-T022, P1-T026, P1-T049.
17. **Blocks.** P2-T016, P2-T019.
18. **Downstream consumers.** P3-T007, P5-T006, P6-T002, P7-T004, P7-T009.
19. **Preconditions.** B0 centralized calibration and held-out score bundles are lineage-complete.
20. **Inputs.** Centralized B0 score identities and pooled threshold configuration.
21. **Allowed scope.** Synthetic threshold/evaluation/reporting tests.
22. **Forbidden actions.** No B0 placement in B1–B4; no FedAvg identity reuse; no real campaign execution.
23. **Required configuration.** B0 pooled p95 threshold configuration: the p95 percentile choice is a genuine locked scientific value — represent it as a `Literal`-typed field (e.g. `PooledThresholdPercentile: Literal[95]`) on a B0 threshold config schema in `config/schemas/scientific.py`, mapped via a pure `map_*` function in `config/mapping/scientific.py`, and backed by a real YAML file under `configs/protocols/` (e.g. `configs/protocols/b0_pooled_threshold.yaml`). The B0 evaluation/statistics/reporting route — i.e. that B0 is evaluated on centralized held-out scores and reported/labeled outside the B1–B4 causal ladder — is a protocol-level routing rule, not a tunable value, and remains a code-level constant/rule with no YAML override, citing Road §4, §9 and Arch §9.2–§9.4, §22 as the authority establishing it as non-overridable.
24. **Implementation responsibilities.** Construct B0 from centralized calibration, evaluate on central held-out scores, and label B0 outside the causal ladder.
25. **Required unit tests.** B0 is not accepted as a B1–B4 policy.
26. **Required property tests.** Threshold identity changes with calibration lineage.
27. **Required contract tests.** Evaluation/report models retain centralized lineage.
28. **Required architecture tests.** Reporting stays framework-free.
29. **Required integration tests.** Synthetic complete B0 route.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** Synthetic anchor smoke path.
32. **Validation commands.** Focused unit/property/contract/integration/system lanes.
33. **Acceptance criteria.** [ ] B0 has threshold, evaluation, statistics, and report coverage. [ ] B0 is outside B1–B4. [ ] B0 pooled-p95 threshold percentile is YAML-backed via a config schema field and mapper under `configs/protocols/`; the B0 evaluation/statistics/reporting route (outside B1–B4) cites Road §4/§9 and Arch §9.2–§9.4/§22 as locked-invariant authority.
34. **Completion evidence.** Changed files; types/contracts; commands/tests/results; generated identity schema; cleanup; blockers; skipped checks/reasons; no unrelated modifications.
35. **Failure and blocker behavior.** Preserve any invalid artifact evidence; correct only diagnosed technical invalidity.
36. **Stop conditions.** Centralized lineage gap.
37. **Deliverables.** Complete B0 anchor route.
38. **Final review checklist.** [ ] B0 branch is distinct end-to-end.

## Part B — Mandatory Phase 2 lifecycle, boundaries, and audits

### B1. Lifecycle checklist

- [ ] Immediately before implementation, read the complete roadmap, complete architecture, this ticket, the Phase 2 index/gate, `docs/tickets/TICKET_STATUS.md`, each direct dependency, and cited authority sections.
- [ ] Verify each dependency, including `P1-T070` where required, is `DONE`.
- [ ] Set this ticket and the corresponding status-register row to `IN_PROGRESS` in the same change; record start time and current step.
- [ ] Confirm no real anchor scientific execution is permitted; keep both status locations synchronized.
- [ ] Set `IN_REVIEW` before audits; record Audit 1, Audit 2, and Audit 3 independently.
- [ ] On any blocker, set both locations to `BLOCKED` immediately, recording exact cause, evidence, affected identity, and unblock condition.
- [ ] Set `DONE` only after all acceptance criteria, validation, audits, cleanup, and completion evidence pass; record synchronized final status and evidence.
- [ ] Verify final status consistency between this ticket and `docs/tickets/TICKET_STATUS.md`; a mismatch is a blocker.

### B2. Scientific and recovered-semantics locks

These are constraints, not authorization to run a campaign: this ticket may perform only the Part A work under the synthetic-only boundary. Work only in `ProtocolTrack.DATP_ANCHOR` and the separate anchor namespace. One authorized FedAvg AE is trained per seed, selected then frozen, and its compatible calibration/test score artifacts feed B1–B4; threshold-only changes never retrain or rescore. Calibration is benign-only; attacks are evaluation-only; no test data, attacks, thresholds, or downstream outcomes select a checkpoint. B0 is a separate centralized model and lineage, not a ladder policy or FedAvg substitute. B1 is shared, B2 local, B3 is an authorized-taxonomy family mean, and B4 is exact k-means++ on `[mean,std,skew,p95]` at canonical `K=3`.

For every recovered historical fact, record subject, read-only evidence location, roadmap rule, architecture representation, recovered behavior, justified difference, tests, uncertainty, and blocker state. Do not copy/import the reference repository, reuse its layout, claim “same as old DATP,” or guess. The roadmap wins scientific conflicts; the architecture wins technical conflicts. Preserve `ANCHOR_CHECKPOINT_PROTOCOL_UNRESOLVED` when applicable; never substitute the journal schedule for historical anchor evidence.

### B3. Synthetic-only boundary

Validate only with deterministic synthetic arrays/clients/labels, fakes, test doubles, property/contract/architecture tests, lineage simulations, dry-run planning, and isolated test storage. Read-only source/reference inspection is allowed only when this ticket explicitly permits it. Real N-BaIoT training/scoring/thresholds/metrics, reduced runs, real-data smoke/debug work, scientific outputs/checkpoints, invented seeds, and result-driven tuning are forbidden. Synthetic outputs never enter an anchor scientific namespace or masquerade as evidence.

### B4. Required repository-wide audits

After implementation, audit the complete repository—not merely the diff—for DATP scope; dependency direction; framework confinement; domain/application/config/composition/CLI boundaries; import cycles and side effects; type placement; named typed requests/results and ports; no service locator/universal context/generic utility; no Pydantic beyond config; typed lineage/artifact identities; atomic persistence; deterministic process isolation; and no aliases, redirects, shims, wrappers, or compatibility paths. An authority-grounded defect is not excused as pre-existing: fix it or record a decision blocker. Run configured import-linter and pytest-archon.

Scan for raw protocol strings/dictionaries, uncontrolled tracks/namespaces/policies/roles/stages, `dict[str, Any]`, broad `Any`/object, mutable scientific specifications, tuple pseudo-objects, Boolean variant flags, optional-field inference, missing `assert_never`, generic Request/Result/Payload/Context/Manager/Handler names, and scattered defaults. Search code/tests (planning Markdown exempt) for ticket IDs, ticket files, authority-document names/sections, and planning-language claims. Audit stale docs/paths/names/status claims, unsupported completion claims, AI/banner/decorative comments, copied architecture prose, and broken links.

Audit the entire repository explicitly: `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter configuration, Ruff configuration, Pyright/Pylance configuration, pytest configuration, Sonar configuration, provider adapters, and root metadata. Do not accept “pre-existing,” “outside the diff,” “another agent,” “legacy code,” or “untouched file” exemptions.

Run full Ruff lint/format, Pyright strict, Pylance parity when available, configured Sonar gate, applicable tests, complete suite, randomized order, mandatory Nox sessions, serialized CUDA where applicable, diff, untracked-file, and cleanup checks. Record unavailable tools honestly; do not weaken tests/types/exclusions to pass.

### B5. Determinism, CUDA, and artifacts

Verify stage-specific seed derivation, no seed-position identity, no CPU fallback, no automatic batch/chunk/precision/model/data reduction, batching-first processing, spawn-safe picklable boundary objects, serialized CUDA tests, terminal CUDA-OOM behavior, no configuration-changing retry, stage lineage, typed artifact identity, and atomic commits. Test benign/attack child artifacts are readable only as a committed aggregate; incompatible reuse is rejected or `RECOMPUTE`, never silently reused.

### B6. Three independent scientific-drift audits

- **Audit 1 — Roadmap fidelity.** Check fixed encoder, FedAvg/E=1/full participation, threshold-only B1–B4, benign calibration, attack isolation, CV(FPR)-primary/AUROC-control, B0 separation, B3 taxonomy, B4 fingerprint/K, anchor/journal and seed separation, no claim promotion, no real execution, and no guesses.
- **Audit 2 — Architecture fidelity.** Check layers, typed boundaries/configuration, lineage/reuse, persistence, determinism/CUDA/batching, namespace isolation, no raw dictionaries/hidden defaults, and no compatibility scaffolding.
- **Audit 3 — Adversarial anchor review.** Cross-check authorities, ticket/dependencies, implementation/tests/evidence/configuration/identities/status for leakage, source copying, fake equivalence, B1–B4 retraining, B0/FedAvg mixing, wrong B4, namespace mixing, real-data paths, vacuous tests, incomplete audits, or unsupported tool claims.
- [ ] Correct each authority-grounded finding and rerun all three audits; preserve separate evidence for each.

### B7. Completion evidence

Record a check-by-check completion ledger: changed files and why; verified dependencies; recovered behavior/evidence where applicable; implementation steps; exact commands/results; every required unit, property, contract, architecture, integration, CUDA, and synthetic end-to-end/system test (or the Part A reason it is not applicable); full-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; all three audit records; blockers; remaining risks; and skipped checks with exact reasons. A mandatory check cannot be skipped for green status, and no pre-existing-defect exemption is valid.

This ticket consumes Phase 0 governance and `P1-T070`; it must not recreate them. `P2-T020` is the Phase 2 gate and cannot pass until every mandatory Phase 2 responsibility reaches a valid terminal state through the authoritative graph.

## Progress evidence — 2026-07-16T18:35:00Z (ticket remains IN_PROGRESS, not DONE or IN_REVIEW)

- Lifecycle and dependencies: confirmed `P2-T022`, `P1-T026`, and `P1-T049` are `DONE` before starting.
- Implemented and fully tested: the B0 pooled-p95 threshold, the one concretely-specified, self-contained requirement in this ticket's own "Required configuration" clause. `domain/thresholding/policies.py` gained `B0PooledThresholdSpec` (locked to `percentile == 0.95`, structurally **excluded** from the `ThresholdConstructionSpec` union — confirmed by the existing `test_threshold_construction_union_has_exactly_nine_tagged_members` pin, which this change does not touch) and `compute_b0_pooled_threshold()`, reusing the existing `exact_quantile` primitive (same exact-computation guarantee as the B1/B2 threshold paths, not reimplemented). Added `B0PooledThresholdConfig` (`config/schemas/scientific.py`, `Literal[95]`) and `map_b0_pooled_threshold_config` (`config/mapping/scientific.py`), backed by `configs/protocols/b0_pooled_threshold.yaml`, following the established YAML→schema→mapper→frozen-spec precedent exactly. 8 new tests (2 domain: locked/excluded-from-union, exact p95 computation on a known set; 3 config: schema rejection/mapping/YAML-load; plus the pre-existing union-membership and no-B0-surface tests re-verified unchanged) all pass.
- **Not implemented in this session — recorded honestly, not fabricated:** evaluation, statistics, and reporting coverage for B0. Investigation found substantial pre-existing infrastructure for these (`domain/evaluation/operating_points.py::PolicyEvaluationResult`, `application/stages/evaluate_policy.py` — a 364-line per-client confusion-matrix/fleet-dispersion/fleet-equity pipeline, `infrastructure/statistics/scipy_adapter.py`, `infrastructure/reporting/{markdown,matplotlib,latex}.py`), contradicting an initial assumption that no such machinery existed yet. However, `evaluate_policy`'s pipeline is built around **per-client threshold assignment** (`ClientEvaluationInputs`, per-client `ThresholdAssignmentSet`), and correctly adapting it for B0's **single pooled threshold applied uniformly** requires careful study of that pipeline's exact contract that this session's remaining time did not allow doing safely. Rather than force-fit an under-verified integration or fabricate placeholder evaluation/statistics/report code, this is recorded as genuine unfinished scope, not a false completion claim.
- Quality gates run for the completed portion: Ruff lint and format (clean), Pyright strict (0 errors on all changed files), import-linter (7/7 contracts kept), `tests/architecture/` (45/45 passed), broader regression across `tests/unit/domain/`, `tests/unit/config/`, `tests/integration/learning/`, `tests/unit/application/` (312 passed).
- Acceptance criteria status: "[x] B0 pooled-p95 threshold percentile is YAML-backed via a config schema field and mapper under `configs/protocols/`" — met. "[ ] B0 has threshold, evaluation, statistics, and report coverage" — **partially met (threshold only)**. "[ ] B0 is outside B1–B4" — met (structural exclusion from `ThresholdConstructionSpec`, verified by test). This ticket cannot be marked `IN_REVIEW` or `DONE` while evaluation/statistics/reporting coverage remains unimplemented; it stays `IN_PROGRESS`.
- Because this is the fifth and final ticket of Checkpoint 3, **Checkpoint 3 cannot close** until this ticket's remaining scope (or an explicit, evidence-based decision to descope it) is resolved. This is recorded truthfully rather than closing the checkpoint on a false completion claim.

## Architecture-fidelity correction — 2026-07-16T19:05:00Z

The user directed a deeper read of the roadmap and architecture documents before continuing. That read (Roadmap §4; Architecture §9.2, §29.1, §29.4) confirmed the B0 pooled-p95 percentile as correct (not guessed), but found two real defects in the P2-T021/P2-T022 implementations, both now fixed:

1. **Architecture invariant #29.4.79** ("Checkpoint-selection logic executing inside `infrastructure/persistence` rather than `application/stages/select_checkpoint.py`" is an explicitly forbidden invalid state) — `select_centralized_checkpoint()` had been placed in `infrastructure/learning/centralized/nbaiot_b0_checkpoint.py` (P2-T021). Moved to `application/stages/select_checkpoint.py`, beside the existing `AnchorCheckpointSelector`.
2. **Architecture §9.2 / invariant #29.4.80** — B0 "is its own `ExperimentSpec` executed through the same reusable stages as every other cell... so the pipeline code path is identical in shape to B1–B4," and a mega-interface bypassing that stage sequence is an explicitly forbidden invalid state. The P2-T022 `B0CalibrationScoreGenerationWorkflow`/`B0TestScoreGenerationWorkflow` were correctly typed but callable only ad hoc (`generate(*, batch_size=...)`), not through `application/stages/generate_scores.py`, mirroring how `train_model.py` already dispatches `FederatedTrainingStageRequest`/`CentralizedTrainingStageRequest`. Fixed by:
   - Adding `GenerateCentralizedCalibrationScoresRequest`/`CentralizedCalibrationScoreGenerationResult`/`GenerateCentralizedTestScoresRequest`/`CentralizedTestScoreGenerationResult`/`CentralizedScoreGenerator` to `application/ports/scoring.py`, and `generate_centralized_calibration_scores`/`generate_centralized_test_scores` stage functions to `application/stages/generate_scores.py`.
   - Refactoring `B0CalibrationScoreGenerationWorkflow.generate_calibration_scores(request)` / `B0TestScoreGenerationWorkflow.generate_test_scores(request)` to the request-driven port shape (checkpoint/split identities now come from the request, not constructor fields; only `model`/`device`/batches remain constructor-injected, mirroring `AnchorFederatedRoundExecutor`'s precedent for infra-internal data).
   - Adding the equivalent threshold-side port (`ConstructCentralizedThresholdRequest`/`CentralizedThresholdConstructionResult`/`CentralizedThresholdConstructor` in `application/ports/thresholding.py`, `construct_centralized_threshold` stage function in `application/stages/construct_thresholds.py`) and a new domain type `CentralizedThresholdAssignment` (`domain/thresholding/policies.py`) — "B0 uses a separate centralized evaluation assignment/result contract" per Architecture §9.2's `ThresholdAssignment` row.
   - Implementing `B0PooledThresholdConstructor` in `infrastructure/thresholding/policies.py` (alongside `ExactThresholdConstructor`, matching file convention), pooling all clients' calibration scores via an injected `CentralizedCalibrationScoreReader` before calling the already-correct `compute_b0_pooled_threshold`.
- Tests: refactored `tests/integration/learning/test_b0_centralized_scoring.py` to the request-driven API (4 tests still pass); added `tests/integration/learning/test_b0_pooled_threshold_constructor.py` (pools two clients' scores and confirms the exact p95 over the pooled 100-value set). Full local suite re-run: **553 passed** (`tests/unit/` + `tests/integration/`), import-linter 7/7, `tests/architecture/` 45/45.
- Evaluation and statistics/reporting coverage remains genuinely unimplemented (unchanged from the prior progress note) — this ticket stays `IN_PROGRESS`.

