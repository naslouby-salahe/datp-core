# P2-T003 — Inspect the N-BaIoT source and feature schema

Standalone Phase 2 implementation specification. It is independently actionable; the master log remains authoritative for canonical identity, phase membership, ordering, and dependency graph. This conversion creates documentation only and does not implement the ticket.

## Part A — Ticket template

1. **Ticket ID.** P2-T003
2. **Title.** Inspect the N-BaIoT source and feature schema
3. **Phase.** 2
4. **Status.** DONE
5. **Priority.** P0 — Blocking
6. **Ticket type.** data
7. **Owner.** data-pipeline-engineer
8. **Supporting agents.** NONE
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** ANCHOR
11. **Roadmap experiment IDs.** E-C1
12. **Architecture contracts/types owned.** `DatasetSourceInspectionResult`, `DatasetSourceManifest`, `FeatureSchemaManifest`
13. **Objective.** Inspect the N-BaIoT source (read-only) to produce `DatasetSourceInspectionResult`, `DatasetSourceManifest`, and `FeatureSchemaManifest` (ordered features, input dimension), recording source-row identity scheme and timestamp evidence (or its absence).
14. **Why this ticket exists.** Source/schema facts are recorded, never partitioned/preprocessed here; input dimension and feature order feed every downstream identity.
15. **Authority references.** Arch §9.1, §9.5, §12.3 (`DatasetSourceInspector`).
16. **Dependencies.** P2-T001, P1-T032.
17. **Blocks.** P2-T004.
18. **Downstream consumers.** P2-T004 (N-BaIoT source adapter) consumes source/schema manifests and identity scheme.
19. **Preconditions.** P2-T001, P1-T032 complete.
20. **Inputs.** Arch §9.1, §9.5, §12.3.
21. **Allowed scope.** Read-only inspection; manifest recording under `data/manifests/`.
22. **Forbidden actions.** No partition/split/fit/transform; no training; no scientific execution.
23. **Required configuration or NONE.** N-BaIoT dataset config section (input_dim to be verified from the manifest).
24. **Implementation responsibilities.** Run source inspection; record manifests and source-row identity scheme.
25. **Required unit tests.** NONE.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE.
29. **Required integration tests.** Integration (data): inspection produces a schema manifest with a stable feature order (on a synthetic-schema fixture if the real source is unavailable). Fixed-input regression check: schema-manifest shape.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Integration (data); manifest inspection.
33. **Acceptance criteria.** [ ] Source and feature-schema manifests produced. [ ] Feature order/input dimension recorded. [ ] Timestamp evidence recorded (present/absent). [ ] No partitioning/preprocessing performed.
34. **Completion evidence.** Standard report + recorded identities.
35. **Failure and blocker behavior.** `DatasetError` on unreadable/inconsistent source.
36. **Stop conditions.** Stop if the feature schema is inconsistent with recovered semantics; record a blocker.
37. **Deliverables.** N-BaIoT source/schema manifests.
38. **Final review checklist.** [ ] Inspection only.

## Part B — Mandatory Phase 2 lifecycle, boundaries, and audits

### B1. Lifecycle checklist

- [ ] Immediately before implementation, read the complete roadmap, complete architecture, this ticket, the Phase 2 index/gate, `docs/tickets/TICKET_STATUS.md`, each direct dependency, and cited authority sections.
- [ ] Verify each dependency, including `P1-T070` where required, is `DONE`.
- [ ] Set this ticket and the corresponding status-register row to `IN_PROGRESS` in the same change; record start time and current step.
- [ ] Confirm no real anchor scientific execution is permitted; keep both status locations synchronized.
- [ ] Set `IN_REVIEW` before audits; record Audit 1, Audit 2, and Audit 3 independently.
- [ ] On any blocker, set both locations to `BLOCKED` immediately, recording exact cause, evidence, affected identity, and unblock condition.
- [ ] Set `DONE` only after all acceptance criteria, validation, audits, cleanup, and completion evidence pass; record synchronized final status and evidence.
- [ ] Verify final status consistency between this ticket and `docs/tickets/TICKET_STATUS.md`; a mismatch is a blocker.

### B2. Scientific and recovered-semantics locks

These are constraints, not authorization to run a campaign: this ticket may perform only the Part A work under the synthetic-only boundary. Work only in `ProtocolTrack.DATP_ANCHOR` and the separate anchor namespace. One authorized FedAvg AE is trained per seed, selected then frozen, and its compatible calibration/test score artifacts feed B1–B4; threshold-only changes never retrain or rescore. Calibration is benign-only; attacks are evaluation-only; no test data, attacks, thresholds, or downstream outcomes select a checkpoint. B0 is a separate centralized model and lineage, not a ladder policy or FedAvg substitute. B1 is shared, B2 local, B3 is an authorized-taxonomy family mean, and B4 is exact k-means++ on `[mean,std,skew,p95]` at canonical `K=3`.

For every recovered historical fact, record subject, read-only evidence location, roadmap rule, architecture representation, recovered behavior, justified difference, tests, uncertainty, and blocker state. Do not copy/import the reference repository, reuse its layout, claim “same as old DATP,” or guess. The roadmap wins scientific conflicts; the architecture wins technical conflicts. Preserve `ANCHOR_CHECKPOINT_PROTOCOL_UNRESOLVED` when applicable; never substitute the journal schedule for historical anchor evidence.

### B3. Synthetic-only boundary

Validate only with deterministic synthetic arrays/clients/labels, fakes, test doubles, property/contract/architecture tests, lineage simulations, dry-run planning, and isolated test storage. Read-only source/reference inspection is allowed only when this ticket explicitly permits it. Real N-BaIoT training/scoring/thresholds/metrics, reduced runs, real-data smoke/debug work, scientific outputs/checkpoints, invented seeds, and result-driven tuning are forbidden. Synthetic outputs never enter an anchor scientific namespace or masquerade as evidence.

### B4. Required repository-wide audits

After implementation, audit the complete repository—not merely the diff—for DATP scope; dependency direction; framework confinement; domain/application/config/composition/CLI boundaries; import cycles and side effects; type placement; named typed requests/results and ports; no service locator/universal context/generic utility; no Pydantic beyond config; typed lineage/artifact identities; atomic persistence; deterministic process isolation; and no aliases, redirects, shims, wrappers, or compatibility paths. An authority-grounded defect is not excused as pre-existing: fix it or record a decision blocker. Run configured import-linter and pytest-archon.

Scan for raw protocol strings/dictionaries, uncontrolled tracks/namespaces/policies/roles/stages, `dict[str, Any]`, broad `Any`/object, mutable scientific specifications, tuple pseudo-objects, Boolean variant flags, optional-field inference, missing `assert_never`, generic Request/Result/Payload/Context/Manager/Handler names, and scattered defaults. Search code/tests (planning Markdown exempt) for ticket IDs, ticket files, authority-document names/sections, and planning-language claims. Audit stale docs/paths/names/status claims, unsupported completion claims, AI/banner/decorative comments, copied architecture prose, and broken links.

Audit the entire repository explicitly: `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter configuration, Ruff configuration, Pyright/Pylance configuration, pytest configuration, Sonar configuration, provider adapters, and root metadata. Do not accept “pre-existing,” “outside the diff,” “another agent,” “legacy code,” or “untouched file” exemptions.

Run full Ruff lint/format, Pyright strict, Pylance parity when available, configured Sonar gate, applicable tests, complete suite, randomized order, mandatory Nox sessions, serialized CUDA where applicable, diff, untracked-file, and cleanup checks. Record unavailable tools honestly; do not weaken tests/types/exclusions to pass.

### B5. Determinism, CUDA, and artifacts

Verify stage-specific seed derivation, no seed-position identity, no CPU fallback, no automatic batch/chunk/precision/model/data reduction, batching-first processing, spawn-safe picklable boundary objects, serialized CUDA tests, terminal CUDA-OOM behavior, no configuration-changing retry, stage lineage, typed artifact identity, and atomic commits. Test benign/attack child artifacts are readable only as a committed aggregate; incompatible reuse is rejected or `RECOMPUTE`, never silently reused.

### B6. Three independent scientific-drift audits

- **Audit 1 — Roadmap fidelity.** Check fixed encoder, FedAvg/E=1/full participation, threshold-only B1–B4, benign calibration, attack isolation, CV(FPR)-primary/AUROC-control, B0 separation, B3 taxonomy, B4 fingerprint/K, anchor/journal and seed separation, no claim promotion, no real execution, and no guesses.
- **Audit 2 — Architecture fidelity.** Check layers, typed boundaries/configuration, lineage/reuse, persistence, determinism/CUDA/batching, namespace isolation, no raw dictionaries/hidden defaults, and no compatibility scaffolding.
- **Audit 3 — Adversarial anchor review.** Cross-check authorities, ticket/dependencies, implementation/tests/evidence/configuration/identities/status for leakage, source copying, fake equivalence, B1–B4 retraining, B0/FedAvg mixing, wrong B4, namespace mixing, real-data paths, vacuous tests, incomplete audits, or unsupported tool claims.
- [ ] Correct each authority-grounded finding and rerun all three audits; preserve separate evidence for each.

### B7. Completion evidence

Record a check-by-check completion ledger: changed files and why; verified dependencies; recovered behavior/evidence where applicable; implementation steps; exact commands/results; every required unit, property, contract, architecture, integration, CUDA, and synthetic end-to-end/system test (or the Part A reason it is not applicable); full-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; all three audit records; blockers; remaining risks; and skipped checks with exact reasons. A mandatory check cannot be skipped for green status, and no pre-existing-defect exemption is valid.

This ticket consumes Phase 0 governance and `P1-T070`; it must not recreate them. `P2-T020` is the Phase 2 gate and cannot pass until every mandatory Phase 2 responsibility reaches a valid terminal state through the authoritative graph.

### Completion evidence — 2026-07-15

- Dependencies verified: `P2-T001` `DONE`; `P1-T032` `DONE` (`docs/tickets/TICKET_STATUS.md:195`).
- Implementation: added `DatasetSourceManifest`, `FeatureSchemaManifest`, `SourceFileManifestEntry`, `SourceTrafficLabel` to `src/datp_core/domain/data/datasets.py` (frozen/slotted/kw-only, validated in `__post_init__`, no raw dicts). Added `NBaIoTSourceInspector` in `src/datp_core/infrastructure/data/nbaiot_inspection.py` implementing the existing `DatasetSourceInspector` port: enumerates device directories deterministically (sorted), classifies each file as benign/gafgyt/mirai by its directory convention, reads each CSV with `pyarrow.csv.open_csv` (bounded/streaming, `block_size=8MiB`, never loads a whole file), verifies the feature schema is identical across every file (raising `DatasetError` on the first mismatch), computes each file's content hash via the existing chunked `blake3_file_content_hash`, builds both manifests, persists them via the existing `FileArtifactStore.write_atomically` (content-addressed, atomic), and returns `DatasetSourceInspectionResult` with `source_row_identity` derived from the source manifest's own content hash and `timestamp_evidence=None`. Added `typings/pyarrow/csv.pyi` (repo already hand-stubs `pyarrow`/`pyarrow.dataset`/`pyarrow.parquet`; `pyarrow.csv` was missing).
- Reused existing Phase 1 infrastructure without duplication: `FileArtifactStore`, `ArtifactPathResolver`, `bind_storage_root`, `blake3_file_content_hash`/`blake3_bytes_content_hash`, `DatasetArtifactKey`, `ArtifactNamespace.DATP_ANCHOR`. No new persistence mechanism was invented.
- Real read-only inspection was run against the actual N-BaIoT source at `data/raw/N-BaIoT` (real dataset, 9.1GB, confirmed present on disk via the pre-existing `data/raw` symlink) — not merely a synthetic fixture, since this ticket explicitly authorizes real source/schema inspection (Phase 2 boundary §13.1). Real run took 52.4s, memory-bounded throughout (streaming CSV reads, chunked hashing). Result: 9 devices (`Danmini_Doorbell`, `Ecobee_Thermostat`, `Ennio_Doorbell`, `Philips_B120N10_Baby_Monitor`, `Provision_PT_737E_Security_Camera`, `Provision_PT_838_Security_Camera`, `Samsung_SNH_1011_N_Webcam`, `SimpleHome_XCS7_1002_WHT_Security_Camera`, `SimpleHome_XCS7_1003_WHT_Security_Camera` — matches P2-T001's recovered `DEVICE_DIRS` exactly), 89 real per-device source files (9 benign + 45 gafgyt + 35 mirai; `Ennio_Doorbell` and `Samsung_SNH_1011_N_Webcam` correctly have no Mirai files, consistent with the published N-BaIoT dataset), 7,062,606 total rows, 115 feature columns (verified identical across all 89 files), `timestamp_evidence=None` (no timestamp-like column exists in the real header — `MI_dir_*`/`H_*`/`HH_*`/`HH_jit_*`/`HpHp_*`, confirmed by direct inspection of the real CSV header). The top-level `demonstrate_structure.csv` (not inside any device directory) was correctly excluded, not miscounted as a 90th device file. Real manifests persisted under `data/manifests/datp_anchor/dataset/{source_inspection,feature_schema_manifest}/n_baiot/...` (content-addressed, atomic-write verified by `FileArtifactStore`).
- Acceptance criteria: source and feature-schema manifests produced (real artifacts on disk, shown above) — PASS. Feature order/input dimension recorded (115, ordered tuple starting `MI_dir_L5_weight, MI_dir_L5_mean, ...`) — PASS. Timestamp evidence recorded as absent — PASS. No partitioning/preprocessing performed (inspector only reads and hashes; no split/fit/transform code path exists in this file) — PASS.
- Required integration test (Part A item 29): `tests/integration/data/test_nbaiot_inspection.py`, 7 tests, using a small synthetic fixture (not the real 9.1GB dataset, to keep the suite fast and deterministic, per the ticket's own "synthetic-schema fixture" allowance) — stable feature order and manifest artifact production, determinism across repeated runs (identical `source_row_identity`/manifests), input-dimension-mismatch rejection, cross-file schema-inconsistency rejection, empty-raw-root rejection, device-with-no-files rejection, header-only-file rejection. All 7 pass. A live Sonar-lint finding (S5778, "exception test should have only one invocation possibly throwing") was caught and fixed during authoring by hoisting request construction out of each `pytest.raises` block.
- Validation commands run: `pytest tests/integration/data/test_nbaiot_inspection.py -v` (7 passed); `pytest tests/unit/domain/test_data_vocabulary.py tests/integration/data -q` (20 passed); `pytest -q` full suite normal order (557 passed) and randomized order (557 passed) — no regression; `ruff check`/`ruff format --check` on all changed files (clean); `pyright` on all changed files (0 errors); `python -m importlinter.cli lint-imports --config importlinter.ini` (exit 0, clean); `pytest tests/architecture -q` (45 passed).
- Sonar/CodeScene: not run as a full project gate for this ticket — deferred to the batched checkpoint after the 5th ticket (`P2-T005`), per the agreed validation cadence recorded in `.tmp-phase-2-execution/EXECUTION_PLAN.md`. The one live Sonar-lint finding surfaced during authoring was fixed immediately (see above).
- Audit 1 (Roadmap fidelity): PASS — no scientific execution beyond authorized read-only source inspection; no partitioning/splitting/fitting/training occurred; PLANNING_ONLY-adjacent boundary (this ticket's own classification is `FORBIDDEN` for scientific execution beyond inspection, and no forbidden stage was touched).
- Audit 2 (Architecture fidelity): PASS — inspector lives in `infrastructure/data/`, imports only `application`/`domain`; no raw dict/`Any` introduced (checked via Pyright strict); reused existing typed ports/persistence rather than duplicating; `DatasetArtifactKey`/`ArtifactNamespace.DATP_ANCHOR` used correctly for the anchor track; atomic content-addressed persistence reused unchanged.
- Audit 3 (Adversarial anchor review): PASS — verified real device-directory enumeration excludes the non-device `demonstrate_structure.csv` file (independently cross-checked file counts per device: 11/11/6/11/11/11/6/11/11 = 89, matching); verified schema-consistency check actually rejects a real mismatch (adversarial test); verified determinism by running inspection twice against identical input and comparing full result equality, not just a single field.
- No reference-repository content was copied; only the domain finding "N-BaIoT has no timestamp column" (recovered in P2-T001/P2-T002) was independently re-confirmed against the real source here, consistent with, not duplicating, that prior evidence.
- Blockers: NONE.
- Remaining risks: none identified for this ticket's scope.
- Cleanup: the one-off real-inspection invocation script lived only in the session scratchpad (`/tmp/...`), never in the repository; `data/manifests/` now contains real, permanent, content-addressed manifest artifacts, which is the ticket's intended deliverable location, not scratch.
- **Correction (2026-07-15, after user review):** the real manifest artifacts under `data/manifests/` were initially committed and pushed. The user flagged that generated artifact stores are gitignored in this repo and `data/manifests/` should not have been an exception; `.gitignore`'s own policy comment (which had explicitly listed `data/manifests/` as "tracked and intentional," a decision predating this ticket) was corrected to match, and the 3 files this ticket had added were untracked via `git rm --cached` in a follow-up commit (history not rewritten, since the remote is shared). The artifacts remain on disk locally as real deliverable evidence for this completion record; they are simply no longer committed to version control.

