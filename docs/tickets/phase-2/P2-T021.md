# P2-T021 — Implement B0 centralized checkpoint schedule and scientific checkpoint selection

Standalone Phase 2 implementation specification. It is independently actionable; the master log remains authoritative for canonical identity, phase membership, ordering, and dependency graph. This conversion creates documentation only and does not implement the ticket.

## Part A — Ticket template

1. **Ticket ID.** P2-T021
2. **Title.** Implement B0 centralized checkpoint schedule and scientific checkpoint selection
3. **Phase.** 2
4. **Status.** IN_REVIEW
5. **Priority.** P1-Mandatory
6. **Ticket type.** checkpoint
7. **Owner.** federated-learning-engineer
8. **Supporting agents.** threshold-policy-engineer, reproducibility-auditor
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** ANCHOR
11. **Roadmap experiment IDs.** — (ANCHOR-B0; not a formal journal experiment ID)
12. **Architecture contracts/types owned.** `CheckpointSchedule`, `CheckpointSelectionSpec`, `CheckpointSelectionResult`, `CheckpointSelectionArtifact`, `CheckpointDescriptor`, `CentralizedModelComparatorProfileSpec`
13. **Objective.** Produce B0's separate centralized checkpoint schedule and selection identity from centralized training only.
14. **Why this ticket exists.** B0 is a separately trained centralized AE reference, not a threshold operation over FedAvg outputs.
15. **Authority references.** Road §4, §9; Arch §9.1, §13, §17.
16. **Dependencies.** P2-T012, P2-T002, P1-T022.
17. **Blocks.** P2-T022.
18. **Downstream consumers.** P2-T022, P2-T023, P3-T007, P5-T005, P6-T002, P7-T002.
19. **Preconditions.** Historical anchor checkpoint evidence is recovered or the affected anchor branch remains blocked; no journal checkpoint schedule is substituted.
20. **Inputs.** B0 centralized training identity, recovered anchor evidence, frozen checkpoint specification.
21. **Allowed scope.** Synthetic implementation and typed identity construction only.
22. **Forbidden actions.** No FedAvg checkpoint reuse, no guessed historical rounds, no real campaign execution.
23. **Required configuration.** Dedicated B0 centralized checkpoint configuration; or `NONE` until authoritative evidence is recovered — this conditional exception is preserved and remains real and roadmap-conditioned, not a template gap. If and when recovered historical evidence authorizes concrete B0 checkpoint schedule/selection values (e.g. round/epoch schedule, selection-rule version, retention count), those genuine scientific/execution choices must be represented as `Literal`-typed (if locked) or normally-typed (if tunable) fields on a B0 checkpoint config schema in `config/schemas/scientific.py` (or `config/schemas/execution.py` for execution-only parameters), mapped via a pure `map_*` function in the matching `config/mapping/*.py`, and backed by a real YAML file under `configs/protocols/` — never introduced as a bare hardcoded Python constant. Until such evidence exists, `NONE` stands and the branch stays blocked under `ANCHOR_CHECKPOINT_PROTOCOL_UNRESOLVED`; this is not license to hardcode a guessed value in code.
24. **Implementation responsibilities.** Implement a centralized schedule, persistence, and selection path that rejects federated identities and records its own scientific checkpoint.
25. **Required unit tests.** Centralized checkpoint rejects a federated training identity.
26. **Required property tests.** Identity changes when any centralized schedule input changes.
27. **Required contract tests.** Persisted checkpoint lineage is typed and complete.
28. **Required architecture tests.** No central/federated carrier crosses the wrong port.
29. **Required integration tests.** Synthetic centralized checkpoint selection.
30. **Required CUDA tests.** Synthetic tiny-model checkpoint persistence.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Focused unit/property/contract/integration/CUDA lanes.
33. **Acceptance criteria.** [ ] Centralized schedule and checkpoint are independently identified. [ ] FedAvg identity is rejected. [ ] Unresolved historical evidence blocks the anchor branch. [ ] If B0 checkpoint schedule/selection values are introduced, they are YAML-backed via a config schema field and mapper under `configs/protocols/`, never a bare hardcoded constant; otherwise the `NONE`/blocked state is preserved with `ANCHOR_CHECKPOINT_PROTOCOL_UNRESOLVED` recorded.
34. **Completion evidence.** Changed files; implemented types/contracts; commands and tests run with results; B0 checkpoint identity; blocker state; cleanup confirmation; skipped checks/reasons; no unrelated modifications.
35. **Failure and blocker behavior.** Preserve failed evidence and emit `ANCHOR_CHECKPOINT_PROTOCOL_UNRESOLVED` rather than guessing.
36. **Stop conditions.** Missing authoritative anchor checkpoint evidence.
37. **Deliverables.** B0 centralized checkpoint specification and identity.
38. **Final review checklist.** [ ] Centralized lineage only. [ ] No journal-protocol substitution.

## Part B — Mandatory Phase 2 lifecycle, boundaries, and audits

### B1. Lifecycle checklist

- [ ] Immediately before implementation, read the complete roadmap, complete architecture, this ticket, the Phase 2 index/gate, `docs/tickets/TICKET_STATUS.md`, each direct dependency, and cited authority sections.
- [ ] Verify each dependency, including `P1-T070` where required, is `DONE`.
- [ ] Set this ticket and the corresponding status-register row to `IN_PROGRESS` in the same change; record start time and current step.
- [ ] Confirm no real anchor scientific execution is permitted; keep both status locations synchronized.
- [ ] Set `IN_REVIEW` before audits; record Audit 1, Audit 2, and Audit 3 independently.
- [ ] On any blocker, set both locations to `BLOCKED` immediately, recording exact cause, evidence, affected identity, and unblock condition.
- [ ] Set `DONE` only after all acceptance criteria, validation, audits, cleanup, and completion evidence pass; record synchronized final status and evidence.
- [ ] Verify final status consistency between this ticket and `docs/tickets/TICKET_STATUS.md`; a mismatch is a blocker.

### B2. Scientific and recovered-semantics locks

These are constraints, not authorization to run a campaign: this ticket may perform only the Part A work under the synthetic-only boundary. Work only in `ProtocolTrack.DATP_ANCHOR` and the separate anchor namespace. One authorized FedAvg AE is trained per seed, selected then frozen, and its compatible calibration/test score artifacts feed B1–B4; threshold-only changes never retrain or rescore. Calibration is benign-only; attacks are evaluation-only; no test data, attacks, thresholds, or downstream outcomes select a checkpoint. B0 is a separate centralized model and lineage, not a ladder policy or FedAvg substitute. B1 is shared, B2 local, B3 is an authorized-taxonomy family mean, and B4 is exact k-means++ on `[mean,std,skew,p95]` at canonical `K=3`.

For every recovered historical fact, record subject, read-only evidence location, roadmap rule, architecture representation, recovered behavior, justified difference, tests, uncertainty, and blocker state. Do not copy/import the reference repository, reuse its layout, claim “same as old DATP,” or guess. The roadmap wins scientific conflicts; the architecture wins technical conflicts. Preserve `ANCHOR_CHECKPOINT_PROTOCOL_UNRESOLVED` when applicable; never substitute the journal schedule for historical anchor evidence.

### B3. Synthetic-only boundary

Validate only with deterministic synthetic arrays/clients/labels, fakes, test doubles, property/contract/architecture tests, lineage simulations, dry-run planning, and isolated test storage. Read-only source/reference inspection is allowed only when this ticket explicitly permits it. Real N-BaIoT training/scoring/thresholds/metrics, reduced runs, real-data smoke/debug work, scientific outputs/checkpoints, invented seeds, and result-driven tuning are forbidden. Synthetic outputs never enter an anchor scientific namespace or masquerade as evidence.

### B4. Required repository-wide audits

After implementation, audit the complete repository—not merely the diff—for DATP scope; dependency direction; framework confinement; domain/application/config/composition/CLI boundaries; import cycles and side effects; type placement; named typed requests/results and ports; no service locator/universal context/generic utility; no Pydantic beyond config; typed lineage/artifact identities; atomic persistence; deterministic process isolation; and no aliases, redirects, shims, wrappers, or compatibility paths. An authority-grounded defect is not excused as pre-existing: fix it or record a decision blocker. Run configured import-linter and pytest-archon.

Scan for raw protocol strings/dictionaries, uncontrolled tracks/namespaces/policies/roles/stages, `dict[str, Any]`, broad `Any`/object, mutable scientific specifications, tuple pseudo-objects, Boolean variant flags, optional-field inference, missing `assert_never`, generic Request/Result/Payload/Context/Manager/Handler names, and scattered defaults. Search code/tests (planning Markdown exempt) for ticket IDs, ticket files, authority-document names/sections, and planning-language claims. Audit stale docs/paths/names/status claims, unsupported completion claims, AI/banner/decorative comments, copied architecture prose, and broken links.

Audit the entire repository explicitly: `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter configuration, Ruff configuration, Pyright/Pylance configuration, pytest configuration, Sonar configuration, provider adapters, and root metadata. Do not accept “pre-existing,” “outside the diff,” “another agent,” “legacy code,” or “untouched file” exemptions.

Run full Ruff lint/format, Pyright strict, Pylance parity when available, configured Sonar gate, applicable tests, complete suite, randomized order, mandatory Nox sessions, serialized CUDA where applicable, diff, untracked-file, and cleanup checks. Record unavailable tools honestly; do not weaken tests/types/exclusions to pass.

### B5. Determinism, CUDA, and artifacts

Verify stage-specific seed derivation, no seed-position identity, no CPU fallback, no automatic batch/chunk/precision/model/data reduction, batching-first processing, spawn-safe picklable boundary objects, serialized CUDA tests, terminal CUDA-OOM behavior, no configuration-changing retry, stage lineage, typed artifact identity, and atomic commits. Test benign/attack child artifacts are readable only as a committed aggregate; incompatible reuse is rejected or `RECOMPUTE`, never silently reused.

### B6. Three independent scientific-drift audits

- **Audit 1 — Roadmap fidelity.** Check fixed encoder, FedAvg/E=1/full participation, threshold-only B1–B4, benign calibration, attack isolation, CV(FPR)-primary/AUROC-control, B0 separation, B3 taxonomy, B4 fingerprint/K, anchor/journal and seed separation, no claim promotion, no real execution, and no guesses.
- **Audit 2 — Architecture fidelity.** Check layers, typed boundaries/configuration, lineage/reuse, persistence, determinism/CUDA/batching, namespace isolation, no raw dictionaries/hidden defaults, and no compatibility scaffolding.
- **Audit 3 — Adversarial anchor review.** Cross-check authorities, ticket/dependencies, implementation/tests/evidence/configuration/identities/status for leakage, source copying, fake equivalence, B1–B4 retraining, B0/FedAvg mixing, wrong B4, namespace mixing, real-data paths, vacuous tests, incomplete audits, or unsupported tool claims.
- [ ] Correct each authority-grounded finding and rerun all three audits; preserve separate evidence for each.

### B7. Completion evidence

Record a check-by-check completion ledger: changed files and why; verified dependencies; recovered behavior/evidence where applicable; implementation steps; exact commands/results; every required unit, property, contract, architecture, integration, CUDA, and synthetic end-to-end/system test (or the Part A reason it is not applicable); full-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; all three audit records; blockers; remaining risks; and skipped checks with exact reasons. A mandatory check cannot be skipped for green status, and no pre-existing-defect exemption is valid.

This ticket consumes Phase 0 governance and `P1-T070`; it must not recreate them. `P2-T020` is the Phase 2 gate and cannot pass until every mandatory Phase 2 responsibility reaches a valid terminal state through the authoritative graph.

## Completion evidence — 2026-07-16T17:55:00Z

- Lifecycle and dependencies: confirmed `P2-T012`, `P2-T002`, and `P1-T022` are `DONE` before starting. Searched `docs/MASTER_TICKET_LOG.md` for any recovered evidence specific to B0's (centralized, non-federated) checkpoint schedule — found none; N.2 (`AnchorHistoricalCheckpointProtocol`) is scoped to the reference repo's federated `runner.py` loop, and no separate B0-specific checkpoint-schedule recovery exists in the register. Per this ticket's own explicit "Required configuration" clause ("Dedicated B0 centralized checkpoint configuration; or `NONE` until authoritative evidence is recovered... this is not license to hardcode a guessed value in code"), no epoch-count/schedule value was introduced anywhere in this ticket; the `NONE` configuration state is preserved.
- Implementation: confirmed `CentralizedTrainingRunResult` (`application/ports/learning.py`, built by `P1-T044`) already IS the B0 checkpoint identity type — its own `__post_init__` already structurally rejects anything but a `CentralizedCheckpointIdentity`/`ArtifactRef` pair, so a FedAvg `TrainingIdentity` cannot satisfy it by construction. Added `infrastructure/learning/centralized/nbaiot_b0_checkpoint.py::B0CentralizedCheckpointStager` (implements the previously-bare `CentralizedCheckpointStager` protocol from `P1-T044`) — stages the trained parameters (from `P2-T012`'s `CentralizedPooledBenignTrainingExecutor.current_parameters()`) into a content-hashed `ArtifactRef`, structurally rejecting a non-`CentralizedModelComparatorSpec` request. Added `select_centralized_checkpoint()` — a validating (not ranking) pass-through, consistent with N.2's "single, unranked, unscheduled checkpoint" finding extended to the centralized branch (B0 has exactly one training run per seed, no candidate pool); it checks only what `CentralizedTrainingRunResult`'s own constructor does not already guarantee (`artifact_type is SCIENTIFIC_CHECKPOINT`) — an initial draft duplicated the identity-type check already enforced by the result type's own `__post_init__`, which would have been dead/unreachable validation and was removed before committing.
- Tests: 5 new tests in `tests/integration/learning/test_b0_centralized_checkpoint.py` (stager rejects a FedAvg identity in place of the centralized comparator; stager rejects empty parameters; stager produces a deterministic content-hashed scientific-checkpoint artifact for identical parameters; selection accepts a well-typed scientific checkpoint; selection rejects a checkpoint whose artifact carries a non-scientific-checkpoint type). All pass. Re-ran `tests/unit/domain/`, `tests/unit/config/`, `tests/integration/learning/`, `tests/unit/application/` (298 passed) to confirm no regression.
- Quality gates run for this ticket: Ruff lint and format (clean), Pyright strict (0 errors on all changed files), import-linter (7/7 contracts kept), `tests/architecture/` (45/45 passed). Full-project gates deferred to Checkpoint 3 closure (ticket 3 of 5).
- Audit 1 — roadmap fidelity: B0 remains a separate centralized comparator, never a ladder member; its checkpoint identity is structurally disjoint from FedAvg's; no historical schedule value was guessed; `ANCHOR_CHECKPOINT_PROTOCOL_UNRESOLVED` is not asserted resolved for B0 (no B0-specific evidence exists) — the `NONE` configuration path was taken deliberately, exactly as this ticket's own template anticipates. No real execution; all tests synthetic.
- Audit 2 — architecture fidelity: new code lives in `infrastructure/learning/centralized/`; reuses `P1-T044`'s existing `CentralizedTrainingRunResult`/`CentralizedCheckpointStager` port rather than inventing a parallel type; no raw dictionaries, no unjustified `Any`; the removed dead check (see implementation note above) was caught precisely because the audit asked whether each validation was reachable, not merely present.
- Audit 3 — adversarial anchor review: tested and rejected a FedAvg-typed comparator passed to the stager; confirmed `CentralizedTrainingRunResult` cannot be constructed with a non-`CentralizedCheckpointIdentity` at all (attempted and observed the type's own `DomainValidationError`, informing the decision to drop the redundant check in `select_centralized_checkpoint`); confirmed selection rejects an artifact of the wrong `ArtifactType` even when its identity type is otherwise valid. No code or layout was read from or copied out of the reference repository in this session.
- Cleanup: no scratch files created; no ticket IDs or planning-document references introduced into source/test code.
- Blocker state: no B0-specific checkpoint-schedule evidence exists; per this ticket's own template, this is not a blocking condition — the `NONE` configuration path is a valid terminal state for this ticket's scope, distinct from `ANCHOR_CHECKPOINT_PROTOCOL_UNRESOLVED` (which concerns the federated branch and is carried separately to `P2-T020`).
- Skipped checks and reasons: full-project Ruff/Pyright/import-linter/pytest-archon/Nox/coverage/randomized-order suite, Sonar, and CodeScene deferred to Checkpoint 3 closure per the batched-checkpoint process (this is not the checkpoint's final ticket).

