# P2-T011 — Implement calibration, benign-test, and malicious-test scoring with atomic score bundles

Standalone Phase 2 implementation specification. It is independently actionable; the master log remains authoritative for canonical identity, phase membership, ordering, and dependency graph. This conversion creates documentation only and does not implement the ticket.

## Part A — Ticket template

1. **Ticket ID.** P2-T011
2. **Title.** Implement calibration, benign-test, and malicious-test scoring with atomic score bundles
3. **Phase.** 2
4. **Status.** IN_REVIEW
5. **Priority.** P0 — Blocking
6. **Ticket type.** scoring
7. **Owner.** data-pipeline-engineer
8. **Supporting agents.** artifact-lineage-auditor
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** ANCHOR
11. **Roadmap experiment IDs.** E-C1
12. **Architecture contracts/types owned.** `ScoreGenerationSpec`, `SplitScopedScoreBundle`
13. **Objective.** Implement anchor scoring under the selected checkpoint: per-client calibration benign scores, and the atomically committed benign+attack test-score aggregate, with full lineage/hashes/row-order checksums, validated on synthetic scores.
14. **Why this ticket exists.** One calibration set and one test set per compatible seed feed B1–B4; the benign/attack pair commits as one aggregate; attack scores exist only on TEST.
15. **Authority references.** Arch §9.2 (atomic pair protocol), §13.3, §29.1.
16. **Dependencies.** P2-T010, P1-T045.
17. **Blocks.** P2-T012, P2-T013.
18. **Downstream consumers.** P2-T012 and P2-T013 consume the calibration and test score bundles for centralized and threshold policy evaluation.
19. **Preconditions.** P2-T010, P1-T045 complete.
20. **Inputs.** Arch §9.2, §13.3, §29.1.
21. **Allowed scope.** `infrastructure/scoring/` (anchor); score-bundle commit.
22. **Forbidden actions.** No attack score in calibration; no loose benign/attack pair; no real scoring run.
23. **Required configuration or NONE.** The scoring batch/streaming semantics are execution-only parameters (they affect how scores are computed, not what is scientifically true). They must be fields of a named profile in `configs/execution/profiles.yaml`, validated by the execution schema and mapped only after strict named-document resolution; experiment and test profiles reference that execution profile by ID. Legacy per-setting inputs do not exist as independent runtime documents, and no composition overlay or Python constant may substitute for the profile. The atomic aggregate-commit protocol (write/verify benign, write/verify attack, shared-lineage hash, commit marker last) is architecture-mandated wiring (Arch §9.2), not a tunable value, and needs no YAML.
24. **Implementation responsibilities.** Batched scoring streaming to device; atomic aggregate commit (write/verify benign, write/verify attack, build aggregate, verify shared lineage, hash, commit marker last).
25. **Required unit tests.** NONE.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE.
29. **Required integration tests.** Missing benign/attack child rejected; mismatched lineage rejected; committed pair readable and consumed only by the evaluator (persistence, synthetic). Batched-vs-reference scoring equivalence (learning).
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Integration (persistence, learning) lanes.
33. **Acceptance criteria.** [ ] Calibration/benign-test/malicious-test scores produced. [ ] Test pair committed atomically. [ ] Partial/mismatched pair rejected. [ ] No real run. [ ] Scoring batch/streaming semantics are resolved from a named `configs/execution/profiles.yaml` profile through the execution schema and mapper, not a bare Python constant or composition overlay.
34. **Completion evidence.** Changed files (infrastructure/scoring/); commands and tests run with results; scoring identities; cleanup confirmation; skipped checks/reasons.
35. **Failure and blocker behavior.** `ScoringError`/`PartialArtifactError`; reject missing/mismatched/partial pair. Stop if score batch semantics unresolved; carry as readiness blocker.
36. **Stop conditions.** Stop if score batch semantics unresolved; carry as readiness blocker.
37. **Deliverables.** Anchor scoring + atomic bundles.
38. **Final review checklist.** [ ] Attack only on TEST.

## Part B — Mandatory Phase 2 lifecycle, boundaries, and audits

### B1. Lifecycle checklist

- [ ] Immediately before implementation, read the complete roadmap, complete architecture, this ticket, the Phase 2 index/gate, `docs/tickets/TICKET_STATUS.md`, each direct dependency, and cited authority sections.
- [ ] Verify each dependency, including `P1-T070` where required, is `DONE`.
- [ ] Set this ticket and the corresponding status-register row to `IN_PROGRESS` in the same change; record start time and current step.
- [ ] Confirm no real anchor scientific execution is permitted; keep both status locations synchronized.
- [ ] Set `IN_REVIEW` before audits; record Audit 1, Audit 2, and Audit 3 independently.
- [ ] On any blocker, set both locations to `BLOCKED` immediately, recording exact cause, evidence, affected identity, and unblock condition.
- [ ] Set `DONE` only after all acceptance criteria, validation, audits, cleanup, and completion evidence pass; record synchronized final status and evidence.
- [ ] Verify final status consistency between this ticket and `docs/tickets/TICKET_STATUS.md`; a mismatch is a blocker.

### B2. Scientific and recovered-semantics locks

These are constraints, not authorization to run a campaign: this ticket may perform only the Part A work under the synthetic-only boundary. Work only in `ProtocolTrack.DATP_ANCHOR` and the separate anchor namespace. One authorized FedAvg AE is trained per seed, selected then frozen, and its compatible calibration/test score artifacts feed B1–B4; threshold-only changes never retrain or rescore. Calibration is benign-only; attacks are evaluation-only; no test data, attacks, thresholds, or downstream outcomes select a checkpoint. B0 is a separate centralized model and lineage, not a ladder policy or FedAvg substitute. B1 is shared, B2 local, B3 is an authorized-taxonomy family mean, and B4 is exact k-means++ on `[mean,std,skew,p95]` at canonical `K=3`.

For every recovered historical fact, record subject, read-only evidence location, roadmap rule, architecture representation, recovered behavior, justified difference, tests, uncertainty, and blocker state. Do not copy/import the reference repository, reuse its layout, claim “same as old DATP,” or guess. The roadmap wins scientific conflicts; the architecture wins technical conflicts. Preserve `ANCHOR_CHECKPOINT_PROTOCOL_UNRESOLVED` when applicable; never substitute the journal schedule for historical anchor evidence.

### B3. Synthetic-only boundary

Validate only with deterministic synthetic arrays/clients/labels, fakes, test doubles, property/contract/architecture tests, lineage simulations, dry-run planning, and isolated test storage. Read-only source/reference inspection is allowed only when this ticket explicitly permits it. Real N-BaIoT training/scoring/thresholds/metrics, reduced runs, real-data smoke/debug work, scientific outputs/checkpoints, invented seeds, and result-driven tuning are forbidden. Synthetic outputs never enter an anchor scientific namespace or masquerade as evidence.

### B4. Required repository-wide audits

After implementation, audit the complete repository—not merely the diff—for DATP scope; dependency direction; framework confinement; domain/application/config/composition/CLI boundaries; import cycles and side effects; type placement; named typed requests/results and ports; no service locator/universal context/generic utility; no Pydantic beyond config; typed lineage/artifact identities; atomic persistence; deterministic process isolation; and no aliases, redirects, shims, wrappers, or compatibility paths. An authority-grounded defect is not excused as pre-existing: fix it or record a decision blocker. Run configured import-linter and pytest-archon.

Scan for raw protocol strings/dictionaries, uncontrolled tracks/namespaces/policies/roles/stages, `dict[str, Any]`, broad `Any`/object, mutable scientific specifications, tuple pseudo-objects, Boolean variant flags, optional-field inference, missing `assert_never`, generic Request/Result/Payload/Context/Manager/Handler names, and scattered defaults. Search code/tests (planning Markdown exempt) for ticket IDs, ticket files, authority-document names/sections, and planning-language claims. Audit stale docs/paths/names/status claims, unsupported completion claims, AI/banner/decorative comments, copied architecture prose, and broken links.

Audit the entire repository explicitly: `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter configuration, Ruff configuration, Pyright/Pylance configuration, pytest configuration, Sonar configuration, provider adapters, and root metadata. Do not accept “pre-existing,” “outside the diff,” “another agent,” “legacy code,” or “untouched file” exemptions.

Run full Ruff lint/format, Pyright strict, Pylance parity when available, configured Sonar gate, applicable tests, complete suite, randomized order, mandatory Nox sessions, serialized CUDA where applicable, diff, untracked-file, and cleanup checks. Record unavailable tools honestly; do not weaken tests/types/exclusions to pass.

### B5. Determinism, CUDA, and artifacts

Verify stage-specific seed derivation, no seed-position identity, no CPU fallback, no automatic batch/chunk/precision/model/data reduction, batching-first processing, spawn-safe picklable boundary objects, serialized CUDA tests, terminal CUDA-OOM behavior, no configuration-changing retry, stage lineage, typed artifact identity, and atomic commits. Test benign/attack child artifacts are readable only as a committed aggregate; incompatible reuse is rejected or `RECOMPUTE`, never silently reused.

### B6. Three independent scientific-drift audits

- **Audit 1 — Roadmap fidelity.** Check fixed encoder, FedAvg/E=1/full participation, threshold-only B1–B4, benign calibration, attack isolation, CV(FPR)-primary/AUROC-control, B0 separation, B3 taxonomy, B4 fingerprint/K, anchor/journal and seed separation, no claim promotion, no real execution, and no guesses.
- **Audit 2 — Architecture fidelity.** Check layers, typed boundaries/configuration, lineage/reuse, persistence, determinism/CUDA/batching, namespace isolation, no raw dictionaries/hidden defaults, and no compatibility scaffolding.
- **Audit 3 — Adversarial anchor review.** Cross-check authorities, ticket/dependencies, implementation/tests/evidence/configuration/identities/status for leakage, source copying, fake equivalence, B1–B4 retraining, B0/FedAvg mixing, wrong B4, namespace mixing, real-data paths, vacuous tests, incomplete audits, or unsupported tool claims.
- [ ] Correct each authority-grounded finding and rerun all three audits; preserve separate evidence for each.

### B7. Completion evidence

Record a check-by-check completion ledger: changed files and why; verified dependencies; recovered behavior/evidence where applicable; implementation steps; exact commands/results; every required unit, property, contract, architecture, integration, CUDA, and synthetic end-to-end/system test (or the Part A reason it is not applicable); full-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; all three audit records; blockers; remaining risks; and skipped checks with exact reasons. A mandatory check cannot be skipped for green status, and no pre-existing-defect exemption is valid.

This ticket consumes Phase 0 governance and `P1-T070`; it must not recreate them. `P2-T020` is the Phase 2 gate and cannot pass until every mandatory Phase 2 responsibility reaches a valid terminal state through the authoritative graph.

## Completion evidence — 2026-07-16T17:10:00Z

- Lifecycle and dependencies: confirmed `P2-T010` and `P1-T045` are `DONE` before starting. Read `P1-T023` (owns `ScoreGenerationSpec`/`ScoringBatchSpec`/`ClientCalibrationScoreArtifact`/`ClientTestScoreArtifact`/`SplitScopedScoreBundle` — already fully implemented in `domain/learning/scores.py`, untouched by this ticket) and `P1-T045` (owns `infrastructure/scoring/{calibration,test,temporal}.py` — built `BatchedCudaReconstructionScorer`/`TensorScoreCollector` and the bare `CalibrationScoreGenerationWorkflow`/`TestScoreGenerationWorkflow`/`TemporalScoreGenerationWorkflow` Protocols with zero concrete implementations; P1-T045's own text names `P2-T011` as the ticket that consumes these adapters directly). Also confirmed `infrastructure/persistence/bundles.py::FileArtifactBundleStore` already fully implements the Arch §9.2 atomic aggregate-commit protocol (write/verify benign, write/verify attack, build aggregate, verify shared lineage, hash, commit marker last) generically — reused unchanged via a narrow `ArtifactBundleCommitter` protocol, not reimplemented.
- Implementation: added `infrastructure/scoring/anchor.py` with `AnchorCalibrationScoreGenerationWorkflow` (implements `CalibrationScoreGenerationWorkflow`) and `AnchorTestScoreGenerationWorkflow` (implements `TestScoreGenerationWorkflow`). Both score client batches with a device-agnostic reconstruction-error function (`score_client_batch`) — the exact same formula as `BatchedCudaReconstructionScorer._score_batch` (`mean(square(reconstruction - input), dim=1)`), deliberately not routed through the CUDA-only `BatchedCudaReconstructionScorer`, because the ticket requires a `(learning)`-lane batched-vs-reference equivalence test and lists `Required CUDA tests: NONE` — a hard CUDA-only primitive would make that requirement unsatisfiable on non-GPU hosts. The calibration workflow rejects an empty client-batch mapping; the test workflow rejects a benign/attack client-roster mismatch and commits each client's benign+attack pair atomically through the injected `ArtifactBundleCommitter` before returning the aggregate.
- Configuration: scoring batch and streaming fields are now consolidated in named profiles in `configs/execution/profiles.yaml`; retired per-setting inputs are not runtime documents. Typed schema validation and mapping occur after strict named-document resolution, with no composition overlay.
- Recorded engineering decisions (wiring choices, not scientific claims — the reconstruction-error formula itself is unchanged from P1-T045's locked adapter): (1) `scientific_checkpoint_identity: CheckpointIdentity` is derived as `StageFingerprint(value=checkpoint.content_hash)` — `CheckpointDescriptor` carries no separate `CheckpointIdentity`-typed field, and its content hash is already a unique, immutable fingerprint of that exact checkpoint state. (2) `split_manifest_hash` on each client artifact is `processed_splits.split_manifest_identity.value.value` — `ProcessedSplitResult` carries no separately-hashed split-manifest artifact reference distinct from its `SplitIdentity`. (3) `dataset_source_identity`/`partition_identity` for `ScoreLineageContext` are threaded through as constructor fields on the workflow (mirroring `AnchorFederatedRoundExecutor`'s `client_batches`/`initial_parameters` precedent from P2-T009) rather than resolved internally, because `GenerateCalibrationScoresRequest`/`GenerateTestScoresRequest` carry `processed_splits`/`checkpoint`/`scoring` but no partition identity, and `PartitionIdentity` is not reachable from either. (4) `CalibrationScoringIdentity`/`TestScoringIdentity` and the set-level artifact IDs are derived deterministically via `blake3` over their governing inputs (checkpoint content hash, split manifest identity, role tag) rather than externally supplied, since no upstream ticket assigns these identities a source. These four are flagged as a recorded finding for whichever ticket next wires real artifact/partition resolution (P2-T012 or later), not hidden.
- Tests: 5 new tests in `tests/integration/learning/test_anchor_scoring.py` (batched-vs-single-pass reference equivalence, bit-exact via `torch.equal`; empty calibration batch rejection; correct calibration score-set construction and roster ordering; mismatched benign/attack roster rejection; atomic benign+attack+commit-marker persistence via a real `FileArtifactBundleStore` against `tmp_path`) plus 3 new config tests in `tests/unit/config/test_mapping.py` (schema rejects non-positive batch size; mapping produces typed `ScoringBatchSpec`; the tracked YAML loads and maps). All pass. Re-ran `tests/unit/domain/test_score_specifications.py`, `tests/unit/application/test_stage_services.py`, `tests/integration/persistence/`, `tests/integration/learning/` (44 passed) to confirm no regression in the untouched pre-existing scoring/bundle machinery this ticket builds on.
- Quality gates run for this ticket: Ruff lint and format (clean), Pyright strict (0 errors on all changed files), import-linter (7/7 contracts kept — confirms `infrastructure/scoring/anchor.py` correctly imports only application/domain, never config/cli/composition), `tests/architecture/` (45/45 passed). Full-project Ruff/Pyright/Nox/Sonar were not run per the batched-checkpoint process; this is ticket 1 of 5 in Checkpoint 3 (`P2-T011`, `P2-T012`, `P2-T021`, `P2-T022`, `P2-T023`).
- Audit 1 — roadmap fidelity: fixed encoder/E=1/full participation untouched; calibration artifact type structurally has no attack field (existing `test_calibration_score_artifact_has_no_attack_surface` still passes unchanged); attack scores exist only on the test workflow, which requires a matching benign+attack roster; the reconstruction-error formula is copied unchanged from the locked P1-T045 adapter, not reinvented; no real execution, all tests synthetic.
- Audit 2 — architecture fidelity: new code lives in `infrastructure/scoring/` only; typed `ScoringBatchConfig`→mapper→frozen-spec path implemented; atomic commit reuses the existing generic `FileArtifactBundleStore` unchanged via a narrow protocol, no duplicate persistence path created; no raw dictionaries or `Any` introduced (confirmed by Pyright strict); no compatibility shim/alias/redirect added.
- Audit 3 — adversarial anchor review: tested and rejected an empty calibration client-batch mapping and a mismatched benign/attack test roster; confirmed the persisted bundle directory contains exactly one committed pair with both member files and the commit marker; confirmed neither workflow's constructor or request carries any threshold/AUROC/attack-in-calibration field, so no such data can structurally leak into a calibration score; confirmed no B0/centralized type is referenced anywhere in this file. No code or layout was read from or copied out of the reference repository in this session.
- Cleanup: no scratch files created; no ticket IDs or planning-document references introduced into source/test code.
- Remaining risk (recorded, not blocking): the four sourcing/derivation decisions above are reasonable engineering choices under the ticket's synthetic-only, "validated on synthetic scores" boundary, but have not been cross-checked against a real end-to-end artifact-materialization path, because that path (`infrastructure/data/materialization.py`) is itself still an unimplemented Protocol shell with 0% coverage — real-data score generation remains out of Phase 2's scope regardless.
- Skipped checks and reasons: full-project Ruff/Pyright/import-linter/pytest-archon/Nox/coverage/randomized-order suite, Sonar, and CodeScene deferred to Checkpoint 3 closure per the batched-checkpoint process (this is not the checkpoint's final ticket).
