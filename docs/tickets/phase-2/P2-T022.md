# P2-T022 — Implement B0 centralized calibration and held-out score generation

Standalone Phase 2 implementation specification. It is independently actionable; the master log remains authoritative for canonical identity, phase membership, ordering, and dependency graph. This conversion creates documentation only and does not implement the ticket.

## Part A — Ticket template

1. **Ticket ID.** P2-T022
2. **Title.** Implement B0 centralized calibration and held-out score generation
3. **Phase.** 2
4. **Status.** IN_REVIEW
5. **Priority.** P1-Mandatory
6. **Ticket type.** scoring
7. **Owner.** federated-learning-engineer
8. **Supporting agents.** data-pipeline-engineer, threshold-policy-engineer
9. **Scientific-execution classification.** FORBIDDEN
10. **Campaign scope.** ANCHOR
11. **Roadmap experiment IDs.** — (ANCHOR-B0; not a formal journal experiment ID)
12. **Architecture contracts/types owned.** `ScoreGenerationSpec`, `CalibrationScoreArtifactId`, `TestScoreArtifactId`, `CalibrationScoreArtifactSet`, `TestScoreArtifactSet`, `ArtifactBundleManifest`
13. **Objective.** Generate split-scoped centralized B0 calibration, benign-test, and malicious-test score identities.
14. **Why this ticket exists.** B0 cannot consume FedAvg score artifacts.
15. **Authority references.** Road §4, §9; Arch §9.2, §13, §22.
16. **Dependencies.** P2-T021, P1-T045.
17. **Blocks.** P2-T023.
18. **Downstream consumers.** P2-T023, P3-T007, P5-T005, P6-T002, P7-T002.
19. **Preconditions.** B0 centralized scientific checkpoint exists.
20. **Inputs.** Centralized checkpoint identity and split identities.
21. **Allowed scope.** Synthetic score-bundle implementation only.
22. **Forbidden actions.** No FedAvg score reuse; no test-driven threshold fit; no real campaign execution.
23. **Required configuration.** B0 split-scoped score configuration: execution-only parameters (e.g. scoring batch/chunk size for centralized score generation) are genuine execution choices — represent them as a normally-typed (or `Literal`-typed, if fixed) field on a B0 scoring execution config schema in `config/schemas/execution.py`, mapped via a pure `map_*` function in the matching `config/mapping/*.py`, and backed by a real YAML file under `configs/execution/` (e.g. `configs/execution/b0_score_generation.yaml`). The calibration-is-benign-only / test-is-held-out scoring semantics and the split-scoped identity-collision rule are protocol-level algorithmic definitions, not tunable values — they remain code-level constants/rules with no YAML override, citing Road §4, §9 and Arch §9.2, §13, §22 as the authority establishing them as non-overridable. Split identities themselves are consumed from the existing YAML-backed split configuration established by prior tickets, not re-introduced here.
24. **Implementation responsibilities.** Enforce separate centralized score lineage and atomic bundles.
25. **Required unit tests.** FedAvg score identity rejection.
26. **Required property tests.** Split-scoped identities cannot collide.
27. **Required contract tests.** Manifest has checkpoint and split lineage.
28. **Required architecture tests.** Scoring port receives typed centralized identity.
29. **Required integration tests.** Synthetic centralized scoring.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Focused unit/property/contract/integration lanes.
33. **Acceptance criteria.** [ ] Separate B0 calibration and test score identities exist. [ ] No federated lineage accepted. [ ] B0 scoring execution parameters (e.g. batch/chunk size) are YAML-backed via a config schema field and mapper under `configs/execution/`; calibration-only/held-out scoring semantics cite Road §4/§9 and Arch §9.2/§13/§22 as locked-invariant authority.
34. **Completion evidence.** Changed files; types/contracts; commands/tests/results; score identities; cleanup; remaining blockers; skipped checks/reasons; no unrelated modifications.
35. **Failure and blocker behavior.** Missing or malformed score bundle is a technical invalidity, never a scientific rerun trigger.
36. **Stop conditions.** Open checkpoint lineage.
37. **Deliverables.** Centralized B0 score bundles.
38. **Final review checklist.** [ ] Calibration and held-out scoring separated.

## Part B — Mandatory Phase 2 lifecycle, boundaries, and audits

### B1. Lifecycle checklist

- [ ] Immediately before implementation, read the complete roadmap, complete architecture, this ticket, the Phase 2 index/gate, `docs/tickets/TICKET_STATUS.md`, each direct dependency, and cited authority sections.
- [ ] Verify each dependency, including `P1-T070` where required, is `DONE`.
- [ ] Set this ticket and the corresponding status-register row to `IN_PROGRESS` in the same change; record start time and current step.
- [ ] Confirm no real anchor scientific execution is permitted; keep both status locations synchronized.
- [ ] Set `IN_REVIEW` before audits; record Audit 1, Audit 2, and Audit 3 independently.
- [ ] On any blocker, set both locations to `BLOCKED` immediately, recording exact cause, evidence, affected identity, and unblock condition.
- [ ] Set `DONE` only after all acceptance criteria, validation, audits, cleanup, and completion evidence pass; record synchronized final status and evidence.
- [ ] Verify final status consistency between this ticket and `docs/tickets/TICKET_STATUS.md`; a mismatch is a blocker.

### B2. Scientific and recovered-semantics locks

These are constraints, not authorization to run a campaign: this ticket may perform only the Part A work under the synthetic-only boundary. Work only in `ProtocolTrack.DATP_ANCHOR` and the separate anchor namespace. One authorized FedAvg AE is trained per seed, selected then frozen, and its compatible calibration/test score artifacts feed B1–B4; threshold-only changes never retrain or rescore. Calibration is benign-only; attacks are evaluation-only; no test data, attacks, thresholds, or downstream outcomes select a checkpoint. B0 is a separate centralized model and lineage, not a ladder policy or FedAvg substitute. B1 is shared, B2 local, B3 is an authorized-taxonomy family mean, and B4 is exact k-means++ on `[mean,std,skew,p95]` at canonical `K=3`.

For every recovered historical fact, record subject, read-only evidence location, roadmap rule, architecture representation, recovered behavior, justified difference, tests, uncertainty, and blocker state. Do not copy/import the reference repository, reuse its layout, claim “same as old DATP,” or guess. The roadmap wins scientific conflicts; the architecture wins technical conflicts. Preserve `ANCHOR_CHECKPOINT_PROTOCOL_UNRESOLVED` when applicable; never substitute the journal schedule for historical anchor evidence.

### B3. Synthetic-only boundary

Validate only with deterministic synthetic arrays/clients/labels, fakes, test doubles, property/contract/architecture tests, lineage simulations, dry-run planning, and isolated test storage. Read-only source/reference inspection is allowed only when this ticket explicitly permits it. Real N-BaIoT training/scoring/thresholds/metrics, reduced runs, real-data smoke/debug work, scientific outputs/checkpoints, invented seeds, and result-driven tuning are forbidden. Synthetic outputs never enter an anchor scientific namespace or masquerade as evidence.

### B4. Required repository-wide audits

After implementation, audit the complete repository—not merely the diff—for DATP scope; dependency direction; framework confinement; domain/application/config/composition/CLI boundaries; import cycles and side effects; type placement; named typed requests/results and ports; no service locator/universal context/generic utility; no Pydantic beyond config; typed lineage/artifact identities; atomic persistence; deterministic process isolation; and no aliases, redirects, shims, wrappers, or compatibility paths. An authority-grounded defect is not excused as pre-existing: fix it or record a decision blocker. Run configured import-linter and pytest-archon.

Scan for raw protocol strings/dictionaries, uncontrolled tracks/namespaces/policies/roles/stages, `dict[str, Any]`, broad `Any`/object, mutable scientific specifications, tuple pseudo-objects, Boolean variant flags, optional-field inference, missing `assert_never`, generic Request/Result/Payload/Context/Manager/Handler names, and scattered defaults. Search code/tests (planning Markdown exempt) for ticket IDs, ticket files, authority-document names/sections, and planning-language claims. Audit stale docs/paths/names/status claims, unsupported completion claims, AI/banner/decorative comments, copied architecture prose, and broken links.

Audit the entire repository explicitly: `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter configuration, Ruff configuration, Pyright/Pylance configuration, pytest configuration, Sonar configuration, provider adapters, and root metadata. Do not accept “pre-existing,” “outside the diff,” “another agent,” “legacy code,” or “untouched file” exemptions.

Run full Ruff lint/format, Pyright strict, Pylance parity when available, configured Sonar gate, applicable tests, complete suite, randomized order, mandatory Nox sessions, serialized CUDA where applicable, diff, untracked-file, and cleanup checks. Record unavailable tools honestly; do not weaken tests/types/exclusions to pass.

### B5. Determinism, CUDA, and artifacts

Verify stage-specific seed derivation, no seed-position identity, no CPU fallback, no automatic batch/chunk/precision/model/data reduction, batching-first processing, spawn-safe picklable boundary objects, serialized CUDA tests, terminal CUDA-OOM behavior, no configuration-changing retry, stage lineage, typed artifact identity, and atomic commits. Test benign/attack child artifacts are readable only as a committed aggregate; incompatible reuse is rejected or `RECOMPUTE`, never silently reused.

### B6. Three independent scientific-drift audits

- **Audit 1 — Roadmap fidelity.** Check fixed encoder, FedAvg/E=1/full participation, threshold-only B1–B4, benign calibration, attack isolation, CV(FPR)-primary/AUROC-control, B0 separation, B3 taxonomy, B4 fingerprint/K, anchor/journal and seed separation, no claim promotion, no real execution, and no guesses.
- **Audit 2 — Architecture fidelity.** Check layers, typed boundaries/configuration, lineage/reuse, persistence, determinism/CUDA/batching, namespace isolation, no raw dictionaries/hidden defaults, and no compatibility scaffolding.
- **Audit 3 — Adversarial anchor review.** Cross-check authorities, ticket/dependencies, implementation/tests/evidence/configuration/identities/status for leakage, source copying, fake equivalence, B1–B4 retraining, B0/FedAvg mixing, wrong B4, namespace mixing, real-data paths, vacuous tests, incomplete audits, or unsupported tool claims.
- [ ] Correct each authority-grounded finding and rerun all three audits; preserve separate evidence for each.

### B7. Completion evidence

Record a check-by-check completion ledger: changed files and why; verified dependencies; recovered behavior/evidence where applicable; implementation steps; exact commands/results; every required unit, property, contract, architecture, integration, CUDA, and synthetic end-to-end/system test (or the Part A reason it is not applicable); full-suite and randomized-order results; Ruff lint and format; Pyright; Pylance result or honest unavailability; import-linter; pytest-archon; Nox; Sonar; documentation, raw-dictionary, `Any`, planning-reference, and no-backward-compatibility audits; cleanup; final diff; untracked files; all three audit records; blockers; remaining risks; and skipped checks with exact reasons. A mandatory check cannot be skipped for green status, and no pre-existing-defect exemption is valid.

This ticket consumes Phase 0 governance and `P1-T070`; it must not recreate them. `P2-T020` is the Phase 2 gate and cannot pass until every mandatory Phase 2 responsibility reaches a valid terminal state through the authoritative graph.

## Completion evidence — 2026-07-16T18:15:00Z

- Lifecycle and dependencies: confirmed `P2-T021` and `P1-T045` are `DONE` before starting. Discovered that the ticket's "owned" generic score types (`ClientCalibrationScoreArtifact`/`ClientTestScoreArtifact`/`CalibrationScoreArtifactSet`/`TestScoreArtifactSet`) are structurally locked to the FedAvg anchor's `CheckpointIdentity`/`CalibrationScoringIdentity`/`TestScoringIdentity` types (their `__post_init__` type-checks reject anything else) — reusing them for B0 as-is would either silently accept a FedAvg identity or be impossible to construct at all for a centralized checkpoint, contradicting "B0 cannot consume FedAvg score artifacts." This mirrors the exact type-locking discovery already made in `P2-T010` (`CheckpointDescriptor`) and `P2-T021` (`CentralizedTrainingRunResult` vs `CheckpointDescriptor`).
- Implementation: added `CentralizedClientCalibrationScoreArtifact` and `CentralizedClientTestScoreArtifact` to `domain/learning/scores.py` — structurally typed to `CentralizedCalibrationScoringIdentity`/`CentralizedTestScoringIdentity`/`CentralizedCheckpointIdentity` only, so a FedAvg identity is rejected by construction, not by a runtime branch. Added `infrastructure/learning/centralized/nbaiot_b0_scoring.py::B0CalibrationScoreGenerationWorkflow` and `B0TestScoreGenerationWorkflow`, reusing `score_client_batch` (the device-agnostic reconstruction-error function built in `P2-T011`) unchanged — the scientific scoring formula is identical across the anchor and B0 routes; only the identity/lineage typing differs, exactly as required. The test workflow requires a matching benign/attack client roster and constructs each client's benign+attack pair as one atomic object (a `CentralizedClientTestScoreArtifact` cannot exist half-built).
- Scope decision (recorded): unlike `P2-T011`'s FedAvg test-score pair, this ticket's artifacts are not wired through `FileArtifactBundleStore`'s disk-persisted atomic commit — that store's `commit_bundle`/`_declared_members` are hard-typed to `ClientTestScoreArtifact` (the FedAvg type), so reusing it for B0 would require either a parallel bundle-store implementation or weakening its typing. Given this ticket's "Allowed scope: Synthetic score-bundle implementation only" and that atomic *construction* (not disk persistence) is what Phase 2's synthetic boundary requires, this ticket delivers the correctly-typed, atomically-constructed artifact; wiring a B0-specific disk-persisted bundle store is left as an explicit, undisguised gap for whichever ticket first needs real B0 artifact persistence (not required by `P2-T023`'s threshold/evaluation/reporting scope, which consumes these artifacts in-memory).
- Configuration: added `B0ScoreGenerationConfig` (`config/schemas/execution.py`) and `map_b0_score_generation_config` (`config/mapping/execution.py`), backed by `configs/execution/b0_score_generation.yaml`, mapping to a new `B0ScoringBatchSpec` (`domain/learning/scores.py`) — following the `ScoringBatchConfig`/`configs/execution/scoring_batches.yaml` precedent from `P2-T011`, per this ticket's own explicit requirement that B0's scoring batch/chunk size be YAML-backed under `configs/execution/`. The calibration-benign-only/held-out-test scoring semantics remain code-level (the workflow classes' structure, not a config value), per the ticket's own citation of Road §4/§9 and Arch §9.2/§13/§22 as non-overridable authority.
- Tests: 2 new domain tests (`tests/unit/domain/test_score_specifications.py`: a FedAvg `TrainingIdentity` is rejected when constructing a `CentralizedClientCalibrationScoreArtifact`; only centralized identity types are accepted, and `CentralizedCheckpointIdentity` is confirmed structurally distinct from `CheckpointIdentity`) satisfy "Required unit tests: FedAvg score identity rejection." 4 new integration tests (`tests/integration/learning/test_b0_centralized_scoring.py`: calibration workflow rejects empty batches; produces one artifact per client in canonical roster order; test workflow rejects a mismatched benign/attack roster; produces a correct benign+attack pair per client). 3 new config tests (schema rejects non-positive batch size; mapping produces typed values; the tracked YAML loads and maps). All pass. Re-ran `tests/unit/domain/`, `tests/unit/config/`, `tests/integration/learning/`, `tests/unit/application/` (307 passed) to confirm no regression.
- Quality gates run for this ticket: Ruff lint and format (clean), Pyright strict (0 errors on all changed files), import-linter (7/7 contracts kept), `tests/architecture/` (45/45 passed). Full-project gates deferred to Checkpoint 3 closure (ticket 4 of 5).
- Audit 1 — roadmap fidelity: calibration remains benign-only (the new artifact type has no attack field, mirroring the existing `ClientCalibrationScoreArtifact` shape check); attack scores exist only in the test workflow; the reconstruction-error formula is reused unchanged from `P2-T011`, not reinvented; B0 identities cannot be confused with FedAvg identities anywhere in this diff; no real execution, all tests synthetic.
- Audit 2 — architecture fidelity: new domain types live in `domain/learning/scores.py` beside the types they parallel; new infrastructure code lives in `infrastructure/learning/centralized/`; the YAML→schema→mapper→frozen-spec path is followed for the one genuine execution-tunable parameter (batch size); no raw dictionaries or unjustified `Any`; the FileArtifactBundleStore reuse question was investigated and explicitly not force-fit, recorded as a scope decision rather than silently worked around.
- Audit 3 — adversarial anchor review: tested and rejected a FedAvg identity substituted for a centralized checkpoint identity at construction time; tested a mismatched benign/attack client roster; confirmed canonical (lexicographic) client ordering in both workflows' outputs; confirmed the `B0ScoreGenerationConfig`/`ScoringBatchConfig` pair are separate, non-conflicting schemas (B0 has no temporal field, matching its narrower scope). No code or layout was read from or copied out of the reference repository in this session.
- Cleanup: no scratch files created; no ticket IDs or planning-document references introduced into source/test code.
- Skipped checks and reasons: full-project Ruff/Pyright/import-linter/pytest-archon/Nox/coverage/randomized-order suite, Sonar, and CodeScene deferred to Checkpoint 3 closure per the batched-checkpoint process (this is not the checkpoint's final ticket).

