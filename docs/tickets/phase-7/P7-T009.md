# P7-T009 — Table/figure/export provenance and frozen-output regeneration

Standalone Phase 7 work contract. It governs only its stated classification and allowed scope. Authority conflicts are recorded as `BLOCKED`; they never authorize execution, status advance, or an implicit choice.

## Part A — Ticket template


- **Ticket ID.** P7-T009
- **Title.** Table/figure/export provenance and frozen-output regeneration
- **Phase.** 7
- **Status.** NOT_STARTED
- **Priority.** P0 — Blocking
- **Ticket type.** reporting
- **Owner.** report-output-auditor
- **Supporting agents.** reproducibility-auditor
- **Scientific-execution classification.** POST_CAMPAIGN_ONLY
- **Campaign scope.** POST_CAMPAIGN
- **Roadmap experiment IDs.** all
- **Architecture contracts/types owned.** NONE
- **Objective.** Verify table/figure/export provenance closure and regenerate every reported output (LaTeX/Markdown/CSV/Parquet/SVG/PNG/PDF) from frozen artifacts, confirming byte-for-byte or content+row-order consistency and that no output is sourced from logs. Regeneration scope includes every entry in the `P5-T006` expected-output inventory, including the Appendix A (`P4-T025`, B2 calibration-versus-held-out FPR analysis) output-inventory entry.
- **Why this ticket exists.** Reported outputs must be regenerable from frozen artifacts; provenance must close; no scientific value comes from a log line.
- **Authority references.** Arch §22, §19.3; Road §14.4.
- **Dependencies.** P7-T002.
- **Blocks.** P7-T011.
- **Downstream consumers.** P7-T011 consumes the regenerated-output and provenance-closure record.
- **Preconditions.** P7-T002.
- **Inputs.** Frozen artifacts; the `P5-T006` expected-artifact/table/figure/export inventory (including the Appendix A entry); reported table/figure/export outputs (LaTeX/Markdown/CSV/Parquet/SVG/PNG/PDF).
- **Allowed scope.** Output regeneration over frozen artifacts; provenance closure.
- **Forbidden actions.** No new experiment; no untraced output; no log-sourced value.
- **Required configuration or NONE.** NONE.
- **Implementation responsibilities.** Regenerate outputs from frozen artifacts; verify provenance closure and format consistency.
- **Required unit tests.** NONE.
- **Required property tests.** NONE.
- **Required contract tests.** NONE.
- **Required architecture tests.** NONE.
- **Required integration tests.** Golden: regenerated table/figure shapes and Parquet content+row order (named fixed-input regression check).
- **Required CUDA tests.** NONE.
- **Required system/end-to-end tests.** Campaign checks: provenance closes for every reported output.
- **Validation commands.** Output regeneration; provenance closure; golden lane.
- **Acceptance criteria.** [ ] Every output regenerated from frozen artifacts. [ ] Provenance closes. [ ] Formats consistent. [ ] No log-sourced value.
- **Completion evidence.** Standard five-heading report; regenerated-output and provenance-closure verdicts; no unrelated modifications.
- **Failure and blocker behavior.** `ProvenanceError`/`ReportingError` on any failure.
- **Stop conditions.** Stop on any provenance/regeneration failure.
- **Deliverables.** Regenerated outputs + provenance record.
- **Final review checklist.** [ ] Regenerable and traced.

## Part B — Mandatory Phase 7 post-campaign lifecycle and audits

### B1. Lifecycle and status discipline

- [ ] Before work, read the complete roadmap, complete architecture, this ticket, direct dependencies, downstream consumers, the Phase 7 index, all frozen Phase 6 evidence, and `docs/tickets/TICKET_STATUS.md`.
- [ ] Synchronize this ticket and its status-register row through `NOT_STARTED → READY → IN_PROGRESS → IN_REVIEW → DONE`; record timestamp, current step, evidence, blockers, and all audit outcomes.
- [ ] Do not mark `DONE` until every Part A acceptance criterion, validation command, applicable audit, cleanup check, and completion-evidence item is complete. A report, regenerated file, or passing narrow check without closed frozen provenance never suffices.
- [ ] Set both records to `BLOCKED` immediately for an authority, dependency, provenance, invariant, claim, or validation blocker, preserving evidence and the exact unblock condition.

### B2. Post-campaign immutability and claim locks

Phase 7 introduces no scientific execution, configuration change, manual result injection, outcome-driven correction, or result suppression. It audits immutable frozen evidence only. Every reported output is regenerated solely from frozen, traced artifacts; logs and untraced files cannot be evidence. Preserve the distinction between campaign-integrity validity and the observed scientific outcome, retain valid weak/null/mixed/unfavorable/opposite results, and use the roadmap's pre-committed fallback wording.

Confirm that the fixed FedAvg model and compatible score artifacts serve B1–B4 within each cell; threshold scope alone changes, calibration is benign-only, CV(FPR) is primary, and AUROC is a control. B0 has separate centralized lineage; stress tests remain outside the core causal ladder and the matched comparator has its stated role. External and temporal B1–B4 branches retain their roadmap-defined external/boundary roles, passed gates, and identities, but never alter the locked Regime-A confirmatory endpoint. Never alter an incomplete or invalid result to strengthen a claim; only the documented technical-invalidity path may have occurred before this phase.

### B3. Mandatory repository-wide audit

Audit the full repository rather than only the diff: source, tests, configurations, documentation, governance, scripts, root metadata, quality-tool configuration, artifact namespaces, campaign/attempt records, result manifests, outputs, and the status register. Check roadmap fidelity; architecture layers and framework confinement; typed configuration, identities, lineage/reuse, persistence, lifecycle, determinism, CUDA/process boundaries, and namespace separation; no raw protocol strings/dictionaries, hidden defaults, stale documentation, unsupported claims, aliases, redirects, shims, compatibility wrappers, or planning-document references in source/test code.

Run every applicable Part A validation lane and the repository quality gates required by the governing tickets; record unavailable tools and `NONE` categories honestly. Completion evidence must list changed files and reasons, exact commands and results, dependency state, frozen identities/manifests/hashes, audit results, cleanup, final diff/untracked-file inspection, blockers, risks, and skipped checks.

### B4. Independent closure audits

- **Roadmap fidelity.** Confirm the locked confirmatory endpoint, statistical direction, fallback wording, complete retention of unfavorable evidence, stress/comparator separation, external/temporal gating, output/claim mapping, originality evidence, and all reviewer-loophole defenses.
- **Architecture fidelity.** Confirm full provenance closure, typed identities and configuration, immutable/atomic persistence, hash consistency, frozen-output regeneration, namespace protection, and every applicable Architecture §29 invariant.
- **Adversarial review.** Check dependencies, status, frozen records, output provenance, claims, originality material, and cleanup for suppressed outcomes, untraced values, stale files, namespace bleed, post-hoc changes, unresolved loopholes, or claimed-but-unrun validation.

Record the three audits separately. P7-T011 is terminal only after its exact direct dependencies—including later-numbered P7-T012 and P4-T025—are terminal with required evidence.
