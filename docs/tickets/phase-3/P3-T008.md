# P3-T008 — Conditional anchor recovery, resume, and infrastructure-retry handling

Standalone Phase 3 implementation specification. The master log remains authoritative for canonical identity, phase membership, ownership, dependency graph, and gate coverage. This documentation file authorizes no work by itself.
## Part A — Ticket template


- **Ticket ID.** P3-T008
- **Title.** Conditional anchor recovery, resume, and infrastructure-retry handling
- **Phase.** 3
- **Status.** NOT_STARTED
- **Priority.** P0 — Blocking
- **Ticket type.** campaign
- **Owner.** reproducibility-auditor
- **Supporting agents.** roadmap-orchestrator
- **Scientific-execution classification.** ANCHOR_CAMPAIGN_ALLOWED
- **Campaign scope.** ANCHOR
- **Roadmap experiment IDs.** ANCHOR-B0–B4 (not a journal experiment ID)
- **Architecture contracts/types owned.** `ExecutionAttemptId` with attempt-transition rules
- **Objective.** Resume the anchor campaign safely after interruption/pressure/transient failure, skipping completed compatible stages and using only compatible recovery checkpoints while preserving the scientific `RunIdentity`; mint a new execution-attempt identity only where the lifecycle rule requires it.
- **Why this ticket exists.** Campaign scope permits resume; recovery must be compatible; infrastructure retries preserving identical scientific identity continue the same campaign; CUDA-OOM requires an explicit new attempt.
- **Authority references.** Arch §16.6–16.7, §17.3, §18; this backlog §B.
- **Dependencies.** P3-T006.
- **Blocks.** P3-T009.
- **Downstream consumers.** P3-T009 consumes resumed anchor artifacts with updated attempt identities.
- **Preconditions.** Available after P3-T006 creates the campaign identity and execution begins; activate only for an interruption, transient infrastructure failure, or compatible committed recovery state.
- **Inputs.** Arch §16.6–16.7, §17.3, §18; backlog §B.
- **Allowed scope.** Resume orchestration; recovery-compatibility validation; retry of classified transient failures.
- **Forbidden actions.** No auto `FAILED_OOM→PAUSED/RECOVERED`; no resume under a changed profile; no scientific-identity change; no retry of scientific/determinism failure.
- **Required configuration or NONE.** Frozen configuration only.
- **Implementation responsibilities.** Skip completed compatible stages; validate recovery compatibility; retain the same `ExecutionAttemptId` for a graceful-pressure boundary and a bounded classified transient-infrastructure retry; mint a new `ExecutionAttemptId` only after interruption, terminal OOM, or an approved technical-invalidity correction, while retaining `RunIdentity` unless scientific configuration changes; record the precise operation and failure evidence.

  | Event | Attempt rule | Scientific identity rule |
  |---|---|---|
  | Graceful pressure boundary | Continue the same attempt after the committed safe unit. | Unchanged. |
  | Classified `RETRYABLE_TRANSIENT` | Retry within the same attempt. | Unchanged. |
  | Interruption with a compatible committed recovery state | Create the required new attempt and resume only compatible unfinished stages. | Unchanged. |
  | Terminal OOM or approved technical-invalidity correction | Create a new attempt only after the correction record and approval; recompute affected stages only. | Unchanged unless science changed. |
  | Changed scientific configuration | Stop this campaign path. | Requires separately authorized new campaign and new `RunIdentity`. |
- **Required unit tests.** NONE.
- **Required property tests.** NONE.
- **Required contract tests.** NONE.
- **Required architecture tests.** NONE.
- **Required integration tests.** NONE.
- **Required CUDA tests.** NONE.
- **Required system/end-to-end tests.** Campaign checks: uninterrupted-vs-resumed equivalence; incompatible recovery rejected; attempt ids recorded.
- **Validation commands.** Resume execution; lineage tracing; artifact inspection.
- **Acceptance criteria.** [ ] Completed compatible stages skipped. [ ] Only compatible recovery used. [ ] The same attempt is retained for graceful-pressure and bounded transient-retry paths; a new attempt is recorded only after interruption, terminal OOM, or approved technical-invalidity correction. [ ] `RunIdentity` is unchanged. [ ] No profile change on resume. [ ] Mark `NOT_APPLICABLE` with uninterrupted-completion evidence when no recovery condition occurs.
- **Completion evidence.** Standard report + attempt identities.
- **Failure and blocker behavior.** `RecoveryStateMismatchError`/`ResumeIncompatibilityError`; bounded transient retry only.
- **Stop conditions.** Mark `NOT_APPLICABLE` when execution completes uninterrupted. Stop if recovery is incompatible or the profile changed; a scientific change requires a separately authorized new campaign with a new `RunIdentity`, never a self-authorized continuation of this ticket.
- **Deliverables.** Resume/retry records.
- **Final review checklist.** [ ] Resume ≠ new campaign.


## Part B — Mandatory Phase 3 lifecycle, campaign boundaries, and audits

### B1. Lifecycle and status discipline

- [ ] Immediately before work, read the complete roadmap, complete architecture, this ticket, every direct dependency, downstream consumers, the Phase 3 index, and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify every unconditional dependency is `DONE`; `P3-T008` is `NOT_APPLICABLE` only with uninterrupted-completion evidence.
- [ ] Keep this ticket and its status-register row synchronized through `NOT_STARTED → READY → IN_PROGRESS → IN_REVIEW → DONE`; record timestamp, current step, three audits, evidence, and every status change.
- [ ] Set both records to `BLOCKED` immediately for a blocking condition, preserving its evidence and recording the exact unblock condition.
- [ ] Before an authorized campaign action, confirm the frozen `RunIdentity`, current `ExecutionAttemptId`, campaign-integrity prerequisites, and that observed result direction has not influenced any decision.
- [ ] Do not mark `DONE` until all acceptance criteria, required validation, audits, cleanup, and complete evidence pass; a valid weak, null, mixed, unfavorable, or opposite-direction result is evidence, never a retry trigger.

### B2. Anchor-campaign scientific locks

Phase 3 is the DATP anchor campaign only. The Phase 3 campaign is neither a single process, command, job, physical write, nor attempt. A scientific configuration determines one `RunIdentity`; a changed scientific configuration is a new campaign. Each operational attempt has a distinct `ExecutionAttemptId` and never silently changes the campaign identity.

Keep resume, compatible recovery, bounded transient-infrastructure retry, diagnosed technical-invalidity correction, affected-stage recomputation, and new attempt versus new campaign distinct. Only diagnosed technical invalidity may justify corrective execution, and only with preserved failed-attempt evidence, root cause, regression tests, unchanged scientific configuration, affected-stage scope, explicit approval, and a new attempt identity where applicable. Never use a preferred result direction to justify retry, seed selection, threshold/checkpoint/eligibility/preprocessing changes, suppression, or replacement.

The anchor ladder preserves fixed FedAvg encoder/model state and compatible benign-calibration and test-score artifacts across B1–B4; threshold scope alone varies. B0 is a separate centralized lineage. B3 requires its authorized family semantics. B4 uses the four-scalar `[mean, std, skew, p95]` fingerprint at canonical `K=3`. Calibration is benign-only; attack data are evaluation-only. CV(FPR) is the operating-point endpoint and AUROC is a control. The anchor cohort and honesty diagnostic are not the journal’s ten-seed confirmatory verdict.

### B3. Execution boundary and repository-wide audits

Real execution is allowed only when this Part A classification is `ANCHOR_CAMPAIGN_ALLOWED`, only for the frozen anchor plan, and never for journal variants, datasets, stress tests, temporal cells, or confirmatory journal analysis. No CPU fallback, automatic batch/chunk/precision/model/data reduction, profile substitution, scientific-configuration mutation, test/attack leakage, or journal-namespace output is permitted.

After future implementation or campaign work, audit the entire repository—not only the diff: `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. Check dependencies and framework confinement; typed contracts and frozen configuration; typed identities, lineage, atomic persistence, lifecycle, determinism, CUDA, batching, and namespace separation; and no aliases, redirects, shims, wrappers, compatibility paths, raw protocol strings/dictionaries, `dict[str, Any]`, broad `Any`/`object`, hidden defaults, incomplete enum handling, or planning-document references in source/test code. No authority-grounded defect is dismissible as pre-existing, another agent's work, out of diff, or legacy.

Run each applicable Part A lane plus Ruff lint/format, Pyright strict, Pylance parity when available, import-linter, pytest-archon, complete and randomized-order suites, mandatory Nox sessions, serialized CUDA lanes where applicable, Sonar, diff/untracked-file inspection, and cleanup. Record unavailable tools and every `NONE`/`NOT_APPLICABLE` category honestly.

### B4. Three independent audits and evidence

- **Audit 1 — Roadmap fidelity.** Verify anchor-only scope, fixed encoder/FedAvg, benign calibration, B0–B4 semantics, seed-role separation, no result shopping, anti-rerun discipline, integrity/outcome separation, and journal unlock independent of preferred direction.
- **Audit 2 — Architecture fidelity.** Verify layers, typed configuration and lifecycle, canonical identities/fingerprints, lineage/reuse, persistence, deterministic CUDA/process behavior, artifact namespace separation, and no raw dictionaries or compatibility scaffolding.
- **Audit 3 — Adversarial cross-phase review.** Cross-check roadmap, architecture, master ticket, dependencies, status, configuration, implementation, tests, attempt/failure records, and artifacts for hidden configuration changes, failed-attempt overwrite, incomplete evidence, B1–B4 retraining, B0/FedAvg mixing, invalid B4, anchor/journal namespace bleed, outcome-driven execution, or claimed-but-unrun tools.

Record all three audit results independently. Before `DONE`, retain an evidence ledger for dependencies, changed files/reasons, exact commands/results, all test categories or their explicit `NONE` reason, identities/lineage/provenance/failures, quality gates, documentation/raw-dictionary/planning-reference/no-backward-compatibility checks, cleanup, final diff, untracked files, blockers, risks, and skipped checks. This evidence requirement does not authorize work outside the Part A classification.
