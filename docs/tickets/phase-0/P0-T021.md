# P0-T021 — Establish the command catalogue and provider thin adapters

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T021` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P0-T021
2. **Title.** Establish the command catalogue and provider thin adapters
3. **Phase.** 0
4. **Status.** NOT_STARTED (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P1 — Mandatory
6. **Ticket type.** command
7. **Owner.** roadmap-orchestrator
8. **Supporting agents.** —
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE.
13. **Objective.** Ensure the command catalogue (contract, gate, and check commands) exists canonically and that provider command adapters (for example `.claude/commands/`) are thin and point back to `ai/`.
14. **Why this ticket exists.** Commands are how a human or agent invokes gates and contracts; provider adapters must not duplicate governance content. `.claude/commands/` already exists at Phase 0 start and must be verified thin.
15. **Authority references.** `AGENTS.md` (Native Adapter Policy); `.claude/commands/`, `.github/instructions/` as they exist at the start of this ticket.
16. **Dependencies.** P0-T016.
17. **Blocks.** P0-T026.
18. **Downstream consumers.** Every governance hook (`P0-T022`–`P0-T025`) invoked through a command adapter.
19. **Preconditions.** `P0-T016` is `DONE`.
20. **Inputs.** `.claude/commands/`, `.github/instructions/`.
21. **Allowed scope.** Command adapter files and the canonical command references they point to.
22. **Forbidden actions.** No duplicated governance content inside a command adapter; no provider command that itself performs scientific execution.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Verify each command adapter is thin and references the canonical gate/contract/check it invokes; add any missing command pointer.
25. **Required unit tests.** NONE.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE.
29. **Required integration tests.** A command-adapter duplication scan.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** The command-adapter duplication scan.
33. **Acceptance criteria.** [ ] Command adapters are thin and canonical. [ ] No duplication exists. [ ] Every gate/contract/check has a corresponding command.
34. **Completion evidence.** Changed files: command adapter files. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** NONE expected.
36. **Stop conditions.** None expected.
37. **Deliverables.** The command catalogue and its provider adapters.
38. **Final review checklist.** [ ] Adapters are thin pointers only. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T016` is `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project — every command adapter under `.claude/commands/`, `.github/instructions/`, and any other provider folder — for duplicated governance content, not only the files this ticket directly edits. A defect cannot be dismissed as pre-existing or inconvenient.

### B3. Architecture boundary audit

`NOT_APPLICABLE — this ticket is command-adapter governance documentation, not source-code architecture`.

### B4. Enum, dataclass, typing, and raw-dictionary audit

`NOT_APPLICABLE — no domain or application contract exists yet`.

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of the required pattern list. Confirm no command adapter cites a ticket ID as its justification instead of the actual gate/contract/check it invokes.

### B6. Documentation and comment audit

Check every command adapter for staleness against the actual `ai/` catalogue; correct or add as needed.

### B7. Complete validation requirements

Applicable: the command-adapter duplication scan (Part A line 32); complete git-diff inspection; untracked-file/cleanup inspection. All test-suite/tooling categories remain `NONE` per Part A lines 25–31.

### B8. Pyright and Pylance requirements

`NONE` — this ticket touches no Python source.

### B9. Sonar requirements

`NONE` — this ticket touches no Python source.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — no scientific configuration or CUDA execution path exists yet in Phase 0`.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, or any Phase 1 behavior — it edits command-adapter documentation only, and specifically confirms no provider command itself performs scientific execution (Part A line 22).

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm no command adapter provides a shortcut that would let an agent bypass the campaign-phase gating (Phase 3/Phase 6) for a real experiment run.

**Audit 2 — Architecture fidelity.** `NOT_APPLICABLE — no domain/application/infrastructure behavior exists yet`.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a command adapter's thin pointer be followed to a stale or renamed target in `ai/` (a broken link), silently degrading the gate it claims to invoke to a no-op? Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is not the canonical owner of any cross-cutting governance responsibility beyond its own scope. It must remain compliant with the gates owned by `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, and `P0-T027`.
