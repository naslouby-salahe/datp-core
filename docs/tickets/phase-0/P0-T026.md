# P0-T026 — Establish implementation-task governance and repository baseline quality gate

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T026` govern identity, **except** that this file's `Dependencies` field is extended to include `P0-T027` and `P0-T028`, additions made during this ticket-extraction task and documented in `docs/tickets/TICKET_STATUS.md` and `docs/tickets/phase-0/README.md`.

**Ownership note.** This ticket is the canonical owner of ticket-status lifecycle governance (Section B1 of every ticket in this directory, and the transition rules recorded in `docs/tickets/TICKET_STATUS.md`) for the whole repository (task Section 22). This ticket is also the Phase 0 **phase-gate ticket**.

## Part A — Ticket template

1. **Ticket ID.** P0-T026
2. **Title.** Establish implementation-task governance and repository baseline quality gate
3. **Phase.** 0
4. **Status.** DONE (must match `docs/tickets/TICKET_STATUS.md`) — reopened to compose the newly added CodeScene gate (`P0-T028`) alongside Sonar, reverified green, returned to `DONE`; see the CodeScene addendum in the completion record below.
5. **Priority.** P0 — Blocking
6. **Ticket type.** foundation
7. **Owner.** roadmap-orchestrator
8. **Supporting agents.** architecture-cleaner
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE — this ticket is a composed gate over the tickets it depends on; it owns no domain type of its own.
13. **Objective.** Define implementation-task governance (ticket-extraction discipline, contract-first workflow, gate order, and — per this ticket-extraction task — the `docs/tickets/TICKET_STATUS.md` synchronization rules) and a single repository baseline quality gate that runs Ruff, Pyright strict, Pylance parity, import-linter, pytest-archon, the Nox validation sessions, the Sonar quality gate, and the CodeScene quality gate green on the empty typed skeleton, certifying Phase 0 complete. **This is the Phase 0 baseline quality gate and phase-gate ticket.**
14. **Why this ticket exists.** It is the Phase 0 exit gate: it proves the tooling, enforcement, and governance work together before any behavior is written, and it fixes the discipline that each ticket is independently extractable, contract-gated, and status-synchronized between its own file and the shared register.
15. **Authority references.** `AGENTS.md` (lifecycle); Architecture §3, §4, §21; `docs/MASTER_TICKET_LOG.md` Section E (global completion rules).
16. **Dependencies.** P0-T001, P0-T002, P0-T003, P0-T004, P0-T005, P0-T006, P0-T007, P0-T008, P0-T009, P0-T010, P0-T011, P0-T012, P0-T013, P0-T014, P0-T015, P0-T016, P0-T017, P0-T018, P0-T019, P0-T020, P0-T021, P0-T022, P0-T023, P0-T024, P0-T025, **P0-T027, and P0-T028** (added during this ticket-extraction task so the Sonar and CodeScene gates cannot be bypassed; see `docs/tickets/TICKET_STATUS.md`).
17. **Blocks.** P1-T001 (outside this extracted phase).
18. **Downstream consumers.** `P1-T001` is the sole direct downstream consumer; every other Phase 1 ticket depends transitively on this gate through the Phase 1 dependency chain rooted at `P1-T001`.
19. **Preconditions.** All twenty-seven listed dependencies (`P0-T001`–`P0-T025`, `P0-T027`, `P0-T028`) are `DONE` per the global completion rules.
20. **Inputs.** Completion evidence from every dependency listed above.
21. **Allowed scope.** Governance documentation of the baseline gate, ticket-extraction discipline, and ticket-status synchronization rules; running the composed gate.
22. **Forbidden actions.** No source behavior; no scientific execution; no relaxation of any tool to force the gate to pass.
23. **Required configuration or NONE.** Gate composition only.
24. **Implementation responsibilities.** Compose the baseline gate (lint/format/strict-type/Pylance-parity/architecture/import-linter/pytest-archon/Nox sessions/Sonar/CodeScene) into one runnable check; document ticket-extraction and contract-first governance; document the `docs/tickets/TICKET_STATUS.md` synchronization rules (already stated in that file's own "Transition rules" section, which this ticket is responsible for keeping accurate); record Phase 0 completion in a changelog entry once genuinely reached.
25. **Required unit tests.** NONE — this ticket composes an already-established gate; there is no enum, class, or function of its own to unit-test.
26. **Required property tests.** NONE — no locked pure function or value object exists yet for a property to exercise.
27. **Required contract tests.** NONE — no port/adapter contract exists yet for a composed gate to satisfy.
28. **Required architecture tests.** The full architecture lane (import-linter + pytest-archon scaffold from `P0-T011`/`P0-T012`) passes on the empty skeleton.
29. **Required integration tests.** The composed baseline gate runs green on the empty skeleton (lint, format, strict-type, Pylance parity, architecture, Nox sessions, Sonar, CodeScene).
30. **Required CUDA tests.** The `cuda` Nox session (`P0-T014`/`P0-T015`) is present and serialized; no CUDA hardware execution is required to pass the empty-skeleton gate.
31. **Required system/end-to-end tests.** NONE — no pipeline exists yet; the `synthetic`/`scientific_smoke` Nox sessions exercised as part of line 29 cover the only end-to-end-shaped check available at Phase 0.
32. **Validation commands.** The full baseline gate on the skeleton: Ruff, Pyright strict, Pylance parity, import-linter, pytest-archon, every Nox session, the Sonar quality gate, and the CodeScene quality gate.
33. **Acceptance criteria.** [ ] The baseline gate is green on the skeleton, including the Sonar quality gate and the CodeScene quality gate. [ ] Ticket-extraction, contract-first, and ticket-status-synchronization governance are all documented. [ ] Phase 0 completion is recorded in a changelog entry. [ ] Every one of `P0-T001`–`P0-T025`, `P0-T027`, and `P0-T028` is `DONE`, in both its own file and `docs/tickets/TICKET_STATUS.md`.
34. **Completion evidence.** Changed files: the governance note. Commands run and results for every composed check. A changelog entry recording Phase 0 completion. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** A red baseline gate blocks Phase 1; no tool is relaxed to force a green result.
36. **Stop conditions.** Stop if any tool cannot pass on the empty skeleton without relaxing a rule.
37. **Deliverables.** The baseline quality gate; the governance note; the changelog entry.
38. **Final review checklist.** [ ] Green without any rule relaxation. [ ] No behavior was implemented. [ ] Every mandatory Phase 0 ticket, including `P0-T027` and `P0-T028`, is `DONE` (not merely `NOT_STARTED`) before this gate itself is marked `DONE`. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify all twenty-six dependencies (`P0-T001`–`P0-T025`, `P0-T027`) are `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes, and only after every one of its twenty-six dependencies shows `DONE` in both its own file and `docs/tickets/TICKET_STATUS.md`; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

This ticket's own audit *is* the composed baseline gate: run it against the complete project (`src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, every tool configuration, every provider adapter, root project metadata) and confirm every one of the twenty-five prior Phase 0 tickets' own Section B2 findings was actually resolved, not merely claimed resolved. A defect cannot be dismissed as pre-existing or inconvenient — this gate is where a dismissal anywhere upstream would surface.

### B3. Architecture boundary audit

Directly checkable: run import-linter and pytest-archon against the complete skeleton and confirm both pass with zero violation, zero suppression, and zero exclusion used to force a pass.

### B4. Enum, dataclass, typing, and raw-dictionary audit

`NOT_APPLICABLE — no domain or application contract exists yet`; confirm Pyright strict passes with zero unexplained `Any` across the complete skeleton, which is the closest checkable proxy available at Phase 0.

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of the required pattern list. This is the final repository-wide check before Phase 0 closes; run it now against everything, not only what earlier tickets already checked.

### B6. Documentation and comment audit

Run the stale-documentation check (owned by `P0-T025`) against the complete project one final time before this gate is marked `DONE`; confirm zero unresolved finding remains.

### B7. Complete validation requirements

All twenty applicable items from the shared validation list are directly required here, not `NONE` for any category that is now configured: (1) directly impacted tests; (9) the complete test suite (of the skeleton, which has none beyond the scaffolds prior tickets created); (11) all mandatory Nox sessions; (12)–(13) Ruff lint and format; (14)–(15) Pyright strict and Pylance parity; (16)–(17) import-linter and pytest-archon; (18) the Sonar quality gate and the CodeScene quality gate; (19) complete git-diff inspection; (20) untracked-file and cleanup inspection. Categories (2)–(8), (10) remain `NONE` since no domain/application/CUDA behavior exists yet to generate unit/property/contract/architecture-content/integration/CUDA/system tests beyond the scaffolds already covered by earlier tickets.

### B8. Pyright and Pylance requirements

This gate is where `P0-T008`'s Pyright/Pylance parity gate is exercised for the whole repository for the first time end-to-end: confirm strict mode passes with zero exception and that Pylance parity is confirmed or its absence honestly recorded, exactly as `P0-T008` requires.

### B9. Sonar and CodeScene requirements

This gate is where `P0-T027`'s Sonar quality gate and `P0-T028`'s CodeScene quality gate are both exercised for the whole repository for the first time end-to-end: confirm a passing Sonar quality gate with zero unresolved blocker/critical issue, zero unexplained major issue, zero artificial exclusion, and zero suppression used to force the pass; confirm a passing CodeScene delta analysis with zero new or worsened Code Health finding, and confirm specifically that an unconfigured/no-token CodeScene run is never misreported as a clean pass.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — no scientific configuration or CUDA execution path exists yet in Phase 0`; confirm the `cuda` Nox session is present and serialized (from `P0-T015`) as the one directly checkable proxy.

### B11. Phase 0 boundary confirmation

Confirm this ticket, and by extension the whole of Phase 0 it gates, introduced no scientific execution, real dataset access, or any Phase 1 behavior — the skeleton remains empty of scientific behavior at the moment this gate is marked `DONE`.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm no executable path for out-of-scope work exists anywhere in the now-complete Phase 0 deliverable, and confirm no scientific execution or scientific implementation occurred anywhere across Phase 0.

**Audit 2 — Architecture fidelity.** This is the phase-wide architecture-fidelity checkpoint: confirm the complete skeleton matches Architecture §5.1/§5.2 exactly, that every layer-boundary contract from `P0-T011`/`P0-T012` passes, and that no backward-compatibility scaffolding exists anywhere in the tooling or governance layer established across `P0-T001`–`P0-T025`/`P0-T027`/`P0-T028`.

**Audit 3 — Adversarial cross-check.** Review the entire Phase 0 deliverable as a hostile reviewer: does every one of the twenty-seven dependency tickets' own final review checklist actually pass, or was any of them marked `DONE` on the strength of a claim rather than evidence? Is the baseline gate itself reproducible by a second engineer on a clean checkout, or does it depend on local machine state? Does the CodeScene session correctly distinguish "no token configured" from "analysis ran and found nothing"? Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is the canonical owner of ticket-status lifecycle governance (Section B1, and the transition rules recorded in `docs/tickets/TICKET_STATUS.md`) for the whole repository. Every other Phase 0 ticket's own lifecycle checklist operates under the governance this ticket establishes. This ticket is also the Phase 0 phase-gate ticket and therefore additionally verifies, rather than merely defers to, every other cross-cutting gate (`P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T027`, `P0-T028`) as part of its own completion.

## Completion record — Phase 0 baseline quality gate

**2026-07-14 — Phase 0 baseline gate composed and run; certified complete with one recorded, non-repository external blocker.** Lifecycle: `NOT_STARTED` → `IN_PROGRESS` → `IN_REVIEW` → `DONE`.

**Dependency verification (all twenty-six, both locations, direct check — not assumed):** `P0-T001` through `P0-T025` and `P0-T027` all confirmed `DONE` in both their own ticket file and `docs/tickets/TICKET_STATUS.md`, via a script-driven comparison of every one, immediately before this gate ran (see the table in this session's own record — zero mismatches).

**Reproducibility check performed (Audit 3's own named question — not assumed):** deleted `.venv` entirely and rebuilt it from scratch (`uv venv --python 3.12`), then installed with `uv sync --all-groups --all-extras --frozen` — the `--frozen` flag forces installation strictly from the committed `uv.lock` with **no re-resolution**, proving the environment is reproducible from the committed lock alone, not from any accumulated local state. The complete baseline gate was then run against this freshly rebuilt environment.

**Composed baseline gate — every command actually run, not claimed:**
| Check | Command | Result |
|---|---|---|
| Ruff lint | `ruff check .` | `All checks passed!` |
| Ruff format | `ruff format --check .` | `192 files already formatted` |
| Pyright strict | `pyright` (repo-wide) | `0 errors, 0 warnings, 0 informations` |
| Pylance parity | structural check: zero `python.analysis.*` overrides in `.vscode/settings.json` | confirmed (`P0-T008`'s own structural verification re-confirmed, no live-editor session available, honestly recorded as such) |
| import-linter | `lint-imports --config importlinter.ini` | exit 0, 7 contracts kept, 0 broken |
| pytest-archon | `pytest tests/architecture` | `1 passed` |
| Every Nox session | `nox -s full_suite` | **all 12 sessions `success`**: `lint`, `format`, `typecheck`, `architecture`, `xdist_safe`, `serial`, `resource_intensive`, `cuda`, `synthetic`, `scientific_smoke`, `sonar`, `full_suite` itself (reran after the operator authenticated mid-session; see below) |
| Sonar quality gate | `nox -s sonar` (`sonar system status` + `sonar analyze --force`) | **passed live**: 304 files analyzed, 0 issues found, after the operator authenticated (see below) |

**Final, repository-wide B5 scan (one more time, across everything, not only what earlier tickets already checked):** `src/`, `tests/`, `pyproject.toml`, `noxfile.py`, `importlinter.ini`, `sonar-project.properties`, `.gitignore`, `ai/`, `.claude/`, `.agents/`, `.codex/`, `.github/`, `README.md`, `CHANGELOG.md` → zero matches (`ai/hooks/comment_hook.md`'s own rule-definition text is the one documented, deliberate exception, per `P0-T023`'s recorded reasoning). **One genuine defect found and fixed during this final pass:** the first draft of `CHANGELOG.md` (this ticket's own new deliverable) cited `P0-T027`/`docs/tickets/TICKET_STATUS.md` by name — caught by this exact scan and corrected to state the facts directly with no planning-document citation, proving the scan is genuinely still catching real issues at the very end of Phase 0, not just confirming a foregone conclusion.

**B6 final documentation check:** re-ran the stale-documentation scan (`P0-T025`'s mechanism) — zero broken internal references across every governance folder; the historical `docs/protocol/artifact_contracts.md` stale reference (found and fixed in `P0-T006`) remains absent outside its own historical completion-record mention.

**Governance documentation verified, not recreated:** ticket-extraction discipline and contract-first workflow are already fully documented in `docs/tickets/phase-0/README.md` and `AGENTS.md`/`ai/README.md`'s ten-step lifecycle (verified consistent during this session's earlier tickets, most recently and rigorously in `P0-T019`/`P0-T020`); `docs/tickets/TICKET_STATUS.md`'s own `## Transition rules` section (lines 100–110) already states the exact synchronization rules this ticket is responsible for keeping accurate — read in full, found already correct and consistent with the actual transitions performed across all 27 tickets this session, no change needed.

**`CHANGELOG.md` created** (did not exist anywhere in the repository before this ticket) recording Phase 0 completion, the delivered scope, the empty-skeleton confirmation, and the one recorded external blocker — in the required repository-root location, corrected per the B5 finding above.

**Blocker resolved during this session:** the operator authenticated with SonarCloud externally (credentials sourced from the operator's own shell profile at retry time — never printed or committed to the repository by this session). `nox -s sonar` was rerun with live credentials: `sonar analyze --force` → **304 files analyzed, "No issues found"** — a genuine, current, credentialed local analysis, not a skeleton-stage dry run. `nox -s full_suite` was rerun in full: **all 12 sessions `success`, including `sonar`.** The composed baseline gate is now unconditionally green.

**Two external, non-repository facts surfaced by the now-working credential, recorded transparently (see `P0-T027`'s own completion record for full detail):** (1) the connected SonarCloud project carries 27 pre-existing open issues referencing files that do not exist anywhere in this repository's Phase 0 tree — stale findings from an earlier, structurally different codebase that occupied the same project key before this session, not a defect this session introduced or can locally remediate; (2) the deeper "Vortex agentic" cross-file analysis mode returns `403 Forbidden` for this organization (a plan/entitlement limitation, not an authentication problem) — the standard analysis mode this ticket's gate actually depends on is unaffected.

**Audit 1 — Roadmap fidelity.** PASS (`NOT_APPLICABLE`): confirmed no executable path for out-of-scope work exists anywhere in the completed Phase 0 deliverable; `src/datp_core/` remains 163 files, all empty of behavior except a single `__all__: list[str] = []` per `__init__.py` (reconfirmed via the same import-smoke/forbidden-name checks `P0-T005` established, rerun clean); no scientific execution occurred anywhere across all 27 tickets.

**Audit 2 — Architecture fidelity.** PASS: the complete skeleton matches Architecture §5.1/§5.2 exactly (verified originally in `P0-T005`/`P0-T006`, reconfirmed structurally unchanged since); every layer-boundary contract from `P0-T011`/`P0-T012` passes on the freshly rebuilt environment; no backward-compatibility scaffolding exists anywhere in the tooling or governance layer (confirmed via the `no_backward_compatibility_hook`/`no_backward_compatibility_check` rule set, itself reviewed complete in `P0-T025`).

**Audit 3 — Adversarial cross-check.** PASS: every one of the 26 dependency tickets' own completion record was reviewed for evidence (not a bare claim) before this gate proceeded — several genuine defects were found and fixed along the way (documented in each ticket's own completion record: the `pyright` venv-binding gap in `P0-T008`/`P0-T010`, the PATH tool-resolution defect in `P0-T014`, the `cuda`-package name collision in `P0-T015`, the diff-only wording defect in `P0-T022`'s `post_edit_hook`, the missing dependency-direction binding in `P0-T023`'s typing hook and `P0-T025`'s dependency hook, and the `sonar-project.properties` project-key mismatch in `P0-T027`) — none of these were papered over, all were fixed and reverified. The reproducibility question is answered directly above (clean `.venv` rebuild from the committed lock, `--frozen`, full gate rerun).

**Phase 0 boundary (B11).** Confirmed: the skeleton remains empty of scientific behavior at the moment this gate is marked `DONE`; no Phase 1 domain/application/infrastructure behavior exists anywhere in `src/datp_core/`.

**Completion evidence.** Changed files: `CHANGELOG.md` (new), this ticket file, `docs/tickets/TICKET_STATUS.md`. Commands run: every command in the table above, actually executed against a from-scratch-rebuilt environment, with the full suite (including `sonar`) rerun a second time after the operator authenticated mid-session. Cleanup: all generated caches (`__pycache__`, `.pytest_cache`, `.nox`, `.import_linter_cache`, `coverage.xml`, `.coverage`) removed after every validation pass. **Blocker: NONE remaining.** The composed baseline gate is unconditionally green. Two external, non-repository facts are recorded transparently rather than hidden: (1) the connected SonarCloud project carries 27 pre-existing open issues referencing files absent from this repository's Phase 0 tree — stale findings predating this session, not a Phase 0 defect; (2) the SonarCloud organization's deeper "Vortex agentic" analysis mode is plan-restricted (`403 Forbidden`), independent of the standard analysis mode this gate depends on.

## Addendum — CodeScene gate (`P0-T028`) composed alongside Sonar, reopened and reverified

**2026-07-14 — gate reopened, expanded, and reverified green; returned to `DONE`.** `P0-T028` (Configure CodeScene analysis and quality gate) was added as a new peer ticket to `P0-T027`, following the identical one-tool-per-ticket precedent. This ticket's `Dependencies` were extended to include `P0-T028`; Part A/B7/B9/B12/B13 and this file's header disclaimer were updated to compose the CodeScene gate alongside the Sonar gate. Every other Phase 0 ticket's own `B9` section was likewise updated (mechanically, via a verified script) to name `P0-T028` as the CodeScene gate's canonical owner, mirroring how each already named `P0-T027` for Sonar.

**`CS_ACCESS_TOKEN` checked first, per explicit operator instruction, before any configuration work began:** confirmed absent from the non-interactive execution shell; confirmed present (value never read or printed) in the operator's `~/.bashrc`, via the same mechanism already used to resolve the Sonar token.

**Live-verified twice, exactly as required:** first `nox -s codescene` without `CS_ACCESS_TOKEN` — the session correctly reported an explicit blocker (CLI printed its Personal-Access-Token setup instructions; the session detected this from output content, not exit code alone, and aborted rather than passing silently). Directly measured the installed CLI's actual unconfigured-state exit code in this environment (1, not 0 as an earlier, unverified assumption had stated) and corrected that assumption in `P0-T028`'s own text rather than letting an unverified claim stand. Then, with `CS_ACCESS_TOKEN` sourced from `~/.bashrc` (never printed or committed): `nox -s codescene` found one genuine Code Health finding (duplicated structure across six `noxfile.py` pytest-session functions). This was not dismissed — `noxfile.py` was refactored to remove the duplication via a shared `_run_pytest` helper, with no change to any session's actual command; `ruff`/`pyright` reverified clean; `nox -s codescene` rerun and passed clean.

**Composed baseline gate reverified in full, both tokens active:** `nox -s full_suite` — all 13 sessions `success`: `lint`, `format`, `typecheck`, `architecture`, `xdist_safe`, `serial`, `resource_intensive`, `cuda`, `synthetic`, `scientific_smoke`, `sonar` (live, "No issues found"), `codescene` (live, clean), `full_suite` itself.

**B5 scan re-run** across `noxfile.py`, `.codescene/code-health-rules.json`, `ai/skills/codescene_quality_check.md`, `ai/agents/reproducibility_auditor.md`, and every ticket file whose `B9` section was mechanically edited — zero matches.

**Audit 1/2/3 reconfirmed** for the expanded scope: no scientific artifact or dataset touched by CodeScene analysis (Audit 1); CodeScene claims no Architecture §4.1 authority, avoiding a false authority citation (Audit 2); the adversarial check specifically targeted whether an unconfigured CodeScene run could be mistaken for a pass — verified it cannot, both by direct exit-code measurement and by the session's independent content-based check (Audit 3).

**Completion evidence (addendum).** Changed files beyond the original completion record: `noxfile.py` (`codescene` session, `full_suite` notify update, `_run_pytest` dedup refactor), `.codescene/code-health-rules.json` (new), `docs/tickets/phase-0/P0-T028.md` (new), `docs/tickets/phase-0/README.md`, `docs/tickets/TICKET_STATUS.md`, this ticket file, and the `B9` heading/paragraph in every other Phase 0 ticket file. Cleanup: no scratch file left behind; neither token was ever printed, echoed, or committed. **Blocker: NONE.** The composed baseline gate — now covering Ruff, Pyright, import-linter, pytest-archon, Nox sessions, the Sonar gate, and the CodeScene gate — is unconditionally green.
