# P0-T020 — Establish the workflow catalogue

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T020` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P0-T020
2. **Title.** Establish the workflow catalogue
3. **Phase.** 0
4. **Status.** NOT_STARTED (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P0 — Blocking
6. **Ticket type.** workflow
7. **Owner.** roadmap-orchestrator
8. **Supporting agents.** —
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE.
13. **Objective.** Ensure workflow definitions (implementation, experiment, audit-only, cleanup/refactor, manuscript, result-audit) exist with an explicit gate order and completion rules matching the mandatory lifecycle in `AGENTS.md`.
14. **Why this ticket exists.** Every ticket selects one of these workflows; the catalogue must define approved paths and gate order or a ticket's "workflow" selection is meaningless. The existing `ai/workflows/` directory already covers these six; this ticket verifies and completes them.
15. **Authority references.** `AGENTS.md` (lifecycle); `ai/workflows/` as it exists at the start of this ticket.
16. **Dependencies.** P0-T016.
17. **Blocks.** P0-T026.
18. **Downstream consumers.** Every ticket in every phase selects one of these workflows.
19. **Preconditions.** `P0-T016` is `DONE`.
20. **Inputs.** The existing files under `ai/workflows/`.
21. **Allowed scope.** `ai/workflows/`.
22. **Forbidden actions.** No provider-folder duplication; no workflow that skips a gate or permits scientific execution outside Phase 3/Phase 6.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Verify/complete each workflow's gate order and completion rules; ensure `experiment_workflow.md` explicitly forbids a real run outside Phase 3/Phase 6.
25. **Required unit tests.** NONE — this ticket completes a workflow catalogue only; there is no enum, class, or function of its own to unit-test.
26. **Required property tests.** NONE — no locked pure function or value object exists yet for a property to exercise.
27. **Required contract tests.** NONE — no port/adapter contract exists yet for a governance catalogue to satisfy.
28. **Required architecture tests.** NONE — this ticket touches no source code and enforces no layer boundary.
29. **Required integration tests.** A workflow-completeness cross-check.
30. **Required CUDA tests.** NONE — no CUDA execution path exists in Phase 0.
31. **Required system/end-to-end tests.** NONE — no pipeline exists yet for an end-to-end test to exercise.
32. **Validation commands.** The workflow-completeness cross-check.
33. **Acceptance criteria.** [ ] All six workflows are present with an explicit gate order. [ ] The experiment workflow forbids a non-campaign real run explicitly, in its own text. [ ] No provider-folder duplication exists.
34. **Completion evidence.** Changed files: `ai/workflows/` files. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** NONE expected.
36. **Stop conditions.** None expected.
37. **Deliverables.** The workflow catalogue.
38. **Final review checklist.** [ ] Gate order is explicit and unambiguous in every workflow. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T016` is `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project for every reference to a workflow name (in `ai/contracts/`, provider command adapters, and this `docs/tickets/` directory) and confirm each resolves to a workflow this catalogue actually defines with an explicit gate order. A gap cannot be dismissed as pre-existing or another ticket's problem.

### B3. Architecture boundary audit

`NOT_APPLICABLE — this ticket is workflow-governance documentation, not source-code architecture`.

### B4. Enum, dataclass, typing, and raw-dictionary audit

`NOT_APPLICABLE — no domain or application contract exists yet`.

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of the required pattern list. Confirm no `ai/workflows/` file cites a ticket ID as if the workflow's existence depended on that one ticket.

### B6. Documentation and comment audit

Check every `ai/workflows/` file for staleness against the actual lifecycle in `AGENTS.md`; correct or add as needed. Confirm each workflow's gate order matches the ten-step lifecycle exactly (Intake, Contract, Pre-edit gate, Implementation or audit, Post-edit gate, No-backward-compatibility gate, Impacted tests, Structure/comment/typing/dependency gates, Cleanup, Final report), with no step silently skipped for convenience.

### B7. Complete validation requirements

Applicable: the workflow-completeness cross-check (Part A line 32); complete git-diff inspection; untracked-file/cleanup inspection. All test-suite/tooling categories remain `NONE` per Part A lines 25–31.

### B8. Pyright and Pylance requirements

`NONE` — this ticket touches no Python source.

### B9. Sonar requirements

`NONE` — this ticket touches no Python source.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — no scientific configuration or CUDA execution path exists yet in Phase 0`.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, or any Phase 1 behavior — it edits governance Markdown only, and specifically confirms `experiment_workflow.md` forbids real execution outside Phase 3/Phase 6 in its own text (Part A line 22).

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm `experiment_workflow.md`'s gate order structurally prevents a "supportive" result from being promoted to "confirmatory" and structurally prevents a rerun triggered by a weak/null/mixed/unfavorable/opposite-direction result — both are explicit roadmap locks (Section A of `docs/MASTER_TICKET_LOG.md`) that a workflow gate order can either protect or silently allow.

**Audit 2 — Architecture fidelity.** `NOT_APPLICABLE — no domain/application/infrastructure behavior exists yet`.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: does `cleanup_refactor_workflow.md` actually forbid a backward-compatibility shim being introduced "as part of cleanup," or does its gate order have a gap where that could slip through? Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is not the canonical owner of any cross-cutting governance responsibility beyond its own scope. It must remain compliant with the gates owned by `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, and `P0-T027`.
