# P0-T019 — Establish the task-contract template set

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T019` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P0-T019
2. **Title.** Establish the task-contract template set
3. **Phase.** 0
4. **Status.** NOT_STARTED (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P0 — Blocking
6. **Ticket type.** agent-governance
7. **Owner.** roadmap-orchestrator
8. **Supporting agents.** —
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE.
13. **Objective.** Ensure task-contract templates (task, implementation, experiment, audit, cleanup/refactor, manuscript) exist and require a contract before any work begins, consistent with the mandatory workflow lifecycle in `AGENTS.md`.
14. **Why this ticket exists.** `CLAUDE.md`/`AGENTS.md` require a contract from `ai/contracts/` before any task begins; the templates must be complete and current for every ticket's workflow to actually be gated. The existing `ai/contracts/` directory already covers these six; this ticket verifies and completes them.
15. **Authority references.** `AGENTS.md` (workflow lifecycle, contract gate); `ai/contracts/` as it exists at the start of this ticket.
16. **Dependencies.** P0-T016.
17. **Blocks.** P0-T026.
18. **Downstream consumers.** Every implementation ticket in every subsequent phase, which must open a contract before work begins.
19. **Preconditions.** `P0-T016` is `DONE`.
20. **Inputs.** The existing files under `ai/contracts/`.
21. **Allowed scope.** `ai/contracts/`.
22. **Forbidden actions.** No provider-folder duplication; no contract template that permits scope drift or a backward-compatibility exception.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Verify each of the six contract templates exists and encodes the full lifecycle (intake → contract → pre-edit gate → implementation/audit → post-edit gate → no-backward-compatibility gate → impacted tests → structure/comment/typing/dependency gates → cleanup → final report); add any missing template.
25. **Required unit tests.** NONE — this ticket completes a contract-template catalogue only; there is no enum, class, or function of its own to unit-test.
26. **Required property tests.** NONE — no locked pure function or value object exists yet for a property to exercise.
27. **Required contract tests.** A contract-completeness cross-check against the required template list.
28. **Required architecture tests.** NONE — this ticket touches no source code and enforces no layer boundary.
29. **Required integration tests.** NONE — a contract-template catalogue has no runtime behavior to integrate; the completeness cross-check in line 27 is the only check this ticket owns.
30. **Required CUDA tests.** NONE — no CUDA execution path exists in Phase 0.
31. **Required system/end-to-end tests.** NONE — no pipeline exists yet for an end-to-end test to exercise.
32. **Validation commands.** The contract-completeness cross-check.
33. **Acceptance criteria.** [ ] All six contract templates are present and current. [ ] The contract-first lifecycle is enforced by each template's own structure. [ ] No provider-folder duplication exists.
34. **Completion evidence.** Changed files: `ai/contracts/` files. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** NONE expected.
36. **Stop conditions.** None expected.
37. **Deliverables.** The contract template set.
38. **Final review checklist.** [ ] A contract is required before work begins, structurally, not merely by convention. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T016` is `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project for every reference to a contract template (in `ai/workflows/`, provider folders, and this `docs/tickets/` directory) and confirm each resolves to a template this ticket's set actually defines. A gap cannot be dismissed as pre-existing or another ticket's problem.

### B3. Architecture boundary audit

`NOT_APPLICABLE — this ticket is contract-governance documentation, not source-code architecture`.

### B4. Enum, dataclass, typing, and raw-dictionary audit

`NOT_APPLICABLE — no domain or application contract exists yet`.

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of the required pattern list. Confirm no `ai/contracts/` template cites a ticket ID as if the template's existence depended on that one ticket.

### B6. Documentation and comment audit

Check every `ai/contracts/` template for staleness against the actual lifecycle in `AGENTS.md`; correct or add as needed.

### B7. Complete validation requirements

Applicable: the contract-completeness cross-check (Part A line 32); complete git-diff inspection; untracked-file/cleanup inspection. All test-suite/tooling categories remain `NONE` per Part A lines 25–31.

### B8. Pyright and Pylance requirements

`NONE` — this ticket touches no Python source.

### B9. Sonar requirements

`NONE` — this ticket touches no Python source.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — no scientific configuration or CUDA execution path exists yet in Phase 0`.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, or any Phase 1 behavior — it edits governance Markdown only.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm the `experiment_contract.md` template itself structurally forbids real execution outside Phase 3/Phase 6 (this is the contract every future scientific-execution ticket must open, so its own text is the load-bearing guard).

**Audit 2 — Architecture fidelity.** `NOT_APPLICABLE — no domain/application/infrastructure behavior exists yet`.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a future ticket open a generic `task_contract_template.md` instead of the more specific `experiment_contract.md` to avoid that template's stricter scientific-execution guard? Is there a rule (in this ticket's own deliverable) requiring the *most specific applicable* contract, not merely *a* contract? Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is not the canonical owner of any cross-cutting governance responsibility beyond its own scope. It must remain compliant with the gates owned by `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, and `P0-T027`.
