# P0-T018 — Complete the canonical skill catalogue

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T018` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P0-T018
2. **Title.** Complete the canonical skill catalogue
3. **Phase.** 0
4. **Status.** DONE (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P0 — Blocking
6. **Ticket type.** skill
7. **Owner.** roadmap-orchestrator
8. **Supporting agents.** —
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE.
13. **Objective.** Ensure a reusable skill exists for every check this backlog relies on: task-contract creation, architecture placement, immutable domain modeling, enum/value-object/dataclass selection, raw-dictionary avoidance, hidden-default prevention, configuration mapping, dependency direction, lineage/identity design, deterministic seed handling, CUDA safety, batching/resource preflight, persistence atomicity, checkpoint/recovery separation, test selection, threshold-policy verification, metric verification, statistical verification, feasibility assessment, experiment readiness, artifact reuse, result traceability, claim discipline, and final review.
14. **Why this ticket exists.** Skills are the pass/fail checks invoked by tickets and hooks; the set must be complete or a hook has nothing concrete to bind to. The existing `ai/skills/` directory already covers many of these; gaps (lineage/identity design, CUDA safety, batching/resource preflight, persistence atomicity, checkpoint/recovery separation, artifact reuse, result traceability) must be closed.
15. **Authority references.** This ticket's own required-skill list (Part A line 13); `ai/skills/` as it exists at the start of this ticket.
16. **Dependencies.** P0-T016.
17. **Blocks.** P0-T022, P0-T026.
18. **Downstream consumers.** Every hook (`P0-T022`–`P0-T025`) that invokes a skill as its pass/fail check.
19. **Preconditions.** `P0-T016` is `DONE`.
20. **Inputs.** The existing files under `ai/skills/`.
21. **Allowed scope.** `ai/skills/` files.
22. **Forbidden actions.** No provider-folder duplication; no skill that authorizes scientific execution outside a campaign phase.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Map each required skill in Part A line 13 to an existing `ai/skills/` file or add it; give every skill explicit, objective pass/fail criteria (never "use good judgment").
25. **Required unit tests.** NONE — governance documents.
26. **Required property tests.** NONE — no locked pure function or value object exists yet; this ticket completes a skill catalogue only.
27. **Required contract tests.** NONE — no port/adapter contract exists yet for a governance catalogue to satisfy.
28. **Required architecture tests.** NONE — this ticket touches no source code and enforces no layer boundary.
29. **Required integration tests.** A catalogue-completeness cross-check against the required-skill list.
30. **Required CUDA tests.** NONE — no CUDA execution path exists in Phase 0.
31. **Required system/end-to-end tests.** NONE — no pipeline exists yet for an end-to-end test to exercise.
32. **Validation commands.** The catalogue-completeness cross-check.
33. **Acceptance criteria.** [ ] Every required skill is present with explicit pass/fail criteria. [ ] No provider-folder duplication exists.
34. **Completion evidence.** Changed files: `ai/skills/` files. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** NONE expected.
36. **Stop conditions.** None expected.
37. **Deliverables.** The completed skill catalogue.
38. **Final review checklist.** [ ] Every skill has objective, checkable criteria, not a vague standard. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T016` is `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project for every reference to a skill name (in `ai/hooks/`, provider folders, and this `docs/tickets/` directory's own audit sections B2–B12, which reference skill-adjacent checks by concept) and confirm each resolves to a skill this catalogue actually defines with objective criteria. A gap cannot be dismissed as pre-existing or another ticket's problem.

### B3. Architecture boundary audit

`NOT_APPLICABLE — this ticket is skill-governance documentation, not source-code architecture`.

### B4. Enum, dataclass, typing, and raw-dictionary audit

`NOT_APPLICABLE — no domain or application contract exists yet`. Note, however, that this ticket's own required-skill list includes "raw-dictionary avoidance" and "hidden-default prevention" as skills to be defined — confirm those two skill files exist with criteria precise enough to actually drive Section B4 of every later ticket in every later phase, since this catalogue is what Phase 1+ tickets' own B4 audits will cite.

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of the required pattern list. Confirm no `ai/skills/` file cites a ticket ID as its justification instead of the actual rule it checks.

### B6. Documentation and comment audit

Check every `ai/skills/` file for staleness against the actual required-skill list; correct or add as needed. Confirm no two skill files check the same thing under different names.

### B7. Complete validation requirements

Applicable: the catalogue-completeness cross-check (Part A line 32); complete git-diff inspection; untracked-file/cleanup inspection. All test-suite/tooling categories remain `NONE` per Part A lines 25–31.

### B8. Pyright and Pylance requirements

`NONE` — this ticket touches no Python source.

### B9. Sonar and CodeScene requirements

`NONE` — this ticket touches no Python source. `P0-T027` remains the canonical owner of the Sonar quality gate and `P0-T028` the canonical owner of the CodeScene quality gate, for the repository generally.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — no scientific configuration or CUDA execution path exists yet in Phase 0`. Note this ticket's own required-skill list includes "deterministic seed handling" and "CUDA safety" as skills to define — confirm those skill files' pass/fail criteria actually match the complete determinism rules of Section B10 used throughout this `docs/tickets/` directory (scattered global seeds, seed-list position used as identity, nondeterministic algorithms, silent CPU fallback, silent batch-size reduction, automatic scientific-configuration mutation, automatic retry after CUDA OOM, unsafe CUDA multiprocessing, incorrect process start methods, non-picklable process-boundary objects, unstable serialization, raw-float hashing, `NaN`/infinity in identity-bearing fields, hidden environment influence on scientific identity, unrecorded configuration changes, and unsafe parallel CUDA execution), so Phase 1+ tickets have a consistent, single-sourced check to cite.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, or any Phase 1 behavior — it edits governance Markdown only, and explicitly confirms (Part A line 22) that no skill authorizes scientific execution outside a campaign phase.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm no skill's pass criteria could be satisfied by a scientifically incorrect but superficially plausible result (for example a "statistical verification" skill that checks a p-value exists but not that the correct one-sided BCa test was used).

**Audit 2 — Architecture fidelity.** `NOT_APPLICABLE — no domain/application/infrastructure behavior exists yet`. The one forward-looking check: does the "lineage/identity design" skill's criteria actually match Architecture §13's lineage chain, not a generic notion of "traceability"?

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: is any skill's pass/fail criterion phrased so loosely ("looks reasonable," "follows best practice") that this task's own Section 5 prohibition on vague requirements would be violated by the skill catalogue itself? Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is not the canonical owner of any cross-cutting governance responsibility beyond its own scope. It must remain compliant with the gates owned by `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, and `P0-T027`.

## Completion record

**2026-07-14 — skill catalogue completed.** `P0-T016` verified `DONE` first. Lifecycle: `NOT_STARTED` → `IN_PROGRESS` → `IN_REVIEW` → `DONE`.

**Gap analysis (all 22 existing `ai/skills/*.md` files read in full before deciding, not assumed from the ticket's own summary):** the ticket's own "why this exists" text names only 7 known gaps (lineage/identity design, CUDA safety, batching/resource preflight, persistence atomicity, checkpoint/recovery separation, artifact reuse, result traceability). Direct line-by-line comparison against the full 24-item required list found **15 genuinely uncovered checks** — the 7 named plus 8 more the ticket text did not enumerate: task-contract creation, architecture placement, immutable domain modeling, enum/value-object/dataclass selection, configuration mapping, dependency direction, and feasibility assessment. All 15 were added as new skill files rather than trusting the ticket's own incomplete gap list.

**Deliberate one-file-covers-two-requirements mappings (recorded explicitly, not silent):**
- "raw-dictionary avoidance" and "hidden-default prevention" both map to the pre-existing `avoid_raw_dict_defaults_check.md`, whose blocking rules already separately name raw protocol dicts (the first concern) and hidden/unexplained defaults (the second) with distinct pass/fail criteria for each — splitting into two files would create a decorative near-duplicate, not a clearer check.
- "metric verification" and "statistical verification" both map to the pre-existing `statistical_validity_check.md`, whose blocking rules already separately name "incorrect metric direction" (metric verification) alongside BCa/Wilcoxon/Cliff's-delta misuse (statistical verification).
- "claim discipline" maps to the pre-existing `confirmatory_claim_guard.md` (its `Purpose` is literally "protect the sole confirmatory endpoint" — the definition of claim discipline), not a new file, since `claim_evidence_map.md` already separately owns the "map claims to evidence" concern.
- "test selection" maps to `test_quality_check.md` (its own `When to apply` line already says "added, edited, **selected**, or skipped").
- "final review" maps to `reviewer_attack_check.md` (the pre-existing adversarial pre-finalization check).

**15 new skill files created**, each with the same five-section structure as the existing 22 (`Purpose`, `When to apply`, `Blocking rules`, `Pass criteria`, `Fail criteria`) and explicit, objective, checkable criteria — never "use good judgment": `task_contract_creation_check.md`, `architecture_placement_check.md`, `immutable_domain_modeling_check.md`, `enum_value_object_selection_check.md`, `configuration_mapping_check.md`, `dependency_direction_check.md`, `lineage_identity_design_check.md`, `deterministic_seed_handling_check.md`, `cuda_safety_check.md`, `batching_resource_preflight_check.md`, `persistence_atomicity_check.md`, `checkpoint_recovery_separation_check.md`, `feasibility_assessment_check.md`, `artifact_reuse_check.md`, `result_traceability_check.md`.

**Validation commands run:**
- Completeness cross-check: all 24 required checks mapped to an existing file (5 pre-existing direct matches, 5 pre-existing shared/repurposed matches recorded above, 15 new files) — every referenced file confirmed to exist via `ls`/direct check; `ai/skills/` now holds 37 files total (22 + 15).
- B5 scan across `ai/skills/` → zero matches.
- Vague-criteria scan (Audit 3): `grep` for "use good judgment," "looks reasonable," "follows best practice," "seems fine," "as appropriate" across all 37 files → zero matches.
- Duplicate-purpose scan: every file's `## Purpose` line extracted and compared → no two files state the same purpose.

**Audit 1 — Roadmap fidelity.** PASS (`NOT_APPLICABLE`): reviewed every new skill's pass criteria for the specific failure mode named (a check satisfiable by a "superficially plausible but scientifically incorrect" result) — each new skill's fail criteria name a concrete, checkable failure (e.g. `cuda_safety_check`: "a CUDA OOM triggers an automatic retry," not "CUDA handling looks wrong").

**Audit 2 — Architecture fidelity.** `NOT_APPLICABLE` (no source behavior); the one forward-looking check named by this ticket — does `lineage_identity_design_check.md` match the architecture's actual lineage-chain concepts (`ArtifactId`, `StageFingerprint`, reuse compatibility, integrity-conflict rejection) rather than a generic "traceability" notion — passed on direct read.

**Audit 3 — Adversarial cross-check.** PASS: every new skill's criteria are objective and checkable (verified above); no two skills — old or new — check the same concern under different names (verified via the duplicate-purpose scan and the explicit shared-mapping decisions recorded above, which were made deliberately rather than accidentally overlapping).

**Phase 0 boundary (B11).** Confirmed: no skill authorizes scientific execution outside a campaign phase — every new skill's blocking rules target a structural/typing/lifecycle defect, never a scientific-execution grant.

**Completion evidence.** Changed files: 15 new `ai/skills/*.md` files, this ticket file, `docs/tickets/TICKET_STATUS.md`. No pre-existing skill file needed modification (the shared-mapping decisions required only recording, not editing). Cleanup: no scratch file left behind. Blocker: NONE.
