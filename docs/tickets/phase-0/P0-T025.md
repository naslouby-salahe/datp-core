# P0-T025 — Implement dependency/no-BC/command-sync/cleanup/final-report/impacted-test hooks

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T025` govern identity.

**Ownership note.** This ticket is the canonical owner of the stale-documentation enforcement mechanism (Section B6 of every ticket in this directory) for the whole repository (task Section 22).

## Part A — Ticket template

1. **Ticket ID.** P0-T025
2. **Title.** Implement dependency/no-BC/command-sync/cleanup/final-report/impacted-test hooks
3. **Phase.** 0
4. **Status.** DONE (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P0 — Blocking
6. **Ticket type.** hook
7. **Owner.** architecture-cleaner
8. **Supporting agents.** reproducibility-auditor, compatibility-blocker
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE.
13. **Objective.** Ensure the dependency, no-backward-compatibility, command/documentation-synchronization, cleanup, final-report, and impacted-test hooks are defined and blocking, including a test-order-independence gate, and that the command/documentation-synchronization hook is explicitly the enforcement mechanism for stale-documentation detection across the whole repository.
14. **Why this ticket exists.** These cross-cutting gates enforce dependency direction, the clean-break (no-backward-compatibility) policy, documentation/command synchronization, cleanup, the mandated final-report format, and impacted-test execution with order independence. The existing hooks are present at Phase 0 start; this ticket ensures completeness, the impacted-test/order-independence coverage, and folds in this ticket-conversion task's stale-documentation audit requirement (Section 13) as an explicit responsibility of the command/documentation-synchronization hook.
15. **Authority references.** `AGENTS.md` (no-backward-compatibility policy, final-report format, lifecycle steps 6–9); Architecture §21 (order independence); `ai/hooks/dependency_hook.md`, `no_backward_compatibility_hook.md`, `readme_makefile_hook.md`, `cleanup_hook.md`, `final_report_hook.md`, `test_hook.md`, all as they exist at the start of this ticket.
16. **Dependencies.** P0-T014.
17. **Blocks.** P0-T026.
18. **Downstream consumers.** Every implementation ticket in every subsequent phase.
19. **Preconditions.** `P0-T014` is `DONE`.
20. **Inputs.** `AGENTS.md`; Architecture §21.
21. **Allowed scope.** The six hook files and their provider adapter pointers.
22. **Forbidden actions.** No provider-folder duplication; no non-blocking downgrade of any of the six gates; no allowance of a legacy alias, shim, or redirect anywhere in the no-backward-compatibility hook's own rule text.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Bind the dependency hook to import-linter/pytest-archon (`P0-T011`/`P0-T012`); bind the no-BC hook to the clean-break policy (no aliases, redirects, shims, fake compatibility, deprecated APIs, legacy config keys, legacy CLI flags, legacy output names); bind the command/documentation-synchronization hook to keep commands and docs aligned with actual behavior *and* to the stale-documentation audit list of task Section 13 (stale paths, stale package names, retired IDs, references to nonexistent files, contradictory documents, inaccurate docstrings, claimed checks not actually run); bind the cleanup hook to no temp files/scratch artifacts/audit clutter/stray generated files; bind the final-report hook to the exact required headings (Changed Files, Checks Run, Cleanup Result, Remaining Risks, Skipped Checks); bind the impacted-test hook to run affected suites and assert order independence.
25. **Required unit tests.** NONE — checklist gates; the order-independence executable check lives in the test lanes established by `P0-T009`/`P0-T014`.
26. **Required property tests.** NONE — no locked pure function or value object exists yet; this ticket defines checklist-gate documentation only.
27. **Required contract tests.** NONE — no port/adapter contract exists yet for checklist-gate documentation to satisfy.
28. **Required architecture tests.** NONE — this ticket touches no source code and enforces no layer boundary directly (its dependency hook references `P0-T011`/`P0-T012`, already established).
29. **Required integration tests.** Hook dry-runs for each of the six gates; an impacted-test lane run; an order-independence run.
30. **Required CUDA tests.** NONE — no CUDA execution path exists in Phase 0.
31. **Required system/end-to-end tests.** NONE — no pipeline exists yet for an end-to-end test to exercise.
32. **Validation commands.** The six hook dry-runs; the impacted-test lane; the order-independence run.
33. **Acceptance criteria.** [ ] All six hooks are defined and blocking. [ ] Final-report headings are enforced exactly as `AGENTS.md` specifies. [ ] The impacted-test/order-independence gate exists and runs. [ ] The command/documentation-synchronization hook explicitly covers the stale-documentation audit list. [ ] No provider-folder duplication exists.
34. **Completion evidence.** Changed files: the six hook definitions. Commands run and results for each dry-run. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** Any failure blocks completion; none is bypassed.
36. **Stop conditions.** None expected.
37. **Deliverables.** The six hook definitions.
38. **Final review checklist.** [ ] The clean-break policy is enforced with no exception. [ ] The final-report format is enforced exactly. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T014` is `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Run the command/documentation-synchronization hook's newly bound stale-documentation check against the complete project (`src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, provider adapters, root project metadata) and confirm zero stale reference exists undetected at the point this ticket completes. A defect cannot be dismissed as pre-existing or inconvenient — this ticket's whole purpose is to make such defects impossible to dismiss going forward.

### B3. Architecture boundary audit

`NOT_APPLICABLE — this ticket is hook-governance documentation, not source-code architecture`; the dependency hook it binds enforces Architecture §3's boundaries via `P0-T011`/`P0-T012`, but this ticket's own deliverable is the hook definition.

### B4. Enum, dataclass, typing, and raw-dictionary audit

`NOT_APPLICABLE — no domain or application contract exists yet`.

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of the required pattern list. Confirm none of the six hook files cites a ticket ID as its justification instead of the actual `AGENTS.md`/Architecture rule.

### B6. Documentation and comment audit

This ticket owns this check for the whole repository going forward; its own self-audit: confirm the command/documentation-synchronization hook's bound rule list actually names every item from task Section 13 (stale documentation/paths/package names, retired IDs, references to nonexistent files, contradictory documents, inaccurate docstrings, comments that no longer match behavior, copied AI explanations, banner/separator/verbose-narrative comments, misleading status claims, duplicated documentation, undocumented behavior changes, claimed checks not actually run), not a partial subset, and run it now against the complete repository.

### B7. Complete validation requirements

Applicable: the six hook dry-runs, the impacted-test lane run, and the order-independence run (Part A line 32); complete git-diff inspection; untracked-file/cleanup inspection. This ticket is itself a contributing source for validation item 1 (directly impacted tests) and item 10 (randomized test ordering, verified end-to-end here) referenced by every other Phase 0 and later-phase ticket's own Section B7.

### B8. Pyright and Pylance requirements

`NONE` — this ticket touches no Python source. `P0-T008` remains the canonical owner of Pyright/Pylance configuration parity generally.

### B9. Sonar and CodeScene requirements

`NONE` — this ticket touches no Python source. `P0-T027` remains the canonical owner of the Sonar quality gate generally; note that the final-report hook's required headings should be extended, once `P0-T027` exists, to include Sonar quality-gate status among "Checks Run" — record this as a forward dependency, not implemented here. `P0-T028` is the independent, canonical owner of the CodeScene quality gate; this ticket must remain compliant with that gate as well.

### B10. Determinism and reproducibility audit

This ticket owns test-order-independence verification end-to-end (building on `P0-T009`'s configuration): confirm the order-independence run actually executes the same suite twice in two different random orders and compares results, rather than merely asserting randomization is "enabled." All CUDA/seed/batch items remain `NOT_APPLICABLE — no CUDA execution path exists yet in Phase 0`.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, or any Phase 1 behavior — it edits governance Markdown and runs dry-runs/lane checks against the existing skeleton only.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm the no-BC hook's rule text would block a future ticket from reintroducing a retired seed-cohort concept or a deprecated threshold-construction name "for compatibility."

**Audit 2 — Architecture fidelity.** `NOT_APPLICABLE — no domain/application/infrastructure behavior exists yet`; the dependency hook's binding to `P0-T011`/`P0-T012` is the one structurally checkable item, confirmed under B3 above.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a future ticket's final report omit the "Remaining Risks" heading by writing "None" as free text elsewhere instead of under the required heading, technically satisfying a loose reading of the final-report hook while violating its intent? Tighten the hook's own wording if so. Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is the canonical owner of the stale-documentation enforcement mechanism (Section B6) for the whole repository. Every other Phase 0 ticket's own documentation audit runs under the gate this ticket establishes.

## Completion record

**2026-07-14 — six cross-cutting hooks verified/fixed.** `P0-T014` verified `DONE` first. Lifecycle: `NOT_STARTED` → `IN_PROGRESS` → `IN_REVIEW` → `DONE`.

**Three genuine, serious gaps found by reading all six hooks' actual content, not their filenames:**
1. `dependency_hook.md`'s entire content was about "prefer an official library over custom code" — it said **nothing** about layer-dependency direction, import-linter, or pytest-archon, despite Part A's explicit requirement ("bind the dependency hook to import-linter/pytest-archon"). This is a content/name mismatch that would have left the dependency hook structurally unable to catch a layer-boundary violation. Fixed by adding the import-linter/pytest-archon binding as the hook's primary check, keeping the pre-existing official-library-simplification content (still valid, still referenced by its own skill).
2. `readme_makefile_hook.md` covered only README/Makefile/config-name synchronization — a small fraction of the task Section 13 stale-documentation list this ticket's ownership note requires it to enforce (stale paths/package names, retired IDs, references to nonexistent files, contradictory documents, inaccurate docstrings, comments not matching behavior, copied AI explanations, banner/separator/verbose-narrative comments, misleading status claims, duplicated documentation, undocumented behavior changes, claimed-checks-not-run). Expanded to name all twelve items explicitly.
3. `test_hook.md` had no order-independence assertion at all, despite this ticket's explicit "bind the impacted-test hook to run affected suites and assert order independence" requirement and B10's explicit demand that the check "actually execute the same suite twice in two different random orders and compare results, rather than merely asserting randomization is 'enabled.'" Fixed by adding an explicit requirement to run the suite twice under different seeds and compare, with failure behavior requiring a root-cause fix rather than seed-pinning to hide an order-dependence defect.

**Audit 3's own named concern fixed in `final_report_hook.md`:** its prior wording ("remaining risks and skipped checks are reported with reasons or `None`") could be satisfied by writing "no remaining risks" as free text anywhere in the report without the actual `Remaining Risks` heading present — exactly the loophole Audit 3 names. Tightened to require "all five required headings... present as literal Markdown headings, in this exact wording... Stating 'no remaining risks' as free text elsewhere in the report, without the `Remaining Risks` heading itself present, does not satisfy this check."

**`no_backward_compatibility_hook.md` and `cleanup_hook.md` reviewed and found already complete** — no change needed; both already enumerate the full clean-break/no-clutter rule sets this ticket requires.

**Validation commands run (real, not simulated):**
- Dependency-hook dry-run: `lint-imports --config importlinter.ini` (7 contracts kept) and `pytest tests/architecture` (1 passed) — both already-passing baselines re-confirmed under the newly-explicit hook binding.
- **Order-independence run (B10's own explicit demand — not merely "randomization enabled"):** ran the complete current test suite twice with different explicit seeds (`--randomly-seed=111` and `--randomly-seed=999`) and compared. Confirmed via `-v` output that the two runs collected tests in **genuinely different orders** (e.g. the CUDA integration tests ran first under seed 999 but third under seed 111) — proving real reordering happened, not a no-op — and both orders produced identical results (`5 passed` both times, same tests). No order-dependence defect found in the current (trivial, still-Phase-0) suite.
- B6 self-audit of the newly-expanded stale-documentation list, run against the complete repository now: broken-internal-reference scan across `ai/`, `.claude/`, `.codex/`, `.agents/`, `.github/` → zero broken references; the `docs/protocol/artifact_contracts.md` stale reference found and fixed back in `P0-T006` reconfirmed fully absent (the only remaining occurrences are inside `P0-T006.md`'s own historical completion record, correctly describing the past finding, not a live violation).
- `nox -s full_suite` (all 11 sessions) rerun after all hook edits → all `success`, no regression.
- B5 scan across all six hook files → zero matches.

**Audit 1 — Roadmap fidelity.** PASS (`NOT_APPLICABLE`): confirmed `no_backward_compatibility_hook.md`'s existing rule text ("no legacy aliases... no dual old/new behavior... no silent fallback for stale values") would block a future ticket from reintroducing a retired seed-cohort concept or deprecated threshold-construction name "for compatibility" — the rule is general enough to cover that specific case without needing a scientific-concept-specific addition.

**Audit 2 — Architecture fidelity.** `NOT_APPLICABLE` — no domain/application/infrastructure behavior exists yet; the dependency hook's binding to import-linter/pytest-archon (B3's one structurally checkable item) is confirmed live via the dry-run above.

**Audit 3 — Adversarial cross-check.** PASS after the fix: the final-report loophole this audit names is closed by requiring the literal heading's presence, not merely the word "None" appearing somewhere in the text.

**Phase 0 boundary (B11).** Confirmed: only governance Markdown was edited; the dry-runs and order-independence run exercised only the existing empty-skeleton test suite.

**Completion evidence.** Changed files: `ai/hooks/dependency_hook.md`, `readme_makefile_hook.md`, `test_hook.md`, `final_report_hook.md` (all substantively changed); `no_backward_compatibility_hook.md`, `cleanup_hook.md` (reviewed, unchanged); this ticket file; `docs/tickets/TICKET_STATUS.md`. Cleanup: all generated tool caches removed after validation. Blocker: NONE.
