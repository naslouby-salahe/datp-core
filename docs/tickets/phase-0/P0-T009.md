# P0-T009 — Configure pytest, coverage, timeout, and order-randomization

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T009` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P0-T009
2. **Title.** Configure pytest, coverage, timeout, and order-randomization
3. **Phase.** 0
4. **Status.** NOT_STARTED (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P0 — Blocking
6. **Ticket type.** foundation
7. **Owner.** implementation-engineer
8. **Supporting agents.** —
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `TestSuiteKind` (Architecture §6.7) — marker registration documented here; the enum itself is implemented in Phase 1 test support.
13. **Objective.** Configure pytest with coverage (pytest-cov), per-test timeouts (pytest-timeout), and order randomization (pytest-randomly, disabled only for the serialized CUDA lane), plus marker registration for every test suite named in Architecture §21.2, and create the empty directory skeleton for the parts of Architecture §21.2's test tree that no other Phase 0 ticket owns.
14. **Why this ticket exists.** Test-infrastructure discipline — timeouts, order independence, coverage gates — is a distinct responsibility from the CUDA lane (`P0-T015`) and from property-test configuration (`P0-T010`). Without it, a flaky or order-dependent test could pass by accident and hide a real defect.
15. **Authority references.** Architecture §4.1 (pytest/pytest-cov/pytest-timeout/pytest-randomly), §21 (test architecture), §21.2 (test tree and markers), §21.3 (configuration ownership: `pyproject.toml` holds markers only, not profile data).
16. **Dependencies.** P0-T002.
17. **Blocks.** P0-T010, P0-T026, P0-T027.
18. **Downstream consumers.** `P0-T010` (Hypothesis profiles), `P0-T012` (pytest-archon scaffold), `P0-T013` (syrupy scaffold), `P0-T014` (Nox sessions), `P0-T015` (CUDA/xdist lanes), `P0-T027` (Sonar coverage ingestion).
19. **Preconditions.** `P0-T002` is `DONE`.
20. **Inputs.** Architecture §21.2's test-suite table.
21. **Allowed scope.** pytest configuration tables and marker registration in `pyproject.toml`; the empty, side-effect-free test-tree directory skeleton named in Implementation responsibilities below.
22. **Forbidden actions.** No test-profile field content inside `pyproject.toml` (profile data belongs under `configs/tests/`, owned by Phase 1 test support); no order-dependence exemption anywhere except the explicitly serialized CUDA lane.
23. **Required configuration or NONE.** Marker registration only; profile data belongs to `configs/tests/`.
24. **Implementation responsibilities.** Register a marker for each `TestSuiteKind` member; configure the coverage policy and a per-profile timeout hook; enable order randomization by default; document explicitly that the serialized CUDA lane (`P0-T015`) is the sole exemption from randomization. Create the empty, side-effect-free directory skeleton for the test tree named in Architecture §21.2 that no other Phase 0 ticket owns: `tests/unit/{domain,application,config,analysis}/`, `tests/property/`, `tests/contract/`, `tests/integration/{data,learning,persistence,reporting}/`, `tests/system/{synthetic,scientific_smoke}/`, `tests/fixtures/`, and `tests/support/` (this last one populated only with the profile-mapping scaffold, never imported by `src/datp_core`) — mirroring `P0-T005`'s empty-skeleton pattern for `src/datp_core/`, with no test content written. `tests/architecture/` (`P0-T012`), `tests/golden/` (`P0-T013`), and `tests/integration/cuda/` (`P0-T015`) are explicitly excluded from this ticket's scope; each is owned by its named ticket.
25. **Required unit tests.** NONE — this ticket edits pytest configuration and an empty test-tree skeleton only; there is no enum, class, or function of its own to unit-test.
26. **Required property tests.** NONE — no locked pure function or value object exists yet for a property to exercise; property-test configuration itself is `P0-T010`'s scope.
27. **Required contract tests.** NONE — no port/adapter contract exists yet for a pytest configuration to satisfy.
28. **Required architecture tests.** NONE — pytest configuration enforces no layer boundary; the boundary-enforcement tools are `P0-T011`/`P0-T012`.
29. **Required integration tests.** A trivial passing collection on the empty tree; genuine order-independence is verified once real test suites exist in Phase 1.
30. **Required CUDA tests.** NONE — the CUDA lane itself is configured by `P0-T015`; no CUDA execution path exists in Phase 0.
31. **Required system/end-to-end tests.** NONE — no pipeline exists yet for an end-to-end test to exercise.
32. **Validation commands.** Test collection on the skeleton; a coverage report on the (empty) skeleton.
33. **Acceptance criteria.** [ ] Markers are registered per `TestSuiteKind`. [ ] Timeout and randomization are enabled by default. [ ] Coverage is configured. [ ] No profile data leaked into `pyproject.toml`. [ ] Every directory in Implementation responsibilities' skeleton list exists, is empty of test content, and is side-effect-free. [ ] `tests/architecture/`, `tests/golden/`, and `tests/integration/cuda/` were not created here (they remain owned by `P0-T012`, `P0-T013`, `P0-T015` respectively).
34. **Completion evidence.** Changed files: the `pyproject.toml` pytest tables and the new empty test-tree directories. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** A timeout violation fails the session outright (the `TestProfileValidationError` path is defined in Phase 1, not silently extended here).
36. **Stop conditions.** None expected.
37. **Deliverables.** The pytest configuration; the empty test-tree directory skeleton.
38. **Final review checklist.** [ ] Order-independence is on by default, with only the documented CUDA-lane exemption. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T002` is `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project — not only the pytest tables. Confirm no test-profile data leaked into `pyproject.toml` anywhere, and that every marker this ticket registers is actually used consistently once later tickets add tests (a marker registered but never bound to a real suite, or a suite tagged with an unregistered marker, is a defect). A defect cannot be dismissed as pre-existing or inconvenient.

### B3. Architecture boundary audit

`NOT_APPLICABLE — no source package behavior exists yet`; the checkable item is that the marker set and the created test-tree directories both match Architecture §21.2's test-suite table exactly, with no invented directory, no omitted one, and no directory created outside the exclusion list (`tests/architecture/`, `tests/golden/`, `tests/integration/cuda/`) that this ticket must not touch.

### B4. Enum, dataclass, typing, and raw-dictionary audit

`NOT_APPLICABLE — no domain or application contract exists yet`; `TestSuiteKind` itself is implemented in Phase 1. Confirm this ticket's marker registration names each marker after the exact `TestSuiteKind` member it will represent, not an ad hoc string that Phase 1 would then need to reconcile.

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of the required pattern list. Confirm no pytest marker description cites a ticket ID instead of the actual Architecture §21.2 suite name.

### B6. Documentation and comment audit

Check for stale documentation describing a prior, incomplete test configuration; correct every reference. Confirm the documented CUDA-lane randomization exemption is stated once, precisely, and not contradicted elsewhere.

### B7. Complete validation requirements

Applicable: test collection on the skeleton (Part A line 32); coverage report on the skeleton; complete git-diff inspection; untracked-file/cleanup inspection. All other test-suite/tooling categories remain `NONE` per Part A lines 25–31, except that this ticket is itself a contributing source for validation items 9–11 (complete test suite composition, randomized ordering, Nox sessions) that later tickets (`P0-T014`, `P0-T015`) build on.

### B8. Pyright and Pylance requirements

`NONE` for this ticket's own scope. `P0-T008` is the canonical owner of Pyright/Pylance configuration parity; this ticket must remain compliant, in particular by ensuring any pytest plugin configuration is itself typed where the plugin provides stubs.

### B9. Sonar requirements

`NONE` for this ticket's own scope, but this ticket is a direct dependency of `P0-T027` because Sonar's coverage ingestion reads this ticket's coverage-report format. `P0-T027` is the canonical owner of the Sonar quality gate; this ticket must produce a coverage-report format `P0-T027` can actually ingest, recorded explicitly rather than assumed compatible.

### B10. Determinism and reproducibility audit

This ticket owns test-order randomization, which is itself a determinism control: confirm randomization is genuinely enabled (not merely configured but disabled by a default flag), and confirm the CUDA-lane exemption is scoped precisely to CUDA-tagged tests, not to an entire test file that happens to contain one CUDA test alongside CPU-safe tests. All other CUDA/seed/batch items remain `NOT_APPLICABLE — no CUDA execution path exists yet in Phase 0`.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, or any Phase 1 behavior — it edits only pytest configuration and marker registration.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm no executable path for out-of-scope work was created.

**Audit 2 — Architecture fidelity.** Checkable directly: does the registered marker set match Architecture §21.2's test-suite table member for member? Is the rule "`pyproject.toml` holds markers only" (§21.3) actually respected, with zero profile data (timeouts, examples counts) embedded in `pyproject.toml` itself?

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a test accidentally run in random order today but silently rely on shared state once real suites exist, because nothing yet enforces isolation beyond ordering? Is the coverage configuration measuring the right package once `src/datp_core` is populated, or a stale/placeholder path? Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is not the canonical owner of any cross-cutting governance responsibility beyond its own scope. It must remain compliant with the gates owned by `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, and `P0-T027`.
