# P0-T003 — Define dependency groups and pin scientific libraries

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T003` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P0-T003
2. **Title.** Define dependency groups and pin scientific libraries
3. **Phase.** 0
4. **Status.** NOT_STARTED (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P0 — Blocking
6. **Ticket type.** foundation
7. **Owner.** reproducibility-auditor
8. **Supporting agents.** implementation-engineer
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE — dependency configuration only.
13. **Objective.** Declare the exact accepted-library set of `docs/DATP Core Architecture.md` §4.1 across purpose-scoped dependency groups, pinning the numerically/order-sensitive libraries (scikit-learn, PyArrow, NumPy, SciPy, blake3, msgspec, PyTorch, Flower) to exact versions, and explicitly excluding every rejected library of §4.2 (Hydra, OmegaConf, hard MLflow dependency, Ray, Dask, Celery, any ORM, any workflow/DAG engine, any dependency-injection/plugin framework, a second internal object system, `torch.compile` enabled by default).
14. **Why this ticket exists.** The `pyproject.toml` at Phase 0 start lacks Pydantic, PyArrow, blake3, msgspec, Flower, scikit-learn, SciPy, structlog, filelock, Hypothesis, import-linter, pytest-archon, syrupy, Nox, Typer, rich, and pynvml/psutil. Reproducibility (Architecture §9.5 `EnvironmentInventory`) requires exact pins for the numerically sensitive libraries. This is kept separate from lock-file discipline (`P0-T004`), which governs regeneration, not the initial declaration.
15. **Authority references.** Architecture §4.1 (accepted libraries, exact role/layer/required-or-optional table), §4.2 (rejected dependencies), §9.5 (pinned exact versions of scikit-learn/PyArrow/NumPy/SciPy/blake3/msgspec).
16. **Dependencies.** P0-T002.
17. **Blocks.** P0-T004, P0-T026.
18. **Downstream consumers.** Every Phase 1 infrastructure ticket that constructs a framework adapter assumes the dependency groups declared here; consumption is gated transitively through `P0-T026` and is never listed as a direct dependency of an individual Phase 1 ticket.
19. **Preconditions.** `P0-T002` is `DONE`.
20. **Inputs.** Architecture §4.1/§4.2 accepted/rejected library tables.
21. **Allowed scope.** `pyproject.toml` dependency-group tables (runtime, dev/tooling, optional).
22. **Forbidden actions.** No Hydra/OmegaConf/hard-MLflow/Ray/Dask/Celery/ORM/DAG-engine/dependency-injection-framework dependency (§4.2); no unpinned numerically sensitive library; no `torch.compile` enabled by default; no source code of any kind.
23. **Required configuration or NONE.** Dependency-group tables only.
24. **Implementation responsibilities.** Group runtime scientific libraries (PyTorch, Flower, scikit-learn, SciPy, NumPy, PyArrow, blake3, msgspec, Pydantic v2, PyYAML, structlog, filelock); group tooling (Ruff, Pyright, pytest, pytest-cov, Hypothesis, pytest-xdist, pytest-timeout, pytest-randomly, import-linter, pytest-archon, syrupy, Nox); add optional groups (Typer, rich, psutil, pynvml); pin every numerically/order-sensitive library (scikit-learn, PyArrow, NumPy, SciPy, blake3, msgspec, PyTorch, Flower) to an exact version; add a comment on each group naming the Architecture §4.1 layer it is confined to.
25. **Required unit tests.** NONE — dependency-group declaration; no enum/spelling test.
26. **Required property tests.** NONE — no locked pure function or value object exists yet for a property to exercise; this ticket declares dependency groups only.
27. **Required contract tests.** An environment-inventory contract test asserting the pinned versions match `EnvironmentInventory` — owned and authored by the Phase 1 telemetry/provenance ticket, not by this ticket.
28. **Required architecture tests.** NONE — no layer package exists yet at the point this ticket runs; a dependency declaration has no layer boundary to check.
29. **Required integration tests.** Dependency resolution of every group succeeds; every rejected library (§4.2) is verifiably absent from the resolved environment.
30. **Required CUDA tests.** NONE — no CUDA execution path exists in Phase 0.
31. **Required system/end-to-end tests.** NONE — no pipeline exists yet for an end-to-end test to exercise.
32. **Validation commands.** Dependency resolution across all groups; a scan confirming rejected libraries are absent from the resolved environment.
33. **Acceptance criteria.** [ ] Every §4.1 accepted library is present in a purpose-scoped group. [ ] Every numerically sensitive library is pinned to an exact version. [ ] No §4.2 rejected library is present anywhere in the resolved environment. [ ] Optional libraries are marked optional, not required.
34. **Completion evidence.** Changed files: `pyproject.toml`. Commands run and results (resolution, rejected-library scan). Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** If an exact pin required by §9.5 cannot be resolved for Python 3.12 in this environment, record a blocker rather than substituting an unpinned or approximate version.
36. **Stop conditions.** Stop on any unresolvable exact-pin requirement.
37. **Deliverables.** Dependency-group configuration in `pyproject.toml`.
38. **Final review checklist.** [ ] Every layer-confinement comment cites Architecture §4.1. [ ] No rejected dependency present anywhere. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T002` is `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS` before implementation begins; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before beginning the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion in Part A and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project (`src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/pytest/Sonar configuration, every provider adapter folder, root project metadata) — not only the dependency-group tables this ticket edits. A defect cannot be dismissed as pre-existing, another ticket's responsibility, outside the diff, legacy, or inconvenient. Fix any authority-grounded issue before `DONE`; if it requires an undefined decision, mark this ticket `BLOCKED` and record it.

### B3. Architecture boundary audit

Check the complete project for dependency-direction violations, framework imports outside infrastructure, framework leakage into domain/application, configuration schemas leaking past the configuration boundary, CLI wiring dependencies directly, import cycles, import-time side effects, service locators, universal context objects, forbidden generic modules, backward-compatibility scaffolding, hidden defaults, silent fallbacks, and behavior forbidden in the current phase (B11). `NOT_APPLICABLE — no source package exists yet`; the one checkable item is that the declared dependency groups themselves respect the §4.1 layer-confinement table (a scientific library declared "required" is not silently used to justify importing it into `domain`, `config`, or `analysis`-science later).

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan for raw strings replacing enums, hardcoded identifiers, mutable scientific objects, object-shaped dictionaries, `dict[str, Any]`, untyped `dict`, `Any`, unexplained `cast`, unjustified `type: ignore`, and primitive obsession. `NOT_APPLICABLE — no domain or application contract exists yet`.

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of: `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture`. Confirm no dependency-group comment cites a ticket ID instead of the actual Architecture §4.1 reason.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, references to nonexistent files, comments that no longer match behavior, banner/separator comments, and claimed checks that were not actually run. Confirm any repository documentation listing "current dependencies" is updated to match the new groups, not left describing the three-dependency Phase-0-start state.

### B7. Complete validation requirements

Applicable: dependency resolution across all groups (Part A line 32); rejected-library absence scan; complete git-diff inspection; untracked-file/cleanup inspection. All test-suite/tooling categories not named in Part A lines 25–31 remain `NONE` for this ticket's own scope.

### B8. Pyright and Pylance requirements

`NONE` for this ticket's own scope. `P0-T008` is the canonical owner of Pyright/Pylance configuration parity; this ticket must remain compliant, in particular by not declaring a dependency whose type stubs are incompatible with strict mode without recording that incompatibility as a blocker.

### B9. Sonar requirements

`NONE` for this ticket's own scope. `P0-T027` is the canonical owner of the Sonar quality gate; this ticket must remain compliant, in particular by not declaring a dependency with a known unresolved critical vulnerability without recording it as a blocker.

### B10. Determinism and reproducibility audit

Check for unpinned numerically sensitive libraries, since an unpinned scikit-learn/PyArrow/NumPy/SciPy/blake3/msgspec version is itself a reproducibility defect under Architecture §9.5. This is the one determinism item genuinely checkable by this ticket; all CUDA/seed/batch items remain `NOT_APPLICABLE — no scientific configuration or CUDA execution path exists yet in Phase 0`.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, preprocessing/model/training/scoring/threshold/evaluation/statistics implementation, experiment configuration values, or any Phase 1 behavior. Declaring a dependency is not implementing behavior with it; confirm no library was exercised (imported and run), only declared.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm no executable path for out-of-scope work was created.

**Audit 2 — Architecture fidelity.** Checkable directly: every accepted library in Architecture §4.1's table is present in a purpose-scoped group with the correct required/optional marking; every rejected library in §4.2 is absent; every numerically sensitive library is pinned exactly. Record pass/fail per library, not a blanket summary.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: is a rejected dependency (Hydra, OmegaConf, Ray, Dask, Celery, an ORM) present transitively through an accepted library's own dependencies without being flagged? Is a "required" library actually pinned, or merely listed with a loose range that defeats §9.5? Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is not the canonical owner of any cross-cutting governance responsibility beyond its own scope. It must remain compliant with the gates owned by `P0-T008` (Pyright/Pylance parity), `P0-T022` (repository-wide post-implementation audit mechanism), `P0-T023` (raw-dictionary and ticket/document-reference prohibitions), `P0-T024` (scientific-drift audit mechanism for Phase 1 onward), `P0-T025` (stale-documentation enforcement), `P0-T026` (ticket-status lifecycle governance), and `P0-T027` (Sonar quality gate).
