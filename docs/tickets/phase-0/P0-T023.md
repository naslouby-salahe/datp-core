# P0-T023 — Implement structure/naming/typing/comment blocking hooks

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T023` govern identity.

**Ownership note.** This ticket is the canonical owner of the raw-dictionary/typing prohibition (Section B4 of every ticket in this directory) and the ticket/document-reference-in-code prohibition (Section B5 of every ticket in this directory) for the whole repository (task Section 22).

## Part A — Ticket template

1. **Ticket ID.** P0-T023
2. **Title.** Implement structure/naming/typing/comment blocking hooks
3. **Phase.** 0
4. **Status.** NOT_STARTED (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P0 — Blocking
6. **Ticket type.** hook
7. **Owner.** naming-auditor
8. **Supporting agents.** architecture-cleaner
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE.
13. **Objective.** Ensure the structure, naming, typing, and comment/docstring hooks enforce `docs/DATP Core Architecture.md` §5.3/§24 (forbidden module/class names, cohesion, no `import *`), strict typing (binding to `P0-T008`'s Pyright configuration), and comment hygiene (no AI-generated, banner, decorative, or stale comments) — and additionally bind the typing hook to the raw-dictionary/`Any`/untyped-`dict` prohibition, and the comment hook to the prohibition on a ticket ID or planning-document reference appearing in source code, tests, runtime configuration, exception messages, logs, names, comments, or docstrings.
14. **Why this ticket exists.** These are the recurring blocking gates every code ticket in every later phase must pass. The existing hooks are present at Phase 0 start and must be bound to the architecture's specific rules; this ticket additionally absorbs two of this ticket-conversion task's own cross-cutting requirements (Sections 11 and 12) because they are the same class of defect the typing and comment hooks already exist to catch.
15. **Authority references.** `AGENTS.md` (universal blocker checklist); Architecture §5.3, §24; `ai/hooks/structure_hook.md`, `naming_hook.md`, `typing_hook.md`, `comment_hook.md` as they exist at the start of this ticket.
16. **Dependencies.** P0-T007, P0-T008, P0-T011.
17. **Blocks.** P0-T026.
18. **Downstream consumers.** Every implementation ticket in every subsequent phase.
19. **Preconditions.** `P0-T007`, `P0-T008`, and `P0-T011` are all `DONE`.
20. **Inputs.** Architecture §5.3, §24.
21. **Allowed scope.** The four hook files (`structure_hook.md`, `naming_hook.md`, `typing_hook.md`, `comment_hook.md`) and their provider adapter pointers.
22. **Forbidden actions.** No provider-folder duplication; no rule relaxation in any of the four hooks.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Bind the structure hook to the forbidden-name list and the cohesion/soft-cap rule; bind the naming hook to the class/enum naming locks of §24; bind the typing hook to `P0-T008`'s Pyright strict configuration *and* to the raw-dictionary/`Any`/untyped-`dict`/unjustified-`cast`/unjustified-`type: ignore` prohibition (task Section 11); bind the comment hook to the no-AI/no-banner/no-stale-comment rule *and* to the prohibition on a ticket ID (`P[0-9]+-T[0-9]+`) or a planning-document name (`MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`) appearing anywhere in source code, tests, runtime configuration, exception messages, logs, names, comments, or docstrings (task Section 12).
25. **Required unit tests.** NONE — checklist gates.
26. **Required property tests.** NONE — no locked pure function or value object exists yet; this ticket defines checklist-gate documentation only.
27. **Required contract tests.** A hook dry-run against a sample module deliberately exercising each of the six bound rules (forbidden name, naming lock, untyped surface, raw dictionary, stale/AI comment, ticket-ID reference).
28. **Required architecture tests.** NONE — this ticket touches no source code and enforces no layer boundary directly (it defines hooks that reference `P0-T011`'s already-existing contracts).
29. **Required integration tests.** NONE — the hook dry-run in line 27 is the only executable check this ticket owns.
30. **Required CUDA tests.** NONE — no CUDA execution path exists in Phase 0.
31. **Required system/end-to-end tests.** NONE — no pipeline exists yet for an end-to-end test to exercise.
32. **Validation commands.** The hook dry-run against a sample module.
33. **Acceptance criteria.** [ ] The structure/naming/typing/comment hooks are bound to Architecture §5.3/§24 and to `P0-T008`'s strict-typing configuration. [ ] The typing hook additionally blocks the raw-dictionary/`Any` patterns of task Section 11. [ ] The comment hook additionally blocks the ticket/document-reference patterns of task Section 12. [ ] All four hooks are blocking. [ ] No provider-folder duplication exists.
34. **Completion evidence.** Changed files: the four hook definitions. Commands run and results (the dry-run, showing each of the six rules actually firing on the deliberately violating sample). Cleanup confirmation, including removal of the deliberately violating sample module before this ticket is marked `DONE`. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** Any of the six bound-rule violations blocks completion; none is bypassed.
36. **Stop conditions.** None expected.
37. **Deliverables.** The four hook definitions.
38. **Final review checklist.** [ ] Names, comments, typing, and the two added prohibitions are all genuinely enforced, not merely documented. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T007`, `P0-T008`, and `P0-T011` are all `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project (`src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, tool configurations, provider adapters, root project metadata) for any existing raw dictionary, unjustified `Any`, or ticket-ID reference this ticket's own new bindings would now catch, and confirm none exists undetected. A defect cannot be dismissed as pre-existing or inconvenient — this ticket's whole purpose is to make such defects impossible to dismiss going forward.

### B3. Architecture boundary audit

`NOT_APPLICABLE — this ticket is hook-governance documentation, not source-code architecture`; the structure/naming rules it binds to are themselves architecture-adjacent (§5.3/§24) but this ticket's own deliverable is the hook definition, not source subject to the architecture directly.

### B4. Enum, dataclass, typing, and raw-dictionary audit

This ticket owns this check for the whole repository going forward; its own self-audit: confirm the typing hook's bound rule list actually names every pattern from task Section 11 (raw strings for enums, hardcoded identifiers, mutable scientific objects, object-shaped dictionaries, `dict[str, Any]`, `Dict[str, Any]`, untyped `dict`, `Any`, unexplained `cast`, unjustified `type: ignore`, tuple-based pseudo-objects, boolean flags replacing discriminated variants, generic request/result objects, incomplete enum dispatch, missing `assert_never`, duplicated constants, primitive obsession), not a partial subset.

### B5. Prohibition on ticket and document references in code

This ticket owns this check for the whole repository going forward; its own self-audit: confirm the comment hook's bound rule list actually names every pattern from task Section 12 (`P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture`), scoped to code/tests/runtime configuration/exception messages/logs/names/comments/docstrings, with planning Markdown under `docs/` correctly exempted. Search the complete repository now for zero matches outside planning documents.

### B6. Documentation and comment audit

Check all four hook files for staleness against Architecture §5.3/§24; correct as needed. Confirm the two newly bound prohibitions (B4, B5 above) are documented in the hook files themselves, not only in this ticket's own text, since the hook file is what a future implementer actually reads while working.

### B7. Complete validation requirements

Applicable: the hook dry-run against a deliberately violating sample module (Part A line 32), covering all six bound rules; complete git-diff inspection; untracked-file/cleanup inspection, confirming the deliberately violating sample module was removed. All other test-suite/tooling categories remain `NONE` per Part A lines 25–31.

### B8. Pyright and Pylance requirements

`NONE` for this ticket's own scope beyond compliance, though the typing hook it defines *is* the mechanism that enforces `P0-T008`'s gate for every future ticket. `P0-T008` remains the canonical owner of the underlying Pyright/Pylance configuration; this ticket owns the hook that binds to it.

### B9. Sonar requirements

`NONE` for this ticket's own scope. `P0-T027` is the canonical owner of the Sonar quality gate; this ticket must remain compliant, and should note that Sonar's own code-smell rules may overlap with the raw-dictionary/`Any` prohibition this ticket's typing hook enforces — confirm the two do not silently contradict.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — no scientific configuration or CUDA execution path exists yet in Phase 0`.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, or any Phase 1 behavior — it edits governance Markdown, exercised only against a throwaway sample module that is removed before completion.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm no executable path for out-of-scope work was created.

**Audit 2 — Architecture fidelity.** Checkable directly: does the naming hook's bound rule set actually match Architecture §24's naming locks (not a generic "use good names" rule), and does the structure hook's forbidden-name list match Architecture §5.3's list (`utils`, `helpers`, `common`, `base`, `misc`, `manager`, `handler`, `processor`, `context`, `payload`, `shared`) exactly?

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a developer write `# type: ignore[misc]  # ticket P1-T042` and have it slip past both the raw-dictionary-adjacent typing hook (because the suppression itself isn't a raw dict) and the comment hook (if the pattern match is too narrow)? Confirm the comment hook's pattern list genuinely catches this combined case. Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is the canonical owner of the raw-dictionary/typing prohibition (Section B4) and the ticket/document-reference-in-code prohibition (Section B5) for the whole repository. Every other Phase 0 ticket's own audits of these two categories run under the gate this ticket establishes.
