# P0-T004 — Establish dependency-lock discipline

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T004` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P0-T004
2. **Title.** Establish dependency-lock discipline
3. **Phase.** 0
4. **Status.** NOT_STARTED (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P0 — Blocking
6. **Ticket type.** foundation
7. **Owner.** reproducibility-auditor
8. **Supporting agents.** implementation-engineer
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `DependencyLockState` (Architecture §9.5) — field mapping documented here; the type itself is implemented in Phase 1.
13. **Objective.** Establish a deterministic, committed dependency lock so `DependencyLockState` is reproducible and can be frozen at campaign time, with a documented, deterministic regeneration rule.
14. **Why this ticket exists.** Campaign freezes (`P3-T002`, `P5-T008`, outside this extracted phase) and Phase 7 provenance audits require an exact, committed lock. Lock generation and its update rule are a distinct governance responsibility from dependency-group design (`P0-T003`), which declares what may be installed, not the exact resolved graph.
15. **Authority references.** Architecture §9.5 (`DependencyLockState`, `EnvironmentInventory` pinned versions), §22.5 (roadmap-to-architecture traceability / provenance).
16. **Dependencies.** P0-T003.
17. **Blocks.** P3-T002, P5-T008, P0-T026.
18. **Downstream consumers.** `P3-T002` (anchor provenance freeze) and `P5-T008` (journal provenance freeze), both outside this extracted phase; `P7-T001` (immutable-result/hash verification, also outside this phase) relies transitively on the lock this ticket establishes.
19. **Preconditions.** `P0-T003` is `DONE`.
20. **Inputs.** The resolved dependency groups from `P0-T003`.
21. **Allowed scope.** The lockfile itself and its regeneration rule, documented in the governance catalogue.
22. **Forbidden actions.** No hand-editing of a pinned version inside the lock; no unlocked scientific dependency; no retention of a stale or legacy lock alongside the new one.
23. **Required configuration or NONE.** None beyond lock metadata.
24. **Implementation responsibilities.** Commit the resolved lock; document exactly how it is regenerated deterministically (the command, and the rule that regeneration is the only way a pin changes); define which lock fields feed `DependencyLockState` and how a campaign freeze snapshots it (referenced for Phase 3/5, not implemented here).
25. **Required unit tests.** NONE here — a provenance contract test for `DependencyLockState` lives in Phase 1/Phase 7.
26. **Required property tests.** NONE.
27. **Required contract tests.** A drift check comparing the committed lock to the resolved environment — owned and authored by the Phase 1/7 provenance ticket, not by this ticket.
28. **Required architecture tests.** NONE.
29. **Required integration tests.** Lock resolution is reproducible on a clean checkout (same lock content on repeated resolution).
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Lock-resolution reproducibility check; clean-versus-dirty lock detection.
33. **Acceptance criteria.** [ ] A committed lock exists and resolves deterministically. [ ] The regeneration rule is documented explicitly (not "regenerate as needed"). [ ] The `DependencyLockState` field mapping is stated explicitly, field by field.
34. **Completion evidence.** Changed files: the lockfile and the governance note documenting the regeneration rule. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** A drift between the committed lock and the resolved environment is surfaced as a provenance finding and recorded, never silently accepted or re-locked without a recorded reason.
36. **Stop conditions.** Stop if the lock cannot be resolved deterministically in this environment.
37. **Deliverables.** The committed lock; the regeneration-rule note in the governance catalogue.
38. **Final review checklist.** [ ] Lock resolution is deterministic across repeated runs. [ ] No manual version edit exists inside the lock. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T003` is `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project (`src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/pytest/Sonar configuration, provider adapters, root project metadata) — not only the lockfile. A defect cannot be dismissed as pre-existing, another ticket's responsibility, outside the diff, legacy, or inconvenient. Fix any authority-grounded issue before `DONE`; escalate an undefined decision as `BLOCKED`.

### B3. Architecture boundary audit

`NOT_APPLICABLE — no source package exists yet`; the checkable item is that the lock introduces no rejected dependency (Architecture §4.2) transitively.

### B4. Enum, dataclass, typing, and raw-dictionary audit

`NOT_APPLICABLE — no domain or application contract exists yet`; `DependencyLockState` itself is implemented in Phase 1, not here — confirm this ticket's field-mapping note describes typed fields, not a raw dictionary, so Phase 1's implementation has an unambiguous, already-typed specification to follow.

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of: `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture`. Confirm the lockfile and its regeneration-rule note contain none of these patterns.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, references to nonexistent files, comments that no longer match behavior, and claimed checks that were not actually run. Confirm the regeneration-rule note is dated and unambiguous about which command regenerates the lock.

### B7. Complete validation requirements

Applicable: lock-resolution reproducibility check (Part A line 32); complete git-diff inspection; untracked-file/cleanup inspection. All test-suite/tooling categories not named in Part A lines 25–31 remain `NONE` for this ticket's own scope.

### B8. Pyright and Pylance requirements

`NONE` for this ticket's own scope. `P0-T008` is the canonical owner of Pyright/Pylance configuration parity; this ticket must remain compliant.

### B9. Sonar requirements

`NONE` for this ticket's own scope. `P0-T027` is the canonical owner of the Sonar quality gate; this ticket must remain compliant, in particular by not locking a dependency version with a known unresolved critical vulnerability without recording it as a blocker.

### B10. Determinism and reproducibility audit

This ticket's entire objective is a reproducibility control: confirm the lock resolves to byte-identical content on repeated resolution in a clean checkout, that no lock field depends on wall-clock time, hostname, or other non-reproducible environment state, and that no numerically sensitive library (scikit-learn, PyArrow, NumPy, SciPy, blake3, msgspec, PyTorch, Flower) is present in the lock without an exact pin. All CUDA/seed/batch items remain `NOT_APPLICABLE — no CUDA execution path exists yet in Phase 0`.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, or any Phase 1 behavior. Locking a dependency graph is not executing it; confirm no library import or run occurred as a side effect of lock generation beyond what the lock tool itself requires for resolution.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm no executable path for out-of-scope work was created.

**Audit 2 — Architecture fidelity.** Checkable directly: does the `DependencyLockState` field-mapping note actually name every field Architecture §9.5 requires (exact resolved versions of the numerically sensitive libraries, at minimum), with no field silently dropped or renamed from the architecture's own vocabulary?

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could this lock be regenerated by two different engineers on two different machines and produce different pinned versions? Is the regeneration rule actually deterministic, or does it depend on an unpinned resolver setting? Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is not the canonical owner of any cross-cutting governance responsibility beyond its own scope. It must remain compliant with the gates owned by `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, and `P0-T027`.
