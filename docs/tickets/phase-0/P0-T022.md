# P0-T022 — Implement pre-edit and post-edit blocking hooks

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T022` govern identity.

**Ownership note.** This ticket is the canonical owner of the repository-wide post-implementation audit *mechanism* (Section B2 of every ticket in this directory) for the whole repository (task Section 22). Every other Phase 0 ticket's own Section B2 runs under the gate this ticket establishes.

## Part A — Ticket template

1. **Ticket ID.** P0-T022
2. **Title.** Implement pre-edit and post-edit blocking hooks
3. **Phase.** 0
4. **Status.** DONE (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P0 — Blocking
6. **Ticket type.** hook
7. **Owner.** architecture-cleaner
8. **Supporting agents.** roadmap-orchestrator
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE.
13. **Objective.** Ensure the pre-edit and post-edit gate hooks are defined and enforce contract presence, scope limits, and post-edit checks (typing, tests, cleanup) as blocking checklist gates — and that the post-edit gate's scope is explicitly repository-wide, not diff-only, so a defect cannot be dismissed merely because it predates the current change.
14. **Why this ticket exists.** Pre/post-edit gates bracket every implementation ticket in every phase; they must exist and actually block, not merely advise. The existing `ai/hooks/pre_edit_hook.md` and `post_edit_hook.md` are present at Phase 0 start and must be verified and completed, with this ticket-conversion task's repository-wide audit requirement (Section 9) folded into the post-edit hook's own scope.
15. **Authority references.** `AGENTS.md` (workflow lifecycle steps 3 and 5: pre-edit gate, post-edit gate); `ai/hooks/pre_edit_hook.md`, `post_edit_hook.md` as they exist at the start of this ticket.
16. **Dependencies.** P0-T018.
17. **Blocks.** P0-T026.
18. **Downstream consumers.** Every implementation ticket in every subsequent phase.
19. **Preconditions.** `P0-T018` is `DONE`.
20. **Inputs.** `ai/hooks/pre_edit_hook.md`, `post_edit_hook.md`.
21. **Allowed scope.** `ai/hooks/pre_edit_hook.md`, `ai/hooks/post_edit_hook.md`, and their provider adapter pointers.
22. **Forbidden actions.** No non-blocking (advisory-only) downgrade of either gate; no provider-folder duplication.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Verify/complete the pre-edit gate (contract present, scope defined, blocker checklist populated) and the post-edit gate (impacted tests, typing, structure, cleanup, and — per this ticket-conversion task's own repository-wide audit requirement — a full-project scan, not a diff-only scan) with explicit failure behavior for each.
25. **Required unit tests.** NONE — checklist gates.
26. **Required property tests.** NONE — no locked pure function or value object exists yet; this ticket defines checklist-gate documentation only.
27. **Required contract tests.** A gate dry-run against a sample edit, exercising both the pre-edit and post-edit paths.
28. **Required architecture tests.** NONE — this ticket touches no source code and enforces no layer boundary directly (it defines the gate that later invokes `P0-T011`/`P0-T012`).
29. **Required integration tests.** NONE — the gate dry-run in line 27 is the only executable check this ticket owns.
30. **Required CUDA tests.** NONE — no CUDA execution path exists in Phase 0.
31. **Required system/end-to-end tests.** NONE — no pipeline exists yet for an end-to-end test to exercise.
32. **Validation commands.** The gate dry-run against a sample edit.
33. **Acceptance criteria.** [ ] Pre-edit and post-edit gates are defined and blocking, not advisory. [ ] The post-edit gate's scope is explicitly repository-wide. [ ] Failure behavior is explicit for each gate. [ ] No provider-folder duplication exists.
34. **Completion evidence.** Changed files: the two hook definitions. Commands run and results (the dry-run). Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** A failed gate blocks completion; it is never bypassed.
36. **Stop conditions.** None expected.
37. **Deliverables.** The pre-edit and post-edit hook definitions.
38. **Final review checklist.** [ ] Both gates are genuinely blocking. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T018` is `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

This ticket defines the mechanism every other ticket's Section B2 relies on, so its own audit must be unusually rigorous: inspect the complete project and confirm the post-edit gate, as written, actually forces a full-project scan rather than a diff-only scan (a hostile read of the hook's own text must find no phrase that could be read as "only the files you changed"). A defect cannot be dismissed as pre-existing or inconvenient — including a defect in this very hook's own wording.

### B3. Architecture boundary audit

`NOT_APPLICABLE — this ticket is hook-governance documentation, not source-code architecture`.

### B4. Enum, dataclass, typing, and raw-dictionary audit

`NOT_APPLICABLE — no domain or application contract exists yet`.

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of the required pattern list. Confirm the two hook definitions state the actual rule (contract presence, repository-wide scope) rather than citing a ticket ID.

### B6. Documentation and comment audit

Check both hook files for staleness against `AGENTS.md`'s actual lifecycle steps 3 and 5; correct as needed.

### B7. Complete validation requirements

Applicable: the gate dry-run (Part A line 32); complete git-diff inspection; untracked-file/cleanup inspection. All test-suite/tooling categories remain `NONE` per Part A lines 25–31.

### B8. Pyright and Pylance requirements

`NONE` — this ticket touches no Python source.

### B9. Sonar and CodeScene requirements

`NONE` — this ticket touches no Python source. `P0-T027` remains the canonical owner of the Sonar quality gate and `P0-T028` the canonical owner of the CodeScene quality gate, for the repository generally.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — no scientific configuration or CUDA execution path exists yet in Phase 0`.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, or any Phase 1 behavior — it edits governance Markdown only.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm the pre-edit gate would block a ticket that tries to begin scientific-execution work without an open, correctly typed contract.

**Audit 2 — Architecture fidelity.** `NOT_APPLICABLE — no domain/application/infrastructure behavior exists yet`.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a future ticket satisfy the post-edit gate's "repository-wide scan" requirement with a superficial scan that technically touches every directory but checks nothing meaningful in most of them? Is the dry-run evidence (Part A line 32) itself falsifiable — could someone claim it ran without actually running it? Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is the canonical owner of the repository-wide post-implementation audit mechanism (Section B2) for the whole repository. Every other Phase 0 ticket's own repository-wide audit runs under the gate this ticket establishes.

## Completion record

**2026-07-14 — pre-edit/post-edit gates verified and a genuine diff-only defect fixed.** `P0-T018` verified `DONE` first. Lifecycle: `NOT_STARTED` → `IN_PROGRESS` → `IN_REVIEW` → `DONE`.

**`pre_edit_hook.md` reviewed:** already correct — confirms contract presence ("source files and task contract read"), scope limits ("allowed files match the requested scope"), a populated blocker checklist, and explicit blocking failure behavior ("stop before editing and report the scope conflict"). No change needed.

**Critical, genuine defect found in `post_edit_hook.md` — exactly the failure mode this ticket exists to prevent:** its `Purpose` line read "Audit **changed files** for quality and scope" and its `Failure behavior` line read "Fix issues in **touched scope**" — both explicitly diff-only/touched-scope phrasing, the literal forbidden wording this ticket's own B2 audit was written to catch ("a hostile read of the hook's own text must find no phrase that could be read as 'only the files you changed'"). This was not a subtle wording nuance; the previous text would have let every other Phase 0 ticket's own repository-wide audit requirement rest on a hook that, read literally, only asked for the current diff to be checked. Rewrote both the `Purpose` and `Failure behavior` lines (and strengthened `Required checks`) to state repository-wide scope explicitly: "audit the complete project for quality and scope, not only the files this task changed," "a defect found anywhere counts, even in a file this task did not touch, even if the defect predates this task, even if another task or agent introduced it," and "a pre-existing, out-of-scope, or previously-introduced defect is never dismissed for any of those reasons alone."

**Gate dry-run performed (Part A line 27/32 — required contract test), evidence recorded then deleted per no-scratch-file discipline:**
- **Pre-edit path:** simulated a task contract scoped to one file, then a simulated attempted edit to a different, out-of-scope file → the pre-edit hook's "allowed files match the requested scope" check correctly blocks it before editing.
- **Post-edit path:** simulated a stale-docstring defect in a file the current hypothetical diff did not touch → confirmed the **old** hook wording ("changed files," "touched scope") would **not** have caught it, while the **revised** wording explicitly requires it be caught regardless of diff. This directly demonstrates the fix closes a real gap, not a cosmetic one.

**Validation commands run:** B5 scan on both hook files → zero matches; command-adapter link check (`pre-edit-check.md`/`post-edit-check.md`) → both still resolve to the correct, existing hook files.

**Audit 1 — Roadmap fidelity.** PASS (`NOT_APPLICABLE`): confirmed (by inspection of `pre_edit_hook.md`'s scope-check requirement) that a ticket attempting scientific-execution work without an open, correctly typed contract would be blocked before editing.

**Audit 2 — Architecture fidelity.** `NOT_APPLICABLE` — no source behavior exists yet.

**Audit 3 — Adversarial cross-check.** PASS: the dry-run above is itself the falsifiability check Audit 3 asks for — it doesn't merely assert the gate works, it shows the specific defect the *old* wording would have missed and the *new* wording catches, which is stronger evidence than a generic "ran the check" claim. The "superficial repository-wide scan that checks nothing meaningful" concern is addressed by `Required checks` naming concrete, specific defect categories (naming, structure, shims, wrappers, comments, docstrings, typing, defaults, boilerplate, docs drift, temp clutter) rather than a vague "scan everything."

**Phase 0 boundary (B11).** Confirmed: only the two hook definitions were reviewed/edited; governance Markdown only.

**Completion evidence.** Changed files: `ai/hooks/post_edit_hook.md`, this ticket file, `docs/tickets/TICKET_STATUS.md`. Cleanup: the dry-run evidence file was written to the temporary execution workspace and deleted immediately after recording its outcome here. Blocker: NONE.
