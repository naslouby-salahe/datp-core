# P0-T002 — Establish the Python 3.12 project and build backend

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T002` govern identity (ID, title, dependencies, blocks, type, priority, scientific-execution classification, campaign scope).

## Part A — Ticket template

1. **Ticket ID.** P0-T002
2. **Title.** Establish the Python 3.12 project and build backend
3. **Phase.** 0 — Repository and Engineering Foundation
4. **Status.** DONE (must match `docs/tickets/TICKET_STATUS.md` at all times)
5. **Priority.** P0 — Blocking
6. **Ticket type.** foundation
7. **Owner.** implementation-engineer
8. **Supporting agents.** architecture-cleaner
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE — project/build metadata only; no domain contract.
13. **Objective.** Reconfigure `pyproject.toml` to Python 3.12 (committed), a single build backend targeting `src/datp_core`, and project metadata consistent with `docs/DATP Core Architecture.md` §4, replacing the current Python 3.11 configuration outright with no compatibility maintained for the prior configuration.
14. **Why this ticket exists.** Architecture §4 commits to Python 3.12 language features (PEP 695 type-parameter syntax and `type` aliases, `StrEnum`, `match`/`case`, and slotted keyword-only frozen dataclasses). The current `requires-python >=3.11` and `target-version py311` would silently permit code that assumes an incompatible interpreter. Project/runtime setup is kept distinct from linting (`P0-T007`), typing (`P0-T008`), and dependency-group design (`P0-T003`), each a separate ticket.
15. **Authority references.** Architecture §4 (Python 3.12 committed, PEP 695/`StrEnum`/`match`/slotted dataclasses), §5.2 (repository tree), §24 (naming); `AGENTS.md` (no-backward-compatibility policy).
16. **Dependencies.** P0-T001.
17. **Blocks.** P0-T003, P0-T005, P0-T007, P0-T008, P0-T009.
18. **Downstream consumers.** Every Phase 0 tooling ticket from `P0-T003` through `P0-T027` assumes the Python 3.12 project/build configuration established here.
19. **Preconditions.** `P0-T001` is `DONE`.
20. **Inputs.** The current `pyproject.toml` as recorded by `P0-T001`'s finding note.
21. **Allowed scope.** The `pyproject.toml` `[project]` table, `[build-system]` table, and the `requires-python`/`target-version` pins.
22. **Forbidden actions.** No dependency-group content change here (that is `P0-T003`'s scope exclusively); no new `src/` module; no legacy Python 3.11 fallback, compatibility shim, or dual-version claim of any kind; no release, tag, or version-bump work.
23. **Required configuration or NONE.** The `[project]` and `[build-system]` tables only; no scientific configuration of any kind.
24. **Implementation responsibilities.** Set `requires-python == "3.12.*"` (or the project's exact equivalent pin syntax); select and configure a single build backend and wheel target for `src/datp_core`; align the project name/description with `docs/DATP Core Architecture.md`; remove every Python 3.11 target marker so later tickets (`P0-T007` Ruff, `P0-T008` Pyright) can set Python 3.12 tool targets without contradiction.
25. **Required unit tests.** NONE — static configuration only.
26. **Required property tests.** NONE — no locked pure function or value object exists yet for a property to exercise; this ticket edits only `pyproject.toml` project/build metadata.
27. **Required contract tests.** NONE — no port/adapter contract exists yet for project/build metadata to satisfy.
28. **Required architecture tests.** NONE — no layer package exists yet at the point this ticket runs (`P0-T005` creates them); there is nothing yet for a boundary test to check.
29. **Required integration tests.** A single import-time smoke test confirming the interpreter is 3.12 and the empty package builds — owned and authored by `P0-T014`'s Nox baseline session, not by this ticket.
30. **Required CUDA tests.** NONE — no CUDA execution path exists in Phase 0.
31. **Required system/end-to-end tests.** NONE — no pipeline exists yet for an end-to-end test to exercise.
32. **Validation commands.** Build/metadata resolution of the empty package against the new `pyproject.toml`; an interpreter-version check confirming 3.12.
33. **Acceptance criteria.** [ ] `requires-python` targets 3.12 exactly. [ ] The build backend targets `src/datp_core`. [ ] No Python 3.11 marker remains anywhere in `pyproject.toml`. [ ] No legacy alias, shim, or dual-version compatibility language was introduced.
34. **Completion evidence.** Changed files: `pyproject.toml`. Commands run and their results (build/metadata resolution, interpreter-version check). Cleanup confirmation: no scratch file left behind. Confirmation of no unrelated modification (`git diff` limited to `pyproject.toml`).
35. **Failure and blocker behavior.** If a dependency required by Architecture §4 is genuinely unavailable on Python 3.12 in this environment, record it as a blocker for `P0-T003` rather than downgrading the Python version to work around it.
36. **Stop conditions.** Stop if Python 3.12 cannot be adopted without violating a hard environment constraint; record the blocker instead of compromising the version commitment.
37. **Deliverables.** An updated `pyproject.toml` `[project]`/`[build-system]` section.
38. **Final review checklist.** [ ] Exactly one build backend is configured. [ ] No dual-version compatibility language anywhere in the file. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap (`docs/Journal_Extension_Master_Roadmap.md`) before starting this ticket.
- [ ] Read the complete architecture (`docs/DATP Core Architecture.md`) before starting this ticket.
- [ ] Read this ticket file completely.
- [ ] Read `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify that every dependency listed in this ticket's "Dependencies" field (Part A) is `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change this ticket's status to `IN_PROGRESS` before implementation begins, and update `docs/tickets/TICKET_STATUS.md` in the same change.
- [ ] Record the start timestamp and current implementation step in `docs/tickets/TICKET_STATUS.md`.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change the status to `IN_REVIEW` before beginning the mandatory audits in B2–B12.
- [ ] Record the result of each of the three scientific-drift audits (B12) separately.
- [ ] Change the status to `BLOCKED` immediately if a blocking condition is found; record the exact blocker and unblock condition in both status locations.
- [ ] Change the status to `DONE` only after every acceptance criterion in Part A and every gate in B2–B12 passes.
- [ ] Record all completion evidence in both this file and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify that this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project — not only `pyproject.toml`, the current git diff, or files this ticket directly owns. At minimum inspect: `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, the import-linter configuration, the Ruff configuration, the Pyright configuration, the pytest configuration, the Sonar configuration (`P0-T027`), every provider adapter folder, and root project metadata. A defect cannot be dismissed because it already existed, another ticket introduced it, this ticket did not touch the file, or fixing it is inconvenient. Fix any authority-grounded issue before `DONE`; if the fix requires an undefined decision, mark this ticket `BLOCKED` and record the exact unresolved decision.

### B3. Architecture boundary audit

Check the complete project for: dependency-direction violations; framework imports outside infrastructure; framework leakage into domain or application; configuration schemas leaking past the configuration boundary; CLI wiring dependencies directly; import cycles; import-time side effects; service locators; universal context objects; deep inheritance where composition is required; incorrect type placement; duplicate abstractions; forbidden generic modules (`utils`, `helpers`, `common`, `base`, `misc`, `shared`); backward-compatibility shims/redirects/aliases/facades; hidden defaults; silent fallbacks; mutable scientific specifications; unstable identity construction; improperly typed process-boundary objects; accidental future-phase implementation; behavior forbidden in the current phase (B11). Run import-linter and pytest-archon where they exist. For this ticket: `NOT_APPLICABLE — enforcement tooling (`P0-T011`/`P0-T012`) is not yet configured and no source package exists yet`; the only checkable item is that `pyproject.toml` itself introduces no framework/tooling dependency outside its own scope.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan the complete project for raw strings in place of enums, hardcoded identifiers, mutable scientific objects, object-shaped dictionaries, `dict[str, Any]`/`Dict[str, Any]`/untyped `dict`, `Any`, unexplained `cast`, unjustified `type: ignore`, tuple-based pseudo-objects, boolean flags replacing discriminated variants, generic request/result objects, incomplete enum dispatch, missing `assert_never`, duplicated constants, and primitive obsession. Internal contracts must use enums/frozen dataclasses/value objects; third-party mappings convert to typed internal models at narrow boundaries only. `NOT_APPLICABLE — no domain or application contract exists yet`.

### B5. Prohibition on ticket and document references in code

Search the complete repository (code, tests, runtime configuration, exception messages, logs, names, comments, docstrings — planning Markdown under `docs/` exempt) for zero matches outside planning documents:

```
P[0-9]+-T[0-9]+
MASTER_TICKET_LOG
TICKET_STATUS
DATP Core Architecture
Journal_Extension_Master_Roadmap
docs/tickets
implemented for ticket
required by the roadmap
required by the architecture
```

Confirm `pyproject.toml`'s new `[project]`/`[build-system]` content contains none of these patterns.

### B6. Documentation and comment audit

Check the complete project for stale documentation/paths/package names, retired IDs, references to nonexistent files, contradictory documents, inaccurate docstrings, comments that no longer match behavior, banner/separator/verbose-narrative comments, misleading status claims, duplicated documentation, and claimed checks that were not actually run. Correct any stale reference to the Python version (for example a README stating "3.11") wherever the correction is unambiguously supported by Architecture §4.

### B7. Complete validation requirements

Applicable categories for this ticket: build/metadata resolution (Part A line 32); complete git-diff inspection (confirm only `pyproject.toml` changed); untracked-file and cleanup inspection. All test-suite categories (unit/property/contract/architecture/integration/CUDA/system/full-suite/Nox/Ruff/Pyright/Pylance/import-linter/pytest-archon/Sonar) are `NONE` for this ticket per Part A lines 25–31, since none of those tools are configured by `P0-T002` itself — never delete a pre-existing test or weaken an assertion to force a pass at any later ticket that does own these categories.

### B8. Pyright and Pylance requirements

`NONE` for this ticket's own scope (Pyright is configured by `P0-T008`). `P0-T008` is the canonical owner of Pyright/Pylance configuration parity for the whole repository; this ticket must remain compliant with that gate without weakening it — in particular, this ticket must not select a build backend or project layout that would make strict typing harder to configure later.

### B9. Sonar and CodeScene requirements

`NONE` for this ticket's own scope (Sonar is configured by `P0-T027`). `P0-T027` is the canonical owner of the Sonar quality gate for the whole repository; this ticket must remain compliant with that gate without weakening it. `P0-T028` is the independent, canonical owner of the CodeScene quality gate; this ticket must remain compliant with that gate as well.

### B10. Determinism and reproducibility audit

Check for scattered global seeds, nondeterministic algorithms, silent CPU/batch-size fallback, unsafe CUDA multiprocessing, incorrect process start methods, non-picklable process-boundary objects, unstable serialization, raw-float hashing, and hidden environment influence on scientific identity. `NOT_APPLICABLE — no scientific configuration or CUDA execution path exists yet in Phase 0`.

### B11. Phase 0 boundary confirmation

Confirm and record explicitly that this ticket introduced none of the following, forbidden throughout Phase 0: scientific execution; real dataset access; preprocessing/model/training/scoring/threshold/evaluation/statistics implementation; experiment execution or configuration values; synthetic implementations that become production algorithms; any Phase 1 domain/application/infrastructure behavior; placeholder scientific code; reduced real-data testing; guessed seeds or protocol values; backward compatibility, shims, redirects, or temporary future-phase behavior. This ticket touches only `pyproject.toml` project/build metadata and inherently satisfies this list.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding required: confirm no executable path for out-of-scope work was created and confirm no scientific execution or implementation occurred during this ticket.

**Audit 2 — Architecture fidelity.** The one genuinely checkable item for this ticket: confirm the Python 3.12 commitment and single-build-backend requirement of Architecture §4 are both met exactly, with no dual-version or dual-backend residue. All other architecture-fidelity items are `NOT_APPLICABLE — no domain/application/infrastructure behavior exists yet`.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: does `requires-python` actually read `3.12` and not a looser range that would silently admit 3.11 or 3.13? Does the build backend actually resolve `src/datp_core` and not a stale package name? Is any 3.11 marker left in a tool section this ticket did not think to check? Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is not the canonical owner of any cross-cutting governance responsibility beyond its own scope defined above. It must remain compliant with the gates owned by `P0-T008` (Pyright/Pylance parity), `P0-T022` (repository-wide post-implementation audit mechanism), `P0-T023` (raw-dictionary and ticket/document-reference prohibitions), `P0-T024` (scientific-drift audit mechanism for Phase 1 onward), `P0-T025` (stale-documentation enforcement), `P0-T026` (ticket-status lifecycle governance), and `P0-T027` (Sonar quality gate), once each is established.

## Completion record

**2026-07-14 — Python 3.12 project/build reconfiguration.**

Dependency `P0-T001` verified `DONE` before starting. Lifecycle: `NOT_STARTED` → `IN_PROGRESS` → `IN_REVIEW` → `DONE`.

**Changes made** (`pyproject.toml`, `[project]`/tool-version markers only, per allowed scope):
- `requires-python = ">=3.11"` → `requires-python = "==3.12.*"` — exact pin, admits only 3.12.x, matches Audit 3's concern about a looser range silently admitting 3.11/3.13.
- `[tool.ruff] target-version = "py311"` → `"py312"` (stray 3.11 marker removed per acceptance criterion 3; full Ruff rule configuration remains `P0-T007`'s scope).
- `[tool.pyright] pythonVersion = "3.11"` → `"3.12"` (same rationale; strict-mode configuration remains `P0-T008`'s scope).
- `[build-system]` already used a single backend (`hatchling`) targeting `src/datp_core` via `[tool.hatch.build.targets.wheel] packages = ["src/datp_core"]` — confirmed compliant, unchanged.
- No dependency-group content changed (out of scope, reserved for `P0-T003`).

**Environment reconciliation.** The pre-existing `.venv` was built on CPython 3.13.12 (a stray artifact, not itself a `pyproject.toml` field, but incompatible with the 3.12 commitment). Rebuilt: `rm -rf .venv && uv venv --python 3.12 .venv` → resolved `/usr/bin/python3.12` (CPython 3.12.3), confirmed via `.venv/bin/python3 --version` → `Python 3.12.3`.

**Validation commands run:**
- `grep -n "3\.11\|py311" pyproject.toml` → zero matches (no 3.11 marker remains anywhere in the file).
- `uv lock` → `Resolved 40 packages in 423ms` (build/metadata resolution succeeds under the new 3.12 constraint).
- `uv sync` → `Installed 39 packages`, including `datp-core==0.1.0 (from file:///home/naslouby/Projects/datp-core)` — the empty package builds and installs under the 3.12 interpreter (the `datp-core = "datp_core.cli:main"` console-script entry point will only become importable once `P0-T005` creates `cli/main.py`; this is expected and out of this ticket's scope).
- `git --no-pager diff --stat` → confirms `pyproject.toml` (6 lines), `uv.lock` (regenerated as a direct consequence of the interpreter/constraint change), plus this ticket's own status bookkeeping changed; no unrelated file touched.

**Audit 1 — Roadmap fidelity.** PASS (`NOT_APPLICABLE`): no scientific artifact or claim introduced; no out-of-scope executable path created.

**Audit 2 — Architecture fidelity.** PASS: Python 3.12 commitment and single-build-backend requirement both verified met exactly; no dual-version or dual-backend residue found anywhere in `pyproject.toml`.

**Audit 3 — Adversarial cross-check.** PASS: `requires-python` reads `==3.12.*` (not a looser range); the build backend resolves the actual `src/datp_core` package name (verified via successful `uv sync` producing `datp-core==0.1.0` from `file:///home/naslouby/Projects/datp-core`); grep confirmed zero remaining 3.11/`py311` markers anywhere in the file, including in the Ruff/Pyright tool sections that are not this ticket's primary scope but were still checked.

**B2 repository-wide audit.** Inspected `src/`, `tests/`, `configs/` (absent), `ai/`, `docs/`, `pyproject.toml`, and confirmed no `noxfile.py`/import-linter/Sonar config exists yet (expected — those are later tickets' scope, not a regression here).

**B5 ticket/document-reference scan.** `pyproject.toml`'s new content contains none of the forbidden patterns (ticket IDs, master-log/status-file names, planning-document titles).

**Phase 0 boundary (B11).** Confirmed: only project/build metadata changed; no scientific, Phase 1, or backward-compatibility content introduced; no 3.11 fallback or dual-version claim remains.

**Completion evidence.** Changed files: `pyproject.toml`, `uv.lock` (regenerated), this ticket file, `docs/tickets/TICKET_STATUS.md`. Cleanup: no scratch file left behind. Blocker: NONE.
