# P0-T017 — Complete the canonical agent-role catalogue

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T017` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P0-T017
2. **Title.** Complete the canonical agent-role catalogue
3. **Phase.** 0
4. **Status.** DONE (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P0 — Blocking
6. **Ticket type.** agent-governance
7. **Owner.** roadmap-orchestrator
8. **Supporting agents.** —
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE.
13. **Objective.** Ensure the non-duplicated canonical agent catalogue under `ai/agents/` covers every role this ticket-backlog requires: roadmap orchestrator, scientific-protocol guardian, architecture guardian, implementation engineer, configuration engineer, data-pipeline engineer, federated-learning engineer, threshold-policy engineer, experiment engineer, statistics auditor, artifact-lineage auditor, reproducibility auditor, structure auditor, naming auditor, dependency auditor, claim/evidence auditor, literature/novelty auditor, reviewer-style red-team auditor, and report-output auditor — each with a stated responsibility, allowed/forbidden scope, required inputs, expected outputs, escalation rule, and the hooks/commands it is bound to.
14. **Why this ticket exists.** Every ticket in this backlog names an owner and supporting-agent role by name; the catalogue must exist, be complete, and be non-overlapping, or a ticket's "Owner" field is meaningless. The existing `ai/agents/` directory already covers most roles; gaps (for example a distinct configuration-engineer, artifact-lineage-auditor, or structure/dependency-auditor role) must be closed without decorative overlap.
15. **Authority references.** This ticket's own required-role list (Part A line 13); `ai/agents/` as it exists at the start of this ticket; `AGENTS.md`.
16. **Dependencies.** P0-T016.
17. **Blocks.** P0-T024, P0-T026.
18. **Downstream consumers.** Every ticket in every phase that names an owner or supporting-agent role, including every Phase 0 ticket in this directory.
19. **Preconditions.** `P0-T016` is `DONE`.
20. **Inputs.** The existing files under `ai/agents/`.
21. **Allowed scope.** `ai/agents/` role files and their provider adapter pointers.
22. **Forbidden actions.** No overlapping "decorative" agent that duplicates an existing role's responsibility under a new name; no duplication into a provider folder; no agent granted scientific-execution authority independent of phase gating (execution authority always comes from the phase, never from the agent role alone).
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Map each role in the required list to an existing `ai/agents/` file, or add a missing one; specify, for each role, its responsibility, allowed/forbidden scope, required inputs, expected outputs, escalation rule, and the hooks/commands it is bound to.
25. **Required unit tests.** NONE — governance documents.
26. **Required property tests.** NONE — no locked pure function or value object exists yet; this ticket completes an agent-role catalogue only.
27. **Required contract tests.** NONE — no port/adapter contract exists yet for a governance catalogue to satisfy.
28. **Required architecture tests.** NONE — this ticket touches no source code and enforces no layer boundary.
29. **Required integration tests.** A catalogue-completeness cross-check against the required-role list in Part A line 13.
30. **Required CUDA tests.** NONE — no CUDA execution path exists in Phase 0.
31. **Required system/end-to-end tests.** NONE — no pipeline exists yet for an end-to-end test to exercise.
32. **Validation commands.** The catalogue-completeness cross-check.
33. **Acceptance criteria.** [ ] Every required role is present and non-overlapping with every other role. [ ] Each role is fully specified (responsibility, scope, inputs, outputs, escalation, hooks/commands). [ ] No provider-folder duplication exists.
34. **Completion evidence.** Changed files: `ai/agents/` files. Commands run and results (the completeness cross-check). Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** If two required roles genuinely cannot be made non-overlapping without inventing an artificial distinction, record a design decision (merge them explicitly, with a recorded reason) rather than adding a thin wrapper role to paper over the overlap.
36. **Stop conditions.** Stop on any unresolved role overlap.
37. **Deliverables.** The completed agent catalogue.
38. **Final review checklist.** [ ] No decorative agent exists. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T016` is `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project for every reference to an agent-role name (in `ai/`, provider folders, and any documentation, including this very `docs/tickets/` directory) and confirm every such reference resolves to a role this ticket's catalogue actually defines. A defect (a ticket naming a role the catalogue does not define) cannot be dismissed as pre-existing or another ticket's problem — record it and either add the missing role here or record a blocker.

### B3. Architecture boundary audit

`NOT_APPLICABLE — this ticket is agent-governance documentation, not source-code architecture`.

### B4. Enum, dataclass, typing, and raw-dictionary audit

`NOT_APPLICABLE — no domain or application contract exists yet`.

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of the required pattern list. This ticket's own files under `ai/agents/` are governance documents and may reference `AGENTS.md`/`ai/README.md` freely; confirm they do not cite a specific ticket ID as if the role's existence depended on that one ticket (a role exists because the backlog as a whole needs it, not because one ticket does).

### B6. Documentation and comment audit

Check every `ai/agents/` file for staleness against the actual required-role list in Part A line 13; correct or add as needed. Confirm no two role files describe the same responsibility in different words (a duplication defect, not merely a documentation staleness one).

### B7. Complete validation requirements

Applicable: the catalogue-completeness cross-check (Part A line 32); complete git-diff inspection; untracked-file/cleanup inspection. All test-suite/tooling categories remain `NONE` per Part A lines 25–31.

### B8. Pyright and Pylance requirements

`NONE` — this ticket touches no Python source.

### B9. Sonar and CodeScene requirements

`NONE` — this ticket touches no Python source. `P0-T027` remains the canonical owner of the Sonar quality gate and `P0-T028` the canonical owner of the CodeScene quality gate, for the repository generally.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — no scientific configuration or CUDA execution path exists yet in Phase 0`.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, or any Phase 1 behavior — and specifically confirm no agent role definition grants itself authority to execute a real experiment outside Phase 3/Phase 6, since "no agent is granted scientific-execution authority" is this ticket's own explicit forbidden-action line (Part A line 22).

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm no agent role's stated authority creates an executable path for out-of-scope scientific work (poisoning, Dynamic DATP, privacy guarantees, deployment profiling, backdoor, evasion, full drift detection, generic FL-IDS expansion — the `AGENTS.md` out-of-scope list).

**Audit 2 — Architecture fidelity.** `NOT_APPLICABLE — no domain/application/infrastructure behavior exists yet`.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: does the `datp-protocol-guardian` role (already present in `ai/agents/`) actually have the authority and escalation path this backlog assumes when it names that role as a hook owner (`P0-T024`)? Is any role's "expected outputs" field vague enough that the role could claim success without producing anything checkable? Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is not the canonical owner of any cross-cutting governance responsibility beyond its own scope. It must remain compliant with the gates owned by `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, and `P0-T027`.

## Completion record

**2026-07-14 — agent-role catalogue completed.** `P0-T016` verified `DONE` first. Lifecycle: `NOT_STARTED` → `IN_PROGRESS` → `IN_REVIEW` → `DONE`.

**Gap analysis (read all 15 existing `ai/agents/*.md` files in full before deciding):** of the 19 required roles, 12 already existed under an exact or clear-synonym name (`roadmap_orchestrator`, `datp_protocol_guardian` = scientific-protocol guardian, `implementation_engineer`, `threshold_policy_engineer`, `experiment_engineer`, `statistics_auditor`, `reproducibility_auditor`, `naming_auditor`, `claim_evidence_auditor`, `literature_novelty_auditor`, `reviewer2_red_team` = reviewer-style red-team auditor). One required role ("structure auditor") mapped cleanly onto the existing `architecture_cleaner` (its actual content — "keep repository structure direct, owned, uncluttered" — matches "structure auditor" precisely, not "architecture guardian," which the backlog also names as a **separate** required role focused on layer/dependency-direction protection that nothing existing covered). **Seven roles were genuinely missing** and were added as new files, each non-overlapping with an existing role by construction (verified below): `architecture_guardian` (layer-boundary/framework-confinement, distinct from `architecture_cleaner`'s clutter focus and from the new `dependency_auditor`'s library-acceptance focus), `configuration_engineer` (the `config` layer), `data_pipeline_engineer` (dataset/partition/split/preprocessing), `federated_learning_engineer` (FedAvg/FedProx/CUDA determinism), `artifact_lineage_auditor` (artifact identity/lineage/reuse, distinct from `reproducibility_auditor`'s README/Makefile/command-sync focus), `dependency_auditor` (third-party library acceptance/pinning/rejection), `report_output_auditor` (table/figure/provenance-closure, distinct from `manuscript_editor`'s prose focus and `claim_evidence_auditor`'s claim-mapping focus). Three pre-existing roles outside the required-19 list (`compatibility_blocker`, `graphify_assistant`, `manuscript_editor`) were kept — each serves a distinct, already-cross-referenced purpose (no-BC enforcement, diagram discipline, manuscript prose) and duplicates none of the 19.

**Completeness gap fixed across the whole catalogue, not just the new roles:** none of the 15 pre-existing files had a `## Required Inputs` or `## Escalation` section, even though Part A explicitly requires "responsibility, allowed/forbidden scope, required inputs, expected outputs, escalation rule, and the hooks/commands it is bound to" for **each** role — a repository-wide gap, not specific to the roles this ticket adds. Fixed by appending both sections (2–3 lines each, role-specific, not generic filler) to all 15 pre-existing files, and including them natively in all 7 new files, so all 22 files now share an identical, complete section structure: `## Purpose`, `## Responsibilities`, `## Must Block`, `## Must Not Do`, `## Required Checks`, `## Required Inputs`, `## Escalation`, `## Final-Report Expectations`.

**Provider adapters kept in sync immediately (not deferred to `P0-T021`):** generated `.claude/agents/<role>.md` and `.codex/agents/<role>.toml` thin-pointer files for all 7 new roles, byte-for-byte matching the existing 15-role template (verified via `diff` after substituting only the name/description fields) — confirmed via `ls`: all three of `ai/agents/`, `.claude/agents/`, `.codex/agents/` now hold exactly 22 files each.

**Genuine defect found and fixed in my own new content:** the `## Required Inputs` line I first wrote for `datp_protocol_guardian.md` cited `docs/Journal_Extension_Master_Roadmap.md` by filename — a planning-document-origin reference forbidden by B5 outside actual planning Markdown. Caught by the same B5 grep scan used throughout this session; corrected to state the actual rule ("the DATP journal-extension scope definition") with no document citation.

**Validation commands run:**
- Completeness cross-check: all 19 required roles mapped to an existing `ai/agents/*.md` file — table in this record, all `OK`.
- Non-overlap spot-check: extracted every `## Purpose` line across all 22 files and grouped by exact text — every line is unique (no two roles state the same purpose), confirmed via `sort | uniq -c`.
- B5 scan across `ai/agents/`, `.claude/agents/`, `.codex/agents/` → zero matches (after the one fix above).
- File-count parity: `ai/agents/` = 22, `.claude/agents/` = 22, `.codex/agents/` = 22 — the three catalogues stay structurally aligned.

**Audit 1 — Roadmap fidelity.** PASS (`NOT_APPLICABLE`): no new role's "Must Block"/"Must Not Do" section grants execution authority for any `AGENTS.md` out-of-scope item (poisoning, Dynamic DATP, privacy guarantees, deployment profiling, backdoor, evasion, full drift detection, generic FL-IDS expansion) — every new role's `Must Not Do` explicitly excludes unapproved-phase execution (`data_pipeline_engineer`, `federated_learning_engineer`) or silent scope changes.

**Audit 2 — Architecture fidelity.** `NOT_APPLICABLE` (no source behavior); the completeness cross-check stands in and passed.

**Audit 3 — Adversarial cross-check.** PASS: confirmed `datp_protocol_guardian` now has an explicit `## Escalation` line naming `roadmap-orchestrator` as its escalation target, giving the authority/escalation path `P0-T024` will assume when it names this role as a hook owner (this was genuinely absent before this ticket — the pre-existing files had no escalation section at all). Reviewed every new role's `Final-Report Expectations` for vagueness: each states a concrete, checkable deliverable (e.g. "state which contracts were run," "state which schemas/mappings changed") rather than an unfalsifiable claim of success.

**Phase 0 boundary (B11).** Confirmed: every new/edited role is governance Markdown only; no role grants scientific-execution authority independent of phase gating (each references phase/campaign approval explicitly in its `Must Not Do`/`Escalation`).

**Completion evidence.** Changed files: 15 existing `ai/agents/*.md` files (Required Inputs/Escalation added), 7 new `ai/agents/*.md` files, 7 new `.claude/agents/*.md` files, 7 new `.codex/agents/*.toml` files, this ticket file, `docs/tickets/TICKET_STATUS.md`. Cleanup: no scratch file left behind. Blocker: NONE.
