# P0-T016 — Audit and consolidate the canonical provider-agnostic AI catalogue

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T016` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P0-T016
2. **Title.** Audit and consolidate the canonical provider-agnostic AI catalogue
3. **Phase.** 0
4. **Status.** NOT_STARTED (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P0 — Blocking
6. **Ticket type.** agent-governance
7. **Owner.** roadmap-orchestrator
8. **Supporting agents.** architecture-cleaner
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE.
13. **Objective.** Confirm `ai/` is the single provider-agnostic source of truth and that `.github/`, `.claude/`, `.agents/`, and `.codex/` are thin adapters pointing back to `AGENTS.md` and `ai/`, duplicating no full governance content; record and close any duplication found.
14. **Why this ticket exists.** `AGENTS.md`'s Native Adapter Policy requires a provider-agnostic canonical catalogue with provider folders as thin adapters only; the repository already has `ai/` plus four provider folders that must never become a second source of truth. This is distinct from completing the individual catalogues (`P0-T017`–`P0-T021`), which assumes this consolidation already holds.
15. **Authority references.** `AGENTS.md` (Native Adapter Policy); `ai/README.md`.
16. **Dependencies.** P0-T001.
17. **Blocks.** P0-T017, P0-T018, P0-T019, P0-T020, P0-T021.
18. **Downstream consumers.** `P0-T017` through `P0-T021` (each catalogue), transitively `P0-T026`.
19. **Preconditions.** `P0-T001` is `DONE`.
20. **Inputs.** `ai/README.md`; the four provider adapter folders (`.github/`, `.claude/`, `.agents/`, `.codex/`).
21. **Allowed scope.** `ai/README.md`; adapter pointer files under the four provider folders.
22. **Forbidden actions.** No duplication of a full agent/skill/hook/contract/workflow/command definition into a provider folder; no second source of truth anywhere; no deletion of `ai/` content without a recorded decision.
23. **Required configuration or NONE.** NONE — governance documents only.
24. **Implementation responsibilities.** Verify each of the four provider adapters references `AGENTS.md`/`ai/` rather than restating content; flag any adapter carrying a full definition; consolidate every flagged adapter to a thin pointer.
25. **Required unit tests.** NONE — documentation governance.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE.
29. **Required integration tests.** A governance-lint scan verifies every adapter file contains pointers only, no full definition.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** An adapter-content scan for duplication across all four provider folders.
33. **Acceptance criteria.** [ ] `ai/` is confirmed the sole source of truth. [ ] All four provider folders are thin adapters. [ ] No duplicated full definition exists anywhere. [ ] Any duplication found is resolved, not merely flagged.
34. **Completion evidence.** Changed files: adapter pointer files, `ai/README.md` if updated. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** If a specific provider's own requirements genuinely conflict with provider-agnosticism (for example, a provider that structurally requires content duplication to function), record a design decision rather than silently duplicating content anyway.
36. **Stop conditions.** Stop on an unresolved provider-agnosticism conflict.
37. **Deliverables.** Consolidated adapter pointers; a catalogue-audit note.
38. **Final review checklist.** [ ] Exactly one source of truth exists (`ai/`). [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T001` is `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project — every file under `ai/`, `.github/`, `.claude/`, `.agents/`, `.codex/`, plus `src/`, `tests/`, `configs/`, `scripts/`, `docs/`, and root project metadata for any stray duplication of governance content outside the four named provider folders (for example a README that itself restates a full hook definition). A defect cannot be dismissed as pre-existing or inconvenient.

### B3. Architecture boundary audit

`NOT_APPLICABLE — this ticket is governance-document consolidation, not source-code architecture`. The one structurally analogous check: confirm no provider adapter "imports" (references as authoritative) anything other than `AGENTS.md`/`ai/`, mirroring the same single-source-of-truth discipline the architecture applies to code layers.

### B4. Enum, dataclass, typing, and raw-dictionary audit

`NOT_APPLICABLE — this ticket touches governance Markdown only, no domain or application contract`.

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of the required pattern list. This ticket's own scope is governance documents under `ai/` and provider folders, which are themselves planning documents and exempt; confirm no *source* file (once one exists) would ever need to reference this catalogue by ticket ID.

### B6. Documentation and comment audit

This ticket's core deliverable *is* a documentation audit: check every provider adapter file for staleness, contradiction with `ai/`, or a reference to a file that no longer exists; correct every finding directly, per this ticket's own allowed scope.

### B7. Complete validation requirements

Applicable: the adapter-content duplication scan (Part A line 32); complete git-diff inspection; untracked-file/cleanup inspection. All test-suite/tooling categories remain `NONE` per Part A lines 25–31.

### B8. Pyright and Pylance requirements

`NONE` — this ticket touches no Python source. `P0-T008` remains the canonical owner of Pyright/Pylance configuration parity for the repository generally.

### B9. Sonar requirements

`NONE` — this ticket touches no Python source. `P0-T027` remains the canonical owner of the Sonar quality gate for the repository generally; confirm this ticket's Markdown changes do not need to be excluded from Sonar's documentation-analysis scope for any undocumented reason.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — no scientific configuration or CUDA execution path exists yet in Phase 0`; governance-document consolidation has no seed, batch, or device behavior.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, or any Phase 1 behavior — it edits governance Markdown only.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm no executable path for out-of-scope work was created — in particular, confirm no provider adapter grants any agent authority to execute a scientific run outside the campaign phases (Phase 3, Phase 6).

**Audit 2 — Architecture fidelity.** `NOT_APPLICABLE — no domain/application/infrastructure behavior exists yet`; the analogous check under B3 above stands in for this audit's substance at Phase 0.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: does any provider adapter, read literally, actually grant more authority or duplicate more content than `ai/` intends, even if the author's intent was "just a pointer"? Is there a provider-specific quirk (for example a tool that only reads a specific file format) that was used as an excuse to duplicate content rather than solved with a genuinely thin pointer? Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is not the canonical owner of any cross-cutting governance responsibility beyond its own scope. It must remain compliant with the gates owned by `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, and `P0-T027`.
