# P0-T013 — Configure syrupy golden-snapshot support

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T013` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P0-T013
2. **Title.** Configure syrupy golden-snapshot support
3. **Phase.** 0
4. **Status.** NOT_STARTED (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P1 — Mandatory
6. **Ticket type.** foundation
7. **Owner.** implementation-engineer
8. **Supporting agents.** —
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE.
13. **Objective.** Establish syrupy snapshot support under `tests/golden/` scoped to `ExperimentManifest`/`ProvenanceRecord` shapes and the fixed B4 assignment/adjusted-Rand evidence, catching silent manifest-shape drift.
14. **Why this ticket exists.** Golden-shape regression is a distinct concern, used only for meaningful manifest/provenance/B4 shapes (Architecture §4.1, §21.2), never for a raw scientific value used as identity. This is separate from unit/property configuration (`P0-T009`/`P0-T010`).
15. **Authority references.** Architecture §4.1 (syrupy), §16.2 (B4 golden equivalence — deterministic device settings), §21.2 (`tests/golden/`).
16. **Dependencies.** P0-T009.
17. **Blocks.** P0-T026.
18. **Downstream consumers.** Phase 1 provenance/manifest tickets and Phase 2 B4 clustering tickets that require golden coverage.
19. **Preconditions.** `P0-T009` is `DONE`.
20. **Inputs.** Architecture §4.1, §16.2, §21.2.
21. **Allowed scope.** The `tests/golden/` scaffold and syrupy configuration; actual snapshots are authored later, once the shapes they cover exist.
22. **Forbidden actions.** No golden snapshot of a scientific value that belongs inside an artifact instead; no snapshot of a raw float used as identity.
23. **Required configuration or NONE.** A snapshot-directory policy (recoverable test evidence, never a scientific output).
24. **Implementation responsibilities.** Wire syrupy into the test configuration; document explicitly, in the scaffold's own README or a code comment at the package level, that golden snapshots cover manifest/provenance/B4 shapes only, never a raw scientific value.
25. **Required unit tests.** NONE — this ticket wires syrupy configuration only; there is no enum, class, or function of its own to unit-test.
26. **Required property tests.** NONE — no locked pure function or value object exists yet for a property to exercise.
27. **Required contract tests.** NONE — no port/adapter contract exists yet for a snapshot-testing configuration to satisfy.
28. **Required architecture tests.** NONE — a snapshot-testing scaffold enforces no layer boundary; the boundary-enforcement tools are `P0-T011`/`P0-T012`.
29. **Required integration tests.** A trivial snapshot round-trips (write then match) on placeholder data.
30. **Required CUDA tests.** NONE — no CUDA execution path exists in Phase 0.
31. **Required system/end-to-end tests.** NONE — no pipeline exists yet for an end-to-end test to exercise.
32. **Validation commands.** A golden-lane pytest run.
33. **Acceptance criteria.** [ ] syrupy is wired under `tests/golden/`. [ ] Scope is documented explicitly (manifest/provenance/B4 shapes only). [ ] The trivial snapshot round-trip passes.
34. **Completion evidence.** Changed files: the `tests/golden/` scaffold. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** A snapshot mismatch fails the golden lane outright; it is never accepted silently or auto-updated without review.
36. **Stop conditions.** None expected.
37. **Deliverables.** The golden-snapshot scaffold.
38. **Final review checklist.** [ ] No scientific value is snapshotted as identity. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T009` is `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project — not only `tests/golden/`. Confirm no other test directory accidentally starts accumulating ad hoc golden files outside this scaffold's policy. A defect cannot be dismissed as pre-existing or inconvenient.

### B3. Architecture boundary audit

`NOT_APPLICABLE — no source package behavior exists yet`; the checkable item is that the scaffold's documented scope (manifest/provenance/B4 shapes only) matches Architecture §21.2 exactly, with no broader scope silently implied.

### B4. Enum, dataclass, typing, and raw-dictionary audit

`NOT_APPLICABLE — this ticket wires a snapshot tool, no domain/application contract exists yet`. Confirm the trivial placeholder snapshot used for the round-trip check is itself a simple, typed structure, not a raw dict masquerading as a manifest shape.

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of the required pattern list. Confirm the golden-scaffold documentation states the actual scope rule, not a ticket ID.

### B6. Documentation and comment audit

Check for stale documentation describing an absent or different golden-snapshot policy; correct every reference. Confirm the "manifest/provenance/B4 shapes only" scope statement is not contradicted anywhere else in the repository.

### B7. Complete validation requirements

Applicable: golden-lane run with the trivial round-trip (Part A line 32); complete git-diff inspection; untracked-file/cleanup inspection. All other test-suite/tooling categories remain `NONE` per Part A lines 25–31.

### B8. Pyright and Pylance requirements

`NONE` for this ticket's own scope beyond compliance. `P0-T008` is the canonical owner of Pyright/Pylance configuration parity; this ticket must remain compliant.

### B9. Sonar requirements

`NONE` for this ticket's own scope. `P0-T027` is the canonical owner of the Sonar quality gate; this ticket must remain compliant, in particular by ensuring `tests/golden/` snapshot fixture files are excluded from Sonar's duplication analysis for a documented reason (they are intentionally repetitive golden data, not duplicated logic), not an arbitrary blanket exclusion.

### B10. Determinism and reproducibility audit

Golden snapshots are themselves a determinism control (silent manifest-shape drift is exactly a reproducibility defect): confirm the trivial round-trip snapshot is generated deterministically (same input, same snapshot, every run). All CUDA/seed/batch items remain `NOT_APPLICABLE — no CUDA execution path exists yet in Phase 0`.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, or any Phase 1 behavior — the round-trip snapshot uses placeholder, non-scientific data only.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm no executable path for out-of-scope work was created, and specifically that no raw scientific value (a score, a threshold, a metric) was used in the placeholder snapshot in a way that could be mistaken for real evidence later.

**Audit 2 — Architecture fidelity.** Checkable directly: does the documented scope match Architecture §21.2/§16.2 exactly (manifest/provenance/B4 shapes, never a raw float used as identity)?

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a future ticket misuse this scaffold to snapshot a raw scientific value "just this once," defeating the entire purpose of the scope restriction? Is the scope restriction merely documented, or is there any structural guard (a naming convention, a directory boundary) that would make a violation visible in review? Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is not the canonical owner of any cross-cutting governance responsibility beyond its own scope. It must remain compliant with the gates owned by `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, and `P0-T027`.
