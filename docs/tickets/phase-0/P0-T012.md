# P0-T012 — Configure pytest-archon in-test boundary assertions

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T012` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P0-T012
2. **Title.** Configure pytest-archon in-test boundary assertions
3. **Phase.** 0
4. **Status.** NOT_STARTED (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P0 — Blocking
6. **Ticket type.** architecture
7. **Owner.** architecture-cleaner
8. **Supporting agents.** —
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE.
13. **Objective.** Establish the pytest-archon in-test boundary-assertion scaffold under `tests/architecture/` so that a boundary failure produces a clear, Grimp-based diff alongside the static import-linter contracts of `P0-T011`.
14. **Why this ticket exists.** Architecture §3.3/§4.1 require both static (import-linter) and in-test (pytest-archon) enforcement, because pytest-archon's failure output is materially clearer for a developer mid-edit than a linter's contract-violation report. This is a distinct tool from `P0-T011`, not a duplicate of it.
15. **Authority references.** Architecture §4.1 (pytest-archon), §3.3 (backed by in-test assertions), §21.2 (architecture tests).
16. **Dependencies.** P0-T005, P0-T009.
17. **Blocks.** P1-T050, P0-T026.
18. **Downstream consumers.** `P1-T050` (the complete architecture test suite, outside this extracted phase).
19. **Preconditions.** `P0-T005` and `P0-T009` are both `DONE`.
20. **Inputs.** Architecture §3.3, §21.2.
21. **Allowed scope.** The `tests/architecture/` scaffold and pytest-archon wiring; complete assertions are authored in Phase 1, not here.
22. **Forbidden actions.** No production import of a test module; no treating this scaffold as a replacement for import-linter (`P0-T011`) rather than a complement to it.
23. **Required configuration or NONE.** None beyond test wiring.
24. **Implementation responsibilities.** Create the `tests/architecture/` package; author one minimal boundary assertion proving the harness actually runs and fails correctly on a deliberately introduced violation during implementation (removed before commit, per the no-scratch-file rule); leave the complete boundary-assertion set to Phase 1's `P1-T050`.
25. **Required unit tests.** NONE — this ticket creates a test scaffold and one minimal boundary assertion only; there is no enum, class, or function of its own to unit-test.
26. **Required property tests.** NONE — no locked pure function or value object exists yet for a property to exercise.
27. **Required contract tests.** NONE — no port/adapter contract exists yet for a test scaffold to satisfy.
28. **Required architecture tests.** A minimal pytest-archon assertion passes on the current skeleton.
29. **Required integration tests.** NONE — no behavior exists yet to integrate; the minimal boundary assertion in line 28 is the only check this ticket owns.
30. **Required CUDA tests.** NONE — no CUDA execution path exists in Phase 0.
31. **Required system/end-to-end tests.** NONE — no pipeline exists yet for an end-to-end test to exercise.
32. **Validation commands.** An architecture-lane pytest run.
33. **Acceptance criteria.** [ ] The pytest-archon harness runs. [ ] The minimal boundary assertion passes. [ ] The `tests/architecture/` scaffold exists and is otherwise empty (no premature Phase 1 assertion).
34. **Completion evidence.** Changed files: the `tests/architecture/` scaffold. Commands run and results, including evidence that a deliberately introduced violation was caught and then removed. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** A boundary breach fails the test outright; it is never suppressed to force a pass.
36. **Stop conditions.** None expected.
37. **Deliverables.** The architecture-test scaffold.
38. **Final review checklist.** [ ] This scaffold complements, and does not replace, import-linter. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T005` and `P0-T009` are both `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project — not only `tests/architecture/`. Confirm no other test module accidentally duplicates this scaffold's responsibility, and that `tests/architecture/` itself is never imported by production code. A defect cannot be dismissed as pre-existing or inconvenient.

### B3. Architecture boundary audit

Directly checkable: confirm the minimal assertion actually exercises a real boundary rule from Architecture §3.3 (not a placeholder assertion that always passes), and that it was proven to fail during implementation against a deliberate violation before that violation was removed.

### B4. Enum, dataclass, typing, and raw-dictionary audit

`NOT_APPLICABLE — this ticket produces a test scaffold, not a domain/application contract`.

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of the required pattern list. Confirm the minimal assertion's failure message states the actual architectural rule, not a ticket ID.

### B6. Documentation and comment audit

Check for stale documentation claiming full architecture-test coverage exists at Phase 0 (it does not; only the scaffold and one minimal assertion exist); correct any such overclaim.

### B7. Complete validation requirements

Applicable: architecture-lane run (Part A line 32); complete git-diff inspection; untracked-file/cleanup inspection. All other test-suite/tooling categories remain `NONE` per Part A lines 25–31, except that this ticket is itself the canonical source for validation item 17 (pytest-archon) referenced by every other Phase 0 and later-phase ticket's own Section B7.

### B8. Pyright and Pylance requirements

`NONE` for this ticket's own scope beyond compliance. `P0-T008` is the canonical owner of Pyright/Pylance configuration parity; this ticket must remain compliant, in particular by ensuring `tests/architecture/` itself passes strict typing.

### B9. Sonar requirements

`NONE` for this ticket's own scope. `P0-T027` is the canonical owner of the Sonar quality gate; this ticket must remain compliant.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — no scientific configuration or CUDA execution path exists yet in Phase 0`; a boundary-assertion scaffold has no seed, batch, or device behavior.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, or any Phase 1 behavior — it creates a test scaffold and one minimal, generic boundary assertion only.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm no executable path for out-of-scope work was created.

**Audit 2 — Architecture fidelity.** Checkable directly: does the minimal assertion actually encode a rule from Architecture §3.3, and was it genuinely proven to fail (not merely asserted to have been tested)?

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: is the "minimal" assertion so trivial that it would pass even if pytest-archon were entirely misconfigured (a false-positive-prone assertion)? Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is not the canonical owner of any cross-cutting governance responsibility beyond its own scope. It must remain compliant with the gates owned by `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, and `P0-T027`.
