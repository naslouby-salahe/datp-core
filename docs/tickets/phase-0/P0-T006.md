# P0-T006 — Establish repository root layout and tracked/generated/gitignored policy

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T006` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P0-T006
2. **Title.** Establish repository root layout and tracked/generated/gitignored policy
3. **Phase.** 0
4. **Status.** NOT_STARTED (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P0 — Blocking
6. **Ticket type.** foundation
7. **Owner.** architecture-cleaner
8. **Supporting agents.** reproducibility-auditor
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** `StorageRootKind`, `StorageVisibility`, `ArtifactNamespace` (Architecture §6.4, §15.1) — root-layout binding documented here; the enums themselves are implemented in Phase 1.
13. **Objective.** Create the repository root layout of `docs/DATP Core Architecture.md` §5.2 — `configs/`, `data/{raw,manifests}`, `checkpoints/`, `outputs/{anchor,journal}`, `results/{tables,figures,exports}`, `.runtime/{recovery,run_state,cache,locks,staging}`, `tests/`, `docs/decisions/` — and the tracked/generated/gitignored policy, with the anchor and journal output namespaces structurally separated from the first commit.
14. **Why this ticket exists.** Storage roots (Architecture §15) bind to these exact directories; the anchor/journal namespace separation prevents one track's artifacts from silently overwriting the other's (Architecture §15.2). This is kept separate from the source skeleton (`P0-T005`) and from path-resolution implementation (Phase 1's `P1-T054`).
15. **Authority references.** Architecture §5.2 (repository tree, tracked vs. generated), §15.1 (semantic roots), §15.2 (logical keys and resolved locations / namespace branches).
16. **Dependencies.** P0-T002.
17. **Blocks.** P1-T054, P0-T026.
18. **Downstream consumers.** `P1-T054` (semantic storage-root binding and path resolution, outside this extracted phase) and every future ticket that persists an anchor or journal artifact assume this exact root layout.
19. **Preconditions.** `P0-T002` is `DONE`.
20. **Inputs.** Architecture §5.2, §15.1, §15.2.
21. **Allowed scope.** Root directory scaffolding; `.gitignore` policy; `configs/` group folders (`protocols`, `experiments`, `execution`, `artifacts`, `reporting`, `tests`); `docs/decisions/`.
22. **Forbidden actions.** No `__init__.py` under `configs/`; no executable code of any kind under `configs/`; no committing the contents of `data/raw`, `checkpoints`, `outputs`, or `.runtime`; no merging the `outputs/anchor` and `outputs/journal` branches into one.
23. **Required configuration or NONE.** `configs/` group folders exist as empty external-data directories only, no code.
24. **Implementation responsibilities.** Create every root named above with its correct tracked/generated/gitignored visibility; set `.gitignore` entries for every generated/ephemeral root (`data/raw`, `checkpoints`, `outputs`, `.runtime`); keep `results/` tracked and append-only; keep `data/manifests/` tracked; keep `outputs/anchor` and `outputs/journal` as separate, non-overlapping subtrees from creation.
25. **Required unit tests.** NONE — layout only.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE — storage-boundary tests are authored in Phase 1.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** `.gitignore` verification for every generated root; a confirmation scan that `configs/` carries no executable code (`.py` file) anywhere in its tree.
33. **Acceptance criteria.** [ ] Every Architecture §5.2 root exists with the correct tracked/generated/gitignored status. [ ] `outputs/anchor` and `outputs/journal` are structurally separate roots. [ ] `configs/` has no `__init__.py` and no executable code. [ ] `results/` is tracked and append-only.
34. **Completion evidence.** Changed files: the new root layout and `.gitignore`. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** If a root's visibility class (tracked vs. generated vs. gitignored) is genuinely ambiguous under Architecture §15.1, record a blocker rather than guessing.
36. **Stop conditions.** Stop on any unresolved visibility-class ambiguity.
37. **Deliverables.** The root layout and its `.gitignore` policy.
38. **Final review checklist.** [ ] No scientific output root was committed. [ ] The anchor/journal namespace branches are structurally separate. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T002` is `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project — not only the new root directories. A defect cannot be dismissed as pre-existing, another ticket's responsibility, outside the diff, legacy, or inconvenient. Fix any authority-grounded issue before `DONE`; escalate an undefined decision as `BLOCKED`.

### B3. Architecture boundary audit

Directly checkable: confirm `data/`, `checkpoints/`, `outputs/`, and `.runtime/` are gitignored (generated), `configs/`, `data/manifests/`, `results/`, `tests/`, and `docs/decisions/` are tracked, and `outputs/anchor`/`outputs/journal` never share a common writable subtree. All other architecture-boundary items (dependency direction, framework confinement) are `NOT_APPLICABLE — no source package behavior exists in this ticket's scope`.

### B4. Enum, dataclass, typing, and raw-dictionary audit

`NOT_APPLICABLE — this ticket creates directories and a `.gitignore` policy only; the enums it references (`StorageRootKind`, `StorageVisibility`, `ArtifactNamespace`) are implemented in Phase 1, not here`. Confirm this ticket's documentation of the root-layout binding names these enums explicitly rather than describing visibility with an untyped string.

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of the required pattern list (`P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture`). Confirm `.gitignore` comments describe the actual rule (e.g. "generated model checkpoints, not tracked"), not a reference to this ticket's ID.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, and references to nonexistent files. Confirm any repository README describing the directory layout is updated to match the new roots, and that no documentation still describes a flat, unstructured `outputs/` directory.

### B7. Complete validation requirements

Applicable: `.gitignore` verification (Part A line 32); `configs/` executable-code absence scan; complete git-diff inspection; untracked-file/cleanup inspection. All other test-suite/tooling categories remain `NONE` per Part A lines 25–31.

### B8. Pyright and Pylance requirements

`NONE` for this ticket's own scope (no Python code is created). `P0-T008` is the canonical owner of Pyright/Pylance configuration parity; this ticket must remain compliant, in particular by not placing a `.py` file under `configs/` that Pyright would then need to type-check as source.

### B9. Sonar requirements

`NONE` for this ticket's own scope. `P0-T027` is the canonical owner of the Sonar quality gate; this ticket must remain compliant, in particular by ensuring generated roots are excluded from Sonar analysis for a documented reason (they are gitignored, not source) rather than an arbitrary blanket exclusion.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — no scientific configuration or CUDA execution path exists yet in Phase 0`; directory-layout policy has no seed, batch, or device behavior. The one relevant check: confirm `.runtime/{recovery,run_state,cache,locks,staging}` are all gitignored, since committing runtime/lock state would itself be a reproducibility defect once Phase 1 populates them.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, or any Phase 1 behavior — it creates directories and gitignore rules only. Confirm no placeholder file was written into `data/raw`, `checkpoints`, or `outputs` that could be mistaken for a real artifact later.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm no executable path for out-of-scope work was created and that the anchor/journal separation itself does not silently pre-decide any scientific content (it is a storage-path separation only).

**Audit 2 — Architecture fidelity.** Directly checkable: does every root in Architecture §5.2 exist with the correct visibility, and does the anchor/journal namespace separation of §15.2 hold structurally (two genuinely separate subtrees, not one tree with a shared prefix that could collide)?

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a future ticket accidentally write an anchor artifact into the journal namespace because the two paths are too similar, or because a helper resolves them ambiguously? Is any root's gitignore rule too broad (accidentally ignoring a tracked file) or too narrow (accidentally tracking a generated one)? Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is not the canonical owner of any cross-cutting governance responsibility beyond its own scope. It must remain compliant with the gates owned by `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, and `P0-T027`.
