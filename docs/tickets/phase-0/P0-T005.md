# P0-T005 — Create the approved layered source skeleton

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T005` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P0-T005
2. **Title.** Create the approved layered source skeleton
3. **Phase.** 0
4. **Status.** NOT_STARTED (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P0 — Blocking
6. **Ticket type.** architecture
7. **Owner.** architecture-cleaner
8. **Supporting agents.** implementation-engineer
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE — empty package directories only; this ticket defines no type.
13. **Objective.** Create the empty `src/datp_core/{domain,application,config,analysis,infrastructure,composition,cli}` package tree exactly as `docs/DATP Core Architecture.md` §5.1 prescribes, with cohesive capability subpackages and no forbidden generic module names, and no behavior of any kind.
14. **Why this ticket exists.** The seven-layer dependency model (Architecture §3) can be enforced only once the layer packages exist; the import-linter contracts of `P0-T011` bind to these exact package names. This is kept separate from behavior implementation (Phase 1, forbidden here) and from enforcement configuration (`P0-T011`/`P0-T012`).
15. **Authority references.** Architecture §3.1 (layers), §5.1 (source-package tree, reproduced in full below), §5.3 (structure rules), §24 (naming), §29.2 (no root `experiments/` package).
16. **Dependencies.** P0-T002.
17. **Blocks.** P0-T011, P0-T026.
18. **Downstream consumers.** Every Phase 1 domain/application/config/analysis/infrastructure/composition/cli ticket writes into a package created here; consumption is gated transitively through `P0-T026`.
19. **Preconditions.** `P0-T002` is `DONE`.
20. **Inputs.** Architecture §5.1's exact source-package tree.
21. **Allowed scope.** Package directories and minimal side-effect-free `__init__.py` files carrying only an explicit `__all__` placeholder; capability subpackage folders named exactly per Architecture §5.1.
22. **Forbidden actions.** No `utils`/`helpers`/`common`/`misc`/`base`/`manager`/`handler`/`processor`/`context`/`payload`/`shared` module anywhere in the tree; no root `experiments/` package; no behavior of any kind; no import side effects in any `__init__.py`; no speculative module for deferred or out-of-scope work.
23. **Required configuration or NONE.** NONE — source layout only.
24. **Implementation responsibilities.** Create, verbatim per Architecture §5.1: `domain/{data,learning,thresholding,evaluation,experiments,artifacts,runtime,mathematics}` (with the specific module files named in §5.1, e.g. `domain/data/datasets.py`, `domain/mathematics/effect_sizes.py`, each initially empty of behavior); `application/{planning,stages,runtime,reporting,ports}`; `config/{schemas,mapping}` plus `config/loader.py` and `config/compose.py`; `analysis/{tables,figures}` plus `analysis/wording.py` and `analysis/report_models.py`; `infrastructure/{data,learning,scoring,thresholding,evaluation,statistics,persistence,runtime,reporting,telemetry}`; `composition/{root.py,registries.py}`; `cli/{main.py,commands/}`. Keep every `__init__.py` side-effect-free.
25. **Required unit tests.** NONE — the no-forbidden-name and no-side-effect architecture tests are authored in Phase 1 and bound to this tree, not authored here.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** Import of every package produces no side effect — a scaffolding-level smoke check only; the full suite is `P1-T050`.
29. **Required integration tests.** NONE.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Import of every created package confirming no side effect; a forbidden-name scan across the new tree; a check for the absence of a root `experiments/` package.
33. **Acceptance criteria.** [ ] Every Architecture §5.1 layer and capability subpackage exists exactly as named. [ ] No forbidden generic module name exists anywhere in the new tree. [ ] No root `experiments/` package exists. [ ] Every `__init__.py` is side-effect-free.
34. **Completion evidence.** Changed files: the empty package tree (every directory and `__init__.py`/module stub created). Commands run and results (import smoke, forbidden-name scan). Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** If a module's ownership under Architecture §5.1 is genuinely ambiguous (two plausible homes for the same responsibility), record a placement blocker per Architecture §3.6 (type-placement decision procedure) rather than guessing.
36. **Stop conditions.** Stop on any unresolved placement ambiguity.
37. **Deliverables.** The empty layered source tree under `src/datp_core/`.
38. **Final review checklist.** [ ] The layout matches Architecture §5.1 exactly, module for module. [ ] No behavior was added anywhere. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T002` is `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project (`src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/pytest/Sonar configuration, provider adapters, root project metadata) — not only the new `src/datp_core/` tree. A defect cannot be dismissed as pre-existing, another ticket's responsibility, outside the diff, legacy, or inconvenient. Fix any authority-grounded issue before `DONE`; escalate an undefined decision as `BLOCKED`.

### B3. Architecture boundary audit

This ticket is directly checkable, unlike most Phase 0 tickets: walk the entire new tree and confirm (a) every directory listed in Architecture §5.1 exists and no undocumented directory was added; (b) no forbidden generic module name (`utils`, `helpers`, `common`, `base`, `misc`, `shared`, `manager`, `handler`, `processor`, `context`, `payload`) appears anywhere; (c) no root `experiments/` package exists; (d) every `__init__.py` is import-side-effect-free (imports the package and nothing else executes); (e) no cross-layer import exists yet (trivially true since every module is empty, but confirm no `__init__.py` imports another layer's package). Record each item's result explicitly.

### B4. Enum, dataclass, typing, and raw-dictionary audit

`NOT_APPLICABLE — this ticket creates empty package directories and side-effect-free `__init__.py` files only; no enum, dataclass, or dictionary of any kind is introduced`. Confirm this is actually true (no placeholder `dict` or `Any` snuck into an `__init__.py` "for now").

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of: `P[0-9]+-T[0-9]+`, `MASTER_TICKET_LOG`, `TICKET_STATUS`, `DATP Core Architecture`, `Journal_Extension_Master_Roadmap`, `docs/tickets`, `implemented for ticket`, `required by the roadmap`, `required by the architecture`. Confirm no `__init__.py` docstring or comment cites a ticket ID or architecture section number as its justification instead of stating the module's actual responsibility.

### B6. Documentation and comment audit

Check for stale documentation, retired IDs, references to nonexistent files, and misleading status claims. Confirm no `__init__.py` carries a banner comment, a separator comment, or a copied AI explanation — each should be either empty or carry, at most, a single explicit `__all__: list[str] = []` placeholder.

### B7. Complete validation requirements

Applicable: package-import smoke test with no side effect (Part A line 32); forbidden-name scan; root-`experiments/`-absence check; complete git-diff inspection; untracked-file/cleanup inspection. All other test-suite/tooling categories remain `NONE` per Part A lines 25–31.

### B8. Pyright and Pylance requirements

`NONE` substantively (no typed code exists yet in this tree), but the new tree must not break Pyright/Pylance workspace resolution once `P0-T008` configures strict mode — confirm every `__init__.py` is syntactically valid Python 3.12. `P0-T008` is the canonical owner of Pyright/Pylance configuration parity; this ticket must remain compliant.

### B9. Sonar requirements

`NONE` for this ticket's own scope. `P0-T027` is the canonical owner of the Sonar quality gate; this ticket must remain compliant, in particular by not leaving a stray non-empty file that Sonar would later flag as unreachable/dead code.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — no scientific configuration or CUDA execution path exists yet in Phase 0`; an empty package tree has no seed, batch, or device behavior to audit.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, or any actual domain/application/infrastructure behavior — every created module is empty. Confirm the tree matches exactly the module list in Architecture §5.1 and does not pre-create a Phase 1+ behavior module with even placeholder logic.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm no executable path for out-of-scope work was created — an empty module cannot itself execute anything, but confirm no module was given a default/fallback value that would silently execute once imported by a later ticket.

**Audit 2 — Architecture fidelity.** This is the ticket where Audit 2 is most substantive in all of Phase 0: cross-check every single directory and module name created against Architecture §5.1's tree line by line; confirm the seven top-level layers (`domain`, `application`, `config`, `analysis`, `infrastructure`, `composition`, `cli`) exist and no eighth layer was invented; confirm naming rules (Architecture §24) are respected in every new name.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: did a directory get created with a plausible-sounding but non-canonical name (for example `domain/scoring` instead of the architecture's `domain/learning/scores.py`)? Did an `__init__.py` acquire an import that, while not a side effect today, sets up a future circular import once Phase 1 populates the modules? Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is not the canonical owner of any cross-cutting governance responsibility beyond its own scope. It must remain compliant with the gates owned by `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, and `P0-T027`.
