# P0-T027 — Configure SonarQube/SonarCloud analysis and quality gate

**Added ticket.** This ticket does not exist in `docs/MASTER_TICKET_LOG.md`. It was added during this ticket-extraction task under the next unused canonical Phase 0 ticket ID, per that task's Section 16 ("determine which Phase 0 ticket naturally owns Sonar setup and enforcement... if no existing ticket can own it without becoming overloaded, create one new Phase 0 ticket using the next unused canonical Phase 0 ticket ID"). Justification: every existing Phase 0 tooling ticket (`P0-T007` Ruff, `P0-T008` Pyright, `P0-T009` pytest, `P0-T010` Hypothesis, `P0-T011` import-linter, `P0-T012` pytest-archon, `P0-T013` syrupy) already owns exactly one tool; `P0-T014` (Nox session wiring) explicitly forbids "business logic in sessions," and Sonar project configuration is substantive configuration, not session wiring. No existing ID was renumbered, reused, or altered; `docs/MASTER_TICKET_LOG.md` itself was not modified.

**Ownership note.** This ticket is the canonical owner of the Sonar quality gate (Section B9 of every ticket in this directory) for the whole repository.

## Part A — Ticket template

1. **Ticket ID.** P0-T027
2. **Title.** Configure SonarQube/SonarCloud analysis and quality gate
3. **Phase.** 0
4. **Status.** NOT_STARTED (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P0 — Blocking
6. **Ticket type.** foundation
7. **Owner.** reproducibility-auditor
8. **Supporting agents.** architecture-cleaner, implementation-engineer
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE — quality-tooling configuration only. Sonar is not an `docs/DATP Core Architecture.md` §4.1 accepted library; this ticket does not claim architecture authority for it, only compliance with this ticket-extraction task's own Section 16 mandate.
13. **Objective.** Configure a project-wide SonarQube/SonarCloud analysis pass and its quality gate (bugs, vulnerabilities, security hotspots, code smells, duplication, coverage) as a Nox-orchestrated, locally invocable check feeding the Phase 0 baseline gate (`P0-T026`), wired to the coverage output of `P0-T009` and consistent with the lint/type configuration of `P0-T007`/`P0-T008`.
14. **Why this ticket exists.** This ticket-extraction task requires a Sonar quality gate as part of every ticket's validation requirements (its Section 16). No existing Phase 0 ticket can absorb project-wide Sonar configuration without overloading a single-tool-configuration responsibility (see "Added ticket" note above), so this new ticket exists to own it cleanly, following the same one-ticket-per-tool pattern already established in Phase 0.
15. **Authority references.** `AGENTS.md` (universal blocker checklist; quality gates); this ticket-extraction task's Section 16 (Sonar requirement) as the operative authority for this ticket's existence; Architecture §4.1 (tool-confinement pattern, by analogy only — Sonar is not itself an Architecture §4.1 accepted library, and this ticket makes no claim otherwise).
16. **Dependencies.** P0-T007, P0-T008, P0-T009.
17. **Blocks.** P0-T026.
18. **Downstream consumers.** `P0-T026` (the baseline gate); every subsequent phase's validation commands that include the Sonar gate (per every ticket's own Section B9 in this directory and in future phase extractions).
19. **Preconditions.** `P0-T007`, `P0-T008`, and `P0-T009` are all `DONE`.
20. **Inputs.** Ruff output format, Pyright strict output format, pytest coverage-report format (all established by the three dependency tickets).
21. **Allowed scope.** A Sonar project-configuration file (for example `sonar-project.properties` or the project's chosen equivalent); a `sonar` Nox session in `noxfile.py` (session wiring only, following `P0-T014`'s no-business-logic-in-sessions pattern); documentation of CI-less local invocation.
22. **Forbidden actions.** No suppression of a Sonar finding solely to obtain a passing gate; no artificial source exclusion; no committed credential or token of any kind; no relaxation of Ruff, Pyright, or pytest to make Sonar pass instead; no scientific execution.
23. **Required configuration or NONE.** Sonar project configuration only; no scientific configuration of any kind.
24. **Implementation responsibilities.** Configure the Sonar project definition targeting `src/`, `tests/`, `ai/`, `scripts/`, and `configs/`; wire coverage ingestion from `P0-T009`'s coverage-report output and lint/type-finding ingestion where the Sonar edition in use supports it; add a `sonar` Nox session (session wiring only) that invokes the Sonar scanner locally; define the quality-gate conditions explicitly (no unresolved blocker/critical issue, no unexplained major issue, and explicit coverage/duplication thresholds, not "review issues" left vague); document credential handling as strictly external to the repository (an environment variable or secret store, never a committed file).
25. **Required unit tests.** NONE.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE.
29. **Required integration tests.** The `sonar` Nox session runs on the empty skeleton and resolves its project configuration without error; a live analysis pass additionally requires network/token availability and is validated at gate time (`P0-T026`), not claimed as complete by this ticket's own skeleton-stage check alone.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** The `sonar` Nox session's configuration-resolution dry-run on the skeleton; a documented manual analysis run once Sonar credentials are available in the target environment.
33. **Acceptance criteria.** [ ] Sonar project configuration exists and targets the correct paths. [ ] The `sonar` Nox session is wired (session-only, no embedded logic). [ ] The quality-gate conditions are documented explicitly, not vaguely. [ ] No credential is committed anywhere in the repository. [ ] No suppression was used to force a pass.
34. **Completion evidence.** Changed files: the Sonar project configuration and the `noxfile.py` addition. Commands run and results, including the dry-run and, where credentials are available, a real analysis result. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** If Sonar credentials or server access are genuinely unavailable in this environment, record the exact blocker (network or token unavailability) rather than silently skipping the gate or claiming it passed without evidence.
36. **Stop conditions.** Stop if a passing quality gate is achievable only through suppression or an arbitrary scope exclusion.
37. **Deliverables.** The Sonar project configuration; the `sonar` Nox session; the documented quality-gate conditions.
38. **Final review checklist.** [ ] The gate is real and enforced, not vacuous. [ ] No suppression was used anywhere. [ ] The Nox wiring is session-only, consistent with `P0-T014`'s pattern. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T007`, `P0-T008`, and `P0-T009` are all `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project — not only the Sonar configuration file. Confirm the Sonar project definition's `src`/`tests`/exclusion paths actually match the real repository layout (`src/`, `tests/`, `ai/`, `scripts/`, `configs/`), not a stale or guessed path list, and that no credential was accidentally committed anywhere, including in `noxfile.py` itself. A defect cannot be dismissed as pre-existing or inconvenient.

### B3. Architecture boundary audit

`NOT_APPLICABLE — Sonar is not an Architecture §4.1 accepted library and enforces no layer boundary`; the one checkable item is that the `sonar` Nox session, like every other session, contains zero business logic, consistent with `P0-T014`'s rule.

### B4. Enum, dataclass, typing, and raw-dictionary audit

`NOT_APPLICABLE — no domain or application contract exists yet`.

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of the required pattern list. Confirm the Sonar project configuration and the `sonar` Nox session contain no ticket-ID reference and no planning-document name.

### B6. Documentation and comment audit

Check for stale documentation describing an absent Sonar setup; correct every reference once this ticket makes it real. Confirm the quality-gate conditions are documented in one place, not scattered or contradicted across multiple files.

### B7. Complete validation requirements

Applicable: the `sonar` Nox session dry-run (Part A line 32); a real analysis run where credentials are available; complete git-diff inspection; untracked-file/cleanup inspection. This ticket is itself the canonical source for validation item 18 (Sonar analysis and quality gate) referenced by every other ticket in this directory's own Section B7/B9.

### B8. Pyright and Pylance requirements

`NONE` for this ticket's own scope beyond compliance. `P0-T008` is the canonical owner of Pyright/Pylance configuration parity; this ticket must remain compliant, in particular by not configuring Sonar in a way that would need a Pyright exclusion to avoid a false conflict.

### B9. Sonar requirements

This ticket owns this gate in full (see the ownership note above and Part A's objective/implementation responsibilities). No deferral to another ticket applies here.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — no scientific configuration or CUDA execution path exists yet in Phase 0`; Sonar analysis configuration has no seed, batch, or device behavior. The one relevant check: confirm the Sonar project configuration is itself deterministic (the same source tree produces the same analysis input) and does not depend on non-reproducible local state.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, or any Phase 1 behavior — it edits Sonar project configuration and Nox session wiring only.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm no executable path for out-of-scope work was created, and confirm Sonar analysis itself never touches a real dataset or scientific artifact (it analyzes source and test code only).

**Audit 2 — Architecture fidelity.** `NOT_APPLICABLE — no domain/application/infrastructure behavior exists yet, and Sonar is not an architecture-governed tool`. The one checkable item: confirm this ticket's own text (Part A line 12, line 15) is honest that it claims no Architecture §4.1 authority, avoiding a false authority citation.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could the quality-gate conditions be satisfied by excluding most of `src/` from analysis under a vague "generated code" label? Is the coverage threshold set low enough to be meaningless? Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is the canonical owner of the Sonar quality gate (Section B9) for the whole repository. Every other Phase 0 ticket must remain compliant with the gate established here without weakening it.
