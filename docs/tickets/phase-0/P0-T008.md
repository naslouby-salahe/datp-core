# P0-T008 — Configure Pyright strict typing

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T008` govern identity.

**Ownership note.** This ticket is the canonical owner of Pyright/Pylance configuration parity for the whole repository (task Section 15). Every other Phase 0 ticket's Section B8 defers to the gate established here.

## Part A — Ticket template

1. **Ticket ID.** P0-T008
2. **Title.** Configure Pyright strict typing
3. **Phase.** 0
4. **Status.** NOT_STARTED (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P0 — Blocking
6. **Ticket type.** foundation
7. **Owner.** implementation-engineer
8. **Supporting agents.** architecture-cleaner
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE.
13. **Objective.** Configure Pyright in strict mode for Python 3.12, with no untyped public surface anywhere in `src/` or `tests/`, so that exhaustiveness (`assert_never` on every closed union) and identity typing can be enforced as compile-time errors, not runtime surprises. Additionally establish workspace configuration that Pylance (the VS Code Pyright integration) reads identically, so no divergence exists between a command-line Pyright run and an editor's Pylance diagnostics.
14. **Why this ticket exists.** The Phase-0-start configuration is `basic` mode; the architecture requires strict typing to make a forgotten enum branch or a framework-leakage import a compile error (Architecture §6.10 exhaustiveness, §18 error model, §29.2 dependency/type consistency). This is a distinct tooling responsibility from lint/format (`P0-T007`). This ticket additionally owns Pylance parity because Pylance's diagnostics are driven by the same Pyright configuration surface — there is no separate "Pylance tool" to configure independently.
15. **Authority references.** Architecture §4.1 (Pyright strict, tooling layer), §6.10 (exhaustiveness and explicit discrimination), §29.2 (Pyright strict enforces type consistency).
16. **Dependencies.** P0-T002.
17. **Blocks.** P0-T026, P0-T027.
18. **Downstream consumers.** `P0-T014` (Nox strict-type session binds to this configuration), `P0-T023` (typing hook binds to this configuration), `P0-T027` (Sonar analysis is configured consistently with this ticket's typing surface), every Phase 1 ticket authoring a closed union or a value object.
19. **Preconditions.** `P0-T002` is `DONE`.
20. **Inputs.** Architecture §4.1, §6.10.
21. **Allowed scope.** The Pyright configuration (in `pyproject.toml` or `pyrightconfig.json`, whichever the project adopts — exactly one, never both) and the corresponding VS Code/Pylance workspace setting that must match it.
22. **Forbidden actions.** No `basic` mode anywhere; no broad `Any` allowance; no untyped public-surface allowance; no separate, divergent Pylance-only override that would let the editor accept what the command line rejects (or vice versa).
23. **Required configuration or NONE.** The Pyright table/file only; no scientific configuration.
24. **Implementation responsibilities.** Set strict mode; target Python 3.12; include `src/` and `tests/` in the checked set; forbid implicit `Any` on public surfaces; document (in a short workspace-settings note, not scattered comments) exactly which workspace setting file Pylance reads and confirm it points at the same configuration Pyright's command-line invocation uses.
25. **Required unit tests.** NONE — this ticket edits static type-checker configuration only; there is no enum, class, or function of its own to unit-test.
26. **Required property tests.** NONE — no locked pure function or value object exists yet for a property to exercise.
27. **Required contract tests.** NONE — no port/adapter contract exists yet for a typing configuration to satisfy.
28. **Required architecture tests.** NONE — Pyright enforces typing, not layer boundaries; the boundary-enforcement tools are `P0-T011`/`P0-T012`.
29. **Required integration tests.** Pyright strict passes on the empty typed skeleton created by `P0-T005`.
30. **Required CUDA tests.** NONE — no CUDA execution path exists in Phase 0.
31. **Required system/end-to-end tests.** NONE — no pipeline exists yet for an end-to-end test to exercise.
32. **Validation commands.** A Pyright strict-mode check across `src/` and `tests/`; a configuration-parity check confirming the Pylance-facing workspace setting resolves to the same options as the command-line configuration (diffed field by field, not eyeballed).
33. **Acceptance criteria.** [ ] Strict mode is enabled with no exception. [ ] The Python 3.12 target is set. [ ] Pyright passes clean on the current skeleton. [ ] The Pylance workspace configuration is confirmed to match the command-line configuration exactly, or the mismatch is recorded honestly as a blocker if it cannot be resolved.
34. **Completion evidence.** Changed files: the Pyright configuration and, if separate, the workspace settings file. Commands run and results, including the explicit parity check. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** NONE expected for Pyright itself; if Pylance parity genuinely cannot be verified in this environment (no editor session available), record that honestly — never claim Pylance ran if it did not — and rely on the Pyright command-line result as the enforced gate.
36. **Stop conditions.** None expected.
37. **Deliverables.** The Pyright configuration and its confirmed Pylance parity.
38. **Final review checklist.** [ ] Strict mode, with no `basic`-mode remnant anywhere. [ ] Pylance parity confirmed or its absence honestly recorded. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T002` is `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Run Pyright strict against the entire tracked tree, not just files this ticket touched, and confirm zero pre-existing untyped surface is silently grandfathered in once strict mode is active. Inspect `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, and every other configuration file for a stray `basic`-mode reference or a suppressed-diagnostics directive left over from before this ticket. A defect cannot be dismissed as pre-existing or inconvenient.

### B3. Architecture boundary audit

`NOT_APPLICABLE — no source package behavior exists yet`; the checkable item is that Pyright's `include`/`exclude` scope actually covers every layer package `P0-T005` created, with no layer silently excluded from strict checking.

### B4. Enum, dataclass, typing, and raw-dictionary audit

This ticket is the canonical typing-tooling owner, so this audit is about the *tool's* capability, not source content (none exists yet): confirm strict mode actually rejects an untyped `dict`, an unexplained `Any`, and an unexhausted `match` over a `StrEnum` in a throwaway local smoke check performed during implementation (and discarded afterward, per the no-scratch-file rule) — do not merely assert this is true from the configuration text.

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of the required pattern list. Confirm the Pyright configuration contains no comment citing a ticket ID instead of the actual Architecture §6.10/§29.2 rule.

### B6. Documentation and comment audit

Check for stale documentation describing the prior `basic`-mode configuration; correct every reference. Confirm no leftover `# type: ignore` comment exists anywhere in the (currently empty) tree without an explicit, individually justified reason.

### B7. Complete validation requirements

Applicable: Pyright strict check (Part A line 32); Pylance parity check; complete git-diff inspection; untracked-file/cleanup inspection. All other test-suite/tooling categories remain `NONE` per Part A lines 25–31, except that this ticket is itself the canonical source for validation items 14–15 (repository-wide Pyright strict and Pylance parity) referenced by every other Phase 0 ticket's own Section B7/B8.

### B8. Pyright and Pylance requirements

This ticket owns this gate in full (see the ownership note above and Part A's objective/implementation responsibilities). No deferral to another ticket applies here.

### B9. Sonar requirements

`NONE` for this ticket's own scope. `P0-T027` is the canonical owner of the Sonar quality gate; this ticket must remain compliant, in particular by not configuring Pyright in a way that would make Sonar's own type-adjacent findings impossible to correlate (for example by excluding files from Pyright that Sonar still analyzes).

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — no scientific configuration or CUDA execution path exists yet in Phase 0`; type-checker configuration has no seed, batch, or device behavior.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, or any Phase 1 behavior — it edits only Pyright/Pylance configuration.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm no executable path for out-of-scope work was created.

**Audit 2 — Architecture fidelity.** Checkable directly: does strict mode actually reject the specific patterns Architecture §6.10/§29.2 exist to prevent (unexhausted enum dispatch, untyped public surface)? This must be verified with an actual throwaway check during implementation, not assumed from reading the configuration.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: is there a per-file or per-rule Pyright suppression anywhere that would let a future ticket's untyped code slip through silently? Does the Pylance workspace setting genuinely match, or does it merely look similar while differing in one field (for example a different `pythonVersion`)? Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is the canonical owner of Pyright/Pylance configuration parity for the whole repository (Section B8). Every other Phase 0 ticket must remain compliant with the gate established here without weakening it.
