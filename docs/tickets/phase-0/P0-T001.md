# P0-T001 — Audit and record repository starting state

Standalone implementation specification. This file is understandable without reading `docs/MASTER_TICKET_LOG.md`; where this file and the master log ever disagree in wording, this file governs execution and the master log's Section G/H rows for `P0-T001` govern identity (ID, title, dependencies, blocks, type, priority, scientific-execution classification, campaign scope).

## Part A — Ticket template

1. **Ticket ID.** P0-T001
2. **Title.** Audit and record repository starting state
3. **Phase.** 0 — Repository and Engineering Foundation
4. **Status.** NOT_STARTED (must match `docs/tickets/TICKET_STATUS.md` at all times)
5. **Priority.** P0 — Blocking
6. **Ticket type.** foundation
7. **Owner.** architecture-cleaner
8. **Supporting agents.** roadmap-orchestrator
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** — (none; this ticket has no scientific content)
12. **Architecture contracts/types owned.** NONE — this is a read-only discovery ticket; no type is implemented.
13. **Objective.** Produce a factual, read-only record of the actual current repository state: the current contents of `src/` and `tests/` (empty at Phase 0 start), the absence or presence of `configs/`, the current `pyproject.toml` (as of the start of Phase 0 work: Python version, dependency count, Pyright mode), the existing `ai/` governance catalogue (agents, skills, hooks, contracts, workflows) and its provider adapter folders (`.github/`, `.claude/`, `.agents/`, `.codex/`), and the tracked/gitignored root layout.
14. **Why this ticket exists.** Every later Phase 0 ticket assumes a known starting point. An unverified assumption about what already exists (for example, that a package skeleton or `configs/` directory already exists) would cause silent duplication, an unintended overwrite, or a false completion claim. This is a discrete, no-code-change discovery responsibility, deliberately kept separate from any ticket that changes files, so that "what exists" and "what changed" are never conflated in the same review.
15. **Authority references.** `docs/DATP Core Architecture.md` §5.2 (repository tree) and §5.3 (structure rules) for what a compliant end state looks like; `docs/Journal_Extension_Master_Roadmap.md` — no scientific content applies to this ticket.
16. **Dependencies.** — (none; this is the first Phase 0 ticket)
17. **Blocks.** P0-T002.
18. **Downstream consumers.** Every other Phase 0 ticket (`P0-T002` through `P0-T027`) assumes the facts this ticket records; none may assume an undocumented starting state.
19. **Preconditions.** The repository is accessible; `docs/Journal_Extension_Master_Roadmap.md` and `docs/DATP Core Architecture.md` are both present and readable.
20. **Inputs.** The current working tree; `git status` output; the current `pyproject.toml`; the current `ai/` catalogue and its provider adapter folders.
21. **Allowed scope.** Read-only inspection of the working tree, tracked files, and `ai/`; recording the finding as a changelog-style entry (a new dated entry, appended, never rewriting prior entries) in a location this ticket's implementer designates for exactly this purpose (for example a "Repository baseline" note referenced by `P0-T026`'s completion evidence) — no other file is created or modified.
22. **Forbidden actions.** No source/config/tooling creation of any kind; no edits to any existing file under `ai/`; no backward-compatibility scaffolding; no scratch or throwaway audit files left behind after this ticket; do not claim this ticket `DONE` while its own `Status` field (line 4 above and the corresponding row in `docs/tickets/TICKET_STATUS.md`) still reads `NOT_STARTED`.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Enumerate every tracked root and its git status; confirm `src/datp_core` does not yet exist (or record its actual state if it does); confirm the state of `tests/` and `configs/`; capture the exact deviations of the current `pyproject.toml` from `docs/DATP Core Architecture.md` §4 (Python version pin, the three current runtime dependencies versus the full accepted-library table of §4.1, and the current Pyright mode versus strict); inventory every file under `ai/agents/`, `ai/skills/`, `ai/hooks/`, `ai/contracts/`, `ai/workflows/`, and every provider adapter folder (`.github/`, `.claude/`, `.agents/`, `.codex/`).
25. **Required unit tests.** NONE — this is a discovery ticket; no enum-existence, file-existence, or spelling test is created for a one-time fact-finding record.
26. **Required property tests.** NONE — no locked pure function or value object exists yet in Phase 0 for a property to exercise; this ticket produces a factual finding note only.
27. **Required contract tests.** NONE — no port/adapter contract exists yet for this read-only discovery ticket to exercise.
28. **Required architecture tests.** NONE — no source tree exists yet at the point this ticket runs; it is the pre-scaffolding discovery step that precedes `P0-T005`.
29. **Required integration tests.** NONE — this ticket produces a one-time finding note, not an integratable behavior.
30. **Required CUDA tests.** NONE — no CUDA execution path exists in Phase 0.
31. **Required system/end-to-end tests.** NONE — no pipeline exists yet for an end-to-end test to exercise.
32. **Validation commands.** A directory listing of the repository root and `src/`/`tests/`/`configs/`; `git status`; a read of `pyproject.toml`; a listing of `ai/` and its subfolders and the four provider adapter folders. All commands are read-only; none may write to the repository.
33. **Acceptance criteria.** [ ] The starting-state finding is recorded in a single, clearly labeled note. [ ] Every deviation of `pyproject.toml` from Architecture §4 is enumerated explicitly (not summarized as "several differences"). [ ] The `ai/` catalogue inventory (agent/skill/hook/contract/workflow file counts and names) is recorded. [ ] No file other than the finding note itself was changed by this ticket.
34. **Completion evidence.** Changed files: the finding note only. Commands run: the read-only listings in line 32, with their actual output attached or summarized verbatim (not paraphrased). Cleanup confirmation: no scratch file left behind besides the finding note. Confirmation that no unrelated file changed (a `git status` taken before and after must match except for the finding note).
35. **Failure and blocker behavior.** If the working tree is dirty (uncommitted changes) at audit time, record the dirty state as a finding rather than cleaning it up as part of this ticket. If the `ai/` catalogue conflicts irreconcilably with `AGENTS.md`'s Native Adapter Policy (for example, a provider folder that duplicates full governance content rather than pointing back to `ai/`), record the conflict as a blocker for `P0-T016` rather than resolving it silently inside this ticket.
36. **Stop conditions.** Stop and raise a blocker immediately if either `docs/Journal_Extension_Master_Roadmap.md` or `docs/DATP Core Architecture.md` is missing or unreadable — no later Phase 0 ticket can proceed without both.
37. **Deliverables.** One changelog-style finding note summarizing the verified starting state, referenced by `P0-T026`'s completion evidence.
38. **Final review checklist.** [ ] No implementation file was touched. [ ] Every finding is factual (observed), never aspirational (what should exist). [ ] `Status` remains `NOT_STARTED` in both this file and `docs/tickets/TICKET_STATUS.md` until the finding is actually recorded and reviewed, then advances per Part B, Section B1.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap (`docs/Journal_Extension_Master_Roadmap.md`) before starting this ticket.
- [ ] Read the complete architecture (`docs/DATP Core Architecture.md`) before starting this ticket.
- [ ] Read this ticket file completely.
- [ ] Read `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify that every dependency listed in this ticket's "Dependencies" field is `DONE` in `docs/tickets/TICKET_STATUS.md` (none exist for this ticket).
- [ ] Change this ticket's status to `IN_PROGRESS` before implementation begins.
- [ ] Update `docs/tickets/TICKET_STATUS.md` in the same change.
- [ ] Record the start timestamp and current implementation step in `docs/tickets/TICKET_STATUS.md`.
- [ ] Keep both status locations (this file's `Status` field and `docs/tickets/TICKET_STATUS.md`) synchronized while work remains active.
- [ ] Change the status to `IN_REVIEW` before beginning the mandatory audits in B2–B10 below.
- [ ] Record the result of each of the three scientific-drift audits (B12) separately.
- [ ] Change the status to `BLOCKED` immediately if a blocking condition is found; record it in both status locations.
- [ ] Record the exact blocker and unblock condition.
- [ ] Change the status to `DONE` only after every acceptance criterion above and every gate in B2–B10 passes.
- [ ] Record all completion evidence in both this file and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify that this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project, not only new/modified files, the current git diff, this ticket's own module, or files directly owned by this ticket. At minimum inspect: `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, the import-linter configuration, the Ruff configuration, the Pyright configuration, the pytest configuration, the Sonar configuration (owned by `P0-T027`), provider-specific adapters (`.github/`, `.claude/`, `.agents/`, `.codex/`), and root project metadata.

A defect found during this audit cannot be dismissed because it already existed, another ticket introduced it, another agent introduced it, this ticket did not touch the file, it is outside the current diff, it is described as legacy code, or fixing it is inconvenient. When the roadmap and architecture provide an unambiguous correction, fix the issue before marking this ticket `DONE`. When the issue requires a scientific or architectural decision that is not defined by either authority, mark this ticket `BLOCKED` and record the exact unresolved decision — never invent a solution.

### B3. Architecture boundary audit

Check the complete project for: dependency-direction violations; framework imports outside infrastructure; framework leakage into domain or application; Pydantic or external configuration schemas leaking past the configuration boundary; CLI wiring dependencies directly; import cycles; import-time side effects; service locators; universal context objects; deep inheritance where composition is required; incorrect type placement; duplicate abstractions; forbidden generic modules (`utils`, `helpers`, `common`, `base`, `misc`, `shared`); backward-compatibility shims, redirects, aliases, or compatibility facades; hidden defaults; silent fallbacks; mutable scientific specifications; unstable identity construction; improperly typed process-boundary objects; accidental future-phase implementation; source behavior forbidden in the current phase (Section B11 below). Run import-linter (`P0-T011`) and pytest-archon (`P0-T012`) where they exist and record their result. For this ticket specifically: none of these tools yet exist at the point `P0-T001` runs (it is the first ticket), so this audit's finding is `NOT_APPLICABLE — no source tree or tooling exists yet; recorded for completeness`.

### B4. Enum, dataclass, typing, and raw-dictionary audit

Scan the complete project for: raw strings where controlled enums are required; hardcoded scientific identifiers; hardcoded status values; hardcoded policy names; hardcoded path fragments that belong in typed path builders; mutable scientific objects; object-shaped dictionaries; `dict[str, Any]`; `Dict[str, Any]`; untyped `dict`; arbitrary nested dictionaries; `Any`; broad `object` payloads; unexplained `cast`; unjustified `type: ignore`; tuple-based pseudo-objects; boolean flags replacing discriminated variants; optional-field combinations used to infer variants; generic request/result/payload/context/manager/handler objects; incomplete enum dispatch; missing `assert_never`; duplicated constants; primitive obsession; raw defaults spread across the codebase. Internal domain and application contracts must use enums, frozen dataclasses, value objects, typed request/result objects, discriminated variants, or dedicated collection types; third-party mappings are allowed only at narrow boundaries, precisely typed, validated, and immediately converted into approved typed internal models before propagating further. For this ticket: `NOT_APPLICABLE — no domain or application contract exists yet; this ticket produces a finding note only`.

### B5. Prohibition on ticket and document references in code

Search the complete repository (implementation code, test code, runtime configuration, exception messages, logs, names, comments, and docstrings — planning Markdown under `docs/` is exempt) for each of the following patterns and confirm zero matches outside planning documents:

```
P[0-9]+-T[0-9]+
MASTER_TICKET_LOG
TICKET_STATUS
DATP Core Architecture
Journal_Extension_Master_Roadmap
docs/tickets
implemented for ticket
required by the roadmap
required by the architecture
```

Runtime and test code must express the actual scientific or engineering rule the code enforces, never its planning-document origin. For this ticket: the search still applies to any existing tracked file at Phase 0 start; this ticket's own finding note is a planning document and is exempt.

### B6. Documentation and comment audit

Check the complete project for: stale documentation; stale paths; stale package names; retired IDs; references to nonexistent files; contradictory documents; inaccurate docstrings; comments that no longer match behavior; copied AI explanations; banner comments; separator comments; verbose narrative comments; misleading status claims; duplicated documentation; undocumented behavior changes; claimed checks that were not actually run. Do not delete useful existing documentation merely to reduce comments; correct stale documentation wherever the correction is unambiguously supported by the roadmap and architecture.

### B7. Complete validation requirements

Run, where applicable to this ticket's own scope (this ticket introduces no test-bearing behavior; every category below is `NONE` for `P0-T001` and must be recorded as such explicitly, not omitted):

1. Directly impacted tests — NONE. 2. All unit tests — NONE. 3. All property tests — NONE. 4. All contract tests — NONE. 5. All architecture tests — NONE. 6. All integration tests — NONE. 7. CUDA tests — NONE. 8. System/end-to-end tests — NONE. 9. Complete test suite — NONE (no test infrastructure exists yet). 10. Randomized test ordering — NONE. 11. Nox sessions — NONE (not yet configured). 12–18. Ruff/Pyright/Pylance/import-linter/pytest-archon/Sonar — NONE (not yet configured). 19. Complete git-diff inspection — required (confirm only the finding note changed). 20. Untracked-file and cleanup inspection — required (confirm no scratch file was left behind).

Impacted tests never replace the complete test suite. Do not delete tests to make the suite pass, weaken assertions, add unjustified skips or `xfail`, exclude difficult files, relax typing, add broad ignores, suppress Sonar issues solely to obtain a passing gate, or add artificial coverage exclusions. A pre-existing failure must be investigated and corrected when the required correction is authority-grounded.

### B8. Pyright and Pylance requirements

Not yet configured at the point this ticket runs; `NONE` for this ticket. `P0-T008` is the canonical owner of Pyright/Pylance configuration parity for the whole repository once configured; every other ticket must remain compliant with the gate `P0-T008` establishes.

### B9. Sonar requirements

Not yet configured at the point this ticket runs; `NONE` for this ticket. `P0-T027` is the canonical owner of the Sonar project configuration and quality-gate definition for the whole repository once configured; every other ticket must remain compliant with the gate `P0-T027` establishes and must not weaken it.

### B10. Determinism and reproducibility audit

Check the complete project for: scattered global seeds; seed-list position used as identity; nondeterministic algorithms; silent CPU fallback; silent batch-size reduction; automatic scientific-configuration mutation; automatic retry after CUDA OOM; unsafe CUDA multiprocessing; incorrect process start methods; non-picklable process-boundary objects; unstable serialization; raw-float hashing; `NaN`/infinity in identity-bearing fields; hidden environment influence on scientific identity; unrecorded configuration changes; unsafe parallel CUDA execution. For this ticket: `NOT_APPLICABLE — no scientific configuration or CUDA execution path exists yet in Phase 0`.

### B11. Phase 0 boundary confirmation

Confirm, and record explicitly, that this ticket's implementation introduced none of the following, which remain forbidden throughout Phase 0 regardless of any other instruction: scientific execution; real dataset access; preprocessing implementation; model implementation; model training; scoring; threshold construction; evaluation; statistics; experiment execution; experiment configuration values; synthetic implementations that become production algorithms; Phase 1 domain behavior; Phase 1 application behavior; Phase 1 infrastructure behavior; placeholder scientific code; reduced real-data testing; guessed seeds; guessed protocol values; backward compatibility; shims; redirects; temporary future-phase behavior. This ticket is read-only and inherently satisfies this list; confirm no write occurred beyond the single finding note.

### B12. Three independent scientific-drift audits

All three audits below are required before this ticket may reach `DONE`; each must be recorded separately with its own evidence, and each must be rerun after any fix until it passes.

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`, per Section B11. Positive finding required: confirm no executable path for out-of-scope work was created, and confirm no scientific execution or scientific implementation occurred during this ticket.

**Audit 2 — Architecture fidelity.** `NOT_APPLICABLE — no domain/application/infrastructure behavior exists yet`. The only checkable item for this specific ticket is that the finding note itself makes no false claim about architecture compliance it did not verify.

**Audit 3 — Adversarial cross-check.** Review this ticket's finding note as a hostile reviewer: does every claimed fact trace to an actual command run in line 32? Is any deviation from Architecture §4 understated or omitted? Is the `ai/` inventory complete, or does it silently skip a provider folder? Fix any issue found and rerun this audit until it passes clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is not the canonical owner of any cross-cutting governance responsibility beyond its own scope defined above. It must remain compliant with the gates owned by `P0-T008` (Pyright/Pylance parity), `P0-T022` (repository-wide post-implementation audit mechanism), `P0-T023` (raw-dictionary and ticket/document-reference prohibitions), `P0-T024` (scientific-drift audit mechanism for Phase 1 onward), `P0-T025` (stale-documentation enforcement), `P0-T026` (ticket-status lifecycle governance), and `P0-T027` (Sonar quality gate), once each is established.
