# P0-T014 — Establish Nox validation sessions

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T014` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P0-T014
2. **Title.** Establish Nox validation sessions
3. **Phase.** 0
4. **Status.** NOT_STARTED (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P0 — Blocking
6. **Ticket type.** foundation
7. **Owner.** implementation-engineer
8. **Supporting agents.** —
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE.
13. **Objective.** Author `noxfile.py` with the named validation sessions of Architecture §21.2 (`cuda`, `serial`, `resource_intensive`, `xdist_safe`, `synthetic`, `scientific_smoke`) plus format/lint/type/architecture sessions, containing no logic beyond session wiring.
14. **Why this ticket exists.** Session orchestration composes every individual tool ticket (`P0-T007` Ruff, `P0-T008` Pyright, `P0-T009` pytest, `P0-T011` import-linter, `P0-T012` pytest-archon) into runnable lanes and is the substrate `P0-T026`'s baseline gate runs. It is distinct from configuring any individual tool.
15. **Authority references.** Architecture §4.1 (Nox owns sessions, no logic beyond wiring), §21.2 (named sessions), §21.3 (configuration ownership).
16. **Dependencies.** P0-T007, P0-T008, P0-T009.
17. **Blocks.** P0-T015, P0-T026.
18. **Downstream consumers.** `P0-T015` (CUDA/xdist lane policy binds to these sessions), `P0-T025` (impacted-test/order-independence gate), `P0-T027` (Sonar session addition), every subsequent phase's validation commands.
19. **Preconditions.** `P0-T007`, `P0-T008`, and `P0-T009` are all `DONE`.
20. **Inputs.** Architecture §21.2's named-session table.
21. **Allowed scope.** `noxfile.py`.
22. **Forbidden actions.** No business logic inside a session (a session invokes a tool; it never implements a check itself); no session that writes to a scientific output root; no CUDA lane run under xdist.
23. **Required configuration or NONE.** None beyond session wiring.
24. **Implementation responsibilities.** Define sessions for lint, format, strict-type, architecture, `xdist_safe`, `serial`, `cuda` (serialized), `synthetic`, `scientific_smoke`, and `resource_intensive`; wire an impacted-test composition and a full-suite composition.
25. **Required unit tests.** NONE.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE.
29. **Required integration tests.** Every session runs on the empty skeleton without error.
30. **Required CUDA tests.** The `cuda` session is present and serialized; no CUDA hardware execution is required to pass at this stage, only the session's wiring is validated.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Each Nox session invoked individually against the current skeleton.
33. **Acceptance criteria.** [ ] All Architecture §21.2 sessions are present. [ ] The CUDA lane is serialized. [ ] Every session runs on the current skeleton. [ ] No session contains logic beyond wiring a tool invocation.
34. **Completion evidence.** Changed files: `noxfile.py`. Commands run and results, one per session. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** A failing session returns a non-zero exit code; nothing silently passes.
36. **Stop conditions.** None expected.
37. **Deliverables.** `noxfile.py`.
38. **Final review checklist.** [ ] Session-only wiring, with zero embedded logic. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T007`, `P0-T008`, and `P0-T009` are all `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Run every Nox session against the entire tracked tree, not a subset. Inspect `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, and every tool configuration to confirm each session actually invokes the correct, already-configured tool rather than a stale default. A defect cannot be dismissed as pre-existing or inconvenient.

### B3. Architecture boundary audit

`NOT_APPLICABLE — no source package behavior exists yet`; the checkable item is that `noxfile.py` itself contains zero business logic (Architecture §4.1's explicit "no logic beyond session wiring" rule) — a hostile read of the file must find only tool invocations, never a conditional that implements a check.

### B4. Enum, dataclass, typing, and raw-dictionary audit

`NOT_APPLICABLE — no domain or application contract exists yet`. Confirm `noxfile.py` itself does not encode session names as ad hoc strings scattered across the file without a single source of truth (a session list defined once, referenced everywhere, is acceptable; a session name typed differently in two places is not).

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of the required pattern list. Confirm no Nox session name or docstring cites a ticket ID instead of the actual Architecture §21.2 session name.

### B6. Documentation and comment audit

Check for stale documentation describing a different or absent set of validation sessions; correct every reference so the documented commands actually match `noxfile.py`.

### B7. Complete validation requirements

Applicable: every session run individually (Part A line 32); complete git-diff inspection; untracked-file/cleanup inspection. This ticket is itself the canonical source for validation item 11 (all mandatory Nox sessions) referenced by every other Phase 0 and later-phase ticket's own Section B7.

### B8. Pyright and Pylance requirements

`NONE` for this ticket's own scope beyond compliance, but `noxfile.py` itself must pass Pyright strict once `P0-T008` is active, since it is part of the tracked source tree. `P0-T008` is the canonical owner of Pyright/Pylance configuration parity; this ticket must remain compliant.

### B9. Sonar requirements

`NONE` for this ticket's own scope, but this ticket's `noxfile.py` is the file `P0-T027` extends with a `sonar` session. `P0-T027` is the canonical owner of the Sonar quality gate; this ticket must leave `noxfile.py` in a state where adding one more session-only entry is trivial and consistent with the existing session-wiring pattern.

### B10. Determinism and reproducibility audit

Confirm the `cuda` session is genuinely serialized (no parallel worker configuration applied to it) and that `xdist_safe` is the only session using pytest-xdist. This is the one determinism item directly checkable by this ticket. All CUDA-hardware-execution items remain `NOT_APPLICABLE — no CUDA execution path exists yet in Phase 0`.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, or any Phase 1 behavior — every session invokes a tool against the empty skeleton only.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm no executable path for out-of-scope work was created — in particular, confirm the `scientific_smoke` session is scoped to a synthetic smoke check and cannot be pointed at a real dataset path in this phase.

**Audit 2 — Architecture fidelity.** Checkable directly: does every named session in Architecture §21.2 actually exist in `noxfile.py`, spelled identically? Is the "no logic beyond wiring" rule genuinely respected (a hostile line-by-line read of `noxfile.py`, not a summary judgment)?

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could the `resource_intensive` session accidentally run under the same worker pool as `xdist_safe`, defeating its own resource-isolation purpose? Does any session silently swallow a tool's non-zero exit code (for example via an `|| true` pattern)? Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is not the canonical owner of any cross-cutting governance responsibility beyond its own scope. It must remain compliant with the gates owned by `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, and `P0-T027`.
