# P0-T024 — Implement scope/threshold/statistics/lineage/config blocking hooks

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T024` govern identity.

**Ownership note.** This ticket is the canonical owner of the scientific-drift audit *mechanism* (the executable hooks that Audit 1 and Audit 2 of every Phase 1-onward ticket bind to) for the whole repository (task Section 22). Phase 0's own three scientific-drift audits (Section B12 of every ticket in this directory) remain boundary-confirmation audits regardless of this ownership, since no scientific or domain code exists yet in Phase 0.

## Part A — Ticket template

1. **Ticket ID.** P0-T024
2. **Title.** Implement scope/threshold/statistics/lineage/config blocking hooks
3. **Phase.** 0
4. **Status.** DONE (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P0 — Blocking
6. **Ticket type.** hook
7. **Owner.** datp-protocol-guardian
8. **Supporting agents.** statistics-auditor, artifact-lineage-auditor
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE — this ticket encodes checklist gates, not domain types.
13. **Objective.** Ensure the scientific-scope, threshold-semantics, statistics, lineage/provenance, and configuration hooks enforce the locked identity from `docs/MASTER_TICKET_LOG.md` Section A: benign-only calibration, CV(FPR) as the primary endpoint with AUROC as a control, threshold-scope as the sole causal variable across B1–B4, stage-scoped lineage, no hidden configuration default, and no out-of-scope drift (poisoning, Dynamic DATP, privacy guarantees, deployment profiling, backdoor, evasion, full drift detection, generic FL-IDS expansion).
14. **Why this ticket exists.** These hooks encode the scientific and lineage invariants as blocking gates for every domain/experiment ticket from Phase 1 onward; without them, a later ticket could silently invent a scientific rule or drift scope with no automatic check. The existing `datp_journal_scope_guard`, `threshold_policy_semantics`, `statistics_hook`, and `claim_evidence_hook` skills/hooks are present at Phase 0 start; a lineage/provenance hook and a configuration hook must additionally be ensured.
15. **Authority references.** `docs/Journal_Extension_Master_Roadmap.md` §2 (locks), §10 (statistics); Architecture §13 (lineage), §11 (configuration), §29.1; `ai/hooks/statistics_hook.md`, `claim_evidence_hook.md`; `ai/skills/threshold_policy_semantics.md`, `datp_journal_scope_guard.md`, `typed_protocol_state_check.md`, all as they exist at the start of this ticket.
16. **Dependencies.** P0-T017.
17. **Blocks.** P0-T026.
18. **Downstream consumers.** Every domain/experiment ticket in Phase 1 onward.
19. **Preconditions.** `P0-T017` is `DONE`.
20. **Inputs.** Roadmap §2, §10; Architecture §11, §13, §29.1.
21. **Allowed scope.** The scope/threshold/statistics/lineage/config hook files and their provider adapter pointers.
22. **Forbidden actions.** No provider-folder duplication; no hook that would permit scope drift, AUROC-as-verdict, attack-data-in-calibration, or a hidden configuration default.
23. **Required configuration or NONE.** NONE.
24. **Implementation responsibilities.** Author/bind five hooks: (a) the scientific-scope hook, blocking out-of-scope drift; (b) the threshold-semantics hook, blocking anything that would violate benign-only calibration, threshold-scope-only causal variation, the canonical B4 `K = 3`, or the naming locks; (c) the statistics hook, requiring CV(FPR) as primary, the positive-direction BCa rule, and typed (not silently dropped) degeneracy handling; (d) the lineage/provenance hook, requiring stage-scoped identity, lineage-compatibility-based reuse (never path or list-position identity), and immutable scientific specifications; (e) the configuration hook, forbidding a hidden default, requiring discriminated tags over boolean-flag combinations, and requiring fingerprint separation between scientifically distinct stages.
25. **Required unit tests.** NONE — checklist gates; the invariants these hooks encode get executable tests once real domain code exists in Phase 1/Phase 2/Phase 7.
26. **Required property tests.** NONE — no locked pure function or value object exists yet; this ticket defines checklist-gate documentation only.
27. **Required contract tests.** A hook dry-run against sample threshold/statistics/config edits that deliberately violate each of the five bound rules.
28. **Required architecture tests.** NONE — this ticket touches no source code and enforces no layer boundary directly.
29. **Required integration tests.** NONE — the hook dry-run in line 27 is the only executable check this ticket owns.
30. **Required CUDA tests.** NONE — no CUDA execution path exists in Phase 0.
31. **Required system/end-to-end tests.** NONE — no pipeline exists yet for an end-to-end test to exercise.
32. **Validation commands.** The hook dry-run against sample threshold/statistics/config edits.
33. **Acceptance criteria.** [ ] All five hooks are present and blocking. [ ] The locked scientific identity (Section A of the master log) is encoded exactly, with nothing invented. [ ] No provider-folder duplication exists.
34. **Completion evidence.** Changed files: the five hook definitions. Commands run and results (the dry-run, showing each of the five rules firing on a deliberately violating sample). Cleanup confirmation, including removal of the deliberately violating samples. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** If a hook rule would require inventing a scientific rule not stated by the roadmap, raise a blocker instead of inventing one.
36. **Stop conditions.** Stop on any invented-rule requirement.
37. **Deliverables.** The five hook definitions.
38. **Final review checklist.** [ ] Every locked invariant is enforced, and none was invented. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T017` is `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project for any existing document, configuration, or governance file that already contradicts one of the five locked invariants this ticket's hooks encode (for example a stray note somewhere describing AUROC as a thresholding verdict). A defect cannot be dismissed as pre-existing or inconvenient.

### B3. Architecture boundary audit

`NOT_APPLICABLE — this ticket is hook-governance documentation, not source-code architecture`; the lineage/configuration rules it binds to (Architecture §11, §13) are themselves architecture content, but this ticket's own deliverable is the hook definition.

### B4. Enum, dataclass, typing, and raw-dictionary audit

`NOT_APPLICABLE — no domain or application contract exists yet`. Confirm the configuration hook's own text requires discriminated tags over boolean-flag combinations and forbids a hidden default — this is this ticket's own forward-looking instance of the raw-dictionary/primitive-obsession prohibition, applied to future configuration schemas rather than to source that exists today.

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of the required pattern list. Confirm none of the five hook files cites a ticket ID as its justification instead of the actual roadmap/architecture rule.

### B6. Documentation and comment audit

Check all five hook files for staleness against Roadmap §2/§10 and Architecture §11/§13/§29.1; correct as needed.

### B7. Complete validation requirements

Applicable: the hook dry-run against deliberately violating samples (Part A line 32); complete git-diff inspection; untracked-file/cleanup inspection, confirming the deliberately violating samples were removed. All other test-suite/tooling categories remain `NONE` per Part A lines 25–31.

### B8. Pyright and Pylance requirements

`NONE` — this ticket touches no Python source. `P0-T008` remains the canonical owner of Pyright/Pylance configuration parity generally.

### B9. Sonar and CodeScene requirements

`NONE` — this ticket touches no Python source. `P0-T027` remains the canonical owner of the Sonar quality gate generally. `P0-T028` is the independent, canonical owner of the CodeScene quality gate; this ticket must remain compliant with that gate as well.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — no scientific configuration or CUDA execution path exists yet in Phase 0`. Confirm, however, that the lineage/provenance hook's own text explicitly forbids seed-list position being used as identity and explicitly requires lineage compatibility over path/filename similarity — this is this ticket's forward-looking instance of the determinism/reproducibility audit, applied to the hook mechanism rather than to executed code.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, or any Phase 1 behavior — it edits governance Markdown, exercised only against deliberately violating throwaway samples that are removed before completion.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** This is the one Phase 0 ticket where Audit 1 is substantively checkable rather than blanket `NOT_APPLICABLE`, because this ticket's own deliverable directly encodes the roadmap's locks: cross-check every item in Roadmap §2/§10 against the five hooks' bound rules — fixed encoder, FedAvg baseline, threshold-calibration scope as sole causal variable, benign-only calibration, CV(FPR) primary/AUROC control, anchor/journal separation, B4 `K = 3`, seed-cohort discipline, no promotion of supportive claims to confirmatory, no rerun on weak/null/mixed/unfavorable evidence, no out-of-scope executable path — and confirm each is represented by an actual blocking rule, not merely mentioned in prose.

**Audit 2 — Architecture fidelity.** Checkable directly: does the lineage/provenance hook's bound rule set match Architecture §13's lineage chain (stage-scoped identity, `ReuseImpact`, lineage-compatibility-based reuse) exactly? Does the configuration hook's bound rule set match Architecture §11's configuration boundary (Pydantic validation confined to `config`, immutable resolved specs, typed spec-diff) exactly?

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could a future Phase 1+ ticket satisfy these hooks' literal text while still promoting a supportive claim to confirmatory through a technicality (for example by relabeling a metric rather than its statistical role)? Is the "no invented rule" stop condition (Part A line 36) itself enforceable, or does it rely entirely on the implementer's good faith with no structural check? Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is the canonical owner of the scientific-drift audit mechanism (the scope/threshold/statistics/lineage/config blocking hooks) that Phase 1 onward binds to for Audit 1 and Audit 2 in every future ticket's Section B12. Phase 0's own scientific-drift audits remain boundary-confirmation audits under Section B11 regardless of this ownership, since no scientific or domain code exists yet in Phase 0.

## Completion record

**2026-07-14 — five scope/threshold/statistics/lineage/config hooks established.** `P0-T017` verified `DONE` first. Lifecycle: `NOT_STARTED` → `IN_PROGRESS` → `IN_REVIEW` → `DONE`.

**Gap found:** of the five required hooks, only `statistics_hook.md` existed (and only partially — see below); `claim_evidence_hook.md` existed but is a different, already-established hook. The scientific-scope and threshold-semantics rules previously existed only as **skills** (`datp_journal_scope_guard.md`, `threshold_policy_semantics.md`), not as blocking **hooks** — a hook and a skill are distinct artifact types in this catalogue (hook = trigger + blocking status + failure behavior; skill = reusable pass/fail criteria a hook or agent invokes), and no hook existed to make either skill an actual blocking gate. The lineage/provenance hook and the configuration hook did not exist in any form.

**Read the exact locked identity directly from source before writing any rule** (never invented): `docs/Journal_Extension_Master_Roadmap.md` §2 (non-negotiable scientific identity) and §3 (locked main claim — Regime A, B1 vs B2, CV(FPR), 10-seed paired, 95% BCa positive-direction, sole confirmatory endpoint) and §4 (threshold-policy/comparator nomenclature — B0–B4 meanings, canonical `K=3`, naming locks); Architecture §11 (configuration architecture — fingerprint categories, `EvaluationConfig.primary = CV_FPR`, no hidden defaults) and §13 (stage-fingerprint/lineage chain — frozen per-stage identity, lineage-compatible reuse only).

**Four new hooks created, transcribing the locked identity verbatim, never inventing a rule:** `scientific_scope_hook.md` (fixed encoder/FedAvg/threshold-scope-sole-variable, benign-only calibration, AUROC-as-control-never-verdict, stress tests stay outside the ladder, the full out-of-scope list, no promotion of supportive evidence); `threshold_semantics_hook.md` (B0–B4 exact meanings, canonical `K=3`, the `B-FedStatsBenign`/τ-shrink naming locks, no attack data in calibration, typed threshold state); `lineage_provenance_hook.md` (frozen per-stage identity hashing, lineage-compatible reuse only — never path/filename/list-position identity, immutable specs, integrity-conflict rejection on hash mismatch); `configuration_hook.md` (no hidden defaults, discriminated tags over boolean-flag variants, `EvaluationConfig.primary = CV_FPR` verbatim, confirmatory `StatisticalConfig` fields explicit, fingerprint separation between scientifically distinct stages).

**`statistics_hook.md` strengthened** (it previously named only generic "BCa CI, Wilcoxon, Cliff's delta" without the specific confirmatory-endpoint lock): added the CV(FPR)-primary/AUROC-control rule, typed (never-silently-dropped) degeneracy handling, and — closing **Audit 3's own named adversarial scenario** directly — an explicit line locking that "Regime A, B1 vs B2, CV(FPR), ten-seed paired evidence, 95% BCa... positive direction only" is the **sole** confirmatory endpoint and that "no other regime, policy pair, or metric may be labeled or presented as confirmatory, **however it is named**" — directly blocking the relabeling technicality Audit 3 asks about, not merely restating the general rule.

**Provider adapters kept complete immediately:** added `.claude/commands/scientific-scope-check.md`, `threshold-semantics-check.md`, `lineage-provenance-check.md`, `configuration-check.md` (4 new commands, byte-for-byte matching the established 10-line template), so every one of the now-19 `ai/hooks/*.md` files has a corresponding command (verified via a coverage scan, zero gaps).

**Hook dry-run performed (Part A line 27/32) — five deliberately violating sample scenarios, one per hook, each checked against the hook's own text (these are governance-text rules with no executable code yet in Phase 0, so the "dry-run" is a documented walkthrough, recorded here, not a throwaway file):**
1. Scope: a hypothetical edit proposing a "poisoning-robustness evaluation module" → `scientific_scope_hook.md`'s out-of-scope list blocks it by name.
2. Threshold: a hypothetical edit proposing "K=5 as the new canonical B4" → `threshold_semantics_hook.md`'s "canonical `K=3`... K=9 and other K are exploratory only, never presented as canonical" blocks it.
3. Statistics: a hypothetical edit proposing "report AUROC as the primary confirmatory verdict" → `statistics_hook.md`'s "AUROC is a control metric only, never the thresholding verdict" and the new sole-confirmatory-endpoint lock both block it.
4. Lineage: a hypothetical edit proposing "reuse this checkpoint because the filename matches" → `lineage_provenance_hook.md`'s "never on file path, filename similarity, or list position" blocks it.
5. Configuration: a hypothetical edit proposing a new `use_full_pooled_variance` boolean flag → `configuration_hook.md`'s explicit naming of that exact pattern as forbidden (transcribed directly from Architecture §11.4) blocks it.

**Validation commands run:** B5 scan across all 5 hook files and the 4 new commands → zero matches; hook/command coverage scan → zero gaps.

**Audit 1 — Roadmap fidelity.** PASS (substantively checkable, not `NOT_APPLICABLE`): cross-checked every item in Roadmap §2/§3 against the five hooks — fixed encoder, FedAvg baseline, threshold-scope-sole-variable, benign-only calibration, CV(FPR)-primary/AUROC-control, B4 K=3, no-promotion-of-supportive-to-confirmatory, and the full out-of-scope list are each represented by an actual blocking rule in `scientific_scope_hook.md` or `statistics_hook.md`, not merely mentioned in prose elsewhere.

**Audit 2 — Architecture fidelity.** PASS: `lineage_provenance_hook.md`'s rule set matches Architecture §13's chain exactly (frozen per-stage hash, lineage-compatible reuse, integrity-conflict rejection); `configuration_hook.md`'s rule set matches Architecture §11's boundary exactly (`EvaluationConfig.primary = CV_FPR`, discriminated tags over boolean flags, fingerprint categories), both transcribed directly from the source sections, not paraphrased from memory.

**Audit 3 — Adversarial cross-check.** PASS: the exact relabeling technicality this audit names is now explicitly closed by `statistics_hook.md`'s "however it is named" clause. The "no invented rule" stop condition is not merely good-faith-dependent: every rule in all five hooks traces to a specific, quoted roadmap/architecture passage recorded above, making an invented rule detectable by direct comparison against the source text, not solely reliant on the implementer's intent.

**Phase 0 boundary (B11).** Confirmed: only governance Markdown was added/edited; the five dry-run scenarios were hypothetical walkthroughs, not real edits, executions, or throwaway files requiring cleanup.

**Completion evidence.** Changed files: `ai/hooks/scientific_scope_hook.md`, `threshold_semantics_hook.md`, `lineage_provenance_hook.md`, `configuration_hook.md` (new), `ai/hooks/statistics_hook.md` (strengthened), 4 new `.claude/commands/*.md` files, this ticket file, `docs/tickets/TICKET_STATUS.md`. Cleanup: no scratch file left behind (dry-run was documentation-only, no throwaway files created). Blocker: NONE.
