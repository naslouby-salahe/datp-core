# P0-T007 — Configure Ruff lint and format

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T007` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P0-T007
2. **Title.** Configure Ruff lint and format
3. **Phase.** 0
4. **Status.** NOT_STARTED (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P1 — Mandatory
6. **Ticket type.** foundation
7. **Owner.** implementation-engineer
8. **Supporting agents.** naming-auditor
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE.
13. **Objective.** Configure Ruff for lint and format targeting Python 3.12, with import-hygiene and style rules that support the architecture's naming rules and prohibition on `import *` (Architecture §24).
14. **Why this ticket exists.** Style and import hygiene is a distinct tooling responsibility feeding the baseline gate (`P0-T026`); the Phase-0-start configuration targets Python 3.11, which must not silently persist once `P0-T002` moves the project to 3.12.
15. **Authority references.** Architecture §4.1 (Ruff's role: lint and format, tooling layer), §24 (naming/imports), §5.3 (structure rules, soft-cap guidance).
16. **Dependencies.** P0-T002.
17. **Blocks.** P0-T026, P0-T027.
18. **Downstream consumers.** `P0-T014` (Nox lint session binds to this configuration), `P0-T023` (structure/naming hook binds to this configuration), `P0-T027` (Sonar ingests Ruff's lint output).
19. **Preconditions.** `P0-T002` is `DONE`.
20. **Inputs.** Architecture §5.3/§24 naming and cohesion rules.
21. **Allowed scope.** The Ruff configuration table in `pyproject.toml`.
22. **Forbidden actions.** No suppression of the forbidden-name rule or the import-hygiene rule; no Python 3.11 target anywhere in the Ruff configuration.
23. **Required configuration or NONE.** The Ruff table only.
24. **Implementation responsibilities.** Set the Ruff target to Python 3.12; enable import sorting and the relevant lint-rule families (at minimum: pyflakes, pycodestyle, import conventions, naming); forbid `import *` outright; align the line-length setting with Architecture §5.3's soft-cap guidance (a lint hint, never a hard code-splitter that would fragment a cohesive module).
25. **Required unit tests.** NONE.
26. **Required property tests.** NONE.
27. **Required contract tests.** NONE.
28. **Required architecture tests.** NONE.
29. **Required integration tests.** Ruff lint and format run clean on the empty skeleton created by `P0-T005`.
30. **Required CUDA tests.** NONE.
31. **Required system/end-to-end tests.** NONE.
32. **Validation commands.** Ruff lint and Ruff format-check across `src/` and `tests/`.
33. **Acceptance criteria.** [ ] Ruff targets Python 3.12. [ ] Import hygiene (including no `import *`) is enforced. [ ] Ruff runs clean on the current skeleton.
34. **Completion evidence.** Changed files: the `pyproject.toml` Ruff table. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** NONE expected; a genuine Ruff-version/Python-3.12 incompatibility is recorded as a blocker rather than worked around by downgrading.
36. **Stop conditions.** None expected.
37. **Deliverables.** The Ruff configuration.
38. **Final review checklist.** [ ] No rule was silently disabled to force a clean run. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T002` is `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project — not only the Ruff table. Run Ruff against the entire tracked tree, not just files this ticket touched, and confirm zero pre-existing violation is silently left unaddressed once the new configuration is active (a violation that predates this ticket must still be fixed or explicitly blocked, never dismissed as legacy). A defect cannot be dismissed as pre-existing, another ticket's responsibility, outside the diff, or inconvenient.

### B3. Architecture boundary audit

`NOT_APPLICABLE — Ruff enforces style/import hygiene, not layer boundaries`; import-linter (`P0-T011`) and pytest-archon (`P0-T012`) own dependency-direction enforcement. Confirm this ticket's configuration does not attempt to duplicate or contradict those tools (for example, do not add a Ruff isort rule that would silently permit a forbidden cross-layer import ordering to look "correct").

### B4. Enum, dataclass, typing, and raw-dictionary audit

`NOT_APPLICABLE — no domain or application contract exists yet`. The relevant Ruff-adjacent check: confirm the enabled rule set includes rules that would flag an obviously untyped/raw pattern once source exists (this is a configuration-completeness check, not a source scan, since no source exists yet).

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of the required pattern list. Confirm the Ruff configuration itself contains no comment citing a ticket ID as its justification instead of the actual Architecture §24 rule.

### B6. Documentation and comment audit

Check for stale documentation describing the prior (Python 3.11-targeted, less strict) Ruff configuration; correct it wherever a document still describes the old rule set.

### B7. Complete validation requirements

Applicable: Ruff lint (Part A line 32); Ruff format-check; complete git-diff inspection; untracked-file/cleanup inspection. All other test-suite/tooling categories remain `NONE` per Part A lines 25–31, except that this ticket is itself the canonical source for validation items 12–13 (repository-wide Ruff lint and format check) referenced by every other Phase 0 ticket's own Section B7.

### B8. Pyright and Pylance requirements

`NONE` for this ticket's own scope (Ruff and Pyright are configured independently). `P0-T008` is the canonical owner of Pyright/Pylance configuration parity; this ticket must remain compliant, in particular by not enabling a Ruff formatting rule that would conflict with Pyright's strict-mode expectations (for example implicit-optional handling).

### B9. Sonar requirements

`NONE` for this ticket's own scope. `P0-T027` is the canonical owner of the Sonar quality gate; this ticket must remain compliant, in particular by ensuring Ruff's rule set does not contradict Sonar's own code-smell rules in a way that would force one tool's suppression to satisfy the other.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — no scientific configuration or CUDA execution path exists yet in Phase 0`; lint/format configuration has no seed, batch, or device behavior.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, or any Phase 1 behavior — it edits only the Ruff configuration table.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm no executable path for out-of-scope work was created.

**Audit 2 — Architecture fidelity.** Checkable directly: does the Ruff target actually read Python 3.12, and does `import *` actually fail lint (verified by a throwaway local check during implementation, not merely by reading the configuration)? Confirm naming-rule enforcement (Architecture §24) is genuinely active, not merely declared.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: is any Ruff rule category disabled wholesale (for example, an entire rule-family suppression) to make the skeleton pass, rather than the skeleton being genuinely clean? Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is not the canonical owner of any cross-cutting governance responsibility beyond its own scope. It must remain compliant with the gates owned by `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, and `P0-T027`.
