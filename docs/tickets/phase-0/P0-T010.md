# P0-T010 — Configure Hypothesis property-testing profiles

Standalone implementation specification. Where this file and `docs/MASTER_TICKET_LOG.md` disagree in wording, this file governs execution; the master log's Section G/H rows for `P0-T010` govern identity.

## Part A — Ticket template

1. **Ticket ID.** P0-T010
2. **Title.** Configure Hypothesis property-testing profiles
3. **Phase.** 0
4. **Status.** DONE (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P1 — Mandatory
6. **Ticket type.** foundation
7. **Owner.** implementation-engineer
8. **Supporting agents.** —
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE.
13. **Objective.** Configure Hypothesis with bounded, deterministic profiles (bounded example counts, deadlines, fixed seeds) for value-object range/finiteness properties and for the locked pure mathematical functions (for example `cv_fpr`, `pooled_variance`, `cliffs_delta`).
14. **Why this ticket exists.** Property testing has distinct determinism and timeout constraints (Architecture §21.2: bounded property tests) that are separate from unit-test configuration (`P0-T009`). An unbounded or non-deterministic property profile could hang a session or produce a flaky pass/fail.
15. **Authority references.** Architecture §4.1 (Hypothesis), §21.2 (property tests bounded by timeout/memory), §7 (value objects), §7.3 (non-finite rejection at construction).
16. **Dependencies.** P0-T009.
17. **Blocks.** P0-T026.
18. **Downstream consumers.** Every Phase 1 property test for a value object or locked pure function.
19. **Preconditions.** `P0-T009` is `DONE`.
20. **Inputs.** Architecture §7/§7.3/§21.2.
21. **Allowed scope.** Hypothesis profile registration under `tests/support/`.
22. **Forbidden actions.** No unbounded example generation; no order-dependent stateful test without deterministic seeding.
23. **Required configuration or NONE.** Hypothesis settings profiles (code-side registration).
24. **Implementation responsibilities.** Register a CI profile and a dev profile, both with bounded example counts and explicit deadlines; provide a deterministic derandomization option for the serialized CUDA/serial lanes.
25. **Required unit tests.** NONE — this ticket registers Hypothesis profiles only; there is no enum, class, or function of its own to unit-test.
26. **Required property tests.** A trivial property runs within the profile's bounded example count and deadline — a self-check of the profile registration itself, not of any domain behavior (none exists yet).
27. **Required contract tests.** NONE — no port/adapter contract exists yet for a property-testing profile to satisfy.
28. **Required architecture tests.** NONE — a Hypothesis profile enforces no layer boundary; the boundary-enforcement tools are `P0-T011`/`P0-T012`.
29. **Required integration tests.** The property lane collects successfully on the empty tree.
30. **Required CUDA tests.** NONE — no CUDA execution path exists in Phase 0.
31. **Required system/end-to-end tests.** NONE — no pipeline exists yet for an end-to-end test to exercise.
32. **Validation commands.** Property-lane collection and a bounded sample run of the trivial self-check property.
33. **Acceptance criteria.** [ ] Bounded, deterministic profiles are registered. [ ] Deadlines are set explicitly (not left at an implicit default). [ ] The self-check property runs within budget.
34. **Completion evidence.** Changed files: the Hypothesis profile registration under `tests/support/`. Commands run and results. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** A deadline exceedance fails the property test rather than being silently extended.
36. **Stop conditions.** None expected.
37. **Deliverables.** The Hypothesis profile registration.
38. **Final review checklist.** [ ] Profiles are bounded and deterministic. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T009` is `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project — not only `tests/support/`. Confirm no other test module registers a competing, uncoordinated Hypothesis profile that would silently override this ticket's bounds. A defect cannot be dismissed as pre-existing or inconvenient.

### B3. Architecture boundary audit

`NOT_APPLICABLE — no source package behavior exists yet`; the checkable item is that `tests/support/` itself does not import anything from `domain`/`application`/`infrastructure` at this stage (nothing exists yet to import).

### B4. Enum, dataclass, typing, and raw-dictionary audit

`NOT_APPLICABLE — no domain or application contract exists yet`. Confirm the profile registration itself is typed (Hypothesis's own `settings` object, not a raw dict of options).

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of the required pattern list. Confirm the profile registration contains no comment citing a ticket ID instead of the actual Architecture §21.2 rule.

### B6. Documentation and comment audit

Check for stale documentation describing an absent or different property-testing setup; correct every reference.

### B7. Complete validation requirements

Applicable: property-lane collection (Part A line 32); bounded self-check run; complete git-diff inspection; untracked-file/cleanup inspection. All other test-suite/tooling categories remain `NONE` per Part A lines 25–31.

### B8. Pyright and Pylance requirements

`NONE` for this ticket's own scope beyond compliance. `P0-T008` is the canonical owner of Pyright/Pylance configuration parity; this ticket must remain compliant, in particular by ensuring the profile-registration module itself passes strict typing once `P0-T008` is active.

### B9. Sonar and CodeScene requirements

`NONE` for this ticket's own scope. `P0-T027` is the canonical owner of the Sonar quality gate; this ticket must remain compliant. `P0-T028` is the independent, canonical owner of the CodeScene quality gate; this ticket must remain compliant with that gate as well.

### B10. Determinism and reproducibility audit

This ticket's entire objective is a determinism control: confirm every registered profile has an explicit, bounded example count and an explicit deadline (no implicit "unbounded until timeout" default), and confirm the derandomization option for the serialized lanes is genuinely deterministic (same seed, same generated examples, across repeated runs). All other CUDA/seed/batch items remain `NOT_APPLICABLE — no CUDA execution path exists yet in Phase 0`.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, or any Phase 1 behavior — it registers test-tooling profiles only, exercised solely by a trivial self-check property.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm no executable path for out-of-scope work was created.

**Audit 2 — Architecture fidelity.** Checkable directly: are the bounds (example count, deadline) actually finite and documented, satisfying Architecture §21.2's "bounded by timeout/memory" requirement, rather than left at a library default that happens to be finite but undocumented?

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could the "bounded" profile still take an impractically long time in aggregate once Phase 1 adds dozens of property tests, because the per-test bound is generous but nothing bounds the aggregate? Record this as a forward-looking risk in Remaining Risks rather than silently assuming it away. Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is not the canonical owner of any cross-cutting governance responsibility beyond its own scope. It must remain compliant with the gates owned by `P0-T008`, `P0-T022`, `P0-T023`, `P0-T024`, `P0-T025`, `P0-T026`, and `P0-T027`.

## Completion record

**2026-07-14 — Hypothesis profiles registered.** `P0-T009` verified `DONE` first. Lifecycle: `NOT_STARTED` → `IN_PROGRESS` → `IN_REVIEW` → `DONE`.

**Deliverables created:** `tests/support/hypothesis_profiles.py` registers two typed `hypothesis.settings` profiles — `ci` (`max_examples=100`, `deadline=400`ms, `derandomize=True` — deterministic, same generated examples every run, for CI and the serialized CUDA/serial lanes) and `dev` (`max_examples=25`, `deadline=400`ms, `derandomize=False` — faster local iteration, still bounded); the active profile is selected via `HYPOTHESIS_PROFILE` env var, defaulting to `ci`. `tests/conftest.py` imports the module (as `hypothesis_profiles as hypothesis_profiles`, the standard explicit-reexport idiom, so the side-effecting import is not flagged as unused by Ruff/Pyright) so registration/activation happens automatically for every pytest run. One required self-check property test: `tests/property/test_hypothesis_profile_self_check.py` (integer-addition commutativity — a trivial, framework-only check with no domain content, satisfying Part A line 26/32's explicit requirement for exactly this kind of test).

**Cross-ticket defect found and fixed (recorded transparently, revalidated in `P0-T008`):** writing the first real third-party import in the repository (`from hypothesis import settings`) surfaced that `[tool.pyright]` had no `venvPath`/`venv` binding, so Pyright could not resolve *any* installed dependency. Fixed by adding `venvPath = "."` / `venv = ".venv"` to `[tool.pyright]` (owned by `P0-T008`; addendum recorded there). Reverified clean repo-wide after the fix.

**Validation commands run:**
- `ruff check tests/` → found and fixed a real lint violation (`B002`, ambiguous double-unary-minus in the original self-check property `-(-value)`), rewrote the property to integer-addition commutativity instead, then `All checks passed!`; `ruff format --check tests/` → `3 files already formatted`.
- `pyright` (repository-wide) → `0 errors, 0 warnings, 0 informations`.
- `pytest tests/property --collect-only -q` → `1 test collected`.
- Determinism verification (Audit 3/B10 — not assumed): ran the `ci` profile twice back-to-back — both runs printed `derandomize=True` and passed identically; ran with `HYPOTHESIS_PROFILE=dev` — printed `derandomize=False`, `max_examples=25`, confirming the two profiles are genuinely distinct and the env-var switch works.
- Adversarial falsification check (Audit 2 — not assumed from config text): temporarily broke the property (`right + left` → `right - left`) and reran — Hypothesis genuinely generated a falsifying example (`left=1, right=0`) and failed within budget, proving the bounded example generation actually exercises the property rather than trivially passing; reverted and reran clean. **Investigation note:** the first revert-rerun attempt showed the test still failing with the *old* broken assertion visible in the traceback despite the source file being correctly restored — root-caused to a stale `__pycache__`/pytest-assertion-rewrite cache, not a real defect; clearing `__pycache__`/`.pytest_cache` resolved it and the test passed. This cache-staleness risk is noted for anyone rerunning bounded checks during Phase 1 test authoring.
- B5 scan across `tests/`, `src/`, `pyproject.toml` → zero matches.
- Cleanup: all `__pycache__`, `.pytest_cache`, `coverage.xml`, `.coverage` artifacts generated during validation were removed; `git status --porcelain` confirmed clean of them.

**Audit 1 — Roadmap fidelity.** PASS (`NOT_APPLICABLE`): no scientific artifact or claim introduced; the self-check property exercises only integer arithmetic, no domain content.

**Audit 2 — Architecture fidelity.** PASS: both profiles have explicit, finite bounds (`max_examples`, `deadline`) — never left at an implicit/unbounded default — satisfying §21.2's "bounded by timeout/memory" requirement; verified live via the falsification check above, not merely read from configuration.

**Audit 3 — Adversarial cross-check.** PASS, with one forward-looking risk recorded rather than assumed away: the per-test bound (100 examples / 400ms deadline under `ci`) is generous per test, but nothing yet bounds the *aggregate* runtime once Phase 1 adds dozens of property tests — flagged below in Remaining Risks for that phase's own Nox/CI timing to monitor, not silently assumed safe.

**Phase 0 boundary (B11).** Confirmed: only test-tooling profiles and one framework-only self-check property were added; no domain/application/infrastructure behavior, no Phase 1 content.

**Completion evidence.** Changed files: `tests/support/hypothesis_profiles.py`, `tests/conftest.py`, `tests/property/test_hypothesis_profile_self_check.py`, `pyproject.toml` (`[tool.pyright]` venv binding, cross-ticket fix), this ticket file, `docs/tickets/TICKET_STATUS.md`, plus the `P0-T008.md` addendum. Cleanup: all generated cache/coverage artifacts removed. Blocker: NONE. **Remaining risk for Phase 1:** aggregate property-suite runtime across many tests is not yet bounded by this ticket's per-test profile alone; Phase 1's Nox session timing should monitor this.
