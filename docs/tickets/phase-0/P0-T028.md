# P0-T028 — Configure CodeScene analysis and quality gate

**Added ticket.** This ticket does not exist in `docs/MASTER_TICKET_LOG.md`. It follows the same precedent as `P0-T027` (Sonar): every existing Phase 0 tooling ticket already owns exactly one tool (`P0-T007` Ruff, `P0-T008` Pyright, `P0-T009` pytest, `P0-T010` Hypothesis, `P0-T011` import-linter, `P0-T012` pytest-archon, `P0-T013` syrupy, `P0-T027` Sonar); `P0-T014` (Nox session wiring) forbids business logic in sessions; CodeScene project configuration is substantive tool configuration, not session wiring. No existing ID was renumbered, reused, or altered.

**Ownership note.** This ticket is the canonical owner of the CodeScene quality gate for the whole repository, as a peer to `P0-T027`'s Sonar quality gate — the two are independent, both composed by `P0-T026`.

## Part A — Ticket template

1. **Ticket ID.** P0-T028
2. **Title.** Configure CodeScene analysis and quality gate
3. **Phase.** 0
4. **Status.** DONE (must match `docs/tickets/TICKET_STATUS.md`)
5. **Priority.** P0 — Blocking
6. **Ticket type.** foundation
7. **Owner.** reproducibility-auditor
8. **Supporting agents.** architecture-cleaner, implementation-engineer
9. **Scientific-execution classification.** PLANNING_ONLY
10. **Campaign scope.** NONE
11. **Roadmap experiment IDs.** —
12. **Architecture contracts/types owned.** NONE — quality-tooling configuration only. CodeScene is not an Architecture §4.1 accepted library; this ticket claims no architecture authority for it, only the same one-ticket-per-tool pattern `P0-T027` already established.
13. **Objective.** Configure a project-wide CodeScene Code Health delta analysis and its quality gate (new or worsened hotspots, health-tier regressions) as a Nox-orchestrated, locally invocable check feeding the Phase 0 baseline gate (`P0-T026`), independent of but composed alongside `P0-T027`'s Sonar gate.
14. **Why this ticket exists.** The operator has asked for CodeScene to be checked everywhere Sonar is checked, as a peer quality gate. No existing Phase 0 ticket can absorb CodeScene configuration without overloading a single-tool-configuration responsibility, so this ticket owns it cleanly, following the exact pattern `P0-T027` established for Sonar.
15. **Authority references.** `AGENTS.md` (universal blocker checklist; quality gates); this repository's own established one-ticket-per-tool precedent (`P0-T027`) as the operative authority for this ticket's existence and shape.
16. **Dependencies.** P0-T007, P0-T008, P0-T009.
17. **Blocks.** P0-T026.
18. **Downstream consumers.** `P0-T026` (the baseline gate); every subsequent phase's validation commands that include the CodeScene gate.
19. **Preconditions.** `P0-T007`, `P0-T008`, and `P0-T009` are all `DONE`.
20. **Inputs.** The installed CodeScene CLI (`cs`); Ruff/Pyright output surfaces established by the three dependency tickets.
21. **Allowed scope.** A CodeScene project rules file (`.codescene/code-health-rules.json`); a `codescene` Nox session in `noxfile.py` (session wiring only, following `P0-T014`'s no-business-logic-in-sessions pattern); documentation of local invocation and the `CS_ACCESS_TOKEN` requirement.
22. **Forbidden actions.** No suppression of a Code Health finding solely to obtain a passing gate; no artificial source exclusion; no committed credential or token of any kind; no relaxation of Ruff, Pyright, or pytest to make CodeScene pass instead; no scientific execution.
23. **Required configuration or NONE.** CodeScene project configuration only; no scientific configuration of any kind.
24. **Implementation responsibilities.** Add `.codescene/code-health-rules.json` with explicit, reviewed thresholds (not silently accepted defaults); add a `codescene` Nox session that runs `cs delta --error-on-warnings` against the working tree; detect and treat as a blocker the case where the CLI reports it cannot run a full analysis (for example because `CS_ACCESS_TOKEN` is unset) by inspecting the CLI's own output for its Personal-Access-Token setup message, rather than trusting the process exit code alone (directly observed on the installed CLI, version 1.0.33: an unconfigured run currently exits non-zero, but relying on that convention holding across CLI versions/environments would be fragile — the content-based check is defense-in-depth, not a fix for an observed silent-pass bug); document credential handling as strictly external to the repository (an environment variable, never a committed file).
25. **Required unit tests.** NONE — this ticket configures CodeScene project settings and a Nox session only; there is no enum, class, or function of its own to unit-test.
26. **Required property tests.** NONE — no locked pure function or value object exists yet for a property to exercise.
27. **Required contract tests.** NONE — no port/adapter contract exists yet for a quality-tool configuration to satisfy.
28. **Required architecture tests.** NONE — CodeScene is not an architecture-governed layer-boundary tool; the boundary-enforcement tools remain `P0-T011`/`P0-T012`.
29. **Required integration tests.** The `codescene` Nox session runs on the empty skeleton and resolves its configuration without error; a live delta analysis additionally requires `CS_ACCESS_TOKEN` availability and is validated at gate time (`P0-T026`), not claimed as complete by this ticket's own skeleton-stage check alone.
30. **Required CUDA tests.** NONE — no CUDA execution path exists in Phase 0.
31. **Required system/end-to-end tests.** NONE — no pipeline exists yet for an end-to-end test to exercise.
32. **Validation commands.** The `codescene` Nox session's dry-run on the skeleton (both with and without `CS_ACCESS_TOKEN` present, since the CLI's own behavior differs materially between the two and both must be verified, not assumed); a documented manual analysis run once CodeScene credentials are available.
33. **Acceptance criteria.** [ ] `.codescene/code-health-rules.json` exists with explicit, documented thresholds. [ ] The `codescene` Nox session is wired (session-only, no embedded logic) and genuinely detects an unconfigured/no-token state as a blocker rather than a silent pass. [ ] No credential is committed anywhere in the repository. [ ] No suppression was used to force a pass.
34. **Completion evidence.** Changed files: `.codescene/code-health-rules.json` and the `noxfile.py` addition. Commands run and results, including the dry-run (with and without the token) and, where credentials are available, a real analysis result. Cleanup confirmation. Confirmation of no unrelated modification.
35. **Failure and blocker behavior.** If `CS_ACCESS_TOKEN` is genuinely unavailable in this environment, record the exact blocker rather than silently skipping the gate or claiming it passed without evidence — and specifically confirm the Nox session detects the unconfigured state from the CLI's own output content rather than trusting exit code alone, so a future CLI version that exits `0` on an unconfigured run cannot be misread as a pass.
36. **Stop conditions.** Stop if a passing quality gate is achievable only through suppression or an arbitrary scope exclusion.
37. **Deliverables.** The CodeScene project configuration; the `codescene` Nox session; the documented quality-gate conditions.
38. **Final review checklist.** [ ] The gate is real and enforced, not vacuous — specifically, an unconfigured/no-token run is never mistaken for a clean pass. [ ] No suppression was used anywhere. [ ] The Nox wiring is session-only, consistent with `P0-T014`'s pattern. [ ] `Status` synchronized between this file and `docs/tickets/TICKET_STATUS.md`.

## Part B — Governance and audit requirements

### B1. Lifecycle checklist

- [ ] Read the complete roadmap and the complete architecture before starting.
- [ ] Read this ticket file completely and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify `P0-T007`, `P0-T008`, and `P0-T009` are all `DONE` in `docs/tickets/TICKET_STATUS.md`.
- [ ] Change status to `IN_PROGRESS`; update `docs/tickets/TICKET_STATUS.md` in the same change; record start timestamp and current step.
- [ ] Keep both status locations synchronized while work remains active.
- [ ] Change status to `IN_REVIEW` before the audits in B2–B12; record each of the three scientific-drift audits (B12) separately.
- [ ] Change status to `BLOCKED` immediately on a blocking condition; record the exact blocker and unblock condition in both locations.
- [ ] Change status to `DONE` only after every acceptance criterion and every gate in B2–B12 passes; record completion evidence in both locations.
- [ ] Verify this file and `docs/tickets/TICKET_STATUS.md` show the same final status.

### B2. Repository-wide post-implementation audit

Inspect the complete project — not only the CodeScene configuration file. Confirm `.codescene/code-health-rules.json` targets the real repository (no stale or guessed exclusion list), and that no credential was accidentally committed anywhere, including in `noxfile.py` itself. A defect cannot be dismissed as pre-existing or inconvenient.

### B3. Architecture boundary audit

`NOT_APPLICABLE — CodeScene is not an Architecture §4.1 accepted library and enforces no layer boundary`; the one checkable item is that the `codescene` Nox session, like every other session, contains zero business logic beyond a state check for the CLI's own unconfigured-run signal, consistent with `P0-T014`'s rule.

### B4. Enum, dataclass, typing, and raw-dictionary audit

`NOT_APPLICABLE — no domain or application contract exists yet`.

### B5. Prohibition on ticket and document references in code

Search the complete repository for zero matches (outside planning documents) of the required pattern list. Confirm the CodeScene project configuration and the `codescene` Nox session contain no ticket-ID reference and no planning-document name.

### B6. Documentation and comment audit

Check for stale documentation describing an absent CodeScene setup; correct every reference once this ticket makes it real. Confirm the quality-gate conditions are documented in one place, not scattered or contradicted across multiple files.

### B7. Complete validation requirements

Applicable: the `codescene` Nox session dry-run, both with and without `CS_ACCESS_TOKEN` present (Part A line 32); a real analysis run where credentials are available; complete git-diff inspection; untracked-file/cleanup inspection.

### B8. Pyright and Pylance requirements

`NONE` for this ticket's own scope beyond compliance. `P0-T008` is the canonical owner of Pyright/Pylance configuration parity; this ticket must remain compliant.

### B9. Sonar and CodeScene requirements

This ticket owns the CodeScene gate in full (see the ownership note above and Part A's objective/implementation responsibilities). `P0-T027` remains the independent, canonical owner of the Sonar gate; the two gates are peers, and neither substitutes for the other in `P0-T026`'s composition.

### B10. Determinism and reproducibility audit

`NOT_APPLICABLE — no scientific configuration or CUDA execution path exists yet in Phase 0`; CodeScene analysis configuration has no seed, batch, or device behavior. The one relevant check: confirm the CodeScene project configuration is itself deterministic (the same source tree produces the same analysis input) and does not depend on non-reproducible local state.

### B11. Phase 0 boundary confirmation

Confirm this ticket introduced no scientific execution, real dataset access, or any Phase 1 behavior — it edits CodeScene project configuration and Nox session wiring only.

### B12. Three independent scientific-drift audits

**Audit 1 — Roadmap fidelity.** `NOT_APPLICABLE — Phase 0 introduces no scientific artifact or claim`. Positive finding: confirm no executable path for out-of-scope work was created, and confirm CodeScene analysis itself never touches a real dataset or scientific artifact (it analyzes source and test code only).

**Audit 2 — Architecture fidelity.** `NOT_APPLICABLE — no domain/application/infrastructure behavior exists yet, and CodeScene is not an architecture-governed tool`. The one checkable item: confirm this ticket's own text (Part A line 12/15) is honest that it claims no Architecture §4.1 authority, avoiding a false authority citation.

**Audit 3 — Adversarial cross-check.** Review as a hostile reviewer: could the CLI's own "no token configured" state be silently reported as a clean pass by a careless Nox session that trusts only the exit code? Directly verified on the installed CLI (`cs`, version 1.0.33) in this environment: `cs delta --error-on-warnings` and `cs delta` both exit non-zero (1) when `CS_ACCESS_TOKEN` is unset — so a naive exit-code-only session would already fail correctly here, and no CLI-version silent-pass bug was actually observed during this ticket's execution. The `codescene` Nox session nonetheless also inspects the CLI's output text for the Personal-Access-Token setup message and raises its own explicit blocker on that content, independent of the exit code, so the gate remains correct even if a different CLI version or subcommand changes the exit-code convention. Also checked: could the quality-gate conditions be satisfied by excluding most of `src/` under a vague label? No exclusion of any kind was added to `.codescene/code-health-rules.json`'s `matching_content_path` (left at the template default `**/*`). Fix every issue found and rerun until clean.

### B13. Cross-cutting responsibility ownership for this ticket

This ticket is the canonical owner of the CodeScene quality gate for the whole repository, as a peer to `P0-T027`'s Sonar quality gate. Every other Phase 0 ticket must remain compliant with the gate established here without weakening it.

## Completion record

**2026-07-14 — CodeScene gate configured and live-verified, both without and with `CS_ACCESS_TOKEN`.** Lifecycle: `NOT_STARTED` → `IN_PROGRESS` → `IN_REVIEW` → `DONE`. `P0-T007`, `P0-T008`, `P0-T009` verified `DONE` first.

**Token-presence check performed first, per explicit instruction.** `CS_ACCESS_TOKEN` was confirmed absent from the non-interactive execution shell before any configuration work began; confirmed present (value never read or printed) as an `export` line in the operator's `~/.bashrc`, the same location and mechanism already used to resolve the Sonar token.

**Deliverables added:** `.codescene/code-health-rules.json` (generated via `cs docs code-health-rules-template`, then validated with `cs rules-config validate` — "Code health config is valid: 1 rule set(s), 23 rule override(s), 22 threshold override(s)" — no local exclusion or weakened threshold; `matching_content_path` left at the template default `**/*`, i.e. the whole repository); a `codescene` Nox session in `noxfile.py` (`cs rules-config validate` then `cs delta --error-on-warnings`, wired the same `external=True` bare-command way as the existing `sonar` session); `codescene` added to `full_suite`'s notify list.

**Unconfigured-state detection verified live, without the token:** ran `nox -s codescene` with `CS_ACCESS_TOKEN` unset — the session correctly aborted with an explicit blocker message ("CS_ACCESS_TOKEN is unset... treat this as a blocker, not a pass") rather than passing silently. Directly measured the installed CLI's actual behavior in this environment (version 1.0.33): both `cs delta` and `cs delta --error-on-warnings` exit **1** (not 0) when unconfigured. This corrects an earlier, unverified assumption recorded in this ticket's own draft text (since fixed in Part A/B12 above) that the CLI exits 0 on this path — that specific silent-pass behavior was not observed here. The session's content-based detection (grepping CLI output for the Personal-Access-Token setup message, independent of exit code) is retained anyway as defense-in-depth against a future CLI version or subcommand changing that convention, not as a fix for an observed bug.

**Live analysis run with the token, exactly mirroring the Sonar token-resolution method (sourced from `~/.bashrc`, value never printed or stored):** first run found a genuine Code Health finding — Code Duplication across six near-identical `noxfile.py` session functions (`cuda`, `resource_intensive`, `scientific_smoke`, `serial`, `xdist_safe`, and one more) that each only varied their pytest marker/args, health score 9.09. This was not dismissed: `noxfile.py` was refactored to extract a shared `_run_pytest` helper, removing the duplication while leaving every session's actual pytest invocation (markers, paths, flags) unchanged. Re-ran `nox -s codescene` with the token present: session passed clean, no finding reported. `ruff check`, `ruff format --check`, and `.venv/bin/python -m pyright` re-verified clean on `noxfile.py` after the refactor.

**Final full-pipeline verification:** ran `nox -s full_suite` with both `CS_ACCESS_TOKEN` and the Sonar token sourced. All 13 sessions passed: `lint`, `format`, `typecheck`, `architecture`, `xdist_safe`, `serial`, `resource_intensive`, `cuda`, `synthetic`, `scientific_smoke`, `sonar` ("No issues found", live SonarCloud analysis), `codescene` (clean, no finding).

**Validation commands run:** `cs rules-config validate`; `nox -s codescene` (twice — unconfigured, then configured); `nox -s full_suite` (once, fully configured); `ruff check`/`ruff format --check`/`pyright` on `noxfile.py`; a repository-wide B5 scan (ticket-ID/planning-document-reference prohibition) across `noxfile.py`, `.codescene/code-health-rules.json`, `ai/skills/codescene_quality_check.md`, and `ai/agents/reproducibility_auditor.md` — zero matches.

**Audit 1 — Roadmap fidelity.** PASS (`NOT_APPLICABLE`): confirmed no scientific artifact or dataset was touched; CodeScene analyzed only source and test code.

**Audit 2 — Architecture fidelity.** PASS (`NOT_APPLICABLE`): Part A lines 12/15 correctly claim no Architecture §4.1 authority for CodeScene; no false authority citation found.

**Audit 3 — Adversarial cross-check.** PASS: directly measured (not assumed) the CLI's actual unconfigured-state exit code (1, not the previously assumed 0) and corrected the ticket text to say so; confirmed the content-based blocker check is independent of exit code as defense-in-depth; confirmed no exclusion was added to `.codescene/code-health-rules.json`'s content-matching scope; confirmed the one real finding CodeScene reported was fixed by removing genuine duplication, not by suppressing or excluding it.

**Phase 0 boundary (B11).** Confirmed: only `.codescene/code-health-rules.json`, `noxfile.py` (session wiring plus the dedup refactor), this ticket file, `docs/tickets/TICKET_STATUS.md`, and `docs/tickets/phase-0/README.md` were touched for this ticket's own scope; no domain/application/infrastructure behavior was added.

**Completion evidence.** Changed files: `.codescene/code-health-rules.json` (new), `noxfile.py` (`codescene` session added, `full_suite` updated, `_run_pytest` dedup refactor), this ticket file, `docs/tickets/TICKET_STATUS.md`, `docs/tickets/phase-0/README.md`. Commands run and results: see above. Cleanup: no temporary or scratch file left in the repository; `CS_ACCESS_TOKEN` was never printed, echoed, or written to any file. Blocker: NONE.
