# P4-T022 — Implement journal expected-artifact/table/figure inventory and completeness audit

Standalone Phase 4 implementation specification. The master log remains authoritative for canonical identity, phase membership, ownership, dependency graph, and gate coverage. This documentation file authorizes no work by itself.
## Part A — Ticket template


- **Ticket ID.** P4-T022
- **Title.** Implement journal expected-artifact/table/figure inventory and completeness audit
- **Phase.** 4
- **Status.** NOT_STARTED
- **Priority.** P0 — Blocking
- **Ticket type.** audit
- **Owner.** roadmap-orchestrator
- **Supporting agents.** report-output-auditor, experiment-engineer
- **Scientific-execution classification.** PLANNING_ONLY
- **Campaign scope.** JOURNAL
- **Roadmap experiment IDs.** all
- **Architecture contracts/types owned.** `JournalInventory`, `CompletenessAudit`, `ExpectedArtifactSpec`, `TraceabilityMatrix`
- **Objective.** Produce the journal expected-artifact/table/figure/export inventory and the implementation-completeness audit certifying every roadmap experiment ID has specification/config/identity/tests/expected-artifacts/output coverage, with no journal real run performed. This is an inventory input to the Phase 4 gate.
- **Why this ticket exists.** Phase 5 planning depends on a complete, audited implementation; this gate proves every mandatory/optional/rejected experiment is represented.
- **Authority references.** Arch §22.5 (traceability matrix), §26; Road §9; this backlog §F (Phase 4 gate).
- **Dependencies.** P4-T002, P4-T003, P4-T004, P4-T005, P4-T006, P4-T007, P4-T008, P4-T009, P4-T010, P4-T011, P4-T012, P4-T013, P4-T014, P4-T015, P4-T016, P4-T017, P4-T018, P4-T019, P4-T020, P4-T021.
- **Blocks.** P4-T026.
- **Downstream consumers.** P4-T026 (the Phase 4 gate) consumes the journal inventory and completeness audit to verify readiness for Phase 5 planning.
- **Preconditions.** all P4 implementation tickets complete.
- **Inputs.** Arch §22.5, §26; Road §9; backlog §F.
- **Allowed scope.** Inventory enumeration; completeness auditing.
- **Forbidden actions.** No journal execution; no missing experiment ID; no supportive-to-confirmatory promotion.
- **Required configuration or NONE.** None beyond the implemented configs.
- **Implementation responsibilities.** Enumerate expected artifacts/tables/figures/exports; map each roadmap ID to its implementation and outputs; record the audit.
- **Required unit tests.** NONE.
- **Required property tests.** NONE.
- **Required contract tests.** NONE.
- **Required architecture tests.** Every roadmap ID present with coverage; rejected IDs non-executable.
- **Required integration tests.** NONE.
- **Required CUDA tests.** NONE.
- **Required system/end-to-end tests.** All implemented experiments run on synthetic data; inventory shape (fixed-input regression check).
- **Validation commands.** Architecture; synthetic system; golden lanes.
- **Acceptance criteria.** [ ] Every roadmap ID has full coverage. [ ] Expected artifact/table/figure/export inventory complete. [ ] No journal real run. [ ] Audit recorded.
- **Completion evidence.** Changed files/records (inventory, audit); commands run and results; recorded identities; cleanup confirmation.
- **Failure and blocker behavior.** A missing experiment/output blocks the gate. Stop if any mandatory experiment ID lacks coverage.
- **Stop conditions.** Stop if any mandatory experiment ID lacks coverage.
- **Deliverables.** Journal inventory + completeness audit.
- **Final review checklist.** [ ] Complete on synthetic data only.

> **Relocated in an earlier revision; full-template-normalized and cross-checked in this revision.** The four tickets below (P4-T023–P4-T026) originally existed only in Section F and were moved here so that Section G's index rows have a corresponding body. All four use the mandatory 38-field template. The E-O1/E-Q separation is now resolved: `P4-T020` (E-O1, mandatory) no longer carries E-Q-scoped content, and Section K.1's `E-Q1`–`E-Q6` rows now correctly cite `P4-T023` (not `P4-T020`) as their implementing ticket. The Phase 7 originality-before-closure ordering was independently audited and confirmed already correct (`P7-T012` is a precondition of `P7-T011` in both Section G and Section H).


## Part B — Mandatory Phase 4 lifecycle, implementation-only boundary, and audits

### Authority-reconciliation note

The master-log Section G index and Section H body disagree on this ticket's dependencies and downstream block. The Part A fields above preserve the detailed Section H body. This ticket may enumerate `P4-T023`–`P4-T025` records, but it cannot certify their terminal completion: `P4-T026` alone verifies `P4-T022`–`P4-T025` and blocks Phase 5. Record the discrepancy as an authority finding; do not silently alter either master-log source.

### B1. Lifecycle and status discipline

- [ ] Immediately before work, read the complete roadmap, complete architecture, this ticket, every direct dependency, downstream consumers, the Phase 4 index, and `docs/tickets/TICKET_STATUS.md`.
- [ ] Verify every dependency has its required terminal state. Synchronize `NOT_STARTED → READY → IN_PROGRESS → IN_REVIEW → DONE`; record timestamps, current step, independent audits, evidence, blockers, and exact unblock conditions.
- [ ] Set both records to `BLOCKED` immediately on an authority, feasibility, lineage, or implementation blocker. Confirm all validation uses synthetic data, test doubles, dry runs, or read-only frozen-anchor-artifact inspection.

### B2. Phase 4 scientific and execution locks

Phase 4 implements and synthetically validates the journal extension; it never performs real journal training, scoring, threshold sweeps, q-sensitivity, shrinkage, B2-conf, B4 ablations, FedProx, personalization, external, temporal, reduced-seed/round/row, one-cell debug, real-data smoke, or integration experiment. If real execution would be required for validation, record a readiness blocker.

The sole confirmatory endpoint remains Regime A, B1 versus B2, CV(FPR), paired ten-seed deltas, 95% BCa interval, and positive-direction survival. Within every compatible ladder cell one fixed model state and the same calibration/test score artifacts feed B1–B4; threshold scope alone varies. No policy-specific retraining, checkpoint selection, preprocessing, or eligibility is allowed. Keep stress/comparator/external/temporal work outside the causal ladder. Never call a `FedRep-AE` or `FedPer-AE` fallback Ditto, and never reuse generic `B5`.

Feasibility, rejection, suppression, unavailable, boundary-condition, and technical-blocker outcomes must be typed and honest. Never fabricate partitions, timestamps, a reduced client count relabeled as the original regime, or pseudo-time. Frozen accepted anchor artifacts are read-only; never overwrite anchor manifests, identities, evidence, or namespaces, and never bypass `P3-T011`. Lineage mismatch never authorizes Phase 4 recomputation: record a requirement for a future authorized campaign or use only synthetic fixtures.

### B3. Repository-wide, architecture, typing, and validation audits

After future implementation, audit the full repository—not only the diff: `src/`, `tests/`, `configs/`, `ai/`, `scripts/`, `docs/`, `pyproject.toml`, `noxfile.py`, import-linter/Ruff/Pyright/Pylance/pytest/Sonar configuration, provider adapters, and root metadata. No authority-grounded defect is excused as pre-existing, another agent's work, out of diff, legacy, or untouched.

Check dependency direction, framework confinement, type/config boundaries, frozen scientific specifications, exhaustive dispatch, no universal context/service locator, composition-only wiring, stage identities/fingerprints, lineage reuse, namespace separation, atomic immutable persistence, typed lifecycle, determinism, CUDA ownership, no CPU fallback/batch reduction/precision change, spawn-safe process boundaries, no side effects, and no aliases, redirects, shims, compatibility wrappers, or generic utilities. Scan for raw protocol strings/dicts, object-shaped dictionaries, `dict[str, Any]`, broad `Any`/`object`, unexplained casts/ignores, mutable specs, pseudo-objects, Boolean variants, hidden optional variants, generic request/result names, incomplete enums, and scattered defaults. Search source/test code (planning Markdown excepted) for ticket IDs and planning-document references. Audit stale documents/status claims/paths/names, unsupported claims, broken links, copied authority prose, and AI/banner/decorative comments.

Run applicable Part A tests plus Ruff lint/format, Pyright strict, Pylance parity when available, import-linter, pytest-archon, complete and randomized-order suites, mandatory Nox sessions, serialized CUDA where applicable, Sonar, diff/untracked-file inspection, and cleanup. Record all unavailable or `NONE` categories honestly.

### B4. Three independent audits and completion evidence

- **Audit 1 — Roadmap fidelity.** Verify no journal execution, locked confirmatory endpoint, fixed-encoder ladder, correct role/tier and stress-test separation, comparator naming, typed feasibility/suppression, anchor/journal separation, and no result-driven protocol change.
- **Audit 2 — Architecture fidelity.** Verify layers, types, configuration, identities, lineage/reuse, persistence, lifecycle, determinism, CUDA, batching, process isolation, no raw dictionaries, and no compatibility scaffolding.
- **Audit 3 — Adversarial cross-phase review.** Cross-check roadmap, architecture, master ticket, dependencies, implementation, tests, configuration, artifacts, status, and recorded evidence for Phase 3 leakage, unauthorized journal execution, anchor-derived journal tuning, null anchor treated as failure, hidden scientific changes, failed artifacts overwritten, B1–B4 retraining, B0/FedAvg mixing, invalid B4, fake feasibility, partial audit claims, or claimed-but-unrun tools.

Record the three audits separately. Before `DONE`, retain an evidence ledger for dependencies, changed files/reasons, actions, exact commands/results, Part A tests or explicit `NONE`, quality gates, documentation/raw-dictionary/`Any`/planning-reference/no-backward-compatibility audits, determinism/lineage/namespace evidence, cleanup, final diff, untracked files, blockers, risks, and skipped checks. This is not authorization for journal scientific execution.

