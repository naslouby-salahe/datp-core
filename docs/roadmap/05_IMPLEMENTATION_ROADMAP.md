# 05 — Implementation Roadmap

**Purpose.** Own the implementation-oriented requirements already present in the source roadmap: the scratch-implementation boundary, execution phases, the stored-score-versus-retraining split, the preserved threshold-scope semantics, the federated-quantile backbone contract, the `B-FedStatsBenign` computational contract, and determinism/traceability requirements. This file records *requirements*, not software design that the source roadmap does not prescribe.

**What this file owns.**
- Scratch implementation boundary and behavioral-reference restrictions.
- Design principles for the from-scratch codebase (as stated in the roadmap).
- Required execution phases, implementation sequence, and experiment dependency order.
- Stored-score work versus retraining-required work.
- Preserved threshold-scope semantics (behavior only).
- Federated-quantile backbone conceptual contract.
- `B-FedStatsBenign` locked computational contract.
- Determinism, reproducibility, provenance/manifest, and implementation-clarity requirements.

**What this file does not own.**
- Experiment definitions, prerequisites, and the experiment matrix → see [03 — Experiment Catalogue](./03_EXPERIMENT_CATALOGUE.md).
- Metric definitions, statistics, and reporting traceability requirements → see [04 — Evaluation and Reporting Protocol](./04_EVALUATION_AND_REPORTING_PROTOCOL.md).
- Claim consequences and decision rules → see [02 — Claims and Decision Rules](./02_CLAIMS_AND_DECISION_RULES.md).
- Scientific identity, nomenclature, and scope boundaries → see [01 — Scientific Identity and Scope](./01_SCIENTIFIC_IDENTITY_AND_SCOPE.md).

> **Scope note.** This file does not introduce architecture classes, modules, dataclasses, enums, filenames, APIs, or directory layouts beyond what the source roadmap explicitly states. Where the roadmap states a requirement but does not prescribe software design, the requirement is retained as-is rather than turned into a design.

**Related files.** [00 — Index](./00_ROADMAP_INDEX.md) · [02 — Claims](./02_CLAIMS_AND_DECISION_RULES.md) · [03 — Experiments](./03_EXPERIMENT_CATALOGUE.md) · [04 — Evaluation](./04_EVALUATION_AND_REPORTING_PROTOCOL.md)

---

## Scratch implementation boundary and behavioral reference

The journal-extension implementation is written from scratch for clarity. `/home/naslouby/Projects/datp` is consulted **only** as a behavioral reference for original DATP logic (threshold policies, calibration/test split semantics, score-artifact reuse, result interpretation). Nothing below reuses that project's source layout.

**Design principles.**
- No backward compatibility with the reference project's structure; no shims, redirects, or path aliases; no migration layers.
- No legacy naming that confuses the journal design; no stale policy labels leaking into new claims.
- No over-engineered boilerplate, no artificial abstraction layers, no autogenerated-looking or bloated comments. Comments only where they explain a non-obvious scientific or statistical decision.
- Clean enums, frozen dataclasses, and configs; explicit policy definitions; deterministic seeds; reproducible run manifests; testable threshold-policy contracts.
- Conceptual separation (not a folder listing) of: raw data, processed splits, per-client score artifacts, threshold outputs, metrics, reports, and manuscript exports — each stage traceable to a manifest.
- Clear experiment IDs (as in [03 — Experiment Matrix](./03_EXPERIMENT_CATALOGUE.md#experiment-matrix)) and clean, conceptual output naming; every table and figure traces to a manifest.

---

## Threshold-scope semantics preserved from the reference project (behavior only)

- Train once per (dataset, regime, seed, α); freeze the AE; score calibration and test benign/attack under the fixed AE; derive B1–B4 from shared score artifacts without retraining.
- Benign-only calibration; n_min = 100 eligibility with τ_global fallback for calibration-pending clients; coverage reported as |K_elig|/|K|.
- CV(FPR) computed over eligible clients; absolute-dispersion checks alongside. (Metric definitions: [04](./04_EVALUATION_AND_REPORTING_PROTOCOL.md#metrics).)

---

## Federated-quantile backbone (conceptual contract)

A single quantile-estimation interface underlies B1 (client-averaged local p95), B1-pool (pooled p95), B1-wt (weighted), B2 (local p95), and `B-FedStatsBenign`, so estimation error, threshold variance, and FPR-target attainment are computed uniformly and comparably. No novel estimator is introduced.

---

## `B-FedStatsBenign` locked contract (before any computation)

Client message: benign-only count n_k, mean µ_k, variance σ_k² (no raw scores, no attack labels). Sample-count-weighted global mean. **Full pooled variance including the between-client mean-shift term:** σ²_global = Σ n_k·[σ_k² + (µ_k − µ_global)²] / Σ n_k. Report within_term, between_term, and between_ratio = between_term / (within_term + between_term) in the main text; if between_ratio > 0.5, state that the between-client mean shift dominates and a single global summary threshold is structurally strained. Matched-exceedance operating point on a fixed candidate grid τ(k) = µ_global + k·σ_global, k ∈ {0.00, …, 5.00} at step 0.01, selecting k* = argmin |Σ c_k(k)/Σ n_k − (1−q)| with ties broken toward larger k; only benign exceedance counts are exchanged. Fixed-k variants (k ∈ {2.0, 2.5, 3.0}) are supplementary sensitivity only.

> Locked by [SB-22, SB-26, SB-27](./01_SCIENTIFIC_IDENTITY_AND_SCOPE.md#scope-boundaries-stable-labels). Experiment: [E-T3](./03_EXPERIMENT_CATALOGUE.md#mandatory-supportive--mechanism--external--stress).

---

## Execution phases and dependency order

The source roadmap organizes execution around a path-free gate/phase narrative (existing-score extensions → dataset expansion → training-side stress tests) rather than a repository layout.

- **Phase 1 — Stored-score work (immediate-start, no retraining).** All existing-score extensions: E-C1 (10-seed confirmatory), E-S1/S2/S3, E-M1–M5, E-V1/V2/V3, E-T3 (`B-FedStatsBenign`), E-O1, plus Appendix A. These populate the confirmatory and most supportive claims and close the tautology critique before any new dataset or training infrastructure is touched.
- **Phase 2 — Dataset expansion (retraining-required).** Edge-IIoTset external validation (E-X1) and the temporal recalibration MVE (E-B1), gated on eligibility coverage (n_k ≥ 100 for ≥ 90% of clients) and the first-principles partition audit.
- **Phase 3 — Training-side stress tests (retraining-required).** FedProx (E-T1) and model-personalization (E-T2) encoders crossed with B1–B4.

Per-experiment blocking dependencies are the `Blocking deps` column of [03 — Experiment Matrix](./03_EXPERIMENT_CATALOGUE.md#experiment-matrix). Feasibility gates that block execution are recorded as decisions in [07 — Go / No-Go](./07_AUDIT_AND_DECISION_LOG.md#go--no-go--conditional-go-summary).

---

## Provenance and traceability

- Experiment manifests are reproducible: config → checkpoint → scores → metrics → table/figure.
- Every table/figure traces to a manifest. (Reporting-side traceability requirement: [04](./04_EVALUATION_AND_REPORTING_PROTOCOL.md#reporting-tables-figures-and-placement).)
- Deterministic seeds and reproducible run manifests; testable threshold-policy contracts.

---

## Implementation-clarity checklist

- ☐ Policy definitions are explicit (enum-backed, no stale labels).
- ☐ Threshold computation is isolated from scoring and from metrics.
- ☐ Calibration data is separated from test data at the type level.
- ☐ Metric computation is deterministic and unit-tested.
- ☐ Regime definitions are explicit and enumerated.
- ☐ Experiment manifests are reproducible (config → checkpoint → scores → metrics → table/figure).
- ☐ Every table/figure traces to a manifest.
- ☐ No test metric selects the main checkpoint.
- ☐ No hidden default changes threshold semantics.
- ☐ No old naming leaks into new claims.
