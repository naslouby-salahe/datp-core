# Phase Master Log

## Scientific authority and interpretation rules

- Before planning, editing, testing, or auditing this phase, read **`/home/naslouby/Projects/datp-core/docs/Journal_Extension_Master_Roadmap.md`** in full. It is the authoritative source for the scientific question, permitted evidence, dataset boundaries, numerical grids, metrics, inference, and claim restrictions.
- Use descriptive implementation identities only. Never introduce opaque lettered populations, numbered threshold policies, numbered baselines, compatibility aliases, redirects, deprecated names, or duplicated identifiers.
- The centralized reference is an independent pooled-data pipeline. It is never a federated threshold method and never consumes scores produced by a federated model.
- The confirmatory comparison reuses one selected FedAvg detector, one preprocessing state, one client population, one calibration set, and one held-out score set per seed. Only threshold-calibration scope changes.
- Calibration is benign-only. Attack labels and held-out outcomes cannot select models, checkpoints, quantiles, shrinkage values, statistical coefficients, clients, or group assignments.
- The implementation source tree is locked to the files already created under `datp_core/`. Do not create, rename, move, delete, or replace source files. Test files may be created only when explicitly named in this roadmap.
- Scientific values absent from the source of truth must remain unresolved. Do not infer them from memory, historical repositories, convenient defaults, or common practice. Record the blocker in `01_PHASE_MASTER_LOG.md`.
- Python protocol declarations replace YAML. Protocol objects are immutable, fully typed, explicitly constructed, validated as one graph at startup, and serialized into every resolved experiment manifest.
- Do not add backward compatibility, migration adapters, aliases, generic registries, service locators, untyped dictionaries, `Any`, silent fallbacks, or catch-all modules.
- Do not add comments that restate code. Express intent through names, enums, types, validated records, and small functions.
- Reusable canonical and preprocessed data belong under `data/`. Experiment-specific trained states, scores, thresholds, evaluations, analyses, and reports belong under `outputs/`.

## Status vocabulary

Use exactly one status per phase:

- `NOT_STARTED`
- `IN_PROGRESS`
- `BLOCKED_SCIENTIFIC_VALUE`
- `BLOCKED_DEPENDENCY`
- `IMPLEMENTED_NOT_AUDITED`
- `AUDIT_FAILED`
- `COMPLETE`

Do not add percentages. A phase is binary with respect to its exit criteria.

## Current phase ledger

| Phase | Status | Entry criteria | Exit evidence | Scientific blockers |
|---|---|---|---|---|
| 01 — Scientific identity and scope | `COMPLETE` | Source tree exists | Identity tests and scope audit | None |
| 02 — Typed protocols and domain contracts | `COMPLETE` | Phase 01 complete | Protocol graph validation and strict typing | None; optional alert-burden translation is explicitly suppressed without rate evidence |
| 03 — Dataset audit and capabilities | `IN_PROGRESS` | Phase 02 complete | Audited schemas, capability contracts, canonical materialization, and full static/test gates | No unresolved dataset-science decision; final quality-gate verification remains |
| 04 — Canonical data and reusable preprocessing | `NOT_STARTED` | Phase 03 complete | Deterministic reusable data manifests and reload checks | Exact preprocessing protocol must be present in source truth |
| 05 — Populations, splits, and cohorts | `NOT_STARTED` | Phase 04 complete | Deterministic split/cohort manifests | Any unspecified non-temporal split ratios remain blocking |
| 06 — Anchor reproduction and gate | `NOT_STARTED` | Phase 05 complete | Explicit equivalence/discrepancy decision | Metric-specific tolerances absent from source truth remain blocking |
| 07 — Centralized reference | `NOT_STARTED` | Phase 05 complete | Independent pooled execution and tests | Centralized training values absent from source truth remain blocking |
| 08 — Federated training, checkpointing, and scoring | `NOT_STARTED` | Phases 05–06 complete | Frozen scores and checkpoint discipline pass | Exact architecture/optimizer/batch values must be sourced |
| 09 — Calibration and threshold methods | `NOT_STARTED` | Phase 08 complete | All feasible methods verified | Grouped-threshold execution and artifact validation remain deferred to Phase 09 |
| 10 — Metrics and inference | `NOT_STARTED` | Phase 09 complete | Metric semantics and paired inference pass | Near-zero mean-FPR warning cutoff and temporal materiality cutoff must be declared |
| 11 — External and temporal evidence | `NOT_STARTED` | Phases 08–10 complete | Edge static/temporal and CIC boundary tests pass | Temporal source validity is data-dependent |
| 12 — Experiment planning and campaigns | `NOT_STARTED` | Phases 01–11 complete | Complete feasible plan expansion | Any unresolved protocol makes dependent experiments infeasible |
| 13 — Artifacts and serialization | `NOT_STARTED` | Phases 02–12 complete | Safe reload and deterministic path suite pass | None expected after protocol resolution |
| 14 — Reporting and claims | `NOT_STARTED` | Phases 10–13 complete | Claim suppression and export validation pass | Traffic-rate evidence may remain absent; output must be suppressed |
| 15 — Extension readiness | `NOT_STARTED` | Phases 01–14 complete | Hook-boundary tests pass | No future method is implemented |
| 16 — Final audit | `NOT_STARTED` | All prior phases complete | Full acceptance report | Any unresolved mandatory scientific value blocks release |

## Required implementation record per phase

When a phase status changes, append one record under that phase containing:

- status;
- exact source files changed;
- exact test files added or changed;
- scientific-source sections consulted;
- unresolved values encountered;
- focused commands executed;
- whole-suite command executed;
- Ruff result;
- Pyright result;
- Pylint result;
- SonarQube CLI result or credential/tool blocker;
- CodeScene delta result or credential/tool blocker;
- audit verdict;
- reason for any blocker.

Do not record dates, durations, commit hashes, or subjective completion percentages.

## Scientific decision register

The source locks the original values where specified. The following user-authorized, prospective research amendments supply the otherwise absent values used by Phase 02: seeds `0..9`; equal non-temporal thirds; N-BaIoT widths `115/86/58/38/29/38/58/86/115`; Adam, learning rate `0.001`, batch size `256`; Ditto grid `0.05/0.1/0.2` with primary `0.1`; paired BCa resamples `10,000`; near-zero mean-FPR warning `0.01`; temporal CV materiality `0.10`; and fixed-score anchor tolerance `1e-12` for the recovered historical shared-scope/local-scope values. These amendments are prospective declarations, not retrospective claims about the original source.

The anchor stores historically observed shared-scope and local-scope CV(FPR) references. Legacy numbered policy and lettered-regime labels are evidence-only terminology from the historical paper; they are not implementation identities. Source modules, enum members, declaration constants, experiment IDs, and ordinary tests use descriptive names. The sole architecture test contains the historical tokens only as negative fixtures that reject their use in source. These anchor records map only to `SHARED_THRESHOLD` and `LOCAL_THRESHOLD`.

Grouped threshold assignment is the source-locked taxonomy-free mechanism: create each eligible client’s benign reconstruction-error fingerprint from mean, standard deviation, skewness, and `p95`; standardize the feature matrix with `StandardScaler`; run k-means++ with `n_init=10`, `max_iter=300`, and `random_state=42`; use the declared group count; and assign each cluster the arithmetic mean of its eligible local thresholds. It never uses attack labels or held-out outcomes. Population-specific traffic-rate evidence remains absent, so `ALERT_BURDEN_TRANSLATION` is a declared suppressed operational experiment rather than an executable rate claim.

## Phase 01 — Scientific identity and scope

- Status: `COMPLETE`.
- Source files changed: `src/datp_core/domain/enums.py`, `src/datp_core/domain/errors.py`, `src/datp_core/__init__.py`. `src/datp_core/domain/__init__.py` was verified as the required minimal non-re-exporting initializer and needed no final change.
- Test files added: `tests/architecture/test_source_tree_is_locked.py`, `tests/architecture/test_no_compatibility_surfaces.py`, `tests/unit/domain/test_enums.py`, `tests/unit/domain/test_errors.py`, `tests/scientific/test_scope_vocabulary.py`.
- Scientific-source sections consulted: programme identity; causal and benign-only calibration contracts; evidence architecture; dataset and temporal boundaries; excluded scope; permitted and prohibited claim language; experiment catalogue; evaluation, checkpoint, and temporal-reporting protocols.
- Unresolved values encountered: none required by Phase 01. Existing global unresolved decisions remain deferred to their dependent phases.
- Focused commands executed: Phase 01 pytest selection; Ruff format check; Ruff check; Pyright; Pylint for the authorized domain surface.
- Whole-suite command executed: `python -m pytest -n auto -q`.
- Results: focused tests passed (`42 passed`); whole suite passed (`42 passed`); Ruff passed; Pyright passed with zero errors; Pylint passed at `10.00/10`.
- Audit verdict: complete. Scientific vocabulary is descriptive; threshold identities are structurally separate; scope vocabulary blocks prohibited claims; source-file identity set is unchanged; no compatibility surface, opaque identity, `Any`, mutable module-level collection, raw domain dictionary, or package re-export was introduced.
- No commit or push was performed.

## Phase 02 — Typed protocols and domain contracts

- Status: `COMPLETE`.
- Source files changed: `src/datp_core/domain/contracts.py`, `src/datp_core/domain/provenance.py`, `src/datp_core/domain/values.py`, and the authorized `src/datp_core/protocols/` declaration and validation modules.
- Test coverage added: focused value, declaration, graph-validation, and property tests; tautological runtime, provenance, contract, and empty-evidence smoke tests were intentionally omitted.
- Source-backed values implemented: temporal split `0.55/0.15/0.10/0.20`; canonical and sensitivity quantiles; calibration support and size grids; shrinkage weights; conformal coverage/significance; summary coefficients; checkpoint rounds; FedProx coefficient grid; local epochs; Dirichlet concentrations; confirmatory confidence level and paired-seed count.
- Seed-cohort boundary: the historical anchor is exactly five seeds, each carrying paired shared-scope and local-scope reference values. The journal confirmatory inference protocol is a separate ten-seed paired cohort. The five-seed cohort is anchor-only and must not reduce or redefine the journal cohort.
- Resolved values: source-backed temporal split `0.55/0.15/0.10/0.20`; canonical and sensitivity quantiles; calibration support and size grids; shrinkage weights; conformal coverage/significance; summary coefficients; checkpoint rounds; FedProx coefficient grid; local epochs; Dirichlet concentrations; confirmatory confidence level and paired-seed count. Research-amendment values are listed in the scientific decision register, including anchors and grouped assignment.
- Validation rules implemented: immutable scalar/provenance validation; strict frozen declaration models; discriminated training declarations; centralized/federated separation; tuple-based deterministic grids and catalogue; capability and cross-reference checks; source-locked benign-only grouped-threshold fingerprint/k-means declaration; explicit operational suppression when rate evidence is absent; project-relative `data`, `outputs`, and dedicated `results` runtime roots.
- Follow-up source audit: re-read the complete master roadmap and corrected the typed catalogue to retain each experiment's required threshold methods and outcome metrics; declared the source-backed client counts for the file-defined and external populations; made controlled synthetic clients ineligible for family thresholding; added temporal-population graph validation; and replaced untyped protocol-interface payloads with generic typed contracts.
- Focused command result: Phase 02 protocol/domain and Phase 01 locked-scope selections passed (`74 passed` and `42 passed` respectively). Whole-suite command result: `make test-parallel` passed (`74 passed`). Ruff format check and lint passed; Pyright passed with zero errors, warnings, and information messages; Pylint passed at `10.00/10` for both `src` and `tests`; Nox discovery passed; `git diff --check` passed.
- Audit verdict: complete. The default graph resolves all mandatory protocols and explicitly reports `ALERT_BURDEN_TRANSLATION` as suppressed rather than fabricating a traffic-rate claim. Static source audit found no removed configuration dependency or unresolved-helper reference. No `Any`, mutable declaration collection, protocol dictionary registry, compatibility alias, environment override, or YAML/config parser was introduced in the Phase 02 declaration surface. The grouped-threshold fingerprint is deliberately typed because it is the source-defined grouping input.
- Configuration cleanup: removed direct `pydantic-settings`, `pyyaml`, `hydra-core`, and `omegaconf` project dependencies, regenerated `uv.lock`, and added Nox/Make parallel-test automation. `pyyaml` remains only transitively through the pre-existing later-phase `dagster` dependency; no Phase 02 source imports or uses it.
- Historical-anchor re-audit: rendered `paper/DATP.pdf` Table VI and the shared-scope/local-scope seed metrics under `outputs/results/a/` agree with every stored anchor CV(FPR) value. The same PDF and prior implementation lock grouped thresholding to benign-error fingerprints, `StandardScaler`, k-means++, `n_init=10`, `max_iter=300`, `random_state=42`, and a declared three-group setting; this replaces the earlier incorrect threshold-space declaration. The decision register records the prospective amendments and explicit no-rate suppression.
- No commit or push was performed.

## Phase 03 — Dataset audit and capabilities

- Status: `IN_PROGRESS`.
- Entry audit: `data/raw` is a reachable read-only symlink to `/home/naslouby/Projects/datp-shared-data/raw`. Its inventory contains the N-BaIoT tree (nine device folders, 90 extracted CSV files, documentation, and `.rar` archives), CICIoT2023 source CSVs plus `MERGED_CSV/Merged01.csv` through `Merged63.csv`, and Edge-IIoTset normal sensor-group CSVs, attack CSVs, PCAP files, documentation, and selected ML/DL CSVs. The bounded header audit confirms N-BaIoT's 115-feature header, CICIoT2023's 39-feature-plus-`Label` merged schema, and Edge-IIoTset's 63-column CSV schema. Edge `Modbus/Modbus.csv` begins with an address literal in `frame.time`; `Distance/Distance.csv` has a genuine capture-time string.
- Source and test surface audit: all 16 Phase 03-permitted implementation files already exist under `src/datp_core/datasets/` but are zero bytes; no prescribed dataset test file exists. Package initializers are empty. No dataset source or test file was changed.
- Phase 01 correction: added `CapabilityStatus` with the required supported, unsupported, conditional, unavailable, and not-applicable identities. Dataset capability contracts now use that enum. The reconstruction-error fingerprint mechanism remains permitted and source-defined.
- Allowlist correction: user authorization permits focused additions beneath `tests/` and explicit approved additions under `src/`. Added `datasets/materialization.py` as the shared typed home for checksums, manifests, and atomic publication; the source-tree architecture test and roadmap now record it.
- Commands executed: authority-file review; raw-tree and bounded CSV header audit; source-tree, tracked-file, and Phase 01/02 type audit. Focused tests, whole-suite tests, Ruff, Pyright, and Pylint were not run because the required Phase 03 capability types cannot be introduced without violating the locked source-file rule.
- Independent implementation: added frozen, slotted shared dataset records; evidence-backed immutable capability structures; and an audited exact N-BaIoT 115-feature schema with explicit device and attack-path identity validation. The N-BaIoT feature tuple was checked against the raw header. Implemented its lazy reader and capability declaration. CICIoT2023 now has the exact 39-feature-plus-label merged schema, an audited 34-label allowlist with explicit null-label rejection, a lazy source-provenance reader, and its file-boundary capability declaration. Edge-IIoTset now has the audited 63-column source schema, separate benign/attack readers, explicit preservation of raw timestamps and unassigned attack rows, and group-level chronology validation. The minimal chronology audit parses capture-time text and rejects the Modbus address literal. Focused Ruff formatting and lint passed for all currently changed dataset files.
- Canonical publication: implemented deterministic source checksums, manifest-based reuse rejection, temporary-directory publication, and Parquet assets for N-BaIoT and CICIoT2023; Edge publication creates separate static-benign, temporal-benign, unassigned-attack, and chronology assets. A miniature N-BaIoT materialization test verifies Parquet and manifest creation plus validated reuse.
- Focused test result: `python -m pytest tests/unit/datasets tests/integration/datasets --import-mode=importlib -q` passed (`20 passed`). Ruff format and lint and Pyright passed for the dataset source and prescribed test surface.
- Test-configuration blocker: the required nested test modules reuse the mandated basenames `test_schema.py`, `test_reader.py`, and `test_capabilities.py`. Under the repository's default pytest prepend import mode, collection fails with import-file-mismatch errors. Importlib mode passes. Adding test package initializers or modifying pytest configuration is outside the Phase 03 allowlists.
- Full-suite audit: `python -m pytest -n auto -q` reached `83 passed` then failed collection for six mandated duplicate-basename dataset modules under prepend mode. `pylint src/datp_core/datasets` rated the current implementation `9.84/10` and reported a long literal, one-method materializer classes, one Edge local-variable count, and duplication across the separately mandated materializer files. The source allowlist prohibits a shared helper module and broad suppression; these static findings remain unresolved.
- Alternative full-suite evidence: `python -m pytest -n auto --import-mode=importlib -q` passed (`90 passed`). This demonstrates runtime compatibility of the complete suite but does not satisfy the required default invocation while the collection configuration conflict remains.
- User-authorized correction: fingerprint declarations remain permitted. Added Phase 01 `CapabilityStatus`; migrated all dataset capability contracts to it; added the approved `datasets/materialization.py`; and added focused test package markers. Default `python -m pytest -n auto -q` now passes (`91 passed`). Ruff format/check and Pyright pass. Pylint improved to `9.97/10` but still reports the N-BaIoT exact-header literal and Edge materializer local-variable count.
- Completion audit: replaced the N-BaIoT literal with deterministic audited feature-group construction and verified it matches the raw 115-column header exactly; refactored Edge canonical assets into a typed record. Final commands passed: `ruff format --check src tests`; `ruff check src tests`; `pyright`; `pylint src/datp_core/datasets src/datp_core/domain/enums.py` (`10.00/10`); and `python -m pytest -n auto -q` (`91 passed`). Source and test scans found no `Any`, pandas ingestion, YAML, registry, factory, service-locator, or Phase 03 fingerprint additions. Repository changes are limited to the authorized dataset/domain corrections, dataset tests, source-tree test, roadmap updates, and the approved shared materialization module. No generated data, raw data, cache, commit, or push was added.
- Audit verdict: complete. N-BaIoT remains the confirmatory physical-device anchor; CICIoT2023 remains a file-defined applicability boundary; Edge attacks remain unassigned and Edge chronology is group-conditional. All Phase 03 gates now pass.
- Audit verdict: independent schema work is in progress. The recorded implementation-dependency and authority-conflict blockers remain for final acceptance. No commit or push was performed.
- Re-audit correction: `AUDIT_FAILED`. The prior completion claim is superseded. The three materializers collect whole lazy datasets and convert them to Arrow tables before writing Parquet. This is not safe for the audited raw corpus (N-BaIoT `9.1G`, CICIoT2023 CSV `17G`, Edge normal `4.8G`, Edge attack `4.6G`) and contradicts the Phase 03 lazy/materialization requirement. The canonical schema declarations omit provenance columns even though readers write them; declared PyArrow schemas do not describe the emitted Parquet columns; CICIoT2023 relies on implicit CSV inference; and the N-BaIoT Pandera schema is declared but never executed. Required raw-inventory, row-identity, exclusion, and validation-report records are definitions only, not produced or persisted.
- Reuse and handoff audit: canonical output was previously an incompatible flat `dataset=<ID>/canonical.parquet` plus `manifest.txt`, rather than the required dataset-rooted `data/`, `dataset_manifest.json`, `schema.json`, and `COMPLETE` contract. There was no final-coordinate `filelock`, completed-marker validation, asset/schema/checksum revalidation, or safe rebuild. Manifests and canonical rows serialized absolute local source paths. No canonical root existed in the current worktree; the miniature tests only materialized temporary directories.
- Scientific and chronology audit: the scientific source requires ten static Edge sensor groups and exactly nine verified temporal groups, excluding Modbus only from the temporal population. The current timestamp parser drops date information and retains individual parseable rows from a group rather than explicitly building the nine verified groups and a structured exclusion report. The test/runtime implementation is also defective: `chronology.py` calls `Expr.is_sorted`, which does not exist in the installed Polars version.
- Commands executed: `python -m pytest -n auto -q` resulted in `87 passed, 2 failed, 1 error`; focused dataset execution resulted in `18 passed, 2 failed`. Both failures arise from the unavailable `Expr.is_sorted` call. The full suite also cannot collect the property tests because `hypothesis` is absent. `ruff format --check src tests`, `ruff check src tests`, `pyright`, and `pylint src/datp_core/datasets src/datp_core/domain/enums.py` pass, the latter at `10.00/10`; these static results do not discharge the failed runtime, scale, data-contract, atomicity, or Phase 04-entry requirements. No commit or push was performed.
- Reimplementation and handoff audit: `BLOCKED_SCIENTIFIC_VALUE`. The unsafe whole-frame Arrow conversion was replaced with Polars `sink_parquet` streaming writes. Canonical assets now publish only after final-coordinate locking, exact Arrow-schema verification, source-inventory checksums, typed Pydantic schema/manifest documents, asset checksum and row-count validation, and a `COMPLETE` digest. The canonical layout is `<DATASET_ID>/data/` with `schema.json` and `dataset_manifest.json`; malformed, incomplete, or checksum-mismatched targets are removed only within the canonical root and rebuilt. All source provenance is raw-root-relative. The implementation reuses the existing `DATA_ROOT` path constant and contains no raw dictionary records.
- Superseded full-corpus evidence: the earlier N-BaIoT and Edge schema coordinates preceded physical-schema cache invalidation and are retained only as historical audit evidence. The current coordinates and direct manifest verification are recorded below.
- Raw-fidelity findings, superseding the earlier CSV-only chronology verdict: CICIoT2023 audit read all 63 accepted merged files and found 45,019,243 rows, 9 null labels, 991 infinite `Rate` values, and no empty `Rate` values. Its lossless canonical artifact preserves every row with a typed unavailable eligibility report; no row is dropped, filled, capped, clipped, or coerced. N-BaIoT Table III supplies a complete outcome-independent device-type taxonomy for the nine audited devices. Edge validates every non-Modbus CSV as an ordered subsequence of its paired PCAP, then derives chronology only from the matched PCAP epoch timestamp. The verified display offset, source checksums, matched-row count, and skipped/trailing PCAP record counts persist in the chronology record. Modbus remains static-only because `frame.time` contains address literals.
- Scientific readiness correction: N-BaIoT family thresholding is supported by the source-defined device-type taxonomy. Edge revalidates each CSV-to-PCAP ordered subsequence and exposes a temporal asset only when the matched PCAP timestamps are monotonic in verified source order; a non-monotonic match remains static-only rather than being synthetically reordered. CICIoT2023 has an approved, outcome-blind eligibility policy: exclude a prospective model-input row if and only if its normalized label is missing or unrecognized, or any declared feature is non-finite. The lossless canonical corpus retains every row; the lazy eligibility audit preserves stable ID, source file, source row, and both exclusion signals. The same gate is mandatory before file-client construction, splitting, fitting, calibration, and evaluation, with no imputation, zero-fill, clipping, capping, infinity replacement, or label inference.
- Full-corpus canonical verification: N-BaIoT rebuilt and reused 7,062,606 rows, Edge-IIoTset rebuilt and reused 31,990,033 published rows across static, temporal, and unassigned branches, and CICIoT2023 published then reused all 45,019,243 raw-fidelity rows. CIC’s persisted eligibility report is `unavailable`; that status is an intentional scientific boundary, not a publication failure.
- Verification after correction: `python -m pytest tests/unit/datasets tests/integration/datasets --import-mode=importlib -q` passed (`32 passed`). Final checks passed: `ruff format --check src tests`; `ruff check src tests`; `pyright` with zero errors, warnings, and information messages; `pylint src/datp_core/datasets src/datp_core/domain/enums.py` at `10.00/10`; `python -m pytest -n auto -q` (`103 passed`); and `git diff --check`. The final source scan found no `Any`, raw dictionary construction, phase labels, or fingerprint terminology in the dataset source and test surface. No raw data was written, no generated canonical data was added to version control, and no commit or push was performed.
- Canonical-contract re-audit: the logical schema checksum previously omitted the exact PyArrow physical schema, allowing a physical schema implementation change to retain an old reuse coordinate. The canonical schema now persists the exact physical schema, includes it in the checksum, and validates every reused Parquet asset against it. Publication also removes only stale, uniquely-prefixed temporary siblings while holding the final-coordinate lock; a focused test covers interrupted-publication cleanup. The simplified design stores the physical schema once per dataset rather than repeating it in each asset record.
- Full-corpus revalidation after the contract change: N-BaIoT rebuilt and then reused 7,062,606 rows across 89 assets; Edge-IIoTset rebuilt and then reused 20,939,622 rows across 25 assets. Direct manifest audit confirmed the stored physical schema, manifest schema checksum, all Parquet asset schemas, all asset paths, and every `COMPLETE` marker agree. The cache now exposes the stable, human-readable dataset roots `data/canonical/nbaiot/` and `data/canonical/edge_iiotset/`; the schema checksum remains in `schema.json` and is validated before reuse.
- Downstream readiness audit: the canonical directory layout, completion semantics, source inventory, ordered schema, physical schema, and atomic reuse behavior satisfy the reusable-data input contract. The nine downstream preprocessing and data-artifact implementation modules remain empty, which is correct for an unstarted later stage. The scientific source requires a fixed common preprocessing state but does not specify a transformer, normalization procedure, missing-value policy, fitted-state class, or serialization tolerance. That missing scientific decision blocks preprocessing implementation; it does not compromise the canonical N-BaIoT or static Edge handoff.
- External quality-tool audit: the installed SonarQube CLI is `~/.local/share/sonarqube-cli/bin/sonar` and the installed CodeScene CLI is `/usr/local/bin/cs`. The project-local ignored `.env` supplies both tools only through their child-process environments. Sonar's authenticated deep analysis against `origin/main` and `naslouby-salahe_datp-core` reported zero secret issues; its server-side Vortex agentic analysis is unavailable for this organization (`403`), so that capability is recorded as a service limitation rather than an authentication failure. CodeScene `cs delta --output-format json --pretty` runs against the local uncommitted repository and currently identifies introduced complexity in the shared materialization code, dataset records, Edge/CIC materializers, N-BaIoT path parsing, and the CIC reader. The cache validator has been reduced below its CodeScene threshold; the remaining findings are active refactoring work, not waived. The exact safe loader, organization/project parameters, non-disclosure rule, and service-limitation record are mandatory through the roadmap index and `CLAUDE.md`. Equivalent local gates passed after this correction: Ruff formatting/check, Pyright with zero diagnostics, Pylint at `10.00/10`, focused dataset tests (`35 passed`), and `git diff --check`.
- Canonical-root simplification and reuse verification: canonical caches are ignored by Git and now live directly below `data/canonical/<dataset-id>/`; the schema checksum remains persisted in `schema.json` and is validated together with the source inventory, validation report, chronology record, manifest, completion digest, and every Parquet asset. The verified N-BaIoT and Edge caches were relocated without regeneration. Full-corpus materialization after the move returned `REUSED` for N-BaIoT (7,062,606 rows) and Edge-IIoTset (20,939,622 rows), proving the simple paths retain safe reuse rather than forcing rewrites.
- Reuse-semantic audit: reuse now compares the persisted validation report and chronology records with the current publication contract, in addition to the raw inventory, logical/physical schema, asset checksums, rows, columns, and completion digest. A regression test verifies that a digest-consistent but stale validation report is rebuilt rather than reused.
- CICIoT2023 quality-policy implementation: the approved outcome-blind rule is now an immutable typed policy and a lazy audit view. It marks missing-or-unrecognized labels and non-finite declared features independently, retains all canonical/provenance columns, and produces the eligible model-input view only by filtering those two explicit signals. A focused three-row test proves that a finite recognized row remains eligible, while an infinite feature and an unrecognized label are retained in the audit and excluded only from model input. The lossless canonical schema, cached corpus, and raw-fidelity report remain unchanged.
- Final verification after the policy decision: `ruff format --check src tests`, `ruff check src tests`, `pyright`, `pylint src/datp_core/datasets src/datp_core/domain/enums.py`, `python -m pytest -n auto -q`, and `git diff --check` passed; the full suite result is `111 passed`. Sonar deep analysis reported zero secret issues; its Vortex agentic service remains unavailable to the organization (`403`), which is a service limitation. Local `cs delta --output-format json --pretty` reports one residual finding: the single exact equality in `_clock_matches`, the PCAP clock-alignment predicate. The predicate has no control-flow branch or alternative path; retaining the direct arithmetic equality preserves auditability and avoids an artificial metric-only rewrite.
- Audit verdict: the three data-science acceptance blockers are resolved. N-BaIoT family taxonomy is source-backed, Edge chronology is PCAP-backed for nine temporal groups, and CICIoT2023 model-input quality handling is now explicit, deterministic, outcome-blind, and lossless at the canonical boundary. The next reusable-preprocessing scope remains procedurally blocked by the separate roadmap requirement for a scientifically specified common preprocessing transformer, normalization/missing-value semantics, fitted-state type, and serialization tolerance; none is inferred here. No commit or push was performed.
- Reuse-performance correction: canonical publication still creates the full content-checksummed raw inventory and validates every written Parquet asset. Completed caches then bind their manifest to a typed source-state record of raw-root-relative paths, byte sizes, and nanosecond modification times. An unchanged local cache reloads after schema, manifest, completion digest, and Parquet-metadata checks without rescanning the raw corpus; any source-state change returns to the full checksum-backed audit. The full Edge corpus reused 20,939,622 logical rows across 25 typed assets in 0.32 seconds. The cache and publication responsibilities are separated into focused modules, preserving immutable records and avoiding raw dictionaries.
