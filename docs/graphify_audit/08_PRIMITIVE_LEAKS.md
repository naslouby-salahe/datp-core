# Primitive-leak audit

No broad production primitive leak was confirmed. Scientific identities, seeds, counts, q, threshold/score/metric values, methods, populations, availability states, and protocol values are typed throughout the live graph.

One meaningful pending issue is **PL-01 / LOW `FIX_PRIMITIVE_LEAK`**: `TrafficRateEvidence.source_locator` is a raw `str`, and evidence kind is not bound at the protocol boundary. This matters only if the currently suppressed operational alert-burden programme is enabled. Use a structured/canonical source locator plus measured/dataset-derived/external-citation kind, unit, rate, and population binding. Do not create cosmetic wrappers elsewhere.

