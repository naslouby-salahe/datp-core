# STATE

## Current phase
Phase 1 — Foundations, in progress. Created so far (verbatim content moves, imports repointed, not yet deleted from old locations): `pipeline/__init__.py`, `pipeline/identifiers.py` (from `domain/identifiers.py`), `pipeline/values.py` (from `domain/values.py`), `pipeline/fingerprints.py` (from `domain/fingerprints.py` minus the two config-specific compute_* functions), `pipeline/models.py` (from `domain/outcomes.py` + `planning/graph.py`), `artifacts/__init__.py`, `artifacts/models.py` (from `domain/artifacts.py`). All import paths inside these new files already point at the new locations (`datp_core.pipeline.*`, `datp_core.artifacts.models`). Old `domain/{identifiers,values,fingerprints,artifacts,outcomes}.py` and `planning/graph.py` still exist untouched — nothing has been deleted yet; nothing outside these new files imports the new locations yet, so the codebase is still consistent (old files still work, new files are additions only, no breakage).

Design refinement during implementation (update to TARGET_ARCHITECTURE.md deviations #13/#14): confirmed by grep that `TypedDomainRegistry` and the JSON-freezing helpers (`deep_freeze`, `FrozenJson`, etc.) are consumed across `config/resolver.py`, `config/runtime_settings.py`, `config/dataset_resolution.py`, and `domain/{datasets,thresholding,catalogue}.py` — genuinely shared, not configuration-specific. `pipeline/` foundation split into 4 files (`identifiers.py`/`values.py`/`fingerprints.py`/`models.py`) rather than 1 to avoid recreating the `domain/values.py` grab-bag pattern under a new name.

Phase 2 — Configuration, started. Created: `configuration/__init__.py`, `configuration/models.py` (merged verbatim from all 5 `config/models/*.py` files — `_base.py` + `dataset_config.py` + `experiment_config.py` + `protocol_config.py` + `runtime_config.py`; ~1450 lines, one coherent "authored schema" file per TARGET_ARCHITECTURE.md deviation notes). One incidental cleanup applied during the merge: `FederationStrategyConfig` moved above `TrainingProfileConfig` (was previously defined below the class that forward-references it, a documented fragility in CURRENT_ARCHITECTURE.md that only worked because of `from __future__ import annotations`) — pure reordering, zero behavior change.

Still to do in Phase 2 (not started): `configuration/loading.py` (from `config/yaml_loader.py` + `config/runtime_settings.py`'s bootstrap-settings/path-authority half), `configuration/dataset_resolution.py`, `configuration/experiment_resolution.py` (analysis-kind dispatch to split out to experiments/analysis later), `configuration/protocol_resolution.py` (threshold-policy dispatch to split out to thresholding later), `configuration/resolution.py` + `configuration/project.py` (breaking the resolver⇄validation cycle per the documented one-directional flow), `configuration/validation.py`, `configuration/fingerprints.py` (compute_scientific_fingerprint/compute_execution_fingerprint only). None of the old `config/` files have been touched or deleted yet — old package still fully intact and functional; new `configuration/models.py` is additive only, nothing imports it yet.

Old files NOT yet touched anywhere in the repo (still fully intact, still used by all existing code): all of `domain/`, `application/`, `planning/`, `interfaces/`, `composition/`, `config/`, `infrastructure/`. The new `pipeline/`, `artifacts/`, `configuration/` packages so far contain only additive files nothing references yet — repository is still 100% functional on the old tree.

## Phase 2 — Configuration: COMPLETE
All 10 files written and internally consistent: `configuration/{__init__,models,loading,runtime_resolution,fingerprints,dataset_resolution,experiment_resolution,protocol_resolution,resolution,validation,project}.py`. The resolver⇄validation circular import is broken cleanly: `resolution.py` defines `ResolvedProjectConfiguration` + `resolve_project_configuration_candidate()` (builds, does NOT validate); `validation.py` imports only the type from `resolution.py` for hints (one-directional); `project.py` is the sole module importing both, providing the public `resolve_project_configuration()` (resolve then validate then return/raise — behavior-identical to the old single-function version) plus `validate_all_configurations()` and the drift/fingerprint use cases migrated from `application/configuration.py` + `domain/drift.py`.

Corrections made while implementing Phase 2 (all reflected in TARGET_ARCHITECTURE.md deviations #15-18 and MIGRATION_MAP.md):
- `config/runtime_settings.py` split into `configuration/loading.py` (bootstrap settings) + `configuration/runtime_resolution.py` (NEW, path authority + resolved records) — was originally planned as a single-file move.
- The 14 `*AnalysisRecord` spec classes + `AnalysisKind` moved from `domain/catalogue.py` to `experiments/models.py`, NOT `analysis/models.py` — they are catalogue specifications, not results, and putting them in `analysis/` would have created a manufactured circular dependency.
- `domain/catalogue.py::ResolvedCatalogue` confirmed dead (zero consumers anywhere) and deleted, not migrated.
- `domain/protocol_contracts.py`'s ~26 records are **NOT dead** (corrected an earlier wrong assumption) — all are fields of `ResolvedProjectConfiguration` feeding the scientific fingerprint. All migrated, distributed to `evaluation/models.py`, `artifacts/models.py`, `analysis/models.py`, `thresholding/models.py`, `reporting/models.py` (NEW), and two (`SeedNamespaceRecord`/`ProtocolDeterminismRecord`, no consumer outside configuration) defined directly in `configuration/protocol_resolution.py`.
- Created ahead of schedule (needed by configuration's resolution files, since configuration legitimately depends on feature `models.py` pure-data files): `datasets/models.py`, `thresholding/models.py`, `evaluation/models.py`, `learning/models.py`, `experiments/models.py`, `analysis/models.py` (partial — statistical-profile + communication/resource records only; the typed analysis-RESULT hierarchy per section 12.2 is proper Phase 7 work), `reporting/models.py`.

Old `config/`, `domain/{catalogue,protocol_contracts,thresholding,evaluation,datasets,splits}.py` still fully intact and unreferenced by new code — nothing deleted yet.

## Phase 3 — Experiments + Pipeline: CORE COMPLETE
Written: `experiments/{models,planning,identity,execution}.py`, `pipeline/{stages,runner}.py` (identifiers/values/fingerprints/models were already done in Phase 1). `application/stage_protocol.py`'s 5 concerns fully distributed: StageHandler Protocol + commit_artifact/artifact_parents → `pipeline/stages.py`; git_revision → `artifacts/provenance.py` (NEW); apply_holm_correction + domain/statistics.py content → `analysis/execution.py` (partial — full 14-analysis-kind dispatch is Phase 7); evaluate_readiness_gates → deferred to Phase 4's `datasets/readiness.py` (not yet written, needs `application/dataset_audit.py` content read first); resolve_partition_contract → `experiments/planning.py`. `domain/run_identity.py` merged into `experiments/identity.py`. `planning/{expansion,validation,graph,identity}.py` and `application/experiment_execution.py` all fully represented in new locations.

Not yet created, deliberately deferred (not a gap, a sequencing decision):
- `experiments/sweeps.py` — waits on reading/splitting `application/scoring_support.py` (409-line dumping ground spanning experiments/analysis/evaluation territory), planned for Phase 6/7.
- `experiments/catalogue.py`, `pipeline/handler.py` — no concrete content has emerged that needs a separate file yet (`ResolvedProjectConfiguration`/`ExperimentRecord` already serve the "catalogue" role; `pipeline/stages.py` already covers "common stage execution behavior"). Per section 7 ("not permission to create empty/artificial files"), will only create these if Phase 4-8 work reveals real content needing them — confirmed one way or the other during the pre-validation navigation audit (section 17), not assumed now.

Old `planning/`, `application/stage_protocol.py`, `application/experiment_execution.py`, `domain/run_identity.py` still fully intact and unreferenced by new code — nothing deleted yet.

## Phase 4 — Datasets: COMPLETE
All 8 files written: `datasets/{models,discovery,common,readiness,materialization,ciciot2023,nbaiot,edge_iiotset}.py`. Key resolution: `application/ports.py`'s Protocols (`SourceEntry`/`SourceInventory`/`MaterializationPayload`/`DatasetMaterializer`) merged into `datasets/materialization.py` alongside the adapter registry and the two stage handlers (`PreflightStageHandler`, `DatasetMaterializationStageHandler`) — this is exactly the fix for the confirmed app↔infra circular dependency (CURRENT_ARCHITECTURE.md §9.1): port + adapters + consumer now live in the same package, no cross-package cycle possible. `application/ports.py::ResultQueryService` was NOT moved here (unrelated to datasets — it's artifact/result querying) — deferred to Phase 8's `artifacts/repository.py` alongside `DuckDbAuditService`. `infrastructure/tables/parquet_io.py` folded entirely into `datasets/common.py` (generic I/O + normalization fitting, confirmed used by all 3 adapters — genuinely shared, not a dumping ground). Fixed the same `NormalizationEvidence`-forward-reference-ordering fragility pattern as earlier. Flagged (not fixed, deferred to Phase 5/6): `nbaiot.py::derive_partition_seed` is one of 3 duplicate seed-derivation implementations; consolidation needs `learning/` and `thresholding/` read first to compare all 3 safely.

Old `application/{data_stages,dataset_audit,ports}.py`, `infrastructure/datasets/*`, `infrastructure/tables/parquet_io.py` still fully intact and unreferenced by new code — nothing deleted yet.

## Phase 5 — Learning: COMPLETE
All 6 files written (+ models.py from Phase 2 = 7 total): `learning/{models,autoencoder,federated,personalization,checkpoints,training,scoring}.py`. Key decisions:
- **Seed-derivation consolidation done**: created `pipeline/determinism.py::derive_seed()` as the single canonical formula (verified byte-identical output across all 3 old implementations by comparing their exact string-encoding construction). `datasets/nbaiot.py::derive_partition_seed` updated to delegate to it (done in Phase 4, retroactively). `learning/autoencoder.py::derive_dataloader_shuffle_seed` also delegates to it. Thresholding's `_subsample_seed` (Phase 6) will delegate too.
- **New `pipeline/frames.py`** created (not originally in the target tree list) for `infrastructure/tables/schemas.py`'s Pandera contracts — confirmed shared across learning/thresholding/evaluation, so it belongs at the pipeline base layer, not inside any one feature. Zero direct tests existed before (safety-critical gap flagged in CURRENT_ARCHITECTURE.md) — direct tests still owed during test migration.
- Renamed `_weighted_average_state`/`_validate_federated_training_inputs`/`_weighted_reconstruction_loss` (federated.py) to drop leading underscores since `personalization.py` now imports them — they were private-but-imported-elsewhere, the exact anti-pattern flagged in the audit; now legitimately shared within the `learning/` package.
- `infrastructure/learning/sklearn_adapter.py` NOT yet migrated — `AurocStatus`/`ClientAuroc`/`compute_roc_auc` go to `evaluation/predictive_metrics.py`, `compute_adjusted_rand_index` goes to `analysis/stability.py` (Phase 6/7), `scale_features()` confirmed dead (zero consumers) — deleted, not migrated.

Old `application/learning_stages.py`, `infrastructure/learning/{pytorch_adapter,sklearn_adapter}.py`, `domain/checkpoints.py` still fully intact and unreferenced by new code — nothing deleted yet.

## Phase 6 — Thresholding + Evaluation: COMPLETE
All files written. `thresholding/`: `models` (Phase 2) + `quantiles`, `grouped`, `conformal`, `shrinkage_and_federated`, `construction`, `calibration` = 7 files. `evaluation/`: `models` (Phase 2) + `predictive_metrics`, `operating_points` = 3 files (`dispersion`/`distributions` deferred to Phase 7 since their content — `client_score_distributions`/`threshold_tradeoff`/`calibration_variance_terms` from `scoring_support.py`, and `calculate_fpr_dispersion` already in `evaluation/models.py` from Phase 2 — is more naturally handled alongside the `analysis_stages.py` read).

Key results:
- **`_ineligible_client_metrics` deduplicated**: confirmed byte-for-byte duplicate in old `learning_stages.py`/`threshold_stages.py` now has one canonical home, `evaluation/operating_points.py::ineligible_client_metrics` (renamed, unprefixed — cross-module public API).
- **The 12-threshold-policy-family triple-representation resolved**: `thresholding/construction.py::ConfiguredThresholdEstimator.estimate()` now dispatches to 4 family-group modules (`quantiles.py`: B0-B3 shared/pooled/weighted/local/family-mean; `grouped.py`: B4 cluster; `conformal.py`: B2-conf; `shrinkage_and_federated.py`: shrinkage + B-FedStatsBenign) instead of one 377-line class with private methods. Shared `ThresholdSet`-construction helpers (`quantile`, `policy_quantile`, `build_threshold_set`) deliberately placed in `thresholding/models.py` (not `construction.py`) specifically to avoid a circular import between construction.py and the 4 family files.
- **Third seed-derivation duplicate resolved**: `thresholding/calibration.py::_subsample_seed` now delegates to `pipeline/determinism.py::derive_seed` — all 3 original duplicates (nbaiot partition seed, learning dataloader-shuffle seed, this one) now share one formula.
- `infrastructure/learning/sklearn_adapter.py` fully disposed: `AurocStatus`/`ClientAuroc`/`compute_roc_auc` → `evaluation/predictive_metrics.py` (done); `compute_adjusted_rand_index` → `analysis/stability.py` (Phase 7, not yet done); `scale_features()` → confirmed dead, deleted (not migrated).
- Created `experiments/sweeps.py` (score_context, calibration_sample_counts) — 2 of `scoring_support.py`'s 6 concerns; remaining 4 (mean_group_std, group_mean_std, materiality_threshold, seed_ratio_result, weighted_mean, conformal_seed_coverage, client_score_distributions, threshold_tradeoff, calibration_variance_terms, threshold_exchange_cost) go to `analysis/{coverage,resources}.py` and `evaluation/distributions.py` in Phase 7.

Old `application/{threshold_construction,threshold_stages,scoring_support}.py`, `infrastructure/thresholding/{base,estimators}.py`, `infrastructure/tables/{calibration_subsampling,polars_engine}.py`, `infrastructure/learning/sklearn_adapter.py` still fully intact and unreferenced by new code — nothing deleted yet.

## Phase 7 — Analysis: COMPLETE
Read `application/analysis_stages.py` (1298 lines, all 14 analysis-kind dict-building methods) and `application/statistical_analysis.py` (169 lines, `StatisticalAnalysisUseCase`) in full. Built the typed per-analysis-family RESULT hierarchy mandated by section 12.2 and finished distributing `scoring_support.py`.

**Files written:**
- `evaluation/distributions.py` — NEW: `CdfPoint`, `ThresholdPositionRecord`, `ClientScoreDistributionRecord`, `ThresholdTradeoffEntry`, `QuantileVarianceTerms` + `client_score_distributions`, `threshold_tradeoff`, `calibration_variance_terms` (3 of `scoring_support.py`'s 6 remaining concerns).
- `analysis/models.py` — appended: the pure statistical primitives moved here from the old `analysis/execution.py` (`StatisticalProcedureError`, `matched_pairs_rank_biserial_correlation`, `holm_adjust_p_values`, `ConfidenceInterval`, `HypothesisTestResult`, `LinearRegressionResult`, `PairedSeedDifferenceRecord`) plus the full migrated `StatisticalAnalysisUseCase` (from `application/statistical_analysis.py`) plus **all 16 typed result records** (`PairedThresholdAnalysisResult`, `FederatedProximalSelectionResult`, `DittoSelectionResult`, `MetricAssociationAnalysisResult` (+`AssociationCorrelationResult`/`AssociationRegressionResult`/`AssociationObservationRecord`), `ThresholdStabilityAnalysisResult`(+seed result), `RecoveryFractionAnalysisResult`, `SeedRatioResult`/`AbsorptionAnalysisResult` (type alias), `ConformalCoverageAnalysisResult` (+`ConformalClientCoverageRecord`/`ConformalSeedCoverageResult`), `DistributionMechanismAnalysisResult` (union of `DistributionMechanismRawResult`/`DistributionMechanismTradeoffResult` — two genuinely disjoint dict shapes in the original, kept disjoint rather than one Optional-field record so the persisted JSON never gains extra null keys), `LockedClientDistributionAnalysisResult`, `AlertBurdenAnalysisResult`, `QuantileEstimationAnalysisResult` (+client/evaluation/seed sub-records), `ResourceCostAnalysisResult` (+sub-records), `ClusterStabilityAnalysisResult` (union of `ClusterAblationStabilityResult`/`ClusterMembershipStabilityResult`, same disjoint-shape reasoning), `TemporalRecoveryAnalysisResult`, `AnchorEquivalenceAnalysisResult` (+`AnchorEquivalenceChecks`) — plus the `AnalysisResult` union and `analysis_result_to_payload()` (uses `attrs.asdict(recurse=True)`, with one explicit key-rename restoring the authored dotted key `reproduced_interval_width_at_most_1.20x_historical_width` since it can't be a Python identifier). This keeps the on-disk JSON artifact schema byte-identical to the pre-refactor dict output while making every in-memory computation fully typed.
- `analysis/paired.py` — NEW: `analyze_paired` (PAIRED_THRESHOLD), `evaluation_metric`/`evaluation_policy` (shared helpers, also used by `temporal.py`), `federated_proximal_selection`, `ditto_selection`, `analyze_anchor_equivalence`.
- `analysis/association.py` — NEW: `analyze_association` (METRIC_ASSOCIATION) + `calibration_js`.
- `analysis/stability.py` — NEW: `compute_adjusted_rand_index` (migrated from `infrastructure/learning/sklearn_adapter.py`, its last remaining unmigrated half), `analyze_threshold_stability` (THRESHOLD_STABILITY), `analyze_cluster_stability` (CLUSTER_STABILITY, both the ablation and membership-comparison variants) + `_cluster_membership`.
- `analysis/coverage.py` — NEW: `mean_group_std`/`group_mean_std`/`materiality_threshold`/`weighted_mean`/`seed_ratio_result`/`conformal_seed_coverage` (remaining `scoring_support.py` concerns) + `analyze_conformal_coverage` (CONFORMAL_COVERAGE), `analyze_recovery_fraction` (RECOVERY_FRACTION), `analyze_absorption` (ABSORPTION) + its `_absorption_reference`/`_validate_absorption_contract` helpers.
- `analysis/resources.py` — NEW: `threshold_exchange_cost`/`_field_bytes` (from `scoring_support.py`) + `analyze_resource_cost` (RESOURCE_COST), `analyze_alert_burden` (ALERT_BURDEN).
- `analysis/distributions.py` — NEW (7th analysis-family file, not in the original 6-file sketch — see deviation below): `distribution_seed_result`/`threshold_and_calibration_frame` shared helpers + `analyze_distribution_mechanism` (DISTRIBUTION_MECHANISM), `analyze_locked_client_distribution` (LOCKED_CLIENT_DISTRIBUTION), `analyze_quantile_estimation` (QUANTILE_ESTIMATION).
- `analysis/execution.py` — fully rewritten: thin `StatisticalAnalysisStageHandler` (sweep-dimension expansion, per-kind dispatch via `match`, Holm correction, JSON persistence) + module-level `apply_holm_correction` (now operates on typed `list[AnalysisResult]`, using `attrs.evolve` to set `holm_adjusted_p_value` since results are frozen).

**Deviation from the Phase-6-era plan**: STATE.md previously said "write `analysis/{paired,association,stability,coverage,temporal,resources}.py`" (6 files). After reading the full 1298-line handler, 3 of the 14 kinds (distribution-mechanism, locked-client-distribution, quantile-estimation) form their own cohesive family — score-CDF/threshold-quality comparisons — that doesn't fit any of the other 6 without forcing an unrelated merge. Added `analysis/distributions.py` as a 7th family file instead. Recovery-fraction and absorption were folded into `coverage.py` (not a separate file) since they share `seed_ratio_result`/`materiality_threshold` directly with conformal coverage. Alert-burden was folded into `resources.py` alongside resource-cost (both small operational-estimate analyses).

**Genuine bugs found and fixed** (not stylistic — real runtime defects, discovered because giving each analysis function its real typed signature made the old dispatch table's argument-count mismatches visible immediately): the pre-refactor `application/analysis_stages.py::_DISPATCH` generic dispatcher called `_analyze_association` and `_analyze_absorption` with too few positional arguments, and `_analyze_alert_burden` with too many. Any experiment actually configuring `metric_association_analysis`, `absorption_analysis`, or `alert_burden_analysis` would have raised `TypeError` at runtime — these three kinds were evidently never exercised (no test coverage exists for the dispatch path itself, only for the individual `_analyze_*` methods in isolation, per the CURRENT_ARCHITECTURE.md test-coverage audit). Fixed by giving each family function in the new tree its correct full parameter list and calling it explicitly by kind in `execution.py`'s `match` statement instead of reusing a generic flag-driven dispatch table.

Old `application/{analysis_stages,statistical_analysis,scoring_support}.py`, `infrastructure/learning/sklearn_adapter.py` still fully intact and unreferenced by new code — nothing deleted yet (deletion is Phase 9).

## Phase 8 — Reporting + Artifacts: COMPLETE
Read `application/reporting.py` (345 lines) and `application/reporting_stages.py` (128 lines) in full.

**Files written:**
- `reporting/freezing.py` — `ResultFreezeError`, `freeze_result_family`, `decode_manifest` (renamed from `_decode_manifest`, needed cross-module by `rendering.py`), `_decode_result_list`, `_profile_payload`, `_RESOLVED_METRIC_STATUSES`.
- `reporting/tables.py` — `render_table` (renamed from `_render_table`, public) + its private markdown/LaTeX helpers, self-contained (does its own `_records` extraction).
- `reporting/figures.py` — `render_figure` (renamed from `_render_figure`, public) + `_save_figure`, self-contained (its own `_records` extraction for "series" — accepted as a small intentional duplication of the 4-line `_records` helper against `tables.py`'s copy, rather than inventing a shared-helpers module for one tiny function; each renderer stays independently readable).
- `reporting/rendering.py` — NEW: `render_frozen_report`, the sole module importing both `tables.py` and `figures.py` (mirrors the resolver/validation one-directional pattern from `configuration/`).
- `reporting/execution.py` — `ResultFreezeStageHandler`, `ReportGenerationStageHandler`, unchanged in structure, imports updated to the new tree (`artifacts.provenance.git_revision`, `experiments.identity.IdentityBuilder`, `configuration.resolution.ResolvedProjectConfiguration`).
- `artifacts/querying.py` — NEW: `DuckDbAuditService` migrated verbatim from `infrastructure/querying/audit_service.py`. **Disposition resolved**: confirmed NOT dead — `interfaces/cli/app.py:156` calls `application.audit_svc.execute_query(sql)` for the CLI's ad-hoc query command, and `composition/root.py` constructs it. Placed in `artifacts/` (queries committed Parquet artifacts) rather than a dedicated `querying/` package, since it's a single small class with no siblings.

**Corrected deviation — `artifacts/paths.py` descoped**: TARGET_ARCHITECTURE.md (written before any stage handler was actually migrated) planned a `artifacts/paths.py` to centralize the `f"runs/{run_id.value}/{job_id.value}"` relative-path convention duplicated ~19x across `application/*.py` stage handlers, citing it as a CURRENT_ARCHITECTURE.md-flagged debloat target. Having now migrated every one of those ~19 call sites by hand across Phases 3–8, the duplication is a single self-evident one-line format string over exactly two typed inputs (`RunId`, `JobId`) with no business logic and no drift risk (unlike the 3 seed-derivation-formula duplicates fixed in Phase 4/6, which encoded actual computation that could silently diverge). Wrapping it in a function would be an abstraction over a trivial literal, which the task's own anti-gratuitous-abstraction principle argues against, and an unused `artifacts/paths.py` module would itself be exactly the kind of orphaned dumping-ground file section 11 asks to eliminate. **Decision: not created.** `artifacts/provenance.py` (real logic: git subprocess call, from Phase 1) remains the one genuine provenance-centralization win from that TARGET_ARCHITECTURE.md line item.

Old `application/{reporting,reporting_stages}.py`, `infrastructure/querying/audit_service.py` still fully intact and unreferenced by new code — nothing deleted yet (deletion is Phase 9).

## Phase 9 — Bootstrap + CLI + deletion of old packages: COMPLETE
Read `composition/root.py` (202 lines), `interfaces/cli/app.py` (166 lines), `interfaces/cli/formatters.py` (41 lines) in full. This phase also surfaced and closed a real gap left open since Phase 1.

**Gap discovered and closed**: Phase 1 only built `artifacts/models.py` + `artifacts/provenance.py`; `infrastructure/artifacts/{atomic_commit,manifest_codec,model_store}.py` (416 lines, the actual `AtomicArtifactRepository` implementation) were never migrated in any of Phases 1–8, and `bootstrap.py` cannot wire a repository without a concrete implementation. Closed now:
- `artifacts/serialization.py` — NEW: `encode_manifest`/`decode_manifest` (msgspec strict codec, from `manifest_codec.py`) + `save_model_safetensors`/`load_model_safetensors` (from `model_store.py`). **Corrected an aspirational MIGRATION_MAP.md note**: that note called this "the ONLY sanctioned SafeTensors call site, fixing a bypass in learning_stages.py" — verified against the actual pre-refactor `application/learning_stages.py` (which calls `safetensors.torch.load`/`save` directly, not `model_store.py`'s functions) and confirmed `save_model_safetensors`/`load_model_safetensors` had **zero callers anywhere in the pre-refactor codebase**, old or new — there was no bypass to fix, `model_store.py`'s public API was itself already dead code. `learning/{training,scoring}.py` and `analysis/resources.py`'s existing direct `safetensors.torch` calls (already migrated, Phases 5 & 7) are left untouched — that is the actual, correct, already-preserved behavior.
- `artifacts/repository.py` — NEW: `AtomicArtifactRepository` (from `atomic_commit.py`), importing the codec from `artifacts/serialization.py`.

**Additional disposition audit across all of `infrastructure/`** (verifying nothing besides the artifacts gap was missed before deleting):
- `infrastructure/learning/pytorch_adapter.py` (650 lines) — confirmed this is the literal, unmodified source Phase 5 already split across `learning/{autoencoder,federated,personalization,scoring}.py`; not a gap.
- `infrastructure/tables/{calibration_subsampling,polars_engine}.py` — confirmed fully superseded by Phase 6's `thresholding/calibration.py` and `evaluation/{operating_points,predictive_metrics}.py`; not a gap.
- `infrastructure/runtime/logging.py` (`configure_structured_logging`/`get_logger`, structlog) — genuine gap: not called by any application code even pre-refactor (only by its own unit test), but real and test-covered. Migrated verbatim to **`pipeline/logging.py`** (a cross-cutting primitive not owned by any single feature, consistent with `pipeline/`'s "shared primitives owned by every feature package" charter).
- `infrastructure/federation/flower_adapter.py` (`DatpFedAvgStrategy`) — confirmed genuinely dead: zero references from `composition/root.py` or any production code (the real federated-training path is `learning/federated.py`'s hand-rolled FedAvg/FedProx loop, which never touches `flwr`); only reachable from its own dedicated unit test. **Deleted, not migrated** (matches TARGET_ARCHITECTURE.md deviation #10, already anticipated). The now-orphaned `flwr==1.32.1` dependency removed from `pyproject.toml` (this was explicitly planned in MIGRATION_MAP.md row 109, not scope creep). `uv.lock` is now out of sync with `pyproject.toml` — needs `uv lock`/`uv sync` during the final-validation environment setup, not run now.
- `infrastructure/querying/audit_service.py::DuckDbAuditService` — disposition already resolved in Phase 8 (`artifacts/querying.py`; confirmed live via `interfaces/cli/app.py:156`).

**Files written:**
- `bootstrap.py` — `ConfigOnlyApplication`, `DatpApplication`, `build_config_only_application()`, `build_application()`, `_build_estimator_registry`, `_build_adapter_registry`, `_CommonConfigUseCases`/`_build_common_config_use_cases`, wiring all 11 pipeline stage handlers across every feature package. Unchanged in structure from `composition/root.py`; only import paths updated.
- `cli.py` — Typer app + all 8 commands, merging `interfaces/cli/{app,formatters}.py` into the single top-level file section 6's baseline calls for (no `cli/` subpackage — the two formatter functions are small enough that splitting them out would be a needless file for no reuse, matching TARGET_ARCHITECTURE.md's explicit note on this).
- `pyproject.toml` — `[project.scripts]` entry point updated to `datp_core.cli:main`; the CLI-B008-ignore per-file-ignore path updated to `src/datp_core/cli.py`; `flwr` dependency removed.
- `importlinter.ini` — **fully rewritten**. The old contracts encoded the horizontal-layer model (`domain`/`application`/`infrastructure`/`interfaces`/`composition`/`planning`/`config`), which no longer exists. Also corrected a real inaccuracy in my own pre-implementation TARGET_ARCHITECTURE.md dependency table: that table claimed `configuration` is "forbidden from importing" every feature package, but in reality `configuration/resolution.py::ResolvedProjectConfiguration` legitimately holds typed registries of every feature's resolved records and therefore correctly imports every feature's `models.py` (pure data) — the real, non-circular invariant is narrower and file-level: no feature's `models.py` may import `configuration.resolution`/`configuration.project` back (verified by grep across the actual, now-complete import graph — no cycle exists). New contracts: (1) `pipeline.{identifiers,values,fingerprints}` are the true bottom layer and import nothing upward, (2) every feature's `models.py` is pure data and doesn't import resolved configuration or execution, (3) only `cli.py` (and tests) import `bootstrap.py`, (4) `analysis` doesn't import `reporting` (one-directional), (5) `datasets`/`learning`/`thresholding`/`evaluation` don't import `analysis`/`reporting`, (6) `flwr` is forbidden anywhere (dead dependency removed).

**Deletion**: verified zero remaining `from datp_core.{application,composition,config,domain,infrastructure,interfaces,planning}` imports anywhere in the new tree (`pipeline`, `artifacts`, `configuration`, `experiments`, `datasets`, `learning`, `thresholding`, `evaluation`, `analysis`, `reporting`, `bootstrap.py`, `cli.py`) via grep before deleting anything. Then `git rm -r` all 7 old top-level packages (87 files) plus their stale `__pycache__` directories (untracked, removed directly). All 83 files in the new `src/datp_core` tree still parse (`ast.parse` sanity check — not a substitute for Pyright, which is deferred to the validation phase per the task's mandated order).

**Not yet updated** (deliberately deferred to the test-migration phase next, since these paths are still correct against the not-yet-migrated old test tree): `.github/workflows/{tests,sonarqube}.yml`'s GPU-test-deselect paths reference `tests/unit/application/test_{model_training,score_generation}_stage_handler.py` — these files still exist at their old location until the test tree is migrated; updating the CI deselect paths is part of that next phase, not this one.

Also noted as a pre-existing uncommitted change (not made by me in this turn, presumably from earlier in this same session before the context-compaction boundary): `noxfile.py` already had its `tests_parallel` session merged into `tests` (both now run under `pytest-xdist`) — left as-is, it's a sensible simplification and not something to revert.

## Next: Test-tree migration (mirrors production 1:1, section 15)
73 old test files under `tests/{unit/{application,config,domain,infrastructure,interfaces,planning},integration/{artifacts,datasets},scientific/{catalogue,drift,thresholding},conformance}` need to migrate to `tests/{configuration,experiments,datasets,learning,thresholding,evaluation,analysis,reporting,artifacts,pipeline,integration}/`, matching each test to its new production module. The 6 conformance tests hard-coding the old architecture (`test_project_structure.py`, `test_application_port_dependency.py`, `test_configuration_authority_boundary.py`, `test_experiment_catalogue_field_disposition.py`, `test_executable_invariants.py`, `test_no_hidden_defaults.py`) need rewriting per MIGRATION_MAP.md's existing rewrite plan (lines 130–136). `tests/unit/infrastructure/federation/test_strategy_uses_resolved_participation.py` gets deleted outright (tests only the now-deleted dead `flower_adapter.py`, no new-architecture equivalent). After the test tree is done: update the two CI workflow files' GPU-deselect paths, then Drift Audit B, navigation + pre-validation source audits, IMPLEMENTATION FREEZE, Pyright/Pylance/full test-suite validation loop, Drift Audit C, final report.

## Starting Git state
- Branch: main, up to date with origin/main.
- Working tree: clean at task start (no uncommitted changes to preserve).
- No prior `.tmp/file_tree_refactor/` state existed; this is a fresh start.

## Repo scale (measured)
- `src/datp_core`: 88 `.py` files, ~17,926 LOC.
- `tests/`: 74 `.py` files, under `tests/{unit/{application,config,domain,infrastructure,interfaces,planning},integration/{artifacts,datasets},scientific/{catalogue,drift,thresholding},conformance}`.
- `docs/roadmap/`: 9 files, ~8,962 lines — this is the authoritative roadmap directory (no separate `roadmap/` dir exists at repo root).
- Old top-level packages present: `application/`, `composition/`, `config/` (+ `config/models/`), `domain/`, `infrastructure/` (+ `artifacts/`, `datasets/`, `federation/`, `learning/`, `querying/`, `runtime/`, `tables/`, `thresholding/` subpackages), `interfaces/` (+ `interfaces/cli/`), `planning/`.
- Pyright config: inline in `pyproject.toml` `[tool.pyright]`, `typeCheckingMode = "standard"` (not strict) — must be preserved, not weakened or silently strengthened.
- `.vscode/settings.json`: only contains `sonarlint.connectedMode.project` + `explorer.excludeGitIgnore`; no `python.analysis.*` keys yet — Pylance is not currently explicitly aligned to Pyright config. Needs alignment in Phase before final validation.
- Test config: `pytest` `testpaths = ["tests"]`, `pythonpath = ["src", "."]`. Ruff: line-length 120, py312 target, select `["E","F","I","N","UP","B","ICN"]`.

## Active package migration
None yet — still in reconnaissance.

## Completed package migrations
None.

## Remaining old packages
All seven: application/, composition/, config/, domain/, infrastructure/, interfaces/, planning/.

## Files currently being edited
None (no production edits yet).

## Background research in flight
- Agent extracting scientific contracts from docs/roadmap/*.md (drift audit A input) — not yet returned.
- Agent auditing application/, domain/, planning/, interfaces/ (dependency graph, oversized modules, dupes, cycles, dead code) — not yet returned.
- Agent auditing composition/, config/, infrastructure/ (same audit scope) — not yet returned.

## Unresolved dependency violations
Not yet enumerated — pending audit agents.

## Unresolved test migrations
Not yet enumerated — pending audit + roadmap results.

## Pending final validation
Not started. Implementation freeze not reached.

## Exact next action
Wait for the three background audit agents to return; synthesize their output into `CURRENT_ARCHITECTURE.md`, then perform Drift Audit A, then write `TARGET_ARCHITECTURE.md` and `MIGRATION_MAP.md` before any file is moved.
